# Cross-account learning pass, 25 Sept 2026, 21:33 UTC (parent worker LEARN, spawned by parent 7d)

Direction: this account (R6/R7/V6/B2/B3/GOLD/GOLD2, parent 7d) reading what the owner account (LANE ZX, LANE ZX2,
parent session_01FXDfYR3CvGk7tcid1Aav1n) pushed since 17:18 UTC that we do not do or do worse. Read: `git log
origin/main --since=17:18` (57 commits after `git fetch`), ROOM.md 18:00-20:55 UTC lines naming ZX/ZX2/owner
account, STATUS.md's "Parent handoff (owner account..." section 17:05-21:20 UTC entries and "LANE ZX2 handoff",
`ciphers/clair349-este-guise-1556/NOTES.md` (ZX-TR349C/D/E sections). `date -u` read 21:30:34 (start) and 21:33
(this line). Read `LEARN-2026-09-25-1718.md` and `LEARN-2026-09-25-2118.md` first per the brief; neither repeats
below. Not applied; the parent applies brief/tool-only diffs.

## 1. Practices/lessons they hit that are not yet in a shared file

| # | Practice / lesson | Evidence (file:line) | Portable? | Diff |
|---|---|---|---|---|
| 1 | **A hand-stitched crop wider than `tools/iiif_lines.py`'s own 2500 px default demonstrably degrades blind-transcription agreement, not just risks truncation or a wall-clock overrun.** ZX-TR349C re-stitched `--max-width 2400` segments back into single 3895 px line images (following an earlier worker's convention) and two Sonnet blind-pass pairs agreed only 39.3% and 42.0% against a 60% gate. ZX-TR349D re-cut the same leaf as the tool's own two native `_s1`/`_s2` segments (each <2500 px, kept apart, not stitched) and pooled agreement jumped to 70.7% on the same 33 lines, same leaf, same solver tier initially (Opus both times, but the crop-width variable was isolated and named as the fix in the worker's own report). | `ciphers/clair349-este-guise-1556/NOTES.md:1119-1134` (the diagnosis, citing CLAUDE.md's own "under the 2500 px reading limit" convention as the rule that was violated) and `:1155-1167` (the fix, `--max-width 2400` unstitched, ticks marking the overlap boundary instead); ROOM.md 2026-09-25 20:07 (LANE ZX check-in 6, "70.7 pct pooled; was 39.3/42.0"). | Yes — general to every transcription job that needs a wide crop (a gloss margin, a long line): the fix is cheap (keep the tool's own segments apart, tell the pass where the boundary tick is) and the tool already defaults to the safe width; the failure mode is a worker re-stitching past it by hand. | `tools/iiif_lines.py` docstring, after the existing "under the 2500 px reading limit" line: add "Re-stitching two segments into one wider image to give a pass full line context measurably lowers blind-transcription agreement (clair349, 25 Sept 2026: 39-42% stitched at 3895 px vs 70.7% on the same two segments kept apart, boundary marked with corner ticks) — pass segments separately and tell the worker where the tick-marked boundary is, never re-stitch past the tool's own `--max-width`." Also worth one line in CLAUDE.md's Access playbook item 8 tool table (iiif_lines.py row) pointing at this. |
| 2 | **When a language judge FAILs a candidate reading close to the gate, score a known-genuine period text specific to the same document (a marginal or interlinear gloss, if one exists) through the same judge as an extra calibration point, not just the corpus's own held-out sample.** ZX-DEC349's fr16 judge FAILed clair349's decode (-1.109 vs real_p05 -0.882), which alone reads as a clean negative — but scoring the leaf's own contemporary interlinear gloss (independently known-genuine period French, not the candidate) through the identical judge gave -1.545, *worse* than the candidate and only marginally above the shuffled-null controls (-1.62 to -1.65). That places the candidate's score much closer to genuine prose than to noise despite missing the p05 gate, which changes how the FAIL should be read (a corpus/threshold problem for this specific short, gappy letter, not necessarily a wrong key) — distinct from CLAUDE.md rule 3's era-corpus-matching lessons (pt18, fr18), which calibrate the corpus in general; this calibrates against a real text from the *same leaf*. | ROOM.md 2026-09-25 20:55 (LANE ZX check-in 7: "the period gloss itself scores -1.545"); `ciphers/clair349-este-guise-1556/NOTES.md` ZX-DEC349 section. | Yes, but conditional — only applies when a document carries its own independent period gloss/annotation to score (rare outside this kind of glossed diplomatic letter); worth naming as an available technique, not a universal step. | CLAUDE.md rule 3, after the pt18/es17c lessons: add one line — "if the target document itself carries an independent period gloss or annotation, score that text through the same judge alongside the candidate reading and the shuffled-null controls; a gloss score near the shuffled controls (not near real_p05) means a judge FAIL close to the gate reflects the corpus/threshold at this document's length and register, not necessarily the key (clair349, 25 Sept 2026)." |

## 2. What we have that they lack

Checked STATUS.md's owner-account handoff section and ZX/ZX2 ROOM lines against this account's own tools
(`tools/family_run.py`, NEAR.md, `intake_gate_check.py`) and briefs: no gap found — ZX/ZX2 use all three already
(ZX2's `cheap_test_done`/intake-gate lines, NEAR.md untouched appropriately since no reading crossed a gate this
window). Same result as LEARN-2118's reverse check: the shared files carry the mutual practices already.

## 3. Overlaps / conflicts

None. Target sets stay disjoint this window: ZX/ZX2 hold clair349-este-guise-1556, clair1161-avis-flandre-1688,
clairambault296-paget-1713, ormond-arran-1678, hellen-frederick-1752, destaing-gerard-1779, la-garde-1577,
decode-4450-bnf-fr20506-1525; this account's lanes (R6/R7/V6/B2/B3/GOLD/GOLD2) hold Salviati, Mercy, Berthier,
Costa Cabral, Xiquena, McCormick, Pollaky, Rubin, Bullet, Moustier, Kaliningrad, Koehler. Zero shared targets
found in git log or ROOM.md for the window.

count: 2 portable items (1 clear tool-doc diff with strong numeric evidence, 1 conditional-technique diff),
0 reverse-direction gaps, 0 conflicts.
