# Retrospective r, 26 Sept 2026 (Sonnet, cap $10 / 40 min, parent 7g)

Window: LEDGER.md rows after "LANE ARM ARM-REC: recovery + crib hunt" (line 981) through the last row at this
session's start ("RETRO-APPLY-Q", line 1008) -- 28 rows, about USD 130 of usage on both accounts (LANE B8's close
and its last 8 workers; LANE V9's QA8 and NA172-3; LANE ARM's round 2 (ARM-DESIGN, ARM-A2, ARM-REC2) and round 3
(ARM-IMG, ARM-TR); LANE AX2's last four workers and its close; retrospective q (RETRO-2026-09-26e.md) and
RETRO-APPLY-Q; LANE B9's first three (bMALG, bMAT1H, bHCP3); SCOUT-OWN; LANE NX's first three (NX-MOR, NX-E318,
NX-WVO1109)). `date -u` read 09:04:57 UTC (start). `python3 tools/room.py --start`: key_probe.py --sync 16 set,
key_livecheck.py 8 present/5 working. ROOM.md digest from 07:30 (31 of 2776 lines). QA/2026-09-26-0735.md (V9-QA8,
1 flag, window 05:44-07:35) read in full. UPDATES.md tail read (rows since 06:51: LEARN-6, RETRO-APPLY-Q's four
rows, the 09:03 NARA host-table row). NEAR.md read directly, all rows, not just the digest. STATUS.md's LANE B8
(closed, line 351), LANE AX2 (closed, line 109) and LANE V9 (live, line 199) handoffs read; the parent's own 07:40,
08:15 and 09:05 check-in paragraphs (STATUS.md tail) read for the takeover/self-correction detail and the ARM-IMG
host-table fix, both already landed before this session started.

## 1. Three incidents this window (hessen-1824, matignon-mayenne-1586, AX2-4612S) show the same shape -- a second
   or third attempt at an unchanged approach, varying only one knob, that fails again without moving toward the
   gate -- and nothing in CLAUDE.md names the result shape that makes a further attempt a non-test before it runs

**Family-level (hessen-1824, Q1).** bHCP2 (26 Sept 07:00) found running_key CONTROL BELOW GATE at 0.372 (gate 0.6)
on a de20 corpus with an era gap (1880-1940 vs 1824). The re-brief changed the one knob it bet would fix this (an
era-matched de19 corpus, built for the purpose) and widened the beam (6000 to 15000): bHCP3 (08:44) came back at
0.331 -- *lower*, not higher, and byte-identical between the two beam settings. Both knobs moved and neither helped;
the row now correctly reads "running_key is untestable at N=164 with this solver on either corpus" (NEAR.md), but
that conclusion took a second full worker (about USD 2.82) to reach, and nothing said in advance what result from
the second run would have counted as "don't bother with a third."

**NEAR-row-level (matignon-mayenne-1586, Q2).** The same alignment approach (blind hand/scripted alignment of
f.78v against f.79r, judged against a pre-registered or ad hoc known-answer gate) was re-run three times this
window at increasing sophistication -- (1f) stopped at 1.8x cap before its control; (1g) fresh-seed control 11/20
vs gate 16, FAIL; (1h) a gate pre-registered *before* the run (n_high>=8, >=90% right), fresh seed 8620, FAIL at
7/8 -- each fix (correcting the transcription gap, then pre-registering the gate) was a genuine, separate finding,
but the row's own high-confidence answers were 23/25 right pooled across all three seeds and the gate still failed
every time. LANE B9 correctly stopped dispatching a same-shaped fourth pass ("Matignon stopped in the breadth
lane... about USD 20 over B8/B9", LEDGER.md), but this was a lane's own judgement call at the point of stopping,
not something rule 5's near-solve amendment tells the *next* lane to check before re-opening the identical step.

**Tool-level (AX2-4612S/S2/S3, Q3).** Three iterations of `key_repair.py` on the syllable-code hypothesis (H-S):
S (26 Sept, LEDGER.md) null control 100/110 false; S2 (fixed the NULL-seeking bug) null 53/110 false, known-answer
5/8, bigram 3/6 -- still CONTROL BELOW GATE; S3 (paired objective) null 29 false but known-answer *fell* to 3.7/8
and bigram to 0/2 -- CONTROL BELOW GATE again, closed by AX2's own words: "not to be re-briefed a 4th time without
a different instrument." Each iteration diagnosed and fixed a real bias in the tool, and each fix moved a different
number in a different direction (never the whole vector above gate at once) -- the AX2 orchestrator's own lesson
line already says this in different words ("each fix moved the bias rather than removing it").

All three are the same test: *a second attempt whose own numbers do not move together toward the gate, after
changing the one thing the re-brief bet on, is evidence the approach itself is the limit, not the setting* -- and
in all three cases here a worker or lane had to spend a further $2-5 finding that out by running it, because no
rule named the shape in advance.

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ (rule 3, appended after the "per-unit (leaf, page) recovery signal merged into one shared key" paragraph, 26
    Sept 2026, RETRO-2026-09-26d)
+   A second (or third) attempt at an unchanged approach -- the same family, the same alignment method, the same
+   scoring tool -- that changes only the one knob the re-brief bet on and still fails its own gate, without every
+   number moving together toward it, is evidence the approach is the limit, not the setting; it is logged
+   "untestable [by this method/tool] at this N/length" and the next attempt needs a genuinely different
+   instrument or new material, not a further tuning of the same knob. Three shapes this applies to, all from the
+   same 26 Sept 2026 window: (a) a family control that moves the *wrong* way after the re-brief's own bet (hessen-
+   1824's running_key: 0.372 on an era-mismatched corpus, then 0.331 -- lower -- on a purpose-built era-matched
+   corpus at 2.5x the beam; bHCP2/bHCP3); (b) a NEAR row's named next step, re-run three times at increasing
+   sophistication (a transcription fix, then a pre-registered gate) with every fix genuinely correcting something
+   real, yet the gate fails all three times (matignon-mayenne-1586's f.78v/f.79r alignment: 1f/1g/1h, 23/25 high-
+   confidence answers right pooled but the registered gate FAILs every seed) -- the row's "what would settle it"
+   cell then names new material (more ciphertext, a different leaf) or an owner-side step, not a fourth pass at
+   the same two leaves, without weakening rule 5 (the target stays `partial`, never `closed-negative`, either
+   way); (c) a tool whose own known-answer control fails twice on the same hypothesis, even after a real fix
+   between attempts (AX2-4612S/S2/S3's `key_repair.py`: null-false 100->53->29, but known-answer recovery 0.000-
+   >0.625->0.458 and bigram 3/6->0/2, never all three numbers above gate together) -- retired for that hypothesis,
+   logged in HYPOTHESES.md as "untested-by-this-tool" (not refuted), and not re-briefed against the same
+   hypothesis without a different instrument.
```

Motivating: NEAR.md hessen-1824 and matignon-mayenne-1586 rows (both current as of this session); LEDGER.md rows
bHCP2, bHCP3, bMAT1F, bMAT1G, bMAT1H (LANE B8/B9); LEDGER.md AX2-4612S/S2/S3 rows and the LANE AX2 orchestrator's
own close-row lesson ("each fix moved the bias rather than removing it").

## 2. A lane's "please change X" ask to another lane or the parent does not name what it read the file at, so a
   stale local copy produces a redundant ask that the reader has to notice and correct by hand

LANE V9's 08:53 ROOM.md line reminded the parent to change status.json's lodewijk grade text from "lowered it to
N2" to the withdrawal V9-NA172-3 had already classed (AUDIT.md A4) -- but the parent had already made that exact
change and pushed it at 08:12 (`grep "withdrew" status.json`, confirmed live). The parent's 09:03 line had to spend
a sentence correcting it ("your 08:53 read was stale"). V9's ask was reasonable given what it had on disk at 08:53
(its own last pull), but nothing in the ask itself said *when* V9 last read status.json, so the parent (or anyone
else reading the room) had no way to tell a live ask from a stale one without independently checking the file --
exactly the diagnostic step the parent then had to do by hand. This is a narrower, cheaper version of the same
principle behind `tools/orphan_check.py` and the self-ledger `get_session`-last rule: a claim about shared state is
only as good as when it was read, and that "when" should travel with the claim rather than be reconstructed by the
reader.

```diff
--- a/.claude/briefs/parent.md
+++ b/.claude/briefs/parent.md
@@ (Handing over / check-in duties, after the "Self-ledger cost" paragraph)
+**Cite what you read, not just what to change (26 Sept 2026, RETRO-2026-09-26f).** A ROOM.md line asking another
+lane or the parent to change a shared file (status.json, STATUS.md, a target's key.tsv) names the exact text or
+line it read and, where practical, the commit it was reading at (`git log -1 --format=%h -- <path>`) --
+`grep <the old text>` is enough when a commit hash is not to hand. A reader can then tell in one command whether
+the ask is still live or already stale, instead of re-deriving that by hand. LANE V9's 08:53 line asked the parent
+to change status.json's lodewijk grade text to match AUDIT.md A4's withdrawal; the parent had already made that
+exact change and pushed it at 08:12, and had to spend a correction line at 09:03 finding this out for itself.
```

```diff
--- a/.claude/briefs/README.md
+++ b/.claude/briefs/README.md
@@ (common tail, after the self-ledger-cost-last bullet)
+- **Cite what you read.** Any "please change X" line to another session names the exact old text (or a commit
+  hash) it read, so a reader can tell a live ask from one the target already overtook without re-deriving it by
+  hand (parent.md's own "Cite what you read" paragraph, 26 Sept 2026).
```

Motivating: ROOM.md 08:53 (LANE V9 orchestrator, "please change") and 09:03 (parent 7g, "your 08:53 read was
stale"); STATUS.md 09:05 check-in paragraph ("V9's ask acted on... your 08:53 read was stale").

## 3. A scout/check-solved candidate is not checked against whether a solver repository's own planning text (not
   just its transcription progress) already names the same target as its next step, which is a different and
   cheaper signal than the "already substantially read" check `tools/solver_repo_diff.py` runs today

NX-E318 (LANE NX, 26 Sept 08:59) found esp318-sicilia-1503 (item 94) genuinely `open` by the six-source check-solved
sweep -- no decipherment anywhere -- but flagged "total duplicate-effort overlap with dbourdeau/cyphersolver
esp318/: his own unread, his stated Next step 1 is this exact letter with the same Bergenroth Gran-cifra
hypothesis, he already holds full-page images of all 4 leaves." `tools/solver_repo_diff.py` (queue mode) already
checks whether a Bourdeau target folder's `profile.json`/NOTES.md/transcription *names* a QUEUE.md candidate's
DECODE id or shelfmark, surfacing `fraction_read` -- that is a "how far along is his own attempt" signal. It does
not read the solver repository's own stated *plans* (a README's or profile's "next step" line, distinct from
progress already made) for a match against a QUEUE.md row before that row is scored or promoted, which is exactly
what NX-E318 had to notice by eye, after the check-solved and intake-gate passes had already run and the target
had reached a breadth/campaign candidacy.

```diff
--- a/.claude/briefs/check-solved.md
+++ b/.claude/briefs/check-solved.md
@@ (six-source sweep, after the two solver-repository checks)
+**Stated next step, not just fraction read (26 Sept 2026, RETRO-2026-09-26f).** Beyond checking whether a solver
+repository has already substantially read the target (`tools/solver_repo_diff.py`'s `fraction_read`), grep that
+repository's own planning text for this target -- a README, a profile note, or a "next"/"todo"/"planned" line
+naming the same sender, date or shelfmark -- before the target is scored or promoted. A target that is a solver
+repository's own explicitly stated next step, with source material already in that repository's hands, is a
+duplicate-effort risk: name it as such in the verdict sentence and weigh it against the queue's other candidates
+(Pipeline 3's expected-value selection rule) rather than promoting it on schedule. NX-E318 (esp318-sicilia-1503)
+found this only after check-solved and the intake gate had both already passed the target through, by reading
+Bourdeau's own repository text, not a tool.
```

Motivating: LEDGER.md/ROOM.md 08:59 (NX-E318, "for LANE NX -- esp318-sicilia-1503... total duplicate-effort
overlap with dbourdeau/cyphersolver esp318/... his stated Next step 1 is this exact letter"); `tools/
solver_repo_diff.py`'s own docstring (queue mode: `fraction_read` from `profile.json`, no planning-text check).

## 4. A ROOM.md line stating a clock time in its own text can drift from the line's auto-generated timestamp, and
   nothing catches the mismatch before a second line has to correct it

Parent 7g's own take-over line (ROOM.md, auto-stamped `2026-09-26 07:37`) reads "took over from 7f at 07:41 UTC 26
Sept 2026 (clock read)" -- the typed time in the signal text (07:41) does not match the line's own auto-generated
timestamp (07:37), four minutes apart, and the parent's own STATUS.md 07:40 paragraph had to spend a clause
correcting it ("from 7f at the 07:37 clock read (the ROOM take-over line wrote 07:41 by estimate, corrected by a
second line)"). This is not the cross-session container-clock skew CLAUDE.md rule 6 already documents (two
different sessions' machine-generated stamps disagreeing) -- it is the *same* session, on its *own* line, typing a
time in the free-text signal that need not have been typed at all, since `tools/room.py` already stamps every line
from `time.gmtime()` when it appends. `warnings_for()` already catches several shapes of a line saying something
its own structure contradicts (a dollar figure, a foreign session id, a missing control number); a typed clock
time that disagrees with the line's own stamp is the same class of self-contradiction and is just as mechanically
checkable.

```diff
--- a/tools/room.py
+++ b/tools/room.py
@@ (near the top, after ROLE_SESSION_RE)
+TYPED_TIME_RE = re.compile(r"\bat (\d{1,2}):(\d{2})\s*UTC\b")
+
+
@@ def warnings_for(role, signal, own_id=_UNSET):
     """Warnings printed before a line is appended ...
+    - a typed clock time ('at HH:MM UTC') in the signal text that disagrees with the line's own auto-generated
+      stamp by more than a couple of minutes: the stamp is machine-generated from time.gmtime() at append time,
+      so a typed time is either redundant (matches) or wrong (an estimate written from memory) -- 26 Sept 2026,
+      parent 7g's own take-over line wrote 'at 07:41 UTC' against its own 07:37 stamp and needed a second line to
+      correct it."""
     if own_id is _UNSET:
         own_id = _own_session_id()
     w = []
     sig = signal.strip()
+    m = TYPED_TIME_RE.search(sig)
+    if m:
+        typed_minutes = int(m.group(1)) * 60 + int(m.group(2))
+        stamp_minutes = int(time.strftime("%H", time.gmtime())) * 60 + int(time.strftime("%M", time.gmtime()))
+        if abs(typed_minutes - stamp_minutes) > 2:
+            w.append(f"WARNING: signal text says 'at {m.group(1)}:{m.group(2)} UTC' but this line's own stamp is "
+                      f"{time.strftime('%H:%M', time.gmtime())} UTC -- the stamp is authoritative, drop the typed "
+                      f"time or use '(clock read)' with no digits (26 Sept 2026, parent 7g takeover line)")
     if sig.lower().startswith("done:") and ...
```

New test case for `tools/tests/test_room.py` (or wherever `warnings_for` is tested): assert
`warnings_for("parent 7g", "handoff: took over from 7f at 07:41 UTC 26 Sept 2026 (clock read)")` (with the real
clock frozen or mocked to 07:37) contains the new WARNING string, and a line with no typed time, or one within two
minutes of the stamp, produces none.

Motivating: ROOM.md 07:37 (parent 7g take-over line, typed "07:41"); STATUS.md 07:40 paragraph ("the ROOM take-over
line wrote 07:41 by estimate, corrected by a second line"); `tools/room.py`'s existing `warnings_for()` (same
function, same style of self-contradiction check, lines 290-324).

## Answered without a diff (the brief's other question)

**(Q6) ARM-IMG found a keyless NARA image route the host table said did not exist -- how does a worker's route
discovery reach the host table without waiting for a parent?** Already working as designed, not a live gap: the
mechanism this repository already has (a `flag:`/`done:` ROOM.md line naming the host-table gap, caught by the
parent's own regular ROOM.md digest at its next check-in) worked end to end this window. ARM-IMG's done line
(08:19 UTC) said in its own text: "New Access-playbook route not yet in CLAUDE.mds table... flagging for the lane
orchestrator/parent to add." The parent's check-in 2 (09:01, per STATUS.md and UPDATES.md) read it and added the
NARA row to CLAUDE.md's host table within the same check-in cycle (UPDATES.md 09:03; ROOM.md 09:03, "the keyless
NARA IIIF route is now in the CLAUDE.md host table"), 44 minutes end to end and inside the parent's normal ~50-70
minute check-in cadence this window. Nothing here needed a worker to edit CLAUDE.md directly (workers correctly
cannot) or a new tool: the existing flag-then-check-in loop is the mechanism, and it closed the gap in one cycle.
If a future retrospective finds a host-table flag sitting unread across two or more parent check-ins, that would
be the point to mechanize it (a `tools/host_table_check.py` grepping ROOM.md for unresolved "not yet in CLAUDE.md"
flags); no evidence of that yet.

## Near solves (review, per brief)

All 11 active-table rows read directly from NEAR.md, not only the digest.

**matignon-mayenne-1586**: moving, not stale (1h landed 08:23, row touched 08:50) -- but see item 1 above: the
row's own next step now correctly reads "reopen only with those crops or new material," i.e. the ladder for the
current instrument (blind/scripted alignment of these two leaves) is spent, matching the general rule this
retrospective proposes.

**malsburg-hessen-1636**: moving. bMALG (08:33, glyph conventions) landed this window; STATUS.md's 09:05 line
records bMAL28/bMAL23 transcribing the 507/505 pool as live. Not stale.

**hessen-1824**: moving and now correctly filed (retro q/e's fix held: this session's own `tools/near_check.py`
placement, read directly, shows the row in the active table, not under Closed). bHCP3 (08:44) closed the
running_key ladder as untestable at N=164 -- see item 1.

**antt-msliv0638-brochado-1712 letter 134**: last touched 26 Sept 06:42 (AX2-BRO4/5, prior window); not touched
this window's ledger rows, not stale under the 48-hour rule. Named next step (a verifier read as a judge-cannot-
decide case) still not dispatched -- not yet past the same fifth-mention threshold as fr2933 below (this is its
second retrospective mention, not its sixth).

**fr2933-salviati-1525**: **due for dispatch, not a further note.** RETRO-2026-09-26e (the immediately preceding
retrospective) stated: "fr2933's diagnostic-first next step... is now unmentioned in four consecutive
retrospectives' worth of window (retro m, n, o, p, and this one) without a worker dispatched -- past the point
where a fifth mention is useful; if the next retrospective finds it still untouched, that is a dispatch, not a
sixth note." No LEDGER.md row or ROOM.md line in this window's 28 rows touches fr2933 or Salviati. Per that
standing plan, this retrospective flags it for the parent to dispatch at its next check-in (the named step: extend
R8-DSN's own syllabary-control error sweep from 5-7% to 10-14%, Sonnet, about USD 2, already fully specified in the
row) rather than writing a further note.

**espagnol142-mercy-1648, koehler-1944, mccormick-1999, bullet-tuscany-1944, untersberg-code, blitz-ciphers**: none
touched this window's ledger rows; none stale under the 48-hour rule (koehler correctly PAUSED per its own standing
idle-hold; the others last moved 25 Sept 2026, within the window when read against this session's own `date -u`
of 26 Sept 2026 09:04:57 UTC).

## What did not need a change

Rules 1, 2, 6, 8, 9, 9a, and the Outreach gates: untouched by anything else in this window. Rule 3 held everywhere
else checked this window (bSIE2, bMALC, bMALDUP, ARM-A2, ARM-DESIGN all report target and control numbers
together). Rule 4 held (no new H-vs-H conflict this window; AX2-172/5801ADJ's existing conflict from the prior
window is unchanged and still logged with witnesses). Rule 5 held (hessen-1824 correctly `partial`, not
`closed-negative`, after its running_key ladder closed as untestable -- item 1's own point). Rule 7 held
(bMAT1G's `build_corrected.py --check` exit 0; ARM-TR's diff against `ciphertext.txt` reported as a match ratio,
not a silent repair). Rule 10 held everywhere checked (ARM-IMG, ARM-TR, NX-MOR, NX-E318, NX-WVO1109 all report
"found/not found" and search method, none claim novelty; V9-NA172-3's AUDIT.md A4 uses "withdrawn... none licensed"
language, not "first" or "unpublished"). Credentials: no mishandling found in this window's ROOM.md/NOTES.md diffs
(ARM-IMG's NARA route and NX-E318/NX-MOR's archive fetches are all keyless or already-documented-key routes).
Absolute dates: `date -u` read at this session's own start (09:04:57 UTC) and used throughout.

Retrospective r done: 4 proposals (item 1 covers three of the brief's seven questions as one rule-3 amendment),
file RETRO-2026-09-26f.md.
