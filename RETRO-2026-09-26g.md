# Retrospective s, 26 Sept 2026 (Sonnet, cap $10 / 40 min, parent 7g)

Window: LEDGER.md rows after "RETRO-APPLY-Q" (line 1008) through "SALV-DIAG" (line 1035, last row at this
session's start) -- 26 rows, about USD 212 of usage on both accounts: LANE B9's nine workers and close (five of
nine over cap on dense malsburg leaves; bMALH's pooled homophonic exclusion with a passing control), LANE ARM's
cycle 1 (ARM-TR, ARM-C1 Fable, ARM-POOL, ARM-S1, ARM-TR2, ARM-CONS1 Fable; no family beat its control), LANE NX's
rounds 1-2 on the owner account (NX-LAU/LAU2, NX-MOR/MOR2/MOR3, NX-E318, NX-WVO1109/WVO174, NX-3151N33/3151G),
retrospective r (RETRO-2026-09-26f.md), LEARN-7, RETRO-APPLY-R, SALV-DIAG. `date -u` read 10:43:25 UTC (start).
`python3 tools/room.py --start`: key_probe.py --sync 16 set; key_livecheck.py 8 present/4 working, last probe
10:39 UTC. ROOM.md digest from 09:00 (35 of 2840 lines, read in full, extending past 09:41 into ARM-REC3/NX-LAU3/
NX-MOR3/both lane closes and the two 09:42/10:43 parent check-ins, since those lines were the most recent facts
on disk at this session's own start). NEAR.md read directly in full, not only the digest. STATUS.md's LANE B9
handoff (line 366, closed 10:17) and LANE V9 handoff (line 225, live) read; LANE ARM's HYPOTHESES.md "Summary,
cycle 1" block referenced via ARM-CONS1's own done line (ROOM.md 09:54) rather than re-read in full (its content
is quoted in that line almost verbatim). UPDATES.md tail read (rows since 08:19: the NARA host-table row, the six
RETRO-APPLY-R rows) to confirm none of this window's five proposals below duplicate an already-applied fix.
QA/2026-09-26-0942.md referenced via its own ROOM.md summary lines (09:49/09:50/09:53) rather than re-opened,
since V9-QA9's three lines already state the two flags and the "otherwise PASS" verdict for all 39 items in full.

## 1. LANE B9 recalibrated dense-leaf transcription to USD 0.4/cipher line and USD 0.1/settled row after five of
   nine workers ran over cap -- nothing records that rate for the next brief to start from

bMAL23 (1.6x cap), bMAL24 (1.6x), bMAL28/bMAL28B (at cap, then 1.6x to finish), and bMALG (1.9x) all ran over a
brief that priced per pass (Usage item 6's own rule), because none of them knew the malsburg hand's actual
per-line cost before pricing: bMAL23's own row states it plainly ("1.6x its USD 10 cap: about USD 0.4 per cipher
line all in, twice the per-pass estimate from bMALDUP -- dense leaves run ~38 signs a line, not ~20"), and
bMAL28B's witness-settling ran "~USD 0.1 a row, three times the brief's estimate." The LANE B9 orchestrator's own
close-row lesson restates both numbers, and STATUS.md's handoff to LANE B10 repeats them a third time -- but all
three copies sit in prose (a ledger row, an orchestrator close line, a handoff paragraph) that a future brief
author has to search for, not a number a brief-writing tool or template surfaces automatically. This is the same
shape Usage item 6 already fixed for time-boxing ("state the unit count and the per-unit estimate in the brief
itself") but for the dollar rate itself: nothing says where a brief author should look it up before guessing a
round number again, the way bMALC's own per-leaf extent estimate (~9 lines, corrected to 32 by bMAL24) already
warned against for line counts.

```diff
--- a/.claude/briefs/README.md
+++ b/.claude/briefs/README.md
@@ (common tail, after "A lane cannot archive itself")
+- **Per-unit pricing precedents (26 Sept 2026, LANE B9).** Before pricing a visual-transcription or
+  witness-settling job (Usage item 6's per-unit box), check whether a comparable unit has a recorded rate here
+  rather than estimating a round number: dense mixed-hand cipher leaf, two blind passes + reconcile + per-leaf
+  gate -- about USD 0.4 a cipher line all in (malsburg-hessen-1636, LANE B9, bMAL23/24/28/28B/16, five of nine
+  workers over cap before this rate was known); settling one hand-flagged disagreement row from a crop or a
+  sibling witness -- about USD 0.1 a row (bMAL28B). A rate recorded here for one target's hand and script style
+  is a starting estimate for a visually similar leaf, not a promise for every hand -- state it in the brief as
+  "priced from <precedent>, confirm on the first unit" (Usage item 6's own 80%-of-cap/box self-stop rule still
+  applies if the first unit runs hotter). A lane orchestrator that recalibrates a rate this way adds the new
+  precedent here in the same close-out pass that writes its STATUS.md handoff, rather than leaving it for a
+  retrospective to notice a second time.
```

Motivating: LEDGER.md bMAL23/bMAL24/bMAL28/bMAL28B rows (26 Sept 2026); STATUS.md "LANE B9 handoff" ("Five of
nine over cap... dense malsburg leaves cost about USD 0.4 a cipher line all in and witness settling about USD 0.1
a row"); CLAUDE.md Usage item 6 (the existing per-unit box-and-cap sizing rule this pricing gap sits beside).

## 2. `tools/leaf_pool_gate.py` holds a whole leaf out of the pool on its M-share alone, even when the leaf
   decisively passes the same-system cosine test -- the per-token merge discipline rule 3 already states for
   cross-leaf key-building is not applied within a single leaf's own admission gate

Three malsburg leaves this window (f.28 M 0.232, f.24 M 0.27, f.16 M 0.36, all against the 0.15 cap) were HELD
by `leaf_pool_gate.py` even though every one of them passed the system gate decisively (cosine 0.68-0.83 against
a relabel p95 of 0.40-0.56) -- the tool's own `gate()` function requires `gate_quality AND gate_form AND
gate_system` for `admitted`, so a leaf that is unambiguously the same cipher system as the pool is excluded
wholesale because a third to a fifth of its individual signs were read at grade M, not because the leaf itself is
doubtful. This is the same shape CLAUDE.md rule 3 already names for merging *across* leaves (the Szembek
bSZL65/66/67 paragraph: "a code attested only on a unit whose own control tied or failed is held pending... not
merged as equally supported") but applied at the wrong grain: here the "unit" that should be held pending
corroboration is the individual M-graded *sign*, not the whole leaf that contains it, once the leaf has already
cleared the one gate (same-system cosine) that actually tests whether it belongs in the pool at all. Holding the
whole leaf means bMAL28's 1,404 agreed (H-grade) signs and bMAL16's 891 agreed signs currently contribute nothing
to the pool while three admitted-vs-held leaves' worth of settling work sits idle, and LANE B10 was handed "the
held leaves become admissible with a cheap settle pass only if a key or crib makes their readings checkable" as
its only route back in -- an all-or-nothing leaf-level re-run, not a per-token one.

```diff
--- a/tools/leaf_pool_gate.py
+++ b/tools/leaf_pool_gate.py
@@ def gate(leaf, pool, form=DEFAULT_FORM, max_m=0.15, max_offform=0.05, shuffles=200, seed=20260926):
     res["admitted"] = res["gate_quality"] and res["gate_form"] and res["gate_system"]
+    # A leaf that clears form + system but fails quality (M-share) is not the same as a leaf that fails
+    # system: the cosine test already established it is the same cipher, so its H-graded signs are safe to
+    # pool for frequency/system purposes. Only its M-graded (uncertain) signs stay excluded from any code-value
+    # attestation, the per-token version of rule 3's per-unit merge-gate paragraph (26 Sept 2026,
+    # RETRO-2026-09-26g; three malsburg leaves f.28/f.24/f.16 all passed cosine decisively and were held whole).
+    res["pooled_h_only"] = res["gate_form"] and res["gate_system"] and not res["gate_quality"]
     return res
@@ def main():
     line = (f"leaf {a.leaf}: N={r['n']} K={r['k']} M={r['m_share']:.3f} (<= {a.max_m}) "
             f"offform={r['offform_share']:.3f} (<= {a.max_offform}) vocab overlap {r['vocab_overlap']:.3f}; cosine real "
             f"{r['cosine_real']:.3f} vs relabel mean {r['cosine_relabel_mean']:.3f} p95 {r['cosine_relabel_p95']:.3f} -> "
-            f"{'ADMITTED' if r['admitted'] else 'HELD'}")
+            f"{'ADMITTED' if r['admitted'] else 'POOLED (H-only, M held per-token)' if r['pooled_h_only'] else 'HELD'}")
     print(line)
+    if r["pooled_h_only"]:
+        print("  H-graded signs join the pool for frequency/system stats; M-graded signs stay out of key.tsv "
+              "value attestation until corroborated by an H occurrence or another leaf's admitted reading "
+              "(CLAUDE.md rule 3, per-unit merge-gate paragraph).")
```

New test case for `tools/tests/test_leaf_pool_gate.py`: a synthetic leaf built with the pool's own value
distribution (so `gate_system` passes decisively) but 30% of its signs marked confidence "M" (so `gate_quality`
fails) asserts `pooled_h_only is True` and `admitted is False`; a leaf that also fails `gate_system` (relabelled
values) asserts `pooled_h_only is False`. A key-building tool consuming `pooled_h_only` leaves is not built by
this diff -- this only stops the whole-leaf discard and records the distinction the field name states; whichever
tool later turns pooled leaves' signs into key.tsv rows is the one that must honor "M-graded stays out of value
attestation," and should cite this paragraph when it does.

Motivating: STATUS.md "LANE B9 handoff" table (f.28/f.24/f.16 rows, all "HELD" despite a passing cosine column);
`tools/leaf_pool_gate.py`'s own docstring ("(3) same system... a leaf failing any gate is held: its signs stay
out of the pool" -- no per-token distinction); CLAUDE.md rule 3's Szembek per-unit merge-gate paragraph (the same
principle, one grain up).

## 3. SALV-DIAG found the syllabary control crosses into the target's failing range at 8-9% injected error while
   the transcription itself disagrees on 14.6% of boxes -- nothing checks that a design-family control's error
   level actually reaches the target's own measured transcription error before a FAIL is logged as a negative

`tools/family_run.py`'s syllabary family already supports `--param err=P` to inject synthetic transcription noise
into the control (LANE R8's DSN/DSN2 work, CLAUDE.md Usage item 8), and the homophonic family has the equivalent
`--param noise=p`. But nothing compares the error level a control was actually run at against the target's own
measured transcription disagreement (the number `reconcile_passes.py` or a leaf's two-pass reconciliation already
reports, e.g. fr2933's atlas passes at 14.6%) before a target FAIL is written up as a design-family exclusion.
R8-DSN and R8-DSN2 ran their controls at 0/5/7% error and logged FAILs at all three as "control-backed negatives"
-- correct arithmetic, but SALV-DIAG (this window) found the crossover into failing territory sits at 8-9%, so
every one of those negatives was drawn from an error range the transcription itself does not support: "the
design-family negatives were non-tests at the measured error," in the row's own words. This is a distinct gap
from the existing PX-BRODEC/pt18/es17c lessons in rule 3 (those calibrate a *judge*'s corpus or notation; this
calibrates a *family control*'s own noise parameter against the same target's measured read-disagreement rate) --
checked and confirmed not already covered by CLAUDE.md's PX-BRODEC paragraph, which is about notation
conventions on two renderings of the same text, not injected-error levels in a synthetic control.

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ (rule 3, appended after "not re-briefed against the same hypothesis without a different instrument.")
+   A family control's injected-error level must bracket the target's own measured transcription error before a
+   FAIL on that control is logged as a design-family negative, the same way a control's N and K must match the
+   target's own (this section's Salviati headline paragraph) -- an error-tolerant family tested only at error
+   levels below where its own control starts to fail is not yet a test of the design at the target's real
+   reliability. Lesson of 26 Sept 2026 (SALV-DIAG, fr2933-salviati-1525): LANE R8's syllabary controls (DSN,
+   DSN2) ran at 0/5/7% injected error and read 83-99% clean, licensing FAILs on the target logged as
+   control-backed negatives -- but a further sweep at 10/12/14% found the control's own recovery collapses
+   between 7% (89% clean) and 10% (already worse than the target's own decode score), a crossover at about 8-9%,
+   which sits inside the atlas transcription's own measured 14.6% pass-to-pass disagreement. Every prior
+   negative on this target was drawn from an error band the transcription cannot back up: not a design
+   exclusion, a non-test, and the row moves to `partial` (rule 5) with the named next step being a transcription
+   pass to lower the measured error, not a further family attempt at the same noise levels.
```

```diff
--- a/tools/family_run.py
+++ b/tools/family_run.py
@@ ap.add_argument("--param", action="append", default=[], help="family parameter k=v (iters, order, tabula, period, beam ...)")
+    ap.add_argument("--measured-error", type=float, default=None, metavar="P",
+                     help="the target's own measured transcription disagreement rate (0-1, from reconcile_passes.py "
+                          "or a leaf's two-pass reconciliation); if the family's own noise/err --param is below this, "
+                          "the run is not refused but its HYPOTHESES.md row is marked as a non-test, not a negative "
+                          "(CLAUDE.md rule 3, SALV-DIAG paragraph, 26 Sept 2026)")
@@ (after params = dict(kv.split("=", 1) for kv in a.param))
+    if a.measured_error is not None:
+        err_param = params.get("err") or params.get("noise")
+        if err_param is not None and float(err_param) < a.measured_error:
+            print(f"WARNING: control error param ({err_param}) is below --measured-error ({a.measured_error}) -- "
+                  f"a FAIL here is not yet a design-family negative (CLAUDE.md rule 3, SALV-DIAG paragraph)",
+                  file=sys.stderr)
+            label_note = f" [control error {err_param} < measured {a.measured_error}: non-test, not a negative]"
+        else:
+            label_note = ""
+    else:
+        label_note = ""
```

(`label_note` appended to the row text written by `append_row`, same place the existing gate/control-below text is
composed; left as a one-line splice rather than a full diff of `append_row`'s call site, which this retrospective
did not re-read line-by-line under the time box.) New test case for `tools/tests/test_family_run.py`: running the
syllabary family with `--param err=0.05 --measured-error 0.10` produces the stderr WARNING and the row-text
marker; `--measured-error 0.03` (below the param) produces neither.

Motivating: NEAR.md fr2933-salviati-1525 row, "SALV-DIAG" entry and its "What would settle it" cell ("the
design-family negatives (DSN, DSN2) are non-tests at the plausible measured error, not a design exclusion");
LEDGER.md SALV-DIAG row; `tools/family_run.py` docstring (syllabary family, `--param err=0.05`).

## 4. `tools/key_livecheck.py` reported Semantic Scholar flipping yes->no between two probes 26 minutes apart
   with no retry, and a transient 429 reads identically to a real lost key

`tools/room.py --start`'s summary this session read "8 present, 4 working, last probe 10:39 UTC" against LEARN-7's
09:07 reading (unchanged from earlier in the morning) of "8 present, 5 working" -- a working-key count that
dropped by one between two probes, and Semantic Scholar's own rate-limit rule (documented in the Access playbook:
"1 request per second, no Retry-After on 429... sleep 1.1s and back off manually") means a single unlucky probe
landing inside another caller's burst reads identically to a genuinely revoked or expired key. `run_probe()`
tests each key once per cooldown window and caches the result; nothing re-checks a key whose result differs from
its own last cached or previously-reported value before writing it to KEYS-STATUS.md and flagging it in
`render_changes()`. This is exactly the retry-before-trusting-a-negative shape CLAUDE.md's good-citizen rule
already applies to a live host fetch ("a single retry after a pause is the limit") and the shape Q3 of this
window's own brief flagged: nothing here says a probe should apply that same one-retry discipline to its own
flip detection before it tells two lane orchestrators a key stopped working.

```diff
--- a/tools/key_livecheck.py
+++ b/tools/key_livecheck.py
@@ def run_probe(http=default_http, now=None, cache=None, force=False, cooldown=900, do_ia_login=False, sleep=time.sleep):
-def run_probe(http=default_http, now=None, cache=None, force=False, cooldown=900, do_ia_login=False, sleep=time.sleep):
+def run_probe(http=default_http, now=None, cache=None, force=False, cooldown=900, do_ia_login=False, sleep=time.sleep,
+              prev=None, retry_pause=5):
     """Pure-ish core: returns a list of row dicts. `http` and `sleep` are injectable for the offline test."""
+    prev = prev or {}
@@         maybe_sleep()
         ok, detail = spec["test"](value, http)
+        prior = prev.get(spec["label"])
+        if prior is not None and prior[1] == "yes" and ok is False:
+            # a live flip from working to not-working: one retry after a pause before trusting it (good-citizen
+            # rule's own one-retry-after-a-pause discipline, applied to the probe's own flip detection --
+            # 26 Sept 2026, RETRO-2026-09-26g, S2 read yes->no with no retry between two probes 26 min apart)
+            sleep(retry_pause)
+            ok2, detail2 = spec["test"](value, http)
+            if ok2:
+                ok, detail = ok2, detail2 + " (retry after transient failure)"
+            else:
+                detail = detail + f" (confirmed on retry after {retry_pause}s: {detail2})"
         if ok is not None:
             cache[spec["id"]] = {"t": now, "ok": ok, "detail": detail}
         row.update(works=ok, detail=detail, checked_live=True)
         rows.append(row)
@@ def main(argv=None):
     prev = parse_previous_status(a.out)
-    rows = run_probe(force=a.force, cooldown=a.cooldown, do_ia_login=a.ia_login)
+    rows = run_probe(force=a.force, cooldown=a.cooldown, do_ia_login=a.ia_login, prev=prev)
```

New test case for `tools/tests/test_key_livecheck.py` (mirroring the existing offline-test style with an
injectable `http`/`sleep`): a fake `http` that returns failure on the first call and success on the second, with
`prev` recording the key as previously "works: yes", asserts the row's final `works` is `True` and its `detail`
notes the retry -- a fake `http` returning failure both times asserts `works` is `False` and `detail` says
"confirmed on retry."

Motivating: `tools/room.py --start`'s own printed summary this session ("8 present, 4 working, last probe 10:39
UTC") against LEARN-7's 09:07 LEDGER.md row ("key_livecheck.py not seen in its check-ins" -- read together with
the room.py digest's own earlier reading of 5 working); CLAUDE.md's good-citizen rule ("a single retry after a
pause is the limit"); `tools/key_livecheck.py`'s own `render_changes()` (the flip-reporting function with no
retry ahead of it).

## Answered without a diff (two of the brief's other questions)

**(Q3) LANE ARM cycle 1 parked A1/A2/C, did not license D, and B/E/S continue -- is the lane idle-standing
already?** No, and no diff is needed to say so: CLAUDE.md's idle-standing definition requires "no untried cheap
step that is not itself gated on a named ASKS.md row," and ARM-CONS1's own cycle-2 briefs name three untried
cheap steps with no ASKS gate at all -- ARM-S2 (shorthand symbol match), ARM-REC3 (Founders/LOC correspondents),
ARM-POOL2 (docket search) -- all three already spawned by the time this retrospective's window closed (ROOM.md
10:16, "Cycle 2 live"). ARM-CONS1's C2 rejection (139 of 369 tokens singleton, capping any context-only solver
near the 0.6 gate) is a real finding about *that family*, not about the target as a whole: the lane's own next
steps are archival material-search, exactly the kind of step that would surface more same-code ciphertext without
an owner ASK, so the standing-campaign stop rule this question asks for is already CLAUDE.md's existing
idle-standing gate -- it just is not yet satisfied, because untried cheap steps exist. If a future cycle's
consolidator finds every remaining step (S, E, and the archival searches) also exhausted with no lead to a
same-code sibling, *that* consolidator's own report is the point to write the one-line idle-standing ROOM.md
entry CLAUDE.md already names, naming an ASKS row for "more ciphertext in the same code" as the blocker -- no new
rule is needed for that step either, since idle-standing already covers "a target gets new material from outside
the lane... that opens an untried cheap step" as its own exit condition.

**(Q5) NX-MOR2 built a grade-H key from a leaf's own interlinear decipherment and NX-MOR3 passed the rule-7
re-derivation in one round -- what made it fast, and is the recipe already required?** Three things, read
together from NX-MOR2's and NX-MOR3's own done lines: (a) the key came from 13 clean recurring-code/gloss pairs
with a shuffle-consistency control run immediately (real 1.000 vs shuffle mean 0.621, p95 0.800) -- the same
control CLAUDE.md's Szembek per-unit paragraph (item 2 above) already requires before a unit's glosses enter a
shared key, applied correctly the first time on a single-leaf key rather than a multi-leaf merge; (b)
`decode_key.py --check` was run clean before anything else touched the reading; (c) NX-MOR3's own re-derivation
built its transcription independently (one blind subagent plus the worker's own crop reads) with the committed
key's values never shown to that subagent, so the 0-diff result is a genuine held-out check, not the same worker
re-reading its own prior work. None of (a)-(c) is spelled out as a required sequence anywhere in `.claude/briefs/`
today (grepped `solver.md` and `README.md` for "shuffle-consistency," "decode_key.py --check," and "withheld" --
no hits): this window is the recipe's second clean instance (MOR2/MOR3 following the Szembek precedent that
prompted CLAUDE.md's own rule-3 paragraph), which is one below CLAUDE.md's own "twice" bar (Usage item 8a: "a rule
that the ledger shows broken twice gets a mechanical check") for turning it into a template requirement -- worth
naming here so a third repetition is recognized as the pattern it is, not proposed as a new template edit before
that bar is met.

## Near solves (review, per brief)

All active-table rows read directly from NEAR.md.

**fr2933-salviati-1525**: moving -- SALV-DIAG (this window, 10:06) is the dispatch retrospective r's own standing
plan called for ("if the next retrospective finds it still untouched, that is a dispatch"); it ran, found the
crossover, and named transcription pass C as the next step. See item 3 above for the rule this result motivates.

**matignon-mayenne-1586**: last touched 08:50 (1h's result), unchanged this window's ledger rows; not stale
under the 48-hour rule. Item 1's diagnosis from retro r (the ladder for this instrument is spent) stands
unchanged; LANE B9 correctly did not re-run it.

**malsburg-hessen-1636**: moving heavily this window (bMAL23/24/28/28B/16/H, the pool wave and its close) -- see
items 1 and 2 above.

**hessen-1824**: closed as untestable by running_key at N=164 (bHCP3, prior window, restated in this window's
LANE B9 close); no new movement this window, correctly `partial` per rule 5.

**antt-msliv0638-brochado-1712 letter 134**: last touched 26 Sept 06:42, prior window; not touched this window's
28 rows; not stale under the 48-hour rule (third retrospective mention, still short of a dispatch threshold).

**espagnol142-mercy-1648, koehler-1944, mccormick-1999, bullet-tuscany-1944, untersberg-code, blitz-ciphers**:
none touched this window's ledger rows; none stale under the 48-hour rule read against this session's own
`date -u` of 10:43:25 UTC 26 Sept 2026.

## What did not need a change

Rules 1, 2, 6, 8, 9, 9a, and the Outreach gates: untouched by anything else in this window. Rule 3 held
elsewhere checked this window (bMALH, ARM-C1, ARM-CONS1, NX-WVO174, NX-3151G, NX-LAU3 all report target and
control numbers together, or CONTROL BELOW GATE/tied-with-shuffle-floor language where that is what happened).
Rule 4 held (no new H-vs-H conflict this window). Rule 5 held (malsburg, hessen-1824, matignon, fr2933 all stay
`partial`/`open`, never `closed-negative`, after their negatives -- item 2's held leaves are reported as HELD, not
excluded). Rule 7 held decisively: NX-MOR3's fresh-instance re-derivation reproduced all 97 tokens of
NX-MOR2's committed reading with 0 diffs, `decode_key.py --check` clean (see Q5 above). Rule 10 held everywhere
checked this window (NX-MOR2/MOR3, NX-E318, NX-WVO1109/174, NX-3151N33/G, NX-LAU/LAU2/LAU3 all report
found/not-found and search method; NX-MOR2's own key is flagged "key: period" language in NOTES.md per the
10:43 owner-account parent's own reminder that Morillo's is a leaf-interlinear key, not a cryptanalytic result --
that reminder landed inside this window and is the parent's job to action, not this retrospective's, since it
names a next worker and a V9 classification step rather than a rule gap). Credentials: no mishandling found in
this window's ROOM.md/NOTES.md diffs; the key_livecheck flip in item 4 above is a monitoring gap, not a
credential leak. Absolute dates: `date -u` read at this session's own start (10:43:25 UTC) and used throughout.

Retrospective s done: 4 proposals (items 1-4), file RETRO-2026-09-26g.md.
