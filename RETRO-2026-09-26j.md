# Retrospective v, 26 Sept 2026 (Sonnet, cap $10 / 40 min, parent 7i)

Window: LEDGER.md rows after "retrospective u: 12 ledger rows" (22 new rows by 18:16 UTC, about USD 178
across both accounts before adjusting for item 5's double-counted row -- see below): PR-LAND-3, parent 7h
(hand-over row, appears twice, see item 5), SCOUT-OWN-5, LANE NX2 NX2-RV, LANE NX2 orchestrator, MAIL-PREP,
SYSTEM-MAP, PR-LAND-5 (twice, see item 5), OUT-CHECK, OUT-CHECK-2, LQ-L20-LAND (twice, see item 5), OUT-CHECK-V,
OUT-CHECK-3, LANE VO1 worker VO1-JAPIKSE (X, never started), QA13, RETRO-APPLY-U, LANE B12 bSALW/bSPECFILL/
orchestrator. `date -u` read 18:07:22 UTC at claim, 18:16:11 UTC before writing. `python3 tools/room.py --start`:
key_probe.py --sync 18 set; key_livecheck.py 9 present/6 working, last probe 18:02 UTC, no allowed_warning.
ROOM.md read via `tools/room.py --digest "2026-09-26 17:03"` (20 lines) plus the raw tail through 18:12 for
lines the digest's window cut off (the owner's mailbox-send report, the OPTIMIZATION-2026-09-26.md
specialisation clarification, LANE B12's close). QA/2026-09-26-1732.md read in full (0 failures, 0 flags).
RETRO-2026-09-26i.md read in full (applied by RETRO-APPLY-U at 17:44, not repeated). UPDATES.md rows from
17:42 to the end read directly (all four RETRO-APPLY-U rows plus LQ-L20-LAND's kind-awareness row). NEAR.md
read directly (13 rows; fr2933-salviati-1525's row already carries bSALW's result as of LANE B12's own 18:11
close, updated by the lane itself before this session reached it -- no gap to report). OPTIMIZATION-2026-09-26.md
read in full plus its 18:12 UTC amendment in ROOM.md (owner's condition: nothing in progress moves between
accounts; each account keeps its own targets to completion; the split decides only who spawns what from now
on). STATUS.md's "Parent handoff" sections for both accounts read from 17:03. outreach/README.md read in full;
six checked mailbox drafts (armstrong-madison-editors, bodleian-rawl-a24-p4, marburg-wvo5551-copy,
nara-koehler-rg65, debosnys-museum, clements-destaing-1779) read directly, headers and bodies, against their
`outreach/mailbox/*.json` files and CONTRIBUTIONS.md rows 27-31. Mid-session the local clone was found one
fast-forward behind `origin/main` (`git pull --rebase` picked up LANE B12's close, the OPTIMIZATION amendment,
and five mailbox sends the owner made at 18:00-18:04 UTC); every number below is read after that pull.

Questions of note, in the brief's own order:

## 1. `tools/lq_answer_check.py`'s first version, built from one incident's shape, bounced a differently-shaped
   true answer the same day -- the gate named no scope

PR-LAND-5 (17:36) bounced PR 24's genuine content negative (a held Internet Archive loan, read page by page,
found nothing) because the gate built from the L19 incident (an image portal's "no items") required a
catalogue-availability rung that a page-read answer never carries. LQ-L20-LAND (17:42, UPDATES.md row) fixed it
the same day by adding row-kind awareness, but nothing in CLAUDE.md's own "rules become tools" definition asks a
gate's *first* version to state what it does and does not cover, so this shape can recur with the next gate.

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -499,6 +499,10 @@
 8a. **Rules become tools (25 Sept 2026, UPDATES.md).** A rule that the ledger shows broken twice gets a mechanical check in
    tools/ with an offline test, and its prose shrinks to one line naming the tool. Precedents: `tools/intake_gate_check.py`
+   A gate built from one incident's shape states, in its own docstring, the row or case kinds it is meant to
+   catch and at least one kind it must NOT block, each backed by an offline test -- a gate that fires on a
+   phrase or pattern with no stated scope will eventually bounce a differently-shaped true answer. Lesson of
+   26 Sept 2026 (PR-LAND-5/LQ-L20-LAND): `tools/lq_answer_check.py`'s first version, built from the L19
+   image-portal "no items" shape, bounced PR 24's L20 answer -- a genuine content negative reached after a
+   successful loan and a full page-by-page read -- fixed the same day by adding row-kind awareness
+   (ia-reader/edition-read/hathitrust-page/jstor skip the catalogue-ladder rungs the image-portal shape needs).
    (the intake gate), `tools/room.py` (ROOM.md hygiene), `tools/ledger_check.py` (outcome codes),
```

Motivating: LEDGER.md's PR-LAND-5 rows (both, see item 5); UPDATES.md 17:42 row (LQ-L20-LAND's kind_for_row()
fix); this retrospective's own brief, question (1).

## 2. Outreach drafts are written as reports, then rewritten in first person, then re-checked for the rewrite's
   own errors -- three sessions where the voice convention itself, stated once, would need one

MAIL-PREP's six drafts (17:12) passed OUT-CHECK's fact check (17:37) still reading "like a report" (the owner's
own word, ROOM 17:45); the parent rewrote them in first-person voice, and OUT-CHECK-V (17:51) then found 8
words in 4 drafts where the rewrite had smoothed an agent-authored "we" into "I" or dropped a hedge (armstrong:
"the four we could locate of the six" had become "I could locate"). Three of the four drafts the owner sent at
18:00-18:04 UTC needed that second check to be sendable at all. outreach/README.md rule 1 requires the
disclosure sentence but says nothing about which voice the sentences after it take.

```diff
--- a/outreach/README.md
+++ b/outreach/README.md
@@ -4,6 +4,12 @@
    in so many words on 26 Sept 2026 (outreach/huygens-nassau-saxony.md, Reply, item 5). The first Huygens email did
    not say it; do not repeat that.
+1a. **Voice, not just disclosure (26 Sept 2026).** Write every sentence after the disclosure in the voice of
+   who actually did it: "I" for what the person decides, asks or sends (the request itself, the sign-off);
+   "we" for what the agents did (searched, transcribed, compared, checked, ruled out) -- never smooth a "we"
+   describing agent work into "I", and never let the whole letter read as a report addressed to the person
+   rather than sent by them. Lesson of 26 Sept 2026: MAIL-PREP's first six drafts read like a report and were
+   rewritten in first person by the parent, and the rewrite itself needed a second check (OUT-CHECK-V) because
+   three drafts had smoothed an agent-authored "we" into "I". Write it this way the first time; a drafting
+   worker unsure which voice a sentence takes defaults to "we" and lets gate 7 correct it.
 2. A reading that rests on a recovered key (no key source) is presented as a proposed reading, with the share of
```

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -247,1 +247,2 @@
 (7) Pre-send fact check (owner, 26 Sept 2026): before the person sends any outward note, a separate session (never the drafter) reads the draft against every file and source it cites, tries to falsify each factual sentence (counts, dates, shelfmarks, names, what was and was not checked, the recipient address on the institution's own page) and writes its verdict as a `checked:` line in the draft's header naming what it corrected; a draft without that line is not sent, and the mailbox draft is updated to the checked text.
+    The same pass also confirms the draft's voice matches outreach/README.md rule 1a ("I" for what the person
+    does, "we" for what the agents did) as one more sentence class to falsify, not a separate session -- a
+    voice error is a `checked:`-line correction like any other (26 Sept 2026: OUT-CHECK-V existed only because
+    the voice rewrite happened after OUT-CHECK's own pass; with rule 1a in place a drafting worker gets the
+    voice right the first time and one OUT-CHECK pass covers both).
```

Motivating: ROOM.md 17:45/17:48/17:51/17:53 lines; LEDGER.md rows for OUT-CHECK, OUT-CHECK-V; the six checked
drafts' `voice:`/`checked:` header lines (all six, read directly); this retrospective's own brief, question (2).

## 3. Two accounts wrote a `.claude/briefs/runs/` file of the same name within ten minutes, and the second
   write silently replaced the first -- the shared-file rebase rule does not name this path

`.claude/briefs/runs/2026-09-26-parent-out-check-2.md` was written by the owner-account parent (56c4256, the
RAH/Dresden fact check) and then overwritten by 7i (its own OUT-CHECK-V brief, same filename) for about ten
minutes before 7i noticed and restored the original, renaming its own file
(2026-09-26-parent-out-check-voice.md). CLAUDE.md's "Rebase before you write to a shared file" paragraph names
five files everyone writes to; brief-run filenames built from a date and a repeatable role name are the same
hazard and are not on the list.

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -349,3 +349,9 @@
 **Rebase before you write to a shared file.** `QUEUE.md`, `QUEUE-scores.json`, `status.json`, `STATUS.md`
 and `ROOM.md` are written by everybody. Fetch and rebase immediately before editing, and when a row
 conflicts, keep both facts rather than overwriting someone else's finding.
+
+**Name a job-brief file for the account that writes it.** `.claude/briefs/runs/<date>-<role>.md` collides the
+same way: two accounts wrote `2026-09-26-parent-out-check-2.md` within about ten minutes of each other on
+26 Sept 2026 (the owner-account parent's RAH/Dresden fact check, 7i's mailbox voice re-check), and the second
+write silently replaced the first, restored only because the overwriting session happened to notice and flag
+it. A filename built only from the date and a repeatable role name (`parent-out-check-2`, `parent-retro-apply`)
+can repeat across two accounts working the same day; include the writing account's `CIPHERLAB_ACCOUNT` tag
+whenever the role name is one that could recur (`2026-09-26-parent-ytbiz-out-check-2.md`), and fetch
+immediately before writing any file under `.claude/briefs/runs/`, the same discipline as the files above -- if
+the path already exists with a commit from a different session in the last hour, pick a new suffix rather
+than overwrite.
```

Motivating: ROOM.md 17:48 flag (7i, "I overwrote your ... 56c4256 ... for about ten minutes"); this
retrospective's own brief, question (3).

## 4. The mailbox send log was invented ad hoc, three times, in three slightly different shapes, and both
   accounts are now writing to the same CONTRIBUTIONS.md rows without a stated convention

7i wrote a clean send-log line on all six checked drafts at 18:00-18:03 UTC (worked example:
armstrong-madison-editors.md's `status:` line, `sent 26 Sept 2026 18:01 UTC by the person from the project
mailbox (Gmail Sent, read by parent 7i 18:03 UTC); reply pending`) and a matching CONTRIBUTIONS.md row, but
nothing on file says to do it that way -- it is one session's invention. At 18:07 the owner-account parent
independently set "the matching CONTRIBUTIONS.md rows to sent with those times where the row text let me
match it" for the same five sends and flagged "please check your rows", i.e. two accounts are now writing the
same field with no shared template, matched only by one parent guessing at the other's row text. Separately,
desk_check.py has no case for a draft whose `status:` line still reads `mailbox-draft` (or whose conditional
header line still reads "not sendable until its checked: line lands") after its own send has already happened
-- true today of nothing on file (7i's own writes were prompt), but it was true for about 15 minutes on all
six drafts between OUT-CHECK-V's `checked:` line landing and the sends, during which every draft's `voice:`
line still read the stale conditional.

```diff
--- a/outreach/README.md
+++ b/outreach/README.md
@@ -21,3 +21,8 @@
 Gate 7 (26 Sept 2026): no mailbox draft is sent until a separate checking session has written a `checked:` line in the draft's header (CLAUDE.md Outreach gate 7); the parent replaces the Gmail draft with the checked text before telling the owner it is ready.
+
+**Send log (26 Sept 2026).** The moment the parent sees the message in the mailbox's Sent folder, it records
+the send in this exact shape (armstrong-madison-editors.md is the worked example): the draft's `status:` line
+becomes `sent <UTC timestamp> by the person from the project mailbox (Gmail Sent, read by parent <session>
+<UTC timestamp>); reply pending`, keeping the prior value after a `Was:`; CONTRIBUTIONS.md's row for the same
+slug gets `sent by the person <UTC timestamp> from the project mailbox (gate 7 checked <the checked: line's
+timestamp(s)>); reply pending` in its status cell; and the ASKS row it answers is marked `sent, awaiting
+reply`. Either parent may see the Sent copy first (both accounts share the one Gmail connector); whichever
+one does writes all three in this shape rather than guessing at the other's wording, so a target is never
+recorded as sent twice in two different phrasings.
```

```diff
--- a/tools/desk_check.py
+++ b/tools/desk_check.py
@@ (Checks list, after (d) NO DATE)
+  (e) SENT MISMATCH: a draft whose `status:` line reads `mailbox-draft` while CONTRIBUTIONS.md already carries
+      a "sent by the person" row for the same slug (the send happened but the draft's own header was not
+      updated), or the reverse (status: sent with no matching CONTRIBUTIONS.md row); and a header line
+      matching "not sendable until its checked: line lands" when a later-timestamped `checked:` line already
+      exists in the same file -- a conditional line the file never revisits once the condition clears. Lesson
+      of 26 Sept 2026: all six mailbox drafts' `voice:` header line read "OUT-CHECK-V ... not sendable" for
+      about 15 minutes after OUT-CHECK-V's own `checked:` line had already landed below it, because nothing
+      cross-checked the conditional line against the unconditional ones.
```

New test cases: `tools/tests/test_desk_check.py` gets a fixture draft at `status: mailbox-draft` with a
CONTRIBUTIONS.md row already reading "sent by the person" for its slug (must flag), and a fixture draft whose
header carries both a stale "not sendable until checked: line lands" sentence and a later `checked:` line
(must flag); a draft where the conditional predates every `checked:` line must not flag.

Motivating: ROOM.md 18:07/18:12 lines (owner-account parent); armstrong-madison-editors.md and the other five
checked drafts' header lines (read directly); CONTRIBUTIONS.md rows 27-31; this retrospective's own brief,
question (5).

## 5. `tools/ledger_check.py`'s duplicate-session check is blind to the exact pattern CLAUDE.md's own
   Improvement loop section names as recurring -- a placeholder-cost self-ledger row followed by the real
   number, appended rather than edited in place

Running `tools/ledger_check.py` live against the current LEDGER.md finds one real duplicate (`parent 7h`,
session_01GT3Yv4kUZrBR8pc2s5EToH, two byte-identical rows, cost 55.91 both times) but silently misses two more
that are the *known* recurring shape CLAUDE.md's "Improvement loop" section already names ("LANE B5, V7 and
GOLD3 all did this in one window ... about USD 17 of orchestrator overhead counted twice"): PR-LAND-5
(session_0184Cjg9WnwGotCxKhi6no2F) has one row with cost `cap 4, exact figure pending the parent's own
get_session read at archive (own-session get_session returns no cost while running)` and a second with cost
`2.01`; LQ-L20-LAND (session_019DNzwRqjndogyeTymb7Hqw) has one row with cost `parent's get_session` (outcome
D) and a second with cost `3.71` (outcome D-). Reading the tool's own `find_cost_index()`: it requires a cell
to match `COST_RE` (a number) before it will even look at the Session cell beside it, so a self-ledgered
placeholder-cost row -- exactly the convention this repository's workers use while still running -- drops out
of the check entirely, and the later row that fills in the real number is then counted once, never flagged as
a duplicate of the row nobody could see. This is not cosmetic: it lets the double-counted-cost problem the
Improvement loop section was written to stop keep happening invisibly, and inflates this very retrospective's
own "about USD 178 across both accounts" window figure by roughly the 55.91 (confirmed exact duplicate) plus
whatever the PR-LAND-5/LQ-L20-LAND pairs' placeholder-vs-real gap is worth.

```diff
--- a/tools/ledger_check.py
+++ b/tools/ledger_check.py
@@ (near find_cost_index)
+def find_session_index(fields):
+    """Fallback when find_cost_index() finds no numeric-cost/outcome pair: locate the Session cell directly
+    by SESSION_ID_RE regardless of what sits beside it, so a placeholder-cost row ("cap N, exact figure
+    pending...", "parent's get_session") still registers its session id for duplicate detection. Lesson of
+    26 Sept 2026: two real duplicate pairs this window (PR-LAND-5, LQ-L20-LAND) each had one row whose Cost
+    cell was placeholder text, which made find_cost_index() return None for that row and drop its session id
+    out of the check -- the later row with a real number was then seen only once, never flagged."""
+    for i, f in enumerate(fields):
+        if SESSION_ID_RE.match(f):
+            return i
+    return None
@@ (in check(), where cost_i is None)
         cost_i = find_cost_index(fields)
         if cost_i is None or cost_i + 1 >= len(fields):
-            continue  # not a well-formed data row
+            si = find_session_index(fields)
+            if si is not None and si + 1 < len(fields):
+                seen_sessions.setdefault(fields[si], []).append((lineno, "(unparsed)", fields[si + 1]))
+            continue
```

New test cases: `tools/tests/test_ledger_check.py` gets the PR-LAND-5 shape (one row cost `cap 4, exact figure
pending...`, one row cost `2.01`, same session id) and the LQ-L20-LAND shape (cost `parent's get_session` vs
`3.71`, outcomes `D` vs `D-`), both asserting the session id lands in `dup_sessions`; keep the existing
byte-identical-row case (parent 7h) passing unchanged.

Motivating: `tools/ledger_check.py` run live against LEDGER.md this session (output saved this session); the
three session ids and their row pairs (LEDGER.md lines shown above); CLAUDE.md's "Improvement loop" section's
own B5/V7/GOLD3 sentence; this retrospective's own brief's window-cost instruction ("~USD 60+ since retro t"
style figures depend on this count being right).

## Confirmed, no diff needed

- **Question (6), lineage depth.** `tools/orphan_check.py`'s `check_lineage_depth()` (added by RETRO-APPLY-U,
  15/15 pytest cases) was run live against the current STATUS.md this session (`python3 tools/orphan_check.py
  --no-sessions`): 0 lineage-depth warnings, correct, since 7i is depth 0 (created by the owner from the UI,
  STATUS.md line 955) and has not yet handed over. The regex pair (`UI_CREATED_RE`, `HANDOVER_TO_RE`) and the
  newest-first section-scanning logic match the live file's actual shape (confirmed by inspection of both
  accounts' "## Parent handoff" sections). No false positive, no diff needed; the offline tests already cover
  the positive case.
- **Question (4), OPTIMIZATION-2026-09-26.md.** Measured against this window's ledger: the three brief/tool
  items (NEXT-STEPS.tsv, key_crossmatch recalibration, the desk cap/dropped-item detector) are already spawned
  as separate parent workers on the owner account (ROOM 18:04, 18:05) with their own caps, so re-proposing them
  here would duplicate work already in flight, not add a diff. The one item that changes the goal/spend --
  account specialisation -- is explicitly a "For the owner" matter per this retrospective's own rules and was
  already negotiated between the two parents in ROOM (7i agreed at 18:05; the owner amended it at 18:12 to
  guarantee nothing in progress moves between accounts). Nothing further for this retrospective to add.

## Near solves (review, per brief)

All 13 active NEAR.md rows read directly.

**fr2933-salviati-1525**: moving, and current as of this session's own pull. bSALW's wordcode family (a new
design, control 0.632 at 6.4pct measured error, letters a control-backed negative, codes a non-test on an
unbalanced class) landed at 18:04-18:06 UTC and LANE B12 folded it into the row's own numbers and named next
step (transcribe the 1,233 plain-Italian boxes for context) before closing at 18:11 -- no propagation gap this
time, unlike the shape retro d found on this same target's row on 24 Sept.

**espagnol142-mercy-1648**: moving. LQ-L20-LAND's landed reading (Cuvelier-Lefevre VI p.647, read on a held
loan, negative) is in the row's step (3) as of 17:42; not stale.

**malsburg-hessen-1636, matignon-mayenne-1586, koehler-1944, mccormick-1999, bullet-tuscany-1944,
untersberg-code, antt-msliv0638-brochado-1712, blitz-ciphers, hessen-1824, fr3625-lauriere-1593,
pollaky-1865-1875**: no ledger rows in this window touch any of these; none crosses the 48-hour staleness rule
against this session's `date -u` of 18:16 UTC 26 Sept 2026 (fr3625-lauriere-1593 at 11:50 is the most recent of
this group, about 6.5 hours old).

## For the owner

Item 4 (mailbox send log) needs no action from you -- it is a brief/tool fix the orchestrator applies. Item 5
(the ledger-check blind spot) is also a mechanical fix, but worth a sentence: the "about USD 178 across both
accounts since retro u" figure quoted at the top of this file, and every similar figure in past retrospectives
and check-ins, has been overstating true spend by whatever these placeholder-then-real-number pairs are worth
(confirmed here: at least the 55.91 exact duplicate, likely more once ledger_check.py's fix lands and is run
against the full file) -- not a new cost, a bookkeeping correction, but you should expect the next
retrospective's window-spend figure to read a little lower once item 5 is applied and re-run against the
whole ledger, not because spending slowed.
