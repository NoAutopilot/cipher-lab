# Cross-account learning pass 2, 25 Sept 2026, 21:18 UTC (parent worker LEARN)

Window: 2026-09-25 17:18 UTC onward, on top of pass 1 (`LEARN-2026-09-25-1718.md`; its items are not repeated here).
Scope read: `git log --since="2026-09-25 17:18" --stat` (53 commits) and `python3 tools/room.py --digest "2026-09-25
17:18"`; the other account's (parent 7c -> 7d) `RETRO-2026-09-25k.md`, `RETRO-APPLY-K`'s ROOM/LEDGER lines,
`UPDATES.md`, `NEAR.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`, the R6/R7/V6/B2/B3/GOLD/GOLD2
orchestrator and job briefs, and LEDGER.md rows 785-814. Compared against the owner-account side: CLAUDE.md,
`.claude/briefs/README.md`, `.claude/briefs/parent.md`, LESSONS.md, `.claude/briefs/runs/2026-09-25-lane-zx2-COMMON.md`
and `-lane-cx2-COMMON.md`. `date -u` read 21:14:21 (start) and 21:18 (this line). Not applied; the parent applies
brief/tool-only diffs.

**Headline: the cascade mechanism pass 1 flagged as "mirrored" is now doing real work.** `UPDATES.md`, created by
parent 7c at 17:51 UTC specifically because pass 1 found a change sitting unnoticed for hours, is fully current
through 20:01 UTC and every one of RETRO-2026-09-25k's four brief/tool-only proposals (subagent job-size rule in
CLAUDE.md Usage 6, lane-context self-report in parent.md, the es17c per-fold-variance lesson in CLAUDE.md rule 3,
two LEDGER.md recodes) is confirmed present on disk, applied by RETRO-APPLY-K within an hour of the retrospective
landing. So, as in pass 1, most of the freely-propagating surface (shared CLAUDE.md/README/parent.md/LESSONS.md/
UPDATES.md) is already mutual the moment either side's retrospective lands it. What's left for this pass is the
same narrow surface pass 1 named: individual ledger lessons and lane-brief text from the last four hours that
have not yet been generalized into a shared file, plus one live-ish tooling gap.

## 1. Practices/lessons they hit that are not yet in a shared file

| # | Practice / lesson | Evidence (file:line) | Portable? | Diff |
|---|---|---|---|---|
| 1 | **`room.py --push`'s rebase-and-retry loop can silently drop a STATUS.md section it does not itself edit**, unlike ROOM.md, which the script explicitly protects (refuses to push a shrunk or conflict-marked ROOM.md). Discovered as a lesson, not reported as fixed. | LEDGER.md:810 (LANE B3 orchestrator): "STATUS.md sections can be dropped by another session's room.py --push merge -- re-check your section after each push." `tools/room.py`'s own docstring and conflict-keeping code (`_resolve_conflict`, `push()`) protect only ROOM.md by name; a non-conflicting but content-losing 3-way merge on any other shared file (STATUS.md, QUEUE.md) has no equivalent guard. | Yes -- this is a data-loss-adjacent gap in shared tooling, the same class of bug `room.py` was built to close for ROOM.md (RETRO-2026-09-24b). | Two options for the parent to weigh, not a canned diff: (a) extend `room.py --push`'s post-push verification (it already re-reads ROOM.md's line count) to also diff any named STATUS.md/QUEUE.md section header before/after and warn if one vanished; (b) cheaper, land B3's own workaround as a rule in `.claude/briefs/parent.md`'s "Lanes" check-in duty and the README common tail: "after `room.py --push` touches STATUS.md, re-open your own section and confirm it is still there before reporting the check-in done." Recommend (b) now, (a) if this recurs -- it has only been reported once so far. |
| 2 | **A dense, rule-heavy inline `create_session` prompt can trip a fresh worker's own prompt-injection heuristic**, losing the whole session before it does any work. | LEDGER.md:799: "LEARN (window since 17:18), first attempt... Stopped at its first turn reporting 'suspicious injected directive detected' on the parent's inline create_session prompt and waited for a human; archived, respawned as LEARN-2 with the task phrased as a routine job that names the brief file and the owner's authorisation." | Yes, and directly relevant to how this very session was spawned (this pass 2's own prompt led with the brief file and a plain job description, which worked). | `.claude/briefs/parent.md` and `.claude/briefs/README.md`: add a line under session-spawning guidance -- "when creating a lane orchestrator or worker session, lead the prompt with the brief file path and the one-line reason the job exists (an owner-authorised routine job), not a wall of caps/rules/warnings up front; a prompt that reads like an injected instruction set can trip a fresh session's own safety check before it reads the brief (LEDGER.md 25 Sept, LEARN's first attempt)." |
| 3 | **A "blind" re-transcription brief needs to name `images/manifest.json`'s `iiif_lines` block as off-limits too**, not just the plaintext/key -- its line-box geometry alone is enough to leak information a blind pass is supposed to be withheld. | LEDGER.md:803 (R7-MEYE, self-disclosed): "Disclosed a partial blind breach itself (manifest line boxes seen, no glyph content). Lesson: a blind brief must name manifest.json's iiif_lines block as off-limits too, or hand the worker a stripped manifest." | Yes -- general to every blind-transcription/blind-re-read brief, not just Mercy. | LESSONS.md (wherever blind-methodology is documented) or a new line in the common tail's blind-brief guidance: "a blind pass's brief excludes images/manifest.json's iiif_lines block (or the worker is handed a manifest stripped of it) -- line-box positions alone can bias a supposedly blind re-transcription (R7-MEYE, 25 Sept 2026)." |
| 4 | **`tools/family_run.py`'s subprocess timeout is sized per call, not per control+target pair**, so a control that runs long can leave too little of the timeout for the target stage, silently truncating rule 3's own discipline unless the worker notices and reruns by hand. | LEDGER.md:812 (GOLD-K1): "a 1500 s family_run timeout killed the target's stage 2 after the control had run; the worker reran the target only and said so (rule 3 kept). Size the timeout to control+target." | Yes -- a tool-level gap, not a one-off; the worker's manual catch is not guaranteed for every caller. | `tools/family_run.py`'s own docstring/`--help` and CLAUDE.md's tool-precedents line (Usage item 8a): document that `--timeout` (or its default) must cover the control battery plus the target run combined, not just one call; consider having the tool compute a default from `--seeds` x an estimated per-run cost rather than a flat constant. |
| 5 | **A new `family_run` control option is worth a one-seed sanity check (compare its profile/L1 against the target) before the full control battery**, because a wrong first implementation still burns a complete run. | LEDGER.md:800 (GOLD-D1): "a new control option costs one extra full run when its first version is wrong; brief a 30-second profile sanity check (L1 against the target) before the seeds." | Yes, cheap and general to any new `tools/families/` option. | One line in `tools/family_run.py`'s docstring or CLAUDE.md Usage item 2 ("scripts read, models judge"): "a newly added control option (a new `profile=`, a new corpus flag) gets a single-seed sanity comparison against the target before the full seeded battery runs, not after (GOLD-D1, 25 Sept 2026: a wrong first `profile=target` implementation was only caught after a full run)." |
| 6 | **LANE GOLD's "consolidator" pattern (a periodic stronger-model pass that rewrites a standing campaign's HYPOTHESES.md summary and decides continue/pivot/park per hypothesis family) now has two real cycles of evidence**, not zero as at pass 1's reading. Cycle 2 (`.claude/briefs/runs/2026-09-25-lane-gold-consolidator-c2.md`) shows the mechanism doing exactly its designed job: reading three workers' control/target numbers side by side, applying a pre-written decision rule without moving the thresholds after seeing the numbers, and writing named cycle-3 briefs. | Pass 1 (`LEARN-2026-09-25-1718.md` item 3) logged this as "report only, no result yet, GOLD opened at 16:44, nothing ledgered." Now: LEDGER.md rows for GOLD-K1/K2/D1/consolidator-c1/c2, all citing HYPOTHESES.md numbers. | Upgrade from "watch" to "worth naming as a role." `tools/family_run.py` is already shared and already writes the numbers; what is not yet named anywhere shared is *who* periodically reads them and decides continue/pivot/park. | Add one sentence to the Operating model section (roles) or CLAUDE.md rule 3's tool paragraph: "a standing multi-week campaign target (one that has run more than two `family_run.py` families) gets a periodic strongest-model 'consolidator' check-in that rewrites the target's HYPOTHESES.md summary block and decides continue/pivot/park per family with the numbers on record, the way LANE GOLD's cycle-1/cycle-2 consolidator briefs do." Medium priority -- two cycles is more evidence than pass 1 had, but still one lane, one target pair. |
| 7 | **A reading revision made during/after an audit has to be propagated to AUDIT.md and any already-queued second-opinion prompt**, or an outward-facing sentence can keep a word the blind read dropped. | LEDGER.md:813 (V6-MERCY2): "after an audit, a reading change downstream (R7-MREV: Cleues -> Eleues) must be logged into AUDIT.md and any queued SO prompt, or outward sentences keep a word the blind read dropped." | Yes -- general to the verifier/outreach pipeline, not just Mercy. | Fold into CLAUDE.md rule 10 or the Outreach gates paragraph, and the verifier brief template's step 5 (postmortem): "if a reading is revised after AUDIT.md is written (a blind-pass correction, a re-derivation fix), the revision is propagated to AUDIT.md and to any `SECOND-OPINIONS-QUEUE.tsv` row already filed for that target before either is treated as current." |

Minor, not counted above: `2026-09-25-lane-zx2-COMMON.md` lines 14-15 carry the same "Cost and time" paragraph
duplicated verbatim that pass 1 flagged in its predecessor `-lane-zx-COMMON.md` (items 14/15) as "worth a one-line
fix next time that file is touched, not a LEARN finding" -- it was copied forward into the successor file unfixed.
Still not escalating it as its own item; noting only so a future pass doesn't re-discover it as new.

## 2. What we have that they lack

Nothing new found, for the same reason pass 1 gave: `CLAUDE.md`, `.claude/briefs/README.md`, `.claude/briefs/parent.md`,
`LESSONS.md` and now `UPDATES.md` are shared files both accounts read and write, so a genuine owner-account-only
practice would have to live in a ZX2/CX2-specific brief with no counterpart concept on the other side. Checked
`2026-09-25-lane-zx2-COMMON.md` and `-lane-cx2-COMMON.md` line by line against `2026-09-25-lanes-7b-COMMON.md`
(section 1 above already covers the reverse): no rule, tool or convention on the owner side that the other side's
COMMON files or briefs lack an equivalent of.

## 3. Conflicts

None new. The one candidate from pass 1 -- `lanes-7b-COMMON.md:2`'s intake gate triggers at "any brief with a cap
above $3," where CLAUDE.md's own Pipeline "Intake gate" paragraph names no dollar threshold at all -- is unchanged
on both sides as of this pass (checked CLAUDE.md:171 verbatim). As pass 1 found, this is not a real conflict: CLAUDE.md's
unconditional "before any deep work" is already stricter than a $3 trigger (it also gates the workers a $3 floor
would let through), so there is nothing for ledger evidence to adjudicate. Not escalated.

count: 7 portable items (1 tooling-gap diff, 5 clear diffs, 1 role-upgrade diff), 0 reverse-direction gaps, 0 new
conflicts (1 pre-existing non-conflict reconfirmed unchanged).
