# Retrospective j, 25 Sept 2026 (Sonnet, session cap $10/45 min)

Window: LEDGER.md rows after "Retrospective i: 13 ledger rows since the closer of 25 Sept" (line 705) through
now -- 20 rows, git log and ROOM.md from 16:56 UTC to 17:30 UTC. Previous retrospective RETRO-2026-09-25i.md,
both proposals already applied by the parent; not re-proposed here.

## Numbers

20 ledger rows, total worker cost about $101.79, mean $5.09/row. Outcome mix: D 10, D- 3, N 6, F 1, X 0, Q 0.
Share needing a poke/overrunning/failing (D- + F): 4/20 = 20%, all four in LANE R6's Salviati leaf pipeline
(L2 F, L3 D-, L4 D-, plus ZX-349B D- elsewhere). Those five Salviati-leaf rows (L1c, L2, L3, L4, L5) alone cost
$58.10 -- 57% of the window's total spend, on one sub-pipeline.

Board coverage: this window delivered cryptanalysis-flavored results (bBUL closed a rejected claim, ZX-BRO2
placed a bucket-control verdict, Y8's Mercy flag from the prior window moved to a next step), access/recovery
groundwork (GOLD intake, Koehler ARCHIVE.md in flight), and one contribution-shaped item (SO-CLOSE landing
second-opinion files) -- all three kinds represented, none of it from queue rank alone (CX2's 21 check-solved
verdicts and the breadth lane's $1-3 first tests are both flat sweeps, not rank-ordered picks).

## 1. LANE GOLD first cycle: control-first gate not yet tested, cost so far is intake-only

`ciphers/koehler-1944/` and `ciphers/debosnys-1883/` carry no HYPOTHESES.md yet (checked directly: `ls` shows
neither folder has the file). The three family jobs that would exercise the sibling-first/control-gate
discipline -- GOLD-1A (Koehler family A), GOLD-2A (Koehler family B tool + control gate), GOLD-4A (Debosnys
test 1 continuation) -- were all still running at window close (ROOM 17:22, orchestrator's 17:21 check-in).
No family has logged a control number against a target number yet, so there is nothing to check compliance
against; the parent's own LEARN pass (ROOM 17:19) reported the same thing as "no result yet to cite" rather
than a pass/fail. Cost so far is job 0 only: $9.66 across two targets' intake plus corpora (GOLD-0K 3.61,
GOLD-0D 4.10, GOLD-C 1.95), already broken out cleanly in STATUS.md's LANE GOLD handoff -- no diff needed
there, the tracking is already right. Recommend the next retrospective re-check this once a family actually
posts a control/target pair to HYPOTHESES.md, rather than either side treating silence as a clean bill of
health.

## 2. R6 Salviati leaf brief: the get_session self-read line is still live and is the likely cause of 3 of 5 leaf-worker overruns this window

`.claude/briefs/runs/2026-09-25-lane-r6-l-salviati-leaf.md` line 18 reads: "Cost: get_session on yourself
after every batch; estimate pass B at your pass A's cost; at 80 percent of cap push a progress section." This
is the exact pattern the shared README common tail already forbids ("You cannot read your own session cost
reliably... your stall alarm is a WALL-CLOCK box instead," itself written after RETRO-2026-09-24b and
RETRO-2026-09-25h found a dozen+ workers reading "well under cap" while running 1.7-4.6x over) -- but this one
lane-specific brief file was never updated to match, and it is the file actually assigned to L1-L5 this
window. Three of five completed full-leaf jobs overran the $12 cap while presumably trusting their own
get_session reads: L2 interrupted by the orchestrator at 1.37x (session read low enough that nobody self-
stopped), L3 finished at 1.33x with its own status line reading "82% of cap" while already at $12.76 of an
eventual $15.95, L4 at 1.07x. Only L1c ($3.19, a narrow disagreement-only pass) and the gate-FAIL leaves that
stopped early (L1, L5, both under cap) came in clean. The lane orchestrator has already worked around this
twice ad hoc by re-splitting an overrun leaf into a narrower finish-only job (L2 -> L2b for the remaining pass
B lines only, L5 -> L5c for pass C on disagreement rows only) -- both of those narrower jobs landed within
their smaller caps. Propose making that split the default shape instead of a recovery move, and fixing the
cost line to match the common tail:

```diff
--- a/.claude/briefs/runs/2026-09-25-lane-r6-l-salviati-leaf.md
+++ b/.claude/briefs/runs/2026-09-25-lane-r6-l-salviati-leaf.md
@@ -1,4 +1,5 @@
-LANE R6 L1-L5 -- fr2933-salviati-1525: one leaf per worker, box-keyed method (Sonnet, cap $12, box 60 minutes; disk only, no fetches).
+LANE R6 L1-L5 -- fr2933-salviati-1525: one leaf per worker, box-keyed method (Sonnet; disk only, no fetches).
+Split every leaf into two capped jobs, not one: (a) classify + pass A + pass B + gate, cap $8, box 40 minutes;
+(b) settle only if the gate fails or disagreements exceed 10 percent, a separate worker, cap $5, box 25 minutes,
+reading only the disagreement rows from recon_box_<leaf>/ (RETRO-2026-09-25j: L2/L3/L4 ran their leaf as one
+$12/60min job and overran at 1.37x/1.33x/1.07x; the two jobs the orchestrator already had to split off mid-window,
+L2b and L5c, stayed within their smaller caps).
 Common: 2026-09-25-lane-r6-common.md. Intake gate: pasted into your spawn prompt (it must read exit 0; if it does not, stop).
@@ -15,7 +16,8 @@
 disagreement from 5x recrops (settled.tsv with a reason per row), write ciphertext_<leaf>.tsv exactly as ciphertext_f55r.tsv. Fail -> push,
 report and stop, no hand-settling.
-Cost: get_session on yourself after every batch; estimate pass B at your pass A's cost; at 80 percent of cap push a progress section.
+Cost: WALL-CLOCK box only, per the common tail -- never get_session on yourself (RETRO-2026-09-25j: this line
+is the one surviving instance of the pattern the common tail already forbids, still assigned to a live lane,
+and L2/L3/L4 this window overran while reading their own cost as at or under cap). At 75 percent of the box's
+minutes, push a progress section and stop regardless of what a self-read says.
 Notes: do NOT edit NOTES.md (five workers share it). Write your section to `ciphers/fr2933-salviati-1525/leafnotes/<leaf>.md` headed
```

## 3. Breadth lane's $3 cap has no floor for image-fetch + two-pass-transcription first tests

Within-window disk-only first tests were cheap and informative: bBUL $1.01 (closed a rejected claim with a
matched-control MASC run), bMCC $0.97 (moved: above every control on IC and n-gram coverage), bKAL $2.31
(inconclusive but under cap, the images-plus-transcription-check case that stayed cheap because it needed no
fetch of new images). The one image-fetch-plus-two-pass-transcription test carried into this window's ledger
sits just before the retro-i cutoff and shows the opposite: bMOU (moustier-altars) ran $7.61 on the same $3
cap, 2.5x over, explicitly because "a two-pass image job with a subagent does not fit $3" (LEDGER.md line
696). The B2 orchestrator already reacted by re-scoping the next such job (debosnys) to skip the pass-B
subagent, but `breadth.md` itself still states a flat "$3" cap for every first test regardless of shape. Since
LANE GOLD's own Debosnys job 4 is the same shape (fetch images, two blind transcription passes, reconcile)
and is running now at a $4 cap set by hand in its own brief rather than by the shared template, fix the
template so the next breadth worker on an untried image-bearing spec does not need the same $3-cap failure to
happen first:

```diff
--- a/.claude/briefs/breadth.md
+++ b/.claude/briefs/breadth.md
@@ -1,7 +1,10 @@
 # Breadth worker (LANE B)
 
 Spec: `specs/<slug>.json`. Run `cheap_tests_in_order[0]` and its matched control, nothing else. Write both
 numbers into the spec's `cheap_test_done` (with date, method, cost). Judge any candidate plaintext with
-`tools/judge_plaintext.py` and paste its output. Cap $3. Report in five lines; do not run test 2.
+`tools/judge_plaintext.py` and paste its output. Cap $3, or $6 if the first test needs an image fetch plus a
+two-pass transcription (never both a subagent pass B and a fetch in the same $3 job -- RETRO-2026-09-25j:
+bMOU ran $7.61 on the flat $3 cap this way, 2.5x over, while every disk-only first test this window landed at
+$1-2.31). Report in five lines; do not run test 2.
```

## 4. LEDGER.md: N is being used for leaf-transcription gate results, not only for literature/archive searches; two more pre-existing rows named in the brief

The legend defines N as "clean negative (a search that found nothing, done right)." Two rows this window code
a Salviati leaf's pass-agreement gate check as N when the job was a transcription QC step, not a search, and
it did exactly what its brief called for (stop at a gate FAIL, no hand-settling) within its cap: L1 ("Gate
FAILS 286/369 = 77.5%... stopped per brief, no settling", $6.53, no over-cap note) and L5 ("Gate FAIL
261/339 = 77.0%... stopped per brief", $9.61, no over-cap note). Both fit **D** (delivered on brief and
stopped) better than N under the legend as written -- the same "closest fit, say why" gap RETRO-2026-09-25h
already found once for a check-solved verdict wrongly coded B. Separately, the two rows the brief named,
LEDGER.md:490 ("JSTOR runner... three READ rows... none prints or discusses its letter; gate 2 closed",
outcome `A`) and :491 ("LANE R4 orchestrator... Posthius read... No reading reached N3+... Lesson: an
orchestrator at $66 own usage... is the cost of 30-minute check-ins", outcome `B`) are two of the roughly 120
pre-existing non-standard codes `tools/ledger_check.py` already flags as pre-25-Sept debt (confirmed this
pass: 136 non-standard rows total, unchanged in count from prior retros, so this is not new drift). Line 490
is a literature search that came back empty and was reported as such -- the closest fit is **N**. Line 491
delivered several real results (a reading, code recoveries, exclusions) but also carries its own
self-critique about running long -- the closest fit is **D-**. Recoding all 136 is out of scope for this
retrospective (the brief asked only about 490-491); recode those two now since they are named, and add the
clarifying sentence so L1/L5-shaped rows stop landing on N going forward:

```diff
--- a/LEDGER.md
+++ b/LEDGER.md
@@ -21,6 +21,8 @@
 Use only these six codes. RETRO-2026-09-25h found `B` used for a blocked check-solved verdict (PX-CS01,
 LEDGER.md:566) where `F` was already the closest fit and should have been used instead -- the "closest of the
+five, say why" instruction applies to that case; RETRO-2026-09-25j found the same gap for **N**: two rows this
+window (L1, L5) used it for a leaf-transcription pass-agreement gate result, not a search -- a gate FAIL that
+stops per brief, within cap, is **D** (delivered on brief and stopped), not N. N is for a source or archive
+search that came back empty, never for a cryptanalytic or transcription QC step.
 five, say why" instruction applies to that case; `Q` exists for the separate case (two `C` rows this window)
 where none of the five target-outcome codes was ever a fit. RETRO-2026-09-24d found 9 of 28 rows in one window using a bare `A`/`B`/`C`
@@ -487,3 +489,3 @@
-| 25 Sep | JSTOR runner on the owner's machine, pass 2: three READ rows | Fable (desktop) | owner's machine | n/a | A | Mignet 1867, Hauser 1923, Philippson 1908 read online; none prints or discusses its letter; gate 2 closed for Gramont, Danzay, Saxony.
+| 25 Sep | JSTOR runner on the owner's machine, pass 2: three READ rows | Fable (desktop) | owner's machine | n/a | N | Mignet 1867, Hauser 1923, Philippson 1908 read online; none prints or discusses its letter; gate 2 closed for Gramont, Danzay, Saxony. (recoded from A, RETRO-2026-09-25j: a literature search that came back empty)
@@ -488,3 +490,3 @@
-| 24 Sep | LANE R4 orchestrator: recovery, 14:46-18:25 (17 workers, $81.16) | Opus | session_01LrTggxL1PyxmJd6eygqaGY | 76.54 | B | Posthius read (then N0), Bowes codes, Salviati f.54r-v transcribed and two cipher designs excluded by control, 1519 key stalled on pass disagreement, 5549 key sources negative, Seure re-segmented, Mellon MS 29 captured; Marie de Medicis blocked on the fr.3642 key leaf. No reading reached N3+. Lesson: an orchestrator at $66 own usage for seventeen workers is the cost of 30-minute check-ins; 45 minutes is enough at this pace.
+| 24 Sep | LANE R4 orchestrator: recovery, 14:46-18:25 (17 workers, $81.16) | Opus | session_01LrTggxL1PyxmJd6eygqaGY | 76.54 | D- | Posthius read (then N0), Bowes codes, Salviati f.54r-v transcribed and two cipher designs excluded by control, 1519 key stalled on pass disagreement, 5549 key sources negative, Seure re-segmented, Mellon MS 29 captured; Marie de Medicis blocked on the fr.3642 key leaf. No reading reached N3+. Lesson: an orchestrator at $66 own usage for seventeen workers is the cost of 30-minute check-ins; 45 minutes is enough at this pace. (recoded from B, RETRO-2026-09-25j: delivered real results but its own lesson flags running long, closest fit D-)
```

## 5. (not proposed as a diff) L2b/L5c and the two GOLD family workers were still open at window close

L2b, L5c (ROOM done lines exist for both, 17:17/17:25) and GOLD-1A/GOLD-2A/GOLD-4A had no ledger rows yet at
17:30 -- ordinary lag between a ROOM done line and the orchestrator's archive-and-ledger step, not a gap to
fix. Left for the orchestrator's normal housekeeping, not a proposal.

Commit and push this file; ROOM done line to follow.
