# Cross-account learning pass, 25 Sept 2026, 17:18 UTC (parent worker LEARN)

Scope read: the other account's (parent 7b/7c) `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`, its per-lane
`-r6-common.md`, `-cx2-COMMON.md`-equivalent files for R6/V6/B2/GOLD (`-lane-r6-orchestrator.md`,
`-lane-gold-orchestrator.md`), LEDGER.md rows from its restart, RETRO-2026-09-25i.md, ROOM.md 16:00-17:18 UTC, and
STATUS.md's "Parent handoff (cipher-lab-7b" section. Compared against the owner-account side: CLAUDE.md,
`.claude/briefs/README.md`, `.claude/briefs/parent.md`, `.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md` and
`-lane-cx2-COMMON.md`, RETRO-2026-09-25h.md, LESSONS.md, `tools/data/README.md`. `date -u` read 17:13:37 (start)
and 17:18 (this line). Not applied; the parent applies brief/tool-only diffs.

**Headline: propagation is working well.** CLAUDE.md, `.claude/briefs/README.md` and `.claude/briefs/parent.md`
are single files shared by both accounts on `main`, so most retrospective fixes from either side (wall-clock box,
AskUserQuestion ban, no-self-reported-cost, era-matched judge corpora, the mechanical intake gate, the `Q` ledger
code, keeping a lane's STATUS.md handoff current) already apply to both parents the moment either one's
retrospective lands them — confirmed directly: RETRO-2026-09-25i's own Proposal 1 diff to `parent.md` and
Proposal 2 diff to CLAUDE.md rule 3 are already present in the files on disk. RETRO-2026-09-25i independently
found zero target overlap across 28 distinct targets in the two accounts' concurrent lanes. So this pass's
remaining surface is narrow: per-lane dated brief text that has not yet been generalized into a shared file.

## 1. Practices they use that we do not (with a diff, or a reason not to)

| # | Practice | Evidence (file:line) | Portable? | Diff |
|---|---|---|---|---|
| 1 | **Lead-class priority order** for which cheap route to try first on a target: same letter already in print > a sibling's decipherment > an interlinear/marginal gloss > a published key of the same office > cryptanalysis. Stated as an explicit try-in-order list, not just a historical breakdown. | `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md:6` ("item 4"); used directly by three R6 job briefs (`-lane-r6-y1-lagarde-margin.md:3`, `-y6-mercy-transcribe.md:6`, `-p5-costacabral-key.md:3`) and by `-lane-ox-triage.md:145`. Evidence it works: RETRO-2026-09-25h's own table shows OX was the cheapest lane to close a counted result that window ($95/solve, the lowest of three closing lanes) using exactly this ranking to pick van Beuningen from a 12-item pool; today R6's P5/Y1 jobs used it to reach a found-solved/N0 result (Costa Cabral) via the interlinear-gloss rung. | Yes | LESSONS.md section 1 already has the *historical* version of this (a table of what actually broke past solves, same five buckets, no explicit priority ordering). Add one sentence turning it into an operational rule: `LESSONS.md`, end of section 1 — "Before a cryptanalytic campaign, check the four cheaper routes in order: the same letter already in print, a sibling letter's decipherment, an interlinear or marginal gloss, a published key of the same office. Stop at the first that reads (OX, 25 Sept 2026)." |
| 2 | **Wall-clock box doubles as a *minimum* working time**, not only a maximum: "do not stop early with the job undone while budget remains." | `.claude/briefs/runs/2026-09-25-lane-r6-common.md:8` | Yes, but weak evidence (no specific incident cited yet — a stated principle, not a measured fix) | `.claude/briefs/README.md` common tail, after the existing wall-clock sentence: add "The box is also a minimum: do not stop early with the job undone while time remains in it." Low urgency; flag for the parent rather than auto-apply, since the brief's own five proposals were all evidence-backed and this one isn't yet. |
| 3 | **Standing hypothesis-family campaign structure** (LANE GOLD): an append-only `ciphers/<t>/HYPOTHESES.md` per method family (method, parameters, seed, corpus, CONTROL and TARGET numbers side by side, cribs tried, dead ends); a family's control must already read at/above its own gate before any worker runs it on the target ("sibling first"); a periodic stronger-model "consolidator" (~every 4 hours) that rewrites the summary, decides continue/pivot/park per family, and drafts the next round's brief; "park a family, not the target" — a family's negative is logged in the spec's `cheap_test_done` and the lane moves to the next family rather than closing the target. | `.claude/briefs/runs/2026-09-25-lane-gold-orchestrator.md` (whole "How this lane works" section) | Structurally useful for any future standing campaign (Salviati-scale multi-week targets), but **no result yet** — LANE GOLD opened at 16:44 UTC today, nothing ledgered from it as of this pass, so there is no evidence-of-success number to cite, only a clean operationalization of CLAUDE.md rule 3's own matched-control requirement. Report to the parent as a pattern to watch, not a diff to apply now; revisit once GOLD ledgers a family. `grep` confirms `HYPOTHESES.md` does not exist anywhere in the repo yet — genuinely novel to GOLD. |
| 4 | **Model-tiering refinement**: use the strongest model specifically for jobs "where the model is the instrument" (tool design, hypothesis-family design, the consolidator, the sibling-first transfer brief, reading a partial decode against history, crib-proposal steps) and Sonnet for the swarm (script restarts, corpora builds, transcription, search) — a sharper version of CLAUDE.md Usage item 1's "reconciliation of passes, key reading, cipher reasoning and verifier verdicts run on the strongest model." | `.claude/briefs/runs/2026-09-25-lane-gold-orchestrator.md:3`, quoting an owner instruction to that account at their 16:44 UTC clock read ("if meaningful we can have them use Fable instead of Sonnet") | This is an owner directive to *that* account specifically, and it changes model spend — per this brief's own scope ("stay out of ... every rate-limit rule" and "leave anything touching the goal, the spend ... to the owner"), this is **not mine to fold into CLAUDE.md**. Flagging it for the owner-account parent to raise with the person: worth asking whether to generalize into Usage item 1 here too. | No diff applied; report only. |

## 2. What we have that they lack

Little found, because `parent.md`, `README.md`, `CLAUDE.md` and `LESSONS.md` are the same files both accounts
read — anything in them is already mutual. Checked specifically:
- **Rolling quality audit** (parent.md item 8, every 2 hours): already mirrored on their side — their V6 lane
  orchestrator's brief explicitly runs "rolling QA every two hours" too (STATUS.md:239's LANE V6 description).
  Not a gap.
- **Cross-account learning pass itself** (parent.md item 9): mirrored — `.claude/briefs/learn-cross-account.md`
  (pushed by their parent 7c at 17:12 UTC today) is their own version of this same worker, spawned "about every
  three hours." Not a gap; both accounts now run this loop independently, which is fine (parent.md's own item 9
  already anticipates telling the other account's parent, via a ROOM line, about anything *it* hasn't picked up —
  nothing found this pass to post there).
- No other unique owner-account tool, brief section, or convention found that isn't already in a shared file.
  (One unrelated housekeeping note, not a cross-account item: `.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md`
  has its "Cost and time" paragraph duplicated verbatim as both item 14 and item 15 — worth a one-line fix next
  time that file is touched, not a LEARN finding.)

## 3. Conflicts

None found rising to a real rule conflict. The one candidate — `lanes-7b-COMMON.md:4` gates the mechanical intake
check at "any brief with a cap above $3 on that target," where CLAUDE.md's own Pipeline "Intake gate" paragraph
names no dollar threshold, just "before any deep-work brief" — is a tightening, not a contradiction (their
version is strictly stricter), so there is nothing for ledger evidence to adjudicate between; if anything the
$3 threshold is worth the owner-account side adopting for the same reason RETRO-2026-09-25h Proposal 4 gave
(a mechanical gate beats a prose reminder), but that is item 1's shape of change, already covered by CLAUDE.md's
existing gate text applying to "any" cap. Not escalated as a conflict.

count: 4 portable items (1 clear diff, 1 weak-evidence diff, 2 report-only/no-diff), 0 reverse-direction gaps, 0
real conflicts.
