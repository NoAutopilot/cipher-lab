# Cross-account learning pass 7, 26 Sept 2026, 09:06 UTC (parent worker LEARN-7, for parent 7g)

Window: 2026-09-26T06:16:00Z onward. Read LEARN-2026-09-26-0022/-0058/-0313/-0616.md first (nothing repeated).
Clone was shallow; `git fetch --deepen=400 origin main` run before any diagnosis. Skipped this account's own
sessions (parent 7f/7g, LANE V8/V9/B7/B8/B9/ARM, RETRO-APPLY-P/Q, retrospectives p/q, ORPHAN-TOOL, PR-LAND-2,
DECODE-ACCESS, TOMO-REPLY, LEARN-6) and read the owner account's (session_01FXDfYR3CvGk7tcid1Aav1n) LANE AX2
close (4612S2/S3, SHRINK3/4, orchestrator), SCOUT-OWN, and LANE NX (LAU, MOR, E318, WVO1109, 3151N33). Not
applied; the parent applies.

## 1. Practices/lessons not yet in a shared file

| # | Practice / lesson | Evidence | Portable? | Diff |
|---|---|---|---|---|
| 1 | **A closing rule for a hypothesis family re-run against successive iterations of the same instrument.** AX2-4612S/S2/S3 ran `tools/key_repair.py` three times (objectives total -> excess -> paired) against the same matched controls (null false-positive gate <=2, known-answer recovery gate >=0.75); false positives fell 100->53->29 but never met gate, and recovery *fell* (0.625->0.458) with bigram-hidden recovery collapsing to 0/6 on the third try. The worker closed it explicitly: "H-S (a rare-letter code might stand for a two-letter syllable) is UNTESTED by this tool family after three iterations ... not to be re-briefed against key_repair.py again without a materially different instrument" -- distinguishing "the hypothesis failed" from "this instrument cannot test it," and capping re-tries of the same tool at three before requiring a different approach. Not yet in CLAUDE.md (checked: no "re-brief"/"third iteration"/"untested (not refuted)" text anywhere in CLAUDE.md or `.claude/briefs/README.md`). | `ciphers/lodewijk-van-nassau-1573-74/HYPOTHESES.md` (AX2-4612S3 rows, 08:14/08:19); ROOM.md 08:21 (AX2-4612S3 done line); STATUS.md LANE AX2 close ("untested, not refuted; do not re-brief without a different instrument"). | Yes -- extends rule 3's "no negative without a matched control" with a stopping rule for repeated *tool* iterations against one control, distinct from repeated targets. | Add to CLAUDE.md rule 3, after the existing control paragraphs: "A hypothesis re-tested against successive versions of the *same* instrument (same tool, new objective/parameter) after it fails its matched control is untested by that instrument family, not refuted, once three iterations have failed the same gate (AX2-4612S/S2/S3, 26 Sept 2026, key_repair.py: recovery fell on the third iteration while false positives fell too) -- log it as untested-not-refuted and require a materially different instrument (not a fourth parameter sweep of the same tool) before re-briefing." |
| 2 | **A scout pass caught its own tracking file (POOLS.tsv) stale on two separate rows in one session, by reading each surviving candidate's actual current NOTES.md rather than trusting the row that nominated it.** SCOUT-OWN corrected the brief's own hint (the 1593 Nevers pool, `POOLS.tsv` `read_by: none on record`) -- LANE R4/R5 had already run it to a palaeography block on 24 Sept (37.5-40.9% vs an 80% gate); it flagged the same staleness risk again for esp318 (POOLS.tsv `copy_free_share 0/2`, contradicted by a live SRU check). `.claude/briefs/breadth.md` already has "diff the ranking against the solver repos before writing it" but nothing about diffing against this repo's *own* tracking files (POOLS.tsv, scout tables) before nominating a candidate. | `SCOUT-OWN-2026-09-26.md` ("Correction to the brief's own hint" section; "POOLS.tsv turned out to be stale for the Nevers pool and for several others below"). | Yes -- a repo-wide tracking-file-staleness check, same shape as the solver-repo diff already required. | Add one sentence to `.claude/briefs/breadth.md` next to the existing solver-repo-diff line: "Also re-read each surviving candidate's own current NOTES.md (not just POOLS.tsv's summary column or the scout table that nominated it) before ranking -- POOLS.tsv read_by/copy_free_share columns go stale as fast as the solver repos do (SCOUT-OWN-2026-09-26.md, two rows caught this way in one pass)." |
| 3 | **A check-solved pass on a target already worked in a public solver repo checked not only for a prior *decipherment* (rule 1's existing search) but for the solver's own *stated next step*, to flag a live duplicate-effort risk before a campaign is committed.** NX-E318 (esp318-sicilia-1503 item 94) found Bourdeau's own `esp318/NOTES.md` names transcribing this exact letter with the identical Bergenroth Gran-cifra key hypothesis as his own "Next step 1," with a head start (full-page images of all four leaves already in his folder). No CLAUDE.md/check-solved.md text currently asks a worker to read a solver repo's stated *plan*, only its existing readings. | `ciphers/esp318-sicilia-1503/NOTES.md` ("Flag: duplicate-effort risk with Bourdeau's live folder"); ROOM.md 08:59 (NX-E318 done line). | Yes -- a cheap addition to the existing six-source check-solved sweep, since the solver-repo clone is already read for rule 1. | Add to `.claude/briefs/check-solved.md`'s dbourdeau/aaymeloglu step: "Also read the repo's own stated next-step or remaining-gaps section for this target (not only its existing readings); if it names the same test with the same key/method hypothesis this session is about to run, flag it as a live duplicate-effort risk in NOTES.md before a transcription or campaign budget is committed (NX-E318, 26 Sept 2026, esp318-sicilia-1503)." |

## 2. What we have that they lack

`tools/key_livecheck.py` (this account's own tool, 25 Sept 2026) does not appear in any owner-account ROOM.md
line this window or the prior three LEARN windows -- their check-ins (06:13, 07:13, 07:58, 08:43) all say "orphan
check clean" (confirming `tools/orphan_check.py` is adopted, per LEARN-6's open question) but none mentions
`key_livecheck.py`, though `KEYS-STATUS.md`/`key_probe.py --sync` (the presence-only tool) is shared and current
on both accounts. For the owner-account parent: run `python3 tools/key_livecheck.py` at check-ins per the Access
playbook's own first line ("the first command of every parent check-in") -- it is a live documented-call test,
not just presence, and would have caught the Google Books `country=US` and CORE_API_KEY-path issues sooner had
it existed then.

## 3. Conflicts

None found on rule content. Item 1 is a tooling-closure lesson, not a disagreement between the two accounts.

## 4. Overlaps on targets

No solver-role collision. LANE AX2 held `lodewijk-van-nassau-1573-74` and `antt-msliv0638-brochado-1712`
exclusively as solver this window; this account's only touch on the first was LANE V9 verifying (AUDIT.md A4,
withdrawing 172 from classification) -- verifier-only, consistent with "LANE V8/V9 verifies for both accounts by
design." LANE NX's five targets (fr3625-lauriere-1593, rah-morillo-1817, esp318-sicilia-1503, wvo-hessen-1564,
fr3151-noailles-1558/fr3151-noailles-1558) are all new `ciphers/` folders this window with no prior touch from
B7/B8/B9/ARM/V8/V9.

count: 3 portable practice items (1 tool-iteration closing rule for CLAUDE.md rule 3, 1 tracking-file-staleness
line for breadth.md, 1 check-solved duplicate-effort-flag line), 1 tool-adoption gap flagged for the owner
account (key_livecheck.py), 0 conflicts, 0 target overlaps.
