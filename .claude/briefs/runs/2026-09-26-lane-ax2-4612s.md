LANE AX2 AX2-4612S -- lodewijk-van-nassau-1573-74: is 4612 the 1574 table with a few codes standing for syllables? Local key repair with multi-letter values, known-answer control first; 5799 as second target (Sonnet, wall-clock box 75 minutes). Parent: LANE AX2 orchestrator session_016sbTkVT8HGNJ6JGjJNmCuM. Read and follow .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md (read "LANE AX2" for "LANE AX") and the common tail of .claude/briefs/README.md (both override anything below), CLAUDE.md rules 3 (all paragraphs), 4, 7, 10 and Usage 8. First: `date -u`; `python3 tools/room.py --start`; last 30 lines of ROOM.md. Claim with tools/room.py before other work; never AskUserQuestion; no dollar figures for yourself. `pip install numpy` if a tool needs it.

Read: NOTES.md sections AX2-4612 and AX2-5801 in full; reading_4612_v3.txt; ciphertext_4612_v3.tsv, decode_4612_v3.json; ciphertext_5799.tsv, key_5799.tsv and the AX-5799 section; ax4612tr/word_share_check_v3.py; tools/french16_ngram.py (fr16 order-5 model, tools/data/fr16/model_o5.pkl.gz); tools/nomenclator_anneal.py docstring (syllable values; its synth subcommand).

Orchestrator's reading (05:16 and 06:40) that this job tests: under key_full, 4612 v3 shows recurring clusters where a French word should be but one or two letters are missing or replaced, e.g. "yus" for "vous" ("yusest", "yusen", "yusont"), "zur" for "pour" ("zurdnt"), frequent "x". Word share 70.7% sits well above the 60.6% shuffle max but below the 93.2% the known 5811 text gives at the same N. Hypothesis H-S: 4612 uses the 1574 table for most codes, but a small set of codes (plausibly those key_full gives rare letters -- x, y, z, b, k, q, h) stand for two-letter syllables (vo, po, ...) or other letters. AX2-4612's free anneal from a 20%-perturbed start could not repair 5811 (0.70 vs 0.90), so a global anneal is the wrong instrument; use local repair.

Units, in order (stop before a unit that would cross 80% of the box; push after each):
1. Tool (about 20 min). Write tools/key_repair.py (--help, offline test in tools/tests/test_key_repair.py): start from a key TSV; for each code in turn (most frequent first), try its current value, every single letter, NULL, and every one of the 60 most frequent French bigrams and 20 most frequent trigrams from the fr16 corpus; score each choice by the fr16 order-5 model's total log-probability of the whole decoded stream (clear words kept as context); keep the best only if it beats the current value by more than --margin (default 3.0 nats). Sweep rounds until no change or --rounds (default 4). Output: changes table (code, count, old, new, gain), the repaired key TSV and the decoded text. Deterministic, no randomness.
2. Controls FIRST (about 20 min), gates written into NOTES.md before running: take 5811's real ciphertext cut to 4612 v3's N (833 numerals 1-120, clear words kept), decoded with key_full (known reading). (a) Null control: run key_repair from key_full unchanged; count proposed changes (false positives). (b) Known-answer control, 3 seeds: re-encipher 5811's known plaintext with key_full modified so that k=8 randomly chosen codes (among codes used >= 5 times) stand for a French bigram instead of their letter -- re-encode by greedily replacing that bigram in the plaintext with the code wherever it occurs at a position that code's letter held; if that is too awkward, instead SWAP the letter values of k=8 random code pairs and hide two bigram codes -- log exactly which design you used. Gate: across seeds, >= 6 of 8 altered codes recovered to the right value on average AND <= 2 false changes in (a) and on untouched codes in (b). A control below gate = CONTROL BELOW GATE; stop and say so (not a negative).
3. Target (about 15 min, only if unit 2 passed). Run key_repair from key_full on 4612 v3, and on 5799 (ciphertext_5799.tsv; its NOTES say it does not read under key.tsv either). For each: changes table, repaired decode, word share (word_share_check_v3.py method) vs 20 value-shuffles of the repaired key vs the 5811 cut, tools/judge_plaintext.py (specs/lodewijk-4612.json) beside the 5811-cut control, and one order-shuffled target control (key_repair on the target's own tokens shuffled within lines -- the n-gram score depends on order, so this control can differ). Write the repaired keys as key_4612_repair.tsv / key_5799_repair.tsv (grade S only for changes; never edit key_full.tsv or key.tsv) and readings through tools/decode_key.py with new decode json files and --check.
No reading is claimed by you: if a target's repaired decode reaches the 0.85x-of-5811 word-share gate or reads as French prose, write ROOM "flag: <letter> repaired-key candidate ready for re-derivation" -- the orchestrator briefs a fresh instance; do NOT write a "for LANE V8" line.
Section "## AX2-4612S: local key repair with syllable values (26 Sept 2026, LANE AX2)". ROOM done line: "for LANE AX2: key_repair control null <n false> / known-answer <x/8, false y> (gate met/not); 4612 <changes, share, judge>; 5799 <..>; verdict".
Files: tools/key_repair.py, tools/tests/test_key_repair.py; ciphers/lodewijk-van-nassau-1573-74/{ax2_4612s/**,key_4612_repair.tsv,key_5799_repair.tsv,decode_*_repair.json,reading_*_repair*.txt,reading_*_repair*_tokens.tsv,HYPOTHESES.md (append),NOTES.md own section}. No network. No images.
