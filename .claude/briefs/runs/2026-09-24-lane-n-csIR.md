ROLE: CHECK-SOLVED worker, batch IR (LANE N). Read .claude/briefs/check-solved.md and .claude/workflows/check-solved.js (the six SOURCES prompts are your six searches; run them yourself or as up to 4 Sonnet subagents; do not invoke the Workflow tool), and QUEUE.md section "Italian regional state archives, Mantua Modena Turin Genoa Naples (LANE N scout ...)" with its rows and caveats, plus sources/solver-diffs/2026-09-24-lane-n-italy-c.tsv.
Targets (each gets its own verdict and folder ciphers/<slug>/): IR1 Della Torre cipher-book with Hague correspondence 1690-1700 (ASTo); IR2 Viganego cipher with Turin dispatches 1717 (ASGe); IR3 Belmesseri cipher with Naples dispatches 1627-28 (ASMo); IR5 Clerville dispatch with cipher-table 1648 (ASMo); IR8 Taurello dispatch entirely in cipher 1527 (ASMo); IR4 Salvago cipher 1691 (ASGe) if budget allows.
For each: (1) EDITIONS FIRST: is the correspondence edited or calendared (e.g. Carteggi diplomatici of Savoy in 'Biblioteca di storia italiana recente'/Miscellanea di storia italiana; Este dispatches printed in Venturi, Balan, Pastor's appendices, the 'Carteggio degli oratori' series; Genoa in Atti della Società ligure di storia patria; Lasry/Tomokiyo/DECODE for Este and Savoy ciphers); a printed decipherment or a key published beside the dispatches (Thurloe lesson); quote verbatim any sentence in Tomokiyo, a solver README or an edition that names this very item. Use opac.sbn.it (Italian union catalogue) to identify editions and WebSearch/WebFetch for full text; if the edition exists and cannot be read, the verdict is blocked (R8 lesson). (2) Confirm the key and the letters are in the same unit and that the letters are ciphered (recovery = key opens letters; say what is known). (3) Digitisation: is any image online (ASTo and ASMo have partial digital collections; check the host's viewer)? copy-free or copy-order; for copy-order draft REQUEST.md (archive, unit, what to photograph: the cipher sheet and every ciphered page; no personal data). Verdict per target in NOTES.md with the date, every query and URL, the status word (open / found-solved / blocked / offline-only). Update each QUEUE.md row with an appended "check-solved 24 Sept 2026: <verdict>" note.
Hosts (yours alone in LANE N): archiviodistatotorino.beniculturali.it, archiviodistatogenova.beniculturali.it, archiviodistatomodena / Archivio Segreto Estense online inventories, opac.sbn.it (>=2 s). No archive.org (another LANE N worker holds the slot), no HTRC, no Google Books, no Gallica.
Cap: $8. For each target whose verdict is open (stage 2), append a ROOM line via room.py: `nomination: ciphers/<slug> | copy-free|copy-order | kind recovery|cryptanalysis|contribution | <why, ten words>`.

COMMON RULES (LANE N worker; parent LANE N orchestrator session_01W4z8JhXJYHRjorPC1Qkpiy; lane goal: stage-2 nominations, at least half copy-free):
- First: `date -u` (use that clock for every timestamp; never estimate it); `python3 tools/room.py --start`. Read CLAUDE.md in full (rules 1, 3, 5, 10; the good-citizen rule; the Access playbook), LESSONS.md sections 1-2, .claude/briefs/check-solved.md (the Thurloe, Raince, Bowes and M9 lessons), and the last 30 lines of ROOM.md. Claim with `python3 tools/room.py "LANE N <role> (Sonnet, <your session id if known>)" "claim: <rows/target> -- files: <paths>"` BEFORE other work. Done line the same way when you stop; `flag:` for anything the lane orchestrator must see.
- Git: commit by explicit path only (never `git add -A`); push with `python3 tools/room.py --push <paths>` (rebase-and-retry loop, keeps both sides of ROOM conflicts). ROOM.md is append-only; on a conflict in QUEUE.md keep both sides. Never rewrite history, never a pull request.
- Hosts (good-citizen rule): one request at a time per host, >=1.5 s apart; descriptive User-Agent `cipher-lab research script (contact via repository)` unless the playbook says a site needs a browser UA; on 429/403/Cloudflare/challenge stop that host, log it in NOTES.md and ROOM.md, one retry after a pause at most. Use ONLY the hosts your brief names plus: WebSearch/WebFetch, github.com (shallow clones of dbourdeau/cyphersolver and aaymeloglu/unsolved-ciphers, grep only), and the files already in the repo (sources/cryptiana/, sources/solver-diffs/). NEVER gallica.bnf.fr or archivesetmanuscrits.bnf.fr (LANE G), never Google Books (LANE V). archive.org: only if your brief gives you the IA slot. DECODE (de-crypt.org): only the DECODE worker logs in; everyone else uses sources/decode/ files and the solver repos' DECODE sweeps. Report your request count per host.
- Quality over quantity. A nomination later found solved costs more than none. Before writing "open": (1) the recipient's and sender's standard printed edition and the calendar for the date (Raince lesson); (2) a printed or interlinear decipherment in the same edition or the catalogue note (Thurloe lesson); (3) quote verbatim any sentence in Tomokiyo, a solver README/NOTES or an edition that names this very letter (M9 lesson). If the standard edition or calendar for the date cannot be located or read, the verdict is `blocked` (say which edition), never `open`, and no nomination line is posted (R8 lesson, 24 Sept 2026).
- Rule 10: never write new, unpublished, unread, first, never printed, previously unknown. Report what was found and where it was not found. Do not classify novelty. Never promote to the board, never solve or transcribe beyond confirming ciphertext is present, never start a target your brief does not name. No copy orders or payments: a target that needs a copy gets ciphers/<slug>/REQUEST.md (no personal data).
- Credentials: never print them; no unfiltered env/printenv/set, no curl -v / --trace / set -x on a credentialed call; test presence with `test -n`.
- Usage: Sonnet; cap stated in your brief (check your own cost; stop at the cap even if unfinished, and push what you have). At most 4 subagents at once, on Sonnet. Scripts read, models judge: fetch once to disk, grep, give the model the hits.
- Finish: commit, push; ROOM `done` line that ends with `nominations: <n> (<k> copy-free)` when your brief is a check-solved; then your final reply is ONE paragraph, first line the answer (per-row verdicts or new row ids), then files touched, request counts per host, cost. Then stop.
