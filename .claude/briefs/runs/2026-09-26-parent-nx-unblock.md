PARENT WORKER NX-UNBLOCK (Sonnet, cap USD 6, box 60 minutes, network allowed for catalogues and free scan hosts; no logins, no paid orders). Parent: the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n). Context: NEXT-STEPS.tsv carries 16 rows whose blocker is needs-image, needs-edition or needs-person (run `python3 tools/next_steps.py --check` first; then `grep -P "\t(needs-image|needs-edition|needs-person)\t" NEXT-STEPS.tsv`). Every one of them is a target a solver cannot start until the supply side hands it something. This job is that supply pass, and it follows the owner's stuck rule (.claude/briefs/README.md common tail): before a row stays blocked, one materially different route is tried.

Read first: CLAUDE.md, .claude/briefs/README.md (common tail), .claude/briefs/parent.md "Account roles" and "Desk", the 16 rows' NOTES.md last paragraph and any REQUEST.md.

For each of the 16 rows, in the order the file lists them, at most six minutes each:
1. Look for a free route the earlier worker did not try: TNA Discovery (is the piece digitised? SP 77, SP 78, PRO 30 series pages are sometimes on the "Digital microfilm" or State Papers Online-free tier); Gallica full-text and the BnF archives et manuscrits for Clairambault and Clair. volumes (a different volume of the same series is sometimes digitised); archive.org, HathiTrust full-view, Google Books full-view, e-rara, MDZ, Persée, Riksarkivet bildvisning (RA rows), Tiroler Landesarchiv online finding aids, RODERIC/Teseo (lope-hurtado), university repositories. Search by shelfmark, by the sender's name plus year, and by the edition title. Record every URL tried and what it returned, in the folder's NOTES.md under a heading "## NX-UNBLOCK (26 Sept 2026)".
2. Outcome A, free scan or edition found: download images into the folder's images/ (rule: cite the URL and the page numbers; never more than 40 images; run tools/file_shrink_guard.py if the folder has it) or record the edition's exact pages, and write the next step a solver can run. The row then reclassifies itself when tools/next_steps.py reruns.
3. Outcome B, nothing free: write or refresh REQUEST.md (institution, exact reference, folios, what is needed and why, in the institution's form of words) and leave the row blocked.
4. The TNA rows (pro30-shaftesbury-1682, sp77-nicholas-1659, sp78-cesy-1628, sp78-doncaster-1621, sp78-yorke-1749, and whitworth-1707 if it is TNA) become ONE consolidated order: a new file outreach/tna-page-copy-batch.md listing every piece and folio with TNA's per-page price from its current price page, a total, and a one-sentence ask. ONE ASKS.md row for the batch, status backlog (the desk is capped at five; the parent promotes it). Never send anything. Never name the owner (rule 9).
5. needs-person rows (lope-hurtado-1522, naf14913-rousseau-venice-1743, thurloe-printed): if the person-step is really a JSTOR or HathiTrust read, convert it into a JSTOR-QUEUE.tsv row (format: target, query, requested, status=queued, hits empty; two query families per CLAUDE.md Verifier brief 2(g)) or a LOCAL-QUEUE.tsv row (format: id, kind, target, instruction, status=queued, result empty; next free id) instead of leaving it on a person. Coordinate with the parallel QUEUE-FILL worker by posting your ROOM claim naming the rows you will add before adding them; append rows only, whole-file read-modify-write, and check `git diff --stat` shows only added lines on those files.

Never touch status.json, BUDGETS.md, LEDGER.md, N4-READINGS.md or another folder's reading files. Post a ROOM claim first (folders named), a done line at the end: first line counts (found free / request written / converted to a queue row / unchanged), second line the three best finds with URL, third line the ASKS row number for the TNA batch and the total price. One UPDATES.md row. Commit per folder or per three folders, `git pull --rebase origin main` before every push, keep both sides on shared-file conflicts, no conflict markers. Stop at the box even mid-list and say in the done line which rows were not reached.
