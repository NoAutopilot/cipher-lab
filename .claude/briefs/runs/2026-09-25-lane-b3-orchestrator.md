LANE B3 ORCHESTRATOR (breadth; successor to LANE B2, whose handoff is STATUS.md "LANE B2 handoff (session_01NS12APP1R55K6TGZrBbP97), 25 September 2026, 19:03 UTC"). Opus. Cap $25 own usage; hand off at $25 or 300k context, keeping a "LANE B3 handoff" section in STATUS.md current from the first worker report (parent.md). Started 25 Sept 2026 by parent 7c (session_01H4AdRiu9g44F1oCBVNzbpx). Window allowed; `rejected` anywhere stops everything.

First: `date -u`; `python3 tools/room.py --start` (it prints UPDATES.md's last rows and the NEAR.md targets); read CLAUDE.md 3a and rule 5's near-solves paragraph, `.claude/briefs/breadth.md` (intake step and the $3/$6 cap rule), `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`, the LANE B2 handoff and its spec table, NEAR.md, UPDATES.md; `python3 tools/room.py --digest "2026-09-25 18:00"`. ROOM claim "LANE B3: breadth, B2's approved next tests, cylob, specs 21-30".

Jobs, in order (Sonnet workers, one host per worker, wall-clock box in every brief, cost read by you with get_session):
1. If LEDGER.md has no row for bMLH (session_01PmhUa6TvpUCGWqnG13xbSh), read its ROOM done line and ledger it with get_session cost, ASSIGNMENTS done line, archive.
2. Approved by the parent (19:10 UTC): bullet-tuscany-1944 test 2 (short-key Vigenere/Caesar with QM and 605YZ/FF as indicator candidates, crib list, N=44 control; cap $3) and rubin-1953 test 2 (`tools/family_run.py specs/rubin-1953.json --family masc` at N=305 with the English control, gate 0.6; cap $3). Both through tools/family_run.py where a family fits; both numbers side by side in the done line and in the spec.
3. mccormick-1999: the NEAR.md row's named step, `tools/family_run.py --family homophonic` with the spec's K and the same three controls (English, vowel-dropped English from tools/data/en_vdrop, shuffled), cap $3; update the NEAR.md row and status.json `near` (touched time, numbers) whatever the result; a PASS goes to LANE V6 by ROOM line, never called solved.
4. cylob-c1995 first test from `.claude/briefs/runs/2026-09-25-lane-b2-cylob-c1995.md` (cap per the breadth rule).
5. Ten more specs for UNSOLVED-SURVEY.md ranks 21-30 (Sonnet, cap $6), each with a judge block that names `min_word_cover` and a wired language; then check bSPEC's ten (ranks 11-20) for the same and repair the blocks that lack it (B2 handoff, last paragraph).
6. First tests on the new specs, one worker each, in rank order, until your cap.
Rules: a first test never runs without the intake step (breadth.md); an image-and-two-pass test is $6 and never a subagent pass B plus a fetch in one $3 job; a control-backed gap or a non-test negative is `partial` and a NEAR.md row (rule 5), never closed-negative; every done line carries target and control numbers; scienceblogs.de one worker at a time; Gutenberg at most 30 fetches per session. After every worker: LEDGER row (get_session cost), ASSIGNMENTS, archive_session, the spec's `cheap_test_done`, the UNSOLVED-SURVEY.md row, ROOM line. Report to the parent only through ROOM lines ("for the parent:" / "flag:").
