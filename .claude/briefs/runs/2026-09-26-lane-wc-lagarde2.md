WC-LAGARDE2 (LANE WC worker; lane orchestrator session_01PgyvWdtkFieEXUYD1Whbyx; parent session_01FXDfYR3CvGk7tcid1Aav1n). Sonnet. Cap USD 5 of usage (stall alarm), wall-clock box 45 minutes from your first `date -u`. Units: spec (1) + two family runs (control+target each, about 10-15 min each, serialized, nothing else running in the background); stop before starting a unit that would cross 80 percent of either figure.

The owner's stuck-rule try for la-garde-1577 (README common tail, 18:53): WC-LAGARDE's negatives (homophonic/monoalphabetic in both sign modes, periodic) were real tests at the measured error. The materially different family is the one the sign system itself suggests: numerals 1-24 carrying overlines and loop marks, i.e. base code = consonant/letter and the mark = a following vowel (syllabary), or signs that stand for whole words (wordcode).

Read first: .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md (read "LANE AX" as "LANE WC"), the .claude/briefs/README.md common tail, ciphers/la-garde-1577/NOTES.md sections L2, L4 and WC-LAGARDE, `python3 tools/family_run.py --help`, and one existing spec for the format (specs/README.md). Files you may touch: specs/la-garde-1577.json (new), ciphers/la-garde-1577/ (HYPOTHESES.md, families/, NOTES.md), ROOM.md.

Step 0: `python3 tools/intake_gate_check.py la-garde-1577`, paste. Nonzero: stop and flag.
Step 1: write specs/la-garde-1577.json from ciphertext_6179_v2.tsv + ciphertext_6467_v2.tsv as WC-LAGARDE left them (source and date stated; tokens keep the overline/loop mark as part of the sign, e.g. `7^`, as solve_l2.py's --mark-signs mode does; a row_pattern for the syllabary control), language fr (16th-c. French, judge corpus fr16), constraints: 239 pooled tokens, two letters, 6467 margin note "Justifier le faict du grand" beside run 1 as an unconfirmed gloss (not a crib). `cheap_tests_in_order`: syllabary, wordcode.
Step 2: `python3 tools/family_run.py specs/la-garde-1577.json --family syllabary --seeds 3 --measured-error 0.23 --param err=0.23 --gate 0.6 --label "WC-LAGARDE2 stuck-rule try"` (0.23 = mid of WC-LAGARDE's measured 20.1-25.5 percent pass disagreement; if the tool refuses --measured-error with --param err, use whichever it accepts and say so). Paste the control and target rows. CONTROL BELOW GATE is "not a test at this N and error", recorded as such, never a negative.
Step 3 (only if time remains under the 80 percent line): same with --family wordcode (--param codes=marked err=0.23).
Step 4: a target that beats its control by a reproducible margin (all seeds) is a candidate, not a reading: stop and flag "flag: for LANE WC: la-garde syllabary/wordcode beats control" with the numbers and judge line; do not decode further. Otherwise write "WC-LAGARDE2 (26 Sept 2026)" in NOTES.md with both rows per family and the one-sentence status (open; if a control was below gate, say the family is untestable at 239 tokens and this error, which is the pooled-length limit, not a negative), and fill the spec's cheap_test_done with the two numbers.

ROOM done line "done: for LANE WC: WC-LAGARDE2 --"; final reply one paragraph per COMMON.
