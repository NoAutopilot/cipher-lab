LANE N3 ORCHESTRATOR (nominations, copy-free only; successor to LANE N2 from its 14:16 handoff). Opus. Cap $35 own usage; hand off at $35 or 500k context.

Owner decision, 24 Sept 2026 14:50 UTC: the seven-day window reads `allowed_warning` (resets Sat 26 Sept 13:00 UTC) and the owner chose to keep running a focused set. `allowed_warning` does NOT stop you; `rejected` on any session does: interrupt every worker, write the handoff, stop. Check `rate_limit_info` on yourself at every check-in (every 20-30 min via send_later). At most 4 live workers, all Sonnet (claude-sonnet-5).

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md, LESSONS.md, BUDGETS.md, STATUS.md sections "Parent handoff" and "LANE N2 handoff" (its lesson: state-archive nominations return copy-order rows; go where the images already are); `python3 tools/room.py --digest "2026-09-24 14:00"`. ROOM claim line "LANE N3 orchestrator: copy-free nominations (cyphersolver re-diff, Trew, Bavarikon 2, IIIF collections)". Workers get `.claude/briefs/runs/2026-09-24-lane-n2-COMMON.md` (your session id in place of N2's) plus a job brief you write to `.claude/briefs/runs/2026-09-24-lane-n3-<job>.md`.

Jobs (copy-free means the images are online now; anything needing a copy order is logged in QUEUE.md as copy-order and not pursued):
1. **scCS2, cyphersolver site re-diff** (Sonnet, cap $6): fetch once, spaced, `https://dbourdeau.github.io/cyphersolver/{writeups,catalogue,keys}.html` (the parent fetched them 24 Sept 14:40 UTC; do not fetch more than these three plus at most 15 write-up pages), text via tools/html2text.py to the scratchpad (do not commit the pages: the footer carries a contact address). Diff against sources/solver-diffs/2026-09-23-queue-vs-solver-repos.tsv and QUEUE.md: (a) write-ups posted 20-23 Sept not in our diff; (b) his "not solved" and "· partial" write-ups whose material is online (IIIF/Gallica/DECODE image) as recovery candidates, with the unread remainder and the key he names; (c) catalogue "attempted, open" rows not in QUEUE.md; (d) keys.html: a TSV of published keys (name, letter read, source) for the recovery lane. Output sources/solver-diffs/2026-09-24-cyphersolver-site.tsv and new QUEUE.md rows marked `copy-free` or `copy-order`. Never copy his text into a target folder beyond a citation (CC BY 4.0: cite him by name and URL).
2. **scTREW, the Trew letter collection** (Sonnet, cap $6): the Erlangen Trew Briefsammlung (~19,000 digitised letters; bavarikon and trew-letters.com per ciphers/trew-posthius-1614-18/NOTES.md "Access"): search the catalogue for cipher terms (Chiffre, Ziffer, cifra, geheim, Geheimschrift, Zahlen, in numeris, notis) through bavarikon's API or search URL, one host, spaced; list every hit with object id, correspondents, date, IIIF link; a look at the leaf for each to confirm ciphertext. Rows to QUEUE.md as copy-free.
3. **check-solved** on every copy-free row from jobs 1-2 (Sonnet, cap $5 per batch of 4), the six-source blind protocol, stage 2 only from its verdict; ROOM "nomination: ... | copy-free | kind ..." for each open row so LANE R4 can pick it up.
4. If workers are free: one more digitised-collection sweep the LANE N2 handoff names as untried (its "next sources" list), copy-free only.

After every worker report: LEDGER, ASSIGNMENTS, archive, ROOM. Never promote to the board, never solve. Handoff: "LANE N3 handoff" at the top of STATUS.md's handoffs (nominations per hour, copy-free count, dead ends, next sources), LEDGER row for yourself, ROOM done line, stop. Rule 10 wording throughout.
