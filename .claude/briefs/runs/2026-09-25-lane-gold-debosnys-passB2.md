JOB GOLD-4D: Debosnys blind pass B on the split inventory, reconcile, and the homophonic control. Sonnet (claude-sonnet-5). Stop and push at $7 or 60 minutes, whichever first. Lane: LANE GOLD orchestrator session_01DKDynpdEwZK5EokxtjCM3P. Written 25 Sept 2026 18:27 UTC.

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md` (at most ONE Sonnet subagent, for pass B only, TSV to disk per page, commit per page), `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), `ciphers/debosnys-1883/NOTES.md` sections GOLD-4A, GOLD-4B and GOLD-4C, `glyphs/inventory.png`/`inventory.tsv`, `tools/reconcile_passes.py --help`, `tools/family_run.py --help`. Claim in ROOM.
Intake gate (live, 18:27 UTC): debosnys-1883: open (line 1) -- edition/page or full-text-search citation found within 6 lines  (exit 0)

State: GOLD-4C split the sign inventory by eye to 160 ids (36 of them composite base+stroke ids) and rebuilt pass A from `glyphs/box_labels.tsv` (confidence H 36.6 / M 48.5 / L 14.9 percent, one pass). Its c4 diagnosis: 76 percent of GOLD-4A's disagreements were inventory confusion, so a second pass on the split inventory is now worth running.
Do:
1. Pass B, blind: one Sonnet subagent transcribes all four cryptograms line by line from the line strips (glyphs/strips or crops) using ONLY inventory.png/inventory.tsv as reference, never passA.tsv, box_labels.tsv or ciphertext_draft.tsv; `passB2.tsv` per page to disk, committed per page. Composite signs: transcribe as the composite id AND record base + stroke parts in a column, so a base+mark recount can be made from either pass.
2. Reconcile with `tools/reconcile_passes.py` against the GOLD-4C pass A (normalise both to one id convention first, 7b COMMON point 3). Report agreement per cryptogram at full id and at base-sign level (composites collapsed to base). Settle the disagreement rows on the image yourself only for cryptogram 1 (the shortest); count the classes as GOLD-4C did.
3. If agreement at full-id level is >= 80 percent on a cryptogram, write that cryptogram into `ciphertext.txt` from the reconciled draft (replace B2's draft section for it, with a header line: source passes, agreement, date); otherwise leave ciphertext.txt and write `ciphertext_draft2.tsv`. Recompute N, K, IC per cryptogram at full id and at base level with scripts/compute_ic.py's controls.
4. Matched control (rule 3), only on a cryptogram written to ciphertext.txt: `python3 tools/family_run.py specs/debosnys-1883.json --family homophonic --control-only --seeds 3 --gate 0.6` at that cryptogram's N and K (the spec's judge corpus is fr19). Report the control number. Do not run the target in this job; the control number decides whether the next job runs it.
Write a "GOLD-4D, pass B2" section in NOTES.md with every number. Done line: agreement per cryptogram (full id / base), cryptograms written to ciphertext.txt, homophonic control a/b at N,K, no cost figure. Rule 10 wording; no decoding.
