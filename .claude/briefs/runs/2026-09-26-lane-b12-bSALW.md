bSALW -- fr2933-salviati-1525: word-level nomenclator scorer as the next design family (DSN sec. 5 b; LANE B12 job 3). Opus. Cap USD 10, box 75 minutes from your first `date -u`. Disk only, no hosts, no image reading, at most one Sonnet subagent (none expected).

Read first: `.claude/briefs/runs/2026-09-26-lane-b11-common.md` (governs; B12 for B11), CLAUDE.md rule 3 in full (measured-error sentence; judge-floor sentence "score the family's own decode of the shuffled target through the same judge"; the repeated-attempt non-test shape; the unbalanced-class paragraph) and rule 5; ciphers/fr2933-salviati-1525/NOTES.md sections "## DSN" (its sec. 5), "## DSN2", "SALV-DIAG", bSALC's pass-C section, bSALR/bSALR2/bSALI; HYPOTHESES.md; specs/fr2933-salviati-1525.json (2,839 tokens, 236 types, 381 sign runs, row_pattern, constraints); `python3 tools/family_run.py --help`; tools/families/nomenclator.py and tools/families/syllabary.py (interface, control construction, `--measured-error`, `--shuffle-target`).

Intake gate (orchestrator ran it 17:40 UTC): `fr2933-salviati-1525: open (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

State: the syllabary ladder is closed at the measured 6.4% error (DSN2 V1/V2/V3 control-backed negatives, DSN regular weaker negative, DSN irregular a non-test; bSALI 16:17). Remaining untested design (DSN sec. 5 b): marked types (and possibly some bases) stand for whole words or names -- a nomenclator inside the sign runs -- which the letter anneal cannot score. Constraint to respect: the plain-Italian boxes between runs are NOT transcribed (spec alphabet line), so there is no context word on disk; runs begin and end at word boundaries (plain boxes are whole words).

The job: build and run one word-level family, control first.
 1. Tool: a new family `tools/families/wordcode.py` (or a `--param` mode of nomenclator.py if that is genuinely smaller) registered in tools/family_run.py, with `--help` text, an offline test in tools/tests/ (under two minutes), and a SYSTEM.md row in the same commit (`python3 tools/system_map_check.py` must pass). Design: each sign type maps to either one Italian letter or one Italian word (a code type, from a candidate list: the it16 corpus's commonest words plus proper-name / title slots as wildcards `<NAME>`); a run is a word sequence with word boundaries at both run edges; score = Italian letter-n-gram for letter stretches + a word-unigram/bigram LM for code words + a segmentation term; Gibbs/anneal over the type -> value map, restarts. Which types may be codes is a --param (e.g. `codes=marked` for every marked type, `codes=topk:N`), because the target's 894 marked tokens are the natural candidates.
 2. Control (rule 3): a synthetic from the held-out it16 text laid on the target's own row_pattern and run lengths (381 runs, same N and K within 10%), the same code/letter design (the same share of code-bearing types, same token share), plain stretches withheld exactly as the target's are (no context), 6.4% error injected via `--measured-error 0.064`. Recovery reported per class (letter types vs code types) as well as blended (unbalanced-class paragraph). Then a 0%-error ceiling row (control-only) so the headroom is on file. If the control mean is below 0.6 at 6.4%: CONTROL BELOW GATE, target not run, and the result is a non-test at this N -- log it, do not tune the same knob more than once (repeated-attempt shape), and name the missing ingredient (almost certainly the untranscribed plain context) as the next step.
 3. Target only if the control clears the gate: 3 control seeds, 1 target seed (2 if budget allows, for cross-seed agreement), then `--shuffle-target 1` scored through the same judge (spec judge block: Italian, it16) -- a PASS on the shuffled decode voids the judge as a gate for this family.
 Units and pricing: one tool build (about 30-40 min, USD 4-5 at the Opus rate of bSALR, 2.36 for three variants) plus one control+target variant (about 10-15 min, USD 2.5-3) plus the ceiling and shuffle rows (USD 1-1.5). Stop before starting a unit that would cross 80% of the cap (USD 8) or the box (60 min); run everything in the foreground, one at a time (GOLD-K3). Check your own cost with get_session on yourself after each unit.

Record: family_run writes HYPOTHESES.md rows; add a "## bSALW: word-level nomenclator family (26 Sept 2026, LANE B12)" section to NOTES.md: the design in five lines, the control table (0% ceiling, 6.4% per seed, per-class), the target score against the control's, judge lines (target and shuffled), Italian word hits, and the verdict in rule-3 words (control-backed negative / non-test / reading candidate). A target that beats its control and PASSes the judge with a non-PASS shuffle floor: write reading_bsalw.txt + a regeneration script, grade S/M per token, and put "flag: salviati reading candidate" in ROOM -- the orchestrator arranges print_check and the rule-7 re-derivation.

Do not edit NEAR.md, LEDGER.md, ASSIGNMENTS or status.json (orchestrator). Write only tools/families/<new>.py, tools/family_run.py (registration), tools/tests/<new test>, SYSTEM.md (one row), ciphers/fr2933-salviati-1525/{HYPOTHESES.md,NOTES.md (your section),families/,control_curve.tsv,reading_bsalw.*}. Commit by explicit path; `git diff --stat` then `python3 tools/room.py --push <paths>` after the tool lands and after each run. ROOM claim first; done line at most 600 chars: "wordcode ctrl <mean> (ceiling <x>; letters <a> / codes <b>) / target <score/sym> vs ctrl <score/sym>, judge <verdict>, shuffle <verdict>". Final reply at most five lines. Report what was found and where it was not found; do not classify novelty; never write solved, new, first or unpublished.
