JOB bSZM (LANE B7, Sonnet, cap USD 3, wall-clock box 35 min, no hosts): szembek-bk1560, merge the three leaf transcriptions (leaf65/, leaf66/, leaf67/ from bSZL65/66/67) into a grade-C key and reading.

Intake gate: exit 0 (pasted in 2026-09-26-lane-b7-szleaf.md; re-run and paste).
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b7-common.md` (it governs); NOTES.md sections "Leaf 65/66/67 transcription"; `python3 tools/interlinear_align.py --help`.
Orchestrator note (05:25, from the three leaf reports): the system is LETTER per code, not nomenclator -- about 20 distinct two-digit codes, vowels homophonic (a, e), 'm' written in clear inside cipher words (bSZL66: 0.918 consistency vs shuffle 0.341 on 87 letter-level glosses). bSZL65 read ~2 letters/code and bSZL67 left most gloss_above blank: their groups.tsv is per-group only where a run's length matched, so the aligner over whole gloss words (plain_raw) is the primary route; use --floor 100 (every code one letter) first, then check whether clear letters inside a code run (m) are tokens in cipher_raw -- if a leaf wrote them as separate clear tokens, --clear-consumes; if the leaf folded them, say so. Abbreviated gloss words (Princeps written short, -us endings dropped) will show as unaligned letters: count them, do not force them.
Steps:
1. Concatenate the three pairs.tsv into ciphers/szembek-bk1560/pairs.tsv and write ciphertext.txt (cipher_raw lines in leaf order, as transcribed, with leaf:line ids).
2. Key, two routes, compare them: (a) direct -- from groups.tsv, each code's gloss_above values with counts (key_direct.tsv); (b) aligned -- `python3 tools/interlinear_align.py align pairs.tsv align.tsv key_aligned.tsv --floor <F>` with F chosen from the leaf workers' note on whether a code stands for a word or letters/syllables (try both 100 and the code range's top+1 if unclear; say which reads better and why). Agreement between (a) and (b) per code.
3. Control (the spec's test 1): consistency = share of codes with >= 2 glossed occurrences whose occurrences all agree (and the token-weighted rate), real vs 1000 shuffles of gloss-to-occurrence (seed fixed; mean, 95th pct, and the real value's percentile); per-class breakdown for codes seen 2, 3-5, 6+ times (CLAUDE.md rule 3's unbalanced-class paragraph).
4. key.tsv + decode.json (layout: tools/tests/decode_configs/), `python3 tools/decode_key.py ciphers/szembek-bk1560` then `--check` exit 0. Grades: C where the code's meaning comes from the gloss at that occurrence or a consistent gloss elsewhere; M where the gloss disagrees across occurrences; U unglossed. Give counts.
5. Judge: `python3 tools/judge_plaintext.py specs/szembek-bk1560.json --file <reading>` -- first check what tools/data/la holds (era, register); if it is classical/medieval rather than 18th-c. chancery Latin, say so next to the line. Paste the output. Update the spec's cheap_test_done with real vs shuffle numbers and the grade counts.
6. Search log for the verifier: tools/print_check.py ciphers/szembek-bk1560 with a phrases.txt of 8-10 distinctive decoded phrases (and any names: Pergenbeio, Sityka, Serbanum, Burgundus) -- the tool's 'no hits' is a search result, never a novelty verdict.
If consistency is high and clearly above the shuffle 95th pct, ROOM: "for LANE V8: ciphers/szembek-bk1560 reading ready" naming print-check.tsv and NOTES.md. ROOM done line <= 600 chars with real vs shuffle consistency and C/M/U counts. Do not edit NEAR.md. Report what was found; do not classify novelty. Final reply at most five lines.
