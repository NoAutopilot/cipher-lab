ROLE: DECODE worker dcA (LANE N2), the lane's only session on de-crypt.org. Read sources/decode/NOTES.md (login, thumbnails-only finding, tools/decode_list.py is login-free for RecordsList by status), STATUS.md "LANE N handoff" DECODE paragraph, QUEUE.md section "DECODE non-decrypted records with images".
JOBS, in order:
1. For LANE W (ROOM 09:51, worker K): search DECODE **Decrypted** records (x_status=1; use tools/decode_list.py, extend it with an option if needed, not a private copy) for Huntington / Blathwayt / Marchmont / Ripperda / Keene / Patiño, and Madrid, Seville, Port Sainte-Marie / Puerto de Santa María 1728-29. Log every query and the count in ciphers/huntington-blathwayt-madrid-1728/AUDIT.md under a dated "DECODE Decrypted search (LANE N2)" subsection (append only; do not change the N-classes), and post a ROOM line beginning "for LANE W:" with the result.
2. DC11-DC20 (QUEUE.md rows; mostly Modena/Milano ids 1121-1167): open each record's RecordsView and DocumentsList (one login, tools/decode_browser_login.js, NODE_PATH=$(npm root -g)); record status, attached documents and their tags ([transc], [decrypt], [key], [pub]), uploader comments and any bibliography. Write one line per row into the QUEUE.md DC table's status (keep other text) and a TSV sources/decode/dc11-20-documents-2026-09-24.tsv. No verdicts: 'Partially decrypted' or a [decrypt]/[key] document = not a candidate; say so.
3. Florence cluster: the 32 census rows with holder "Florence State Archives", shelfmarks Dieci_di_Balia_Responsive_* (ids 3758-3789; list them from sources/decode/records-non-decrypted-2026-09-24-diff.tsv). Same capture as job 2 (status, documents and tags, comments, bibliography, uploader/project, language, page count, date) into sources/decode/florence-dieci-2026-09-24.tsv, plus a short paragraph in sources/decode/NOTES.md naming any publication or project the records cite. Do not download images beyond what the page view pulls; do not transcribe.
Host: de-crypt.org only (one login for the whole session; if it fails once, stop and say so), >=1.6 s apart, at most 150 requests; scrub the account name from anything saved. Plus WebSearch for a named publication only. Cap: $5. No nomination lines (you are not check-solved).

COMMON RULES (LANE N2 worker; parent LANE N2 orchestrator session_01DfQyAaXAgcoFZMAbBTGj4f; lane goal: stage-2 nominations, at least half copy-free):
- First: `date -u` (use that clock for every timestamp; never estimate it); `python3 tools/room.py --start`. Read CLAUDE.md in full (rules 1, 3, 5, 10; the good-citizen rule; the Access playbook), LESSONS.md sections 1-2, .claude/briefs/check-solved.md (the Thurloe, Raince, Bowes and M9 lessons), and the last 30 lines of ROOM.md. Claim with `python3 tools/room.py "LANE N2 <role> (Sonnet, <your session id if known>)" "claim: <rows/target> -- files: <paths>"` BEFORE other work. Done line the same way when you stop; `flag:` for anything the lane orchestrator must see.
- Git: commit by explicit path only (never `git add -A`); push with `python3 tools/room.py --push <paths>` (rebase-and-retry loop, keeps both sides of ROOM conflicts). ROOM.md is append-only; on a conflict in QUEUE.md keep both sides. Never rewrite history, never a pull request.
- Hosts (good-citizen rule): one request at a time per host, >=1.5 s apart; descriptive User-Agent `cipher-lab research script (contact via repository)` unless the playbook says a site needs a browser UA; on 429/403/Cloudflare/challenge stop that host, log it in NOTES.md and ROOM.md, one retry after a pause at most. Use ONLY the hosts your brief names plus: WebSearch/WebFetch, github.com (shallow clones of dbourdeau/cyphersolver and aaymeloglu/unsolved-ciphers, grep only), and the files already in the repo (sources/cryptiana/, sources/solver-diffs/). NEVER gallica.bnf.fr or archivesetmanuscrits.bnf.fr (LANE G), never Google Books (LANE V). archive.org: only if your brief gives you the IA slot. DECODE (de-crypt.org): only the DECODE worker logs in; everyone else uses sources/decode/ files and the solver repos' DECODE sweeps. Report your request count per host.
- Quality over quantity. A nomination later found solved costs more than none. Before writing "open": (1) the recipient's and sender's standard printed edition and the calendar for the date (Raince lesson); (2) a printed or interlinear decipherment in the same edition or the catalogue note (Thurloe lesson); (3) quote verbatim any sentence in Tomokiyo, a solver README/NOTES or an edition that names this very letter (M9 lesson). If the standard edition or calendar for the date cannot be located or read, the verdict is `blocked` (say which edition), never `open`, and no nomination line is posted (R8 lesson, 24 Sept 2026). When a free PDF or image set is the source, open every page of it before calling a letter open: WVO PDFs bundle the printed edition page (Jan van Nassau 5218/5222, 24 Sept 2026).
- Rule 10: never write new, unpublished, unread, first, never printed, previously unknown. Report what was found and where it was not found. Do not classify novelty. Never promote to the board, never solve or transcribe beyond confirming ciphertext is present, never start a target your brief does not name. No copy orders or payments: a target that needs a copy gets ciphers/<slug>/REQUEST.md (no personal data).
- Credentials: never print them; no unfiltered env/printenv/set, no curl -v / --trace / set -x on a credentialed call; test presence with `test -n`.
- Rate limit (parent, 06:34 UTC): the shared five-hour window must last to 11:10 UTC; work lean, no exploratory fan-out, no subagents unless the brief names them.
- Usage: Sonnet; cap stated in your brief (check your own cost after every major step; at 80 percent of the cap push what you have, write the remaining steps as one line in NOTES.md, and stop -- two LANE N workers ran 59 and 86 percent over on 24 Sept). At most 4 subagents at once, on Sonnet. Scripts read, models judge: fetch once to disk, grep, give the model the hits.
- Finish: commit, push; ROOM `done` line that ends with `nominations: <n> (<k> copy-free)` when your brief is a check-solved; then your final reply is ONE paragraph, first line the answer (per-row verdicts or new row ids), then files touched, request counts per host, cost. Then stop.
- LANE N2 additions (24 Sept 2026): (a) a printed edition can print the cipher passage in clear without marking it (Jan van Nassau 5218/5222 in Groen; Oxenstierna 1888): read the edition's text of the letter against the cipher leaf's extent, and search the national historical journal for five years after the edition; (b) a free PDF or scan can bundle the printed edition -- open every page before 'open'; (c) DECODE 'Partially decrypted' and an attached [transc]/[decrypt]/[key] document are hard filters: check DocumentsList before calling a DECODE record open; (d) copy-free means the holding archive or a library serves a readable full-size image of the leaf online now (DECODE's 200-px thumbnails do not count); name the image URL in NOTES.md; (e) scouts do not post nomination lines, only check-solved workers do, and only for stage-2 verdicts.
