PARENT WORKER RETRO-APPLY-LEARN2 (Sonnet, wall-clock box 40 minutes, no subagents, no network except git). Job: apply the brief- and tool-only diffs from two cross-account learning passes, LEARN-2026-09-25-2118.md (items 1-7) and LEARN-2026-09-25-2133.md (items 1-2), for the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n). Read CLAUDE.md, then both LEARN files in full, then each target file before editing it.
Apply, keeping every edit minimal and in the file's existing voice:
1. LEARN-2118 item 1: tools/room.py push path -- before pushing after a rebase or merge, compare the '## ' headings of STATUS.md (and QUEUE.md if it exists) against the pre-merge copy; if a heading vanished, refuse to push and print which section was lost (the same shape as the existing ROOM.md shrink guard). Add an offline test under tools/tests/ that exercises the guard on a temp repo or on a fixture; run python3 -m pytest tools/tests -q (or the repo's test command) and keep it green.
2. LEARN-2118 item 2: .claude/briefs/parent.md -- one sentence on create_session prompts: lead with the brief file path and a plain job description, pass source_url, and keep rule text in the brief (two workers stopped on dense inline prompts, LEDGER.md rows 799/818/819).
3. LEARN-2118 item 3: LESSONS.md (or the common tail in .claude/briefs/README.md, wherever blind-transcription guidance lives) -- a blind brief names images/manifest.json's iiif_lines block as off-limits, or hands the worker a stripped manifest.
4. LEARN-2118 items 4 and 5: tools/family_run.py docstring and --help -- the subprocess timeout must cover control plus target (size it to both, or run the target in its own call); a newly added control option gets a single-seed profile sanity comparison against the target before the seed battery. If a --timeout-per-stage option is a one-screen change, add it with a test; otherwise document only.
5. LEARN-2118 item 6: .claude/briefs/README.md common tail -- a short paragraph describing the consolidator pattern for standing campaigns (periodic stronger-model pass that rewrites HYPOTHESES.md and decides continue/pivot/park per family with a pre-written decision rule), pointing at .claude/briefs/runs/2026-09-25-lane-gold-consolidator-c2.md as the worked example.
6. LEARN-2118 item 7: CLAUDE.md Outreach gates paragraph (or rule 10) and the verifier brief template's postmortem step -- a reading revised after AUDIT.md is written must be logged into AUDIT.md and every queued second-opinion prompt before any outward sentence.
7. LEARN-2133 item 1: tools/iiif_lines.py docstring -- keep segments under the 2500 px default and never re-stitch them; the clair349 numbers (39.3/42.0 percent stitched vs 70.7 percent native segments) as the evidence line.
8. LEARN-2133 item 2: CLAUDE.md rule 7 (language judge) -- when a judge FAILs close to the gate and the document carries its own known-genuine period text (a marginal or interlinear gloss), score that text through the same judge as a calibration point and report it beside the verdict; a candidate that beats the gloss and every shuffle control is 'judge cannot decide', not a negative.
Never touch BUDGETS.md or any rate-limit rule. Add one UPDATES.md row per instituted change (the file's existing format, clock-read time). Post a ROOM claim line first and a done line at the end as `parent worker RETRO-APPLY-LEARN2 (Sonnet, <session id>)` with tools/room.py; commit, fetch and rebase, push (never force). Never call AskUserQuestion.
