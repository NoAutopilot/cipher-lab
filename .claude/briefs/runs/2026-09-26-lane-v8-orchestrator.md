LANE V8 ORCHESTRATOR (verification and rolling QA; successor to LANE V7, whose handoff is STATUS.md "LANE V7 handoff (session_018VkFfDWY4drC9a9aozmop9)", closed 02:54 UTC 26 Sept 2026). Opus. Cap $50 own usage; hand off at $50 or 400k context (write your "LANE V8 handoff" section in STATUS.md from the first worker report; keep its heading fixed). Started 26 Sept 2026 at the 03:05 UTC clock read by parent 7e (session_01SvjMDFfJZJ3uK47RYxAQrM). Authorised by CLAUDE.md rule 10 (a verifier is a separate session from the solver and assigns the N-class in AUDIT.md), the verifier brief template in CLAUDE.md "Workers", and parent.md duty 9 (rolling QA every two hours). Window allowed; `rejected` anywhere stops everything.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md rules 3, 7, 10 and the verifier template, `.claude/briefs/verifier.md`, `.claude/briefs/runs/2026-09-25-lane-v7-orchestrator.md` (your pattern) and `2026-09-25-lanes-7b-COMMON.md`, the LANE V7 handoff, NEAR.md, UPDATES.md tail; `python3 tools/room.py --digest "2026-09-26 02:40"`. ROOM claim "LANE V8: verifiers and rolling QA, V7's successor".

Jobs, in order:
1. V8-QA6 now: `.claude/briefs/runs/2026-09-26-lane-v7-qa6.md` is ready (window from 01:42 UTC 26 Sept; Sonnet, cap $5, 40 min); change its addressee to LANE V8 and spawn it. Then a QA every two hours (QA7 about 05:40, window from QA6's start), each a copy with the window changed. An item it flags counts toward no total until its owner clears it; a flag on a result already reported to the owner goes to the parent by ROOM line.
2. Verifiers for every "reading ready" line in ROOM.md addressed to LANE V7 or V8, from either account (LANE AX on the owner account is finishing a merged period key on the Lodewijk van Nassau letters 5797, 4610, 4611, 4616 with a fresh re-derivation next; expect "for LANE V8: reading ready" from it; LANE B6 may post matignon-mayenne-1586). Opus, cap $10, 30-60 minutes, the CLAUDE.md verifier template with the source families listed; a search log per family; the N-class and key source (`ours`, `period`, `published`) and `text: known` where the plaintext is in print, in AUDIT.md; corrections to any over-claiming sentence; a JSTOR-QUEUE.tsv row per JSTOR query. For a Nassau-pool reading the families are Groen van Prinsterer's Archives (whole volumes, both series), Glawischnig 1973, the WVO catalogue notes, Tomokiyo's Nassau pages, both solver repositories, and a phrase search on the decoded text through tools/print_check.py. Post the class to both parents ("for the parent: ... for the owner-account parent: ...") with the safe sentence.
3. Second-opinion pull requests: any open PR titled [SO-...] above number 15 (`git ls-remote origin 'refs/pull/*/head'`): mark the SECOND-OPINIONS-QUEUE.tsv row posted, citation-check it with a Sonnet worker ($4), route GitHub writes (closing) through a short-lived worker.
4. Mercy 1648 gate-2 closer: only when the owner answers JSTOR-QUEUE rows 80-83 and the Lonchay-Cuvelier IV page read lands (owner's machine); until then nothing.
Rules: verifiers never decode; rule 10 wording only (never new, first, unpublished; an `ours` key at N3 or better is said as "read with a key recovered by our own cryptanalysis"); no AskUserQuestion. Spawn with create_session giving source_url https://github.com/NoAutopilot/cipher-lab and source_revision main explicitly, prompt opening with the job brief file and the one-line job. Job briefs to `.claude/briefs/runs/2026-09-26-lane-v8-<job>.md` before spawning; ledger every report with the get_session cost; ASSIGNMENTS line; archive after ledgering; check-ins with send_later (45 minutes; sooner when a verifier runs), re-armed before ending each turn; "for the parent: ..." in ROOM.md with cost and context at every check-in. Hand-off: LANE V8 handoff section, ledger yourself, ROOM handoff line, stop.
