LANE R4 ORCHESTRATOR (recovery; successor to LANE R3 from its 13:52 handoff, and heir to LANE G3's Salviati/M36 work). Opus. Cap $50 own usage; hand off at $50 or 600k context.

Owner decision, 24 Sept 2026 14:50 UTC: the seven-day window reads `allowed_warning` (resets Sat 26 Sept 13:00 UTC) and the owner chose to keep running a focused set. So: `allowed_warning` does NOT stop you; `rejected` on any session does: interrupt every worker, write the handoff, stop. Check `rate_limit_info` on yourself at every check-in (every 20-30 min via send_later). At most 5 live workers; Sonnet (claude-sonnet-5) for passes and image work, Opus only for reconciliation, key reading and cipher reasoning. Page-scaled caps (BUDGETS.md).

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md, LESSONS.md, BUDGETS.md, STATUS.md sections "Parent handoff", "LANE R3 handoff", "LANE G3 handoff"; `python3 tools/room.py --digest "2026-09-24 14:00"`. Post a ROOM claim line "LANE R4 orchestrator: recovery (Posthius, Salviati M35, M36 key, 5549 key source, Bowes codes)". Workers get `.claude/briefs/runs/2026-09-24-lane-r3-common.md` (with your session id in place of R3's) plus a job brief you write to `.claude/briefs/runs/2026-09-24-lane-r4-<job>.md`.

Jobs, in priority order (spawn 1-3 at once, then the rest as they close):
1. **Posthius to Eysenmenger 1614 and 1618** (`ciphers/trew-posthius-1614-18`, stage 2, kind recovery, IIIF images already on disk): the 1614 letter carries its SALUTEM keyword alphabet on the leaf and the 1618 letter two key blocks in two hands. Sonnet capture worker: crops with tools/iiif_lines.py from the images on disk (no refetch unless a crop needs native resolution; host: the Erlangen/bavarikon IIIF server named in NOTES.md, good-citizen rule), two blind passes of the cipher lines and of the key blocks, reconcile. Then an Opus solver ($6): key.tsv from the leaf's own key, decode.json, `tools/decode_key.py --check`, grades H for key-read tokens; report what was found and where not found; ROOM "for LANE V4: reading ready".
2. **M35 fr.2933 Salviati 1525** (`ciphers/fr2933-salviati-1525`): follow the G3 handoff item (1): revise the atlas to split eps/e and h/bh, two atlas passes of f.54r against the 80% gate, disk only (Opus lead, Sonnet passes, cap $6). If the gate passes, an Opus solver ($8): try the keys already in the repo and in Tomokiyo's/Lasry's published tables for 1525-26 papal and French ciphers (Raince 1526 key in ciphers/dupuy452-carpi-1520, the Gramont 1530 key, the Montmorency keys), then a homophonic anneal with a matched control (rule 3) only if no key fits.
3. **M36 fr.5761 1519 election key** (`ciphers/fr5761-election-key-1519` or the folder G3 created; see G3 handoff item 2): resume the interrupted transcription from what is on disk; atlas for the alphabet signs, two atlas passes of f.104, key.tsv as a dataset (Sonnet, cap $5). A key is a result row (dataset) even with no letter read yet; then grep QUEUE.md and Bourdeau's catalogue text for 1519 letters it might open.
4. **5549 key source** (`ciphers/jan-van-nassau-1572-75`): the R3 handoff item 1: one-page fit of 5205 and 5209 with j6/fit.py (Sonnet, cap $3). Positive → Opus reader ($6). Negative → one line in NOTES.md, stop.
5. **Bowes numerical name-codes** (`ciphers/bowes-walsingham-1583`, NOTES.md "Specialist reply"): Opus, cap $5, disk and the printed sources already in the folder (corpus/, the Letter-Book and calendar texts): collate every numerical code across both letters with its printed context, fix 189 = Montrose (grade C, from Tomokiyo's check), propose identifications for 870, 149, 19, 29, 85 and the rest with the 1910 Calendar as the check; grade C only where the calendar names the person at that place in the sentence, else M. No image fetch.

Do not touch other targets. After every worker report: LEDGER row (with session id), ASSIGNMENTS row, archive, status.json results row when a reading or dataset lands, ROOM line. Readings go to the parent for verification ("for LANE V4" in ROOM); do not verify. Handoff: "LANE R4 handoff" at the top of STATUS.md's handoffs (results table, open items, lessons), LEDGER row for yourself, ROOM done line, stop. Rule 10 wording throughout.
