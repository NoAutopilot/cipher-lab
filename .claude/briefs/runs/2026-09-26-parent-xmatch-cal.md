PARENT WORKER XMATCH-CAL (Opus, cap USD 8, box 60 minutes, no network except git). Parent: the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n); context: OPTIMIZATION-2026-09-26.md section (d). tools/key_crossmatch.py (LANE KX, 25 Sept; KEY-CROSSMATCH.md describes the repair pass and its open problem: the judge's pass_real gate cleared for none of the 35 own-text pairs, including two independently verified readings, august-van-saksen-1561-64/key_53 and oxenstierna-gustav-adolf-1632/key.tsv, so the tool's verdicts are not trusted and it has not run since). Job: make the cross-match trustworthy and runnable nightly.
1. Read KEY-CROSSMATCH.md in full, tools/key_crossmatch.py, tools/judge_plaintext.py, CLAUDE.md rule 3 (controls, calibration gate: the RETRO-APPLY-T rule that a family's own decode of shuffled text must fail before a judge PASS is used), and the status.json rows of the verified readings (every row with nclass N3 or N4 after two audits -- these are the positive controls).
2. Calibrate: for every verified reading whose key and ciphertext are on disk, decode with its own key and record the judge's scores; for every such pair, decode with 20 shuffled-value keys and record the null scores; set the cross-match gate at the point that admits every verified own-text pair and rejects at least 99 percent of shuffled decodes (say the margin); if no single gate does both, say so with the numbers and use the tightest gate that admits the verified pairs, flagging the false-positive rate. Write the calibration table to KEY-CROSSMATCH-CAL.tsv and the method to a new section of KEY-CROSSMATCH.md.
3. Rerun the cross-match with the calibrated gate over every key table against every ciphertext on disk it was not built for; write KEY-CROSSMATCH.tsv fresh; list in KEY-CROSSMATCH.md every pair that clears the gate with its scores and the shuffled-null margin, and say for each whether it is a known relation (same office, sibling) or a new lead; post new leads "for the parent" in ROOM one line each.
4. Make it nightly: a `--since <git ref>` mode that runs only ciphertexts and keys changed since that ref, a one-line command the parent can put in a routine, and a ROOM line format for hits. Tests in tools/tests/ (offline, fixtures) for the gate and the --since selection.
Never edit a reading, a key or status.json. Post a ROOM claim first and a done line at the end (first line: the gate and how many verified pairs it admits; second: new leads) as `parent worker XMATCH-CAL (Opus, <session id>)` with tools/room.py (two positional arguments); add your LEDGER.md row; UPDATES.md row per change; commit by explicit path, fetch and rebase, push (never force). Never call AskUserQuestion.
