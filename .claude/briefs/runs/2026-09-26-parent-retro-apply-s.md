PARENT WORKER RETRO-APPLY-S (Sonnet, cap $6 of usage, wall-clock box 40 minutes, no subagents, no network except git; the container is a depth-50 clone). Job: apply the brief- and tool-only proposals of RETRO-2026-09-26g.md (retrospective s, 26 Sept 2026 10:51 UTC) for parent 7g (session_01Jb4UHR8so3yBUG1NTMdf6C, "LIVE parent 7g · Orchestrator 8"). Authorised by CLAUDE.md "Improvement loop" (the orchestrator applies changes that only touch briefs, tools or workflows; anything that changes the goal, the spend or the owner's asks goes to ASKS.md). Written 26 Sept 2026 at the 11:31 UTC clock read.

First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker RETRO-APPLY-S (Sonnet, <session id>)`; read CLAUDE.md, RETRO-2026-09-26g.md in full, then each target file before editing it. Edits minimal, in the file's voice. Apply each of the four proposals as the retro's diff, in order: (1) the dense-leaf transcription pricing table (USD 0.4 a cipher line all in, USD 0.1 a witness-settled row, from LANE B9's own figures) into .claude/briefs/README.md's common tail or the breadth brief as the retro names, so the next per-unit brief starts from it. (2) tools/leaf_pool_gate.py admits a leaf by the relabel-control cosine with M tokens held per token (a code attested only in M rows stays out of the shared key) instead of holding the whole leaf on M-share, with the new test case in tools/tests/test_leaf_pool_gate.py the retro specifies; keep the docstring's gate list in step. (3) A design-family control's injected-error level must reach the target's measured transcription error before a FAIL is logged as a negative: CLAUDE.md rule 3 one sentence next to the transcription-noise paragraphs (check the PX-BRODEC and Salviati paragraphs first and cite SALV-DIAG's 8-9 percent crossover against the atlas passes' 14.6 percent), plus the mechanical half the retro proposes in tools/family_run.py (a --measured-error argument, or a spec field, that the control sweep must exceed, else the FAIL is written as "non-test at measured error"), with a test. (4) tools/key_livecheck.py retries once after a pause before reporting a present/works flip, and records "transient" when the retry recovers, with a test. One UPDATES.md row per instituted change (`By` = "RETRO-APPLY-S for parent 7g", clock-read time). Never touch BUDGETS.md or rate-limit rules; never touch status.json or any ciphers/ folder. Run every tools/tests/ file you touch directly (pytest is not installed). Commit by explicit path, `python3 tools/room.py --push <paths>`; ROOM done line `done: RETRO-APPLY-S: N of 4 items applied (...), tests <pass/fail>, UPDATES rows M`; one-paragraph reply; stop. Never call AskUserQuestion; never print or commit credentials.
