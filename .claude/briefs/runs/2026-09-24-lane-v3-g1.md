# LANE V3 worker G1: residual-family sweep on the Lodewijk and Saxony N4 items (Sonnet, cap $5)

Common rules: `.claude/briefs/runs/2026-09-24-lane-v3-common.md` (claim, hosts, commit, ROOM done line `for LANE V3:`).
You are a searcher, not a verifier: you close named gaps in two N4 decision tables and log what you find. You do not
change any N-class. If you find the text or the decipherment of any listed item in print, stop, post
`flag for LANE V3: <item> possible prior print <citation>` and report; an Opus verifier decides.

Items (read each target's AUDIT.md principal-families table first, then its NOTES.md for dates and incipits):
- ciphers/lodewijk-van-nassau-1573-74: WVO 4610 (3 Jun 1573), 4611 (2 Jul 1573), 4616 (12 Apr 1574), Lodewijk to Orange.
- ciphers/august-van-saksen-1561-64: WVO 53 (Orange to August, Breda 24 Oct 1561, postscript), 57 (August to Orange,
  Torgau 18 Nov 1561, enclosure), 126 (Orange to August, 16 Sept 1564, postscript).

Gaps to close, in this order:
1. Daussy 2007, "Louis de Nassau et le parti huguenot" (HAL; Lodewijk table: "unread"). Find it on hal.science (API:
   `api.archives-ouvertes.fr/search/?q=...&wt=json&fl=uri_s,title_s,fileMain_s`), fetch the PDF once to scratch, text it
   (`pdftotext` if present, else python), grep for chiffre, chiffr, 1573, 1574, juin, juillet, avril, Orange/Orenge and
   the incipits of the three letters. Report whether it quotes or prints any of 4610/4611/4616 and at which page.
2. Poullet/Piot, Correspondance du cardinal de Granvelle, t. IV (1572) and t. V (1573-74) (Lodewijk table: "not
   covered"). Spanish-side intercepts of Lodewijk's letters are the risk. Routes: Google Books API (yours; `filter=full`
   first, then any view, `&key=$GOOGLE_BOOKS_KEY&country=US`, >=3 s apart) for the volumes, then
   `tools/gbooks_search_within.py VOLUMEID` with Lodewijk/Louis de Nassau/chiffre/the dates; HathiTrust Bibliographic API
   plus HTRC Extracted Features (`tools/htrc_ef_headwords.py`) are LANE W2's hosts: post `for LANE W2: V3 G1 using
   catalog.hathitrust.org + data.htrc.illinois.edu, <=10 requests` before touching them. archive.org: claim the item id
   in ROOM first. If neither volume is reachable, log the family as unreachable with every route tried.
3. Kluckhohn, Briefe Friedrich des Frommen I (Saxony table: IA bub_gb_3N1SAAAAcAAJ, OCR Fraktur-garbled, "a negative is
   weak"). Get a clean text: Google Books search-within on the same volume (find its Google id from the IA metadata
   `source` field or the API), queries Oranien, Oranien Zettel, Chiffre, Ziffer, Torgau, Breda, 1561 November/October,
   plus a control that must hit (e.g. Heidelberg). Report hits with page numbers and whether any concerns 53 or 57.
4. OpenAlex and Semantic Scholar: one request each at start; both answered 429 all day (OpenAlex budget resets at
   midnight UTC). If still 429, log unreachable and move on; do not retry.

Write: append a section "Residual families (LANE V3 G1, <date -u>)" to each target's AUDIT.md with a table (family /
searched or unreachable / route and queries / result), and rows to OPEN-INDEX-RESULTS.tsv for the scholarship queries
(same columns as existing rows). Touch nothing else except JSTOR-QUEUE.tsv if you form a JSTOR query. Report requests per
host. Stop at the cap.
