LANE R4 WORKER J -- M35 SALVIATI f.54v-f.57v: box-keyed transcription of the other seven cipher leaves (Sonnet, cap $8; disk only).
Target: ciphers/fr2933-salviati-1525. Common rules: .claude/briefs/runs/2026-09-24-lane-r4-common.md. Read NOTES.md sections "Atlas revision
and script-assisted passes", "Pass B finished and gate" and "Solver (LANE R4 I)". Method exactly as f.54r (B, G): natives on disk
(images/canvas56-59_native.jpg), the shared atlas (glyphs/), script classification of every box (the f54r_boxes.tsv procedure; reuse
pass2_long.py / the atlas tool, do not rewrite), per-line strips with box ids, then two confirm/correct passes: pass A by you, pass B by
one Sonnet subagent blind to pass A. Per leaf: commit boxes + pass A + pass B, compare by (line, pos) excluding both-plain, report
agreement per leaf. Gate per leaf 80% on base codes; settle disagreements from the strips; write ciphertext_<leaf>.tsv as ciphertext_f54r.tsv.
Stop at the cap with whatever leaves are done (in order f.54v, f.55r, ...). Report per-leaf agreement and total sign tokens with marks.
No solving. NOTES.md section "Leaves f.54v-f.57v (24 Sept 2026, LANE R4 J)". ROOM done: "for LANE R4: salviati leaves <n> done, <tokens>".
