YX-BRO79 (Sonnet, wall-clock box 45 minutes; at most 2 subagents). Target: ciphers/antt-msliv0638-brochado-1712. Parent: LANE YX orchestrator session_01PHaEdHeQY2FtMHLo9yGeoe. Kind: recovery (key source `period`). Read the lane COMMON, .claude/briefs/solver.md, and in NOTES.md the sections PX-BROGLYPH, PX-BRODEC, PX-BRODEC2 and PX-BRODEC3 (lines ~677 to the end), plus the LANE PX handoff in STATUS.md. Scripts to reuse: scripts/06_decode_agreement.py, scripts/11_loo_control.py, scripts/12_gloss_diff.py.

The one named next test (LANE PX): appendix entry Carta 79 on leaf m0287 scores 26.7% leave-one-out against a noise-matched control of ~93% on 15 aligned positions. That is an entry problem, not generic noise. The gate: pooled leave-one-out real 80.2% vs noise-matched synthetic 91.0% (5 seeds), gap 10.8 against a 10-point band.
1. Hand-check Carta 79 against its image. Use images/ if m0287 is on disk; else fetch it once from DigitArq (tools/digitarq_fetch.py; docId 7049343e7af44ba7b4ec18e74e1b2cd7; >=3 s apart, at most 10 DigitArq requests). Crop the Carta 79 cipher line and its Deciffrada line at native resolution; read every token of both, and compare with ciphertext_appendix.tsv and plaintext_appendix.tsv rows for Carta 79. Also check whether the cipher run and the Deciffrada line on the leaf actually belong together (a mis-paired entry, a skipped line, an abbreviation, the decipherment paraphrasing rather than spelling out, or the entry belonging to a different letter), and whether the same fault affects Carta 80 and the m0284 Passage 2a and Carta 15 (the other drags named in PX-BRODEC3; check them only by the same crop method, only if time allows).
2. Normalize both texts to one spelling convention before any diff (RETRO-2026-09-25h proposal 2: period abbreviations such as q./q.mos/hé, doubled consonants, u/v, i/j, y/i, tilde forms) and write the convention into NOTES.md. Correct the transcription tsv rows the image proves wrong (grade them H only if read from the image), never silently: each change gets a row in a corrections table in NOTES.md (leaf, entry, position, old, new, reason).
3. Re-run scripts/11_loo_control.py (and 06) with the same seeds and the same noise model. Report real vs control pooled and per entry, before and after, side by side (rule 3). If the entry is mis-paired rather than misread, the honest fix is to exclude it from both real and control, and say so; do not tune the control.
4. If the gap closes to within the 10-point band: run PX-BRODEC's steps 3-5 exactly as in .claude/briefs/runs/2026-09-25-lane-px-brodec.md (letter 134 decode with per-token grades C/M/I/U, spec specs/antt-msliv0638-brochado-1712.json, judge output pasted verbatim, fresh-instance re-derivation by one subagent that sees only ciphertext_appendix.tsv, plaintext_appendix.tsv and body_ciphertext.tsv). If the gap does not close: stop, write the numbers, and name what would close it.
NOTES.md gets one section "## YX-BRO79 (25 Sept 2026)". Status line stays `partial` unless the letter-134 reading passes judge and re-derivation. Touch only ciphers/antt-msliv0638-brochado-1712/**, specs/antt-msliv0638-brochado-1712.json and ROOM.md. Push after each step. Report what was read and where it was not found; do not classify novelty; write "cost: see the lane ledger".
