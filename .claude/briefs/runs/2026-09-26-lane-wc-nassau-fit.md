WC-NASSAU-FIT (LANE WC worker; lane orchestrator session_01PgyvWdtkFieEXUYD1Whbyx; parent session_01FXDfYR3CvGk7tcid1Aav1n). Sonnet. Cap USD 8 of usage (stall alarm), wall-clock box 60 minutes from your first `date -u`. Unit-priced: at most 8 candidate letters; per candidate one PDF fetch plus one Sonnet transcription call on line crops of its densest cipher page (about USD 0.8-1.2 per call, the J6/R4-C rate); stop before starting a candidate that would cross 80 percent of either cap or box.

Read first: .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md (read "LANE AX" as "LANE WC"; lane goal "WVO circles: one control-backed recovery test per circle, close or promote"), the .claude/briefs/README.md common tail, and in ciphers/jan-van-nassau-1572-75/NOTES.md the sections J5S, "J6 new key", "5205 and 5209 fitted" and "J7 crib cryptanalysis". Files you may touch: ciphers/jan-van-nassau-1572-75/ (j6/, images/, NOTES.md) and ROOM.md only. Do NOT touch lodewijk-van-nassau-1573-74 (the 4612/5799 pool is closed to this lane).

Why: WVO 5549 (Jan van Nassau to Willem, Dillenburg, 21 Nov 1573) has a 539-numeral body in the "verendertte Instruction oder Ciffer" that nothing reads; Groen Suppl. no.45 prints those numerals raw. J7 showed crib cryptanalysis cannot reach it at this length (matched control 10.9 percent), so only a sibling in the same key (a letter with a period decipherment or Groen's clear text) can open it. Already fitted and excluded: 5550, 5552, 5557, 5575, 5797, 5204, 5205, 5207, 5208, 5209, 5213 (j6/fit.tsv).

Step 0: `python3 tools/intake_gate_check.py jan-van-nassau-1572-75`, paste the output (WC-MAP read exit 0). Nonzero: stop and flag.
Step 1 (candidate list, no images): from the WVO database (resources.huygens.knaw.nl/wvo, the route in sources/wvo/NOTES.md; >=2 s apart, descriptive UA, at most 40 requests on this host in total) list every letter dated 1 Sept 1573 - 31 Dec 1574 that (a) is between Willem and Jan van Nassau in either direction, or (b) is from or to Jan, Lodewijk, Hendrik or Willem's secretaries in that window, and whose Opmerkingen mentions cijfer/chiffre/cijferschrift/ontcijferd, or whose record you cannot rule out from the fields. Also check sources/wvo/cipher-letters-2026-09-24.tsv. Drop the eleven already fitted. Write j6/candidates_wc.tsv (briefnr, date, direction, correspondents, opmerkingen excerpt, printed edition if WVO names one, pdf url, why included). Name every unseen witness the records list (minutes, copies, KHA or Marburg holdings without a PDF).
Step 2 (fit test, per candidate in date order, nearest 21 Nov 1573 first): fetch the PDF once to scratch (not committed), render the densest cipher page at 300 dpi, cut line crops under 2500 px with `python3 tools/iiif_lines.py --image <page.png> --out <scratch>/crops_<briefnr>` and paste the command before the subagent call, give one Sonnet subagent only those crops and ask for the numerals in order; write j6/fit_<briefnr>.tsv in the existing format and run `python3 j6/fit.py`. Commit a 150 dpi JPEG of the fitted page to images/ with a manifest.json entry. Gate, written before the run (the R4-C threshold): positive = >=6 of top-10 values shared with the 5549 body AND share>99 within 0.10 of the body's 0.24 AND cosine >=0.7. Controls (rule 3; a shuffled-order copy of the body cannot fail differently, because these statistics ignore order, so it is not used): known-positive = the 5549 body's first half fitted against its second half (it must pass the gate, or the gate cannot detect a sibling at a one-page length and the fit is 'not a test'); known-negative = 5204 p2 (Lodewijk's table, must fail). Run both once at the start and report their three numbers beside every candidate's.
Step 3: a positive stops the loop -- write it up and flag the orchestrator ("flag: for LANE WC: <briefnr> fits the 5549 body key") with the candidate's printed edition, since the next job is an alignment (tools/interlinear_align.py, leave-one-line-out gate 0.60), not this one. No positive after all candidates: write "WC-NASSAU-FIT (26 Sept 2026)" in NOTES.md: the candidate table, the fit rows with both controls, the unseen witnesses, and the one-line next step (normally: the KHA or Marburg holdings named as unseen witnesses; the lane orchestrator decides the ASKS row). Status stays open (rule 5).

Push with `python3 tools/room.py --push <paths>` after each fitted candidate; ROOM done line "done: for LANE WC: WC-NASSAU-FIT --"; final reply one paragraph per COMMON with request counts per host.
