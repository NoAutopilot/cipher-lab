PARENT WORKER RETRO-APPLY-LEARN4 (Sonnet, wall-clock box 40 minutes, cap USD 4, no subagents, no network except git). Job: apply the brief- and tool-only diffs left open by LEARN-2026-09-26-1252.md (owner-account pass 4) and the two host-table findings of D1-CHECK. Read CLAUDE.md, LEARN-2026-09-26-1252.md and LEARN-2026-09-26-1313.md (the other account's pass 8, 13:13 -- do not re-apply anything it or RETRO-APPLY-R/S already applied; check git log and the target file first) and each target file before editing it.
1. LEARN4 item 1 (QA mechanics): add to `.claude/briefs/runs/2026-09-25-parent-quality-audit.md` (the owner-account rolling-QA brief) an explicit checklist item: run `python3 tools/near_check.py`, `python3 tools/ledger_check.py` and `python3 tools/intake_gate_check.py <each touched target>` and paste the output; a nonzero exit or an unresolved flag is a QA failure. If the other account's QA brief (.claude/briefs/runs/2026-09-26-lane-v9-qa*.md or their template) already carries the same wording, copy that wording rather than inventing new.
2. LEARN4 item 2 (technique): the editor's-note phrase sweep. In `.claude/briefs/scout.md` (or the scout template that exists) and `.claude/briefs/check-solved.md`, add one paragraph each: a numeral-run detector cannot see an edition that replaces an undeciphered passage with an editorial note; sweep Google Books (with &country=US) and archive.org full text for the editor's own phrasing in the edition's language ("cifra no descifrada", "cifra non decifrata", "não se pôde decifrar", "nicht entziffert", "chiffre non déchiffré", "in cipher, not deciphered" and their variants), then gate every hit first on whether the edition PRINTS cipher groups at all (D1-CHECK's three-marker test in ciphers/_triage/doria-donjuan-1568.md is the worked example: notes-only closes the item). Add the same as a docstring line in tools/print_check.py if it has a docstring on search routes; no code change.
3. D1-CHECK host findings for the CLAUDE.md Access playbook host table: (a) books.google.com page/text view (pg=..&output=text, PDF and epub download) is captcha/bot-blocked from the cloud, 2 sessions for 2 (SCOUT-OWN-3, D1-CHECK) -- Google Books API snippets (www.googleapis.com, with &country=US) still work; (b) cervantesvirtual.com (Biblioteca Virtual Miguel de Cervantes) is Cloudflare-blocked to curl and to tools/browser_fetch.js alike (D1-CHECK, 3 requests) -- a desktop/LOCAL-QUEUE route only. Add both as rows in the table's existing format with the date and the worker that established them.
4. LEARN-8 (13:13) item 2 for our side: nothing to port. If LEARN-8 item 1 (EMLO manifestation-level Solr note, bibo_Note per letter, as a check-solved source stronger than a collection-title inference) is not yet in `.claude/briefs/check-solved.md` by the time you read it, add it there in one sentence citing EM3-CHECK (QUEUE.md "EM3 check-solved, 26 Sept 2026"); if the other account's RETRO-APPLY has already added it, skip.
Never touch BUDGETS.md or rate-limit rules. One UPDATES.md row per instituted change (existing format, clock-read time from date -u). Run any existing tests under tools/tests/ that touch files you edit. Post a ROOM claim first and a done line at the end as `parent worker RETRO-APPLY-LEARN4 (Sonnet, <session id>)` with tools/room.py (two positional arguments: role, text); add your LEDGER.md row; commit by path, fetch and rebase, push (never force; keep both sides on shared-file conflicts). Never call AskUserQuestion; the owner is asleep.
