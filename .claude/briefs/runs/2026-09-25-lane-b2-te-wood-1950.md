LANE B2 breadth worker bWOO: te-wood-1950. Sonnet (claude-sonnet-5). Cap $3 or 30 minutes, whichever first. No subagents.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b2-common.md` (read it first; it governs) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Spec: specs/te-wood-1950.json. Run `cheap_tests_in_order[0]` and its matched control, nothing else. Write both into cheap_test_done (date, method, script path). Report in five lines; rule 10 wording; a hit is a candidate for a person's inspection, never a PASS at N=21.

Test 1: Gillogly's running-key-sum method (read the spec and sources/schmeh/posts/35-thouless.txt for exactly how Thouless's scheme combines key text and plaintext) as a dictionary attack: for each candidate key text and every start offset, derive the plaintext stream and score it for real words (a word list in the plaintext's language; English first, then the key text's language). Key texts: up to 5 from the Internet Archive (_djvu.txt of a French, German and Latin Bible or a classic novel named in the spec; advancedsearch then download, at most 15 archive.org requests, >=1.5 s apart, one host only; not gutenberg.org). Control first: build a synthetic N=21 cryptogram with the same method from one of the fetched key texts at a random offset and a real English sentence, and show the same script ranks the true offset in its top 10 (3 seeds). Report control rank per seed and the target's top 10 candidates with scores side by side. Script and output under specs/cheap-tests/te-wood-1950/.
