PARENT WORKER SO-LAND-26 (Opus, cap $5 of usage, box 30 minutes; network: GitHub MCP tools for the PR, archive.org/be-api, Google Books API with country=US and GOOGLE_BOOKS_KEY, OpenAlex and Semantic Scholar with their keys; one request per host, 1.5 s apart). Parent 7i (session_01U1NiMakQ8PPySH8WixXsmc). Two hats in one bounded job, landing then verifying, because the second-opinion loop's first end-to-end run is what is being tested (tools/second_opinion_runner_prompt.md "Our side"). Written 26 Sept 2026 at the 19:00 UTC clock read.

Job: PR 26 on NoAutopilot/cipher-lab, titled [SO-LODEWIJK-5797], is the ChatGPT Verifier runner's answer to SECOND-OPINIONS-QUEUE.tsv row SO-LODEWIJK-5797 (prompt ciphers/lodewijk-van-nassau-1573-74/second-opinions/PROMPT-chatgpt-5797.md: the two blanks in WVO 5797, Groen IV no. CDXLIV pp.223-225, code 161 'Landgraf' and 153 'Pfaltzgraf', N4 after two audits). (1) Read the PR with the GitHub MCP tools (pull_request_read get_files, then get_file_contents on its branch); never merge. Save the answer verbatim as ciphers/lodewijk-van-nassau-1573-74/second-opinions/SO-LODEWIJK-5797-answer-2026-09-26.md (header: pr 26, branch, date, runner "ChatGPT second-opinion task"). Set the queue row: status `posted`, pr 26. (2) Verifier hat (rule 10, CLAUDE.md "Workers", the SO loop's "a verifier checks every citation"): list every citation and factual claim in the answer; check each against its source as far as the cloud allows (a printed edition on archive.org by full-text search, a scholarly work through OpenAlex/S2/CrossRef, a catalogue by its own page); classify each as confirmed / not found / misattributed / unverifiable from the cloud; say whether the answer locates any prior plaintext or decipherment of codes 161 or 153 in WVO 5797 that the two audits (V8-NA5797, V8-NA5797-2, AUDIT.md A2.5) did not, and whether anything in it would change the class N4 (only a verifier writes a class: propose, with the evidence, in AUDIT.md; do not edit status.json). Write an AUDIT.md subsection "Second opinion SO-LODEWIJK-5797 (PR 26, 26 Sept 2026)" with the citation table and the verdict sentence; set the queue row's `outcome` cell to one of: confirms-N4 / candidate-print-found: <what> / unverifiable-citations: <n of m> / off-topic. (3) Close PR 26 without merging with a comment naming the commit that carries the saved answer (after `git ls-remote origin main` shows it), ending with the footer "---" then "_Generated by [Claude Code](https://claude.ai/code)_". Also note in the done line whether the runner followed its prompt's form (one PR, one file, title, branch name), since this is the write path's first live test.
Rules: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker SO-LAND-26 (Opus, <session id>)`; every edit reads the whole file and writes it back changed in one place; `git diff --stat` before every commit; commit by explicit path; push only through `python3 tools/room.py --push <paths>`; `python3 tools/file_shrink_guard.py` on touched files. Never decode; never write solved, new, first or unpublished; never print or commit credentials; never call AskUserQuestion. Finish: ROOM done line "done: for parent 7i: SO-LAND-26 -- PR 26 landed (<commit>), citations <confirmed/not found/unverifiable counts>, outcome <cell>, runner form <ok / deviations>"; one-paragraph reply; stop.
