bSALS -- fr2933-salviati-1525 folder shrink (LANE B11 job 2). Sonnet. Cap USD 4, box 30 minutes from your first `date -u`. No transcription, no image reading by a model.

Read first: `.claude/briefs/runs/2026-09-26-lane-b11-common.md` (governs), CLAUDE.md "Access playbook" last paragraph before the hosts table (the AX2-SHRINK method), and the two worked examples: ciphers/lodewijk-van-nassau-1573-74/{NOTES.md section "AX2-SHRINK", images_manifest_full.tsv, regen_images.sh} and ciphers/malsburg-hessen-1636/{NOTES.md section "bMALS: folder shrink", images_manifest_full.tsv, regen_images.sh} (copy the shape).

Intake gate (orchestrator ran it 12:24 UTC): `fr2933-salviati-1525: open (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

Job: ciphers/fr2933-salviati-1525 is 36 MB (images/ 12 MB, strips/ 9.8 MB, glyphs/ 8.7 MB, atlas_review/ 3.9 MB), over the 30 MB line. Shrink it to at most 26 MB.
1. `images_manifest_full.tsv`: one row per image file under images/, strips/, glyphs/, atlas_review/, recon_*/ (path, source: the Gallica IIIF URL from images/manifest.json for pages and native crops, or the local script + args that derived it for strips/glyphs/montages -- grep the folder's *.py for the output path; sha1, bytes, pixel dimensions, `cited_by`: every NOTES.md / HYPOTHESES.md / *.tsv / *.py line naming the file or its stem; grep, do not guess).
2. `regen_images.sh`: re-derives every Gallica-sourced file from its recorded IIIF URL (gallica.bnf.fr IIIF image API only, browser User-Agent, >= 2 s between requests) and every derived file with the exact local command that made it; a file whose derivation is not recorded gets a row saying so and is KEPT.
3. Byte-identical regen test BEFORE deleting anything, into a scratch dir: at least 2 Gallica-sourced files (note: Gallica's JPEG encoder may not be byte-stable; if a fetched file differs, compare decoded pixels too and record both results -- a pixel-identical but byte-different page counts as regenerable only if you say so explicitly) and at least 3 locally derived files (strips/glyphs/montage). Paste the results into NOTES.md.
4. Delete uncited files that regenerated in the test's class; convert cited PNG full pages or strips to JPEG at the same pixel dimensions (quality 85) only if every citing use stays valid (scripts that read the file keep working -- check their paths). Keep everything the settle_*/recon_* scripts or specs/ read at run time.
5. `du -sh` before and after in NOTES.md section "bSALS: folder shrink (26 Sept 2026)"; update images/manifest.json entries for anything deleted or converted (a `local: deleted, regen via regen_images.sh` field).
Concurrent worker: bSALR runs family_run.py on this target (writes HYPOTHESES.md, NOTES.md's bSALR section, families/, control_curve.tsv). Do not touch those files except appending your own NOTES.md section; rebase before every push.
Host: gallica.bnf.fr IIIF image API only (named here, overriding the common file's NEVER), at most 6 requests, >= 2 s apart; report the count. Commit by explicit path (`git rm` for deletions), `python3 tools/room.py --push <paths>`. ROOM claim first, done line at the end (at most 600 chars: before/after MB, sample regen result). Final reply at most five lines.
