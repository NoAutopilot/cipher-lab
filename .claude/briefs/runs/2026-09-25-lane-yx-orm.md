YX-ORM (Sonnet, wall-clock box 40 minutes; at most 2 subagents). Target: ciphers/ormond-arran-1678 (Ormond to Arran, 24 Jan 1677/8, HMC Ormonde n.s. iv p.93: about 20 numeral code groups up to 732 embedded in clear English). Parent: LANE YX orchestrator session_01PHaEdHeQY2FtMHLo9yGeoe. Kind: recovery test (published period-key reconstructions). Read the lane COMMON, the target NOTES.md in full, ciphertext.txt, and sources/cryptiana/web/charlesii2.htm (Tomokiyo: this letter "seems different" from the Ormond-Longford Cipher 1; reconstructed Cipher 2 from HMC Ormonde vol.6 p.xix and Cipher 3 from vol.7 p.xx are listed as not tried). Intake gate: tools/intake_gate_check.py exits 0.
Job: one cheap key-application test, with a control.
1. Transcribe Tomokiyo's reconstructed key tables for Cipher 1, 2 and 3 from charlesii2.htm (on disk; credit Tomokiyo, rule 8) to keys/cipher{1,2,3}.tsv, and if the HMC vol.6 p.xix / vol.7 p.xx pages are on archive.org, check the tables against them (IA, 1.5 s apart, at most 15 requests).
2. Apply each key to the ~20 groups; per key report how many groups it covers and whether the covered groups make sense in their clear-English context (names that fit "If what he says of 445 and 342, be true ...").
3. Control (rule 3): the same coverage count for each key against 1,000 random reassignments of the same group values within the observed range (or the other keys' groups), so a coverage number means something; report target and control side by side. Below unicity, a key "fits" only if its coverage is above the control's 99th percentile and the covered words read sensibly in context.
If a key fits, write the partial reading with per-group grades (H from the key, M uncertain, U unkeyed) and stop; no judge (too short), no novelty search. NOTES.md gets "## YX-ORM (25 Sept 2026)" at the end of the file. Touch only ciphers/ormond-arran-1678/** and ROOM.md. Report what was found and where it was not; do not classify novelty; "cost: see the lane ledger"; request counts per host.
