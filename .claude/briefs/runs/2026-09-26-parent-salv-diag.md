PARENT WORKER SALV-DIAG (Sonnet, cap $3 of usage, wall-clock box 35 minutes, no subagents, no network). Job: run the diagnostic step NEAR.md names for fr2933-salviati-1525 (the row's "Diagnostic first (26 Sept 2026, RETRO-2026-09-26a)" cell): extend LANE R8's R8-DSN syllabary-design control error sweep from the 5-7 percent transcription-error range it covered up to 10-14 percent, to find where the control itself crosses into the target's own failing range. Authorised by CLAUDE.md rule 5 (a NEAR.md row's named next step runs with its control) and `.claude/briefs/parent.md` duty 0; retrospectives q and r both flagged this step as undispatched. Written 26 Sept 2026 at the 09:53 UTC clock read by parent 7g (session_01Jb4UHR8so3yBUG1NTMdf6C).

First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker SALV-DIAG (Sonnet, <session id>): fr2933-salviati-1525 NEAR diagnostic`; read NEAR.md's fr2933-salviati-1525 row, ciphers/fr2933-salviati-1525/NOTES.md (the LANE R8 / R8-DSN section and its file references), HYPOTHESES.md, and the sweep script R8-DSN used (find it by the NOTES.md citation; reuse it with its error parameter raised, do not write a new one). Run the control at 10, 12 and 14 percent injected type error, same seeds and settings as R8-DSN's 5-7 percent rows, one unit per level (about 5-8 minutes each on this container: stop before starting a level that would cross 80 percent of the cap or the box), and write one row per level into HYPOTHESES.md beside R8-DSN's rows. Report: the error level at which the control's read rate falls into the target's range (the target reads -2.66 to -2.70 per symbol with 4-19 percent seed agreement), and whether that crossover sits inside the transcription-error range the atlas passes measured (14.6 percent disagreement boxes) -- if it does, the earlier negatives were non-tests at the measured error and the row's next step is the transcription pass C; if it does not, the design-family negatives stand and the row's next step is the word-level scorer. Update the NEAR.md row (Evidence and "What would settle it" cells, Last touched from the clock) and NOTES.md; status stays partial; no family is re-run on the target. Commit by explicit path with `python3 tools/room.py --push`; ROOM done line `done: SALV-DIAG: control at 10/12/14 pct = <numbers>, crossover <inside|outside> the measured error range, next step <named>`; one-paragraph reply; stop. Never call AskUserQuestion; never print or commit credentials; rule 10 wording.
