ARM3-DICT (LANE ARM3 job 2, Sonnet, cap USD 6 stall alarm, wall-clock box 45 minutes from your first `date -u`). Written 26 Sept 2026 by LANE ARM3 orchestrator (session_01PmFYDrdFDjraA2rc932HcQ) after the 18:55 UTC clock read.

COMMON RULES: `.claude/briefs/runs/2026-09-26-lane-arm-COMMON.md` applies in full, with "LANE ARM3" for "LANE ARM". Claim as `LANE ARM3 worker ARM3-DICT`. Intake gate (pasted by the orchestrator, 26 Sept 2026): `ciphers/armstrong-madison-1808: open (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

Target: ciphers/armstrong-madison-1808 (Armstrong to Madison, Paris, 20 Feb 1808; 369 numeric groups, use `ciphertext_ms.txt`). Read NOTES.md top, HYPOTHESES.md "Summary, cycle 2", "Summary, cycle 1" and the ARM-DESIGN section in full (`design/design_stats.py` is reusable: it already encodes en18 prose under several simulated designs), and `ciphers/antt-linhares-chave/NOTES.md` top (the repository's worked pocket-dictionary key).

Question (family G, new): is the book part (values above ~100, about 900-1800 forms) a page-and-word DICTIONARY code keyed to a printed English dictionary a US diplomat carried in 1806-08? ARM-DESIGN's premise was a hand-built family book (decade = word family, units digit = fixed member slot); a dictionary code is a different, testable design: value order follows the book's alphabetical order.

UNITS: U1 structural test (script only, no network); U2-U4 one per dictionary (at most three), only if U1 does not already exclude the design. Per-unit estimate about USD 1.5 and 10 minutes (script-heavy worker, priced from ARM-DESIGN/ARM-A2 script jobs; confirm on the first unit). Stop before starting a unit that would cross 80% of the box (36 minutes); unfinished units go as one line in NOTES.md.

U1 -- alphabetical-position test, control first (rule 3). In any dictionary code the value of a word is monotone (or block-monotone) in its alphabetical position, so the book-range values of the COMMONEST content words sit where those words fall in the alphabet. Build it this way:
 (i) CONTROL: encode en18 prose windows of the target's own length and particle/book split (132 particle tokens at 1-99, 237 book tokens) under a synthetic dictionary code: book vocabulary = the en18 word list of a plausible pocket-dictionary size, sorted alphabetically, mapped linearly (and, as a second control, in page-plus-entry form: page = value // 10, entry = value % 10) onto 100-1899. Compute a statistic that needs no plaintext, e.g. the correlation between each repeated book value's rank position and the alphabetical rank of the en18 word with the same frequency rank, or the entropy/placement of the top-k repeated values across the 100-1899 range compared with where the top-k en18 content words fall alphabetically. Choose one statistic, fix it BEFORE looking at the target, and show on 60 control letters that it separates the dictionary design from ARM-DESIGN's `hdec` and `seq_pblock` simulations (both are the alternative) -- if it cannot separate them, U1 is a non-test, say so, and go to U2 anyway.
 (ii) TARGET: the same statistic on the target's book tokens, with its percentile in each simulated design's distribution.
 (iii) The units-digit fact (0 about 25%, 1 about 18%, 2/3/5/9 about 2-5%) must also be checked against the page-plus-entry dictionary control: in a (page, entry) scheme is the entry digit that skewed? report the control's units-digit distribution beside the target's.

U2-U4 -- actual dictionaries (only if U1 does not reject the design at p<0.05 against the dictionary simulations): find on archive.org, full-text public items only (advancedsearch.php, then `_djvu.txt`), up to three of: Entick's New Spelling Dictionary (1780s-1800s editions), Johnson's Dictionary abridged/miniature, Perry's Royal Standard English Dictionary (US editions 1788-1806), Sheridan/Walker pronouncing dictionaries. For each, derive a per-page headword list from the OCR (page breaks in `_djvu.txt`, or the `_page_numbers.json` if public). Try the two schemes (value = running entry number; value = page*10 + entry-on-page, and page*100 + entry if the page count demands), decode the target's book tokens, leave particles as `<P>`, and score the implied word sequence with `python3 tools/judge_plaintext.py specs/armstrong-madison-1808.json --file <decode>` (en18). AGAINST: the same dictionary+scheme decode of the SHUFFLED target (3 shuffles), scored identically -- rule 3: a PASS on the shuffled decode voids the judge for this family at this N (CLAUDE.md rule 3, ARM-C1 precedent). Also a known-answer control: encode an en18 window with that dictionary's own OCR headword list under the same scheme and decode it back through your pipeline -- if your OCR-derived list cannot read its own synthetic letter to at least 80% of book tokens, that dictionary is untestable from its OCR, logged as such.

Output: one section "## Family G, ARM3-DICT dictionary code (26 Sept 2026, LANE ARM3 worker ARM3-DICT)" appended to HYPOTHESES.md: the U1 statistic, control distributions and target percentile; per dictionary: identifier, edition, pages, scheme, known-answer control %, shuffled-decode judge scores, target judge score, verdict. A negative with a passing control is a real result; a design exclusion (U1) is the best outcome of a cheap test. Scripts under `ciphers/armstrong-madison-1808/dict/` (offline part testable), fetched OCR text there too with a manifest.tsv (under 10 MB). Files you may touch: that folder, HYPOTHESES.md (append), NOTES.md (append a short dated section), ROOM.md.

Report what was found and where it was not found; do not classify novelty. A decode that PASSes must not be called a reading: post a `flag:` and stop for the orchestrator. Push with `python3 tools/room.py --push <paths>`; done line; one-paragraph final reply (answer first, files, request counts per host, "cost: see the lane ledger"); stop.
