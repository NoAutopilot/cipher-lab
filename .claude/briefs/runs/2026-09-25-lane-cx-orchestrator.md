LANE CX ORCHESTRATOR (check-solved sweep, intake). Opus. No dollar budget (owner: cost is not a concern, Max plan); the binding limit is the rate-limit window in `rate_limit_info`. Opened by the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n) at 11:40 UTC 25 Sept 2026, because LANE YX's triage (ciphers/_triage/2026-09-25-lane-yx-triage.md) found that only 2 of 99 open targets pass the intake gate, most because no formal check-solved was ever run.

Rate limit, check-ins (every 30-45 minutes with send_later), worker count (at most 6 live Sonnet workers), first steps, ledgering and handoff: exactly as in .claude/briefs/runs/2026-09-25-lane-lx-orchestrator.md, with LANE CX for LANE LX and this lane's COMMON (2026-09-25-lane-cx-COMMON.md). ROOM claim line "LANE CX orchestrator: check-solved sweep". Read each worker's cost and elapsed time with get_session at every check-in; interrupt a worker past its wall-clock box and brief a successor. Never trust a worker's own "under cap".

Pool: the YX triage's sections A (recovery) and B (cryptanalysis) and any other target it lists as gate FAIL that is copy-free (images reachable without an order) -- about 36 targets; skip its section D (blocked on an archive, a copy order, DECODE or a person) and section C (Heinsius, one archive request). Re-check ROOM.md for a claim in the last 6 hours before assigning. Never touch: the other account's lanes R5, N4, B, V2 and their targets (fr2933, fr3151, fr3985-3990, fr5761, roell-vandedem-1809 and every N4 nomination); any target LANE YX has claimed; anything with an AUDIT.md.

Job: check-solved per .claude/briefs/check-solved.md, one worker per 3-5 targets grouped by edition family (the same printed series or archive), recovery-shaped targets first. For each target the verdict line in NOTES.md must be one of: `open` naming the standard edition and the pages or a full-text search with a live control hit elsewhere in the same volume; `blocked` naming exactly what could not be read and who can supply it (and a LOCAL-QUEUE.tsv or JSTOR-QUEUE.tsv row when the owner's machine can read it); `found-solved` with the citation. Paste `python3 tools/intake_gate_check.py ciphers/<target>` output for each target into the worker's done line; exit 1 means the verdict is not finished. No transcription, key application or cryptanalysis in this lane: an `open` that passes goes to LANE YX in a ROOM line addressed 'for LANE YX', with the route the triage gave it.
For the parent: every found-solved (a correction worth logging), every target handed to YX, and the running count of targets that now pass the gate.
