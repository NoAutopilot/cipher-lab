V6-SOCHK (Opus, cap $8, wall-clock box 60 minutes from your first `date -u`: stop and push at $8 or 60 minutes, whichever first; at most 3 Sonnet subagents, one per PR, budget for them). Citation check of three second-opinion pull requests. Parent: LANE V6 orchestrator session_01V2BHwhVh1k72qSYuBFCyGd; the request comes from parent 7b (ROOM 16:17 UTC 25 Sept 2026). Read `.claude/briefs/verifier.md` section "Second opinions from outside models", CLAUDE.md rule 10 and Git section, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`, and each target's AUDIT.md.

PRs (fetch with `git fetch origin 'refs/pull/10/head:pr10' 'refs/pull/11/head:pr11' 'refs/pull/12/head:pr12'`; each adds one file under the target's second-opinions/): 10 SO-LINHARES-M0002 (ciphers/antt-linhares-chave), 11 SO-VANBEUNINGEN-1657, 12 SO-NASSAU-5551. Do not merge or close any PR and do not comment on GitHub; the parent's successor merges or closes.

For each PR: list every factual claim and citation in the answer (edition, page, archive reference, DOI, URL, person, date). Trace each to the source itself (archive.org full text / be-api fts, Google Books with &key=$GOOGLE_BOOKS_KEY&country=US >=3 s, HathiTrust bibliographic API, OpenAlex with Bearer $OPENALEX_KEY, Semantic Scholar x-api-key $S2_KEY 1.1 s, CrossRef, the publisher page). Mark each confirmed (with what you read), not confirmed (searched, not found: invented or unverifiable), or unreachable. Then decide: does any confirmed lead show prior print or prior decipherment that would move the target's class in its AUDIT.md? If yes, say which and why, but do not change the class yourself; post "flag: for the parent: <target> class may move, see <file>". Write the verdict to `ciphers/<target>/second-opinions/CHECK-<PR label>.md` (claims table, verdict merge/close: merge when the file is a faithful answer whose unconfirmed citations are listed; close when it is mostly invented), and add a "Second-opinion claims not confirmed" list to the target's AUDIT.md (append only; the class line untouched). Commit by explicit path to main (no PR), push. JSTOR-only leads go as rows in JSTOR-QUEUE.tsv.
ROOM done line: "for the parent: PR 10 checked, <merge|close>; PR 11 checked, <merge|close>; PR 12 checked, <merge|close>" plus confirmed/unconfirmed counts per PR and requests per host, no cost figure. Report five lines. Stop.
