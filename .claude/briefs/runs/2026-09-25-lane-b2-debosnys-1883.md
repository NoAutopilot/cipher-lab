LANE B2 breadth worker bDEB: debosnys-1883. Sonnet (claude-sonnet-5). Cap $3 or 30 minutes, whichever first.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b2-common.md` (read it first; it governs) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.
(Carried over from LANE B's unrun brief of 24 Sept 2026, 2026-09-24-lane-b-debosnys-1883.md; wording kept.)

Spec: specs/debosnys-1883.json. Run `cheap_tests_in_order[0]` and its matched control, nothing else. Write both numbers into the spec's `cheap_test_done` (with date, method, cost). Judge any candidate plaintext with `tools/judge_plaintext.py` and paste its output. Cap $3. Report in five lines; do not run test 2; rule 10 wording; a negative's done line carries the target and control numbers side by side.

cheap_tests_in_order[0], re-scoped by the B2 orchestrator 25 Sept 2026 16:33 UTC (bMOU ran a 158-sign two-pass job to 2.5x its $3 cap because the pass-B subagent's usage folds in; Debosnys is several times larger and in invented symbols): NO subagent and NO second pass in this job. (1) Fetch the cryptogram images from Schmeh's post (URLs in sources/schmeh/posts/03-debosnys.html; at most 4 requests to scienceblogs.de, >=1.5 s apart, browser UA as bMOU used) into ciphers/debosnys-1883/images/ with manifest.json. (2) Build the sign inventory with tools/glyph_atlas.py (segment with --debug, cluster, atlas; `pip install numpy scikit-image scikit-learn pillow` if missing; if the tool will not run, count distinct signs by eye on the shortest cryptogram only). (3) Report per cryptogram: sign count N, distinct K, IC with a script, line structure, and whether any plain-letter/digit text sits on the page. Control: IC of an English and a French text at the same N and a uniform random string at the same K (tools/data texts, no host). All numbers are conditional on a single machine-segmented pass (rule 2): say so. Put ciphertext.txt (sign codes as segmented, marked 'single pass, draft'), NOTES.md (status open, check-solved not run: say so) in ciphers/debosnys-1883/, and point the spec at it. A second blind pass is test 2, not yours. At $3 or 30 minutes push whatever is done with a progress section.
