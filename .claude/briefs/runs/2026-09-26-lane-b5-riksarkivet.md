JOB bRIK (LANE B5 reserve, Sonnet worker). One line: open ciphers/riksarkivet-r4282-1628/, check-solved, then use R4284's own key-test leaf (four Latin phrases over their cipher equivalents) as a crib against R4282, with the same crib alignment against three other Chifferklaver key records as the control.

Read first: `.claude/briefs/runs/2026-09-26-lane-b5-common.md` (and the 7b COMMON it names), `.claude/briefs/breadth.md`, CLAUDE.md rules 1-5, 7, 8, 10. Source row: QUEUE.md "Re-rank for LANE B5" rank 4 and QUEUE.md G4. Note G4's own caveat: four unrelated correspondences bundled as one catalogue row; R4282/R4284 key-sharing is a hypothesis.

Cap and box: $4 or 40 minutes from your first `date -u`, whichever first. Git and disk first; DECODE only through the login-free tools/decode_list.py listing (no login: full-size images are account-blocked); Riksarkivet's own API/IIIF only if a key record you need is not in Bourdeau's repository (at most 10 requests, >= 1.5 s apart, descriptive UA). If the three control key records are not obtainable as transcriptions, use three synthetic keys of the same shape (same number of cipher symbols per plaintext letter, same homophone counts) and say so.

Steps:
0. `date -u`; `python3 tools/room.py --start`; ROOM claim (tools/room.py) naming ciphers/riksarkivet-r4282-1628/ and specs/riksarkivet-r4282-1628.json.
1. Clone once `git clone --depth 1 https://github.com/dbourdeau/cyphersolver <scratch>/cs`; read only `riksarkivet1628/` (NOTES.md, r4282_*.txt, r4284_transcription.txt, profile.json, qg.py). Record HEAD commit and date; establish what Bourdeau tried on R4282 and whether he used R4284's key-test leaf. If he did, write that and stop.
2. Check-solved (rule 1, breadth.md intake), minimal: Riksarkivet catalogue for SE/RA R4282/R4284's series (what the catalogue says of a decipherment), Tomokiyo, DECODE listing status fields, aaymeloglu/unsolved-ciphers (clone shallow, grep, delete), one OpenAlex + one Semantic Scholar query. NOTES.md lines 1-2 in the gate's format; `python3 tools/intake_gate_check.py riksarkivet-r4282-1628` output pasted; proceed only on exit 0.
3. Test: build the partial key the four Latin phrases of R4284's key-test leaf give (plaintext letter -> cipher symbols), apply it to R4282 (tools/decode_key.py via decode.json; add a tool option with an offline test if needed): report the fraction of R4282 symbols the crib key covers and whether the covered stretches read as Latin or German (judge: la or de16 via tools/judge_plaintext.py, paste output; de16 era-fits 1628-33). Control: the same procedure with three other key records (or three synthetic keys of the same shape) against R4282, and the R4284 crib key against three shuffles of R4282. Report target vs control coverage and judge.
4. specs/riksarkivet-r4282-1628.json with cheap_test_done (date, method, both numbers). Finish per the common file. Report what was found and where it was not found; do not classify novelty.
