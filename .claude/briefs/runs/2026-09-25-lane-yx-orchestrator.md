LANE YX ORCHESTRATOR (left-open targets, copy-free). Opus. No dollar budget: the owner's Max plan is not billed per token, so the binding limit is the rate-limit window in `rate_limit_info`. Opened by the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n) at 10:35 UTC 25 Sept 2026 after LANE TX and LANE PX closed on their handoffs.

Rate limit, check-ins (every 30-45 minutes with send_later), worker count (at most 6 live Sonnet workers), first steps, ledgering and handoff: exactly as in .claude/briefs/runs/2026-09-25-lane-lx-orchestrator.md, with LANE YX for LANE LX and this lane's COMMON (2026-09-25-lane-yx-COMMON.md). ROOM claim line "LANE YX orchestrator: left-open targets". Cost: a worker cannot see its own cost; read each worker's cost with get_session at every check-in and interrupt it at its alarm. Never trust a worker's own "under cap".

Pool, starting points (re-check ROOM.md for a claim in the last 6 hours before assigning; drop any that has one):
- antt-msliv0638-brochado-1712 (LANE PX handoff, STATUS.md): period key 40 codes / 29 C from the Deciffrada appendix; body letter 134 (m0275-m0276, ~70 tokens) lies past the appendix's last deciphered letter. Gate failed by 0.8 points on a noise-matched leave-one-out control (80.2% vs 91.0%). PX's one named next test: hand-check Carta 79 (the worst-dragging entry) against its image, then re-run the gate. Normalize both texts to one convention before diffing (RETRO-2026-09-25h proposal 2).
- oldenbarnevelt-brederode-1605 (LANE TX handoff): open, no key route found; NA 1.01.02 inv. 6016 unread past 2 leaves; LOCAL-QUEUE L12.
- clairambault1225-paget-1714 (LANE OX handoff): attempt 1 negative with control; AN Marine B7 key route is LOCAL-QUEUE L11 on the owner's machine -- hold until L11 answers.
- willem-van-hessen-1567, la-garde-1577 and any other target whose NOTES.md check-solved verdict is `open` with no live claim, no reading and no N-class, found by job 1.
Never touch: the other account's lanes R5, N4, B, V2 and their targets (fr2933, fr3151, fr3985-3990, fr5761, roell-vandedem-1809 and every N4 nomination); the live owner-account lanes KX (colbert26-lathuillerie-1644, key cross-match) and VX (na-* targets, schonenberg, suriname, janssens, oldenbarnevelt-2442); LX's, DX's, OX's closed items unless listed above.

Jobs; each worker gets the COMMON plus a job brief you file first as 2026-09-25-lane-yx-<job>.md.
1. Triage (Sonnet, no fetching): list every ciphers/*/NOTES.md whose check-solved verdict is `open` (and passes the intake gate: it names the standard edition and the pages or full-text search read, a full-text search counting only with a live control hit), with no claim in 6 hours, no reading and outside the never-touch list; per target the route (recovery: decipherment on the leaf, decrypted sibling, published key, known plaintext; cryptanalysis: which design, which control). Rank recovery routes first. Where tools/intake_gate_check.py exists (RETRO-2026-09-25h proposal 4, being built now), paste its output per target.
2. Read, top routes first: transcription (two blind passes, .claude/briefs/transcription.md, the 60% gate), then solver (key application, per-token grades, judge output, fresh-instance re-derivation).
3. Cryptanalysis only for a target the triage ranks worth it and only with a design-matched control: one attempt, a second failure closes it as a clean negative with target and control numbers side by side.
4. Verifier per reading, a separate Sonnet session (.claude/briefs/verifier.md), AUDIT.md with an N-class and the key source (ours / period / published).
5. Second audit for anything at N3 or better (a second, adversarial verifier), then a second-opinion prompt (tools/second_opinion_runner_prompt.md) queued in SECOND-OPINIONS-QUEUE.tsv.
For the parent: the triage's top five, each reading that passes judge and re-derivation, each AUDIT.md verdict.
