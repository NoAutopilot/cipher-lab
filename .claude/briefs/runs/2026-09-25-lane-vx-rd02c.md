VX-RD02C (Sonnet, time box 40 min, at most 2 subagents). Final worker on ciphers/na-janssens-java-1811 (VX-N02, partial: 208-code key from dispatches No.2, No.3, No.5; leaf 188 No.1 43.6% keyed). Kind: recovery (key source period). Parent: LANE VX orchestrator session_01EYwyYDeAaQReWy7ZuQcoxe. Read .claude/briefs/runs/2026-09-25-lane-vx-COMMON.md (wall-clock box, no self-reported cost, host budgets are hard limits), .claude/briefs/solver.md, and the target NOTES.md in full; build on the files on disk, never redo a finished pass. Rule 7: judge output pasted into NOTES.md (a FAIL reported as a FAIL, never a threshold change), decode.json + `tools/decode_key.py <folder> --check`, one fresh-instance re-derivation subagent for anything newly keyed. Do not classify novelty. This is the FINAL worker on this target in LANE VX: finish with the status line (partial or solved), a "State at close" paragraph in NOTES.md (what is read, grade counts, coverage, the single best next step for a future lane), and stop.
Steps, no new fetches except leaf 208 if not on disk (service.archief.nl <=6 requests, hard): (1) the control RD02B named: decode the No.5 raw-cipher slip with key.tsv and compare token by token with the plain fair copy no5_plaintext.txt -- per-token agreement, and every code the plain copy corrects or adds goes into key.tsv as C (say which). (2) fresh-instance re-derivation of the codes added from No.5 (subagent given only the No.5 transcription TSVs, the plain copy and the crops). (3) leaf 208 "Vanteau" decipherment tail: say whether it uses the same code (overlapping codes agree?); if yes, transcribe it (two passes, crops) and add its pairs. (4) re-decode leaf 188; coverage before/after; judge; per-token counts; reading of what is keyed, with [?] for the rest, French and English gloss.
Touch only ciphers/na-janssens-java-1811/**, specs/na-janssens-java-1811.json, ROOM.md.
