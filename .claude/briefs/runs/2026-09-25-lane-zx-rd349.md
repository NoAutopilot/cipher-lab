ZX-RD349 (Sonnet; wall-clock box 45 minutes, also a minimum working time; no subagents). Rule-7 fresh-instance re-derivation for ciphers/clair349-este-guise-1556. Parent: LANE ZX orchestrator session_01MxueEQJUGF9PWJiYcVyvBM. Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md) and CLAUDE.md rule 7. You are the check, not the solver: you have not seen the solver's reasoning and must not read it.
You may read ONLY: specs/clair349-este-guise-1556.json; in ciphers/clair349-este-guise-1556/: key_alpha.tsv, key_nomen.tsv, ciphertext.tsv, exceptions.tsv, decode.json, build_decode.py, gloss_votes.tsv, and (only at step 3, for the diff) reading_tokens.tsv; and tools/decode_key.py. Do NOT open NOTES.md, reading.txt, reading_en.txt, reading_vs_gloss.tsv, corrections.tsv or any briefs of other ZX jobs.
1. Pipeline reproduction: copy the folder to your scratchpad; there run build_decode.py (then --check) and `python3 tools/decode_key.py <scratch copy>` and --check; confirm the regenerated reading_tokens.tsv is byte-identical to the committed one, or list the differences.
2. Independent application: write your own short script (in your scratchpad, then committed as ciphers/clair349-este-guise-1556/rederive_rd349.py) that reads key_alpha.tsv and key_nomen.tsv directly (not key_decode.tsv), applies them to ciphertext.tsv token by token, applies exceptions.tsv rows only where each row states its rule, and writes one value per token. Where the key gives two values for one code and exceptions.tsv has no row, output both (a|b).
3. Diff your step-2 values against the committed reading_tokens.tsv, token by token, after normalizing both (lower case, u=v, i=j, long s=s, doubled-letter codes as the pair). Report: tokens compared, tokens differing, how many of the differing tokens the committed file grades M, I or U, and a table of every differing token graded H or C (line, position, code, yours, committed). Rule 7 verdict: PASS if every differing token is graded M, I or U in the committed file; otherwise SEND BACK with the list.
Write one section "## ZX-RD349 re-derivation (25 Sept 2026)" at the end of NOTES.md (you may append to it without reading it: use `cat >>`), commit rederive_rd349.py and that section, push, ROOM done line with the verdict and the counts. Do not decode anything else, do not grade, do not edit any other file, classify nothing (rule 10). Write "cost: see the lane ledger". Never call AskUserQuestion.
