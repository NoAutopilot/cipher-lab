bSALI -- fr2933-salviati-1525: the one syllabary variant LANE B11 did not reach, DSN irregular. Parent worker for parent 7h (session_01GT3Yv4kUZrBR8pc2s5EToH), run directly because a lane created by this parent sits at the lineage-depth limit; authorised by CLAUDE.md Pipeline 3a and the LANE B11 handoff (STATUS.md) next step (1). Sonnet. Cap USD 3, box 30 minutes from your first `date -u`. Disk only, no hosts, no subagents, no image reading. Written 26 Sept 2026 at the 16:12 UTC clock read.

Read first: `.claude/briefs/runs/2026-09-26-lane-b11-common.md` (governs), `.claude/briefs/runs/2026-09-26-lane-b11-bSALR2.md` (same rules, judge, row format; its unit 3 is this job), ciphers/fr2933-salviati-1525/NOTES.md sections "## bSALR" and "## bSALR2" (exact commands and the table you extend), CLAUDE.md rule 3 (the measured-error paragraph and the calibration-gate paragraph).

Intake gate (B11 ran it 12:24 UTC): `fr2933-salviati-1525: open (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0; re-run `python3 tools/intake_gate_check.py fr2933-salviati-1525` and paste the line.

One unit: DSN irregular (assign=irregular), 24 restarts, `--measured-error 0.064 --param err=0.064`, 3 control seeds, target seed 1, with the shuffled-target decode scored through the same judge (the `--shuffle-target` form bSALR2 used) -- control first; if the control mean misses the gate the tool writes CONTROL BELOW GATE and you stop. About 15-18 minutes of CPU by bSALR2's own rate; do not start anything else. Record one row in HYPOTHESES.md in bSALR2's format (control, target, judge, shuffle floor, verdict at 6.4 pct), a "## bSALI" section in NOTES.md, and update the NEAR.md fr2933-salviati-1525 row's Evidence and Last touched cells in place (the named next step becomes the word-level nomenclator scorer, DSN sec. 5 b, unless your result says otherwise). Status stays partial (rule 5). Never write solved, new, first or unpublished. Rule 3 wording: a FAIL is "control-backed negative at the measured 6.4 pct error (conditional on the model-based error estimate)" only if the control clears the gate; else "non-test".
First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker bSALI (Sonnet, <session id>): fr2933-salviati-1525 DSN irregular`. Finish: commit by explicit path (NOTES.md, HYPOTHESES.md, families/*, NEAR.md), `python3 tools/room.py --push <paths>`, ROOM done line "done: for parent 7h: bSALI -- ctrl X / target Y / judge Z / shuffle W, verdict ..."; one-paragraph reply; stop. Never call AskUserQuestion; never print or commit credentials.
