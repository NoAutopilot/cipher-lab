JOB bMAT1D (LANE B7, Sonnet, cap USD 3, wall-clock box 35 min, hosts: github.com shallow clone only): matignon-mayenne-1586 NEAR steps (1c') revert and (1d) U signs as code words from the period decipherments.

Intake gate (orchestrator, 04:50 UTC 26 Sept 2026):
    $ python3 tools/intake_gate_check.py matignon-mayenne-1586
    matignon-mayenne-1586: partial (line 1) -- edition/page or full-text-search citation found within 6 lines
    exit 0

Read first: common file `.claude/briefs/runs/2026-09-26-lane-b7-common.md` (it governs); NOTES.md sections "NEAR step (1b)" (bMATBEAM) and "NEAR step (1c) (bMAT1C)"; NEAR.md row matignon-mayenne-1586.
Steps:
(1c') Revert bMAT1C: restore exceptions.tsv (and anything else bMAT1C changed that feeds the reading) to its pre-bMAT1C content with `git log -- ciphers/matignon-mayenne-1586` / `git show <commit>^:<path>` (a new commit, never history rewriting); M back to M. `python3 tools/decode_key.py ciphers/matignon-mayenne-1586` then `--check` exit 0; re-judge `python3 tools/judge_plaintext.py specs/matignon-mayenne-1586.json --file <reading>` and confirm about -1.371 / cover 0.814 (paste). Grade counts back to H 10,074 / M 1,648 / U 1,272 or state the difference.
(1d) U signs as nomenclator code words. Source of meanings: Bourdeau's transcriptions of the period decipherments (dbourdeau/cyphersolver folder matignon1586: ff.14v/15r, 18-21/19, 78v/79r -- MIT code / CC BY 4.0 text; shallow clone, read only that folder, credit line, delete clone). For each of the 48-62 U signs: does it occur in any ciphered passage that the period decipherment covers? If yes, align that passage to its deciphered clear text (tools/interlinear_align.py align with --clear-consumes if the pair shape fits; else a scripted alignment around the H-graded anchors either side) and read the U sign's meaning, grade C, with the leaf and line. KNOWN-ANSWER CONTROL, run first and reported: apply the identical procedure to 20 H signs with Bourdeau's values hidden; report how many it recovers correctly. Only if the control recovers >= 16/20 commit the U meanings to key.tsv (grade C, source column naming the leaf) and re-run decode_key.py --check and the judge; paste both. If no U sign occurs inside a deciphered passage, say so with the count checked -- that closes 1d as a non-test (no meaning source), not a negative.
Output: NOTES.md dated section "NEAR steps (1c') and (1d) (bMAT1D)"; ROOM done line <= 600 chars with: reverted judge, control k/20, U signs given C, new judge vs shuffled -1.78/-1.894. Do not edit NEAR.md. Report what was found; do not classify novelty. Final reply at most five lines.
