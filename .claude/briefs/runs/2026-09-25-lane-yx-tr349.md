YX-TR349 (Sonnet, wall-clock box 60 minutes; at most 2 subagents). Target: ciphers/clair349-este-guise-1556 (cardinal d'Este to the duc de Guise, 4 Jan 1556 o.s. = 1557, BnF Clairambault 349 f.3 = Gallica canvas f9 right page, ~33 lines of arbitrary symbols and figures, no word spacing). Key: Tomokiyo's identification, BnF fr.20974 cipher no.15 (substitution alphabet with 1-3 homophones per letter and nulls, plus a nomenclator), on disk as images/fr20974_p57_key.jpg and images/fr20974_p69_key.jpg. Parent: LANE YX orchestrator session_01PHaEdHeQY2FtMHLo9yGeoe. Kind: recovery (key source `published` identification of a `period` key sheet; the verifier decides the label). Read the lane COMMON, .claude/briefs/transcription.md (incl. "Symbol alphabets"), .claude/briefs/solver.md, the target NOTES.md in full (YX-CS349 part 2 lists canvas oddities), sources/cryptiana/web/guise.htm (credit Tomokiyo, rule 8). Paste the output of `python3 tools/intake_gate_check.py ciphers/clair349-este-guise-1556` at the top of your NOTES section; if it exits nonzero, stop and flag.
The key sheet defines the sign inventory, so the atlas comes from the key, not from the letter.
1. Key transcription (step 1 of 3): crop every symbol of the p.57 substitution alphabet (letter -> its homophones, and the nulls) into images/atlas/ with a code per distinct sign (S01, S02 ...; figures keep their digits), and write key_alpha.tsv (code, letter or NULL, crop file, grade H if legible, M otherwise). Then the nomenclator entries on p.57 and p.69 (code -> word) to key_nomen.tsv, H/M per row; note the pasted slip on p.69. Two blind reads by subagents of the key alone are enough where they agree; settle disagreements yourself from the image. Push.
2. Ciphertext, two blind passes: cut line crops of f9 right (tools/iiif_lines.py or Pillow on images/clair349_f9_right_full.jpg; check the overlay), give each subagent the line crops plus the atlas (not the key's letter values, not each other's output), and have it write sign codes per line (unknown shape -> `?` plus a description). Reconcile with tools/reconcile_passes.py; the 60% gate per line block applies; settle disagreements from the image. Write ciphertext.tsv (line, position, code, grade). Push.
3. Only if the reconciled ciphertext passes the gate: describe the layout in decode.json and run `python3 tools/decode_key.py ciphers/clair349-este-guise-1556 --check` with key_alpha.tsv + key_nomen.tsv as key (add an option to decode_key.py if two key files need merging, with a test; do not write a private decoder). Grade per token (H from the key sheet, M uncertain, U unkeyed). Write the spec specs/clair349-este-guise-1556.json (pattern: specs/antt-linhares-chave.json; language fr; ciphertext source and date) and paste `python3 tools/judge_plaintext.py specs/clair349-este-guise-1556.json --file reading.txt` verbatim. Do not run a re-derivation or any novelty search; the lane orchestrator briefs those.
At 80% of the box, push what you have and write the remaining steps as one line in NOTES.md. NOTES.md gets "## YX-TR349 (25 Sept 2026)". Touch only ciphers/clair349-este-guise-1556/**, specs/clair349-este-guise-1556.json, tools/decode_key.py with its test only if needed, and ROOM.md. No network needed (images on disk). Report what was read and where it was not; do not classify novelty; "cost: see the lane ledger".
