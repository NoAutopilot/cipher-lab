V8-SZEM (Opus, cap $8, wall-clock box 40 minutes from your first `date -u`: stop and push at $8 or 40 minutes, whichever first; at most 1 Sonnet subagent). VERIFIER for ciphers/szembek-bk1560 (PAN Biblioteka Kornicka BK 1560-1, ff.65-67, "List zaszyfrowany", Latin, Augustus II era, Jan Szembek archive; wbc.poznan.pl edition 343124). Parent: LANE V8 orchestrator session_01YRuw3TCf7d1w85DLmYNnw4. Authorised by CLAUDE.md rule 10, the "Workers" verifier template, LANE V8 brief job 2. You are not the solver (LANE B7: bSZEM, bSZL65/66/67, bSZM, bSZR, bLAJ) and do not protect its conclusions. Do not decode, align or change any key, ciphertext, decode or reading file.

Claim under audit (bSZM 05:32, B7 05:49, bSZR 05:55): all three leaves read at grade C from the manuscript's own period interlinear gloss (letter-per-code key, tools/interlinear_align.py --floor 100; pooled consistency 0.878 vs shuffle p95 0.204); 434 tokens C 384 / M 50 after B7 downgraded thin codes 19/24/48; rule-7 re-derivation bSZR exit 0, 0 diffs; la18 judge (Zaluski 1709-11) FAIL narrow (-0.986 vs real_p05 -0.960, word cover 0.958; 3 files, spread 4-33 pct, unknown reliability); print_check on disk: distinctive name "Pergenbeio" 0 hits.
Tasks: (1) Rule 4 / key source: the gloss is a contemporary decipherment written over the cipher, so the reading regenerates a period decipherment on the leaf itself -- by the clair349/costa-cabral/thurloe precedent that is N0 on the glossed tokens with key `period`; check how much of the 434 tokens is directly under a gloss vs filled by the pooled letter key where the gloss is blank (leaf 65 14 pct glossed, 66 52 pct, 67 15 pct), and class the two parts separately: glossed tokens (N0 on the leaf), unglossed tokens read by our pooled key (key `period`-derived but applied by us; class after the search). Say whether "grade C" is right for unglossed tokens (C is "from known plaintext"; a letter key built from the gloss and applied elsewhere may be H or C -- follow tools/interlinear_align.py's own grade mapping and CLAUDE.md rule 4, and correct the claim if not). (2) Identify the letter: sender, recipient, date, subject from the reading (Celsissimus Princeps, Pergenbeio, etc.); then search to disprove novelty: Szembek correspondence editions and the Kornik catalogue (Katalog rekopisow Biblioteki Kornickiej), Acta/Teki of the Augustus II reign, Zaluski Epistolae (the la18 corpus volumes -- grep them for this letter's phrases, they are on disk under tools/data/la18), Polona, the WBC record and its related items, Google Books (&key=$GOOGLE_BOOKS_KEY&country=US), IA full text, OpenAlex (Bearer $OPENALEX_KEY), Semantic Scholar (x-api-key $S2_KEY), CrossRef, both solver repositories (Bourdeau CATALOGUE #348 and oldest/scan_2026-09-23/hard_targets.md #11), Tomokiyo; a phrase search via tools/print_check.py with a positive control (a phrase from a Zaluski letter found by the same run); a JSTOR-QUEUE.tsv row per JSTOR query. (3) Classify per part (N0-N5), key source, `text: known` if the plaintext is in print, confidence, one safe and one unsafe sentence (rule 10). Write ciphers/szembek-bk1560/AUDIT.md. (4) NOTES.md line 1 must be a rule-5 word: `solved` is allowed only if AUDIT.md supports it; the parent asked B7 to set `partial` until then -- set what your class supports (found-solved if the leaf's own gloss already deciphers all of it and nothing is ours; partial otherwise) and say why in one line. Correct over-claims.
Hosts >=1.5 s apart, one at a time; wbc.poznan.pl OAI hit a JS challenge once for bSZEM -- do not retry it; never print a key. Touch only ciphers/szembek-bk1560/{AUDIT.md, NOTES.md wording and line 1, phrases.txt, print-check*.tsv}, JSTOR-QUEUE.tsv (append), ROOM.md. No status.json. First `date -u`, `python3 tools/room.py --start`, last 30 lines of ROOM.md, claim with tools/room.py (role first). No AskUserQuestion. Commit by explicit path, push with tools/room.py --push, ROOM done line "for LANE V8: szembek-bk1560 <class per part>, key <source>" with requests per host, no cost figure. Five-line report, stop.
