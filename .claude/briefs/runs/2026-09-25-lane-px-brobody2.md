PX-BROBODY2 (Sonnet, cap $6 as a stall alarm; the orchestrator watches your cost, you cannot; at most 2 subagents). Target: ciphers/antt-msliv0638-brochado-1712. Job: widen the body sweep for ciphered passages, targeted by letter number. No decoding. Parent: LANE PX orchestrator session_01KapVpHVzNpnnQce5C8c3LY. Read the lane COMMON, .claude/briefs/transcription.md, NOTES.md section "PX-BROBODY", body_leaves.tsv (never re-check a leaf listed there) and plaintext_appendix.tsv (the letter numbers that have a period decipherment). PX-BROGLYPH is writing the appendix files: touch only the files named here.
Finding of PX-BROBODY: each body letter's page/letter number matches the appendix's "Carta N" (m0179 = p.80 = Carta 80). So:
1. Map image -> letter number for the body (read the numbers from thumbnails at a stride, interpolate, confirm a few), into body_letter_map.tsv.
2. Check every letter that the appendix deciphers (to confirm its cipher runs, as controls) only where cheap; spend most of the budget on letters with NO appendix entry, and on the letters around m0276, looking for cipher runs (small numbers and single letters with dots, inline).
3. Full resolution for up to 12 leaves with cipher runs not yet transcribed; two blind passes (two subagents) of each run with context, appended to body_passA.tsv / body_passB.tsv; reconcile into body_ciphertext.tsv (grades H/M), flag each run as control (letter has an appendix entry) or undeciphered.
DigitArq (only worker on it; say so in ROOM): one request at a time, >=3 s apart, at most 60 requests; tools/digitarq_fetch.py, docId 7049343e7af44ba7b4ec18e74e1b2cd7. Push after each step. Touch only ciphers/antt-msliv0638-brochado-1712/{body_letter_map.tsv,body_leaves.tsv,body_pass*.tsv,body_ciphertext.tsv,images/body/**,NOTES.md (new section "## PX-BROBODY2")} and ROOM.md. Write "cost: not visible to me". Report leaves checked, letters mapped, undeciphered runs and tokens found, requests per host.
