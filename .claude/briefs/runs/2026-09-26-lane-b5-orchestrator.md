LANE B5 ORCHESTRATOR (breadth; successor to LANE B4, whose handoff is STATUS.md "LANE B4 handoff (session_01V2WMavWGMeAUYELUpmyEcX), 25 September 2026, 21:31-23:57 UTC (closed ...)"). Opus. Cap $25 own usage; hand off at $25 or 300k context (write your "LANE B5 handoff" section in STATUS.md from the first worker report; re-check it survived after every push: commits 9a1e34b and a34cd00 dropped B3's and B4's sections in room.py --push merges). Started 26 Sept 2026 at the 00:54 UTC clock read by parent 7e (session_01SvjMDFfJZJ3uK47RYxAQrM). Authorised by CLAUDE.md Pipeline 3a (breadth lane: a spec and one first cheap test with its matched control before any campaign) and the selection rule of Pipeline 3 (pools first). Window allowed; `rejected` anywhere stops everything.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md Pipeline 3 and 3a, rules 3 and 5 (near solves), `.claude/briefs/breadth.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`, `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (reuse it as your common file, header changed to B5), the LANE B4 handoff and its spec table, QUEUE.md section "Re-rank for LANE B5, 26 Sept 2026, 00:03 UTC" (your queue), NEAR.md, UPDATES.md tail, RETRO-2026-09-26a.md proposals 1-2 (size the brief for a shift: this brief names a first wave and a reserve wave); `python3 tools/room.py --digest "2026-09-26 00:40"`. ROOM claim "LANE B5: breadth, from the SCOUT-RERANK queue". The other account's LANE AX (live from 00:15) holds the WVO Nassau pool, antt-msliv0638-brochado-1712 and pro3055-clinton-1779: never touch them. LANE GOLD3 holds koehler-1944, debosnys-1883, kaliningrad-2015.

First wave (Sonnet workers, one host per worker, wall-clock box in every brief, cost read by you with get_session, never the worker's own figure; each brief pastes `python3 tools/intake_gate_check.py <slug>` exit 0, or sends a check-solved worker first per `.claude/briefs/check-solved.md`, whole-volume grep, the clair349 lesson):
1. Re-rank row 1, Matignon/Mayenne/Forget despatches (BnF fr.15572 + fr.15571 f.177; github-held `dbourdeau/cyphersolver` `matignon1586`, MIT code, CC BY 4.0 text: clone once, grep only that folder, cite it). Create ciphers/<slug>/ with NOTES.md (status word, sources, what is established vs inferred), check-solved (rule 1: the printed Lettres/Correspondance of Mayenne and Matignon, Tomokiyo, DECODE, both solver repositories -- Bourdeau's own write-up says which leaves he read and which not), then the first cheap test: the verified Cipher-1 key on the leaves transcribed only in part, control the same key on a scrambled-cipher-order copy of the same leaves; and the Cipher-3 interlinear decipherment leaf transcribed directly. Cap $4 plus $6 if an image fetch and two blind passes are needed (breadth.md). Gallica: IIIF only, one fetcher, >= 2 s apart.
2. Re-rank row 2, Lope Hurtado de Mendoza siblings (RAH Salazar 9/26; DECODE R9634/R9646/R9649; `lopehurtado` in cyphersolver): the 49 code values from R9644's contemporary clear copy applied to the three untried records; control the same key on three random-digit strings of matching length and alphabet; grade by eye (C/M per token), no es17c judge score without the era-mismatch flag. DECODE full-size images are account-blocked (Access playbook): the material is the repository's transcription; if none exists for a record, the worker stops at that block and says so. Cap $3.
3. Re-rank row 3, Castelcicala 1816 (ASNa Esteri 2337; DECODE R9553-R9589; `castelcicala1816`): hand-read the un-glossed 1816 letters against the 162 confirmed values and the Decazes/Florida crib vocabulary; no it16 judge score without the 300-year era flag. Cap $4; strongest model only for the read step if the Sonnet read stalls.
4. NEAR steps from the B4 handoff, each with its matched control and the numbers into the NEAR row whatever the result: blitz-ciphers German masc rerun with a de20 judge ($1), then the six-page fetch ($6) only if the rerun leaves German open; untersberg-code joint alignment of the six lines against Herzog's Hs 13/3/3a/11 with the shuffle control bUNT5 used, and a blind transcription of the confirmed Hs. 2398 leaf opening ($6); bullet-tuscany-1944 stays parked; mccormick-1999 only if you can name a code-word model whose control passes its gate.
Reserve wave (start it when the first wave has fewer than two live workers, not after it is spent):
5. Re-rank row 4, Riksarkivet R4282 against R4284's key-test leaf as a crib, control the same alignment against three other Chifferklaver key records ($4).
6. Re-rank rows 5-9, one $3 Sonnet sampling pass each (RAH 9/23-25; BAV Barb.lat 6956; ARA Carpio-Fuenmayor; ASV France 17-18; ASV Spain 364C): sample 3-5 pages, count real signs per page, language, copy-free status, and write specs/<slug>.json only where the count supports a pool (2,000+ signs); hosts per the Access playbook table (RAH sits behind Anubis: `tools/browser_fetch.js --binary`; DECODE listing is login-free via tools/decode_list.py; one host per worker, good-citizen limits).
7. A $3 Sonnet scoring pass over QUEUE.md's unscored VX-*/KX-*/KT-*/PX-* rows into QUEUE-scores.json (RETRO-2026-09-26a proposal 2), ranked alongside the re-rank rows, so your successor's queue exists before you close.
Rules: a first test never runs without the intake step (breadth.md) and the gate output pasted in the brief; every family through tools/family_run.py where one fits (masc, homophonic, periodic_vigenere, running_key); every done line carries target and control numbers (room.py warns otherwise); a control-backed gap or a non-test negative is `partial` and a NEAR.md row (you write the row; workers never edit NEAR.md, LEDGER.md, ASSIGNMENTS or status.json); never `closed-negative` without every family in the ladder logged with a passed control; a reading is reported with tools/judge_plaintext.py output pasted and its era-matched corpus named (tools/data has fr16, fr18, it16, es17, es17c, de16, de20, pt17, pt18, nl20, la; none for 16th-century Spanish or 19th-century Italian -- say so, grade by eye); rule 10 wording only; no AskUserQuestion, the owner is away until about 03:00 UTC 26 Sept, anything for them is an ASKS.md row. Spawn workers with create_session giving source_url https://github.com/NoAutopilot/cipher-lab and source_revision main explicitly, and a prompt that opens by naming the job brief file and the one-line job, rule text in the brief. Write each job brief to `.claude/briefs/runs/2026-09-26-lane-b5-<job>.md` before spawning; ledger every report (LEDGER.md row with the get_session cost and an outcome code from tools/ledger_check.py's list) and a hub-seed/ASSIGNMENTS.md line; archive_session after ledgering; keep your check-ins with send_later (30 minutes while workers run) and re-arm before ending each turn; post "for the parent: ..." in ROOM.md with your own cost and context estimate at every check-in. A reading ready for a verifier goes to ROOM as "for LANE V7: ciphers/<t> reading ready" with the search log named. Hand-off: write the LANE B5 handoff section with a results table like B4's, ledger yourself, post the ROOM handoff line, stop.
