LANE B3 spec writer bSPEC2: ten specs for UNSOLVED-SURVEY.md ranks 21-30, then a judge-block repair pass on ranks 11-20. Sonnet (claude-sonnet-5). Cap $6 or 45 minutes, whichever first. No subagents.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (read it first; it governs) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Part 1 -- ten specs, ranks 21-30 (Erba murder letter 2006; SS radio message to Lippert 1944; Rayburn 2004-06; Blitz ciphers; Chinese gold bars 1933; YOG'TZE 1984; Sufi fiddle inscription; Untersberg code; Fair Game end-credits letters 2010; censorship-manual steganograms WW2). Model on specs/README.md and on specs/lima-1916.json and specs/mccormick-1999.json (the repaired one). Fields: slug, rank, schmeh_top50, name, date, language_candidates, ciphertext (as transcribed, with ciphertext_source and date) or ciphertext_pending (exactly where it is and what one fetch gets it), alphabet, constraints, cheap_tests_in_order (three, cheapest first, each naming its matched control, a family_run.py family where one fits, and est. cost; the first runnable by a $3 Sonnet worker in 30 minutes, disk-only where possible), matched_control, judge block, value, model, status line dated 25 Sept 2026 ('open' etc. per the survey row; never solved/new), schema_note, written. Sources: the survey row, sources/schmeh/top50-scienceblogs.txt and manifest.tsv, and the three posts already on disk (25-ss-radio, 43-rayburn, 49-goldbar: read the .txt). Network: sources/schmeh/posts/ lacks the other seven posts; you may fetch those seven Cipherbrain posts from scienceblogs.de ONLY after ROOM.md shows bCYL's done line (scienceblogs.de is one worker at a time); save each unmodified as sources/schmeh/posts/NN-slug.html plus a tools/html2text.py .txt and a manifest.tsv row; at most 8 requests, >=2 s apart, no images. If bCYL is still live when you reach that step, write ciphertext_pending instead and say so. Where an item has no ciphertext at all (film, legend, a picture), the spec says that plainly and its first test is the fetch or the provenance check, not a solve.
Judge block, every spec: `language` set to a code actually wired in tools/judge_plaintext.py (read its LANG_CORPORA; it fails closed on an unwired code -- if the right language has no corpus, say so in schema_note and set the nearest honest option or leave language out and say the judge cannot be used yet), `min_word_cover`, letters_min/letters_max from the real size, control_samples, cribs where the item gives any. Run judge_plaintext.py on a dummy file for each spec and confirm it loads without a fail-closed error (or that the fail-closed is the intended, documented state).
Part 2 -- repair: check the judge block of each of the ten rank 11-20 specs (copenhagen-1835, pollaky-1865-1875, catokwacopa-1875, te-wood-1950, cylob-c1995, scorpion-1991, mccormick-1999, rubin-1953, mlh-1976, bullet-tuscany-1944). Leave rubin-1953 and bullet-tuscany-1944 alone if a live LANE B3 worker's claim in ROOM.md names them (bRUB2, bBUL2 repair their own). For the rest, add a wired language and min_word_cover where missing, with a one-line note in the block saying who changed it and when; never touch cheap_test_done. Report the before/after table in specs/cheap-tests/judge-repair-2026-09-25.tsv.
Add ` spec: specs/<slug>.json` to each rank 21-30 row's "Working on it" column in UNSOLVED-SURVEY.md (fetch+rebase right before; keep others' edits). Push after every three specs. Done line: the ten slugs, ciphertext on disk vs pending, and the repair count.
