IMAGE CIPHER DETECTOR (parent worker detIMG; Sonnet, cap $10; stop at the cap with what is on disk). Started 24 Sept 2026 19:30 UTC by the parent (cipher-lab-7b, session_01K7ZbE95o1pUW5gof8VA5PR) under the owner's goal of unique results: every scout so far searched catalogue text for "chiffre"; this worker looks at the page images themselves, so leaves no finding aid names can be found.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md (rules 3, 6, 9, Usage 8, the good-citizen rule), `.claude/briefs/detector-test.md` (the printed-text detector's lessons: index pre-filter, controls before any sweep), tools/iiif_lines.py --help, tools/gallica_folio.py --help. ROOM claim line "detIMG: image cipher-page detector -- files: tools/cipher_page_detector.py, tools/tests/test_cipher_page_detector.py, sources/detector-img/, QUEUE.md section 'Image detector'; host gallica.bnf.fr (one fetcher slot; if two Gallica claims are live in ROOM, wait)".

Deliverables, in order:
1. `tools/cipher_page_detector.py` (numpy and PIL only; `--help`; offline test in tools/tests/ on a fixture of 4 small images committed under tools/tests/fixtures/, under 200 KB total): given a page image (or a IIIF canvas at 400 px width), compute cheap layout features that separate cipher from plain text: fraction of ink in small isolated components (numeral groups and invented signs are short, spaced tokens), the distribution of token widths and gaps per text line, digit-like stroke regularity, presence of interlinear writing (a second, smaller row between main lines), line count and spacing. Score with a small logistic model whose weights are fitted by `--fit` on labelled files and stored in a JSON beside the tool. `--scan MANIFEST_URL --out DIR` walks a IIIF manifest at 400 px (one request at a time, 1.5 s apart, at most 300 thumbnails per session, browser User-Agent, everything to disk once with a manifest.json) and writes scores.tsv (canvas, label, score, features).
2. Controls (rule 3), before any sweep: positives = the cipher pages already on disk under ciphers/*/images (their images/manifest.json entries say which canvases are cipher; use at least 25 from at least 8 different targets, hands and centuries: Gramont, Danzay, Salviati, Seure, Lodewijk, Saxony, Blathwayt, Eckert, Letellier, Carpi); negatives = plain pages from the same volumes (fetch 30 at 400 px from the same Gallica arks at folios the manifests list as plain, or take them from the reference copies on disk where a target folder holds whole-page images), plus 10 printed pages. Hold out a third. Report precision and recall on the held-out third in the tool's docstring and in QUEUE.md; the gate to sweep is recall >= 0.85 and false-positive rate <= 0.15 on the held-out set. If the gate fails, stop after one round of feature changes and report what separates and what does not; that is a result.
3. First sweep, only if the gate passes: (a) one volume known to hold cipher leaves the catalogue names (BnF fr.3986 or fr.3987, Nevers letters, QUEUE.md rows KS-01 to KS-07 give the arks) as a live test: does the detector find the known leaves and what else does it flag; (b) one volume of the same series with no "chiffre" in its finding aid (pick the next fr.39xx ark in the Nevers series from sources/ on disk). Every flagged canvas above the threshold goes to QUEUE.md under a new section "Image detector (24 Sept 2026)" with ark, canvas, folio label, score and a one-line description of what is on the page (read the 400 px thumbnail; fetch nothing larger), marked `copy-free`, kind unknown, for check-solved; never call anything a cipher letter without the image showing cipher signs, and never call anything unsolved (rule 10; check-solved decides).
Requests: gallica.bnf.fr only, at most 300 thumbnails plus 2 manifests; report the count. No other host. Push with `MSG=... python3 tools/room.py --push <paths>`; ROOM done line with precision, recall, hits and cost; final paragraph of five lines. Stop.
