ROLE: KEY READING + RECOVERY, La Luzerne code, Huntington mssDE (LANE R worker R6). Model Opus, cap $14; passes on Sonnet subagents (claude-sonnet-5).
Target: ciphers/huntington-luzerne-destouches-1781 (U1). Read NOTES.md, images/inventory.tsv (R1, 05:36 UTC: mssDE 68 (31 Jan 1781), 37 (26 Feb) and 55 (3 Mar) each carry an interlinear French decipherment in a contemporary hand; mssDE 108(A) (dated 16 Jan 1781 on the leaf, ~550 groups over pp.1-6) carries none), and R2's key_tomokiyo.tsv, alignment_yale_8jan1781.tsv, build_key_tomokiyo.py (Yale 8 Jan 1781 letter: 73 figures grade C; the code is two-part with homophones, so no interpolation between figures). Images are on disk; do not refetch Huntington (one request per missing page at most, >=1.5 s).
Step 1: is mssDE 68's own decipherment complete? Two blind Sonnet passes of the cipher groups of mssDE 68, 37 and 55 (passA/passB_decipher.tsv: item, page, line, idx, group), tools/reconcile_passes.py, settle disagreements on the image; then pair each group with the interlinear plaintext written over it (read from the image yourself) into pairs_contemporary.tsv (grade H). If every group of mssDE 68 is deciphered on the leaf, write that plainly in NOTES.md: mssDE 68 is read by its own contemporary decipherment (a transcription of it, not a solve), and say so in ROOM `for LANE W` at once.
Step 2: key.tsv = contemporary pairs (H) + Tomokiyo/Yale pairs (C), with conflicts listed, not merged silently.
Step 3: mssDE 108(A): two blind Sonnet passes (passA.tsv/passB.tsv) of pp.1-6, reconcile, settle on the image, ciphertext.tsv; decode.json + tools/decode_key.py --check exits 0; grade every group (H from the contemporary key, C from Tomokiyo, S only for a context-fitted value with a matched control: synthetic 108(A)-length letter in a two-part code of the same size and the same key coverage, report the control's accuracy; M otherwise; U unread). Push after each step.
Search log in NOTES.md (what was searched for a prior decipherment or printed text of the 16 Jan 1781 letter and where it was not found: Doniol vol. 5 already covered by check-solved; add Stevens's Facsimiles index and Founders Online only if cheap). End: ROOM `for LANE W: ciphers/huntington-luzerne-destouches-1781 reading ready`. Do not classify novelty.
