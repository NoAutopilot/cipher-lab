PARENT WORKER KEYPROBE-TOOL (Sonnet, wall-clock box 60 minutes, no subagents; network only to the API hosts named below, one test call each). Why: the owner adds keys and logins to the environment and nobody notices -- GOOGLE_BOOKS_KEY was set 20 Sept and first used from the cloud 25 Sept 22:55 (a country=US parameter was all it needed); IA_USER/IA_PASS login worked from 23 Sept and no worker tried a borrow until 25 Sept 22:32; CORE_API_KEY was probed only when the owner asked. Work sat in ASKS.md all day waiting on access we already had. Job: make the check mechanical.
1. Write tools/key_probe.py: for every credential the CLAUDE.md Access playbook names (EUROPEANA_API_KEY, DPLA_API_KEY, DDB_API_KEY, CORE_API_KEY, APE_API_KEY, GOOGLE_BOOKS_KEY, OPENALEX/Semantic Scholar keys if any, IA_USER/IA_PASS, JSTOR_USER/JSTOR_PASS, DECODE_USER/DECODE_PASS, HATHI_*, and any other *_KEY/*_USER/*_PASS the playbook lists -- read it in full), report: present (yes/no, by `os.environ`, never the value, never the length), and works (one documented test call per key, exactly the call the playbook gives, with the known cloud fixes: Google Books `country=US`, CORE trailing-slash path, Europeana `wskey=`, DPLA `api_key=`, IA `services/xauthn/?op=login` only -- never a borrow; DECODE presence-only, never a login attempt, since the playbook allows one attempt per session by a worker who needs it; JSTOR presence-only, Cloudflare blocks the cloud). Output a Markdown table and write it to KEYS-STATUS.md at the repo root with the UTC time, plus a `--json` mode. Exit 0 always; a failing key is a table row, not a crash. Credential rules: never print, never `env`, never `curl -v`/`set -x`, redact any error body that could echo a key. Offline test under tools/tests/ with a fake environment (monkeypatched os.environ and a stubbed HTTP layer).
2. Wire it in: (a) CLAUDE.md Access playbook: one paragraph "Key probe" -- `python3 tools/key_probe.py` is the first command of every parent check-in and of every worker whose brief names an external host; a worker may not write an ASKS.md row asking the owner for access, nor a LOCAL-QUEUE.tsv row, until the probe shows the relevant key absent or failing, and the row must quote the probe line. (b) .claude/briefs/parent.md: add the probe to the check-in list (before duty 1) and a rule that a changed probe result (a key that went from absent to present or from failing to working) triggers a re-scan of open ASKS.md and LOCAL-QUEUE.tsv rows for work now doable from the cloud, assigned to a worker in the same check-in. (c) tools/intake_gate_check.py or tools/room.py --start: print the last KEYS-STATUS.md line ("keys: N present, M working, last probe <time>") so every session sees it. (d) UPDATES.md rows for each instituted change.
3. Run the probe for real once, commit KEYS-STATUS.md.
4. Re-scan: read every open ASKS.md row and every queued LOCAL-QUEUE.tsv row against the probe result and the playbook, and write RESCAN-2026-09-25.md at the repo root: a table of rows a cloud worker can do now with an existing key or login (which key, what call), rows that truly need a person, and rows already reassigned today (IA-BORROW: ASKS 53, 55b, 59, L7, Dorabella; GBOOKS: L9, L16). Post the count in your done line and a ROOM line "for the parent" listing the doable rows.
Post a ROOM claim first and a done line at the end as `parent worker KEYPROBE-TOOL (Sonnet, <session id>)` with tools/room.py; keep tests green; commit, fetch and rebase, push (never force). Never call AskUserQuestion.
