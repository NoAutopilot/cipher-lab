PARENT WORKER GBOOKS (Sonnet, wall-clock box 40 minutes, no subagents, network: www.googleapis.com/books only, plus git). Job: answer LOCAL-QUEUE.tsv rows L9 and L16 from the cloud through the Google Books API now that CLAUDE.md's Access playbook (Google Books paragraph, "Cloud fix 25 Sept 2026") records that `&country=US&key=$GOOGLE_BOOKS_KEY` clears the 403. Credential rules: never print the key, never `env`, never `curl -v` or `set -x`; build URLs in a script that reads the key from os.environ.
1. L9 (ciphers/fr16092-maisse-1582): Boucher, Lettres de Henri III, tomes V-VI (1582-83). Find every edition/volume the API returns for the series (query the title with inauthor Boucher, and the 1959-2012 Societe de l'histoire de France volumes), note viewability per volume id; then run snippet queries scoped to those volumes for "Maisse" with the 1582-83 dates and for "chiffre", "dechiffre", "en chiffres"; record each volume id, viewability and every textSnippet verbatim (with the API's own snippet text, no invention) in ciphers/fr16092-maisse-1582/NOTES.md under a heading "Google Books API (GBOOKS, 25 Sept 2026)"; state plainly whether any letter to Maisse is marked as from a decipherment, or that snippets cannot show it.
2. L16 (ciphers/pro3055-clinton-1779): Saberton, The Cornwallis Papers vol.6; the earlier NO_PAGES verdict for ids PPyytQEACAAJ, WJxDtAEACAAJ, cLR9zQEACAAJ, cRl7zQEACAAJ, Q8WBzQEACAAJ was recorded without country=US -- re-run those ids' volumes/<id> lookups and the snippet queries the row names ("16 August 1781", "3 October 1781", Clinton + cipher) with country=US; record verbatim snippets or NO_PAGES/none in ciphers/pro3055-clinton-1779/NOTES.md under "Google Books API (GBOOKS, 25 Sept 2026)".
3. For each row: if the API answered the row's question (a snippet that settles it, or a definite none on a full-view volume), set the LOCAL-QUEUE.tsv row status to `done <date>` with a one-line result; if the API cannot see inside (NO_PAGES on every edition), leave the row `queued` and append " (API with country=US checked 25 Sept: NO_PAGES; browser search-inside still needed)" to its instruction. Do not touch other rows.
4. Also scan the other queued LOCAL-QUEUE.tsv rows and ASKS.md open rows for any that name Google Books full view as the route (e.g. Hamilton 1650 HMC report, Boucher, Saberton); if the volume is full view under country=US, note the volume id and viewability in the row's target NOTES.md in one line (do not do the read; a follow-up worker will).
Rate: at most 60 API calls, 2 s apart. Post a ROOM claim first and a done line at the end as `parent worker GBOOKS (Sonnet, <session id>)` with tools/room.py; commit, fetch and rebase, push (never force). Never call AskUserQuestion.
