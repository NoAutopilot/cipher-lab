PARENT WORKER RETRO-APPLY-U (Sonnet, cap $6 of usage, wall-clock box 45 minutes, no subagents, no network except git; the container is a depth-50 clone). Job: apply the brief- and tool-only proposals of RETRO-2026-09-26i.md (retrospective u, 26 Sept 2026 16:42 UTC) for parent 7i (session_01U1NiMakQ8PPySH8WixXsmc, "LIVE parent 7i · Orchestrator 10"). Authorised by CLAUDE.md "Improvement loop" (the orchestrator applies changes that only touch briefs, tools or workflows; anything that changes the goal, the spend or the owner's asks goes to ASKS.md). Written 26 Sept 2026 at the 17:30 UTC clock read. Copied from 2026-09-26-parent-retro-apply-t.md.

First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker RETRO-APPLY-U (Sonnet, <session id>)`; read CLAUDE.md, SYSTEM.md section 7, RETRO-2026-09-26i.md in full, then each target file before editing it. Edits minimal, in the file's voice. Apply the four proposals as the retro's diffs, in order:
(1) `tools/file_shrink_guard.py <paths...>` with offline test `tools/tests/test_file_shrink_guard.py` (the PR-LAND-3 numbers as the regression case: a 26-line TSV replaced by 1 line "PLACEHOLDER" fails; 26 -> 24 lines after a row edit passes; a HEAD commit message containing "shrink", "regen", "restore" or "AX2-SHRINK" passes regardless). The guard compares each named path's working-tree or staged line count against `git show HEAD:<path>` and exits nonzero naming (path, before, after) when the count drops below 20 percent of the prior count AND below 5 lines. Also call it from `tools/room.py --push` on every path being pushed (same refusal shape as headings_ok), add the CLAUDE.md Usage 8a precedent clause the retro gives, the README.md common-tail bullet ("before your final push, any worker that lands or applies content into an existing tracked file runs tools/file_shrink_guard.py on every file it touched and pastes the output in its done line"), and the SYSTEM.md 3a row -- then run `python3 tools/system_map_check.py` and paste its output (it must pass before you push).
(2) Lineage depth as a standing rule in `.claude/briefs/parent.md` after the "Lineage depth" paragraph -- BUT with this amendment, which supersedes the retro's "every parent from the UI" wording (hub-seed/SUCCESSOR-PROMPT.md, 7h's hand-over block of 17:05, the plan in force): a UI-created parent sits at depth 0 and its line can hand over by `create_session` six more times; every hand-over line records the successor's depth and how it was created ("successor created via UI" or "via create_session, depth N"); the parent at depth 6 files the ASKS row asking the owner to create the next one from the UI before it hands over, so the owner-created step recurs once every seven hand-overs, planned not discovered; a hand-over that had to use create_session at depth 6 because the owner was away names that in ASKS.md at once. Add the depth early-warning to `tools/orphan_check.py` as the retro's docstring describes (count hand-over lines in STATUS.md's "Parent handoff" sections since the last one saying "created via UI" or "from the UI"; WARNING at 5 or more, printed at every run), with a test case in `tools/tests/test_orphan_check.py` if that file exists (create it if not, offline).
(3) CLAUDE.md Usage 6: the reconciliation-pass pricing paragraph, as the retro's diff (bMALS: N reads + 1 reconciliation, not N reads).
(4) CLAUDE.md Operating model: the two paragraphs as the retro's diff -- only a parent commits an edit to a `tools/*_runner_prompt.md` (a lane or worker hands the change up by ROOM line), with the self-containment grep; and a verifier that assigns N3 or better appends the SECOND-OPINIONS-QUEUE.tsv row in the same session as AUDIT.md. Also add the same one sentence to `.claude/briefs/verifier.md` (step 5 area) so the verifier brief carries it, not only CLAUDE.md.
One UPDATES.md row per instituted change (`By` = "RETRO-APPLY-U for parent 7i", clock-read time). Never touch BUDGETS.md or rate-limit rules; never touch status.json or any ciphers/ folder. Run every tools/tests/ file you touch directly (pytest is not installed) and paste the results. Before every commit run `git diff --stat` and paste it; commit by explicit path, push only through `python3 tools/room.py --push <paths>` (this retro's own item 1 is about a worker that did otherwise). ROOM done line `done: for parent 7i: RETRO-APPLY-U: N of 4 items applied (...), tests <pass/fail>, system_map_check <pass/fail>, UPDATES rows M`; one-paragraph reply; stop. Never call AskUserQuestion; never print or commit credentials; never write the owner's name.
