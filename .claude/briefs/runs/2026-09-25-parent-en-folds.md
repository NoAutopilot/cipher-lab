PARENT WORKER EN-FOLDS (Sonnet, wall-clock box 40 minutes, no subagents, network only to gutenberg.org and github). Job: satisfy the rule 3 fold-count amendment (UPDATES.md 20:01, CLAUDE.md rule 3) for the English judge corpus, flagged in ROOM.md 21:48 and QA/2026-09-25-2140.md item at line 71 and failure 1. Read CLAUDE.md rule 3, tools/judge_plaintext.py (LANG_CORPORA, the held-out method), tools/data/fr18/README.md and tools/data/es17c's README (the worked examples of a per-fold false-negative spread), then:
1. Compute the leave-one-out held-out false-negative rate for the current 2-file en corpus at N200 and N500 with the same window method the fr18 README documents, plus the shuffled-null false-positive rate. Report the per-fold values, not only the blend.
2. If the spread between the two folds exceeds 0.05 at either N, or either fold's rate exceeds 0.10, add up to three more public-domain English prose sources of the periods the en specs need (check specs/*.json for language en and their dates: mostly 19th-20th c. English) from gutenberg.org (plain text, one fetch each, >=2 s apart), put them under tools/data/en/ with a README naming source, licence and letter count, and recompute the folds.
3. Write tools/data/en/README.md with the per-fold table, the blended rate and the null FP rate, dated with the clock-read time; add one line to the rule 3 paragraph in CLAUDE.md only if it names corpora individually (else leave CLAUDE.md alone); add an UPDATES.md row.
4. Re-run the two in-window verdicts that cited en without a spread (ciphers/blitz-ciphers/NOTES.md lines 140-142, ciphers/mccormick-1999/NOTES.md cheap test 4): append one line under each stating the fold spread now on file and whether the verdict stands (do not change the verdicts themselves; those lanes own them).
Never touch BUDGETS.md or rate-limit rules. Post a ROOM claim line first and a done line at the end as `parent worker EN-FOLDS (Sonnet, <session id>)` with tools/room.py; commit, fetch and rebase, push (never force). Never call AskUserQuestion.
