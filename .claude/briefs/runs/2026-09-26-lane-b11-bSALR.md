bSALR -- fr2933-salviati-1525: re-run LANE R8's design families on the pass-C spec with the control at the measured error (LANE B11 job 1). Opus. Cap USD 10, box 60 minutes from your first `date -u`. Disk only, no hosts, no subagents, no image reading.

Read first: `.claude/briefs/runs/2026-09-26-lane-b11-common.md` (governs), CLAUDE.md rule 3 in full (the 11:39 measured-error sentence, the judge-floor sentence, the repeated-attempt non-test shape) and rule 5, ciphers/fr2933-salviati-1525/NOTES.md sections "## DSN", "## DSN2", "SALV-DIAG" and bSALC's pass-C section, HYPOTHESES.md rows 29-54 (R8-DSN, R8-DSN2, SALV-DIAG), the R8 briefs `2026-09-25-lane-r8-dsn-salviati-design.md` and `2026-09-25-lane-r8-dsn2-salviati-marks.md` (their exact --param strings, restarts and seeds), `python3 tools/family_run.py --help` (`--measured-error`, `--shuffle-target`).

Intake gate (orchestrator ran it 12:24 UTC): `fr2933-salviati-1525: open (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

State: specs/fr2933-salviati-1525.json was rebuilt by bSALC from the pass-C votes (2,839 tokens, 236 types; estimated 6.4% error per sign, model-based, below SALV-DIAG's 8-9% crossover). Every R8 negative was run at err=0.05 against a transcription then measured at 14.6% box disagreement, so they are non-tests (rule 3); this job re-runs them where the control is at the measured error.

Units and pricing: up to five control+target variants; R8-DSN's rate was 4 variants in 24 minutes for USD 11.45 (Fable), so budget about USD 2.5-3 and 8-12 minutes per variant (3 control seeds + 1 target seed, R8's restarts). Do not start a variant that would cross 80% of the cap or the box (CLAUDE.md Usage 6). Run them in the foreground, one at a time (no concurrent background computation, GOLD-K3).

Order (stop at 80%; the ones not run are listed as "not run" with the reason):
 1. DSN regular: `--family syllabary --measured-error 0.064 --param err=0.064` plus R8-DSN's other params, 3 control seeds, R8-DSN restarts (6), gate 0.6. Then the same with `--shuffle-target 1` (the judge floor for this family at this N) -- run this one whatever the target's verdict.
 2. DSN2 V1 (marks=mixed), err=0.064, R8-DSN2 restarts (24).
 3. DSN2 V3 (bases=8, boundary=1), err=0.064, restarts 24.
 4. DSN2 V2 (marks=mixed, boundary=1), err=0.064, restarts 24.
 5. DSN irregular assignment (R8-DSN's irregular param), err=0.064.
 For variants 2-5, run `--shuffle-target 1` only if the target's judge PASSes or its score/symbol reaches the control's range; a PASS on the shuffled decode voids the judge as a gate for that family (rule 3).
 Judge: the spec's own judge block (Italian, it16 era corpus; the lane brief's "fr16" is a slip -- this target is Italian). If family_run's label-note says the control error is below measured, you set the params wrong: fix and re-run, do not log it as a negative.

Per variant record: control mean and range at 6.4%, target score/symbol against the control's, cross-seed agreement if more than one target seed was run, judge line, Italian word hits in the decode. family_run writes HYPOTHESES.md rows; add a "## bSALR: R8 families at the measured error (26 Sept 2026, LANE B11)" section to NOTES.md with a one-table summary beside R8's 5% numbers, and whether each family's outcome is now a control-backed negative at the measured error (both numbers) or still a non-test (control below gate at 6.4%). A target that beats its control and PASSes the judge with a non-PASS shuffle floor: write reading_bsalr.txt + a regeneration script, grade S/M per token, and put "flag: salviati reading candidate" in ROOM -- no decode_key.py step (no key yet); the orchestrator arranges the rule-7 re-derivation before V9.

Do not edit NEAR.md, LEDGER.md, ASSIGNMENTS or status.json (orchestrator). Do not touch images/, strips/, glyphs/, atlas_review/ (a concurrent shrink worker, bSALS, owns them); you write only HYPOTHESES.md, NOTES.md (your section), families/, control_curve.tsv, reading_bsalr.* if any. Commit by explicit path; `python3 tools/room.py --push <paths>` after each variant. ROOM claim first; done line at most 600 chars, per variant "<name> ctrl <mean> / target <score/sym> vs ctrl <score/sym>, judge <verdict>". Final reply at most five lines. Report what was found and where it was not found; do not classify novelty.
