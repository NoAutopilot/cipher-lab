ROLE: SCOUT scDIGI2 (LANE N4), DigitArq (ANTT) cipher letters by description, not by stride. Sonnet. Cap $5.
Read first: QUEUE.md "PARES / DigitArq cipher letters" (scPARES and scDIGI sections), tools/digitarq_fetch.py --help (the anonymous full-resolution API scDIGI found), and Bourdeau's oldest/scan_2026-09-23/iberia.md (shallow clone dbourdeau/cyphersolver): drop every unit it lists.
Job: (1) list the DigitArq units whose OWN description or sub-unit titles say cifra / cifrada / em cifra / decifrada / chave / contra-cifra (the search API with its known pagination limits; walk the hierarchy of the hit units' children rather than paging past 10), and keep only units at document or sub-unit level (a letter, a maço item), not whole codices; (2) for each kept unit (<=15), fetch the item's images at modest size with tools/digitarq_fetch.py and confirm by eye that the leaf carries cipher, and whether a decipherment is interlined, facing, or catalogued as a separate unit ("decifrada"); do not use tools/cipher_page_detector.py to choose leaves (it failed its gate). (3) For PP-01/PP-02, only if their sub-unit list names a cipher letter: open that sub-unit.
Output only in: QUEUE.md, the PARES/DigitArq section extended with rows PP-03.. (unit reference, image numbers, viewer URL, date, sender -> recipient, decipherment present?, copy-free, kind), and the TSV. No ciphers/ folders, no nomination lines; list copy-free letters without a decipherment in your done line.
Hosts: digitarq.arquivos.pt (one at a time, >=3 s, <=150), github.com (one shallow clone), WebSearch (<=10).
