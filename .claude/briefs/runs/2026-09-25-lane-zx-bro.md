ZX-BRO (Sonnet, wall-clock box 75 minutes; at most 2 subagents). Target: ciphers/antt-msliv0638-brochado-1712. Parent: LANE ZX orchestrator session_01MxueEQJUGF9PWJiYcVyvBM. Kind: recovery (key source `period`: the manuscript's own "Deciffrada" appendix). Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md), .claude/briefs/solver.md, CLAUDE.md rule 3 (both lessons: match the design; normalize both texts to one convention before a diff), and in NOTES.md the sections PX-BRODEC3, "YX-BRO79" and "YX-PTJUDGE judge re-run" (line ~1000 to the end); the YX-BRO79 brief (.claude/briefs/runs/2026-09-25-lane-yx-bro79.md) is the method you continue. Intake gate run by the lane orchestrator at 15:44 UTC 25 Sept 2026: `ciphers/antt-msliv0638-brochado-1712: partial (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

State: the leave-one-out gate passes (82.6% real vs 91.4% noise-matched, gap -8.8, after Carta 79 was fixed from the image). The letter 134 candidate FAILs the pt language judge (-1.443 vs null_p99 -1.428). Five other entries still drag by >=20 points: Carta 15, Carta 80, Passage 2a (m0284), Carta 101, Passage 2a (m0294).
1. Hand-check each of those five entries against its image (images/ first; else DigitArq once per leaf with tools/digitarq_fetch.py, docId 7049343e7af44ba7b4ec18e74e1b2cd7, >=3 s apart, at most 15 DigitArq requests). Crop the cipher run and its Deciffrada line at native resolution; read every token of both; check pairing (mis-paired entry, skipped line, abbreviation, paraphrase, entry of another letter).
2. Build the normalization RETRO-2026-09-25h proposal 2 named (period abbreviations q./q.mos/hé/S.d± etc. expanded, doubled consonants, u/v, i/j, y/i, tilde forms, one case, one punctuation set) as a function in scripts/, applied to BOTH sides of every diff, and write the convention into NOTES.md. Correct only rows the image proves wrong, each in a corrections table (leaf, entry, position, old, new, reason); a mis-paired entry is excluded from both real and control, said so; never tune the control.
3. Re-run scripts/11_loo_control.py and 06 with the same seeds and noise model; real vs control, pooled and per entry, before and after, side by side.
4. If the gate still passes: re-decode letter 134 from the corrected key (per-token grades C/M/I/U), judge it with `python3 tools/judge_plaintext.py specs/antt-msliv0638-brochado-1712.json --file <reading>` and paste the output verbatim. Use the pt18 corpus if LANE V6's V6-PTCORP has landed tools/data/pt18 and wired it (check `git log --oneline -5 -- tools/data/pt18 tools/judge_plaintext.py` after a fetch), else pt17, and say which. Also score a matched control for the judge: the same decode procedure applied to one appendix entry held out (leave-one-out) of similar length, so we know whether a correct 65-letter decode of this material can pass this judge at all; report both numbers. Then a fresh-instance re-derivation by one subagent that sees only ciphertext_appendix.tsv, plaintext_appendix.tsv, body_ciphertext.tsv and the key rule; report the diff against your reading in tokens, and whether it exceeds the M-graded count.
5. If the judge still FAILs and the held-out control of the same length passes, the letter 134 decode is a candidate that failed check X; if the control also fails, say the judge cannot decide at this length (a judge limit, not a verdict) and stop there.
NOTES.md gets one section "## ZX-BRO (25 Sept 2026)". Status line stays `partial` unless letter 134 passes judge and re-derivation. Touch only ciphers/antt-msliv0638-brochado-1712/**, specs/antt-msliv0638-brochado-1712.json and ROOM.md (never tools/data or tools/judge_plaintext.py: LANE V6 owns those this window). Push after each step. Report what was read and where it was not found; do not classify novelty; write "cost: see the lane ledger". Never call AskUserQuestion.
