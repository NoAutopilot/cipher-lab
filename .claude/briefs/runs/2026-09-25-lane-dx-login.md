LANE DX job 2, DECODE LOGIN + FETCH (Sonnet, cap $4, no subagents). You are the ONE DECODE worker of LANE DX. Parent: LANE DX orchestrator session_01LgrmyB7HMcMaeLTRNbEqb4. Filed 25 Sept 2026; start at or after 01:30 UTC (owner decision, ROOM 00:41 parent line: the single attempt uses the values the owner set around 01:15).
Read first: .claude/briefs/runs/2026-09-25-lane-dx-COMMON.md; CLAUDE.md Access playbook item 3 (DECODE paragraphs and the handling rule); sources/decode/NOTES.md (the 24 Sept permission test: documents and full-size images came back as "Insufficient permissions" placeholders on the other account; forbidden.png sha1 035489a0605851154ab88372216354b63596ca22); ciphers/intercepted-royalist-1646/NOTES.md; ASKS.md row 1.

Route: the lane brief names tools/decode_fetch.sh, but the playbook (24 Sept 04:40 UTC) records that the curl form POST is never evaluated by the server and that tools/decode_browser_login.js is the route that works. Use the browser route; the curl route would spend the one attempt on a known dead end.
1. Presence test (`test -n` only) of DECODE_USER and DECODE_PASS, and whether DECODE_USER contains `@`. If either is MISSING or DECODE_USER has `@`: no attempt; ROOM `for the parent:` line naming the exact variable/state; stop.
2. ROOM line first: `note: DECODE login: owner account taking the one attempt now; others hold`, pushed.
3. ONE login, all fetches in that one browser session:
   `NODE_PATH=$(npm root -g) node tools/decode_browser_login.js 8725 ciphers/intercepted-royalist-1646/decode/ --shot <scratch>/login.png --fetch-page /decrypt-web/RecordsView/413,/decrypt-web/RecordsView/4930 --guess-fullsize --max-files 30`
   If the script cannot save pages for several records into different folders in one run, save all into ciphers/intercepted-royalist-1646/decode/ and then move R413's files to ciphers/boswell-1628/decode/ and R4930's to ciphers/randolph-sussex-1569/decode/ afterwards. Add to the same run any record ids listed in ROOM.md by LANE DX workers NB-ES / NB-FI as "owed to the DX login worker" at the time you start (save under sources/decode/records/<id>/), within --max-files.
   Rejected (IS_LOGGEDIN:false present, or the login page returns): NO retry, no variant. ROOM `for the parent: DECODE rejected on the owner account (probe: ...)`; update ASKS row 1 with the date, route and non-secret signal; stop.
4. On success: for every saved file, record in each target's NOTES.md (a "DECODE fetch, 25 Sept 2026" section) the file, size, sha1, and whether it is real content or the permission placeholder (compare the sha1 above, and grep documents for "Insufficient permissions"). Scrub the account name from every saved HTML before committing (grep for it by reading DECODE_USER into a sed expression inside one command; never echo it). Update ASKS row 1 (done or the new permission state). Commit by path, push.
Touch only: ciphers/intercepted-royalist-1646/decode/, ciphers/boswell-1628/decode/ (create; plus a minimal NOTES.md if the folder has none, status line `found-solved` per ASKS row 1, Woodard 2021), ciphers/randolph-sussex-1569/decode/, the three NOTES.md fetch sections, sources/decode/records/, ASKS.md row 1, ROOM.md. Final paragraph: login verdict, per record what was real vs placeholder, request count to de-crypt.org, cost.
