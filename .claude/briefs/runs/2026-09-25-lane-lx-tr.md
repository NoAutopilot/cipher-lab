LX-TR (Sonnet, cap $6, at most 2 subagents). Target: ciphers/antt-linhares-chave. Job: transcribe the ciphertext on m0002 exactly, then look for more letters under the same key. Kind: recovery (key system known; the book is being identified in parallel by LX-BOOK).

Read CLAUDE.md (rules 2, 7, Usage 4 and 6), .claude/briefs/transcription.md and ciphers/antt-linhares-chave/NOTES.md first. The images are DigitArq docId a03cef08d3c04758aa148f5be56d3401 (CC BY-SA, no login); NOTES.md says full-resolution m0002-m0004 are already committed under images/ (fetched with tools/digitarq_fetch.py). Use those; fetch again only if missing.

Part 1, m0002: about 26 groups of 5-7 digits over pages "2" and "3" of a letter, some with a small number written under the group (letters to trim). Numerals only, so no glyph atlas. Two independent blind passes by Sonnet subagents from the image (never from each other), then reconcile with tools/reconcile_passes.py where it fits, settling disagreements from the image. Self-check that no other target has: every group must parse under the key's rule (first digit 1-3 = how many following digits are the page; the next digit is the column and must be 1, 2 or 3; the rest is the rank, at least 1; a first digit 4-9 is a null). Any group that does not parse is a misread until the image says otherwise. Write ciphers/antt-linhares-chave/ciphertext.tsv with columns: page_of_letter, line, pos, group, trim (blank if none), book_page, book_col, rank, is_null, grade (H read clearly / M uncertain), and push after each pass lands (passA.tsv, passB.tsv, then ciphertext.tsv). Also re-read the worked example on m0003-m0004 digit by digit into key_example.tsv, as a check on the parse in the orchestrator's brief to LX-BOOK. If pass agreement is under 60%, stop and report the blocker.

Part 2, only after Part 1 is pushed: the rest of the item (m0005, m0006, if present) and the neighbouring units of fonds PT/TT/CLNH on DigitArq, using the same search route scDIGI2 used (QUEUE.md, "PARES / DigitArq cipher letters" and the DigitArq section). List any unit carrying numeric groups that fit this scheme in NOTES.md under "## More under this key" (unit code, docId, image numbers, a group count, one line on what it is). Do not transcribe those; just list them.

Do not attempt to decode (no book yet). Touch only ciphers/antt-linhares-chave/** and ROOM.md. Do not edit QUEUE.md, STATUS.md, status.json or any other target.

First action: `tools/room.py --start` (fetches, force-checks-out `main` onto `origin/main`, and refuses a
ROOM.md under 50 lines rather than a shrunk stub; replaces raw `git fetch`/`git reset` for this step,
RETRO-2026-09-24b — the prose fix alone let the identical stale-clone/detached-HEAD failure recur at least
twice more the same day). If ROOM.md says 'retrospective starting' or 'swap starting', push what you hold and
stop until 'retrospective done' or 'swap done', then re-run `tools/room.py --start`. If ROOM.md
says 'swap starting', push what you hold and stop until 'swap done', then `git fetch origin && git reset --hard
origin/main`. Read the last 30 lines of ROOM.md first; append a line before editing a shared file and a `done` line when you stop; use `flag` for anything the orchestrator must see. Cap: about $6 of usage; at most 2 subagents, on Sonnet unless the brief says otherwise. Commit, `git fetch
origin main && git rebase FETCH_HEAD && git push -u origin main`, report in a short paragraph (first line:
the answer), stop. Per rule 10, report what was found and where it was not found; never new, unpublished,
first. Per rule 7, a claimed reading on a target with a spec is reported only with `tools/judge_plaintext.py`'s
output pasted in, and stands only after a fresh-instance re-derivation from the spec and key. Do not start other
targets. Never print or commit credentials, and never echo a credential into your own transcript: no unfiltered `env`, no `curl -v` or `set -x` on a call that carries one (Access playbook item 3). A negative's done line carries
target and control numbers side by side, or it is not a negative (rule 3).
