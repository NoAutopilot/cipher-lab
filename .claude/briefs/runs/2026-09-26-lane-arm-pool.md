JOB ARM-POOL: find more ciphertext in the same code. ARM-C1 showed family C is a non-test at N=369 (matched control reads 0.135 blended, 0.007 on the content class; ceiling about 0.5 even with perfect anchors) and named "more ciphertext in the same code" as the next step. Also fetch frame 0033 (the last ~37 groups of the target, missing from ARM-TR). Sonnet (claude-sonnet-5). Stop and push at USD 6 or 60 minutes (units: frame 0033 at native size, 2 minutes; a roll-14 survey at about 110 low-res frame fetches, 1.5 s apart, about 15 minutes of fetching plus a classification pass by script; a candidate unit at about 5 minutes each for up to 4 candidate letters at native size; one margin). At most 2 subagents, Sonnet, each given at most 20 low-res frame thumbnails per call for classification only (numeral-code page yes/no, shorthand marks yes/no, date line if legible). Lane: LANE ARM orchestrator session_011gEKqukZAxE6FjtQCzsUQ8. Written 26 Sept 2026 by LANE ARM.

INTAKE_GATE_OUTPUT (live, 26 Sept 2026 06:46, TOMO-REPLY): ciphers/armstrong-madison-1808: open (line 1) -- edition/page or full-text-search citation found within 6 lines; EXIT:0.

Read first: `.claude/briefs/runs/2026-09-26-lane-arm-COMMON.md` (binding), ciphers/armstrong-madison-1808/{NOTES.md (ARM-IMG, ARM-TR sections),images/manifest.json,HYPOTHESES.md ARM-DESIGN and ARM-C1 summaries}, tools/data/uscodes-1800/README.md (THE=972 is the office code; its letters are NOT candidates). `python3 tools/key_livecheck.py` first. Claim in ROOM.

Do:
1. Frame M34-014-0033 at native resolution into images/ (the route ARM-IMG used; check the file is a JPEG, not the HTML shell), manifest updated.
2. Roll 14 (NAID 188671566, 664 frames, Jan 1808 - Sept 1810) survey: fetch every 6th frame at 600 px wide (about 110 requests) to a scratch directory outside the repo; classify each as clear text / numeral code / code with shorthand marks / other. Then, around every code frame, fetch its neighbours at 600 px until the letter's extent is known. For each coded letter, note date and frame range.
3. For each coded letter found (other than the target), decide office code vs other code WITHOUT transcribing it in full: transcribe one line (about 15 groups) at native size by eye and compute (a) the fraction of groups THE=972_bourdeau.tsv defines, (b) the units digits. The target's signature is last digit 0/1 dominant, 2/3/5/9 rare, plus shorthand marks; THE=972 letters are near-flat. Any letter matching the target's signature is a pool candidate: fetch its frames at native size into ciphers/armstrong-madison-1808/pool/<date>/ with a manifest (under 30 MB total).
4. Write pool/SURVEY.tsv (frame, class, letter date if legible, code-or-clear, signature numbers) and one paragraph in NOTES.md "ARM-POOL". Rule 3 note: the signature test is a screen, not a result; say so.
Files: ciphers/armstrong-madison-1808/{images/M34-014-0033.jpg,images/manifest.json,pool/**,NOTES.md}, ROOM.md. Host: catalog.archives.gov (its IIIF endpoint) at most 150 requests this job, 1.5 s apart, descriptive UA unless the route needs the browser UA as ARM-IMG found.
Done line (ROOM, starting `done: for LANE ARM: ARM-POOL`): frame 0033 fetched yes/no; frames surveyed; coded letters found (dates); pool candidates with signature numbers; requests; no cost figure.
