ZX-349 (Sonnet, wall-clock box 90 minutes; at most 2 subagents at once). Target: ciphers/clair349-este-guise-1556 (BnF Clairambault 349 f.9, 4 Jan 1556/57, Este-Guise cipher; key from fr.20974 on disk as key_alpha.tsv/key_nomen.tsv). Parent: LANE ZX orchestrator session_01MxueEQJUGF9PWJiYcVyvBM. Kind: recovery (key source `period`). Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md), .claude/briefs/transcription.md (the "Symbol alphabets" lesson), .claude/briefs/solver.md, and NOTES.md sections "YX-TR349" and "YX-TR349B" in full. Intake gate run by the lane orchestrator at 15:44 UTC 25 Sept 2026: `ciphers/clair349-este-guise-1556: partial (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

State: 33 real cipher lines cut as images/lines/line01..33.jpg. Two blind passes (passA.tsv, passB.tsv) invented separate sign vocabularies; agreement 51.9% (partial atlas) to 65.6% (any sign = any sign), digit-only 60.9%; no line reaches 0.70. "2429" on line 1 is a BnF item number, not ciphertext. YX's lesson: arbitrary-sign passes need a shared atlas before agreement means anything.
1. Build a shared ciphertext-side atlas FROM THE KEY: every sign on key_alpha.tsv and key_nomen.tsv gets a code (S01..Snn, the key's own atlas codes where YX-TR349 already assigned them) and one crop from the key leaf image in images/ (atlas/ folder, a contact sheet atlas/sheet.jpg, atlas/atlas.tsv: code, crop file, shape description; NO letter value or meaning in anything a pass sees). Add an "X?" code for a shape not on the sheet (pass describes it). Pattern: ciphers/dupuy468-carpi-1520/glyphs/.
2. Two fresh blind passes (subagents, Sonnet), each given only the 33 line images plus atlas/sheet.jpg and atlas.tsv (codes and shapes only), not each other's output nor the old passes: transcribe every token as a digit group or an atlas code, grade H/M per token. Lines to be given in crops, not the whole leaf. Write passC.tsv, passD.tsv.
3. Reconcile with tools/reconcile_passes.py passC.tsv passD.tsv --crops images/lines; report agreement overall and per line. Gate: 60% pooled. If under 60%, settle disagreements from the image yourself only on lines where time allows, report the before/after numbers, keep ciphertext_draft.tsv (do not promote), and stop with the per-line table.
4. Past the gate: settle every disagreement from the image (grade what you settle M), write ciphertext.tsv, a decode.json for tools/decode_key.py (key: ["key_alpha.tsv","key_nomen.tsv"]; the loader YX-TR349B added), run `python3 tools/decode_key.py ciphers/clair349-este-guise-1556` and commit reading.txt with per-token grades (H key row read at H and token agreed blind; M any M in the chain; I inferred; U unread). Write specs/clair349-este-guise-1556.json on the pattern of specs/thurloe-barriere-1654.json with judge {"language": "fr"} and paste `python3 tools/judge_plaintext.py specs/clair349-este-guise-1556.json --file <reading>` verbatim. Matched control for the judge: the same key applied to a shuffled ciphertext.tsv (same tokens, 5 seeds) scored by the same judge; report target vs control. Then a fresh-instance re-derivation by one subagent that sees only ciphertext.tsv, the two key files and decode.json, diffed in tokens against yours; `tools/decode_key.py --check` must exit 0.
NOTES.md gets one section "## ZX-349 (25 Sept 2026)". Status stays `partial` unless the reading passes judge and re-derivation. Touch only ciphers/clair349-este-guise-1556/**, specs/clair349-este-guise-1556.json and ROOM.md. Push after each step. Report what was read and where it was not found; do not classify novelty; write "cost: see the lane ledger". Never call AskUserQuestion.
