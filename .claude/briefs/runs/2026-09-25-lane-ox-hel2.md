OX-HEL2, successor to OX-HEL (Sonnet, cap $6 as a stall alarm, at most 2 subagents). Brief: .claude/briefs/runs/2026-09-25-lane-ox-hel.md, all of it, plus:
- OX-HEL stalled at 02:08 on an AskUserQuestion prompt nobody could answer, and pushed nothing. Never call AskUserQuestion: no human watches this session. When a choice comes up, take the conservative option within the brief, log it in NOTES.md, and carry on.
- 185 thumbnails at >=1.5 s apart from one host is within the good-citizen rule (a few hundred requests per host per session); fetch them sequentially, save to disk, and have subagents read them from disk in batches. Post `touching host nationaalarchief.nl` in ROOM.
- If the NA scan viewer needs a browser or an API you cannot reach, log the route tried and stop the fetch there; do not retry in a loop.
