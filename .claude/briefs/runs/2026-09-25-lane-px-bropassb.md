PX-BROPASSB (Sonnet, cap $6 as a stall alarm; the orchestrator watches your cost from session metadata, you cannot, so keep the job small; at most 2 subagents). Target: ciphers/antt-msliv0638-brochado-1712. Job: the second blind transcription pass of the Deciffrada appendix (rule: two passes), then reconcile and rebuild the key. Parent: LANE PX orchestrator session_01KapVpHVzNpnnQce5C8c3LY. Read the lane COMMON and .claude/briefs/transcription.md; read NOTES.md only for the leaf list (m0279-m0296, images already under images/) and the TSV column layout -- do NOT open ciphertext_appendix.tsv, plaintext_appendix.tsv or reading_appendix*.txt/tsv before step 2.
1. Pass B, blind: transcribe every appendix entry from the images into passB_cipher.tsv (same columns as ciphertext_appendix.tsv: leaf, entry_label, position, token, token_type) and passB_plain.tsv (same columns as plaintext_appendix.tsv), period spelling kept. Push after every 6 leaves. No network needed.
2. Reconcile: compare pass B with the existing pass per entry and token (tools/reconcile_passes.py if it fits the layout, else a short script in scripts/07_reconcile.py); write disagreements.tsv and the agreement rate; settle each disagreement from the image crop; update ciphertext_appendix.tsv and plaintext_appendix.tsv with a grade column (H both agree, M settled or uncertain). If agreement is under 90%, stop after writing the numbers and flag it.
3. Re-run scripts/01..04 and scripts/06, then `python3 tools/decode_key.py ciphers/antt-msliv0638-brochado-1712 --check`; report codes, grade-C count, and control agreement before/after.
Touch only ciphers/antt-msliv0638-brochado-1712/** and ROOM.md. Do not sweep the body, write a spec, or decode anything new. Report counts; do not classify novelty.
