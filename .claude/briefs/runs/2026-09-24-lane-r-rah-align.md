ROLE: ALIGNER (recovery from an interlinear note), RAH Cañada 1869 (LANE R worker R8). Model Opus, cap $8. No subagents.
Target: ciphers/rah-canada-1869 (RAH 9/6958, Leg. XIX nº 117/2-3). Read NOTES.md, images/inventory.tsv, glyphs/atlas.md, passA.tsv, passB.tsv, disagreements.tsv. The note is interlinear: each Spanish clear sentence is followed below, same hand, by its encipherment in digits and pen-drawn signs. The two blind passes agreed on only 7.6% of columns; do not reconcile them blindly. Instead, from the two page images (images/, on disk; do not refetch), transcribe the Spanish lines and the cipher lines yourself, line by line, and align cipher signs to plaintext letters (or syllables/words: decide the unit from the data). Use passA/passB only as a check on your sign reading; record every place you differ from both.
Output: plaintext.tsv (line, Spanish as written), ciphertext.tsv (line, idx, sign in atlas codes), key.tsv (sign -> value, grade H: read from the document's own plaintext), decode.json and tools/decode_key.py --check exiting 0; the per-sign grade counts; any sign the plaintext does not explain (M) and any plaintext without cipher. Say in NOTES.md what the system is (simple substitution, homophones, nulls, word signs) and whether the key would read other ciphered items in the Isabel II/González Bravo papers (one-line suggestion, do not fetch). Search log: what was checked for a prior transcription (from check-solved), where not found. End: ROOM `for LANE V: ciphers/rah-canada-1869 reading ready`. Do not classify novelty.
