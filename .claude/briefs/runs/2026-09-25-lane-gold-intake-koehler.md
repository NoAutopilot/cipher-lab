JOB GOLD-0K: intake + check-solved, koehler-1944. Sonnet (claude-sonnet-5). Stop and push at $4 or 30 minutes, whichever first. Lane: LANE GOLD orchestrator session_01DKDynpdEwZK5EokxtjCM3P. Written 25 Sept 2026 16:47 UTC.

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md` (common rules; where it names the LANE R4 orchestrator read LANE GOLD), then `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (addendum, binding), `.claude/briefs/check-solved.md` and the check-solved skill, `specs/koehler-1944.json`, `sources/schmeh/posts/47b-koehler-update-2021.txt`. Claim in ROOM with `python3 tools/room.py "GOLD-0K (Sonnet, <your session id>)" 'claim: ...'`.

Do:
1. Create `ciphers/koehler-1944/`: `ciphertext.txt` with the five messages exactly as the spec's `groups` (one message per block, header line "# msg N, Kahn header L, letters as printed M"), and a header comment: source (Schmeh 2021 reproducing Kahn, Cryptologia 5:2, April 1981; transcription, not an image), and that message 3 is headed 137 by Kahn but has 140 letters as printed. Never repair.
2. `NOTES.md`: first line the bare status word (open / blocked / found-solved), second line one sentence naming the edition or source actually read by you and the pages/full-text search (check-solved.md's rule; the intake gate reads this). Then sources, established vs inferred, a check-solved search log with every query, URL and date (`date -u`).
3. Check-solved sweep, each logged searched/unreachable: Kahn, Cryptologia 5:2 (1981) and the Cryptologia index since (OpenAlex with OPENALEX_KEY header, Semantic Scholar with S2_KEY header, CrossRef; titles/abstracts mentioning Koehler/Köhler); Schmeh's 2021 update and its comment thread (the saved copy in sources/schmeh/posts/; one fresh fetch of the live post's comments allowed); Bauer, Unsolved! (2017) via Google Books API (GOOGLE_BOOKS_KEY, &country=US) and IA full-text search; Cipher Mysteries (ciphermysteries.com search for Koehler); DECODE listing (login-free `tools/decode_list.py`, no login); both solver repositories' snapshots in `sources/solver-diffs/` (grep; clone only to grep for Koehler); a search engine (WebSearch) incl. "Koehler cipher solved", "Köhler 1944 cryptogram", and the model-solve family ("Koehler cipher Claude", "... GPT"). Also note what Johnson (Warfare History Network) and any FBI/NARA page say about decrypts.
4. Run `python3 tools/intake_gate_check.py koehler-1944`; if nonzero, fix the citation line honestly (or set blocked) and re-run. Paste the output into NOTES.md.
5. Do not decode, do not run statistics, do not start the archive lookup (that is job GOLD-1A). If you find a published decryption: status found-solved, say who/where, and put `flag: koehler-1944 found-solved candidate, for LANE V6` in ROOM.
Hosts: good-citizen rule; request count per host in the done line. Push by explicit path (`tools/room.py --push ciphers/koehler-1944`). Done line: status word, gate exit code, request counts per host, no cost figure. Rule 10 wording only.
