JOB bMAT2 (LANE B5, Sonnet worker). One line: matignon-mayenne-1586 NEAR step (1) -- split the Cipher-1 reading into the spans Bourdeau already reads versus the spans he leaves unread, then resolve ambiguous (M) and unkeyed (U) codes on the unread spans by the two-context rule with a shuffled-context control, and re-judge with fr16.

Read first: `.claude/briefs/runs/2026-09-26-lane-b5-common.md` (and the 7b COMMON it names), CLAUDE.md rules 3, 4, 7, 8, 10, NEAR.md's matignon-mayenne-1586 row, ciphers/matignon-mayenne-1586/NOTES.md, specs/matignon-mayenne-1586.json. Intake: `python3 tools/intake_gate_check.py matignon-mayenne-1586` (re-run and paste; exit 0 required).

Cap and box: $4 or 45 minutes from your first `date -u`, whichever first. Git and disk only (one shallow clone of dbourdeau/cyphersolver to read matignon1586/ NOTES.md and his per-leaf reading files at fc0c9e8 or later; delete after).

Steps:
0. `date -u`; `python3 tools/room.py --start`; ROOM claim (tools/room.py) naming ciphers/matignon-mayenne-1586/.
1. Split file: from Bourdeau's per-leaf readings (his reading/decoded files and "Coverage, measured" table), mark every line or token span of ciphertext.txt as `bourdeau_read` or `unread`; write ciphers/matignon-mayenne-1586/spans.tsv (folio, line, token range, status, source file). Give counts per leaf. If his repo has no span-level record, say so and use his per-leaf coverage only, marking the split as estimated.
2. On the `unread` spans only: for each M code (2-3 candidates) and each U code, propose a value only where the same code gives a French word in at least two independent contexts (grade S; one context M). Control: the same procedure on the same spans with the tokens shuffled within each line (3 seeds) -- report how many S values the control also yields (the false-positive rate of the rule). Write exceptions.tsv or a key update per decode_key.py conventions; do not edit Bourdeau's values, add yours as separate graded rows.
3. Re-decode (`tools/decode_key.py ciphers/matignon-mayenne-1586 --check` must exit 0), re-judge the unread spans alone with `tools/judge_plaintext.py` (fr16) and paste the output: before vs after, plus the scrambled-order control on the same spans.
4. Grade counts H/C/S/M/I on the unread spans (rule 4; Bourdeau's period-verified values are H with the key source named). Append cheap_test_done; push; done line with target and control side by side; if the unread spans now read, ROOM "for LANE V7: ciphers/matignon-mayenne-1586 reading ready" naming the NOTES check-solved section as the search log. Report what was found and where it was not found; do not classify novelty.
