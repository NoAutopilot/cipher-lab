JOB GOLD-KAL1: kaliningrad-2015 (the lane's reserve, lane brief job 5), two families with headroom, control first each: (1) the word-pattern crib test of the one specific host-text claim on record (Frank, Cipherbrain comment #58, 19 Feb 2021: a chapter of the 1876 Russian Synodal Bible), and (2) `tools/family_run.py --family homophonic` in German at N 978, K 36. Sonnet (claude-sonnet-5). Stop and push at $5 or 45 minutes, whichever first. NO subagents. Lane: LANE GOLD2 orchestrator session_013JXDgLDkW2y5Ldi2gWzTkY (Fable) or its successor. Written by the cycle-3 consolidator GOLD-CONS3 (Fable, session_01UBBcnhMmQEpPpdNQgDfsTE), 25 Sept 2026 23:10 UTC; the orchestrator fills the gate line and the spawn time.

INTAKE_GATE_OUTPUT (live, 23:35 UTC): kaliningrad-2015: open (line 1) -- edition/page or full-text-search citation found within 6 lines (exit 0). Spawned 25 Sept 2026 23:35 UTC by LANE GOLD2 (session_013JXDgLDkW2y5Ldi2gWzTkY); its successor GOLD3 adopts this worker from the LANE GOLD2 handoff in STATUS.md.

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), `specs/kaliningrad-2015.json`, `ciphers/kaliningrad-2015/NOTES.md` in full (LANE B2's test 1 and the check-solved sweep), `ciphers/kaliningrad-2015/ciphertext.txt` (Ernst's transcript; the header explains the "x" convention and the bracketed counts), `ciphers/kaliningrad-2015/scripts/ic_analysis.py` (the sign tokeniser: letter + optional apostrophe = one sign, K 36), `python3 tools/family_run.py --help`, the docstring of `tools/families/homophonic.py` (`profile=target`), `tools/data/nl_dev/README.md` (the corpus README shape and the api.getbible.net route GOLD-K1 used). Claim in ROOM (`tools/room.py`). Read the clock with `date -u` before any date.

What the record says, pasted so you do not rediscover it:
- Target: N 978 signs, K 36 (letter + optional apostrophe as one sign), IC 0.0657; 206 space-separated words (word divisions are visible: lengths 1-12, ten one-letter words, twelve two-letter, ten dotted abbreviation groups such as `x.s.f.d.`); top signs e 156 (15.9 pct), n 92, i 68, x 62, s 61, u 43, n' 42, h 42, d 41, f 40; the apostrophe follows 11 different letters and only consonant-looking ones (n' t' x' d' f' l' s' m' z' h'), the shape of a soft sign in a Russian transliteration. Matched-N IC controls (LANE B2, 20 windows, seed 42): transliterated Russian 0.0563 (0.0543-0.0605), German (de16, ENHG, 6,855 letters, overlapping windows) 0.0724 (0.0676-0.0765). Two private, unpublished, mutually contradicting solve claims in the comment thread (Ernst 2017: "political", cross-language polyalphabetic; Frank 2021: a Synodal Bible chapter); check-solved verdict open; intake gate exit 0 at 18:19 UTC.
- Why these two families and not a Russian masc: `tools/homophonic_anneal.py` folds plaintext to a-z (26 letters), so a Russian (33-letter) control cannot be built or solved by `family_run.py --family masc` or `homophonic` without a tool change, and `tools/data` has no Russian corpus (the 34k-letter Gutenberg 30774 fetch in `scripts/` is too small for a model). A pure-Python word-pattern solver has no alphabet limit and, under the crib claim, the vocabulary is the claimed text itself: the highest lead class on the COMMON addendum's item 4 (a claimed host text beats cryptanalysis). German is the one language in tools/data whose plain IC (0.072-0.076 in de20) a light 36-sign homophonic would bring down to about the target's 0.0657; the `homophonic` family handles K 36 over 26 letters natively. Transposition is not tested separately: a transposition keeps the language's IC (Russian 0.056, German 0.073), the target matches neither, and the apostrophes sit after consonants in-word, which a transposition would scatter.
- Tokenisation is the first decision and both conventions are run: (A) letter + apostrophe = one sign (K 36, as ic_analysis.py); (B) the apostrophe as its own sign (K about 27), which is the natural reading if ' is the soft sign written in clear. Dotted abbreviation groups keep their letters as signs and drop the dots.

Do, in this order:
1. Corpus (aim 8 minutes, one host). `tools/data/ru19/`: the Russian Synodal Bible from api.getbible.net (the host GOLD-K1 used for nl_dev; `https://api.getbible.net/v2/translations.json` lists it as `synodal`, "Synodal Translation (1876)", HTTP 200 on 25 Sept 2026 23:09 UTC). Fetch `https://api.getbible.net/v2/synodal.json` once (whole Bible in one call); if that fails, `https://api.getbible.net/v2/synodal/{book}.json` for at most 20 books, Genesis first, the four Gospels, Psalms, Isaiah, then in canonical order. At most 20 requests, 1.5 s apart, descriptive User-Agent. Write one `.txt.gz` per book (Cyrillic, lower-cased, verse text only) plus README.md (source, licence as the API states it, date, letter count, chapter count) and MANIFEST.tsv (book, file, letters, chapters), the nl_dev shape. Commit the corpus on its own commit before any run.
2. Family 1, crib test (aim 20 minutes). Script `ciphers/kaliningrad-2015/scripts/pattern_crib.py` (pure Python; `--help`; an offline test in `tools/tests/test_pattern_crib.py` on a 300-letter synthetic case in under 10 s): tokenise the ciphertext under convention A and B; build the vocabulary (word forms with counts) and a chapter index from `tools/data/ru19`; for a cipher word, its pattern is the sequence of first-occurrence indices of its signs (`elhxikixacel` -> 0.1.2.3.4.5.4.3.6.7.0.1); solve by constraint propagation with backtracking over cipher words longest-first (candidates = vocabulary words of the same pattern consistent with the partial sign->letter map; one letter per sign; under convention A a sign `n'` may map to a letter followed by ь, so the plaintext alphabet for A is Cyrillic letters plus consonant+ь pairs, and under B ь is a letter), scoring a complete or partial assignment by the share of cipher words that map to vocabulary words, ties broken by word-frequency-weighted log-count; report the best map, the share of words matched, the share of letters mapped, and the best-matching chapter (the chapter whose word multiset covers the most decoded words). CONTROL (rule 3), seeds 1-3, before the target: a Synodal chapter chosen at random and cut to about 978 letters, enciphered under a random 36-sign key (33 Cyrillic letters + consonant+ь pairs folded to the 36 most frequent, apostrophe rendered as in the target, one-letter words and abbreviations left as they fall), solved by the same script with (a) the vocabulary including that chapter (the claim's own condition) and (b) the chapter held out (what the solver does on Russian outside the crib); the number is the share of plaintext letters recovered. GATE: control (a) mean >= 0.9 on 3 seeds; if (a) is under 0.9 the tool is fixed (more backtracking, frequency prior), never the target, and at the box the section says CONTROL BELOW GATE with the number. Then the target under both conventions; the null for the target is the same solver on three shuffles of the target's own words' letters (share of words matched by chance). A target word-match share above the shuffle null's maximum by 0.2 or more, or a chapter that covers over half the decoded words, is a ROOM `flag:` line ("flag: GOLD-KAL1 crib test target x vs null y, chapter Z"), not a reading; there is no Russian judge in `tools/judge_plaintext.py`, so no PASS is possible this job and no decoded text is described. Record the Ernst 2017 claim as untestable as stated (no text, no method named).
3. Family 2, German homophonic (aim 12 minutes; run only if at least 15 minutes of the box remain after step 2, else log it as owed). Write the ciphertext as a long-format TSV with a `sign` column under convention A to `ciphers/kaliningrad-2015/ciphertext_signs.tsv` (generated by `scripts/ic_analysis.py --tsv` or a five-line sibling; committed, reproducible). Point the spec's judge at de20: in `specs/kaliningrad-2015.json` set `judge.corpora` to the seven `tools/data/de20/*.txt.gz` files exactly as `specs/koehler-1944.json` does, with a `corpora_note` that de16 is ENHG and wrong for a Soviet-era text; leave `language: de`. Then `python3 tools/family_run.py specs/kaliningrad-2015.json --family homophonic --cipher ciphers/kaliningrad-2015/ciphertext_signs.tsv --corpus tools/data/de20 --param profile=target --seeds 3 --restarts 8 --gate 0.9 --label "GOLD-KAL1 homophonic de20 K36 profile=target, control before target"`. Control first by the tool (calibration: `masc` reads 0.994 at N 581 in English on blitz-ciphers, NEAR.md; at N 978 and K 36 a German homophonic control should read above 0.9; if it does not, say so and do not lower the gate). A judge PASS is pasted, "for LANE GOLD: judge PASS on kaliningrad homophonic de, re-derivation owed" goes to ROOM, and the job stops; a FAIL is a control-backed negative for German light-homophonic substitution, logged as such.
4. Write `ciphers/kaliningrad-2015/HYPOTHESES.md` (create it: the header sentence of the Köhler file, then a section "GOLD-KAL1, crib test and German homophonic" with the control table (seed, a/b recovery), the target's two convention rows (words matched, letters mapped, best chapter, null max), the family_run row the tool appends, dead ends, exact commands); one `cheap_test_done` entry per family in the spec ("2-crib" and "2-homophonic-de") with both numbers; one paragraph in NOTES.md; the UNSOLVED-SURVEY.md row is the orchestrator's. Never repair the transcription; never describe decoded text unless the judge passed (family 2) -- for family 1 report numbers only.

Wall clock: the units are (1) the corpus, (2) the crib control+target, (3) the homophonic control+target; do not start a unit that would cross 80 pct of the cap or of the 45 minutes. Hosts: api.getbible.net only, at most 20 requests, 1.5 s apart; report the count. Subagents: none. Rule 10: never solved, new, first, unpublished; NOTES.md status stays `open`.
Done line (ROOM, starting `done: for LANE GOLD: GOLD-KAL1`): corpus letters and chapters; crib control (a) x/y/z and (b) x/y/z (gate 0.9 met/not); target words matched under A and B vs the shuffle null max, best chapter and its cover; homophonic control a/b/c vs target judge line, or "owed"; files; requests per host; no cost figure; no reading described.
