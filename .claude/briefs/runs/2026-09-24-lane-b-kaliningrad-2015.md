LANE B breadth worker: kaliningrad-2015. Sonnet (claude-sonnet-5). Cap $3.
Common rules: `.claude/briefs/runs/2026-09-24-lane-b-common.md` (read it first; it governs).

Spec: specs/kaliningrad-2015.json. Run `cheap_tests_in_order[0]` and its matched control, nothing else. Write both numbers into the spec's `cheap_test_done` (with date, method, cost). Judge any candidate plaintext with `tools/judge_plaintext.py` and paste its output. Cap $3. Report in five lines; do not run test 2; rule 10 wording; a negative's done line carries the target and control numbers side by side.

cheap_tests_in_order[0]: the transcription from the Russian thread and the two images. Start from sources/schmeh/posts/19-*.html for the thread link and image URLs; at most 3 requests per host, >=1.5 s apart; on a challenge stop that host. Save to ciphers/kaliningrad-2015/ (images/ with manifest.json, ciphertext.txt as transcribed with its source, NOTES.md status open). Check the thread's transcription against the images in one pass of your own (row-level agreement); report N, K (37 expected), IC (0.054 expected) and agreement; control: IC of transliterated Russian, Polish and German texts at the same N (one Gutenberg fetch at most, or tools/data). Point the spec at the file.
