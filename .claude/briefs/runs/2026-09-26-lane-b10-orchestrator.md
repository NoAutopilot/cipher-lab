LANE B10 ORCHESTRATOR (breadth; successor to LANE B9, whose handoff is STATUS.md "LANE B9 handoff (session_0167Uwemu8CfMzGaGRJJhx6W)", closed 10:22 UTC 26 Sept 2026). Opus. Cap $25 own usage; hand off at $25 or 300k context (B8 and B9 each reached 250-320k in about 90-120 minutes with eight or nine workers: write your "LANE B10 handoff" section in STATUS.md from the first worker report, keep its heading fixed, re-check it after every push). Started 26 Sept 2026 at the 10:41 UTC clock read by parent 7g (session_01Jb4UHR8so3yBUG1NTMdf6C, "LIVE parent 7g · Orchestrator 8"). Window: allowed on this account; `rejected` on any session stops every lane. The owner's standing word (07:58): keep chasing momentum.

Everything in `.claude/briefs/runs/2026-09-26-lane-b9-orchestrator.md` and, through it, B8's and B7's (rules, pricing, the crop-tool command, the reading-ready protocol, the LIVE/ARCHIVED title rule, ledger and archive after every worker, ROOM-only reporting) applies unchanged with B10 for B9; the common file is `.claude/briefs/runs/2026-09-26-lane-b10-common.md` (`python3 tools/tests/test_common_briefs.py` passes). Also read: CLAUDE.md rule 3 in full as of 09:58 (RETRO-APPLY-R added the non-test shape of a repeated attempt and the judge-floor rule: a judge PASS on a family's decode of the shuffled target voids the judge as a gate), UPDATES.md rows since 08:19, NEAR.md every row (fr2933-salviati-1525's row now names transcription pass C after SALV-DIAG's 10:08 sweep; malsburg's row carries the pool count), RETRO-2026-09-26f.md, the LANE B9 handoff in full (its "For LANE B10" paragraph is your job list), ciphers/malsburg-hessen-1636/NOTES.md and pool/POOL.md, ciphers/fr2933-salviati-1525/NOTES.md (the atlas passes and their 14.6 pct disagreement boxes, R8-DSN and SALV-DIAG rows in HYPOTHESES.md); `python3 tools/room.py --digest "2026-09-26 09:50"`. ROOM claim "LANE B10 orchestrator: breadth, B9's successor". A lane cannot archive itself: at close, retitle ARCHIVED, self-ledger last from get_session, and the parent archives you.

Jobs, in order, each a Sonnet worker unless named, each brief pasting `tools/intake_gate_check.py <target>` output first, each per-unit brief stating the unit count and the per-pass estimate (dense malsburg leaves cost about USD 0.4 a cipher line all in and witness settling about USD 0.1 a row, B9's own figures), and for any transcription pass the crop step as a command to run and paste: (1) malsburg-hessen-1636 folder shrink, cap $4: the folder is 33 MB, over the 30 MB line; shrink it the AX2-SHRINK way (images_manifest_full.tsv with cited_by, regen_images.sh, byte-identical regen test on a sample before any deletion, uncited full pages deleted, cited pages to JPEG at the same dimensions) -- no transcription. (2) malsburg-hessen-1636 Marburg keys, cap $4, no blind leaf passes: Bourdeau's open lead is the cipher keys HStAM 4 d Nr. 1219-1224 (HCPortal records 519-523; Antal and Mirka, HistoCrypt 2022): fetch the key images through the HCPortal route already in the target's NOTES.md (one request at a time, 1.5 s apart, log the count), read each key's value inventory, and test each against the pooled code-frequency profile (pool/pooled.tsv, K=173) and the embedded clear clauses on ff.16/23/24/28 -- a key whose inventory matches the pool's sign set and reads a clear clause is a recovery, posted "reading ready" to LANE V9 only after decode_key.py --check and a rule-7 re-derivation; a key that does not fit is logged in HYPOTHESES.md with the overlap numbers. (3) fr2933-salviati-1525 transcription pass C, Opus orchestrating Sonnet subagents, cap $8: SALV-DIAG showed the syllabary control crosses into the target's failing range at 8-9 pct injected error while the atlas passes disagree on 14.6 pct of boxes, so every design-family negative on this target so far was a non-test at the measured error; pass C reads only the disagreement boxes (disagreements.tsv, the crop_passC*.py scripts already in the folder cut them) against the atlas, two blind subagent calls per page of boxes, then reconcile; report the residual disagreement rate; if it falls below 8 pct, rebuild the spec's ciphertext and post to NEAR.md that the R8 design families are re-runnable (do not re-run them in this lane); if it does not, the row's next step is an owner-side high-resolution request. (4) If (2) finds no key: a nomenclator/code family at the pooled N through tools/family_run.py -- tools/families/nomenclator.py exists since ARM-C1 (09:04) -- control first at the pool's N and K, and score the family's decode of the shuffled pool through the same judge before any judge PASS counts (rule 3's judge-floor sentence); the clear clauses and record 509's scattered codes are the cribs. Parked, reopen only with new material: matignon-mayenne-1586 (native margin crops), hessen-1824, zeschau (ASKS 64), hessen-daenemark-1672, untersberg-code, blitz-ciphers, siena-concistoro-2308. Not this lane: armstrong-madison-1808 (LANE ARM/ARM2), LANE NX's targets (owner account: fr3625-lauriere-1593, rah-morillo-1817, esp318-sicilia-1503, wvo-hessen-1564, fr3151-noailles-1558), verification (LANE V9).

After every worker: LEDGER row with the get_session cost (never the worker's own figure), ASSIGNMENTS row, retitle ARCHIVED, archive_session, ROOM line; NEAR.md row updated the moment a step's numbers exist; status.json results row only when V9 has classed a reading. Your own close cost is provisional until the parent replaces it from get_session. Check-in every 45 minutes by send_later while workers run; a "for the parent:" ROOM line at each check-in with own cost and context. Never call AskUserQuestion; never print or commit credentials.
