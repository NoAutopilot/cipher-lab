JOB bMALH (LANE B9, Sonnet, cap USD 3, wall-clock box 40 min; hosts: none): malsburg-hessen-1636 -- homophonic profile=target through tools/family_run.py at the pooled N (1,828 signs, ciphers/malsburg-hessen-1636/pool/pooled.tsv), control first.
Intake gate: run and paste `python3 tools/intake_gate_check.py malsburg-hessen-1636` (orchestrator 09:35: exit 0). Nonzero = stop.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b9-common.md` (it governs); NOTES.md bMAL3 (the N=352 run, control 0.392 below gate), bMALC (control at projected N 1000/2000/3000: 0.928/0.844/0.976), pool/POOL.md; specs/malsburg-hessen-1636.json (judge de16, era flag -- 1637 letter).
Why: the pool is now past 1,000 signs, where the matched control reads this design. This is the first licensed test of the target.
Steps, serialized: (1) `python3 tools/family_run.py specs/malsburg-hessen-1636.json --family homophonic --cipher ciphers/malsburg-hessen-1636/pool/pooled.tsv --param profile=target --seeds 3 --gate 0.6 --label "bMALH pooled N=1828"` (if --cipher does not read the 5-column pool file, add the minimal option with an offline test). The control is built at the pool's own N and K (K is about 173, higher than bMALC's 95: report the control at this K, it may differ). (2) Only if the control meets 0.6 the target runs; paste the HYPOTHESES.md rows and the judge line (de16, era flag beside it). (3) A de-duplicated variant: drop off-form signs (leaf_pool_gate's grammar) and rerun the same command once, so a FAIL cannot be blamed on the 4% noise.
Degenerate optimum to avoid: at K=173 a homophonic anneal can map rare codes to whatever letters raise the n-gram score; report the share of target tokens decoded by codes seen fewer than 3 times, and whether the best decode's top words survive a shuffle of the target's order (family_run --shuffle-target, one seed).
Output: NOTES.md section "Pooled homophonic (bMALH)"; HYPOTHESES.md rows. CONTROL BELOW GATE is a non-test. A target FAIL with a passed control is a family exclusion at this N, not closed-negative (rule 5). ROOM done line <= 600 chars: control mean vs target judge per variant. Final reply at most five lines. Do not edit NEAR.md or pool/.
