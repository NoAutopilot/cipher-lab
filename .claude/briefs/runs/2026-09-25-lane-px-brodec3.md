PX-BRODEC3 (Sonnet, cap $6 as a stall alarm; the orchestrator watches your cost, you cannot; at most 2 subagents). Target: ciphers/antt-msliv0638-brochado-1712. Job: the last gate attempt on the period key this lane will make -- fix the measurement bug, noise-match the control, rerun; decode letter 134 only if it passes. Parent: LANE PX orchestrator session_01KapVpHVzNpnnQce5C8c3LY. Read the lane COMMON, .claude/briefs/solver.md, NOTES.md sections PX-BROKEY2, PX-BROGLYPH, PX-BRODEC, PX-BRODEC2; scripts/03,04,06,11. No network.
1. Fix scripts/06_decode_agreement.py to compare same-length aligned spans position by position (difflib only where lengths differ, and then say so per entry); regenerate conflicts.tsv; correct the headline self-consistency figure wherever NOTES.md states it (strike the old number, give the new one, say why).
2. Noise-matched synthetic control (rule 3, match the design): in scripts/11_loo_control.py add a synthetic mode that, on top of the shuffled 40-code key over the appendix's own plaintexts, injects the target's measured noise: (a) token substitution at the transcription M rate (tokens graded M / all tokens in ciphertext_appendix.tsv), drawn from the observed confusion pairs in disagreements.tsv; (b) the gloss-side mismatch rate: the share of entries where code count differs from gloss letter count (abbreviation/paraphrase), applied by dropping those entries' spans exactly as the real run drops them. Run 5 seeds; report mean and range of the synthetic LOO next to the real 80.2% (recomputed after step 1 if it changes).
3. Gate: real pooled LOO >= 80% and within 10 points of the noise-matched synthetic mean. If it passes: PX-BRODEC's steps 3-5 (.claude/briefs/runs/2026-09-25-lane-px-brodec.md): letter 134 (m0275-r1+m0276-r1+m0276-r2, one run) with per-token C/M/I/U, Portuguese period spelling + English gloss, I-grade completions apart; specs/antt-msliv0638-brochado-1712.json + `python3 tools/judge_plaintext.py specs/antt-msliv0638-brochado-1712.json --file reading_body_letter134.txt` output verbatim; fresh-instance re-derivation by one subagent given only ciphertext_appendix.tsv, plaintext_appendix.tsv, body_ciphertext.tsv, compared per code and token. If it fails: stop, report both numbers, and write in NOTES.md, as one line, the single next test you would run.
NOTES.md section "## PX-BRODEC3". Push after each step. Touch only ciphers/antt-msliv0638-brochado-1712/**, specs/antt-msliv0638-brochado-1712.json and ROOM.md. Report what was read and where it was not found; do not classify novelty; write "cost: not visible to me".
