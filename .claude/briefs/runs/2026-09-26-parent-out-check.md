OUT-CHECK (Opus, cap $6 of usage, box 40 minutes; network for the cited sources and the institution contact pages only, one request per host, 1.5 s apart). Parent worker for parent 7h (session_01GT3Yv4kUZrBR8pc2s5EToH), CLAUDE.md Outreach gate 7 (owner, 26 Sept 2026: "if it turns out we have a very rudimentary mistake or overlook, we'd look quite bad"). You are the checker, never the drafter: your job is to falsify. Written at the 16:57 UTC clock read.

Job: for every file in outreach/mailbox/*.json (prepared by MAIL-PREP) and the outreach/*.md draft it names, check every factual sentence against the repository files it rests on (the target's NOTES.md, HYPOTHESES.md "Summary" blocks, AUDIT.md, REQUEST.md, LEDGER.md rows, ROOM.md done lines) and the cited external sources (Founders Online, NARA catalog, the archive's own catalogue page): counts (letters, pages, groups, systems), dates, shelfmarks and arks, names of codes and people, the claim of what was and was not checked, every link (fetch it, HTTP 200 and the right page), and the recipient address on the institution's own contact page today. Known question to settle first: the Madison editors' draft says "six other coded Armstrong letters of December 1807 to March 1808 (the 'confidentially sent' batch docketed on roll 14, frame 0645)" while ARM-POOL2's ledger row says the docket's four located items are THE 972 -- read ciphers/armstrong-madison-1808/HYPOTHESES.md and NOTES.md (ARM-POOL, ARM-POOL2, ARM-JEF sections) and state the right count and wording. Rule 10 sweep on every body (solved, new, first, unpublished, unread, never printed). For each draft: correct the json body and the md body in place (minimal edits, keep the `[SIGN-OFF]` line), add `checked: <UTC time> by OUT-CHECK -- <n> sentences verified, <m> corrected: <list>` to the md header, and if a claim cannot be verified from the repo or the source, rewrite it to what is verifiable or cut it and say so. Never add claims. Never write the owner's name. Commit by explicit path, `python3 tools/room.py --push <paths>`.
First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker OUT-CHECK (Opus, <session id>)`. Finish: ROOM done line "done: for parent 7h: OUT-CHECK -- <k> drafts checked, corrections: <one clause each>, addresses <verified/changed>"; one-paragraph reply; stop. Never call AskUserQuestion; never print or commit credentials.
