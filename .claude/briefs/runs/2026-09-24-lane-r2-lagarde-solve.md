ROLE: SOLVER, La Garde 1577 (LANE R2 worker L2). Model Opus, cap $6. No subagents. No network (Groen's printed text of Lettre DCCLXXXIX is on DBNL, LANE V2's host: if NOTES.md or sources/ already hold its text use it; otherwise post a ROOM line asking LANE V2 for it and work without).
Target: ciphers/la-garde-1577. Read NOTES.md (Source, R9 inventory, L1 transcription), ciphertext_6179.tsv, ciphertext_6467.tsv, freq.py. L1: 228 numeral tokens, values 1-24 only: numbers for letters, probably a 24-letter French alphabet, with overline and a loop mark whose role is unknown.
1. Before solving: settle the M rows of L1's transcription on the image (only those rows); commit.
2. Matched control first (rule 3): French of 1577 (take clear prose from this same letter's own clear text), same token count and same 24-symbol alphabet, enciphered with a random letter-to-number table; run your solver (hill-climb or exhaustive on the obvious a=1...z=24 style tables first, then general monoalphabetic with French n-grams, using the clear context words around each group as cribs) and report the share read on the control. Then the target.
3. If it reads: key.tsv (grade S) and decode.json; tools/decode_key.py --check exits 0; per-token S/M/U; say what the overline and the loop mark do. If it does not: the negative with both numbers.
4. NOTES.md "L2: cryptanalysis". ROOM `for LANE V2: ciphers/la-garde-1577 reading ready (counts)` with the search log (what was checked in print: Groen VI 249-251 per NOTES), or a done line with the negative. Stop at the cap. Do not classify novelty.
