LANE B3 breadth worker bGLD: goldbar-1933 first test. Sonnet (claude-sonnet-5). Cap $3 or 30 minutes, whichever first. No subagents.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (read it first; it governs; its INTAKE STEP applies) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Spec: specs/goldbar-1933.json (written by bSPEC2, 25 Sept 2026). Intake step first (breadth.md): its network calls are allowed even where this brief says disk-only -- one shallow clone of each solver repository (grep, delete), one OpenAlex and one Semantic Scholar query with the keys from the environment (never print them). Create ciphers/<slug>/NOTES.md in the gate format if absent and paste the gate output.
Then run `cheap_tests_in_order[0]` only, exactly as the spec states it, with its matched control; write both numbers side by side into the spec's cheap_test_done (date, method, target, control, verdict) and scripts/outputs under specs/cheap-tests/goldbar-1933/. Hosts beyond intake: none (disk-only). The random-permutation control is 1000 shuffles of the same multiset; report the target statistic percentile against it.
A control-backed gap or a control below its gate is 'partial' and a line 'for LANE B3: NEAR candidate' in your done line -- never closed-negative. Do not run test 2. Done line: target vs control.
