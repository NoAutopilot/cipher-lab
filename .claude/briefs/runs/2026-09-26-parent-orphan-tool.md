PARENT WORKER ORPHAN-TOOL (Sonnet, cap $6 of usage, wall-clock box 45 minutes, no subagents, no network except git). Job: make the orphan check across orchestrator swaps mechanical, so the owner never has to ask (owner's ask, 26 Sept 2026 about 05:00 UTC: "make sure we don't have any orphaned tasks across orchestrator swaps, and make sure this is systematized"). Authorised by CLAUDE.md Usage 8a (rules become tools) and .claude/briefs/parent.md. Written 26 Sept 2026 at the 05:08 UTC clock read by parent 7e (session_01SvjMDFfJZJ3uK47RYxAQrM).

The gap: a parent hands over (7d->7e->7f tonight) and a lane hands over (B5->B6->B7, GOLD2->GOLD3); each successor adopts what its predecessor's handoff names. Nothing checks that every still-running or unarchived session, every enabled trigger and every ROOM.md claim without a done line belongs to a live owner. Scripts cannot call the session API, so the tool takes the parent's own `list_sessions` and `list_triggers` results as JSON files and cross-checks them against the repository's registers.

Build `tools/orphan_check.py` with `--help` and an offline test in `tools/tests/test_orphan_check.py` (fixtures: a small sessions JSON, a triggers JSON, a ROOM.md excerpt, an ASSIGNMENTS excerpt):
1. Inputs: `--sessions FILE` (the JSON the parent saved from list_sessions; accept the raw tool-result text, which may wrap the JSON in a `{"ccr": ...}` envelope or a list under "data"/"sessions": parse defensively), `--triggers FILE` (list_triggers JSON), plus ROOM.md and hub-seed/ASSIGNMENTS.md from the working tree; `--now` for tests.
2. Report, one line each, exit 1 if any: (a) a non-archived session whose parent_session_id is archived or absent and that is not named as adopted in the last 6 hours of ROOM.md or in any live session's ASSIGNMENTS row (an ORPHAN SESSION: worker or lane running with no one to ledger it); (b) a non-archived session idle (updated_at) more than 3 hours with no done line in ROOM.md naming its id or its worker name (a STALE SESSION); (c) an enabled trigger whose persistent_session_id is archived or not in the sessions list (an ORPHAN TRIGGER); (d) a ROOM.md claim line older than 6 hours with no later done/handoff line from the same role (a STALE CLAIM, the six-hour rule in CLAUDE.md "Collaborators"); (e) an ASSIGNMENTS row for a session id that the sessions list shows archived but that has no done/handed line (an UNLEDGERED CLOSE). Print a one-line summary "orphans: N sessions, M triggers, K claims, J unledgered" and, with `--room`, append that line to ROOM.md through tools/room.py.
3. Wire it in: `.claude/briefs/parent.md` -- new check-in duty 3a "Orphan check": at every check-in the parent saves `list_sessions` (mine: true, limit 100) and `list_triggers` results to files under its scratchpad and runs `python3 tools/orphan_check.py --sessions S --triggers T`; every line it prints is acted on (adopt into a lane, or ledger + archive, or delete the trigger) in the same check-in; and in "Handing over": the outgoing parent runs it and pastes a clean result into its hand-over line, and the successor runs it as its first duty after the reading list. Same two sentences in hub-seed/SUCCESSOR-PROMPT.md's check-in duty list and in the lane orchestrator common tail in `.claude/briefs/README.md` (a lane orchestrator runs it on its own workers before writing its handoff). One UPDATES.md row (`By` = "ORPHAN-TOOL for parent 7e", clock-read time) and a one-line pointer in CLAUDE.md Usage 8a's tool list.
4. Run the tool once on the real files if the parent left `orphan-sessions.json`/`orphan-triggers.json` in the repo root scratch (it may not; then run only the offline test and the ROOM/ASSIGNMENTS halves with `--no-sessions`) and put the output in your done line.
First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker ORPHAN-TOOL (Sonnet, <session id>)`. Finish: `python3 tools/room.py --push <paths>`; ROOM done line `done: ORPHAN-TOOL: tools/orphan_check.py + test, wired into parent.md/SUCCESSOR-PROMPT.md/README.md, UPDATES row; live run: <summary line or not run>`; one-paragraph reply; never call AskUserQuestion; never print or commit credentials.

ADDENDUM 05:12 UTC (owner, 05:10): the check applies to every account. Word parent.md's new duty, the UPDATES.md row and the README common-tail sentence as "each account's parent runs `tools/orphan_check.py` on its own `list_sessions`/`list_triggers` output at every check-in and at both ends of a hand-over; no account can see another's sessions, so each covers its own", and post one ROOM.md line "for the owner-account parent: tools/orphan_check.py landed -- run it on your own sessions from your next check-in; parent.md duty 3a" when you push.

ADDENDUM (RETRO-2026-09-26d, 05:33 UTC): make the 05:17 ROOM.md ask ("add one check if cheap") a formally named
item, not only a message this one worker instance might or might not reach before its own cap: item 2 gets check
(f) TITLE MISMATCH -- a non-archived session whose title does not start with `LIVE ` (parent.md's own convention),
or an archived session whose title does not start with `ARCHIVED`. Add a fixture for it to
tools/tests/test_orphan_check.py before landing, and fold the count into the summary line ("orphans: N sessions, M
triggers, K claims, J unledgered, L title-mismatches"). If ORPHAN-TOOL already added this per the 05:17 ask, this
addendum only confirms the fixture and summary-line wording match; it is not a conflicting instruction.

Confirmed landed by RETRO-APPLY-P (26 Sept 2026, 06:1x UTC): check (f) TITLE MISMATCH added to
`tools/orphan_check.py` (`check_title_mismatch`), a fixture in `tools/tests/test_orphan_check.py`
(`test_title_mismatch_live_and_archived`), and the summary line now reads "orphans: N sessions, M triggers,
K claims, J unledgered, L title-mismatches" exactly as asked.
