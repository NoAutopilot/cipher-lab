PX-BROBODY (Sonnet, cap $6 as a stall alarm; the orchestrator watches your cost, you cannot, so keep the job small; at most 2 subagents). Target: ciphers/antt-msliv0638-brochado-1712. Job: find and transcribe the ciphered passages in the body of the volume (outside the appendix m0279-m0296). No decoding. Parent: LANE PX orchestrator session_01KapVpHVzNpnnQce5C8c3LY. Read the lane COMMON, .claude/briefs/transcription.md, and NOTES.md (the m0276 body passage; images/filelist.json). PX-BROPASSB is working in the same folder on the appendix files: touch only the files named here.
DigitArq (you are the only worker on it; say so in ROOM): one request at a time, >=3 s apart, at most 60 requests. tools/digitarq_fetch.py --thumbs DOC_ID --stride N --montage, then --full --names for chosen leaves. DocId 7049343e7af44ba7b4ec18e74e1b2cd7.
1. Sweep m0001-m0278 and m0297-m0306 by thumbnail montage at a stride the budget allows (plan it: ~35 thumbs + <=10 full res); the cipher runs are small numbers and single letters with dots, inline in the letters (see m0276). Record in body_leaves.tsv every leaf looked at: image, what it shows, cipher yes/no, letter number if visible.
2. Full resolution for up to 10 leaves with cipher runs (m0276 first); images into images/body/ with a manifest entry.
3. Two blind passes (two subagents) of each cipher run with 5-10 clear words either side, into body_passA.tsv and body_passB.tsv (leaf, letter_no, run_id, position, token, token_type, context_before, context_after); reconcile into body_ciphertext.tsv with grades H/M and the agreement rate. Note any body run whose letter number also has an appendix entry (a control, not a reading).
Push after each step. Touch only ciphers/antt-msliv0638-brochado-1712/{body_leaves.tsv,body_pass*.tsv,body_ciphertext.tsv,images/body/**,NOTES.md (one new section "## PX-BROBODY")} and ROOM.md. Report leaves swept, cipher leaves found, runs and tokens transcribed, agreement, requests per host.
