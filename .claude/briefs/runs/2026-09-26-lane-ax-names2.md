LANE AX AX-NAMES2 -- lodewijk-van-nassau-1573-74: merge names.tsv into key_full.tsv under the orchestrator's re-gate, re-decode 5797 and 4610/4611/4616 with it (Opus: key reading; wall-clock box 90 minutes). Parent: LANE AX orchestrator session_01VzK62xX92yKfnUD93zprD8. Read and follow .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md and the common tail of .claude/briefs/README.md (both override anything below they conflict with), CLAUDE.md in full, the last 20 lines of UPDATES.md, and NEAR.md. First: `date -u`; `python3 tools/room.py --start`; last 30 lines of ROOM.md; if ROOM.md shows a "retrospective ... starting" with no matching "done", wait before claiming. Claim with tools/room.py before other work; never AskUserQuestion; no dollar figures for yourself. The box is a MINIMUM as well as a maximum: if you finish the named steps early, do the listed extension steps before stopping.

Read NOTES.md section "AX-NAMES" (01:16 UTC) in full, names.tsv, axnames/*.

Orchestrator's decision (01:42 UTC 26 Sept 2026, written before any step-4 run; paste it verbatim into your section):
"AX-NAMES' held-out gate could not fail for a null-heavy map (rule 3 ceiling warning), so it is replaced, not relaxed, by three gates set per class. (a) Word/name codes whose value is read from a contemporary interlinear gloss (5550, 5557) or printed by Groen at that very cluster are key-source readings (rule 4: H for gloss, C for print-at-cluster), not aligner output; they merge without an aligner control: 153, 161, 171, 202, 336, 339, 350 (gloss), 154, 200 (print at the 5797 cluster). Aligner-only word codes merge only at C with >=2 agreeing observations (223 Harlem); every single-observation word code stays M and out of key_full. (b) NULL codes merge if they have >=4 empty observations and 0 contradicting (false-null rate 0.18^4 = 0.1%, AX-NAMES' diagnostic); 139, 140, 142 (2 obs) stay out. (c) key.tsv conflicts 123, 128, 136 (key.tsv M values vs 13/15/8 NULL observations): key_full takes NULL with a note; key.tsv itself stays unchanged. Before merging, re-run the known-answer check on 4613/4615 (occ_sib) with key_full: every key.tsv C letter there must still decode as before (0 regressions) -- if any regresses, stop and flag."

Steps.
(0) Intake gate output pasted. (1) Build key_full.tsv = key.tsv + the rows licensed above (script axnames/build_key_full.py with --check; columns as key.tsv plus grade H/C and source naming the gloss/print). (2) Known-answer regression check as above. (3) New decode configs decode_5797_full.json, decode_4610_full.json, decode_4611_full.json, decode_4616_full.json -> reading_<n>_full.txt/_tokens.tsv via tools/decode_key.py --check (never edit the existing readings). (4) Per 5797 spot: the decoded string with grades against Groen's frame (e.g. "und [161 Landgraf, H] ist [w]illens"); per 4610/4611/4616: U before/after, every changed word with grade, as a table "revisions_for_audit.tsv" (letter, line, old, new, grade, source) for the orchestrator to carry into AUDIT.md and any SECOND-OPINIONS-QUEUE.tsv row (rule 10 revision rule; you do not edit those files). (5) judge: specs/lodewijk-5797.json on reading_5797_full.txt, paste output with the per-fold caveat; say plainly if spots are below judge length.
Extension steps if time remains: (6) fit the 5810 and 5811 readings under key_full (U before/after) as a sanity check that the merge adds no garbage to letters whose text Groen prints (count words that now disagree with Groen). (7) list the codes still U across all table letters with their contexts, sorted by count, as axnames/still_unread.tsv for AX-GLOSS.
(8) NOTES.md section "## AX-NAMES2: key_full and the 5797 spots (26 Sept 2026, LANE AX)"; ROOM done line "for LANE AX: key_full +<n> rows (H a / C b / NULL c); regressions 0; 5797 spots with a value <k>/6; 4610/4611/4616 U <x> -> <y>; revisions <r>; reading ready for re-derivation".
Files: ciphers/lodewijk-van-nassau-1573-74/{NOTES.md,key_full.tsv,decode_*_full.json,reading_*_full*,revisions_for_audit.tsv,axnames/**}. Do not touch key.tsv, the N4 readings, AUDIT.md or SECOND-OPINIONS-QUEUE.tsv.
