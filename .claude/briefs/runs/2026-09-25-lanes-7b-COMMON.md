COMMON ADDENDUM for parent 7b's lanes of 25 Sept 2026 (R6, V6, B2). Read after CLAUDE.md and before any job brief. Written 25 Sept 2026 15:39 UTC from the other account's 25 Sept lanes (VX, KX, YX, CX, LX, PX, OX, TX, four QA runs, RETRO-2026-09-25h) so their lessons are rules here from the start.

1. Cost is read by the orchestrator, never self-reported. Every worker brief carries a wall-clock box ("stop and push at <cap> or at <minutes> minutes, whichever first") and the orchestrator reads `get_session` usage.cost_usd at every check-in; a worker's own "under cap" line counts for nothing (25 Sept: nine of 82 workers overran, up to 4.6x, all claiming under cap; KX-LATHKEY ran to $44 on an $8 alarm). A worker with subagents budgets for them (24 Sept H2: $12.59 on $12 because the subagent's usage folded in late).
2. The intake gate is mechanical: `python3 tools/intake_gate_check.py <target>` exit 0 before any brief with a cap above $3 on that target; paste the live output into the brief. An `open` or `partial` verdict with no edition-and-page citation within six lines of the status word reads as blocked, and a target that fails the gate gets a check-solved worker, not deep work.
3. Rule 7 as amended: a reading is reported only with `tools/judge_plaintext.py <spec> --file <reading>` output pasted, and the judge counts only when its language has a real corpus in tools/data (pt17 is Vieira only and is not a corpus for 1780-1830 Portuguese; nl, es, la are not wired). Before comparing passes or a reading to a control, normalise transcription conventions (abbreviations, mark notation, run boundaries); PX-BRODEC manufactured an 11-point gap by diffing differently abbreviated transcriptions.
4. Lead classes beat queue rank for cheap wins (OX): the same letter in print > a sibling's decipherment > an interlinear gloss > a published key of the same office > cryptanalysis. Try them in that order and stop at the first that reads.
5. Breadth before campaign (CLAUDE.md 3a): no cap above $10 on a target without a spec and a first cheap test with a control on record.
6. Verifier and solver are never one session; novelty words only from AUDIT.md (rule 10). A found-solved gets a verifier before it counts as a catch on the board (bl-charles-digby is waiting for one).
7. Hosts: one worker per host, the good-citizen limits, the per-host rules in the Access playbook's "Image and catalogue hosts" table; digitarq 3 s; Gallica two fetchers; Reddit only through the script-app pair by variable name.
8. Cross-account: the other account's lanes (LX, DX, OX, PX, TX, KX, VX, YX, CX) are closed until its window resets about 05:00 UTC 26 Sept; their handoff sections in STATUS.md are the record; parent 7b has taken their three post-reset items (Portuguese 1780-1830 judge corpus and the Linhares re-run; a verifier for bl-charles-digby; deep work on YX's 13 gate-passing targets) and says so in ROOM; when their parent returns it reads ROOM and STATUS, never a transcript.
9. Every done line carries the target number and the control number side by side, the request count per host, and no cost figure (the orchestrator reads it). Times from `date -u`, never estimated. Quote a dollar figure in ROOM lines with single quotes or through tools/room.py (double-quoted $8 vanishes in the shell).
