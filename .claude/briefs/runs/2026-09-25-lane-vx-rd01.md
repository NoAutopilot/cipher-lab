VX-RD01 (Sonnet, stall alarm $6, at most 2 subagents). LANE VX job 3: read ciphers/na-schonenberg-1678-1716 (QUEUE VX-E01; check-solved `open` 25 Sept 2026 by VX-CS01, intake gate passed: NA 1.02.04 finding aid read in full, Heinsius Briefwisseling full-text searched). Kind: recovery (key source `period`: the leaf's own interlinear gloss). Parent: LANE VX orchestrator session_01EYwyYDeAaQReWy7ZuQcoxe. Read .claude/briefs/runs/2026-09-25-lane-vx-COMMON.md, .claude/briefs/transcription.md, .claude/briefs/solver.md, and the target's NOTES.md in full.

Material: NA 1.02.04 invnr 63, one leaf, full size https://service.archief.nl/api/file/v1/default/fc3a8d42-b89e-4715-9753-0320355365c7 (on disk under images/ if CS01 saved it; fetch once otherwise). CS01: ~266 two-digit groups on 17 lines, ~246 carry an interlinear Spanish gloss, the last ~20 (two closing lines before the address) carry none.
Steps:
1. Crops: tools/iiif_lines.py or a PIL crop per line (under 2500 px wide), with a debug overlay you check; images/manifest.json.
2. Two blind transcription passes (two Sonnet subagents from the crops, per transcription.md): per line, every cipher group exactly as written and the gloss written above it (period Spanish, abbreviations in brackets), aligned group by group; mark illegible as ?. Reconcile with tools/reconcile_passes.py; settle disagreements from the crops. Agreement under 60% on the groups: stop and report the blocker. Write ciphertext.tsv (line, pos, group, gloss_as_written, grade H/M).
3. Key: decide the system from the data (letter substitution with homophones? syllables? nomenclator codes for names?; nulls; how word divisions are shown) and state it with evidence. key.tsv (code, value, n, lines, grade C when fixed by >=2 glossed occurrences or one unambiguous alignment, M otherwise), conflicts.tsv (a code glossed two ways). decode.json so `tools/decode_key.py ciphers/na-schonenberg-1678-1716 --check` regenerates reading.txt; report how well the key re-reads the glossed stretch against its own gloss (per-token agreement, the control).
4. The unglossed closing groups: decode with the key; per token H only where the glossed stretch fixes the code (use C per rule 4 if you judge the gloss to be known-plaintext; say which convention and why), M uncertain, I inferred; codes never seen in the gloss stay [?]. Also check whether the gloss itself has gaps or errors the key corrects (a period decipherer's slip is a finding, report it with the evidence).
5. Spec and judge: specs/na-schonenberg-1678-1716.json on the pattern of specs/antt-linhares-chave.json (language es, judge block); paste `python3 tools/judge_plaintext.py specs/na-schonenberg-1678-1716.json --file <reading of the unglossed stretch, or the full letter if the stretch is too short for the judge -- say which>` into NOTES.md (a FAIL is reported as a FAIL).
6. Fresh-instance re-derivation: one subagent given only ciphertext.tsv (groups + gloss column) and the crops, not key.tsv and not your reasoning, rebuilds the key and decodes the unglossed stretch; report agreement per code and per token; a difference beyond the M tokens goes in NOTES.md as unresolved.
7. NOTES.md section "Reading (VX-RD01, 25 Sept 2026)": counts H/C/M/I, the reading of the closing stretch in Spanish with an English gloss, the full letter's gist in two lines, what was not found. Do not classify novelty.
Touch only ciphers/na-schonenberg-1678-1716/**, specs/na-schonenberg-1678-1716.json, ROOM.md. service.archief.nl <=10 requests. At 80% of the alarm push what you have, one line of remaining steps in NOTES.md, stop.
