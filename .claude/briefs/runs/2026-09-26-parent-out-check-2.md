OUT-CHECK-2 (Opus, cap $4 of usage, box 25 minutes; no network needed beyond git). Parent worker for parent 7i (session_01U1NiMakQ8PPySH8WixXsmc, "LIVE parent 7i · Orchestrator 10"), CLAUDE.md Outreach gate 7. You are the checker, never the drafter. Written 26 Sept 2026 at the 17:47 UTC clock read.

Why: OUT-CHECK (17:37, 03890c5) fact-checked the six outreach/mailbox/*.json drafts and wrote `checked:` lines. The owner then read one in Gmail and said it read like a report, not a person writing to a person, so parent 7i rewrote all six bodies in first-person plain voice (7482f8e). Each json now holds `body` (the rewrite) and `body_checked_1737` (OUT-CHECK's verified text). A rewrite is a new draft until a non-drafter checks it, so the drafts are not sendable until you have written your line.

Job, per json file: (1) diff `body` against `body_checked_1737` fact by fact -- every name, number, date, shelfmark, ark, folio, edition, page, count, URL, and every claim of what was and was not checked; flag anything in `body` that is not in the checked text (an added fact) or that changed meaning (a softened claim hardened, a "comparison" made an "exclusion", a "we" made an "I" where the checked text attributed the act to the agents' searching -- the disclosure split is: the person directs and sends, the agents read, search and check). Also check: the first paragraph still says plainly who is responsible and how the work is divided (outreach/README.md rule 1: the substance, not a fixed sentence); rule 10 sweep (solved, new, first, unpublished, unread, never printed); the `[SIGN-OFF]` line is present and last; the repository URL is exact. (2) Fix in place, minimally, keeping the plain voice: a fact that moved goes back to the checked value; do not restore the report tone. (3) In the matching outreach/<slug>.md, replace the body text with the final json `body` so the two agree (leave the header lines: status, to, subject, checked, voice), and append one header line `checked: <UTC time> by OUT-CHECK-2 (Opus, <session id>) -- voice rewrite re-checked against the 17:37 text: <n> facts compared, <m> corrected: <list or "none">`. (4) Update the json `notes` first clause from "re-check by OUT-CHECK-2 pending" to "re-checked <time> by OUT-CHECK-2". Never add a fact; never write the owner's name; never touch the Gmail drafts (the parent updates them from your final json). Before every commit `git diff --stat`; commit by explicit path; push only through `python3 tools/room.py --push <paths>`. First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker OUT-CHECK-2 (Opus, <session id>)`. Finish: ROOM done line "done: for parent 7i: OUT-CHECK-2 -- 6 rewrites re-checked, corrections: <one clause each or none>; sendable: <slugs>"; one-paragraph reply listing any correction so the parent can mirror it into Gmail; stop. Never call AskUserQuestion; never print or commit credentials.
