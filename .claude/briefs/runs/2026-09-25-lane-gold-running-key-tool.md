JOB GOLD-2A: `tools/running_key.py`, the Köhler family-B tool, with its matched control gate. Opus (claude-opus-5-5). Stop and push at $15 or 90 minutes, whichever first. Lane: LANE GOLD orchestrator session_01DKDynpdEwZK5EokxtjCM3P. Written 25 Sept 2026 16:50 UTC.

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding: the wall-clock box, cost read by the orchestrator), CLAUDE.md rule 3 and Usage item 8 (shared tools: `--help` and an offline test in tools/tests/), `specs/koehler-1944.json` (ciphertext, five message lengths 237/178/140/140/229), `specs/cheap-tests/koehler_periodic.py` and `.out` (the period 2-30 negative and its controls), UNSOLVED-SURVEY.md "Gold-lane ladder for Köhler". Claim in ROOM.

Hypothesis family B: each Köhler message is Vigenère-family enciphered under a running key taken from a natural-language book (Dutch or German; the FBI-run channel used a Dutch prayer book). A running key leaves the flat, weakly non-uniform letter distribution seen (pooled IC 0.0399).

Build `tools/running_key.py` (pure Python + optional numpy; follow the style of tools/homophonic_anneal.py):
1. Tabulae: `--tabula vig` (c = p + k), `beau` (c = k - p), `varbeau` (c = p - k), mod 26.
2. Two-stream decoding: find p maximising LM_p(p) + LM_k(k(p,c)) with letter n-gram models (order 3-5, add-k or Kneser-Ney-lite) built from `--pcorpus` and `--kcorpus` files (gz allowed); Viterbi/beam search over the n-gram state (beam width option), plus an optional local-improvement pass. Each message decoded independently (key offset unknown per message); output both streams per message.
3. `--crib-drag WORDLIST`: slide each crib through each message under each tabula, score the implied key fragment under the key LM, print the top hits (a crib-drag table, not a claim).
4. `--control`: take German plaintext (from `--pcorpus`, a random start per message, seeded) cut to the five message lengths, encipher each under a running key cut from a DIFFERENT book in `--kcorpus` at a random start (key text must never also be in the model's training text for the control: hold the key book out of the key LM, as for the real target whose book is unknown), decode blind with the same settings, print per-message and pooled share of plaintext letters recovered. `--seed`.
5. Side output `--period-scan 31 120`: per-message coset IC for periods 31-120 (a long-period Vigenère is the other design a running-key test must not confuse).
6. `--help`; offline test `tools/tests/test_running_key.py` on a tiny synthetic case that passes in under 60 s.
Corpora: GOLD-C (Sonnet, in parallel) is fetching `tools/data/de20` and `tools/data/nl20` from Gutenberg and pushes them first. Do not fetch Gutenberg yourself (one worker per host). Build and unit-test on what is on disk; `git pull` for de20/nl20 before the gate runs (if they have not landed 45 minutes in, flag in ROOM and run the gate on what exists, saying so).

GATE (rule 3): `--control` with German plaintext (de20) and a key from a held-out de20 book and, separately, a held-out nl20 book, all three tabulae at least for vig, seeds 1-3: pooled plaintext-letter recovery >= 60% on 3 seeds. Report the numbers per seed and message length (140-letter messages will be hardest). If the gate is not reached, improve the tool (model order, beam, smoothing, iterating) within your box; do NOT run the target on a tool that fails its gate.
Only if the gate passes: run the target once per tabula x key language (vig/beau/varbeau x de/nl key, German plaintext model; also nl plaintext x nl key), seeds 1-3, and record for each run the pooled mean per-letter log-likelihood of both streams next to the same statistic on the control decodes and on a control of random (one-time-key) ciphertext of the same lengths -- the control-noise level. Write all of it into `ciphers/koehler-1944/HYPOTHESES.md` (create it if absent; a top summary block, then an append-only section "Family B, running key" with method, parameters, seeds, corpora, CONTROL numbers and TARGET numbers side by side, partial decodes of 15+ letter stretches in both streams, dead ends). A target decode is interesting only if both streams score clearly above the one-time-key noise band; any such stretch goes to ROOM as `flag:` with the numbers, and no plaintext is claimed without `tools/judge_plaintext.py specs/koehler-1944.json --file <reading>` output.
Also write the period 31-120 scan result into HYPOTHESES.md with the one-time-key control's band.
Push by explicit path after the tool+test land, after the gate, after the target runs. Done line: control a/b per seed, target best vs noise band, no cost figure. Rule 10 wording only; the words solved/new/first never appear.
