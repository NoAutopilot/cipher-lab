ROLE: SCOUT, Italian and Vatican archives B (LANE N). Read .claude/briefs/scout.md, the rubric in .claude/workflows/scout.js, README "What counts as a result", STATUS.md's lane table, and QUEUE.md sections "Digitised candidates outside the BnF" and "Europeana and Real Academia". Goal: ciphered nuncio despatches, avvisi, cardinals' letters and Venetian ambassadors' dispacci 1450-1800 in cipher (cifra, in cifra, cifrato, deciferato, deciferazione, zifra, 'dispacci in cifra', 'lettere in cifra', 'Segreteria di Stato, cifre'; Latin 'notis arcanis') with NO decipherment beside them, strongest preference for items with a public image online. Note the Vatican Library's digitised Barberini and Vaticani latini manuscripts often hold cipher registers and keys (a key beside undeciphered letters elsewhere is a recovery lead). Tomokiyo (sources/cryptiana/) and Lasry's papal-cipher work: exclude what is already decoded.
Hosts (yours alone in LANE N): Vatican Apostolic Archive (archivioapostolicovaticano.va, its online indexes/inventories), DigiVatLib (digi.vatlib.it, the Vatican Library's IIIF manifests and search: use the IIIF/JSON endpoints, not HTML scraping, where they exist), Archivio di Stato di Venezia (archiviodistatovenezia.it, its Divenire / SIASVe online inventories and digitised series), Biblioteca Nazionale Marciana's manuscript catalogue if it has a GET search, and Internet Culturale / Manus online (manus.iccu.sbn.it) for manuscripts described 'in cifra'. Egress-test each host first; 000 means blocked: log and move on. Browser tool allowed for JS pages, one page at a time.
For every candidate: normalise (holder, reference, date, sender, recipient, extent, description, image URL if any); exclude anything already named in QUEUE.md, CATALOG.md, LANDSCAPE.md, ciphers/, sources/cryptiana/, or fresh shallow clones of github.com/dbourdeau/cyphersolver and github.com/aaymeloglu/unsolved-ciphers (grep holder+shelfmark and names), and anything whose catalogue says deciphered/decoded/with decipherment unless a sibling letter stays undeciphered (then it is a recovery candidate). View the record page and, if public, the first leaf image before scoring, to confirm ciphertext is present (never transcribe). CONTROL (required, report it): before trusting any host's zero or low count, show that your query form surfaces at least one cipher item already known on that host (from QUEUE.md, CATALOG.md, Tomokiyo, or a solver repo); a host whose control fails is logged "query form unverified", not "no candidates". Score every axis of the rubric in .claude/workflows/scout.js including unread and kind (cryptanalysis / recovery / contribution; drop editions); copy-free (public image of the leaf online, no login, no payment) scores above copy-order.
Output: a NEW QUEUE.md section "Vatican and Venetian archives (LANE N scout of <date>)" with an intro (hosts reached, queries, controls and their result, blocked hosts with the one attempt logged, raw/kept/copy-free counts), a table with row ids VA1, VA2, ... (this prefix is reserved for you; at most 20 rows, best first, copy-free first), and a caveats paragraph; raw hits and exclusions to sources/solver-diffs/2026-09-24-lane-n-italy-b.tsv; append rows to QUEUE-scores.json under key "lane_n_italy-b" (read its structure first; rebase immediately before editing). Report per host: raw, kept, copy-free count. Never check-solved, never promote: a later worker runs check-solved on your top rows.
Cap: $8.

COMMON RULES (LANE N worker; parent LANE N orchestrator session_01W4z8JhXJYHRjorPC1Qkpiy; lane goal: stage-2 nominations, at least half copy-free):
- First: `date -u` (use that clock for every timestamp; never estimate it); `python3 tools/room.py --start`. Read CLAUDE.md in full (rules 1, 3, 5, 10; the good-citizen rule; the Access playbook), LESSONS.md sections 1-2, .claude/briefs/check-solved.md (the Thurloe, Raince, Bowes and M9 lessons), and the last 30 lines of ROOM.md. Claim with `python3 tools/room.py "LANE N <role> (Sonnet, <your session id if known>)" "claim: <rows/target> -- files: <paths>"` BEFORE other work. Done line the same way when you stop; `flag:` for anything the lane orchestrator must see.
- Git: commit by explicit path only (never `git add -A`); push with `python3 tools/room.py --push <paths>` (rebase-and-retry loop, keeps both sides of ROOM conflicts). ROOM.md is append-only; on a conflict in QUEUE.md keep both sides. Never rewrite history, never a pull request.
- Hosts (good-citizen rule): one request at a time per host, >=1.5 s apart; descriptive User-Agent `cipher-lab research script (contact via repository)` unless the playbook says a site needs a browser UA; on 429/403/Cloudflare/challenge stop that host, log it in NOTES.md and ROOM.md, one retry after a pause at most. Use ONLY the hosts your brief names plus: WebSearch/WebFetch, github.com (shallow clones of dbourdeau/cyphersolver and aaymeloglu/unsolved-ciphers, grep only), and the files already in the repo (sources/cryptiana/, sources/solver-diffs/). NEVER gallica.bnf.fr or archivesetmanuscrits.bnf.fr (LANE G), never Google Books (LANE V). archive.org: only if your brief gives you the IA slot. DECODE (de-crypt.org): only the DECODE worker logs in; everyone else uses sources/decode/ files and the solver repos' DECODE sweeps. Report your request count per host.
- Quality over quantity. A nomination later found solved costs more than none. Before writing "open": (1) the recipient's and sender's standard printed edition and the calendar for the date (Raince lesson); (2) a printed or interlinear decipherment in the same edition or the catalogue note (Thurloe lesson); (3) quote verbatim any sentence in Tomokiyo, a solver README/NOTES or an edition that names this very letter (M9 lesson).
- Rule 10: never write new, unpublished, unread, first, never printed, previously unknown. Report what was found and where it was not found. Do not classify novelty. Never promote to the board, never solve or transcribe beyond confirming ciphertext is present, never start a target your brief does not name. No copy orders or payments: a target that needs a copy gets ciphers/<slug>/REQUEST.md (no personal data).
- Credentials: never print them; no unfiltered env/printenv/set, no curl -v / --trace / set -x on a credentialed call; test presence with `test -n`.
- Usage: Sonnet; cap stated in your brief (check your own cost; stop at the cap even if unfinished, and push what you have). At most 4 subagents at once, on Sonnet. Scripts read, models judge: fetch once to disk, grep, give the model the hits.
- Finish: commit, push; ROOM `done` line that ends with `nominations: <n> (<k> copy-free)` when your brief is a check-solved; then your final reply is ONE paragraph, first line the answer (per-row verdicts or new row ids), then files touched, request counts per host, cost. Then stop.
