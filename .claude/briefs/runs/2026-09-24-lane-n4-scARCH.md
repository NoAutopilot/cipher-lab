ROLE: SCOUT scARCH (LANE N4), holding-archive viewers for the CS2 copy-order rows. Sonnet. Cap $5.
Input: the 22 copy-order CS2 rows (QUEUE.md scCS2 section and sources/solver-diffs/2026-09-24-cyphersolver-site.tsv; their only image route is a DECODE record, thumbnail only). Also CS2-18 (KHA Fagel inv. 5206) and CS2-19 (KHA A31-902) if the Koninklijk Huisarchief has a viewer.
Job: for each row, name the holding archive and shelfmark (from the TSV / DECODE record metadata in sources/decode/), and test whether the archive's OWN digital viewer serves the leaf at readable size now. Start with the likeliest: Sächsisches Staatsarchiv / Hauptstaatsarchiv Dresden (archiv.sachsen.de, the online Findbuch and its digitised-item viewer) for DECODE R5005-R5008; Bayerisches Hauptstaatsarchiv (gda.bayern.de / the Digitale Sammlungen der Staatlichen Archive Bayerns) for ÄA 4591 (R9319, R9424); then the rest grouped by archive. One host at a time, >=3 s apart, <=25 requests per host; a challenge or 403 stops that host (logged). Bavarikon, Kalliope, Europeana, e-manuscripta, e-codices, handschriftenportal and Vatican digi.vatlib are closed as tested negatives: do not use them.
Output (only here): QUEUE.md, a section "## CS2 copy-order rows: holding-archive viewers (LANE N4 scARCH, <date -u>)": per row the archive, shelfmark, viewer URL tested, result (copy-free with the leaf image URL / no viewer / not digitised / blocked host), and the row's new state (copy-free or copy-order). Update the CS2 row's own state word where it changes. No ciphers/ folders, no nomination lines (scouts never do); list the copy-free rows in your done line for the check-solved batch.
Hosts: archiv.sachsen.de and its viewer hosts, gda.bayern.de and the Bavarian archives' viewer hosts, other holding archives' own sites as the rows require (each <=25), WebSearch. No DECODE login.
