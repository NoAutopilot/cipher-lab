JOB GOLD-K1: Köhler families B and B' rerun with a devotional Dutch key corpus (the one documented Koehler key is a Dutch prayer book; every B/B' negative so far is conditional on 1880-1920 novel models for the key stream). Sonnet (claude-sonnet-5). Stop and push at $8 or 60 minutes, whichever first. NO subagents. Lane: LANE GOLD orchestrator session_01DKDynpdEwZK5EokxtjCM3P. Written by the cycle-1 consolidator GOLD-CONS1 (Fable, session_017PRh6MAX17rgp5ayuycZ6c), 25 Sept 2026 19:35 UTC; the orchestrator fills the gate line and the spawn time.

INTAKE_GATE_OUTPUT: <orchestrator pastes `python3 tools/intake_gate_check.py koehler-1944` output and exit code here before spawning>

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), the top block and the "Family B" and "Family B'" sections of `ciphers/koehler-1944/HYPOTHESES.md`, `tools/data/nl20/README.md` and `MANIFEST.tsv` (the shape a corpus folder takes), `python3 tools/running_key.py --help`, `python3 tools/family_run.py --help`, CLAUDE.md's good-citizen rule. Claim in ROOM (`tools/room.py`). Read the clock with `date -u` before any date.

What the last round learned, pasted:
- GOLD-2A (family B, standard tableau, `tools/running_key.py`): CONTROL German plaintext at 237/178/140/140/229 under a held-out Dutch novel key (nl20), vig, order 8, beam 1000, `--spaces`: 72.7 / 60.4 / 68.3 pct strict plaintext recovery, seeds 1-3 (gate 60 met); German key 73.7 / 79.1 / 77.1 either-stream (the two streams swap under vig with same-language models). TARGET on 7 configs: pooled joint ll -3.57 to -3.62 per letter, inside the one-time-key noise band -3.57 to -3.64; controls -3.12 to -3.17. Model error, not search error, was the diagnosis: a larger or register-matched corpus is the lever, not a larger beam. GOLD-C found no Dutch devotional text on Gutenberg.
- GOLD-2C (family B', keyword-mixed tableau via `tools/family_run.py --family keyed_running_key`): CONTROL 79.1 / 74.0 / 64.7 pct (gate 0.5 met); TARGET winner -3.534 inside the full-pipeline noise band -3.507 to -3.544, judge FAIL. Settings: order 6, beam 300, top 30, spaces 1, LM_p de20, LM_k nl20.
- Memory: order-8 decoders take 3-4 GB each, four at once were OOM-killed twice; run at most two order-8 processes, three at order 6. Never rebase (or `tools/room.py --push`) while a decoder writes a log in the tree: log to the scratchpad, copy in after exit.
- Dutch spelling 1880-1947 (De Vries-Te Winkel) is the spelling of a 1944 key, so nl20's orthography is right; the register (novel vs prayer book / Bible) is what this job changes.

Do:
1. Corpus `tools/data/nl_dev/` (README.md with source URL, edition, date fetched, licence; MANIFEST.tsv like nl20's; one `.txt.gz` per book of the Bible or per prayer-book section, at least 10 files so a control can hold one out, at least 1,000,000 letters in all). Sources in order, reachability first with `curl -sS -o /dev/null -w "%{http_code}" URL` (000 or 403 or a challenge page: log it, move to the next; never retry in a loop): (a) the Statenvertaling on dbnl.org (the 1637 Biblia or a 19th-century edition; dbnl serves plain-text downloads per title); (b) statenvertaling.net or bijbel-statenvertaling.com (chapter pages); (c) the getbible API (`api.getbible.net/v2/statenvertaling.json` or its per-book endpoints); (d) any Gutenberg Dutch psalter, "Gebedenboek", "Kerkboek" or catechism (`gutendex.com/books?languages=nl&search=...`). A Catholic Dutch prayer text is welcome beside the Bible if one is reachable, and is logged as its own file. At most 40 requests in total, one host at a time, 1.5 s apart, descriptive User-Agent, stop on the first 429/403 per host. If no source is reachable, write a LOCAL-QUEUE.tsv row for the owner (what to download, where to put it), say so in the done line, and stop: the job's premise is the corpus.
2. Family B rerun. CONTROL first: `python3 tools/running_key.py --control --pcorpus tools/data/de20 --kcorpus tools/data/nl_dev --spaces --order 8 --beam 1000 --seed S --tabula vig` for S = 1, 2, 3 (the key window comes from a held-out nl_dev file). Gate: strict plaintext recovery at or above 60 pct on the three seeds. If one seed's control takes more than 15 minutes, drop to `--order 6 --beam 300` for everything in this job and say so (the control number is then what those settings read). Then the TARGET, once per tabula: `python3 tools/running_key.py specs/koehler-1944.json --pcorpus tools/data/de20 --kcorpus tools/data/nl_dev --spaces --order 8 --beam 1000 --tabula vig` and `--tabula beau`. Report pooled joint ll per letter beside the control's and beside GOLD-2A's noise band (-3.57 to -3.64 at order 8 / beam 1000; if you dropped to order 6 / beam 300, re-run one uniform-random noise text with `--noise` at those settings and report that band instead).
3. Family B' rerun, control first, through the tool: `python3 tools/family_run.py specs/koehler-1944.json --family keyed_running_key --corpus tools/data/de20 --param kcorpus=tools/data/nl_dev --param top=30 --param beam=300 --param order=6 --param spaces=1 --seeds 3 --gate 0.5 --label "GOLD-K1 B' keyword-mixed, devotional Dutch key, control before target"`. The row lands in HYPOTHESES.md by itself.
4. Append "Families B and B', devotional Dutch key (GOLD-K1)" under the append-only part of `ciphers/koehler-1944/HYPOTHESES.md`: the corpus (files, letters, source), the control numbers beside the target numbers for B (vig, beau) and B', the noise band used, the first 40 letters of both streams of message 1 for each target run (documenting noise, never a reading), and the limits (register still not military German on the plaintext side; a prayer book is not a Bible). One line each in `specs/koehler-1944.json` `cheap_test_done` and in NOTES.md "Next steps".
5. A candidate reading exists only if `tools/judge_plaintext.py specs/koehler-1944.json --file <decode>` prints PASS: paste it, write "for LANE GOLD: judge PASS, re-derivation owed" in ROOM, stop. A target pooled joint ll above the top of the noise band by 0.1 nats or more without a PASS is a ROOM `flag:` line.

Done line (ROOM, starting `done: for LANE GOLD: GOLD-K1`): corpus size and source (or "no source reachable, LOCAL-QUEUE row N"); B control a/b/c pct vs target vig x / beau y per letter vs noise band; B' control a/b/c pct vs target z, judge line; requests per host; files; no cost figure; no reading described unless the judge passed. Rule 10: never solved, new, first, unpublished; NOTES.md status stays `open`.
