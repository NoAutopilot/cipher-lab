OX-WV69 (Sonnet, cap $6 as a stall alarm, 2 subagents for the passes). Parent: LANE OX orchestrator, session_01BE3g8tWbS4T24KXMpShHt4. COMMON: .claude/briefs/runs/2026-09-25-lane-ox-COMMON.md applies in full; .claude/briefs/transcription.md. Never call AskUserQuestion.
Target folder: ciphers/willem-van-hessen-1567, sub-item siblings/ WVO briefnr 1069 (Willem van Hessen to Willem van Oranje, Marburg, 23 Mar 1563; KHAG origineel B12,6; WVO: "bij het origineel een exemplaar in cijferschrift en met ontcijfering"; no printed edition found by OX-WVH). Images on disk: siblings/01069_p1-p7.png (fetched by OX-WVH, manifest.json). Read NOTES.md's OX-WVH section and siblings/key_1069.tsv (10 pairs, p2 line 1 only).
Job: read the whole cipher exemplaar of 1069 with its interlinear decipherment (recovery at grade H).
1. Two blind passes (subagents) over p2-p4 (and any other page with cipher): per line, the cipher signs as written (describe each glyph with a stable ASCII name; build siblings/glyphs_1069.tsv with a crop reference per glyph) and the interlinear plaintext above them. Reconcile (tools/reconcile_passes.py or a documented aligner), 60% gate, settle from crops.
2. siblings/key_1069.tsv extended: sign -> letter (or word) with support counts, grade H where the decipherment stands above the sign.
3. siblings/ciphertext_1069.tsv and siblings/reading_1069.txt (the interlinear decipherment as written, German/Latin/French as it is); decode.json-style check if tools/decode_key.py can take the sibling (else a small check script in siblings/ that regenerates the reading from ciphertext + key and exits non-zero when stale).
4. Compare: is 1069's sign system the same as 174's nomenclator leaf (siblings/key_174_nomenclator.tsv)? One table of shared/different glyphs.
5. One line in NOTES.md for the orchestrator: whether WVO or any printed source gives 1069's text (check WVO's own record page once; no wider search -- that is a verifier's job).
Host: resources.huygens.knaw.nl only if a page image is missing; one request at a time. Files: ciphers/willem-van-hessen-1567/**, ROOM.md. Report what was found and where it was not found; no novelty wording.
