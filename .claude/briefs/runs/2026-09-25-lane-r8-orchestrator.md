LANE R8 ORCHESTRATOR (recovery and deep work; successor to LANE R7, whose handoff is STATUS.md "LANE R7 handoff (session_01UpWfpbLwYL1xmDG1vFyi6h)", closed 22:08 UTC 25 Sept 2026 with its queue spent). Opus. Cap $60 own usage; hand off at $60 or 400k context, keeping a "LANE R8 handoff" section in STATUS.md current from the first worker report and re-checking it after every push. Started 25 Sept 2026 by parent 7d (session_01744aLgcLnadR1XQckyHwcu). Window allowed; `rejected` anywhere stops everything.

First: `date -u`; `python3 tools/room.py --start`; read the R7 brief `.claude/briefs/runs/2026-09-25-lane-r7-orchestrator.md` and `.claude/briefs/runs/2026-09-25-lane-r6-common.md` (your worker rules), `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`, CLAUDE.md rules 1, 3, 5, 7, 10 and the intake gate, the LANE R7 and R6 handoffs, NEAR.md (the fr2933-salviati-1525 row is yours), ciphers/fr2933-salviati-1525/HYPOTHESES.md and NOTES.md top, ciphers/fr2933-salviati-1525/leafnotes/siblings.md, UPDATES.md; `python3 tools/room.py --digest "2026-09-25 22:00"`. ROOM claim "LANE R8: Salviati design change, BnF scout leads Francais 3034 item 68 and 5761 item 3".

Jobs, in order:
1. Salviati 1525, the NEAR row's named next step: a design change, not more restarts. R7-CM3 excluded a letter-per-type code+mark at the measured 5 pct transcription error (control 61.7/81.7/86.0 pct at 5 pct, gate 60 met; target -2.656 to -2.678 per symbol vs control -2.38 to -2.51; cross-seed agreement 1-20 pct, no Italian). Fable design worker (cap $15, 90 minutes, no subagents): read the 1525 Florentine key family (LESSONS.md, the R4/R6 notes on period keys, sources/ on Florentine nomenclators) and propose the next design family (syllabic or bigram tables with a nomenclator, or nulls plus a small code) with a matched control at N 2,820, K 205, 5 pct type noise, run through tools/family_run.py (add the family under tools/families/ with a test if it does not exist); control first, gate 60 on 2 of 3 seeds, target only above the gate; both numbers to HYPOTHESES.md and the NEAR row. If the control cannot reach the gate, the row records that as the reason and names the transcription step that would.
2. Scout leads from R7-SSIB (copy-free, Gallica): BnF Francais 3034 item 68, a cipher letter (Landriano, 1528) and Francais 5761 item 3, a key ("voiage d'Allemagne"), both digitised. One Sonnet worker each, cap $4, 40 minutes: folder, NOTES.md with sources, check-solved verdict per `.claude/briefs/check-solved.md` (Desjardins, the BnF finding aid, Tomokiyo, both solver repositories), `tools/intake_gate_check.py` exit 0, the leaf images once (tools/gallica_folio.py, tools/iiif_lines.py, manifest.json), sign count and design class. If the 5761 key's sign set matches the 3034 letter or any catalogued cipher of the same office (pools first, CLAUDE.md Pipeline 3), post "for the parent:" with the sign counts and stop; a key application is a separate brief after the intake gate.
3. Berthier 1812 stays parked (spec written, test 0 mid-field); Mercy 1648 waits on the owner (ASKS 59, 60, JSTOR rows 80-83). Do not reopen either without a new lead.
Rules: spawn every worker with create_session giving source_url https://github.com/NoAutopilot/cipher-lab and source_revision main, and a prompt that opens by naming its brief file and the rule that authorises it; wall-clock box in every brief and cost read by you with get_session at 15-minute intervals on any image job; one host per worker, Gallica 1-2 s apart; LEDGER row (ledger_check.py), ASSIGNMENTS, archive_session after every worker; NEAR.md row and status.json `near` updated whatever the result; a reading candidate goes to a fresh re-derivation session (spec and key only) before "for LANE V7: reading ready"; rule 10 wording only; no AskUserQuestion. Report to the parent only through ROOM lines with your context estimate.
