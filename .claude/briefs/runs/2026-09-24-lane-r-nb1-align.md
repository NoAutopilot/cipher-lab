ROLE: ALIGNER + KEY READER, Lodewijk van Nassau 1573-74 (LANE R worker R18). Model Opus, cap $8. No subagents.
Target: ciphers/lodewijk-van-nassau-1573-74. Read NOTES.md (sections from R9, R12, R13), images/inventory.tsv, passA_sib.tsv/passB_sib.tsv (blind passes agreed only 32.5 percent on 4613 p1 and 56.3 percent on 4615 p1), plaintext_4613.txt, plaintext_4615.txt (the contemporary decipherments on separate sheets).
1. Align, do not reconcile blindly (the RAH method, R8): the cipher is numeral groups interspersed with clear French words; the clear words anchor the plaintext sheet. Walk 4613 p1 and 4615 p1 line by line on the image (images/04613_p1.png, 04615_p1.png; crops in images/ if R12 cut them) with the plaintext beside you, and write ciphertext_sib.tsv (briefnr, line, idx, token) and pairs_sib.tsv (group -> plaintext unit). Use the two passes only as a check; log where you differ from both. Decide the unit (letter, syllable, word, name) from the data.
2. key.tsv (grade C: known plaintext from the contemporary decipherment); key_conflicts.tsv.
3. If R13's target passes exist for any target letter (passA.tsv/passB.tsv, recon/), apply the key to that letter's reconciled draft with decode.json + tools/decode_key.py (--check exits 0) and report C/M/U counts; do not transcribe the targets yourself.
4. NOTES.md "R18: key from 4613/4615": unit, key size, counts, what is left. ROOM line (`for LANE V: ciphers/lodewijk-van-nassau-1573-74 reading ready` only if a target was read). Check your cost after each step; stop at the cap with what is pushed. Do not classify novelty.
