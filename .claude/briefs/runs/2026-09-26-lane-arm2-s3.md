JOB ARM-S3: family S, third step -- the symbol-by-symbol match of ARM-S2, run exactly as ARM-S2 ran it, on four period shorthand systems not yet tried: Blanchard (Thomas? / Blanchard's "Complete System of Short Hand", about 1779-1786), Annet (Peter Annet, "Short-Hand Perfected", 1749 and later editions), Holdsworth & Aldridge ("Natural Short-Hand", 1766), and Lewis 1812 (James Henry Lewis, "The Ready Writer" / "An Historical Account of the Rise and Progress of Short Hand" system, 1812 or nearest edition). Sonnet (claude-sonnet-5). Stall alarm USD 6 and a 60-minute wall-clock box from your first `date -u`. Priced per subagent call (Usage 6: per pass, not per system): 4 candidate comparisons + 1 Taylor re-run (known-answer calibration, same prompt, so this session's scores are comparable with ARM-S2's) = 5 calls at about USD 0.9 each (4.5); specimen location and fetch on archive.org about 25 requests (USD 0.5); script and write-up (USD 0.5). Stop before starting a call that would cross 80 percent of either figure (USD 4.8 / 48 min); if a specimen cannot be found, that system is logged "no specimen located" and costs no call. At most 1 subagent at a time, Sonnet, each given the 12 exemplar crops and ONE specimen plate, never a full frame. Lane: LANE ARM2 orchestrator session_01CbcHcbkYKV5HRKwBW4w7aq. Written 26 Sept 2026 12:56 UTC (clock read) by LANE ARM2, after ARM-LIV and ARM-JEF found no pool candidate (ARM2 brief item 3).

INTAKE_GATE_OUTPUT (live, 26 Sept 2026 11:33 UTC, LANE ARM2): armstrong-madison-1808: open (line 1) -- edition/page or full-text-search citation found within 6 lines; exit 0.

Read first: `.claude/briefs/runs/2026-09-26-lane-arm-COMMON.md` (binding; read "LANE ARM" as "LANE ARM2"), `.claude/briefs/runs/2026-09-26-lane-arm-c2-s2-shorthand-symbols.md` (the method you copy: step 2's prompt, step 3's rule-3 reading), ciphers/armstrong-madison-1808/HYPOTHESES.md "ARM-S2 symbol match" section, NOTES.md "ARM-S2 pass", images/shorthand/{INVENTORY_reconciled.tsv, specimens/manifest.tsv, specimens/symbol_match.tsv, specimens/score_summary.py}. Claim in ROOM.

What is on file (paste-in): 12 reconciled shapes cover 189 of about 221.5 marks (85.3 pct); top shape 33.9 pct, then 13.1, 10.8, long tail; profile_check.py says the top stroke fits neither a letter alphabet nor a Zipf sign list, more like a connector. ARM-S2 scores (shape_score / freq_score): Taylor known negative 0.532/0.289; Pitman control 0.429/0.295; Byrom 0.585/0.218; Gurney 0.659/0.506 (artifact, running-prose specimen); Mavor 0.653/0.389; Macaulay 0.529/0.135; Weston 0.421/0.875 (below the control, a method artifact). All six EXCLUDED: none beat Taylor by a real margin with a frequency-consistent profile. Lead (M): the superscript tick is the baseline dash repositioned (a Mavor/Weston-style vowel-position convention).

Do:
1. Specimens, archive.org only (advancedsearch + metadata + one page image per system via the IIIF/BookReader page URL), 1.5 s apart: for each system find a scan with the alphabet plate (prefer an alphabet table over running prose -- ARM-S2's Gurney artifact came from a prose specimen). Save one plate per system to images/shorthand/specimens/ as JPEG, at most 1600 px on the long side and about 300 KB each (the folder is at 26 MB of its 30 MB limit); add a manifest.tsv row each (system, file, source identifier + leaf, host, fetched date, note). No specimen found after 5 searches for a system: log it and move on.
2. Calibration first (rule 3): the Taylor re-run with ARM-S2's identical prompt and score_summary.py. If this session's Taylor score differs from ARM-S2's 0.532/0.289 by more than 0.1 on either number, say so -- the per-call drift is then larger than any margin the method can show, and every candidate result this session is reported as "non-test at this drift", not an exclusion or an identification.
3. The four candidates, one call each, identical prompt; append rows to specimens/symbol_match.tsv and extend the summary table. A candidate is "identified" only if it beats this session's Taylor by a clear margin AND its letter assignments give a frequency-consistent profile; otherwise "excluded". Pitman's ARM-S2 numbers stay the control on file (not re-run).
4. Append "ARM-S3 symbol match" to HYPOTHESES.md (the calibration, the four scores beside Taylor and Pitman, the verdict per system, which systems had no specimen) and one paragraph to NOTES.md. If all are excluded, say in one line that ten named systems are now excluded and the marks are most plausibly a private symbol set, untestable at one letter's worth of marks (about 220). Do not read the marks as words; do not decode; rule 10 wording.
Files: ciphers/armstrong-madison-1808/{images/shorthand/specimens/**,HYPOTHESES.md (append only),NOTES.md}, ROOM.md. Hosts: archive.org at most 40 requests, 1.5 s apart, descriptive UA; no other host.
Done line (ROOM, starting `done: for LANE ARM2: ARM-S3`): specimens found per system (identifier + leaf) or not; Taylor calibration numbers vs ARM-S2's; the four scores; verdict per system; requests; no cost figure.
