JOB GOLD-D2: Debosnys, the base-level noise curve between the two points D1 measured (controls only; no target run, no images, no transcription). It prices the settlement pass before anyone buys it. Sonnet (claude-sonnet-5). Stop and push at $3 or 30 minutes, whichever first. NO subagents. Lane: LANE GOLD2 orchestrator session_013JXDgLDkW2y5Ldi2gWzTkY (Fable). Written by the cycle-2 consolidator GOLD-CONS2 (Fable, session_01SPixjGTp23T4YyxPvHwkWs), 25 Sept 2026 21:16 UTC; the orchestrator fills the gate line and the spawn time.

INTAKE_GATE_OUTPUT: (orchestrator pastes `python3 tools/intake_gate_check.py debosnys-1883` here, with the clock time)

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), the top block ("Summary, cycle 2": the gate you are producing numbers for is written there, before your numbers exist) and the section "GOLD-D1, noise-matched controls" of `ciphers/debosnys-1883/HYPOTHESES.md`, `python3 tools/family_run.py --help`, the docstring of `tools/families/homophonic.py` (`profile=target`, `noise=p`), and `python3 ciphers/debosnys-1883/scripts/base_mark_recount.py --check` (exit 0 means `ciphertext_draft_base.tsv` is fresh; a nonzero exit is a ROOM flag and a stop). Claim in ROOM (`tools/room.py`). Read the clock with `date -u` before any date.

What the last round learned, pasted:
- D1 (fr19, seeds 1-3, restarts 8, `--tokens space`, `profile=target`): at K 160 the control reads 0.440 clean, 0.325 at 10 pct type noise, 0.230 at 20 pct; at base level (K_base 128, `ciphertext_draft_base.tsv`) 0.859 clean (0.802-0.966) and **0.314 at 10 pct** (0.245-0.378); K 160 minus X 0.230. Branch (b) of the cycle-1 rule: base + mark has headroom clean, none at 10 pct.
- A settled two-pass transcription carries somewhere between 5 and 10 pct type noise (c4's settled agreement ceiling is about 91 pct, GOLD-4C), and D1's curve has no point between 0 and 0.10. The cycle-2 decision: the c1 settlement (about $4 of image work) is bought only if this job's numbers clear the gate below. Config runs took D1 about one minute each; the whole job is CPU on scratch copies.
- Procedure, exactly D1's: scratch copies (in your scratchpad, never in the tree) of `ciphertext_draft_base.tsv` with the `_` and `MULTI` rows removed; `--cipher` pointing at them; every run appends its own row to `ciphers/debosnys-1883/HYPOTHESES.md` under the tool's marker with `--label`. At most three anneals at once. If one seed of configuration A takes more than 6 minutes, cut every configuration to `--seeds 2` and say so.

Do:
1. Configurations, in this order, `--control-only --seeds 3 --restarts 8 --corpus tools/data/fr19 --tokens space --param profile=target`:
   A. base level, all four cryptograms (N 1251, K_base 128): `--param noise=0.025`, then `0.05`, then `0.075`. Labels "GOLD-D2 homophonic fr19 control, base level profile=target noise=0.05" and so on.
   B. c2 alone at base level (rows whose line id starts with c2; N 734, K_base 102 per D1's recount table -- report the realised K): `noise=0` and `noise=0.05`. This prices the alternative of settling the one cryptogram long enough to read on its own before the three short ones.
   C. only if under 15 minutes have been used after A and B: K 160 (`ciphertext_draft.tsv`, filtered) at `noise=0.05`, for the record.
2. Write "GOLD-D2, base-level noise curve" under the append-only part of `ciphers/debosnys-1883/HYPOTHESES.md`: one table with the full curve, D1's points (0 and 0.10) copied beside yours (0.025, 0.05, 0.075), mean and range per point; the c2-alone rows; then the gate, read exactly as the top block wrote it: **settlement licensed** if the base-level mean is at or above 0.5 at noise 0.05 AND at or above 0.4 at noise 0.075; otherwise **(c) for the letter families**. State which, with the two numbers; do not brief the next job. A "3-control-2" entry in `specs/debosnys-1883.json` `cheap_test_done` with the numbers and "target not run: transcription below gate"; one paragraph in NOTES.md ("GOLD-D2") pointing at the section.
3. Never run the target (the draft is a single pass at about 20 pct noise; a target number on it is the thing this job prices). No decoding, no images, no transcription, no edits to the draft tsv files.

Hosts: none. Requests per host in the done line: 0. Subagents: none.
Done line (ROOM, starting `done: for LANE GOLD: GOLD-D2`): per point "base level noise p: control mean (range)"; the c2-alone two numbers; the gate verdict in the top block's words (settlement licensed / (c) for the letter families) with the 0.05 and 0.075 numbers; files; no cost figure; nothing about what the text says. Rule 10: never solved, new, first, unpublished; NOTES.md status stays `open`.
