JOB bMAT3 (LANE B5, Sonnet worker). One line: matignon-mayenne-1586 Cipher-3 -- fetch BnF fr.15571 f.179 (and fr.15572 f.276) at native resolution from Gallica IIIF, settle whether the magenta per-sign letter labels on Tomokiyo's image are on the manuscript itself or a modern overlay, then transcribe sign-by-sign (sign id, label letter) and build the partial key the labels give.

Read first: `.claude/briefs/runs/2026-09-26-lane-b5-common.md` (and the 7b COMMON it names), CLAUDE.md rules 2, 4, 7, 8, 10 and the Gallica rows of the Access playbook, ciphers/matignon-mayenne-1586/NOTES.md (the Cipher-3 section) and cipher3_f179_gloss.tsv (bMAT's first look: labels are one letter per cipher sign, band 1 reads "s a c o mm o d e r ... a v i e s", which looks like French). Intake: `python3 tools/intake_gate_check.py matignon-mayenne-1586` exit 0 required (re-run and paste).

Cap and box: $6 or 60 minutes from your first `date -u`, whichever first. Units: (a) Gallica fetch, about $0.7; (b) f.179 sign-by-sign pass, about $2.5; (c) f.276 apply, about $1.5; margin one unit. Stop before starting a unit that would cross 80% of either figure. Gallica: IIIF manifest + image API only (`tools/gallica_folio.py` to find the canvas, then native region crops or `tools/iiif_lines.py`), one fetcher, >= 2 s apart, at most 15 requests, browser UA per the playbook; on altcha/SSL reset stop and log. You are this account's only Gallica fetcher in LANE B5.

Steps:
0. `date -u`; `python3 tools/room.py --start`; ROOM claim (tools/room.py) naming ciphers/matignon-mayenne-1586/ (images/, cipher3_*).
1. (a) Find fr.15571 f.179 and fr.15572 f.276 in Gallica (ark via the BnF catalogue record or Gallica SRU; the NOTES may already name them); fetch native crops to images/ with manifest.json. Compare with images/BnFfr15571f179.jpg: are the magenta labels on the Gallica image? Write the answer in NOTES.md (period annotation / modern overlay / cannot tell, and why). If modern overlay (Tomokiyo's working marks), the labels are a published modern key attempt, credited to Tomokiyo, not a period gloss.
2. (b) f.179: one pass writing cipher3_f179_signs.tsv (band, position, sign id from a small sign inventory you define with a crop per sign type in images/signs/, label letter or blank). Concatenate the labels per band into cipher3_f179_labels.txt; mark word breaks only where the manuscript shows them.
3. Key: sign id -> letter(s) from the labels, with counts and conflicts (the same sign labelled two ways). A reading of f.179 from the labels is graded H if the labels are a period gloss, M if they are a modern overlay (a published modern attempt, not our key).
4. (c) If budget allows: transcribe f.276's signs with the same inventory (one pass), apply the key (tools/decode_key.py via decode.json), control: the same key on f.276 with sign order shuffled (3 seeds) and a scrambled key (3 seeds); judge fr16 via tools/judge_plaintext.py on specs/matignon-mayenne-1586.json (a sibling judge block if needed), paste output, both numbers side by side.
5. Append cheap_test_done; push after each unit; done line with target and control; report what was found and where it was not found; do not classify novelty.
