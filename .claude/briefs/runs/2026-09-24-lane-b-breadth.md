LANE B ORCHESTRATOR (breadth; new lane from PROCESS-2026-09-24.md proposals 1 and 5). Opus. Cap $20 own usage; hand off at $20 or 400k context. Started 24 Sept 2026 21:19 UTC by the parent (cipher-lab-7b, session_01K7ZbE95o1pUW5gof8VA5PR) under the owner's standing decisions (BUDGETS.md 16:53 and 20:16: keep momentum, usage is not a concern; `rejected` on any session stops every lane).

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md, PROCESS-2026-09-24.md in full, UNSOLVED-SURVEY.md, specs/README.md and every specs/*.json, `python3 tools/judge_plaintext.py --help`; `python3 tools/room.py --digest "2026-09-24 21:00"`. ROOM claim "LANE B orchestrator: breadth, first cheap test per spec at $3". Workers get the common tail `.claude/briefs/runs/2026-09-24-lane-r4-common.md` (your session id in place of R4's) plus a job brief per spec written to `.claude/briefs/runs/2026-09-24-lane-b-<slug>.md` that says exactly: "Spec: specs/<slug>.json. Run `cheap_tests_in_order[0]` and its matched control, nothing else. Write both numbers into the spec's `cheap_test_done` (with date, method, cost). Judge any candidate plaintext with `tools/judge_plaintext.py` and paste its output. Cap $3. Report in five lines; do not run test 2; rule 10 wording; a negative's done line carries the target and control numbers side by side." Sonnet for every worker; two at a time; one host per worker and the good-citizen limits; the survey already ran the first test for Somerton, Lima and Koehler (see UNSOLVED-SURVEY.md), so start those specs at their next test only if the survey's row names it, else skip them and take the seven others (ranks 2, 4, 5 need one image fetch each first, at most 3 requests, saved under the spec's folder with a manifest). After each worker: LEDGER row (ledger_check.py), ASSIGNMENTS, archive, ROOM line, and the spec's status line in UNSOLVED-SURVEY.md updated. Promote nothing yourself: a spec whose first test moved it (judge PASS, found-solved, or a control-backed negative that names the next test) gets a ROOM line "for the parent: <slug> moved, next test <name>, est. $<n>". Handoff "LANE B handoff" at the top of STATUS.md's handoffs: a table of spec, test, target number, control number, verdict, cost; LEDGER row for yourself; ROOM done line; stop.
