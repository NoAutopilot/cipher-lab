JOB ARM-TR: independent two-pass transcription of the manuscript's code groups (NARA M34 roll 14 frames 0029-0032, fetched by ARM-IMG) and a diff against the Founders/Bourdeau transcription in ciphertext.txt, with the units digits as the priority. Sonnet (claude-sonnet-5). Stop and push at USD 10 or 80 minutes. Priced per pass, not per page (Usage 6, AX-COMP2): about 4 letter pages x 2 blind passes = 8 subagent calls at about USD 1 each on line crops, plus a reconcile unit (15 minutes) and a diff unit (10 minutes); stop before a call that would cross 80 percent of either figure. At most 2 subagents at once, Sonnet, each given ONLY line-crop paths of ONE page, never a full frame. Lane: LANE ARM orchestrator session_011gEKqukZAxE6FjtQCzsUQ8. Written 26 Sept 2026 by LANE ARM.

INTAKE_GATE_OUTPUT (live, 26 Sept 2026 06:46, TOMO-REPLY): ciphers/armstrong-madison-1808: open (line 1) -- edition/page or full-text-search citation found within 6 lines; EXIT:0.

Read first: `.claude/briefs/runs/2026-09-26-lane-arm-COMMON.md` (binding), ciphers/armstrong-madison-1808/{NOTES.md (ARM-IMG section),images/manifest.json,ciphertext.txt}, HYPOTHESES.md "ARM-DESIGN" (why the units digits matter), `python3 tools/iiif_lines.py --help`, `python3 tools/reconcile_passes.py --help`. No network needed (frames are on disk). If numpy/Pillow are missing, `pip install numpy pillow` once.

Do:
1. Look at each frame once at reduced size (downscale to about 1200 px wide with PIL for your own look, not for a subagent) and write images/layout.md: which frame holds which page of the 20 Feb letter, whether 0031 is a second copy of 0030 (Founders says a cleaner copy) or a different page, where the shorthand passages sit.
2. Crops, MANDATORY and pasted: `python3 tools/iiif_lines.py --image ciphers/armstrong-madison-1808/images/<frame>.jpg --out ciphers/armstrong-madison-1808/images/crops_<frame> --debug` (add --region for a two-page spread, one page at a time); check each debug overlay; paste the command lines and crop counts into NOTES.md before the first subagent call.
3. Two blind passes per page (pass A and pass B, separate subagent calls, neither sees ciphertext.txt nor the other pass): each crop transcribed as the groups in order, `*` for a shorthand/graphic mark (count marks), `?` for an unreadable digit; per crop a TSV line. Where 0030 and 0031 are the same page in two copies, pass A reads 0031 and pass B reads 0030 -- two witnesses, not one.
4. Reconcile with `tools/reconcile_passes.py passA.tsv passB.tsv --crops <crop dir>`; settle only disagreements.tsv by looking at the crop yourself (one crop at a time), and write ciphertext_ms.txt (same format as ciphertext.txt, header saying frames, passes, date, method).
5. Diff ciphertext_ms.txt against ciphertext.txt token by token (align with difflib on group sequences): every substitution, insertion, deletion, with the crop path; a table of units-digit changes; the units-digit distribution of ciphertext_ms.txt beside the transcription's (0:93 1:66 2:17 3:10 4:41 5:12 6:32 7:46 8:45 9:7). Never overwrite ciphertext.txt (Layout rule: as transcribed, never silently repaired).
6. Shorthand: crop each shorthand passage once to images/shorthand/ with an index (position in the group sequence) for a later family S job; do not try to read them.
Append "ARM-TR manuscript check" to HYPOTHESES.md (agreement per page between passes, diff counts, the digit table) and one paragraph in NOTES.md. Keep the folder under 30 MB.
Files: ciphers/armstrong-madison-1808/{images/**,ciphertext_ms.txt,tr/**,HYPOTHESES.md,NOTES.md}, ROOM.md.
Done line (ROOM, starting `done: for LANE ARM: ARM-TR`): pages and passes, pass agreement per page, diff counts vs ciphertext.txt (subs/ins/del), units-digit distribution ms vs transcription, shorthand crops count; no cost figure.
