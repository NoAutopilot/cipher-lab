PARENT WORKER SEND-QUEUE-TOOL (Sonnet, cap $6 of usage, box 45 minutes, no network except git, no subagents). Parent 7i (session_01U1NiMakQ8PPySH8WixXsmc, "LIVE parent 7i · Orchestrator 10"). Owner's decision, 26 Sept 2026 about 18:56 UTC in chat: the checked outreach drafts and reproduction-quote requests are sent by the owner's own ChatGPT runner (browser, the owner's identity), which reads a queue on main -- the same loop shape as tools/local_queue_runner_prompt.md. Written 26 Sept 2026 at the 19:00 UTC clock read. CLAUDE.md Usage 8a: a loop gets its register, its runner prompt, its landing gate and its SYSTEM.md rows in the same commit.

Build, in this order:
1. `SEND-QUEUE.tsv` at the root, tab-separated, header `id	kind	target	draft	to	subject	checked	status	result`: id S1, S2 ...; kind `email` or `form`; target = the ciphers/ folder (or `outreach` for a project-level message); draft = the outreach/mailbox/<slug>.json path; to = the address or `FORM: <url>`; checked = the UTC time of the gate-7 `checked:` line in the matching outreach/<slug>.md; status `queued` | `sent <date time>` | `blocked` | `bounced`; result one line. Seed ONE row: S1, form, ciphers/destaing-gerard-1779, outreach/mailbox/clements-destaing-1779.json, FORM: https://clements.umich.edu/research/contact-us, its subject, checked 2026-09-26 17:50 (OUT-CHECK-V), queued. Nothing else is queued yet: the parent queues a row only after a `checked:` line exists.
2. `tools/send_queue_check.py FILE [--row ID]` with offline test `tools/tests/test_send_queue_check.py`: exits nonzero, naming the row and the missing rung, when a `queued` row points at a json that does not exist, whose md header has no `checked:` line at or after the row's `checked` time, whose body lacks the `[SIGN-OFF]` placeholder or the disclosure sentence's substance in its first paragraph (outreach/README.md rule 1), or that has no CONTRIBUTIONS.md row naming its slug (Outreach gate 5); a `form` row must also carry `form_fields` in the json. Exit 0 otherwise. The parent runs it before pushing any new row; the landing worker runs it before landing a receipt.
3. `tools/send_queue_runner_prompt.md`, in the shape of tools/local_queue_runner_prompt.md (a "Fetched live" note; "Our side of the loop" for us; a "Paste this as the scheduled task's instruction" section that is SELF-CONTAINED: every path written as a full https://github.com/NoAutopilot/cipher-lab/blob/main/... or raw.githubusercontent.com URL, no credential, no private address, no owner name). The pasted section tells the runner: read SEND-QUEUE.tsv on main; take rows with status `queued` whose `checked` cell is filled, up to five per run; for each, open the draft json; for `email`: in the project mailbox cipherlab.research@gmail.com (the runner's own browser session, already signed in), compose a new message to `to` with the json `subject` and `body`, replace the line `[SIGN-OFF]` with the sign-off given in the task's own instruction (never taken from the repository), send; for `form`: open the form URL, fill the fields from the json `form_fields` in order, with the name from the task's own instruction where a field says `[OWNER NAME]` and cipherlab.research@gmail.com for the email field, submit once; never bypass a captcha or a block page (record `blocked: <what it saw>` instead); never change the body text; then create branch `send-queue/<id>` from main, add exactly one file `<target>/outreach-receipts/<id>-<UTC date>.md` with the lines `row: <id>`, `kind:`, `date:` (UTC, to the minute), `to:`, `subject:`, `result: sent | blocked: <why>`, `reference: <any confirmation number or page text>`, then the exact text as sent, and open a pull request titled exactly `[SENT-<id>] <target>` with the one-line body "Row <id>. Sent from the owner's browser; the repository logs it."; never edit SEND-QUEUE.tsv or any other file, never commit to main, never merge; finish with one line per row.
4. "Our side of the loop" paragraph (in the runner prompt file) and `.claude/briefs/runs/2026-09-26-parent-pr-land-3.md` step: a PR-LAND worker matches `[SENT-<id>]` pull requests to rows, runs `tools/send_queue_check.py`, then sets the row `sent <date time>` (from the receipt's `date:`), and writes the send in the three places RETRO-2026-09-26j.md item 4 specifies (read that item and use its exact shape for the CONTRIBUTIONS.md status cell, the outreach md `status:` line and the ASKS.md status cell; if item 4 also proposes a tool or a desk_check case, implement it here rather than leaving it to RETRO-APPLY-V, and say so in your done line), closes the PR without merging with the footer "---" then "_Generated by [Claude Code](https://claude.ai/code)_".
5. Wiring: CLAUDE.md Operating model "Loops that run outside this repository" gets a fourth bullet (Send runner: SEND-QUEUE.tsv, tools/send_queue_runner_prompt.md, [SENT-] PRs, send_queue_check gate; only a checked draft is ever queued; the owner's identity lives in the runner's own task instruction, never here); CLAUDE.md Usage 8a precedent clause for send_queue_check.py; SYSTEM.md rows: section 2 (the loop), 3a (the gate), 4 (the register), and the runner prompt; run `python3 tools/system_map_check.py` (must pass); one UPDATES.md row per instituted piece (`By` = "SEND-QUEUE-TOOL for parent 7i"); `python3 tools/file_shrink_guard.py` on every touched file before the push (its output in your done line).
Rules: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker SEND-QUEUE-TOOL (Sonnet, <session id>)`; read tools/local_queue_runner_prompt.md, tools/second_opinion_runner_prompt.md, tools/lq_answer_check.py and its test, RETRO-2026-09-26j.md item 4, outreach/README.md before writing; `git diff --stat` before every commit; commit by explicit path; push only through `python3 tools/room.py --push <paths>`. Run the test file directly (no pytest). Never write the owner's name or any credential; never send anything yourself. Finish: ROOM done line "done: for parent 7i: SEND-QUEUE-TOOL -- SEND-QUEUE.tsv (1 row), send_queue_check.py tests n/n, runner prompt, PR-LAND step, SYSTEM.md rows, system_map_check <ok/fail>, item 4 <implemented here / left to RETRO-APPLY-V: why>"; one-paragraph reply; stop. Never call AskUserQuestion.
