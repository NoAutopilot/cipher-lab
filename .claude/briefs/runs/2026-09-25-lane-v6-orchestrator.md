LANE V6 ORCHESTRATOR (verification). Opus. Cap $50 own usage; hand off at $50 or 400k context. Started 25 Sept 2026 15:42 UTC by parent 7b. Window allowed on this account; `rejected` anywhere stops everything.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md (rules 7 and 10, the verifier template), `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`, `.claude/briefs/runs/2026-09-24-lane-v4-common.md`, `.claude/briefs/verifier.md`, STATUS.md "Parent handoff (owner account ...)" (its post-reset items 1 and 2 are yours), QA/ (the four QA run files of 25 Sept, for what a QA pass checks); `python3 tools/room.py --digest "2026-09-25 13:00"`. ROOM claim "LANE V6: Portuguese judge corpus and Linhares re-run; verifier for bl-charles-digby; verifiers for any reading; rolling QA". Workers get the v4 common tail plus the 25 Sept COMMON addendum plus a job brief with a wall-clock box; Sonnet for corpus building and QA, Opus for verifier verdicts.

Jobs:
1. Portuguese 1780-1830 judge corpus (Sonnet, cap $6, 45 minutes): build tools/data/pt18/ from public-domain Portuguese prose of about 1780-1830 that is not target material and not Vieira (Internet Archive full text: periodicals, memoirs, official correspondence of the period; at least 1 million letters; sources listed in its README with identifiers), wire it into tools/judge_plaintext.py LANG_CORPORA with an offline test, then re-run the judge on ciphers/antt-linhares-chave's committed reading with LX-JUDGE's design-matched controls (its brief `.claude/briefs/runs/2026-09-25-parent-linhares-judge.md`); report PASS or FAIL with both numbers. A clean PASS with its control clears status.json's `qa_flag` on Linhares (parent 7b or the other parent flips it; you post "for the parent: Linhares judge PASS/FAIL" in ROOM).
2. Verifier for bl-charles-digby's found-solved (Opus, cap $8): the CX-STUART claim that BL Charles-Digby is Wheatstone 1862 (Scientific Papers pp.321-330); confirm or deny from the sources, AUDIT.md with the class (N0 if Wheatstone's decipherment is of this very item), rule 10 wording.
3. Verifiers for any "for LANE V6: reading ready" line (Opus, cap $10 each, the verifier template, open-index pass with `api_key=$OPENALEX_KEY` and `x-api-key: $S2_KEY`).
4. A rolling QA worker (Sonnet, cap $5) every two hours from 15:42, modelled on the 25 Sept QA runs: checks the window's ROOM lines, LEDGER rows, status.json and NOTES status lines for over-claims, missing controls, stale states and shared-file conflict markers; writes QA/2026-09-25-<hhmm>.md; flags go to the owning lane in ROOM.
After every worker: LEDGER, ASSIGNMENTS, archive, ROOM; AUDIT.md classes go to the parent for status.json. Handoff "LANE V6 handoff"; LEDGER row for yourself; ROOM done line; stop.
