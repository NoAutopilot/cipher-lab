LANE ARM3 ORCHESTRATOR (armstrong-madison-1808; successor to LANE ARM2, whose handoff is STATUS.md "LANE ARM2 handoff", idle-standing on ASKS 66 since 13:45 UTC 26 Sept 2026 -- that ask was answered by the owner at 18:01 UTC: the question went to the Papers of James Madison editors, reply pending). Opus. Cap $40 own usage; hand off at $40 or 300k context by writing "LANE ARM3 handoff" in STATUS.md. Written 26 Sept 2026 at the 18:42 UTC clock read by parent 7i (session_01U1NiMakQ8PPySH8WixXsmc, "LIVE parent 7i · Orchestrator 10"; report through ROOM.md "for the parent:" lines). Window: allowed; `rejected` on any session stops every lane. You are at lineage depth 1 and can create workers and check-ins.

Why now, in the owner's words (18:35 UTC, chat): "if you're working on Tomokiyo's stuff, make sure we keep pushing hard on that, evaluate if we could do something different, what would it be, then try it, get scrappy" -- the AI-mathematics pattern (PROCESS-2026-09-24.md): many cheap, genuinely different attempts, each with its control, rather than a fourth pass at the same knob (CLAUDE.md rule 3, the "unchanged approach" paragraph). Tomokiyo recommended this letter to us on 26 Sept.

Everything in `.claude/briefs/runs/2026-09-26-lane-arm2-orchestrator.md` and, through it, the ARM lane's common rules applies unchanged with ARM3 for ARM2 (LIVE/ARCHIVED titles; a lane cannot archive itself; ledger with get_session cost, ASSIGNMENTS row, ROOM line after every worker; ledger_check before the self-ledger row; no typed clock digits in a took-over or handoff line; intake_gate_check pasted before any deep-work brief; per-unit pricing from README "Per-unit pricing precedents", unit count and per-pass estimate in every brief; a calibration that misses its tolerance ends the box before any candidate is scored; print_check before any "reading ready" line; `git diff --stat` before every commit and push only through `python3 tools/room.py --push`). First: `date -u`; `python3 tools/room.py --start`; read ciphers/armstrong-madison-1808/HYPOTHESES.md "Summary, cycle 2" and "Summary, cycle 1" in full and skim every family section's verdict line; NOTES.md; the ARM and ARM2 handoffs in STATUS.md; NEXT-STEPS.tsv if it exists (the owner account's NEXT-STEPS worker is building it tonight); `python3 tools/room.py --digest "2026-09-26 17:30"`. ROOM claim "LANE ARM3 orchestrator: armstrong-madison-1808, cycle 3 -- different instruments". Intake: `python3 tools/intake_gate_check.py ciphers/armstrong-madison-1808`, paste the output.

What is already excluded or non-test (do not repeat at the same knob): family A office codes (THE=972 Bourdeau part-key, WE028 Tomokiyo full) do not fit; family C nomenclator anneal: control below gate at N=369; family E siblings: docket 0645's located items, Livingston 1807-09, Armstrong-Jefferson 1807-09 all clear or office code; family S: ten period shorthand systems, symbol-by-symbol, none identified, the last four non-test at a 0.211 calibration drift. The cycle-1 consolidator's verdict: under-determined from its own text (369 groups, over a third singletons).

Cycle 3, three genuinely different instruments, cheapest first; each job is a Sonnet worker unless named, each with a matched control that CAN differ from the target (rule 3), each writing its section to HYPOTHESES.md and its numbers to the family table:
Job 1 (recovery, the "other correspondent", Sonnet, cap $6, box 45): Madison wrote to Jefferson on 15 May 1808 that the cipher "must be one concerted with another correspondent". Nobody has yet tested the men Armstrong actually shared a channel with in Paris 1806-08. Candidates, in order: James Bowdoin (joint commissioner with Armstrong for the Spanish negotiation 1806-08; Bowdoin-Temple Papers, Massachusetts Historical Society, and his letters to Madison and Jefferson on Founders Online), David Bailie Warden (Armstrong's secretary and consul; Warden Papers, Library of Congress and Maryland Center for History and Culture), Fulwar Skipwith (commercial agent, Paris), Daniel Parker, Joel Barlow, John Mason. Method, scripts first (Usage 2): Founders Online's search API for every letter FROM each candidate TO Madison or Jefferson 1806-1809 whose text or editorial note says "cipher", "code", "in cypher" or shows numeral runs; for each hit, download the text, extract its number groups and run the same signature screen ARM-POOL used (group-length distribution, range, top-frequency groups) against the target -- a match is any letter sharing 3 or more of the target's 20 most frequent groups, with a shuffled-groups control for the chance rate; record every letter screened with its verdict. Also: Weber, "United States Diplomatic Codes and Ciphers 1775-1938" is the catalogue the WE027/WE028 numbers come from -- query archive.org's full-text API (be-api, no login) for "Armstrong" and "1808" inside any Weber item and record what code 27 (WE027, "unpublished" in our notes) is described as and whose it was; if Weber ties a code to Armstrong or Bowdoin, that is the lead of the cycle.
Job 2 (design, book or dictionary code, Sonnet, cap $6, box 45): ARM-DESIGN found code forms 900-1800, English content words, no usable alphabetical order, OOV words probably in the shorthand -- the shape of a page-and-word dictionary code (the Linhares 1809 pocket-dictionary key is the repository's worked example, ciphers/antt-linhares-chave). Test with tools/families or a small script, control first: fit the groups to a (page, entry) or (page, column, line) scheme for the dictionaries a US diplomat carried in 1806-08 (Entick's Spelling Dictionary editions, Johnson abridged, Perry, Sheridan; list what archive.org holds in full text), score the implied English word sequence with the en18 judge AGAINST the shuffled-target decode scored the same way (rule 3: a PASS on the shuffled decode voids the judge for this family); report per-dictionary numbers. Negative with a passing control is a real result here; note that a dictionary code with 900-1800 forms may point into a specific printed page range, which itself narrows the book.
Job 3 (cryptanalysis with a new instrument, only after 1 and 2 report; Fable design then Sonnet runs, cap $12 total): the model-in-the-loop crib loop of 24 Sept (solvEX: +13.5 to +15 points at N=244 on a simple design, +4.9 on code+mark at N=720) has never been run on this letter. Build the matched control first: a synthetic one-part code of the target's N, K and singleton share in 1800s diplomatic English; measure the loop's gain over blind on the control (rule 3's headroom check: the control's blind baseline must be well under ceiling); run the target only if the control shows a gain above its own noise band. Also try, as the cheap variant, treating the shorthand runs as the spelled-out proper names and testing whether the groups adjacent to them behave like a closed set (titles, prepositions) -- a structural test with a shuffled-position control.
Reserve (if jobs 1-3 leave cap and the numbers make it decisive): a second full-page transcription pass of the shorthand runs at line-crop scale (tools/iiif_lines.py --image, one page per subagent call, two passes per page priced per pass) to lower the mark-inventory error before any further family S work.
Rules: a reading candidate needs a rule-7 re-derivation worker and print_check before a "reading ready" line; LANE VO1 (owner account) verifies this lane's readings (ROOM 17:52 division). Update NEAR.md only if a control-backed margin appears (rule 5: then `partial` with a row, never `closed-negative`). Never write the owner's name; never call AskUserQuestion; never write solved, new, first or unpublished. Check-ins by send_later, 30-45 minutes while a worker runs; close the lane when the three jobs are done or the cap is reached: handoff prose in STATUS.md, ledger_check, self-ledger from get_session, retitle ARCHIVED, ROOM done line for the parent with the one sentence the owner should read.
