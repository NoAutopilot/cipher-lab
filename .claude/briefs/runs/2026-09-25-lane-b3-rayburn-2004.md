LANE B3 breadth worker bRAY: rayburn-2004 first test. Sonnet (claude-sonnet-5). Cap $3 or 30 minutes, whichever first. No subagents.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (read it first; it governs; its INTAKE STEP applies) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Spec: specs/rayburn-2004.json. Intake step first (breadth.md): its network calls are allowed -- one shallow clone of each solver repository (grep, delete), one OpenAlex and one Semantic Scholar query with the keys from the environment (never print them). Create ciphers/<slug>/NOTES.md in the gate format if absent and paste the gate output.
Then `cheap_tests_in_order[0]` only: fetch the image (scienceblogs.de only -- you are its one holder; at most 4 requests, >=2 s apart (sleep 2 between requests; bSSR did not space its two), browser UA per the common file; image to ciphers/rayburn-2004/images/ with manifest.json) and transcribe blind, recording each symbol identity, position and its struck-through or underlined state separately, as the spec says, one pass, no subagent. Write ciphertext.txt (say single pass, rule 2), the spec's ciphertext and cheap_test_done (N, K, separator positions; IC against an English control at the same N and a uniform-random control at the same K, both numbers side by side). Do not run test 2. Done line: N/K/IC target vs controls; say 'scienceblogs.de free' in it so the next holder can start.
