bMALS -- malsburg-hessen-1636 folder shrink (LANE B10 job 1). Sonnet. Cap USD 4, box 30 minutes from your first `date -u`. No transcription, no image reading by a model.

Read first: `.claude/briefs/runs/2026-09-26-lane-b10-common.md` (governs), CLAUDE.md "Access playbook" last paragraph before the hosts table (the AX2-SHRINK method), ciphers/lodewijk-van-nassau-1573-74/NOTES.md section "AX2-SHRINK: images relocated" and that folder's `images_manifest_full.tsv` and `regen_images.sh` (the worked example -- copy the shape, not the WVO specifics).

Intake gate (orchestrator ran it 10:47 UTC): `malsburg-hessen-1636: partial (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

Job: ciphers/malsburg-hessen-1636 is 33 MB (images/ 23 MB, crops/ 7.4 MB), over the 30 MB line. Shrink it to at most 22 MB (headroom: job 2 of this lane may add key images under keys/, do not touch keys/ if it appears).
1. `images_manifest_full.tsv` in the target folder: one row per file under images/ and crops/ (path, source URL from images/manifest.json -- HCPortal `api.hcportal.eu/media/...` -- sha1, bytes, dimensions, and `cited_by`: every NOTES.md / HYPOTHESES.md / pool/POOL.md / recon_*/ / transcription/ / *.py line that names the file or its folio stem; grep, do not guess).
2. `regen_images.sh`: re-derives every full page from its HCPortal media URL (browser User-Agent and `Origin: https://crypto.hcportal.eu`, as NOTES.md item 4; 1.5 s between requests) and every recorded crop from the page with the exact command that cut it (tools/iiif_lines.py --image ... or the recorded region); crops whose cut command is not recorded get a row saying so and are KEPT.
3. Byte-identical regen test on a sample BEFORE deleting anything: at least 2 full pages and 3 crops, into a scratch dir, sha1 compared to the committed files. Paste the result into NOTES.md. If a page does not regenerate byte-identically, do not delete that page; say so.
4. Delete uncited full pages; convert cited full pages to JPEG at the same pixel dimensions (quality 85) only if that keeps the cited use valid (the crops are cut from the originals -- the regen script must fetch the original, not use the JPEG). Crops stay as they are unless uncited.
5. `du -sh` before and after in NOTES.md section "bMALS: folder shrink (26 Sept 2026)"; update images/manifest.json entries for anything deleted or converted (a `local: deleted, regen via regen_images.sh` field).
Host: api.hcportal.eu only, at most 10 requests, 1.5 s apart; report the count. Commit by explicit path, `python3 tools/room.py --push <paths>`. ROOM claim first, done line at the end (at most 600 chars: before/after MB, sample regen result). Final reply at most five lines.
