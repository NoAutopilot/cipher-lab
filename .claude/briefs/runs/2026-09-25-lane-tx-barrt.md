TX-BARRT (Sonnet, stall alarm $6, at most 2 Sonnet subagents). Parent: LANE TX orchestrator, session_01UDxtM9Xv2dnPfoo5z9T6wA. Read .claude/briefs/runs/2026-09-25-lane-tx-COMMON.md, .claude/briefs/transcription.md, .claude/briefs/solver.md, and ciphers/thurloe-barriere-1654/NOTES.md in full (verdict open, 25 Sept 2026, TX-BARR). LESSONS.md section 2 on sibling/partial-gloss recoveries.
Target: Barriere to Conde, London 20 Nov 1654 N.S., Birch vol.2 pp.721-722 (images/leaf0729.jpg, leaf0730.jpg on disk). ~320 numerals embedded in French prose, with sparse interlinear gloss phrases printed under some runs.
1. Transcription: two blind passes over the two leaves (yours plus one Sonnet subagent that sees only the images): every numeral run and the gloss printed under it, in reading order, with the clear French around each run. ciphertext.tsv (run id, tokens, gloss-as-printed, left/right clear context). Reconcile disagreements from the image (tools/reconcile_passes.py if the format fits).
2. Alignment: for each glossed run, align tokens to gloss letters/syllables (tools/interlinear_align.py if it fits; ciphers/thurloe-printed s.18 is the method). Say what the unit is (one number = one letter, syllable or word) from the aligned runs, and build key_gloss.tsv (code, value, grade C, source run).
3. Spec: specs/thurloe-barriere-1654.json on the pattern of specs/antt-linhares-chave.json (ciphertext as transcribed with source and date, alphabet, constraints: the gloss pairs, French 1654, the clear context; cheap tests in order; a judge block).
4. First cheap test (breadth rule, one only): apply key_gloss.tsv to every run; report coverage (tokens read / total) and whether the newly covered unglossed runs give French sense with their clear context. With a matched control: a synthetic French 1654-style letter of the same N, the same number range and homophone count as your alignment implies, with the same fraction of gloss revealed; report control coverage and sense rate side by side (rule 3). Write both numbers into the spec's cheap_test_done. Do not hill-climb beyond that.
Grade every read token (C from gloss, S only with the control, M, I). A script decode.py --check regenerates any reading (rule 7), and a fresh-instance subagent re-derives it from key_gloss.tsv and ciphertext.tsv. Report what was read and where it was not found; do not classify novelty.
Files you may touch: ciphers/thurloe-barriere-1654/**, specs/thurloe-barriere-1654.json, ROOM.md. Finish per COMMON.
