ROLE: SOLVER, Rumpf to Heinsius 1716 printed cipher (LANE R2 worker H1). Model Opus, cap $7. No subagents. Host: resources.huygens.knaw.nl retroboeken only if the page is not already on disk (<=10 requests, >=1.5 s; LANE N2's harvest has finished there -- post a ROOM host line first).
Target: ciphers/rumpf-vandebie-heinsius-1716-19 (claim it). Read NOTES.md. Letter 142 (Rumpf, Rotterdam, 17 Nov 1716; Veenendaal, Briefwisseling Heinsius 18, p.95) prints ~180 numeral tokens in French prose, superscripts seen (70³); footnote: not solved by d'Alonne. Letter 309 (to d'Alonne, 1718) per NOTES may carry more.
1. Transcribe the printed ciphertext from the page image (and the OCR text as a cross-check; print is easy -- one careful reading, superscripts as separate marks) with its clear context: ciphertext_142.tsv. If 309's page prints ciphertext too, fetch it once and do the same.
2. Design analysis: token count, distinct values, range, repeats, superscript role; is it homophonic letters, syllabary or code words? Say what the clear context predicts.
3. Matched control first (rule 3): French of 1716 (clear prose from this same letter), same length, same symbol count and homophone profile, enciphered the same way; run tools/homophonic_anneal.py (see its test) with a French LM (tools/data/ if present, else build one from French text on disk and say which); report the control's share read. Then the target, several restarts. A reading only if the control reads and the target gives connected French; grade S/M; key.tsv + decode.json, decode_key.py --check 0. Otherwise the negative with both numbers.
4. NOTES 'H1: cryptanalysis'. ROOM `for LANE V2: ... reading ready` or a done line with the negative. Stop at the cap. Do not classify novelty.
