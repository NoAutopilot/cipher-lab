LANE AX2 AX2-BRO4 -- antt-msliv0638-brochado-1712: NEAR.md step for letter 134 -- read the 69 undecidable thin-code occurrences in the Deciffrada appendix by chunk alignment, image-check the rest, re-score (Sonnet, wall-clock box 75 minutes). Parent: LANE AX2 orchestrator session_016sbTkVT8HGNJ6JGjJNmCuM. Read and follow .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md (read "LANE AX2" for "LANE AX") and the common tail of .claude/briefs/README.md (both override anything below they conflict with), CLAUDE.md rules 3 (all paragraphs, especially the per-class and known-answer ones), 4, 7, 10 and Usage 6/8. First: `date -u`; `python3 tools/room.py --start`; last 30 lines of ROOM.md; `python3 tools/intake_gate_check.py antt-msliv0638-brochado-1712` pasted into NOTES.md. Claim with tools/room.py before other work; never AskUserQuestion; no dollar figures for yourself.

Read: NEAR.md row antt-msliv0638-brochado-1712; NOTES.md sections ZX-BRO2 and AX-BRO3 in full; scripts/02_anchor.py, 03_align_pairs.py, 15_judge_bucket.py, 16_thin_attest.py; thin_codes_attest.tsv; conflicts.tsv; `python3 tools/interlinear_align.py --help`.

Why: AX-BRO3 resolved only 11 of 80 new thin-code occurrences because its span check needs a code span's token count to equal its letter count, and 34 of 38 appendix entries drift. In a dictionary code, drift is expected (a code can stand for a syllable or a word). tools/interlinear_align.py was built for this: dynamic-programming hard-EM that lets each cipher group take a chunk of the clear span.
Units, in order (stop before a unit that would cross 80% of the box; push after each):
1. Alignment (about 25 min). Build (cipher groups, clear span) pairs per appendix entry from ciphertext_appendix.tsv + plaintext_appendix.tsv (the existing anchors from 02_anchor.py bound the spans), run tools/interlinear_align.py (add an option to the tool, with an offline test, if the entry format needs one; never a private DP copy). Known-answer control FIRST, gate written into NOTES.md before running: mask each key.tsv code with >= 5 observations one at a time (value hidden from the aligner, and absent from its prior), align, and count how often the aligner's value for the masked code equals key.tsv's; report per class (single letter / syllable / word codes) and against a shuffled-gloss baseline (gloss spans permuted across entries). Gate: per-class recovery >= 0.80 on single-letter codes AND >= 30 points above the shuffled-gloss baseline for that class. Only if the gate passes: read the aligner's value at each of the 69 cannot-tell occurrences of x, z, d, f, 16, 9 (thin_codes_attest.tsv), each as agree / differ / still undecidable.
2. Images (about 25 min, only for occurrences still undecidable after unit 1, or all 69 if the gate failed). Crops only: cut from the appendix leaf images on disk (images/full_*m0279..m0296* if present; else DigitArq per tools/digitarq_fetch.py, >= 3 s apart, <= 20 requests) with `tools/iiif_lines.py --image <leaf> --out <scratchpad> --debug` (mandatory, command pasted into NOTES.md), under 2500 px; settle by eye or one blind subagent per entry's crops; never a full leaf to a subagent.
3. Re-score. A key.tsv change needs >= 2 independent new occurrences agreeing on a different value (AX-BRO3's rule); write any change with grade and the occurrences, then `scripts/15_judge_bucket.py` for letter 134 under pt17 and pt18 (percentiles before and after) and tools/decode_key.py --check. Update thin_codes_attest.tsv in place (new column for this pass's verdict).
Do not touch NEAR.md, status.json or AUDIT.md (the orchestrator and parent do); write the proposed NEAR.md row text in your NOTES.md section. No reading is claimed; do not classify novelty.
Section "## AX2-BRO4: thin codes by chunk alignment (26 Sept 2026, LANE AX2)". ROOM done line: "for LANE AX2: brochado known-answer control <per class> vs shuffled <..> (gate <met/not>); 69 occurrences -> <agree/differ/undecidable>; key changes <n>; letter 134 percentile pt17 <a -> b>, pt18 <c -> d>".
Files: ciphers/antt-msliv0638-brochado-1712/{scripts/17_*.py,scripts/18_*.py,thin_codes_attest.tsv,key.tsv (only under the >= 2 rule),judge_bucket.tsv,reading_*,align/**,NOTES.md own section}; tools/interlinear_align.py and tools/tests/test_interlinear_align.py (option only, default output unchanged). Cited crops as JPEG under align/crops/ (under 3 MB). Hosts: digitarq.arquivos.pt <= 20 (only if the leaves are not on disk).
