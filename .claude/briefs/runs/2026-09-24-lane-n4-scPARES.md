ROLE: SCOUT scPARES (LANE N4, decided by the parent 19:27), Spanish and Portuguese state archives. Sonnet. Cap $6.
Step 1, reachability (stop early if dead): `curl -sS -o /dev/null -w "%{http_code}"` once each to pares.cultura.gob.es, pares.mcu.es (connection-reset on 19 Sept) and digitarq.arquivos.pt; if curl fails, one `NODE_PATH=$(npm root -g) node tools/browser_fetch.js URL out.html --shot out.png` per host. A host that answers neither is logged as a tested negative and dropped. If all three are dead, write that and stop (a short negative is a result).
Step 2: search cifra / cifrado / en cifra / descifrado / chiffre / clave / contracifra (and Portuguese cifra / cifrada / decifrada) in the Estado and Guerra series (AGS Estado, AHN Estado; ANTT Corpo Cronológico, Gavetas, Manuscritos da Livraria) restricted to described units with online images. For each candidate open the viewer and confirm the leaf is readable at full size and carries cipher; record the image URL.
Drop: anything already in QUEUE.md, CATALOG.md, LANDSCAPE.md or the solver repos (grep sources/solver-diffs/ and a shallow clone each of dbourdeau/cyphersolver and aaymeloglu/unsolved-ciphers; CS2-31 Estrada 1503 is Bourdeau's), and anything with a decipherment on the same unit (Luzerne rule).
Output only in: QUEUE.md section "## PARES / DigitArq cipher letters (LANE N4 scPARES, <date -u>)" with rows PP-01.. (archive, signatura, date, sender -> recipient, image URL tested, copy-free/copy-order, kind), plus sources/solver-diffs/2026-09-24-pares-digitarq.tsv. No ciphers/ folders, no nomination lines (scouts never do); list the copy-free rows in your done line for check-solved.
Hosts: pares.cultura.gob.es / pares.mcu.es (<=60, >=3 s), digitarq.arquivos.pt (<=60, >=3 s), github.com (two shallow clones), WebSearch (<=15). Browser UA only where plain curl is refused.
