BOARD WORKER: hall of fame (Sonnet, cap $6). Owner's ask, 24 Sept 2026 18:48 UTC: "add a hall of fame to our dashboard, where we'll keep public citations of my work, like with Tomokiyo (and hopefully others); for grants and for me to pat my own back."

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md (rules 8, 9, 10), tools/build_dashboard.py in full, status.json's keys, CONTRIBUTIONS.md, ciphers/bowes-walsingham-1583/NOTES.md section 'Specialist reply'. ROOM claim line "board hall of fame: files CITATIONS.md, tools/build_dashboard.py, status.json, docs/index.html".

Deliverables:
1. `CITATIONS.md`: a table, one row per public citation of this project's work by someone outside it. Columns: date (absolute), who (person or institution, as they sign publicly), where (the public page or issue, URL), what of ours they cite (target folder, linked), how (credit, link, correction adopted, co-authorship, reply), quote (their words, at most 30 words, verbatim), evidence (our snapshot path under sources/ or the commit that recorded it). Header lines say what counts (a public page or a public post by someone outside the repository that names or links this work; a private email reply is logged in CONTRIBUTIONS.md, not here, until it becomes public) and that the owner is never named (rule 9: the repository is credited, not a person). First row: S. Tomokiyo, Cryptiana, unsolved.htm updated 24 Sept 2026 with a credit and a link to the Bowes 1583 folder (CONTRIBUTIONS.md row of 23 Sept, the 'Specialist reply' section). Fetch that page once (`http://cryptiana.web.fc2.com/code/unsolved.htm` or the URL in sources/cryptiana/, browser User-Agent, one request), save the snapshot to sources/cryptiana/web/ with the date if the existing snapshot predates the credit, and quote the credit line verbatim. If the page does not carry it, say so in the row's evidence column and keep the row as "reported by email, public page not yet showing it" (rule 10 discipline applies to credits too).
2. `tools/build_dashboard.py`: a fourth view button "Hall of fame" beside Readings, Your desk, The machine, in the same style (same tokens, light and dark, phone width). It reads CITATIONS.md at build time (parse the table like load_asks/load_drafts do), shows one card per row (who, where as a link, what as a link to the folder, the quote, the date), a one-line count at the top ("N public citations since 23 Sept 2026"), and, below the cards, a short "pending" list built from CONTRIBUTIONS.md rows whose status contains "sent" and "reply pending" (recipient institution, date sent, what was sent) so the owner sees what may arrive. Nothing else on the board changes. Add a test in tools/tests/ that builds the page from a two-row CITATIONS fixture and asserts the view and count render; the existing tests still pass.
3. Rebuild: `python3 tools/build_dashboard.py`; check docs/index.html renders the new view (open it with tools/browser_fetch.js --shot if the fix in the playbook makes that possible, else read the HTML). Commit with `MSG="board: hall of fame view from CITATIONS.md" python3 tools/room.py --push CITATIONS.md tools/build_dashboard.py tools/tests/<test> docs/index.html sources/cryptiana/web/<snapshot>`.
Do not publish the artifact (the parent republishes). Do not edit status.json's results rows. ROOM done line with the commit and the request count per host; final paragraph: what is in the view, what the Cryptiana page shows, cost.
