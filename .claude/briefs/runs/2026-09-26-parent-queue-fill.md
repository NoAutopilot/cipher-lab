PARENT WORKER QUEUE-FILL (Sonnet, cap USD 5, box 45 minutes, network allowed for catalogue lookups only). Parent: the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n). Context: the owner's ChatGPT runners work for free and are idle: JSTOR-QUEUE.tsv has 97 rows, all done; SECOND-OPINIONS-QUEUE.tsv has one queued row. LOCAL-QUEUE.tsv has 11 queued rows already, so leave it alone unless a row is plainly missing. This job refills the two idle queues from NEXT-STEPS.tsv so that tonight's runs produce something a solver can use tomorrow. It also applies the owner's stuck rule (.claude/briefs/README.md common tail) at zero cost: a stuck target gets one second-opinion prompt asking for leads.

Read first: CLAUDE.md (Verifier brief 2(g) for JSTOR query families; Outreach), SYSTEM.md rows "JSTOR runner", "Verifier runner", "Desk runner", .claude/briefs/parent.md "Account roles" (which targets belong to the other account: Salviati, Malsburg, Armstrong, GOLD, the verifier lineage's targets, and anything claimed in ROOM.md by parent 7i or a LANE it runs; do not queue for those), ciphers/fr2980-gramont/second-opinions/PROMPT-chatgpt.md as the prompt model, NEXT-STEPS.tsv.

Part 1, JSTOR rows (target 20-30 new rows). Walk NEXT-STEPS.tsv runnable rows whose next step names an article, a journal, a review, a bibliography or a "has this been printed" question. For each, two JSTOR query families (one on the sender/recipient and year, one on a distinctive phrase or the shelfmark), format: target, query, requested (today's date), status queued, hits empty. Skip a target that already has 4 or more JSTOR rows. Prefer targets whose folder has an N1-N3 reading (grep status.json) so a hit changes a class.

Part 2, second-opinion lead prompts (target 4-6). Pick owner-account or unclaimed targets whose NOTES.md records a negative or a "not a test" on the cheap test and no different route since (la-garde-1577 is one; find the others from NEXT-STEPS rows with words like "negative", "FAIL", "not a test", "no fit"). For each write ciphers/<folder>/second-opinions/PROMPT-chatgpt-leads.md on the Gramont model but asking for LEADS: known keys of the same chancery and decade, printed editions or calendars of the same correspondence, sibling letters with a decipherment in the same volume, a scholar who has written on that key family, and what the runner would try first. Give it the raw GitHub URLs of ciphertext.txt, NOTES.md and any key.tsv. State plainly that we are an AI-assisted project and that leads, not verdicts, are wanted. Add a SECOND-OPINIONS-QUEUE.tsv row: label SO-<FOLDER-SHORT>-LEADS, folder, prompt path, added today, status queued, pr empty, outcome empty. Never name the owner (rule 9).

Files: JSTOR-QUEUE.tsv, SECOND-OPINIONS-QUEUE.tsv (append only, whole-file read-modify-write; `git diff --stat` must show only added lines on both), the new PROMPT files, one UPDATES.md row, one line in each touched folder's NOTES.md ("queued JSTOR rows / SO lead prompt, 26 Sept 2026, QUEUE-FILL"). The parallel worker NX-UNBLOCK may also append queue rows: post your ROOM claim first naming the targets, read ROOM before each append, `git pull --rebase origin main` before every push, keep both sides on conflicts, no conflict markers. Never touch status.json, BUDGETS.md, LEDGER.md, LOCAL-QUEUE.tsv or reading files. Done line in ROOM: first line counts (JSTOR rows added, SO prompts added, targets covered), second line the six targets you judge most likely to gain a class from a hit and why in five words each.
