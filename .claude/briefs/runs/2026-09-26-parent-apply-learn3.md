PARENT WORKER RETRO-APPLY-LEARN3 (Sonnet, wall-clock box 40 minutes, no subagents, no network except git). Job: apply the brief- and tool-only diffs left open by LEARN-2026-09-26-0022.md (owner-account pass 3, items 2 and 3; item 1 was applied by the other account's RETRO-APPLY-M at 01:03) and LEARN-2026-09-26-0058.md (the other account's pass 4, items 1, 2 and 3; item 4 is report-only). Read CLAUDE.md, both LEARN files in full, and each target file before editing it.
1. tools/room.py --push path validation (LEARN-0022 item 2 = LEARN-0058 item 3): every entry in the path list after --push must exist on disk (or be a deleted tracked file) and must not start with "-"; otherwise refuse with a one-line message naming the offending argument and push nothing. Offline test in tools/tests/ (the existing room test suite's style); keep the suite green.
2. COMMON-file paragraph duplication (LEARN-0022 item 3 = LEARN-0058 item 2): in every .claude/briefs/runs/*-COMMON.md on disk, remove the second verbatim copy of any paragraph that appears twice back to back (the "Cost and time" paragraph in cx, cx2, yx, zx, zx2 and ax); keep the first copy unchanged. Then add a mechanical check so it cannot recur: a small tools/tests/test_common_briefs.py that fails when any *-COMMON.md contains a paragraph twice, and one line in .claude/briefs/README.md's common tail telling a lane opener to run the test after copying a COMMON file forward.
3. Tool reuse (LEARN-0058 item 1): tools/interlinear_align.py already does the hard-EM cipher-to-print alignment that ciphers/jan-van-nassau-1572-75/align/em_align.py and ciphers/lodewijk-van-nassau-1573-74/align/em_align_5799.py re-implement. Do not delete the folder copies (their outputs are cited); instead add a one-line header comment to each pointing at the shared tool, add a line to CLAUDE.md's tool-precedents list (Usage item 8a or wherever tools are enumerated) naming tools/interlinear_align.py as the alignment tool for key recovery from a printed clear text, and post a ROOM flag "for LANE AX" saying AX-NAMES and later workers use the shared tool.
Never touch BUDGETS.md or rate-limit rules. One UPDATES.md row per instituted change (existing format, clock-read time). Post a ROOM claim first and a done line at the end as `parent worker RETRO-APPLY-LEARN3 (Sonnet, <session id>)` with tools/room.py; commit by path, fetch and rebase, push (never force). Never call AskUserQuestion.
