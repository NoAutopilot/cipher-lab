OX-VBS solver (Sonnet, cap $8 as a stall alarm, at most 2 subagents). Parent: LANE OX orchestrator, session_01BE3g8tWbS4T24KXMpShHt4. COMMON: .claude/briefs/runs/2026-09-25-lane-ox-COMMON.md applies in full; solver template .claude/briefs/solver.md (best case N1: the plaintext is in print, so this is a key recovery = contribution).
Target: ciphers/vanbeuningen-dewitt-1657. On disk (OX-VB, 25 Sept 2026): NA 3.01.17 inv.nr 1538 ff.208-209 plain copy and ff.210-211 cipher copy (images/), ciphertext.tsv (862 tokens: plain Dutch function words left in clear, content words as comma-separated 2-digit numeric groups; 666 H, rest M with alternates, cluster L37 ambiguous), plaintext_print.txt (Brieven aan Johan de Witt I pp.405-406).
Job: recover the key by aligning cipher copy to plain text. No host fetches needed (the NA images are on disk; if a crop at higher resolution is truly required, nationaalarchief.nl only, one request at a time).
1. Align ciphertext.tsv to plaintext_print.txt using the clear words as anchors; for each coded run, the plaintext span it replaces. Work out the system: letter-level (2-digit groups per letter, homophones?), syllabic, or nomenclator codes for whole words/names -- decide from the alignment, state the evidence.
2. key.tsv (code -> value, grade C for each value fixed by the known plaintext, count of occurrences; conflicts listed, not hidden). Use the alignment to settle M tokens where the plaintext forces one reading; mark them in exceptions or a notes column, never silently repair ciphertext.tsv (CLAUDE.md layout rule).
3. decode.json for tools/decode_key.py so that `tools/decode_key.py ciphers/vanbeuningen-dewitt-1657 --check` regenerates reading.txt from ciphertext.tsv + key.tsv; per-token grade counts (H/C/S/M/I, rule 4). Differences between the cipher copy's decoded text and the printed text (the two copies are in different hands) listed in a table, not corrected.
4. Fresh-instance re-derivation (rule 7): one subagent that sees only ciphertext.tsv, plaintext_print.txt and your system description (not your key.tsv) rebuilds the key; report agreement per code.
5. judge: if specs/vanbeuningen-dewitt-1657.json does not exist, say so (no spec; Dutch); do not write one.
6. NOTES.md section "Key recovery, 25 Sept 2026": system, key size, coverage, grades, re-derivation agreement, and one line: other letters in NA 3.01.17 inv.nr 1538 (the 1657 bundle) or the Van Beuningen series that look like the same numeric system, if you saw any while aligning (do not fetch a sweep).
Report what was found and where it was not found; do not classify novelty. Files: ciphers/vanbeuningen-dewitt-1657/**, ROOM.md.
