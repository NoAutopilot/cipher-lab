bMALS -- malsburg-hessen-1636: token-level reconciliation of the two crib spans on ff.32/33 (HCPortal record 509) from the crops already on disk. Parent worker for parent 7h (session_01GT3Yv4kUZrBR8pc2s5EToH), run directly because a lane created by this parent sits at the lineage-depth limit; authorised by CLAUDE.md Pipeline 3a and the LANE B11 handoff (STATUS.md) next step (2). Sonnet. Cap USD 3, box 40 minutes from your first `date -u`. Disk only, no hosts. Written 26 Sept 2026 at the 16:12 UTC clock read.

Read first: `.claude/briefs/runs/2026-09-26-lane-b11-common.md` (governs), `.claude/briefs/runs/2026-09-26-lane-b11-bMALX.md` (the crib pass whose two leads this settles), ciphers/malsburg-hessen-1636/NOTES.md section "## bMALX" (the spans, the crop paths, the K.K. vs A.A. question), CLAUDE.md Usage 6 (line crops only, one span per subagent call, two blind passes per span, price per pass).

Units: two spans -- (a) "X.X. oder [mark] nach G.G. genannt" (the second mark: K.K. or A.A.), (b) the "Extrait 70.56.10.28 ... schreibt auf" header. Protocol per span: two independent blind subagent reads (Sonnet) of the existing line crops for that span only, never a full leaf; then you reconcile per token from the crops, recording every disagreement and how it was settled. Four subagent calls in all, about USD 0.4 a line by README.md's pricing precedent; state each call's crop paths in NOTES.md. Stop before starting a unit that would cross 80 pct of cap or box. Output: a "## bMALS" section in NOTES.md with the reconciled token rows (code, gloss, grade M or C, pass agreement), the settled or unsettled second mark, and one sentence on whether any code now carries a consistent word across 3 or more occurrences (the gate LANE B11 set for a nomenclator family -- do not open that family yourself; report the count). No cryptanalysis, no key edits, no pool edits. Update the NEAR.md malsburg-hessen-1636 row's Evidence and Last touched cells in place. Status stays partial. Never write solved, new, first or unpublished.
First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker bMALS (Sonnet, <session id>): malsburg-hessen-1636 crib-span reconciliation`. Finish: commit by explicit path, `python3 tools/room.py --push <paths>`, ROOM done line "done: for parent 7h: bMALS -- span a ..., span b ..., consistent-word codes N"; one-paragraph reply; stop. Never call AskUserQuestion; never print or commit credentials.
