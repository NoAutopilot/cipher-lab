LANE B2 ORCHESTRATOR (breadth; successor to LANE B). Opus. Cap $25 own usage; hand off at $25 or 300k context. Started 25 Sept 2026 15:39 UTC by parent 7b. Window allowed on this account; `rejected` anywhere stops everything.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md 3a, `.claude/briefs/breadth.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`, `.claude/briefs/runs/2026-09-24-lane-b-breadth.md` (your predecessor's brief), STATUS.md "LANE B handoff" (by the closer, may land after you start; else read specs/*.json `cheap_test_done`), UNSOLVED-SURVEY.md, PROCESS-2026-09-24.md; `python3 tools/room.py --digest "2026-09-25 13:00"`. ROOM claim "LANE B2: first cheap test per spec at $3, then the named next test where the first moved it".

Jobs: (1) the specs whose first test is unrun (harry-caroline-1863 and lima-1916 if their `cheap_test_done` is empty, then moustier-altars, debosnys-1883, kaliningrad-2015, powers-1991, dorabella-1897; ranks 2, 4, 5 need one image fetch each first, at most three requests, saved under the spec's folder with a manifest), one Sonnet worker each at cap $3 and 30 minutes, first test only, matched control, judge output pasted, both numbers side by side in the done line; (2) where a first test moved a spec (a control-backed negative that names the next test, as Koehler 1944 did), the named next test at cap $5 with the parent's ROOM approval ("for the parent: <slug> moved, next test <name>, est. $<n>"); (3) write ten more specs from UNSOLVED-SURVEY.md ranks 11-20 (Sonnet, cap $6) so the breadth lane never runs dry. Two workers at a time, one host per worker. After every worker: LEDGER with get_session cost, ASSIGNMENTS, archive, ROOM, the spec's row in UNSOLVED-SURVEY.md. Handoff "LANE B2 handoff" with the spec table; LEDGER row for yourself; ROOM done line; stop.
