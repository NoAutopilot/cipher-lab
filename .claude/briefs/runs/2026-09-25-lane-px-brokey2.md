PX-BROKEY2 (Sonnet, cap $8 as a stall alarm, at most 2 subagents). Target: ciphers/antt-msliv0638-brochado-1712 (QUEUE PX-01, check-solved open, intake gate passed). Job: make the period key and its appendix transcription rule-7 sound. Kind: recovery (key source `period`). Parent: LANE PX orchestrator session_01KapVpHVzNpnnQce5C8c3LY. Read the lane COMMON, .claude/briefs/transcription.md, and the target's NOTES.md in full, above all "## PX-BROKEY" and "Not done this pass, continued".

Hard rules for this job (lesson of PX-BROKEY, which ran to 3.4x its alarm without pushing for 45 minutes and then reported itself under cap): push after every numbered step and at least every 20 minutes; before each push, record your cost from the session metadata (the /cost figure) in one ROOM line; at $6.40 push, write what is left as one line in NOTES.md, and stop.

Steps (no network needed except step 1's optional re-fetch; the appendix leaves are on disk under images/):
1. Second blind transcription pass of the appendix (m0279-m0296), by a Sonnet subagent that has not seen ciphertext_appendix.tsv/plaintext_appendix.tsv, same columns. Reconcile against the existing pass (tools/reconcile_passes.py where it fits), settle disagreements from crops, write the agreement rate and disagreements.tsv; update the two TSVs with per-token grades H (both passes agree) / M.
2. Alignment repair: extend scripts/02_anchor.py to strip mid-span abbreviations (N.Exª etc.) and resolve the unanchored entries (Carta 13, 15, 58, 73, 74, 92, 110) by hand-check against the image; rebuild key.tsv with scripts/04_build_key.py. Report codes and grade C counts before/after.
3. decode.json for tools/decode_key.py so `python3 tools/decode_key.py ciphers/antt-msliv0638-brochado-1712 --check` regenerates the appendix reading from ciphertext_appendix.tsv + key.tsv; report per-entry agreement of the key's decode with each entry's own Deciffrada text (the control: the key must re-read its own source). Entries it fails go in conflicts.tsv.
Do not start the body sweep, the spec or the re-derivation (the next worker's job). Touch only ciphers/antt-msliv0638-brochado-1712/** and ROOM.md. Report what was read and where it was not found; do not classify novelty.
