SOLVER EXPERIMENT solvEX2 (parent worker; Opus; cap $12; disk only, no hosts, no subagents). Started 24 Sept 2026 20:11 UTC by the parent (cipher-lab-7b, session_01K7ZbE95o1pUW5gof8VA5PR). Follows solvEX (ciphers/fr2933-salviati-1525/NOTES.md "Model-in-the-loop crib rounds (solvEX)", commit a75b9c9): the crib loop adds 13.5-15 points where the blind solve is mid-range and nothing where it is at ceiling. The open question it left: does the loop lift the code+mark (cm) design at N=720, where the blind solve reads 22-67% (worker P's curve, "Code+mark control curve (LANE R4 P)")? That is the design and length of the Salviati pooled ciphertext.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md (rules 3, 4, 6, 10), the two NOTES sections above, `python3 tools/crib_rounds.py --help`, `ciphers/fr2933-salviati-1525/control/codemark_curve.py` and `control/make_interleaved.py` (how P built cm controls on the target's own row pattern). ROOM claim line "solvEX2: crib loop on the code+mark design at N=720 -- files ciphers/fr2933-salviati-1525/control/solvex2/, NOTES section, tools/crib_rounds.py (option only if needed)".

Method:
1. Build three cm controls at N=720 exactly as P did (126 code+mark types allotted by frequency deficit, the target's own sign/box row pattern, Italian plaintext held out of the corpus, seeds 1-3), with the hidden key kept in a JSON you do not open. If crib_rounds.py cannot take P's interleaved control format, add a `--cipher-tsv/--plain` option to it (with a test) rather than a private script.
2. Baseline: blind anneal at P's settings (6 restarts x 120,000) and at 24 restarts, per seed. Then the loop: at most 4 rounds per seed, at most 12 cribs per round, proposed only from the language of the partial decode; score after each round. Report per seed: blind 6, blind 24, rounds 0..4, cribs right/wrong.
3. Verdict, in the NOTES section "Crib loop on code+mark at N=720 (solvEX2)": the gain at this design and length, and whether a mid-range blind decode (22-67%) gives enough readable Italian for cribs at all (solvEX found a 33% decode gave none). If the loop's mean gain across seeds is 15 points or more AND at least two seeds end above 85%, run the pooled target (ciphertext_f54r.tsv + ciphertext_f54v.tsv, cm model, same crib limit) and report its per-symbol score against the controls'. No reading is claimed unless the decode reads as Italian at the control's score; then grade S with the control numbers and post ROOM "for LANE V5: reading ready". Otherwise: "the loop adds X points on cm controls at N=720; target run/not run; no reading".
Cost: get_session on yourself after the controls are built, after seed 1, before the target step; stop at $12 with what is on disk pushed. Push with `MSG=... python3 tools/room.py --push <paths>`; ROOM done line with the numbers; final paragraph of five lines; stop.
