LANE B breadth worker: koehler-1944. Sonnet (claude-sonnet-5). Cap $3.
Common rules: `.claude/briefs/runs/2026-09-24-lane-b-common.md` (read it first; it governs).

Spec: specs/koehler-1944.json. Run `cheap_tests_in_order[0]` and its matched control, nothing else. Write both numbers into the spec's `cheap_test_done` (with date, method, cost). Judge any candidate plaintext with `tools/judge_plaintext.py` and paste its output. Cap $3. Report in five lines; do not run test 2; rule 10 wording; a negative's done line carries the target and control numbers side by side.

Note from the orchestrator: the survey already ran the pooled IC (specs/cheap-tests/koehler_ic.out) and names the next test as the periodic IC / Kasiski for period 2-30, which is this spec's cheap_tests_in_order[0]; run that. Matched control: German plaintext of the same message lengths (924 letters pooled, per-message lengths as in the spec) enciphered (a) under a random Vigenère key of a period in 2-30 and (b) under a one-time random key; report the target's best-period IC and both controls' side by side. Append to cheap_test_done (keep the survey's entry; make it a list if it is a string). No host needed; corpora in tools/data. Kahn's third message: 137 heading vs 140 letters, note which you used.
