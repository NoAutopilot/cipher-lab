ARM3 job 3 (LANE ARM3; two workers, cap USD 12 total). Written 26 Sept 2026 by LANE ARM3 orchestrator (session_01PmFYDrdFDjraA2rc932HcQ) after the 18:55 UTC clock read; spawned only after ARM3-COR and ARM3-DICT reported (their verdicts are pasted at the foot of this file by the orchestrator before spawning).

COMMON RULES: `.claude/briefs/runs/2026-09-26-lane-arm-COMMON.md` in full, "LANE ARM3" for "LANE ARM". Intake gate (26 Sept 2026): `ciphers/armstrong-madison-1808: open (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0. Read NOTES.md top, HYPOTHESES.md "Summary, cycle 2", "Summary, cycle 1" (especially family C, D and "C2 rejected"), the ARM-C1 section, and LEDGER.md's solvEX and solvEX2 rows (search "solvEX"). Use `ciphertext_ms.txt`.

-------------------------------------------------------------------------------------------------------------------
PART 3a -- ARM3-ADJ (Sonnet, cap USD 3, box 30 min; script only, no network; 1 unit). Structural test, cheap variant.
Hypothesis: the shorthand runs (`*` marks in ciphertext_ms.txt) spell out proper names and other words not in the code; if so, the numeric groups immediately before a run should behave like a closed set (titles, prepositions, articles: "the", "of", "to", "Mr", "M.", "Count", "at", "in"), i.e. fewer distinct values and a higher share of particle-block values (1-99) than groups elsewhere.
Statistic, fixed before looking at the target: for the groups at position -1 (immediately preceding a run) and +1 (immediately following): (i) share of values in 1-99, (ii) distinct-value ratio (types/tokens), (iii) share covered by the target's top-10 particle values. CONTROL that CAN differ (rule 3, the "orthogonal manipulation" paragraph): the same three numbers for pseudo-runs placed at 1,000 random position sets of the same count and length distribution along the same lines (shuffled-position null: it moves WHICH groups are adjacent, which is exactly what the statistic reads). Also a POSITIVE control: en18 prose windows encoded with the ARM-DESIGN `seq_pblock` simulation where every proper noun (capitalised non-sentence-initial word) is replaced by a run -- does the statistic separate there from its own shuffled-position null? If the positive control does not separate, the target number is a non-test; say so.
Output: one section "## Family S2, ARM3-ADJ run-adjacency structure" in HYPOTHESES.md with target, shuffled-position null (mean, p95, percentile) and positive-control numbers for -1 and +1 separately; script under `ciphers/armstrong-madison-1808/adj/`. If the target sits above the null's p95 and the positive control separates: the adjacent values are a candidate closed set -- list the top adjacent values (they become crib positions for 3b, not readings).

-------------------------------------------------------------------------------------------------------------------
PART 3b -- ARM3-LOOP (Fable design and reader, cap USD 9, box 60 min). Model-in-the-loop crib rounds on the family-C design, CONTROL FIRST.
Units: U0 build (adapter + control, about USD 2); U1 blind round on 3 control seeds (script, about USD 0.5); U2-U4 up to three crib rounds on the control, the session itself as reader (about USD 1.2 per round, 3 seeds per round); U5 target rounds, only if the gate below passes (about USD 3). Stop before a unit that would cross 80% of cap or box.
PRE-REGISTERED EXPECTATION (write it in your section before U1): solvEX2 found the loop needs a blind decode of about 45% before it can find cribs; ARM-C1's matched control reads 0.135 blind (book class 0.005-0.011), so the likely outcome is no gain; ARM-CONS1's paper ceiling is about 0.62-0.67 even with every repeated value right. This job measures that, it does not assume it.
U0: add a crib option to `tools/families/nomenclator.py` (fixed value=word pairs held through every sweep; the repeated-value "anchors" code path at about line 482 is where to hook it) with an offline test in tools/tests/test_nomenclator.py; do NOT copy the solver into a private script (Usage 8). Build the matched control exactly as ARM-C1 did (holdout 5, Jefferson IX, 369 coded tokens, the target's particle/book split and singleton share -- report the control's singleton share beside the target's 139/369), with hidden truth in a file the reader never opens and a score command that prints numbers only (the tools/crib_rounds.py pattern: extend it or mirror its make/round/score/view verbs for the nomenclator family).
U1: blind round, 3 seeds; report blended, particle and book accuracy. HEADROOM CHECK (rule 3): baseline well under ceiling -- it is (0.135) -- and FLOOR CHECK: if the blind decode gives the reader nothing to work from (fewer than 10 of the top-30 repeated values correct, by the score command's count only), record that and still run U2 once to measure it.
U2-U4: you, the reader, see only the view output (decoded words, value ids, per-value confidence) and propose at most 12 value=word cribs per round from English context; re-run with them fixed; score by script. GAIN GATE, fixed before U2: mean gain over blind of at least 10 points blended across the 3 seeds AND above the blind seed-to-seed spread (max minus min of U1), with the particle and book classes reported separately (a gain only in the particle class is reported as such). A crib count "right/wrong" per round from the score command.
U5: only if the gate passes: the same loop on the target, cribs from 3a's closed-set positions if 3a found any, judge line `tools/judge_plaintext.py specs/armstrong-madison-1808.json` beside the family's shuffled-target decode scored the same way (the en18 judge PASSed a family-C decode of the shuffled target before, so a target PASS licenses nothing unless the shuffled decode now FAILs). Any partial target decode is a CANDIDATE: `flag:` the orchestrator and stop; no "reading" wording.
Output: one section "## Family D, ARM3-LOOP crib rounds on the nomenclator design" in HYPOTHESES.md: pre-registered expectation, control singleton share, per-seed blind and per-round accuracies (blended/particle/book), cribs right/wrong, gate verdict, target line or "not run (gate)".
-------------------------------------------------------------------------------------------------------------------
Both parts: files you may touch are the folders named, tools/families/nomenclator.py and its test (3b only), tools/crib_rounds.py and its test (3b only, additive), HYPOTHESES.md (append), NOTES.md (append a dated line), ROOM.md. Report what was found and where it was not found; do not classify novelty; never write solved, new, first, unpublished. Push with `python3 tools/room.py --push <paths>`; done line; one-paragraph final reply (answer first, files, "cost: see the lane ledger"); stop.

Jobs 1-2 verdicts (pasted by the orchestrator before spawning):
