JOB bUNT8 (LANE B6, Sonnet, cap USD 2, wall-clock box 25 min; single leaf, at most 12 crops viewed): untersberg-code step (4b): reconcile pass_b7.tsv (bUNT7) with bUNT6's specs/cheap-tests/untersberg-code/blind_pass.tsv using tools/reconcile_passes.py (reformat_for_reconcile.py exists), settle only disagreements.tsv rows on crops of the on-disk image, and identify the recurring ligature symA (5 occurrences, lines 2, 4, 6) against a period abbreviation table (Cappelli, Lexicon abbreviaturarum, archive.org full text; <= 15 requests) -- candidates qu/que/-rum/-us/-orum/con. Do not read Herzog's print until the reconciled text is committed; then report reconciled-vs-Herzog agreement beside bUNT6's 75/109. Line 6: state what the image and the table support. No expansion of the whole text (that is step 5).
Output: ciphers/untersberg-code/ reconciled.tsv, disagreements.tsv; NOTES.md section "Reconciliation and symA (bUNT8)"; ROOM done line <= 600 chars.
Common rules: read `.claude/briefs/runs/2026-09-26-lane-b6-common.md` in full; it governs. Report what was found and where it was not found; do not classify novelty. Final reply at most five lines.
