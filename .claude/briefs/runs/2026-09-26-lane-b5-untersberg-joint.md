JOB bUNT6 (LANE B5, Sonnet worker). One line: untersberg-code NEAR step (3b) -- joint alignment of Hs 1 (Salzburg Museum Hs. 2398) against Herzog's witnesses Hs 13, 3, 3a and 11 by token length and count, with bUNT5's shuffle control; then, if the box allows, step (2b) a single blind transcription pass of the confirmed leaf (opening 11) compared against Herzog's diplomatic print.

Read first: `.claude/briefs/runs/2026-09-26-lane-b5-common.md` (and the 7b COMMON it names), `.claude/briefs/breadth.md`, NEAR.md's untersberg-code row, specs/untersberg-code.json, specs/cheap-tests/untersberg-code/ (README.md, witnesses.tsv, align_witnesses.py, align_output.txt), ciphers/untersberg-code/NOTES.md. Intake: `python3 tools/intake_gate_check.py untersberg-code` printed "open (line 3) -- edition/page or full-text-search citation found within 6 lines", exit 0, at 01:05 UTC 26 Sept (re-run and paste).

Cap and box: $5 or 45 minutes from your first `date -u`, whichever first. Unit estimates: (3b) disk only, about $1.5; (2b) one leaf image already on disk or one IIIF fetch from the Salzburg Museum host (at most 5 requests, >= 2 s apart, descriptive UA) plus one blind pass, about $2.5. Do not start (2b) if (3b) has used more than 40% of either figure.

Steps:
0. `date -u`; `python3 tools/room.py --start`; ROOM claim (tools/room.py) naming specs/cheap-tests/untersberg-code/ and ciphers/untersberg-code/.
1. (3b) Extend align_witnesses.py (or a sibling script beside it): align Hs 1's six lines jointly against Hs 13, Hs 3, Hs 3a and Hs 11 as printed by Herzog pp.28-29 (witnesses.tsv), scoring per token on initial letter AND token length/count agreement, and whether any witness's spelled-out word expands an Hs 1 abbreviation (the Hs 12 'Bellum. Famus. ...' pattern). Control: the same score over all orders of the Hs 1 tokens (720 permutations or the same shuffle scheme bUNT5 used); report target score and its percentile. A token pair counts toward a grade only at C (a witness spells it) or M; no expansion from the corpus expander.
2. (2b) If budget allows: the leaf image (check ciphers/untersberg-code/images/ first; bUNT3/bUNT4 fetched f.52v-53r and opening 11). One blind pass of opening 11's inscription lines into a TSV WITHOUT looking at Herzog first; then diff against Herzog's diplomatic Hs 1 (normalise conventions per rule 3 before diffing). Report agreement per token.
3. Write both numbers into specs/untersberg-code.json cheap_test_done (append), push, done line with target and control side by side, and the NEAR-ready sentence (what the next step is). Report in five lines.
