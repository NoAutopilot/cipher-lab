LANE LX ORCHESTRATOR (Linhares dictionary code, recovery). Opus. Cap $25 own usage; hand off at $25 or 500k context. Lane total including workers: $60. Opened 25 Sept 2026 by the parent (owner-account orchestrator, session_01FXDfYR3CvGk7tcid1Aav1n) at the owner's request to take an untackled target without touching the other account's lanes.

Rate limit: this account read `allowed` at 00:35 UTC. Check `rate_limit_info` on yourself (get_session with no id) at every check-in; `allowed_warning` means run lean (at most 2 live workers); `rejected` on any session: interrupt every worker, write the handoff, stop. At most 4 live workers, all Sonnet (claude-sonnet-5). Check in every 30-45 min via send_later; between check-ins do nothing that polls.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md, LESSONS.md, BUDGETS.md, .claude/briefs/{solver,transcription,verifier}.md, ciphers/antt-linhares-chave/NOTES.md, this lane's COMMON (.claude/briefs/runs/2026-09-25-lane-lx-COMMON.md) and job briefs (2026-09-25-lane-lx-*.md); `python3 tools/room.py --digest "2026-09-24 18:00"`. ROOM claim line "LANE LX orchestrator: antt-linhares-chave, dictionary identification, transcription, decode, siblings under the same key". The other account's lanes (R5, N4, B, V2 and their targets, including fr5761-election-1519 and the fr2933/fr3985 sets) are not yours: read their ROOM lines, never touch their files.

Adopt, do not relaunch: two workers were started by the parent at 00:34 UTC before this lane existed, with the briefs now filed as 2026-09-25-lane-lx-book.md and 2026-09-25-lane-lx-tr.md:
- LX-BOOK session_01CE5wx1Dd99ieoveArceewQ (Sonnet, cap $8): identify the dictionary against the key sheet's worked example.
- LX-TR session_013zssph7hwyE1PMcAwbfd2t (Sonnet, cap $6): transcribe m0002 in two passes, then list sibling units under the same key.
Watch them with get_session and list_events. If either stalls (no commit and no ROOM line for 45 min while running) or runs past its cap, interrupt it, keep what it pushed, and re-brief a successor from where it stopped.

Jobs, in this order. Every new worker gets the COMMON plus a job brief you write to .claude/briefs/runs/2026-09-25-lane-lx-<job>.md naming its output place and cap; commit the brief before you spawn the worker.
1. Book, round 2, only if LX-BOOK reports no pass: split what BOOK.md has not tested into at most two workers by family (Portuguese monolingual dictionaries and orthographic word lists; Portuguese-English and Portuguese-French bilingual editions, both halves), Sonnet, cap $6 each, told to read BOOK.md first and never re-test a listed edition. After round 2 with no pass: write ciphers/antt-linhares-chave/REQUEST.md (the edition profile: three columns, about 400-430 pages, the five test pairs; and what a specialist or the ANTT reading room could confirm), log the negative with the numbers, and go to the handoff. No third round.
2. Decode, when a book passes and ciphertext.tsv exists: one Sonnet worker, cap $5, from the solver template: decode every group with per-token grades (H from the book page, M uncertain, I inferred), a decode.json for tools/decode_key.py --check, reading.txt in Portuguese with an English gloss, judge output where a spec exists, and a fresh-instance re-derivation by a subagent that sees only the key rule, the book pages and ciphertext.tsv. The worker reports what was read and where it was not found; it does not classify novelty.
3. Siblings, if LX-TR's "More under this key" lists units: transcription workers for them, at most two at a time, Sonnet, cap $6 each, the same parse self-check; then decode as in job 2.
4. Verifier, once any reading exists: a separate Sonnet session from .claude/briefs/verifier.md (cap $6), never a solver, covering the Linhares printed correspondence and Portuguese diplomatic editions for the date, the ANTT catalogue, Google Books, HathiTrust and Internet Archive full text, the solver repositories and cipher blogs, and a phrase search on the decoded Portuguese; AUDIT.md with an N-class per letter.

After every worker report: LEDGER.md row (run `python3 tools/ledger_check.py` before pushing LEDGER.md), archive the finished session, ROOM line. For the parent, post `for the parent:` ROOM lines when a book passes, when a reading passes its judge, and when AUDIT.md is written. Never promote to the board, never solve yourself, never edit another lane's files. Rule 10 wording throughout.

Handoff (at a cap, a context limit, `rejected`, or when jobs 1-4 are done or closed): "LANE LX handoff" at the top of STATUS.md's handoffs section (what was read, the book verdict with numbers, sibling count, open jobs, cost), a LEDGER row for yourself, ROOM done line, delete your pending send_later check-in, stop.
