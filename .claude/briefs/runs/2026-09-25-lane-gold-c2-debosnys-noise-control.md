JOB GOLD-D1: Debosnys, the noise-matched synthetic control that prices the transcription before any more transcription spend (controls only, no target run, no images, no transcription). Sonnet (claude-sonnet-5). Stop and push at $5 or 60 minutes, whichever first. NO subagents. Lane: LANE GOLD orchestrator session_01DKDynpdEwZK5EokxtjCM3P. Written by the cycle-1 consolidator GOLD-CONS1 (Fable, session_017PRh6MAX17rgp5ayuycZ6c), 25 Sept 2026 19:35 UTC; the orchestrator fills the gate line and the spawn time.

INTAKE_GATE_OUTPUT (live, 19:56 UTC): debosnys-1883: open (line 1) -- edition/page or full-text-search citation found within 6 lines  (exit 0). Spawned 25 Sept 2026 19:57 UTC.

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), the top block of `ciphers/debosnys-1883/HYPOTHESES.md` (the decision rule you are producing numbers for), the GOLD-4C section of `ciphers/debosnys-1883/NOTES.md` (the composite ids), `glyphs/inventory.tsv`, `python3 tools/family_run.py --help`, `tools/families/homophonic.py`, `python3 tools/homophonic_anneal.py --help`, and `ciphers/fr2933-salviati-1525/control/codemark_curve.py` lines 20-45 and 130-145 (how LANE R6 injected type noise into a control: a share p of control tokens replaced by a type drawn at the target's own frequencies). Claim in ROOM (`tools/room.py`). Read the clock with `date -u` before any date.

What the last round learned, pasted:
- 1251 signs over 160 ids after GOLD-4C's by-eye split (`ciphertext_draft.tsv`, single pass A; rows with sign `_` or `MULTI` are noise/segmentation and are excluded from every count). Pooled IC 0.0391 vs uniform-at-K 0.0063, French 0.0697. X is 201 of 1251 (16.1 pct), self-adjacent 28 times; without X: N 1050, K 159, IC 0.019. 36 composite ids (base sign + stacked strokes, named by their parts: O-TILDE, OX-TILDE, O-DASH2, II-DASH, CC-DASH, ARCH-DASH, C-BAR-X, II-O ...) cover 267 boxes. 73 ids occur under three times.
- Blind pass agreement: c4 22.4 pct at 68 ids (three quarters inventory confusion; reading-error floor 9 pct once settled on the image), c1 62.3 pct full-id / 66.4 pct family-level at 160 ids (52 columns unsettled). So a single pass carries about 20 pct type noise, a settled two-pass about 10 pct. GOLD-4D (all four cryptograms through one subagent) ran to 3.3x its cap with nothing pushed: one cryptogram per worker, no subagent, cost read at 15 minutes.
- Salviati's curve (LANE R6 CM, code+mark, N 2820 K 223): control 87-94 pct clean, 27-43 pct at 10 pct type noise, 24-27 pct at 20 pct. Debosnys has K/N 0.128 against 0.079 there, so its curve can only be worse. Nobody has measured whether a CLEAN transcription at N 1251, K 160 reads at all. That is this job.

Do:
1. Base + mark recount, mechanical. `ciphers/debosnys-1883/scripts/base_mark_recount.py`: an explicit table (in the script, not a string split) mapping each of GOLD-4C's composite ids to (base, mark class) -- e.g. O-TILDE -> (O, TILDE), OX-TILDE -> (OX, TILDE), II-DASH -> (II, DASH), CC-DASH -> (CC, DASH), ARCH-DASH -> (ARCH, DASH), C-BAR-X -> (C, BAR-X) -- and every non-composite id to (itself, none); BAR-THIN and BAR-SOLID stay distinct bases, the twenty pictograms stay twenty ids, PCT-SLASH folds into PCT as inventory.tsv's `family` column already says. Output `glyphs/base_mark.tsv` (sign, base, mark, count), `ciphertext_draft_base.tsv` (same rows as ciphertext_draft.tsv with sign replaced by base, and a `mark` column), and a table in the output: K_base, number of mark classes, counts per mark class, IC at base level pooled and per cryptogram, beside the 160-id numbers. (A crude first-token split gives K 85, IC 0.061; your table will differ because it does not fold pictograms or BAR variants.) `--check` exits non-zero if the tsv files are stale.
2. Two control options in `tools/families/homophonic.py`, each with a case in `tools/tests/test_family_run.py` (or a new `tools/tests/test_homophonic_family.py`), run offline: `--param profile=target` allots homophones so the control's expected sorted sign-count profile matches the target's own sorted counts (walk the target's counts in descending order, giving each letter signs whose counts sum to about its corpus frequency times N; each occurrence of a letter draws among its signs with probability proportional to those counts) -- so a control at K 160 also carries one sign at 16 pct, as the target does; `--param noise=p` replaces a share p of control tokens by a type drawn at the target's frequencies after enciphering (the Salviati recipe), before solving. Default behaviour unchanged when the params are absent. Document both in the module docstring and in family_run.py's families list in one line each.
3. Controls only (`--control-only`), corpus `tools/data/fr19`, `--seeds 3 --restarts 8`, `--cipher` pointing at a filtered copy (in your scratchpad) of the draft with `_` and `MULTI` rows removed, `--tokens space`; every run appends its own row to `ciphers/debosnys-1883/HYPOTHESES.md` under the tool's marker (use `--label`). Configurations, in this order; run at most three anneals at once; if one seed of configuration A takes more than 6 minutes, cut every configuration to `--seeds 2` and say so:
   A. K 160 (`ciphertext_draft.tsv`), profile=target, noise 0, then 0.1, then 0.2.
   B. base level (`ciphertext_draft_base.tsv`), profile=target, noise 0, then 0.1.
   C. K 160 with every X row dropped (N 1050, K 159), profile=target, noise 0.
   Labels: "GOLD-D1 homophonic fr19 control, K160 profile=target noise=0.1" and so on.
4. Side output, five lines of Python: for one generated control per configuration, the L1 distance between its sorted sign-count profile and the target's, so the consolidator knows whether letter-homophonic at K 160 reproduces a 16 pct sign at all.
5. Write "GOLD-D1, noise-matched controls" under the append-only part of `ciphers/debosnys-1883/HYPOTHESES.md`: the recount table from step 1, the control table (configuration, N, K, noise, seeds, mean and range of plaintext letters recovered), the L1 side output, and which branch (a), (b) or (c) of the top block's decision rule the numbers fall in -- state it, do not brief the next job. One paragraph in NOTES.md ("GOLD-D1") pointing at it; `specs/debosnys-1883.json` `cheap_test_done` gets a "3-control" entry with the numbers and "target not run: transcription below gate".
6. Never run the target: the draft is a single pass at about 20 pct type noise, and a target number on it would be the thing this job exists to price. No decoding, no images, no transcription.

Hosts: none. Requests per host in the done line: 0. Subagents: none.
Done line (ROOM, starting `done: for LANE GOLD: GOLD-D1`): K_base and mark-class count; per configuration "control mean (range) at noise p"; the L1 line; the branch letter; files; no cost figure; nothing about what the text says. Rule 10: never solved, new, first, unpublished; NOTES.md status stays `open`.
