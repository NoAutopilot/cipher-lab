YX-TRIAGE (Sonnet, wall-clock box 40 minutes from your start `date -u`; no network fetching at all). Parent: LANE YX orchestrator session_01PHaEdHeQY2FtMHLo9yGeoe. Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-yx-COMMON.md) first, then .claude/briefs/check-solved.md (the intake gate), the LANE PX, TX and OX handoffs in STATUS.md, and the OX triage (.claude/briefs/runs/2026-09-25-lane-ox-triage.md) as a model of the output.

Job: one disk-only triage of every target the lane could still finish, written to ciphers/_triage/2026-09-25-lane-yx-triage.md (create the folder; touch nothing else except ROOM.md).
1. Candidates: every ciphers/*/NOTES.md whose status line (first lines) is `open` or `partial`. Script it (grep the first 5 lines), do not read folders by hand. Drop: any target named in a ROOM.md claim in the last 6 hours without a done line (script: read ROOM.md lines with timestamps >= now-6h); anything with an AUDIT.md; anything on the never-touch list: fr2933*, fr3151*, fr3985* to fr3990*, fr5761*, roell-vandedem-1809, every N4 nomination, colbert26-lathuillerie-1644, every na-* target, schonenberg*, suriname*, janssens*, oldenbarnevelt-2442*, and the lane's own three (antt-msliv0638-brochado-1712, oldenbarnevelt-brederode-1605, clairambault1225-paget-1714). Keep willem-van-hessen-1567 and la-garde-1577 in the pool.
2. For each survivor, one block: target; status word; intake gate (PASS / FAIL, quoting the sentence of the check-solved verdict that names the standard edition and the pages or full-text search read; a full-text search counts only if the NOTES say a control phrase was found by the same search; FAIL if the edition is named as unopened or the verdict is missing); what is on disk (images yes/no, ciphertext.tsv tokens, key files, spec); route: recovery (decipherment on the leaf, decrypted sibling, published key, known plaintext) or cryptanalysis (which design, which design-matched control exists or would be needed) or blocked (copy order, login, owner machine; name the ASKS/LOCAL-QUEUE row); the one next cheap test, and what a worker needs to run it copy-free from the cloud.
3. Rank: recovery routes with PASS gate first, then cryptanalysis with a named control, then gate FAILs (which need a check-solved worker first), then blocked. Give each a score 1-10 as expected value = P(first cheap test moves it) x value / cost (CLAUDE.md Pipeline 3).
4. End the file with "Top five" (target, route, next test, gate) and a one-line count: candidates scanned, dropped by claim, dropped by never-touch, gate PASS, gate FAIL, blocked.
Rule 10 wording; no novelty words. Commit ciphers/_triage/2026-09-25-lane-yx-triage.md by explicit path, push with tools/room.py --push, ROOM done line, final one-paragraph reply (top five first).
