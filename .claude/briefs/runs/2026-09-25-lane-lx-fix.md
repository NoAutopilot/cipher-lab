LX-FIX (Sonnet, cap $4 as a stall alarm, at most 1 subagent). Target: ciphers/antt-linhares-chave. Job: settle the one unresolved group and the six M tokens of LX-DEC's reading from the images. Parent: LANE LX orchestrator session_01UXTpujdthrPiBDUG57oNwf. Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-lx-COMMON.md), BOOK.md, NOTES.md ("Reading (LX-DEC)" and LX-TR's disagreement table), ciphertext.tsv, key.tsv.

1. Group p3 line1 pos4 (settled as 285219, p.85 col.2 rank 19, but that column has 15 headwords). Before looking at any earlier reading, have one Sonnet subagent re-read that group blind from a fresh high-zoom crop of images/full_PT-TT-CLNH-0086-11_m0002 (the caret-inserted digit above the line). Then list every digit string within one misread digit of the settled one that parses under the key rule, and test each against the book pages (fetch any new page from archive.org newpocketdiction00viey as LX-DEC did, >=1.5 s apart, reading the printed page number). Also test a column-count convention change on p.85 only if the same convention reproduces all 12 worked-example groups and the other 25. Record every candidate and what the page shows.
2. The six M tokens: for each, one line on whether the key's convention (as the 12 worked-example groups fix it) decides it; change a grade only on evidence from an image, never on what makes better Portuguese. Whether "man"+"o"+"d" etc. join into names across groups: note candidates as I (inferred), never H.
3. Update key.tsv (grades, notes), rerun `python3 tools/decode_key.py ciphers/antt-linhares-chave --check` (exit 0) and `tools/judge_plaintext.py specs/antt-linhares-chave.json --file ciphers/antt-linhares-chave/reading.txt`; write a "Fix pass (LX-FIX)" section in NOTES.md with the before/after grade counts. Do not change the status line beyond `partial`.
Touch only ciphers/antt-linhares-chave/{key.tsv,reading*.txt,reading_tokens.tsv,NOTES.md,images/**} and ROOM.md. Do not touch AUDIT.md (a verifier is writing it). Report what was settled and what was not; archive.org request count.
