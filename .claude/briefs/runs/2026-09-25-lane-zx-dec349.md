ZX-DEC349 (Opus, per CLAUDE.md Usage 1: key reading and cipher reasoning; wall-clock box 75 minutes, also a minimum working time; at most 2 subagents, Opus). Target: ciphers/clair349-este-guise-1556. Parent: LANE ZX orchestrator session_01MxueEQJUGF9PWJiYcVyvBM. Kind: recovery (key `period`: fr.20974; plus the contemporary interlinear decipherment on the leaf, known plaintext, grade C). Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md), .claude/briefs/solver.md, NOTES.md sections "ZX-KEY349", "ZX-TR349C" and "ZX-TR349D", key_alpha.tsv, key_nomen.tsv, ciphertext.tsv (token + gloss columns, from ZX-TR349D). Intake gate: `partial`, exit 0 (orchestrator 15:44 UTC 25 Sept 2026; re-run it and paste the output). No network.
1. Settle the key's homographs from the gloss, never by taste: for each homograph ZX-KEY349 listed (5 D/L, 22 sc?/pour, 9 C/round-ss, S37 T/fit, S17 Monsr/fist, S32 Q/n'aye) and any the key-vs-gloss table shows, record the rule the gloss supports (e.g. "|22| alone between dividers = pour") with its count. Write exceptions.tsv for token-position overrides the gloss proves, each with a reason.
2. decode.json for tools/decode_key.py (key ["key_alpha.tsv","key_nomen.tsv"], exceptions.tsv, dividers `|` as word breaks, gloss column ignored by the decoder). Run `python3 tools/decode_key.py ciphers/clair349-este-guise-1556` and commit reading.txt (French as decoded, word breaks at dividers) and the per-token grades: H key row read at H and token agreed blind (ciphertext H); C the key and the gloss agree on this token; M any M in the chain; I inferred or repaired; U unread. Give the counts. `--check` must exit 0.
3. Reading vs gloss: diff the decoded letter stream against the gloss column where a gloss exists (normalize both first: u/v, i/j, long s, one case, no punctuation; CLAUDE.md rule 3's convention lesson); report agreement pooled and per line, and list every disagreement with its likeliest cause (transcription, key cell, homograph, gloss abbreviation).
4. Spec specs/clair349-este-guise-1556.json on the pattern of specs/thurloe-barriere-1654.json (ciphertext source, alphabet, constraints, judge {"language": "fr"}: the fr16 corpus is Catherine de Medicis's letters, era-matched to 1557). Paste `python3 tools/judge_plaintext.py specs/clair349-este-guise-1556.json --file ciphers/clair349-este-guise-1556/reading.txt` verbatim. Matched control: the same key applied to ciphertext.tsv with tokens shuffled within lines (5 seeds), judged the same way; report target vs control side by side.
5. English gloss of the French (a separate file reading_en.txt), and a short paragraph in NOTES.md on what the letter says, stated as read at its grades.
Do NOT run the re-derivation yourself: rule 7 needs a fresh session that sees only the spec and the key; the lane orchestrator briefs it after you push. NOTES.md gets one section "## ZX-DEC349 (25 Sept 2026)". Status stays `partial` (the orchestrator moves it after the re-derivation). Touch only ciphers/clair349-este-guise-1556/**, specs/clair349-este-guise-1556.json and ROOM.md. Push after each step. Report what was read and where it was not found; do not classify novelty (no new, unpublished, first); write "cost: see the lane ledger". Never call AskUserQuestion.
