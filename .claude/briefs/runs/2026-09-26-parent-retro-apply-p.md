PARENT WORKER RETRO-APPLY-P (Sonnet, cap $6 of usage, wall-clock box 40 minutes, no subagents, no network except git; the container is a depth-50 clone). Job: apply the brief- and tool-only proposals of RETRO-2026-09-26d.md (retrospective p, 26 Sept 2026 05:35 UTC) for parent 7f (session_01J57XZYekjroNoFt7SoSS3W). Authorised by CLAUDE.md "Improvement loop" (the orchestrator applies changes that only touch briefs, tools or workflows; anything that changes the goal, the spend or the owner's asks goes to ASKS.md). Written 26 Sept 2026 at the 06:10 UTC clock read.

First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker RETRO-APPLY-P (Sonnet, <session id>)`; read CLAUDE.md, RETRO-2026-09-26d.md in full, then each target file before editing it. Edits minimal, in the file's voice. Apply each of the five proposals as the retro's diff, in order: (1) hand-over verification -- the LIVE/ARCHIVED title rule and archive-on-takeover already sit in .claude/briefs/parent.md "Handing over" and hub-seed/SUCCESSOR-PROMPT.md (UPDATES.md 05:17); add only what the retro proposes beyond that, and if it proposes a mechanical check, put it in tools/orphan_check.py (landed 05:23, tools/tests/test_orphan_check.py) as a new check with a test case rather than a new script; (2) session-id self-check for ROOM.md role fields -- into tools/room.py `warnings_for()` (non-blocking WARNING, same slot as the existing ones) with a test in tools/tests/; (3) per-leaf merge gate for a sparse gloss -- into .claude/briefs/breadth.md or the recovery brief the retro names, and CLAUDE.md rule 3 only if the retro's diff says so; (4) per-unit box priced by subagent-call count -- CLAUDE.md Usage item 6, one paragraph, no repetition of the existing GOLD-4D/AT55V text; (5) JSTOR quoted-phrase query family -- the verifier brief template in CLAUDE.md "Workers" and tools/jstor_runner_brief.md as the retro proposes. Two extra tool-only items from V8-QA7 (05:55-05:56) and the retro's hygiene note: (6) tools/intake_gate_check.py REUSED_SEARCH_RE fires on the bare word "re-cited" in prose describing an old verdict (lope-hurtado-1522 NOTES.md:5) -- narrow it so a citation is flagged only when the same source line is cited as this pass's own search, add the lope-hurtado sentence as a negative test case, run tools/tests/test_intake_gate_check.py directly (pytest is not installed; run test files directly); (7) the retro's NEAR.md stale-column note -- fix the columns it names in NEAR.md in place, numbers unchanged, and run tools/near_check.py. One UPDATES.md row per instituted change (`By` = "RETRO-APPLY-P for parent 7f", clock-read time). Never touch BUDGETS.md or rate-limit rules; never touch status.json or any ciphers/ folder. Commit by explicit path, `python3 tools/room.py --push <paths>`; ROOM done line `done: RETRO-APPLY-P: N of 7 items applied (...), tests <pass/fail>, UPDATES rows M`; one-paragraph reply; stop.
