bSPECFILL -- backfill `cheap_test_done` on seven specs whose first test already ran in the target folder (LANE B12 reserve wave). Sonnet. Cap USD 3, box 30 minutes from your first `date -u`. Disk only, no hosts, no subagents, no new tests.

Read first: `.claude/briefs/runs/2026-09-26-lane-b11-common.md` (governs; B12 for B11), CLAUDE.md rule 3 (both numbers side by side) and rule 5, Pipeline 3a.

Why: the orchestrator listed every specs/*.json with an empty `cheap_test_done` (17:42 UTC): ten specs. Each one's first named test was already run by an earlier lane and recorded in the target folder, but the spec field was never written, so the breadth queue shows them as unrun. A breadth worker takes one first test per spec, never a second; running these again would duplicate paid work.

Do, for each of these seven, in this order (about 3-4 minutes each): destaing-gerard-1779 (ZX2-EST tests 1, 3, 4: HYPOTHESES.md period_code_overlap rows), fr3985-nevers-f176 (LANE R5 G2 key60 apply + judge; target folder ciphers/fr3985-nevers-revol-1593 -- check which leaf f.176 is), na-janssens-java-1811 (VX-RD02/RD02C), na-schonenberg-1678-1716 (VX-RD01), na-suriname-map-1781 (RD03), antt-linhares-chave (LX-DEC and the pt18 judge, V6-PTCORP), catokwacopa-1875 (blocked: no ciphertext on disk, pending LOCAL-QUEUE L13 -- write it as a blocked result quoting the L13 row's status).
 For each: find the run in ciphers/<t>/NOTES.md, HYPOTHESES.md, AUDIT.md and LEDGER.md (grep the worker label); write `cheap_test_done` as a JSON object {"date", "by" (the worker label and lane that ran it, not you), "test" (which numbered test), "target" (the number), "control" (the matched control's number, or "none on file" -- never invent one), "judge" (the verdict line if one was pasted), "verdict" (the record's own words: control-backed negative / partial / reading at grade H etc.), "source" (file:section), "backfilled": "bSPECFILL 26 Sept 2026 <your clock>"}. Where the record has a reading but no control number, say so in "control"; do not run one.
 Leave alone (other lanes/accounts own them; list them in your report only): antt-msliv0638-brochado-1712, lodewijk-5797 (LANE AX), rah-morillo-1817 (owner account).
 Validate each file with `python3 -m json.tool` after editing; keep all other keys byte-identical (load, set one key, dump with the file's existing indent).

Write only specs/<the seven>.json. Commit by explicit path; `git diff --stat` then `python3 tools/room.py --push <paths>` once at the end (and at 80% of cap with what is done). ROOM claim first; done line at most 600 chars: per spec "<slug>: target <x> / control <y or none>". Final reply at most five lines. Do not classify novelty; never write solved, new, first or unpublished.
