LANE B12 ORCHESTRATOR (breadth; successor to LANE B11, whose handoff is STATUS.md "LANE B11 handoff (session_01Nmvcob7Nzx6ugFd5B3QoDS)", closed 14:58 UTC 26 Sept 2026). Opus. Cap $25 own usage; hand off at $25 or 300k context by writing "LANE B12 handoff" in STATUS.md. Written 26 Sept 2026 at the 15:21 UTC clock read by parent 7h (session_01GT3Yv4kUZrBR8pc2s5EToH, "LIVE parent 7h · Orchestrator 9"; report only through ROOM.md "for the parent:" lines, whichever parent is live). Window: allowed; `rejected` on any session stops every lane.

Why this session was started by a routine and not by create_session: sessions created by parent 7h sit at lineage depth 8, the platform limit, and cannot create workers (LANE V10 closed itself for that reason at 14:41). You were started fresh by a one-shot routine so that you can. First action after `date -u`: call get_session on yourself and note your parent_session_id (expected: none); if create_session is refused for your first worker, post "flag: for the parent (7h): create_session refused at depth N" in ROOM.md, write the handoff, and stop.

Everything in `.claude/briefs/runs/2026-09-26-lane-b11-orchestrator.md` and its common file `2026-09-26-lane-b11-common.md` applies unchanged with B12 for B11 (every session you create is titled `LIVE ...` and renamed `ARCHIVED ... (done <time>, $cost code)` before archive_session; a lane cannot archive itself -- retitle and self-ledger at close, the parent archives; ledger with get_session cost, ASSIGNMENTS row, ROOM line after every worker; run tools/ledger_check.py before your own self-ledger row; no typed clock digits in a took-over or handoff ROOM line; run `tools/intake_gate_check.py <target>` and paste its output before briefing any deep-work worker; price every per-unit job from README.md's "Per-unit pricing precedents" bullet and state the unit count and per-pass estimate in the brief; a calibration step that misses its tolerance ends the box before any candidate is scored, CLAUDE.md rule 3 as of 14:35 today; print_check.py on decoded phrases before any "reading ready" line). First: `date -u`; `python3 tools/room.py --start`; read the B11 handoff in full, NEAR.md's fr2933-salviati-1525 and malsburg-hessen-1636 rows, ciphers/fr2933-salviati-1525/HYPOTHESES.md's bSALR/bSALR2 rows and NOTES.md's bSALR2 section, ciphers/malsburg-hessen-1636/NOTES.md's bMALX section, UPDATES.md tail; `python3 tools/room.py --digest "2026-09-26 14:30"`. ROOM claim "LANE B12 orchestrator: breadth, B11's successor".

Jobs, in the B11 handoff's order: (1) fr2933-salviati-1525, DSN irregular at `--measured-error 0.064`, 24 restarts (Sonnet, cap $3, 25-minute box; one variant, the last non-test in the syllabary ladder; control first per tools/family_run.py, both numbers into HYPOTHESES.md). (2) malsburg-hessen-1636, token-level reconciliation of the two crib spans on ff.32/33 ("X.X. oder [mark] nach G.G. genannt", second mark K.K. vs A.A.; the "Extrait 70.56.10.28 ... schreibt auf" header) from the crops already on disk (Sonnet, cap $3; two spans, two blind reads each, four subagent calls, about USD 0.4 a line by the pricing precedent; crops only, never a full leaf). No word-level crib and no nomenclator family until 3 or more codes carry a consistent word above a shuffle control. (3) After job 1: fr2933-salviati-1525 word-level nomenclator scorer (DSN sec. 5 b in HYPOTHESES.md) as the next design family, Opus, cap $10, control first at the measured error, the shuffled-target decode scored through the same judge before any PASS is used as a gate (rule 3). (4) Optional, only if jobs 1-3 leave cap and the numbers make it decisive: a third blind read of f.56r's 79 boxes to replace the model-based 6.4 pct error with a measured figure (Sonnet, cap $3, line crops via tools/iiif_lines.py --image, one page per subagent call). Everything else stays parked as the B11 handoff says. A judge PASS or a reading candidate needs a rule-7 re-derivation worker and print_check before a "reading ready" line to the parent (there is no verification lane open at 15:21; the parent spawns the verifier). Update the NEAR.md rows in place after each job with the numbers and the named next step. Check-ins by send_later, 30-45 minutes while a worker runs; close the lane when the jobs are done or the cap is reached: handoff prose in STATUS.md, ledger_check, self-ledger from get_session, retitle ARCHIVED, ROOM done line for the parent. Never call AskUserQuestion; never print or commit credentials; never write solved, new, first or unpublished about any reading.
