ROLE: ACCESS + TRANSCRIPTION (LANE R worker R4). Model Sonnet, cap $8.
Target: ciphers/rah-canada-1869 ("Nota cifrada del Conde de la Cañada", RAH Sig. 9/6958, Legajo XIX, Nº 117, record id 14495, Real Academia de la Historia). Read NOTES.md (copy status, the redirect behaviour noted from QUEUE.md's caveats).
Step 1 (host bibliotecadigital.rah.es only, <=120 requests >=1.5 s apart): find the record's image route (the site's IIIF manifest if it has one, else its own image/PDF download), fetch every page of the note and of the letters around it in the same Nº 117 (the covering letter may say what the cipher is or carry a decipherment), to images/ with manifest.json. Record the working route in NOTES.md Access notes (and one line in CLAUDE.md's Access playbook only if the route is non-obvious and reusable). Push.
Step 2: images/inventory.tsv (page, content: cipher / decipherment / clear, cipher type: digits, letters, symbols; approx tokens). If there is any interlinear or separate decipherment, say so in ROOM at once (`note: for LANE R`), since that makes this recovery.
Step 3: two blind Sonnet subagent passes of the cipher (passA.tsv, passB.tsv: page, line, idx, token) from the images (crop lines first; tools/iiif_lines.py if IIIF, else a PIL crop script added as an option to tools/iiif_lines.py, not a private copy); if the signs are invented glyphs, build one shared atlas first (.claude/briefs/transcription.md). tools/reconcile_passes.py; do not settle disagreements (the reconciler does). Push after each pass.
Report token count, distinct symbols, pass agreement, and whether a decipherment or key is in the images. Do not decode.
