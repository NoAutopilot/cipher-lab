LANE B2 breadth worker bCPH: copenhagen-1835. Sonnet (claude-sonnet-5). Cap $3 or 30 minutes, whichever first. No subagents.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b2-common.md` (read it first; it governs) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Spec: specs/copenhagen-1835.json. Run `cheap_tests_in_order[0]` and its matched control, nothing else. Write both numbers into the spec's `cheap_test_done` (date, method, script path). Judge any candidate with `tools/judge_plaintext.py` where the language is wired and paste its output; for Danish say the judge has no Danish corpus and report the anneal score against the Danish control distribution instead. Report in five lines; do not run test 2; rule 10 wording; target and control numbers side by side.

The transcription is Pelling's interpretive one (rule 2: every result conditional on it). Danish corpus: one period Danish text from the Internet Archive (advancedsearch.php to find a 19th-century Danish prose item, then its `_djvu.txt`, fetched once to tools/data/da19/ with a README naming the item; at most 10 archive.org requests, >=1.5 s apart). Wiring 'da' into judge_plaintext.py's LANG_CORPORA is welcome if it takes one small edit plus its test; otherwise leave it and say so. Gutenberg is LANE GOLD's host today: do not use it. Solver tools/homophonic_anneal.py as a 1:1 MASC (or tools/subst_hillclimb.py), same restarts on target and controls; controls: Danish, German and English MASC-enciphered plaintext at N=98, 3 seeds each, percent letters right. Script and output under specs/cheap-tests/copenhagen-1835/.
