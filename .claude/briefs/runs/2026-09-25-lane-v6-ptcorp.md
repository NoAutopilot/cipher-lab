V6-PTCORP (Sonnet, cap $6, wall-clock box 45 minutes from your first `date -u`: stop and push at $6 or 45 minutes, whichever first; at most 2 subagents, and budget for them). Parent: LANE V6 orchestrator session_01V2BHwhVh1k72qSYuBFCyGd. Read CLAUDE.md (rules 3 and 7, Good-citizen rule, Internet Archive rows of the hosts table), `.claude/briefs/runs/2026-09-24-lane-v4-common.md` items 1, 4, 5, 7 (common tail) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`. First: `date -u`; `python3 tools/room.py --start`; last 30 lines of ROOM.md; claim with tools/room.py: "V6-PTCORP (Sonnet, for LANE V6)" "claim: tools/data/pt18 corpus + judge wiring + Linhares re-run -- files: tools/data/pt18/, tools/judge_plaintext.py (LANG_CORPORA only), tools/tests/, ciphers/antt-linhares-chave/NOTES.md (new section), ciphers/antt-linhares-chave/scripts/; host archive.org".

Job, in order:
1. Build `tools/data/pt18/`: public-domain Portuguese prose written about 1780-1830, at least 1,000,000 letters after the judge's fold(). Internet Archive only (advancedsearch.php for candidates, `_djvu.txt` full text; one request at a time, >=1.5 s apart, a few hundred requests at most). Good kinds: periodicals (Correio Braziliense 1808-1822, Investigador Portuguez em Inglaterra, Gazeta de Lisboa), memoirs and political pamphlets of the Peninsular War years, official correspondence, the Academia Real das Sciencias Memorias of the period. Excluded: Vieira (that is pt17), anything by or about Linhares/Sousa Coutinho or from this target's own material, dictionaries and grammars (Vieyra's dictionary is the key -- circular), translations from English/French where you can tell, verse. Check each volume's OCR quality (share of folded letters in words found in the others; drop anything with heavy OCR junk or mostly Latin/French). Several sources, no single one above 40% of the letters. Store gzipped as pt17 does. README.md: one row per file (IA identifier, title, author, year, letters kept, why chosen), the date fetched, and the exclusions.
2. Wire `"pt18"` into `LANG_CORPORA` in tools/judge_plaintext.py (keep `pt` as it is, add a `pt18` key; the spec chooses by its `language` field or `corpora`). Add an offline test in tools/tests/ (follow test_judge_plaintext_lang.py: the files exist, fold to >=1M letters, a held-out real window scores above a shuffled one). Run the existing tests; all must pass.
3. Re-run on Linhares. Score the committed reading two ways with `language: pt18`: (i) `tools/judge_plaintext.py specs/antt-linhares-chave.json --file ciphers/antt-linhares-chave/reading.txt` with the spec's language switched to pt18 in a scratch copy of the spec (do not edit the committed spec unless step 4 says so), and (ii) the normalized 25-word plain rendering LX-JUDGE used (its script `ciphers/antt-linhares-chave/scripts/judge_discrimination_check.py`; the judge now drops `#` lines). Then run LX-JUDGE's design-matched controls against the pt18 model: (a) real 1780-1830 Portuguese windows of the same letter count, from a pt18-quality source **held out of the pt18 training files** (name it; LX-JUDGE's `exposiodosfa00cevauoft` is fine if you hold it out); (b_trim) trim-matched Vieyra-headword salad; (c) shuffled reading; 200 draws, fixed seed. Report the reading's score, null_p99, real_p05, and its percentile in each control, beside the same numbers under pt17 from LX-JUDGE's table.
4. Verdict: PASS (reading above both null_p99 and real_p05 under pt18, and above the held-out real-prose false-negative rate shown by control (a)) or FAIL, with both numbers side by side. If PASS, set the committed spec's `language` to `pt18` and paste the judge output. Write a new NOTES.md section "V6-PTCORP pt18 judge re-run (25 Sept 2026)" with the judge output pasted (rule 7) and the control table. Do not touch status.json, AUDIT.md, key.tsv or reading.txt.
Commit by explicit path, push (`tools/room.py --push <paths>`; if it says nothing staged, fetch/rebase/push by hand). ROOM done line starting "for LANE V6: Linhares judge PASS" or "... FAIL", with reading score and control numbers side by side, pt18 letter count, requests per host, no cost figure. Final report: five lines. Stop.
