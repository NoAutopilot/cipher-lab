PARENT WORKER KEY-DESIGN (Opus, cap USD 10, box 75 minutes, no network except git). Parent: the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n); context: OPTIMIZATION-2026-09-26.md section (d), the owner's intuition made mechanical: understanding key a's structure narrows the hypothesis for unread letter b. Read KEY-CROSSMATCH.md (XMATCH-CAL's calibrated gate, 26 Sept 18:24: the cross-match licenses key fit, not a reading), KEY-OFFICES.tsv (52 rows), KEY-CROSSMATCH-CAL.tsv, tools/key_crossmatch.py (robust_load_key), tools/family_run.py (design families), tools/glyph_atlas.py, CLAUDE.md rule 3 (controls and the calibration gate) first.
1. KEY-DESIGN.tsv: one row per usable key table on disk (the 57 the cross-match can load, plus any it excludes with the reason): key_path, office (from KEY-OFFICES.tsv), correspondents, years, language, design family (alphabet substitution / homophonic / nomenclator / syllabary / code numbers / mixed), signature numbers computed from the table itself: number of distinct plaintext letters covered, homophones per letter (mean and max), nomenclator size (words and names), numeral range (min-max, digit lengths), syllable-table presence, nulls declared, sign inventory type (digits / letters / glyphs / mixed), plus the ciphertext statistics of its own text where one exists (token count, distinct tokens, singleton share, top-token share, index of coincidence on tokens). Write tools/key_design.py to build it idempotently, with `--check`.
2. tools/design_prior.py: given an unread ciphertext (a tokenised TSV in the repo's format), compute the same ciphertext statistics and score its likeness to each design family and to each office-decade cluster in KEY-DESIGN.tsv (a simple, explained distance on the statistics, no learned model), with a shuffled control (the same scores on the ciphertext's tokens relabelled at random, 200 draws) and a plain verdict: which families are plausible above the null, which are excluded, and the three nearest keys on file with their offices and years. Calibrate on the verified readings: for every ciphertext with a verified key on disk, the true key's family must rank in the top two; report the calibration table and the false-positive rate on shuffled inputs. Add an offline test with fixtures.
3. Run design_prior.py on every ciphertext in an open or partial folder that has no key (NEXT-STEPS.tsv blocker needs-key and the 'below unicity' rows), write the verdicts to KEY-DESIGN-PRIORS.tsv, and post "for the parent" one ROOM line naming any ciphertext whose nearest key on file is in the same office and decade (the leads a lane should test first).
4. A closing rule, one sentence in .claude/briefs/README.md's common tail and in CLAUDE.md Usage 8a: every solved or recovered key is added to KEY-OFFICES.tsv and KEY-DESIGN.tsv at the lane's close-out, and design_prior.py is run before an attack family is chosen for an unread letter.
Never edit a reading, a key or status.json. Post a ROOM claim first and a done line at the end (first line: rows in KEY-DESIGN.tsv and the calibration result; second: the leads) as `parent worker KEY-DESIGN (Opus, <session id>)` with tools/room.py (two positional arguments); add your LEDGER.md row; UPDATES.md row per change; commit by explicit path, fetch and rebase, push (never force). Never call AskUserQuestion.
