LANE DX ORCHESTRATOR (DECODE unlock and the recovery it opens). Opus. No dollar budget: the owner's Max plan is not billed per token (owner, 25 Sept 2026 00:50 UTC), so the only binding limit is the rate-limit window in `rate_limit_info`. Hand off to a successor at 500k context. Worker caps below are stall alarms, not budgets: a worker at its cap that is still producing gets a successor briefed from where it stopped; one that is stuck gets interrupted. Opened 25 Sept 2026 by the parent (owner-account orchestrator, session_01FXDfYR3CvGk7tcid1Aav1n). The owner has said some credentials may be set only on the other account's environment: when a job needs a variable this account lacks, do not work around it; post `for the parent:` naming the exact variable and stop that job.

Rate limit, check-ins, worker count, first steps, ledgering, handoff: exactly as in .claude/briefs/runs/2026-09-25-lane-lx-orchestrator.md, with LANE DX for LANE LX and this lane's COMMON (2026-09-25-lane-dx-COMMON.md). ROOM claim line "LANE DX orchestrator: credential probe, one DECODE login, then 8725/R413/R4930 and the neighbour-record pairs". Not yours: every other lane's targets (R5, N4, B, V2 on the other account; LX and OX here).

Jobs, in order; each worker gets the COMMON plus a job brief you file first as 2026-09-25-lane-dx-<job>.md.
1. Probe (Sonnet, cap $1.50), in a fresh container of this account. Report set or MISSING, and nothing else about the value, for: DECODE_USER, DECODE_PASS, IA_USER, IA_PASS, JSTOR_USER, JSTOR_PASS, GOOGLE_BOOKS_KEY, OPENALEX_KEY, S2_KEY, NARA_API_KEY, and any other credential variable the Access playbook names. For DECODE_USER add only whether it contains an `@` (the site wants a username; the owner removed the address part on 23 Sept). No lengths, no values, `test -n` only. Result as one ROOM line and in the worker's final paragraph.
2. DECODE login, only if the probe shows DECODE_USER set without an `@` and DECODE_PASS set, AND the last 24 hours of ROOM.md show no other DECODE login attempt: post `note: DECODE login: owner account taking the one attempt now; others hold` first, then exactly ONE attempt with `tools/decode_fetch.sh 8725 ciphers/intercepted-royalist-1646/decode/` (ASKS row 1's instruction). Success: fetch R413 (boswell-1628, with its key) and R4930 (the Randolph key, randolph-sussex-1569) in the same session, save under each target's decode/ folder, and update ASKS row 1. Rejected: no retry; `for the parent:` DECODE rejected on the owner account, with the probe result; then the lane goes to its handoff, since jobs 3 and 4 need the login.
3. Readings from the DECODE records, if job 2 fetched them: one worker per target (Sonnet, cap $6), from the solver template: apply the key the record carries, per-token grades, judge output, a fresh-instance re-derivation. Randolph first (its images and first-pass ciphertext are already in the repo, and R4930 is its key fit).
4. Neighbour-record pairs (QUEUE.md "Neighbour-record recovery candidates", tools/decode_neighbours.py): check-solved first on each cluster (editions first: Nuntiaturberichte, CODOIN, calendars), then for open clusters open the letter and its decrypted sibling, transcribe in two passes, apply the sibling's key. At most three workers at once, Sonnet, cap $6 each. Only the job-2 worker logs in; if a later worker needs DECODE, it hands a list of record ids to one DECODE worker.
5. Verifier per reading, a separate session (.claude/briefs/verifier.md, Sonnet, cap $6), AUDIT.md with an N-class.
For the parent: the probe result, the login verdict, each reading that passes its judge, each AUDIT.md.
