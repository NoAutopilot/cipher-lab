YX-PTJUDGE (Sonnet, wall-clock box 45 minutes; at most 2 subagents). Tools job, no target reading. Parent: LANE YX orchestrator session_01PHaEdHeQY2FtMHLo9yGeoe. Read the lane COMMON, CLAUDE.md Usage 8 (shared scripts, --help, offline test in tools/tests/), tools/judge_plaintext.py in full, tools/tests/ for the judge's existing test, and tools/data/*/MANIFEST.tsv.

Problem: tools/judge_plaintext.py's LANG_CORPORA has only en, de, fr, so every pt spec (antt-linhares-chave, antt-msliv0638-brochado-1712) gets a length-only PASS that says nothing about language (YX-BRO79, LX-DEC). Folders tools/data/it16, nl_repo, es_repo, la_repo, pt_repo exist but are not wired in, and pt_repo holds only one of our own readings (circular: never use a target's own reading as its corpus).
1. Build tools/data/pt17/: early-modern Portuguese prose, 1650-1750 if possible, public domain, at least ~300k characters, from Internet Archive `_djvu.txt` full text (advancedsearch for language:por and date 1600-1760, letters/cartas/memorias preferred; e.g. António Vieira's Cartas, or diplomatic letters of the period) or a Google Books full-view volume (GOOGLE_BOOKS_KEY, &country=US). Never use Brochado's own letters or any text from the MSLIV 0638 volume or Dória 1944 (the judge must not have seen the target's own voice verbatim); name every file in MANIFEST.tsv (source URL, identifier, date, why chosen). gzip if large; keep the folder under 5 MB. IA 1.5 s apart, at most 40 requests.
2. Wire LANG_CORPORA: add "pt" -> tools/data/pt17 files; and for it, nl, es, la, check what each folder holds; wire a language only if its folder holds a real period corpus of at least ~200k characters that is not a target's own reading; otherwise list it as not wired and why, in a short comment in the code and in tools/data/README or MANIFEST.
3. Offline test in tools/tests/ (same style as the judge's existing test): a held-out Portuguese passage (not in the corpus) passes the language check; the same passage shuffled, and a random-letter string of the same length, fail it. Run the whole tools/tests suite for the judge and paste the result in your final paragraph.
4. Re-run, and paste verbatim into the target NOTES.md under a short section "## YX-PTJUDGE judge re-run (25 Sept 2026)": `python3 tools/judge_plaintext.py specs/antt-msliv0638-brochado-1712.json --file <the letter-134 reading file named in its spec or NOTES.md>`. Also run it for specs/antt-linhares-chave.json on its committed reading and paste the output into ciphers/antt-linhares-chave/NOTES.md the same way. Touch nothing else in those folders. A FAIL is reported as a FAIL.
Touch only tools/judge_plaintext.py, tools/data/pt17/**, tools/data MANIFEST/README files, tools/tests/** for the judge, the two NOTES.md sections named above, and ROOM.md. Push after steps 2, 3 and 4. Rule 10 wording; "cost: see the lane ledger".
