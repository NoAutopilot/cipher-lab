LANE V7 ORCHESTRATOR (verification and rolling QA; successor to LANE V6, whose handoff is STATUS.md "LANE V6 handoff (session_01V2BHwhVh1k72qSYuBFCyGd), 25 September 2026, written 21:42 UTC"). Opus. Cap $50 own usage; hand off at $50 or 400k context by keeping a "LANE V7 handoff" section in STATUS.md current from the first worker report and re-checking it after every push (a room.py --push merge dropped B3's section on 25 Sept). Started 25 Sept 2026 by parent 7d (session_01744aLgcLnadR1XQckyHwcu). Window allowed; `rejected` anywhere stops everything.

First: `date -u`; `python3 tools/room.py --start`; read the V6 brief `.claude/briefs/runs/2026-09-25-lane-v6-orchestrator.md` (your rules and templates: verifiers are Opus, cap $10, 30-minute boxes are enough per V6's lesson; the verifier brief template in CLAUDE.md "Workers"; rule 10), `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`, the LANE V6 handoff in full, `.claude/briefs/runs/2026-09-25-lane-v6-qa.md`, QA/2026-09-25-2140.md, NEAR.md, UPDATES.md; `python3 tools/room.py --digest "2026-09-25 21:40"`. ROOM claim "LANE V7: verifiers and rolling QA, V6's successor".

Jobs, in order:
1. clair349-este-guise-1556 (LANE ZX, owner account): ZX-RD349's rule-7 re-derivation PASSed at 21:47 (ROOM). If ZX has posted "reading ready" (or when it does), start the verifier from the clair1067 precedent (the leaf carries an interlinear contemporary decipherment, so N0 on the leaf is the likely class; the audit still logs every family and records key source `period`), Opus, cap $10, 30 minutes; AUDIT.md in ciphers/clair349-este-guise-1556/; post the class to both parents by ROOM line ("for the parent:" and "for the owner-account parent:").
2. The `en` judge corpus (V6-QA3 systemic failure, ROOM 21:48): tools/judge_plaintext.py LANG_CORPORA `en` has two source files, so rule 3's fold-count amendment cannot be met and two verdicts (blitz test 2, mccormick test 4) cite it without a spread. One Sonnet worker, cap $5, 40 minutes: extend `tools/data/en` (or a new `en19` set) to at least six Gutenberg English prose files spanning 1850-1950 (at most 30 fetches, README of sources, public domain only), compute the leave-one-file-out false-negative rate and per-fold spread the way V6-PTCORP did for pt18 (tools/tests entry), write the spread into judge_plaintext.py's corpus comment, and re-run the two cited verdicts' judge lines so their NOTES.md carry the spread; report both old and new numbers. No target decoding.
3. Rolling QA every two hours (next about 23:40 UTC, window from 21:40, output QA/2026-09-25-2340.md, Sonnet, cap $5).
4. Verifiers for any "for LANE V6/V7: reading ready" line (Opus, cap $10, 30 minutes, the V6 templates 2026-09-25-lane-v6-mercy.md and -costa.md).
5. Mercy 1648 gate-2 closer only when the owner answers JSTOR-QUEUE rows 80-83 and the Lonchay-Cuvelier IV page read lands (owner is away until about 03:00 UTC 26 Sept; nothing to do before).
Rules: spawn every worker with create_session giving source_url https://github.com/NoAutopilot/cipher-lab and source_revision main, and a prompt that opens by naming its brief file and the rule that authorises it; cost only from get_session; LEDGER row (ledger_check.py), ASSIGNMENTS, archive_session after every worker; a new AUDIT.md class is posted to the parent by ROOM line and the parent copies it to status.json; rule 10 wording only; no AskUserQuestion. Report to the parent only through ROOM lines with your context estimate.
