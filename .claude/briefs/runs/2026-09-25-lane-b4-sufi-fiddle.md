LANE B4 breadth worker bSUF: sufi-fiddle first test. Sonnet (claude-sonnet-5). Cap $3 or 30 minutes from your first `date -u`, whichever first. No subagents. Parent: LANE B4 orchestrator session_01V2WMavWGMeAUYELUpmyEcX (written 25 Sept 2026 21:34 UTC).
Common rules: `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (read it first; it governs; its INTAKE STEP applies; read "LANE B3" there as "LANE B4") and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Spec: specs/sufi-fiddle.json. Intake step first (breadth.md): no ciphers/sufi-fiddle/ folder exists, so do the minimal check-solved -- the Cipherbrain post under sources/schmeh/ (read its comment thread), one shallow clone of each solver repository (grep, delete), one OpenAlex and one Semantic Scholar query with the keys from the environment (never print them). Create ciphers/sufi-fiddle/NOTES.md in the gate format (status word line 1; line 2 names what was read, with dates), run `python3 tools/intake_gate_check.py sufi-fiddle`, paste the output; proceed only on exit 0.
Then `cheap_tests_in_order[0]` only: fetch Suffi-Fiddle-614.png (scienceblogs.de only -- you are its one holder; at most 4 requests, sleep 2 between requests, browser UA per the common file; image to ciphers/sufi-fiddle/images/ with manifest.json), and assess script identification: is the inscription consistent with a documented script (Arabic-script variants, Baybayin/other Philippine or Indic-derived abugidas, Syriac, Hebrew, a Latin cursive, a pseudo-script)? The matched control for a script-identification judgement: apply the same checklist (sign inventory size, repeats, direction, ligature/joining behaviour, diacritic-like marks) to one known Arabic-script line and one known Baybayin line of similar length (reference images from Wikimedia Commons, at most 2 requests, or from memory stated as such) and report the checklist for all three side by side. One blind sign pass on the target: record each sign with position (sign ids of your own; ciphertext.txt with a rule-2 note: single pass). Write the spec's `cheap_test_done` (N signs, K types, checklist target vs the two references, the verdict in one line: which families it is and is not consistent with). Do not run test 2.
Done line: N/K and the checklist verdict target vs references; say 'scienceblogs.de free' in it so the next holder can start.
