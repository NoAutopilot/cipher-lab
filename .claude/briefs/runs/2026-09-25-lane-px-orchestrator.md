LANE PX ORCHESTRATOR (Portuguese holdings and dictionary codes: scout, check-solved, read). Opus. Opened 25 Sept 2026 by the parent (owner-account orchestrator, session_01FXDfYR3CvGk7tcid1Aav1n) because every copy-free, verified-unsolved target is done or held, and tonight's one new-key result (Linhares: ANTT DigitArq, CC BY-SA images, a dictionary code with its key sheet) came from a vein the earlier scout barely touched: scPARES read only the first ten results per query, because DigitArq's JSON search breaks past ten (QUEUE.md "PARES / DigitArq cipher letters").

Mechanics, exactly as .claude/briefs/runs/2026-09-25-lane-lx-orchestrator.md: rate limit (BUDGETS.md scaling rule), check-ins by send_later every 30-45 minutes, at most 4 live Sonnet workers, stall alarms not budgets, ledgering, the intake gate, job 6 (second opinion), the handoff. Workers get this lane's COMMON (2026-09-25-lane-px-COMMON.md) plus a job brief you file first as 2026-09-25-lane-px-<job>.md. ROOM claim line "LANE PX orchestrator: Portuguese holdings and dictionary codes". Not yours: antt-linhares-chave (LX, closed, and its maço 86 items), every OX, R5, N4, B, V2 target, and anything claimed in ROOM.md in the last six hours.

Jobs:
1. Scouts, two in parallel, copy-free only (images viewable at full size online now; record the tested image URL per row):
   a. DigitArq deep harvest (cap alarm $6): past the ten-result limit, by fonds and by unit description rather than keyword pages (scDIGI2 in QUEUE.md shows the route), across ANTT diplomatic and noble-house fonds (Condes de Linhares, Ministério dos Negócios Estrangeiros, Miscelâneas, Manuscritos da Livraria, family archives of ambassadors), for units whose description or images show cipher (cifra, cifrado, chave, algarismos, decifrado, "em cifra"). DigitArq: one request at a time, >=3 s apart, <=60 per session.
   b. Dictionary and book codes (cap alarm $6): cipher letters or key sheets anywhere copy-free (Gallica, BNP Biblioteca Nacional Digital purl.pt, Europeana, Nationaal Archief, the repo's own QUEUE.md and CATALOG.md rows) whose key names a printed dictionary or book ("dictionnaire", "diccionario", "dictionary", page/column/line groups); list what the key says and the candidate editions.
   Each scout writes rows to a new QUEUE.md section "Portuguese holdings and dictionary codes (LANE PX, 25 Sept 2026)", PX-01 onward, never re-listing a row already in QUEUE.md.
2. Check-solved on every copy-free row (Sonnet, alarm $5 per batch of four), per .claude/briefs/check-solved.md and the intake gate: the verdict names the edition and pages read, or it is `blocked`. Only `open` rows go on.
3. Read the open rows, recovery first: key sheet, sibling decipherment or identified book -> two-pass transcription -> decode with per-token grades, judge output, fresh-instance re-derivation. A dictionary code is tested the way LX-BOOK did it: the key's worked example or any known-plaintext groups against candidate scans, page and column exact. Cryptanalysis only with a matched control, one attempt per target.
4. Verifier per reading (separate session, .claude/briefs/verifier.md, with the key source), then job 6.
For the parent: the scouts' counts (rows, copy-free, open after check-solved), each reading that passes its judge, each AUDIT.md.
