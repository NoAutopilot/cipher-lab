JOB GOLD-K3: Köhler family B'-c, the cipher-side placements of the keyword-mixed tableau (never run by anyone), control first each time, plus the 10-text beau noise band in the background. Sonnet (claude-sonnet-5). Stop and push at $6 or 75 minutes, whichever first. NO subagents. Lane: LANE GOLD2 orchestrator session_013JXDgLDkW2y5Ldi2gWzTkY (Fable). Written by the cycle-2 consolidator GOLD-CONS2 (Fable, session_01SPixjGTp23T4YyxPvHwkWs), 25 Sept 2026 21:16 UTC; the orchestrator fills the gate line and the spawn time.

INTAKE_GATE_OUTPUT (live, 21:38 UTC): koehler-1944: open (line 1) -- edition/page or full-text-search citation found within 6 lines (exit 0). Spawned 25 Sept 2026 21:38 UTC by LANE GOLD2.

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), the top block ("Summary, cycle 2") and the sections "Family B', keyed-tableau running key (GOLD-2C)" and "Family B' variants (GOLD-K2)" of `ciphers/koehler-1944/HYPOTHESES.md`, `python3 tools/family_run.py --help`, the docstring of `tools/families/keyed_running_key.py`, `running_key.mixed_tabula` (tools/running_key.py, about line 112) and `tools/tests/test_keyed_running_key.py`. Claim in ROOM (`tools/room.py`). Read the clock with `date -u` before any date.

What the last round learned, pasted so you do not rediscover it:
- Family B' = a running (book) key through a mixed tableau c = S3(S1(p) + S2(k)). `mixed_tabula` builds S1, S2, S3 from ONE keyword-mixed alphabet M in four modes: plain (S1 = M), key (S2 = M), both (S1 = S2 = M), full (S1 = S2 = S3 = M). Every run so far (GOLD-2C, K1, K2: vig and beau, Dutch novel, Dutch Bible and German key models) used those four modes; every control cleared its gate (72.6 / 78.1 / 89.6 / 84.8 pct mean plaintext letters recovered) and every target sat at noise (pooled joint ll per letter -3.534 / -3.478 / -3.573 / -3.583 against the two-text uniform band -3.507 / -3.544), judge FAIL each time.
- Not among the four modes, and not among Bourdeau's six (his test 4 assumes the identity on the cipher side): a mixed alphabet on the CIPHER side alone, c = M[(p + k) mod 26] with standard plain and key letters -- the rows of the square filled with a shifted mixed sequence, the commonest practical way a Vigenere square was mixed -- and the two pairs {plain, cipher} and {key, cipher}. Its sorted letter profile is the STANDARD band's, where the target sits at percentile 0.16 (GOLD-2C step 1), so nothing in the statistics excludes it. This job runs those three placements.
- K2's beau/Dutch-key target read -3.478, 0.029 nats above the higher of only TWO uniform-text noise draws (-3.507, -3.544). A two-draw band has no tail. This job widens it to ten texts for the beau pipeline so the record reads the number against a real band. It is bookkeeping, not a lead: the controls' true tableau stands 0.35-0.5 nats above noise, and the beau excess is under a tenth of that.
- Decoder facts: deterministic on the target (one run per configuration; seeds apply to controls and noise). Memory: at order 6 / beam 300 run at most THREE decoder processes at once. Never `git rebase --autostash` or `tools/room.py --push` while a decoder writes a log inside the tree: log to your scratchpad and copy in after the process exits. Each family_run variant (3 control seeds + target) took K2 about 25 minutes with contention; budget on that.

Do, in this order:
1. Tool change (aim 15 minutes). In `tools/running_key.py` add three modes to `MIXED_MODES` and `mixed_tabula`: `cipher` (S3 = M, S1 = S2 = identity), `plaincipher` (S1 = M, S3 = M, S2 = identity), `keycipher` (S2 = M, S3 = M, S1 = identity); S3 = M means `S3[s] = IDX[M[s]]` exactly as mode `full` does it. Keep the default `modes=plain,key,both,full` in `tools/families/keyed_running_key.py` unchanged so every earlier row still reproduces; the new modes are selected with `--param modes=...`. Check that `keyed_running_key.stage1` derives the predicted letter distribution from the tabula dict generically (through `enc`) and not from the mode name; if it special-cases modes, fix it so all seven modes go through one path. One offline case in `tools/tests/test_keyed_running_key.py`: `mixed_tabula('grube', 'cipher')` is the standard vig table with M applied to the output (compare against `encipher('vig', p, k)` mapped through M for all 676 cells) and `key_of` inverts `encipher` for the three new modes. Run the test file; push the tool change on its own commit before any run.
2. Background, started right after step 1 and read at the end: the beau noise band, ten texts through the full beau pipeline with the SAME settings as K2 variant 1 (arith=beau, kcorpus=tools/data/nl20, top=30, beam=300, order=6, spaces=1, modes default): six uniform-random texts of the five target lengths (seeds 1-6) and four shuffles of the target's own letters (seeds 1-4; a shuffle keeps the target's letter counts, so stage 1 ranks identically to the real target and stage 2 is the only difference -- the sharper of the two floors). Reproduce GOLD-2C's procedure for a pipeline-noise run (`ciphers/koehler-1944/running-key/keyed_pipeline_noise_1.out` shows the output shape; call `families.keyed_running_key.solve` on synthetic messages from a short script `ciphers/koehler-1944/scripts/keyed_noise_band.py`, not `family_run.py`, which would rerun the control every time). At most three processes at once. Output: one line per text with its pooled joint ll per letter and the winner's name, to `running-key/keyed_beau_noise_band.tsv`.
3. Family runs, sequential, control first, through `tools/family_run.py` (gate 0.5 on the control's mean plaintext-letter recovery over 3 seeds; a variant whose control fails its gate is NOT run on the target: the tool writes CONTROL BELOW GATE, say so, move on):
   a. `python3 tools/family_run.py specs/koehler-1944.json --family keyed_running_key --corpus tools/data/de20 --param kcorpus=tools/data/nl20 --param modes=cipher --param arith=vig --param top=30 --param beam=300 --param order=6 --param spaces=1 --seeds 3 --gate 0.5 --label "GOLD-K3 B'-c cipher-side mixed alphabet, vig, Dutch key, control before target"`
   b. the same with `--param arith=beau`, label "GOLD-K3 B'-c cipher-side, beau, Dutch key".
   c. the same as (a) with `--param modes=plaincipher,keycipher`, label "GOLD-K3 B'-c plain+cipher and key+cipher, vig, Dutch key". Run (c) only if at least 20 minutes of the box remain after (b); otherwise log it as "not run, owed" in the section and the done line.
   The control's keyword and mode are drawn from the modes given, so each control tests exactly the placement set its target run searches.
4. Append one section "Family B'-c, cipher-side placements (GOLD-K3)" under the append-only part of `ciphers/koehler-1944/HYPOTHESES.md` (the family_run rows land in the table by themselves): per variant the control's three numbers, the target's stage-1 best (nats over uniform) and stage-2 best-of-30 on message 1, the winner's pooled joint ll per letter against BOTH bands (GOLD-2C's two-text -3.507 / -3.544 and your ten-text beau band: min, median, max, listed in full), the judge line, and the first 40 letters of both decoded streams of message 1 (documenting noise, never as a reading). Then one paragraph reading K2's -3.478 against the ten-text beau band (above its maximum by how much, or inside it), and the same for your own beau target. One line per variant in `specs/koehler-1944.json` `cheap_test_done` with both numbers; one line in NOTES.md "Next steps".
5. A candidate reading exists only if `tools/judge_plaintext.py specs/koehler-1944.json --file <decode>` prints PASS: paste it, write "for LANE GOLD: judge PASS on <variant>, re-derivation owed" in ROOM, stop. A target pooled joint ll above -3.40 (0.1 nats above the two-text band's top) or above the ten-text band's maximum by 0.1 nats, without a PASS, is a ROOM `flag:` line ("flag: GOLD-K3 <variant> target -x.xxx above the noise band, judge FAIL").

Hosts: none (disk and CPU only). Requests per host in the done line: 0. Subagents: none.
Done line (ROOM, starting `done: for LANE GOLD: GOLD-K3`): per variant "control a/b/c pct (gate 0.5 met/not), target pooled ll x vs two-text band -3.507/-3.544 and ten-text beau band min/median/max, judge PASS/FAIL"; the ten-text band's three numbers and K2's -3.478 read against them; variants not run and why; files touched; no cost figure (the orchestrator reads it); no reading described unless the judge passed. Rule 10: never solved, new, first, unpublished; NOTES.md status stays `open`.
