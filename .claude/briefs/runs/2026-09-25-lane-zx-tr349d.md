ZX-TR349D (Opus; wall-clock box 100 minutes, also a minimum working time; up to 4 subagents at once, ALL on Opus (model "opus"): CLAUDE.md Usage 1 tiers reconciliation and key reading to the strongest model, and three Sonnet blind passes on this leaf failed at 39-42 pct while an Opus read at native zoom was clean). Target: ciphers/clair349-este-guise-1556. Parent: LANE ZX orchestrator session_01MxueEQJUGF9PWJiYcVyvBM. Kind: recovery (key `period`, fr.20974, plus the contemporary interlinear decipherment on the leaf). Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md; its "subagents on Sonnet" line is overridden for this job only), .claude/briefs/transcription.md, NOTES.md sections "ZX-KEY349" and "ZX-TR349C" in full, images/atlas/atlas.tsv. Intake gate: `partial`, exit 0 (orchestrator 15:44 UTC 25 Sept 2026). No network.
1. Crops under 2500 px. Recut the 33 lines as the two native segments per line (tools/iiif_lines.py with --top-margin 45 and the default --max-width, NOT stitched), so each crop is under 2500 px wide, with the overlap between s1 and s2 recorded so a token is counted once (write the overlap x-range into images/manifest.json, and tell the passes to stop s1 at the overlap boundary). Replace images/lines_g with images/lines_h/lineNN_s1.jpg, _s2.jpg; keep images/ under 30 MB. Eye-check lines 1, 17 and 33.
2. Two blind passes, each done by two Opus subagents (lines 1-17 and 18-33; four subagents total, the two passes never seeing each other's output), given only the lines_h crops, images/atlas/sheet.jpg and atlas.tsv, and the ZX-TR349C rules (z-curl is digit 2; `|` word divider as its own token; atlas code or X? with a description; S34/S69/S76 kept apart; the small letters above a code go in the `gloss` column of that token's row; larger marginal plain words are kind `plain`, not cipher; skip stamp and "2429"/"3"). Tell each subagent to zoom (crop sub-regions of its crop at 2x) wherever a mark is small. Wait for each subagent's completion report before committing. Write passG.tsv and passH.tsv (same columns as passE.tsv, plus kind `plain`).
3. `python3 tools/reconcile_passes.py passG.tsv passH.tsv --crops images/lines_h --rows` (use --halves if it fits the s1/s2 naming). Report pooled and per-line token agreement against the 60 pct gate, beside 39.3 and 42.0, and gloss agreement.
4. If the gate passes: settle every disagreement yourself from the image (grade M), promote to ciphertext.tsv with a gloss column, and rerun key_vs_gloss.py on the full agreed-gloss set; report match share per code. Then stop: the decode is the next job. If the gate fails: settle lines 1-5 by hand from the image anyway, report whether the settled lines agree with the key via their gloss, and stop with the per-line table.
NOTES.md gets one section "## ZX-TR349D (25 Sept 2026)". Status stays `partial`. Touch only ciphers/clair349-este-guise-1556/** and ROOM.md (tools/ only to add an option with its test passing, logged). Push after each step. Report what was found and where it was not found; do not classify novelty; write "cost: see the lane ledger". Never call AskUserQuestion.
