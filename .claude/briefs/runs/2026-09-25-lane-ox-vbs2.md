OX-VBS2 solver, successor to OX-VBS (Sonnet, cap $10 as a stall alarm, at most 2 subagents). Parent: LANE OX orchestrator, session_01BE3g8tWbS4T24KXMpShHt4. COMMON: .claude/briefs/runs/2026-09-25-lane-ox-COMMON.md applies in full; .claude/briefs/solver.md.
Target: ciphers/vanbeuningen-dewitt-1657. Read NOTES.md's "Key recovery, 25 Sept 2026 (LANE OX solver OX-VBS)" first. OX-VBS identified the system (clear Dutch function words + a 9-code nomenclator + a homophonic letter alphabet, codes ~4-65, comma-separated, colon at word end) and keyed only 13 letters / 25 codes; most codes are still unkeyed although the whole plaintext is in hand (plaintext_print.txt). That is the gap to close.
Job: key every code the known plaintext can fix.
1. Write align.py (committed in the folder): tokenise ciphertext.tsv into clear words and coded words (colon-terminated runs), tokenise plaintext_print.txt into words (normalise u/v, i/j, ij/y, case), and align the two sequences with the clear words as hard anchors (dynamic programming over word sequences; allow the two copies to differ by insertions/omissions -- they are in different hands). Between anchors, each coded word maps to one plaintext word or name: accept a letter-level mapping only when the code count equals the plaintext word's letter count (or a documented digraph/omission rule explains the difference), or a nomenclator code for a whole word/name.
2. From all accepted pairs, build key.tsv by voting: each code's value, its support count, grade C when >=2 independent words agree, M when 1 word or a minority conflict exists; list every conflict with the words that caused it (conflicts.tsv). Resolve code 40 and the L04 "Staet" issue from the data, or state why not.
3. Regenerate reading.txt via decode.json and `tools/decode_key.py ciphers/vanbeuningen-dewitt-1657 --check` (exit 0). Report coverage: coded tokens keyed / total, grade counts (rule 4), and the words where the decoded cipher copy differs from the print (a table, not corrections).
4. Fresh-instance re-derivation (rule 7): a subagent that sees only ciphertext.tsv, plaintext_print.txt and the one-paragraph system description (not key.tsv) runs its own alignment and keying; report per-code agreement. A difference larger than the M-graded codes sends the key back: say so plainly.
5. NOTES.md section "Key recovery round 2"; status stays partial unless every coded token is keyed at C.
No host fetches. Report what was found and where it was not found; no novelty wording. Files: ciphers/vanbeuningen-dewitt-1657/**, ROOM.md.
