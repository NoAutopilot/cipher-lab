LANE B breadth worker: somerton-1948. Sonnet (claude-sonnet-5). Cap $3.
Common rules: `.claude/briefs/runs/2026-09-24-lane-b-common.md` (read it first; it governs).

Spec: specs/somerton-1948.json. Run `cheap_tests_in_order[0]` and its matched control, nothing else. Write both numbers into the spec's `cheap_test_done` (with date, method, cost). Judge any candidate plaintext with `tools/judge_plaintext.py` and paste its output. Cap $3. Report in five lines; do not run test 2; rule 10 wording; a negative's done line carries the target and control numbers side by side.

Note from the orchestrator: the survey ran the spec's test 3 and names 'establish the 1941 Whitcombe & Tombs edition's contents' as next, which is cheap_tests_in_order[0]; run that. Append to cheap_test_done (keep the survey's entry; make it a list). Hosts, one at a time: api.trove.nla.gov.au or trove catalogue pages (Trove API needs a key; if none, the catalogue HTML once), archive.org metadata/fts, natlib.govt.nz catalogue; at most ~20 requests per host. If the preface-bearing edition is established, rerun specs/cheap-tests/somerton_rubaiyat.py on that exact text only if it is available as text without a new host; control numbers from that script. If the edition cannot be established, that is the result: a blocked line with what was searched.
