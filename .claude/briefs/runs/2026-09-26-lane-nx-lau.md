LANE NX job NX-LAU (Sonnet). Parent: LANE NX orchestrator, session_01RDXRYmATas1Xy6K1Jjwtvo. COMMON rules: .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md, read it and follow it, with "LANE NX" for "LANE AX" and lane goal "copy-free candidates with a key route, one control-backed cheap test each".
Target: ciphers/fr3625-lauriere-1593 (BnF fr.3625 no.55, f.66r, Lauriere to Nevers, Chalons, 9 July 1593; ark btv1b52511322v, canvas 143). Intake gate run by the orchestrator 08:45 UTC: `fr3625-lauriere-1593: open (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.
Box: 100 minutes wall clock from your first `date -u`; stall alarm USD 12 (6 transcription calls at about USD 1.5 each = 2 pages x 2 passes + margin, plus alignment). Stop before starting a unit that would cross 80 percent of the box.
Files you may touch: ciphers/fr3625-lauriere-1593/** , specs/fr3625-lauriere-1593.json (new), ROOM.md.

Steps, in order:
0. Intake extras (rule 1, 10 min max): shallow-clone github.com/dbourdeau/cyphersolver at its latest commit and read champagne1590/ (NOTES.md, key_lauriere.txt, any transcription of no.10 and no.55); record the commit hash. If his folder now reads more than the 19 of 86 groups on file, STOP and flag: with the hash. Also grep aaymeloglu/unsolved-ciphers (shallow clone) for 3625/Lauri. Credit Bourdeau (MIT code / CC BY text); copy his transcription files only with attribution in the header.
1. Images: sibling no.10 (f.10r, Lauriere to Nevers, 13 July 1593: seven interlinear-glossed runs) and no.55 (f.66r, canvas 143). `python3 tools/gallica_folio.py btv1b52511322v --folio 10` to get f.10r's canvas; then cut line crops with `python3 tools/iiif_lines.py --ark btv1b52511322v --canvas N --region x,y,w,h --out ciphers/fr3625-lauriere-1593/images --debug` and check the debug overlay. Paste both commands' output into NOTES.md. Transcription subagent calls get crop paths only, never a full leaf (CLAUDE.md Usage 6: a full-page argument is an error).
2. Transcribe: for no.10, each glossed run as (cipher groups, the interlinear gloss above them, letter by letter as written); for no.55, the 86 cipher groups in their eight runs with the clear words either side. Two blind passes (Sonnet subagents, at most 2 at once), compared by content alignment (difflib), not position; reconcile disagreements from the crops yourself. Where Bourdeau's transcription exists, report agreement with it as a third witness, never as a pass.
3. Align (grade C, no cryptanalysis): build PAIRS.tsv from no.10's runs in tools/interlinear_align.py's format and run `align` (try --floor to suit this nomenclator: read its value range first and state the floor you chose and why in NOTES.md; seed with --prior from Bourdeau's 12-value table only as a second run, and report both). Output key_align.tsv (value -> meaning, count, agreement).
   Control, gate written in NOTES.md BEFORE the run: (a) known-answer: leave each of the seven glossed runs out in turn, align on the other six, and predict the held-out run's gloss; report per-run exact-chunk accuracy and the mean; gate mean >= 0.60 on values attested at least twice in the other six (report how many such values each held-out run has -- if fewer than 5 in total across runs, the control cannot fail and is a non-test, say so). (b) shuffled pairing: re-run align with the seven glosses permuted across runs (10 permutations); its leave-one-out accuracy must fall well below (a)'s. Report both numbers side by side.
4. Apply: only if (a) meets its gate. Write key.tsv for no.55 from key_align.tsv (graded C for gloss-attested values; anything else M), a decode.json (see tools/tests/decode_configs/), and run `python3 tools/decode_key.py ciphers/fr3625-lauriere-1593 --check`. Report per-token grade counts (H/C/S/M/I) and how many of the 86 groups now read vs the 19 on file. Shuffled-value control: 20 shuffles of the key's values over the same 86 groups, French-word share (or the judge score) target vs shuffle mean and max, gate written before (target above shuffle max). Write specs/fr3625-lauriere-1593.json (ciphertext as transcribed with source/date, alphabet, the judge block with language fr and corpora tools/data/fr16) and paste `python3 tools/judge_plaintext.py specs/fr3625-lauriere-1593.json --file <reading>` output into NOTES.md. Report fr16's per-fold spread if tools/data/fr16/README.md gives it (or say it is not on file); with only a few read words the judge may be uninformative -- say so, never soften a FAIL.
5. Stop. Do NOT run a re-derivation yourself (the orchestrator sends a fresh instance). Do not classify novelty. NOTES.md gets a dated "NX-LAU" section: what was found, where it was not found, both numbers for every control, and one next-step line. The "LANE R5: below unicity" caveat applies to a blind anneal only: do not run any blind cryptanalysis on the 67 unread groups.
Finish per COMMON (one paragraph; first line the answer).
