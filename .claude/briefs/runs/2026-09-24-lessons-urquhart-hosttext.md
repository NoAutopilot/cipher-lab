LESSONS INGEST (parent worker; Sonnet; cap $3). Started 24 Sept 2026 20:38 UTC at the owner's request: ingest the transferable method from Vals AI's post "Claude Fable 5.1 Solves the Cyphral Distich" (31 Aug 2026, https://www.vals.ai/blogs/fable-solves-cyphral-distich), the solve that LANDSCAPE.md line 16 already cites as the project's starting point.

Steps: `date -u`; `python3 tools/room.py --start`; ROOM claim "lessons: Urquhart host-text method -- files sources/vals-ai/, LESSONS.md, .claude/briefs/check-solved.md". Fetch the post once (browser User-Agent) to `sources/vals-ai/2026-08-31-fable-solves-cyphral-distich.html` plus a text copy via tools/html2text.py; read the repository's own `ciphers/urquhart/NOTES.md` if it exists so the lesson cites what we already know. Then:
1. LESSONS.md, new dated section "24 September 2026: a printed cryptogram's key is often the host text (Urquhart 1653, read by a model on 31 Aug 2026)", at most 20 lines, with the three transferable rules and how each maps to our pipeline: (a) before any substitution or frequency work on a cryptogram printed inside a book, test the book's own structure as the key: count things (sections, stanzas, lines, words) that match the cipher's counts, and try index-into-text rules (the i-th number is a word index into the i-th section, take the first letter); (b) a solution that is self-verifying by form (exact length per line, rhyme, metre, a sentence in the period's language) is grade C evidence without a key source, and should be stated with the form checks that verify it; (c) persistence over simple hypotheses beat 370 years of frequency analysis, so the printed-ciphertext detector's candidates (the 23 unswept Thurloe passages, STATUS lane table) get a host-text-as-key pass before a solver. Cite the post, its author's name as printed, the numbers (64 numbers, 32 Proquiritations, 44 minutes, 176k tokens) and Schmeh's list.
2. `.claude/briefs/check-solved.md`: add one line to the web-search source: model-solve announcements (Vals AI's blog, the AI labs' and evaluation companies' posts) are a source family for "already solved" since September 2026; search the cipher's name with "solves" and "Claude" or "GPT".
3. `.claude/briefs/detector-test.md` (if it exists; else QUEUE.md's detector section): one line, "host-text-as-key pass before solver".
Push with `MSG="lessons: Urquhart host-text-as-key method (Vals AI, 31 Aug 2026); model-solve posts as a check-solved source" python3 tools/room.py --push <paths>`; ROOM done line; five-line report; stop. Requests: vals.ai 1.
