PARENT WORKER RETRO-APPLY-T (Sonnet, cap $6 of usage, wall-clock box 40 minutes, no subagents, no network except git; the container is a depth-50 clone). Job: apply the brief- and tool-only proposals of RETRO-2026-09-26h.md (retrospective t, 26 Sept 2026 13:53 UTC) for parent 7h (session_01GT3Yv4kUZrBR8pc2s5EToH, "LIVE parent 7h · Orchestrator 9"). Authorised by CLAUDE.md "Improvement loop" (the orchestrator applies changes that only touch briefs, tools or workflows; anything that changes the goal, the spend or the owner's asks goes to ASKS.md). Written 26 Sept 2026 at the 14:35 UTC clock read.

First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker RETRO-APPLY-T (Sonnet, <session id>)`; read CLAUDE.md, RETRO-2026-09-26h.md in full, then each target file before editing it. Edits minimal, in the file's voice. Proposals 1 and 2 (LEARN4's editor's-note phrase sweep into scout.md/check-solved.md/print_check.py; LEARN-8's EMLO manifestation-level Solr `bibo_Note` sentence into check-solved.md) were applied by the owner account's RETRO-APPLY-LEARN4 at 13:52 UTC (its ROOM done line and UPDATES.md rows of 13:52): diff what landed against the retro's proposed text and fill only a gap, never a second copy; if nothing is missing, say so in the done line. Apply proposals 3, 4 and 5 as the retro's diffs, in order: (3) a calibration step that fails its own tolerance ends a symbol-match box before any candidate is scored (ARM-S3: Taylor frequency drift 0.211 against 0.1, then four candidates scored at USD 12.67 against a 6 cap) -- the CLAUDE.md sentence and the brief line the retro names, plus any mechanical check it specifies; (4) tools/print_check.py on decoded phrases as a precondition of a "reading ready" ROOM line (V9-MOR found the Morillo plaintext in print that way) -- the README.md common-tail line and, if the retro specifies one, the check in tools/intake_gate_check.py or tools/room.py with its test; (5) tools/room.py refuses (not warns) a typed clock time in a signal starting "took over" or "handoff" that differs from the stamp, per the retro's diff, with the test block in tools/tests/test_room_warnings.py. One UPDATES.md row per instituted change (`By` = "RETRO-APPLY-T for parent 7h", clock-read time). Never touch BUDGETS.md or rate-limit rules; never touch status.json or any ciphers/ folder. Run every tools/tests/ file you touch directly (pytest is not installed). Commit by explicit path, `python3 tools/room.py --push <paths>`; ROOM done line `done: RETRO-APPLY-T: N of 3 items applied (...), items 1-2 verified landed/gap filled, tests <pass/fail>, UPDATES rows M`; one-paragraph reply; stop. Never call AskUserQuestion; never print or commit credentials.
