PARENT WORKER DECODE-ACCESS (Sonnet, cap $4 of usage, wall-clock box 40 minutes, no subagents). Job: establish whether DECODE's full-size image block on our account (ASKS.md row 42; sources/decode/NOTES.md "Full-size images: account permission test") is a per-record holding-archive restriction or an account-wide role, and fix the addressee of the access request, whose email bounced (owner, 26 Sept 2026 about 04:30 UTC). Authorised by CLAUDE.md Access playbook item 3 (DECODE) and the good-citizen rule. Written 26 Sept 2026 at the 04:36 UTC clock read by parent 7e (session_01SvjMDFfJZJ3uK47RYxAQrM).

Finding to test: every DECODE record page carries an "Access mode" field. R1601 (Vienna HHStA) and R3754 (Florence, our blocked test record) both read "Authentication required", and R1601 adds "The image is not in the public domain. Publishing it is only possible with the permission of the archive." So the block may be tied to the holding archive's terms, not to our account.
1. `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker DECODE-ACCESS (Sonnet, <session id>)`. Read sources/decode/NOTES.md sections on the permission test and the login tool, ASKS.md row 42, outreach/decode-image-access.md.
2. Login-free (no credentials): with tools/decode_list.py or plain curl at >= 2 s apart, at most 40 requests to de-crypt.org, read the "Access mode" field on a sample of record pages: the five targets ASKS 42 gates (R2077 Marie de Medicis key, R9634 Lope Hurtado, Barb.lat 6956 records, ASV doss. 346 bagno1652, the Barriere Add MS 4200 records 8395/8398, plus 3754 and 8725) and about 15 records with a different value if one exists (search the listing for "Public" or "Open" access modes). Tabulate record, holding archive, access mode, public-domain note into sources/decode/access-modes-2026-09-26.tsv.
3. One browser login only (`NODE_PATH=$(npm root -g) node tools/decode_browser_login.js RECORD_ID OUT_DIR`, the single-attempt rule): pick ONE record whose access mode is NOT "Authentication required" (public/open) and fetch its full-size image; then ONE that is "Authentication required". Record whether each serves the real image or the forbidden placeholder (sha1 035489a0605851154ab88372216354b63596ca22). Scrub the account name from any saved page before committing; never print credentials.
4. Read https://de-crypt.org/termsofuse.php and the site footer for the published contact route (the terms name decode@stp.lingfil.uu.se, an Uppsala mailbox; the project's PI is now at Stockholm University and the system administrator is named on the front page). Write the finding into sources/decode/NOTES.md (new dated section), update ASKS.md row 42's next-step column (what the block is, which published address is dead, which institution's public contact page to use -- put an institution's public contact address on the recipient line of outreach/decode-image-access.md with the date read; never a private individual's address in the file), and add one line to CLAUDE.md's DECODE host row only if the verdict changes it ("per-record archive restriction" vs "account role").
5. Rule 10 wording; no other target; `python3 tools/room.py --push <paths>`; ROOM done line `done: DECODE-ACCESS: verdict <per-record | account-wide | undecided>, public-mode image <served|blocked>, auth-mode image <served|blocked>, N records sampled, addressee <found|not found>`; one-paragraph reply; never call AskUserQuestion.
