PARENT WORKER D1-CHECK (Sonnet, wall-clock box 30 minutes, cap USD 3, at most 1 subagent; network: www.googleapis.com/books (with &country=US and the GOOGLE_BOOKS_KEY from the environment, test -n only, never print it), books.google.com (one fetch per page with a Chrome User-Agent, one retry, stop at a captcha), archive.org, api.europeana.eu, bdh.bne.es, gallica, HathiTrust bibliographic API only (babel page images do not work from the cloud), Tomokiyo's cryptiana pages; 1.5 s between requests). Job: settle SCOUT-OWN-3's D1 (read SCOUT-OWN-3-2026-09-26.md, candidate table and Recommendation, first). Edition: Lettere di D. Giovanni d'Austria a D. Giovanni Andrea Doria I (1568-78), ed. Alfonso Doria Pamphilj, Rome, Forzani, 1896, 108 pp., Spanish; Google Books ids 9-ZcAAAAcAAJ, c-GJNFDx2tUC, QzIoAAAAYAAJ (public domain, all pages viewable). The editor annotates ciphered passages inline: "(cifra descifrada)" recurs and at least p.90 reads "(cifra no descifrada)". The one question: does the edition PRINT any cipher groups (digits, symbols, numeral runs) anywhere, or only the notes? Method, in order: (1) Google Books API phrase queries that would only hit digit-bearing passages in this volume ("cifra no descifrada" plus q terms like "Doria" and neighbouring words from the p.90 snippet; also q=cifra with inauthor/intitle filters) and read every textSnippet; (2) the books.google.com text view for p.90 and its neighbours (output=text, pg=PA90 etc.), one attempt and one retry each, stopping at any captcha wall (the scout hit one); (3) a second digitisation of the same 1896 edition or of the same letters elsewhere: archive.org full-text search (not just title), Europeana, BDH, HathiTrust catalogue (record the HathiTrust id for a LOCAL-QUEUE row if only that copy is readable); (4) the check-solved side: are these letters printed elsewhere with the cipher resolved (CODOIN, Colección de documentos inéditos; the Doria archive editions; Fernández Duro, Armada Española)? grep what you fetch for the p.90 letter's date and correspondents. Key route to record if digits exist: Philip II's general and particular ciphers of 1556-1580 are published (Devos 1950, Tomokiyo's Spanish cipher pages); name which one the Don Juan-Doria channel would use and whether the table is on file under sources/. Deliverable: ciphers/_triage/doria-donjuan-1568.md (new; do NOT create a ciphers/ target folder) with: verdict YES digits printed (pages, sample groups, estimated sign count, key route, cheap test + control + gate for a lane) / NO notes only (close D1) / UNRESOLVED (exactly what a person must do, as a LOCAL-QUEUE.tsv row in the file's own format); every request and hit count; no novelty wording (rule 10). Post a ROOM claim line first and a done line at the end (first line: the verdict) as `parent worker D1-CHECK (Sonnet, <session id>)` with tools/room.py (two positional arguments: role, text); add your LEDGER.md row; commit, fetch and rebase, push (never force). Read clock times with date -u. Never call AskUserQuestion; the owner is asleep.
