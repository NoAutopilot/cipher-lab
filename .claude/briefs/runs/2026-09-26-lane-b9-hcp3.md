JOB bHCP3 (LANE B9, Sonnet, cap USD 3, wall-clock box 45 min; hosts: www.gutenberg.org only, >= 1.5 s apart, descriptive UA, at most 12 requests): hessen-1824 -- running_key with an era-matched de19 corpus and a wider beam, control first at N=164.
Intake gate (orchestrator 08:20 UTC): `hessen-1824: partial (line 1) -- ...`, exit 0. Paste your re-run; nonzero = stop.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b9-common.md` (it governs); ciphers/hessen-1824/NOTES.md (bHCP, bHCP2: running_key control 0.372 vs gate 0.6 on de20, a non-test); specs/hessen-1824.json; tools/data/pt18/README (how an era corpus was built) and tools/data/en/README.md (per-fold spread check); CLAUDE.md rule 3's corpus-era and fold-count paragraphs.
Units, serialized (never two CPU jobs at once, Usage 6): U1 build tools/data/de19 from 5-8 German prose texts first printed 1800-1840 on gutenberg.org (prefer non-fiction or letters -- e.g. Kleist, Heine's Reisebilder, Goethe's Italienische Reise, Humboldt, Clausewitz if present -- name each file, its first-print year and source URL in de19/README.md), normalized the way de20 is; ~12 min. U2 leave-one-file-out false-negative check of judge_plaintext with de19 (per-fold rates and the blended rate; flag if under 5 files or spread > 3x). U3 `tools/family_run.py specs/hessen-1824.json --family running_key --corpus tools/data/de19 --param beam=<wider than bHCP2's> --control-only --seeds 3` first; state bHCP2's beam and yours. Only if the control mean >= 0.6: run the target the same way. If the control stays below gate at the widest beam the box allows, stop: running_key is untestable at N=164 with this solver; say so.
Degenerate optimum to avoid: a running key drawn from the same corpus the judge scores against can read "German" from any key text; report the target's best key offset and whether the decoded span is word-for-word inside a corpus file.
Output: NOTES.md section "running_key de19 (bHCP3)"; HYPOTHESES.md rows (family_run writes them); tools/data/de19/. ROOM done line <= 600 chars: corpus files, fold spread, beam, control mean vs target. Never write closed-negative. Do not edit NEAR.md. Final reply at most five lines.
