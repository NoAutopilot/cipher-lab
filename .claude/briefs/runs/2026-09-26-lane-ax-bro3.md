LANE AX AX-BRO3 -- antt-msliv0638-brochado-1712: the NEAR.md named step for letter 134 -- more observations of its thin codes from the rest of the letterbook, then re-score (Sonnet, wall-clock box 75 minutes; also a minimum). Parent: LANE AX orchestrator session_01VzK62xX92yKfnUD93zprD8. Read and follow .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md and the common tail of .claude/briefs/README.md (both override anything below they conflict with), CLAUDE.md in full, the last 20 lines of UPDATES.md, and NEAR.md. First: `date -u`; `python3 tools/room.py --start`; last 30 lines of ROOM.md; if ROOM.md shows a "retrospective ... starting" with no matching "done", wait before claiming. Claim with tools/room.py before other work; never AskUserQuestion; no dollar figures for yourself.

Read the folder NOTES.md sections from "## The leaves" onward, especially PX-BRODEC steps 4-6, YX-PTJUDGE, ZX-BRO and ZX-BRO2 (its "Step 3: verdict" and "Not done" list), plus key.tsv, ciphertext_appendix.tsv, body_letter_map.tsv, body_leaves.tsv, judge_bucket.tsv and scripts/15_judge_bucket.py. NEAR.md row: letter 134's candidate scores at the 5th percentile of the appendix's own 40-70-letter bucket under pt17 and pt18; 19 of its 70 tokens ride on codes with 1-3 observations (x, z, d, f, 16, 9 -- confirm the exact list from YX-BRO79 step 4 before starting).

Steps.
(0) `python3 tools/intake_gate_check.py antt-msliv0638-brochado-1712` (exits 0 at 00:18 UTC); paste its output.
(1) Pools first: from body_letter_map.tsv / body_leaves.tsv and the leaf images on disk (images/, DigitArq if a leaf is missing: tools/digitarq_fetch.py, >=3 s apart, <=40 requests), list every letter in the MSLIV 0638 letterbook that carries coded runs in the same dictionary code (same code shape as letter 134 and the appendix). Table: letter, leaf, number of coded tokens, and how many occurrences of each thin code it carries (find them by eye on crops or from any existing body transcription; body_passA/passB/agreement exist for some leaves).
(2) Transcribe only the coded tokens in the immediate context (the token itself plus two either side) of each thin-code occurrence found: crops, two blind Sonnet subagent passes (one leaf per call), reconcile_passes.py, 60% gate. Do not transcribe whole letters.
(3) For each new occurrence, decide from context (the surrounding decoded tokens under key.tsv plus any clear Portuguese on the leaf) whether the key's current value fits, a different value fits, or context cannot tell; log each as a row in thin_codes_attest.tsv (code, leaf, context tokens, decoded context, verdict, why). Do not change key.tsv unless >=2 independent new occurrences agree on a different value; if you change it, regenerate the reading with the folder's decode route and --check.
(4) Re-score letter 134 under pt17 and pt18 with ZX-BRO2's whole-bucket control (scripts/15_judge_bucket.py; same bucket definition), and also the two spans separately with a scratch spec (letters_min lowered, stated as outside the spec's range). Report percentile of letter 134 in the bucket before and after, both corpora, side by side.
(5) NOTES.md section "## AX-BRO3: thin-code attestation (26 Sept 2026, LANE AX)"; update the target's NEAR.md row text only through a ROOM line "for LANE AX: NEAR row brochado -> <new numbers>, <next step>" (the orchestrator edits NEAR.md). Status stays partial unless the rule-5 conditions change; no novelty words.
Files: ciphers/antt-msliv0638-brochado-1712/{NOTES.md,thin_codes_attest.tsv,key.tsv (only under step 3's rule),reading_*,judge_bucket*.tsv,scripts/16_*.py,images/crops_ax/**,passA_thin.tsv,passB_thin.tsv}. ROOM done line: "for LANE AX: brochado <n> new thin-code observations across <m> letters, key changes <k>; letter 134 bucket percentile pt17 a->b, pt18 c->d".
