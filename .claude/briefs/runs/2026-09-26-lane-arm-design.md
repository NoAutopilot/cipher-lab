JOB ARM-DESIGN: family B design -- what kind of code is Armstrong's 20 Feb 1808 letter in, tested first on siblings whose answer is known. Fable (claude-fable-5-1). Stop and push at USD 12 or 70 minutes (units: 4 sibling tests at about 10 minutes each, 1 target unit 15 minutes, 1 margin; stop before a unit that would cross 80 percent). At most 2 subagents, Sonnet, for scripted runs only. Lane: LANE ARM orchestrator session_011gEKqukZAxE6FjtQCzsUQ8. Written 26 Sept 2026 by LANE ARM.

INTAKE_GATE_OUTPUT (live, 26 Sept 2026 06:46, TOMO-REPLY): ciphers/armstrong-madison-1808: open (line 1) -- edition/page or full-text-search citation found within 6 lines; EXIT:0.

Read first: `.claude/briefs/runs/2026-09-26-lane-arm-COMMON.md` (binding; you are on Fable, not Sonnet, for this job), ciphers/armstrong-madison-1808/{NOTES.md,HYPOTHESES.md} (the orchestrator's structural note H-DEC / H-HOM, and ARM-CODES' stats section), tools/data/uscodes-1800/README.md and stats.tsv, sources/cryptiana/web/madison_armstrong.htm. No network needed; if you fetch, cryptiana.web.fc2.com only, at most 10.

Questions, each answered with a statistic that CAN differ between the hypotheses (CLAUDE.md rule 3, orthogonality paragraph) and computed first on at least two siblings whose design is known (the control), then on the target:
1. One-part vs two-part vs blockwise alphabetical: in a one-part code, value adjacency predicts plaintext adjacency, so neighbouring groups in the text that are close in value should begin with the same letters more than chance. You cannot see the target's plaintext, so design a plaintext-free statistic (e.g. value-gap distribution between successive tokens; repeat structure; the fraction of the value space used vs Zipf expectation for a codebook of size K) and show it separates the siblings' known designs before reading it on the target.
2. H-DEC vs H-HOM vs plain sequential numbering, for the units-digit skew: does any sibling show a units-digit skew in its USED values (encode a sibling's decoded letter under its own table and count), and does its table have a decade/inflection layout? Simulate each design with a plausible 1808 vocabulary (tools/data/en18 if present, else en) at N=369 and compare the target's units-digit and decade-variant profile to each simulation (at least 50 simulated letters per design; report the target's percentile under each).
3. Codebook size and the particle block: estimate K from the repeat rate (369 tokens, 216 distinct) under each design; what does the 1-99 block hold (its tokens are 36 percent of the text: particles, letters/syllables for spelling, numerals?) -- compare with the siblings' low blocks.
4. Write the design verdict and, from it, the concrete spec for family C (tools/family_run.py `nomenclator` family): the unknown to anneal over (group->word, or decade->root plus units->suffix), the score (word n-gram over en18 plus the sibling vocabulary prior, plus the alphabetical-order constraint if one-part), and the matched control (a synthetic letter of N=369 in the SAME design, from en18 text, solved blind with the prior; and a real sibling letter if one exists at about that length). Name the control's gate and warn if its blind baseline could be near ceiling (rule 3, Salviati lesson).

Write: ciphers/armstrong-madison-1808/design/ (scripts, per-sibling and target tables, simulations with seeds), a section "ARM-DESIGN" appended to HYPOTHESES.md with every control and target number side by side, the family C spec as ciphers/armstrong-madison-1808/design/family_C_spec.md, one paragraph in NOTES.md. Do not decode the target to words and do not build the family C tool (that is the next job).
Done line (ROOM, starting `done: for LANE ARM: ARM-DESIGN`): design verdict per question with control vs target numbers; K estimate; the family C unknown, score and control in one sentence each; no cost figure.
