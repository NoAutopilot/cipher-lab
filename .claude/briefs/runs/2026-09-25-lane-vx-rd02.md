VX-RD02 (Sonnet, stall alarm $8, at most 2 subagents). LANE VX job 3: read ciphers/na-janssens-java-1811 (QUEUE VX-N02; check-solved open 25 Sept 2026 by VX-CS05, intake gate passed: Colenbrander Gedenkstukken VI read, names the series, does not print the dispatches). Read .claude/briefs/runs/2026-09-25-lane-vx-COMMON.md, .claude/briefs/transcription.md, .claude/briefs/solver.md, and the target NOTES.md in full; parent LANE VX orchestrator session_01EYwyYDeAaQReWy7ZuQcoxe. Kind: recovery (key source `period`). The stall alarm is real this time: two earlier VX workers ran 1.5-3.5x past theirs -- check your cost after each step and at 80% push, write the remaining steps as one line in NOTES.md, and stop. Transcription passes: give each subagent only the crops it needs (not whole-page images at full size repeatedly). Rule 7: spec + `tools/judge_plaintext.py` output pasted into NOTES.md (a FAIL reported as a FAIL); decode.json so `tools/decode_key.py <folder> --check` regenerates the reading; a fresh-instance re-derivation by one subagent given only ciphertext.tsv, the key-source transcription and the crops (not key.tsv, not your reasoning), agreement per code and per token, any difference beyond the M tokens written as unresolved. Grades per rule 4 (C where the key comes from a period decipherment of other text, H only from a period key sheet; M uncertain; I inferred), with counts. Report what was read and where it was not found; do not classify novelty. Two-pass agreement under 60%: stop and report the blocker.

Material: NA 2.01.27.05 invnr 12 (233 leaves; scan list in the item page's drupal-settings-json; full-size JPEG at service.archief.nl). CS05: cipher cluster leaves 186-200; cipher + decipherment on 190-191 ('N.2') and 199-200 ('Numero 3'); raw cipher only on 188, 192, 198.
Steps: (1) fetch leaves 180-210 at full size once (images/manifest.json; service.archief.nl <=45 requests) and inventory each: raw cipher / cipher+decipherment / decipherment only / plain, with dispatch number and date where visible; fix NOTES.md so line 2 (not line 3) carries the check-solved sentence. (2) Key source: two-pass transcription of the decipherment pages (code -> French word pairs as written); key.tsv (code, value, n, pages, grade C/M), conflicts.tsv; state the system (numeric code book, 3-5 digits, one- or two-part? nulls? spelling codes?) with evidence; control: the key re-reads each deciphered page to its own decipherment (per-token agreement). (3) Target: two-pass transcription of every raw-cipher page with no decipherment beside it (first check whether a raw page is the ciphertext of a decipherment page -- then it is a control, not a reading); decode with the key, codes never seen stay [?]. (4) specs/na-janssens-java-1811.json (language fr, judge block) on the pattern of specs/antt-linhares-chave.json; judge output; decode.json; re-derivation. (5) NOTES.md section "Reading (VX-RD02, 25 Sept 2026)": per dispatch, reading in French with an English gloss, counts H/C/M/I, coverage (share of groups read).
Touch only ciphers/na-janssens-java-1811/**, specs/na-janssens-java-1811.json, ROOM.md.
