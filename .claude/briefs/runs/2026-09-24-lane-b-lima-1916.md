LANE B breadth worker: lima-1916. Sonnet (claude-sonnet-5). Cap $3.
Common rules: `.claude/briefs/runs/2026-09-24-lane-b-common.md` (read it first; it governs).

Spec: specs/lima-1916.json. Run `cheap_tests_in_order[0]` and its matched control, nothing else. Write both numbers into the spec's `cheap_test_done` (with date, method, cost). Judge any candidate plaintext with `tools/judge_plaintext.py` and paste its output. Cap $3. Report in five lines; do not run test 2; rule 10 wording; a negative's done line carries the target and control numbers side by side.

Note from the orchestrator: the survey ran the spec's test 2 (anneal, negative vs controls) and names the commercial-code grep as next, which is cheap_tests_in_order[0]; run that. Append to cheap_test_done (keep the survey's entry; make it a list). One host: archive.org (advancedsearch / metadata / download of the code books' _djvu.txt full text, >=1.5 s apart, at most ~30 requests). Matched control: grep 17 random letter strings with the same length distribution, and 17 real code words drawn from the same books, against the same books; report hits target vs controls.
