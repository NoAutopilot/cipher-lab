PARENT WORKER LQ-L20-LAND (Sonnet, cap USD 3, box 30 minutes, no subagents, network only git and the GitHub MCP tools). Parent: parent 7i, session_01U1NiMakQ8PPySH8WixXsmc ("LIVE parent 7i · Orchestrator 10"), .claude/briefs/parent.md duty 7. Written 26 Sept 2026 at the 17:41 UTC clock read.

Why: PR-LAND-5 (owner account, ROOM 17:36) bounced PR 24 ([LQ-L20] ciphers/espagnol142-mercy-1648, branch local-queue/L20) because `tools/lq_answer_check.py` exited nonzero -- the answer is a negative with no holding-catalogue availability rung. But L20 is an `ia-reader` row: the runner borrowed archive.org/details/correspondancede0006jose and read p.647 to say whether Cuvelier-Lefevre VI quotes, summarises or merely lists the 6 June 1648 instruction to the abbe de Mercy. That is a content read of a printed page, and its "no" is the answer (ASKS 59, NEAR.md Mercy row step (3), Outreach gate 2 for the Gramont/Mercy notes); the catalogue-ladder rung exists for image-portal "no items" answers about digitisation (the L19 shape) and cannot apply to it. The gate has to distinguish the two kinds, and the answer has to land.

Job: (1) Read `tools/lq_answer_check.py`, its test, and LOCAL-QUEUE.tsv's `kind` column. Add a `kind` awareness: when the row's kind is `ia-reader`, `edition-read`, `hathitrust-page` or `jstor` (a page or text read, not a digitisation lookup), the catalogue-ladder rungs are not required and a plain negative passes; `browser-check` and `catalogue-lookup` keep the current rule. Read the kind from LOCAL-QUEUE.tsv via `--row`, with a `--kind` override for a file without a row. Add test cases (the L20 shape passes as ia-reader; the L19 shape still fails as browser-check). Run the test file directly (no pytest). One UPDATES.md row; SYSTEM.md's row for the tool gets the one-clause change; run `python3 tools/system_map_check.py`. (2) Fetch PR 24's one added file from branch local-queue/L20 with the GitHub MCP tools (mcp__github__get_file_contents or pull_request_read get_files; never merge), save it as `ciphers/espagnol142-mercy-1648/local-runner/L20-2026-09-26.md`, run the fixed gate on it (`--row L20`) and paste the output; if it passes, land it: a dated "Local runner L20 (26 Sept 2026)" section in the target's NOTES.md quoting the runner's wording about p.647 and the index entries; LOCAL-QUEUE.tsv row L20 status `done 26 Sept 2026`, result one line; ASKS.md row 59 status cell to what was learned; NEAR.md espagnol142-mercy-1648 row step (3) updated in place with the finding and the date; then `python3 tools/desk_check.py` and fix any STALE DRAFT on this target. Comment once on PR 24 (already closed) naming the landing commit after `git ls-remote origin main` shows it, with the footer "---" then "_Generated by [Claude Code](https://claude.ai/code)_". If the fixed gate still fails the file, stop and say why in your done line instead of landing.
Rules: every edit reads the whole file and writes it back changed in one place; `git diff --stat` pasted before every commit; refuse to commit if any tracked file lost more than 20 percent of its lines; commit by explicit path and push only through `python3 tools/room.py --push <paths>`. ROOM claim first, done line "done: for parent 7i: LQ-L20-LAND -- gate <kind rule added, tests n/n>, L20 <landed commit / still bounced: why>, desk_check <...>" as `parent worker LQ-L20-LAND (Sonnet, <session id>)`; LEDGER.md row with cost cell "parent's get_session". Never touch status.json, BUDGETS.md or any other ciphers/ folder. Never call AskUserQuestion; never print or commit credentials; no novelty wording.
