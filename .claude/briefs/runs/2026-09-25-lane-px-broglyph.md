PX-BROGLYPH (Sonnet, cap $5 as a stall alarm; the orchestrator watches your cost, you cannot; at most 2 subagents). Target: ciphers/antt-msliv0638-brochado-1712. Job: settle the appendix transcription's disagreements, above all the one glyph read z / 7 / 2, then rebuild the key. Parent: LANE PX orchestrator session_01KapVpHVzNpnnQce5C8c3LY. Read the lane COMMON, .claude/briefs/transcription.md, NOTES.md sections "PX-BROKEY", "PX-BROKEY2" and "PX-BROPASSB", disagreements.tsv, agreement.tsv. PX-BROBODY may still be writing body_* files in this folder: do not touch them.
1. Crop tool: add a --crop option to an existing shared tool if one fits (tools/iiif_lines.py works on IIIF; for plain JPEGs on disk write the smallest option or script in tools/ with --help and an offline test, per CLAUDE.md Usage 8), and cut crops of every disputed token instance (leaf, entry, position) into images/crops/.
2. Glyph atlas: for the z/7/2 shape, put 15-25 crops side by side with undisputed instances of z, 7 and 2 from the same hand; decide whether it is one sign or several, from letterforms, not from the key. Then check the decision against the Deciffrada text only as a consistency test (pass A's key already maps z->e and 7->e; code 2's second value is e): report how many disputed tokens each reading makes consistent with the period decipherment. Write glyphs.md with the crops referenced and the decision.
3. Settle the remaining disagreements from their crops; update ciphertext_appendix.tsv and plaintext_appendix.tsv with a grade column (H both passes agree or settled unambiguously from the crop, M otherwise); report the post-settlement agreement.
4. Re-run scripts/01..04 and 06 and `python3 tools/decode_key.py ciphers/antt-msliv0638-brochado-1712 --check`; report codes, grade-C count and control agreement before and after, in one table in NOTES.md ("## PX-BROGLYPH").
Push after each step. Touch only ciphers/antt-msliv0638-brochado-1712/{ciphertext_appendix.tsv,plaintext_appendix.tsv,key.tsv,conflicts.tsv,decode.json,reading_appendix*,glyphs.md,images/crops/**,scripts/**,NOTES.md}, the new tool and its test, and ROOM.md. Write "cost: not visible to me" rather than a guess. Do not decode body passages or classify novelty.
