JOB bHCP2 (LANE B8, Sonnet, cap USD 3, wall-clock box 40 min; hosts: none): hessen-1824 -- the remaining families through tools/family_run.py at N=164, control first.
Intake gate (orchestrator, 06:58 UTC): `hessen-1824: open (line 1) -- edition/page or full-text-search citation found within 6 lines` (exit 0). Paste your re-run.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b8-common.md` (it governs); ciphers/hessen-1824/NOTES.md (bHCP: N=164, periodic_vigenere control 1.000 on de20, target FAIL at periods 16, 6, 7 -- excluded); specs/hessen-1824.json (de20 judge, era note: target 1824).
Steps, serialized (Usage 6): (1) `--family masc --seeds 3`; (2) `--family running_key --corpus tools/data/de20 --seeds 1` (needs >= 3 corpus texts; if the control alone would overrun the box, run `--control-only` first and stop there, reporting it); (3) only if the IC in NOTES.md says the text is not flat-polyalphabetic, also `--family homophonic --param profile=target --seeds 3`. Each: CONTROL BELOW GATE is a non-test, not a negative. Paste every HYPOTHESES.md row.
Output: NOTES.md section "Remaining families (bHCP2)"; spec cheap_test_done row. If every family in the spec's ladder now has a logged run with a passed control and a target FAIL, say so plainly (the orchestrator decides the status word; never write closed-negative yourself). ROOM done line <= 600 chars, target and control side by side per family. Final reply at most five lines.
