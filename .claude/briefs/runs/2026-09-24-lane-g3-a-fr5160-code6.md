# LANE G3 worker A: fr5160 key_1659 code 6, table change or misread (Opus, cap $3, Gallica fetcher, one native)

Target: ciphers/fr5160-letellier-1653. Read NOTES.md sections 'key_1659 codes 6 and 65 (24 Sept 2026)' and 'Folio 67'.
Question (LANE G2 handoff priority 1): code 6 reads qu on f.86/f.88 (5/5) and a on f.67 (13/14). Is f.67's "6" really a 6, or a different
sign misread by the passes (so key_1659 has one value), or a genuine table revision between the letters?
1. Fetch ONE native: canvas 129 of the fr.5160 ark (f.67; confirm canvas with tools/gallica_folio.py or NOTES), to images/ with a manifest entry,
   or scratchpad + manifest if the folder would pass 30 MB. At most 4 Gallica requests.
2. Crop every f.67 token read as 6 and compare it by eye with the f.86/f.88 sixes (crops/f86_cipher_L08.jpg and others on disk) and with the
   nearest-looking other signs of the table. Record per occurrence: line, what it is on the image, confidence.
3. If it is a misread: correct ciphertext_f67.tsv (pass-level provenance note), drop LETTER_SCOPED in align_f67.py if no longer needed, rerun
   the alignment and `tools/decode_key.py ... --check` on every letter; report the grade counts before and after. If it is a real 6: say so,
   keep the letter scope, and note it as a table revision with the evidence. Also look at the _12 = ri conflict row while the image is open;
   report, do not force.
NOTES.md section 'Code 6 image compare (24 Sept 2026)'. Update status.json only for the f.67 grade counts if they change. Readings go for LANE V3.
