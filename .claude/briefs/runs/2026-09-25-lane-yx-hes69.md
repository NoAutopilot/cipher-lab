YX-HES69 (Sonnet, wall-clock box 60 minutes; at most 2 subagents). Target: ciphers/willem-van-hessen-1567, sibling WVO 1069 (Hessen to Orange, 1563) whose cipher carries an interlinear German decipherment in the same hand-set. Parent: LANE YX orchestrator session_01PHaEdHeQY2FtMHLo9yGeoe. Kind: recovery (period key rebuilt from a decipherment). Read the lane COMMON, .claude/briefs/transcription.md ("Symbol alphabets"), the target NOTES.md in full, especially OX-WV69's "Explicit handoff for a successor (page/line level)" and YX-CSHES's section; and siblings/ (glyphs_1069.tsv, key_1069.tsv, key_1069_candidates.tsv, passA/passB_p2line1.tsv, reconcile_p2line1.md, check_1069.py). Intake gate: tools/intake_gate_check.py exits 0. The target letter itself (1127) is only in the KHA original (REQUEST.md); this job builds the office key from 1069 so it can be applied when 1127 is imaged.
Job:
1. Glyph atlas: cut tight crops of each cipher sign on 1069 p2 (siblings/01069_p2.png) by connected components or by hand-set boxes, not by ink-row projection (it failed here because gloss and cipher lines touch). Assign a code to each distinct sign in glyphs_1069.tsv (extend, never renumber the 10 confirmed), with a crop per code in siblings/atlas/.
2. Two blind passes over p2 lines 1-24 (subagents get line crops and the atlas, not each other's output, not the German gloss): sign codes per line. Reconcile with tools/reconcile_passes.py; the 60% gate applies, per line block.
3. Read the interlinear German decipherment above each reconciled line as paleography (a separate read, graded H/M per word), align sign codes to letters where the counts match (the Brochado alignment rule: accept a span only when sign count equals letter count), and extend key_1069.tsv with per-sign counts and grade (C for aligned-with-agreement, M for one observation or a tie).
4. If time remains, the same for p3. Stop at 80% of the box.
check_1069.py (or tools/decode_key.py with a decode.json) must regenerate reading_1069.txt from ciphertext_1069.tsv + key_1069.tsv with --check (rule 7). NOTES.md gets "## YX-HES69 (25 Sept 2026)": signs in the atlas, pass agreement, key size and grades, what is left. Touch only ciphers/willem-van-hessen-1567/** and ROOM.md. No network needed. Push after each step. Report what was read and where it was not; do not classify novelty; "cost: see the lane ledger".
