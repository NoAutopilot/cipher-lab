ZX-BAR2 (Sonnet, wall-clock box 60 minutes; this box is also a minimum working time: do not stop early unless the brief's stop condition is met; at most 2 subagents). Target: ciphers/thurloe-barriere-1654. Parent: LANE ZX orchestrator session_01MxueEQJUGF9PWJiYcVyvBM. Kind: cryptanalysis (the printed gloss is period known plaintext, grade C). Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md), CLAUDE.md rule 3 (the Salviati lesson: match the design, not only N and K), the NOTES.md status note at the top (the orchestrator's correction), sections 8, "YX-BARB" and "ZX-BAR", and specs/thurloe-barriere-1654.json. Intake gate: `open`, exit 0 (orchestrator, 15:44 UTC 25 Sept 2026).

Open hypothesis: the letter is code+mark (the mark printed over about half the numerals is part of the code), which would split the 9 conflicting bare codes in key_gloss.tsv. ZX-BAR's permutation test ran on bare digits and is not decisive.
1. Mark typing. From the page images already in images/ (archive.org collectionofstat02thur leaves 729-730; if the copy on disk is below ~300 dpi equivalent, fetch the leaf JP2/full-res once from archive.org, one request at a time >=1.5 s, and record it in images/manifest.json), crop every numeral token that ciphertext.tsv flags with a mark at native resolution. Two blind subagent passes type each mark from a fixed closed set (circumflex, grave, acute, macron/overline, caron, diaeresis, dot, none, unreadable), per token, H/M. Reconcile; report agreement; settle disagreements from the crop yourself (grade M). Write marks.tsv (run, position, code, mark, grade).
2. Rebuild key_gloss_marked.tsv with code+mark as the key unit (same gloss alignment as key_gloss.tsv, unchanged). Report: how many of the 9 conflicting bare codes stop conflicting once split by mark, and how many new conflicts appear. This count is the first result; compare it against a shuffle: the same marks permuted among the same tokens (1000 permutations, fixed seed), so the reader knows whether the marks resolve conflicts more than random labels would.
3. Rerun permutation_test.py on key_gloss_marked.tsv (add a --key option, do not fork the script), and extend matched_control.py/permutation_control.py with a code+mark control option (the same share of marked tokens, marks carrying meaning, same alignment), 10 seeds. Target vs control z side by side.
4. Verdict in numbers. If marks resolve the conflicts well above the shuffle and the marked target's z enters the control range, write that as a lead (no reading claimed) and name the next test (apply the marked primary key to the unglossed tokens with the coverage test and its marked control). If marks resolve no more than the shuffle and the marked z stays near zero, the letter is closed-negative on cryptanalysis alone: set the status line to closed-negative with both numbers, pending the 1655 cipher / DECODE Add MS 4200 leads.
NOTES.md gets one section "## ZX-BAR2 (25 Sept 2026)"; the spec gets a new cheap_test_done entry (keep the others). Touch only ciphers/thurloe-barriere-1654/**, specs/thurloe-barriere-1654.json and ROOM.md. Push after each step. Write "cost: see the lane ledger". Never call AskUserQuestion.
