LANE B4 spec writer bSPEC3: specs for UNSOLVED-SURVEY.md ranks 31-41, famous-item filter applied. Sonnet (claude-sonnet-5). Cap $4 or 40 minutes from your first `date -u`, whichever first. No subagents. Disk only (no scienceblogs.de: another LANE B4 worker holds it; no other host). Parent: LANE B4 orchestrator session_01V2WMavWGMeAUYELUpmyEcX (written 25 Sept 2026 21:37 UTC).
Common rules: `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (it governs; read "LANE B3" as "LANE B4") and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Why: every rank 1-30 spec now has its first test run or running; the breadth lane needs the next ranks specified. Ranks 31-41 are mostly famous items (Shugborough, Zodiac Z13/Z32, Kryptos K4, Beale, Rohonc, Codex Seraphinianus, Voynich, WW2 pigeon message, Double Column Transposition "reloaded", Schmeh's 65-bit challenge, Bonus 22 M-209). CLAUDE.md Pipeline 3: famous items enter only with a named untried cheap test.
Part 1 -- triage table first (specs/cheap-tests/specs31-41-triage.tsv): for each rank, one row: does the survey's approach paragraph (UNSOLVED-SURVEY.md "Approach per item") or LANDSCAPE.md/QUEUE.md's dropped table name a cheap test (under $3, disk-first) that no one has published a matched-control negative for? Check the repo first (ciphers/zodiac-z13-stress/, LANDSCAPE.md, QUEUE.md "Dropped this sweep", sources/solver-diffs/, any cyphersolver notes quoted there). Columns: rank, name, untried cheap test (or 'none'), why, spec yes/no. Not a cipher (Seraphinianus by the author's statement), plaintext already found (K4, Sept 2025 per the survey), or a pure compute challenge (65-bit) is 'no spec' with the reason.
Part 2 -- specs only for the 'yes' rows (expect 3-6), modelled on specs/README.md, specs/lima-1916.json and specs/mccormick-1999.json: slug, rank, schmeh_top50, name, date, language_candidates, ciphertext (as transcribed, with source and date, only if on disk: sources/schmeh/posts/20-pigeon.txt etc.) or ciphertext_pending (where and what one fetch gets it), alphabet, constraints, cheap_tests_in_order (three, cheapest first, each naming its matched control, a tools/family_run.py family where one fits, est. cost; the first runnable by a $3 Sonnet worker in 30 minutes, disk-only where possible), matched_control, judge block (a language actually wired in tools/judge_plaintext.py LANG_CORPORA plus min_word_cover, or no language and the reason in schema_note; confirm it loads by running judge_plaintext.py on a dummy file), value, model, status line dated 25 Sept 2026 per the survey row (never solved/new), schema_note, written.
Add ` spec: specs/<slug>.json` or ` no spec (B4 triage: <reason>)` to each rank 31-41 row's last column in UNSOLVED-SURVEY.md (fetch+rebase right before; keep others' edits). Push after the triage and after every two specs. Done line: the triage counts (spec / no spec) and the slugs written, ciphertext on disk vs pending.
