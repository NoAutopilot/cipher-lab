PARENT WORKER RETRO-APPLY-N (Sonnet, cap $6 of usage, wall-clock box 40 minutes, no subagents, no network except git). Job: apply the brief- and tool-only proposals of RETRO-2026-09-26b.md (retrospective n, 26 Sept 2026 02:27 UTC) plus LEARN-2026-09-26-0058.md item 4, for parent 7e (session_01SvjMDFfJZJ3uK47RYxAQrM). Authorised by CLAUDE.md "Improvement loop" (the orchestrator applies changes that only touch briefs, tools or workflows; anything that changes the goal, the spend or the owner's asks goes to the owner). Written 26 Sept 2026 at the 03:05 UTC clock read.

First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker RETRO-APPLY-N (Sonnet, <session id>)`; read CLAUDE.md, RETRO-2026-09-26b.md in full, LEARN-2026-09-26-0058.md item 4, then each target file before editing it. Edits minimal, in the file's voice.
Apply, in order, each as the retro's diff: proposal 1 (tools/room.py); proposal 2 (.claude/briefs/breadth.md); proposal 3 (tools/intake_gate_check.py, two hunks, and .claude/briefs/check-solved.md) -- add or extend an offline test in tools/tests/ for the intake_gate_check.py change and run the room and intake tests directly (`python3 -m pytest tools/tests -q` is known to INTERNALERROR on test_codemark_curve.py's module-level sys.exit; if a one-line guard fixes that collection failure, apply it and say so, else run the affected test files directly); proposals 4 and 5 (CLAUDE.md, two hunks). Then LEARN-0058 item 4: one sentence in `.claude/briefs/README.md`'s common tail -- a job brief may carve out a partial scope (known-plaintext alignment only, grade C, no decode of unprinted text) when tools/intake_gate_check.py blocks a target, with the gate failure flagged in ROOM.md, the AX-5799 pattern. One UPDATES.md row per instituted change (`By` = "RETRO-APPLY-N for parent 7e", clock-read time). Anything in the retro that changes the goal, the spend, rate limits or the owner's asks: an ASKS.md row, not applied; never touch BUDGETS.md. Commit by explicit path, `python3 tools/room.py --push <paths>`; ROOM done line `done: RETRO-APPLY-N: N of 5 proposals + LEARN item applied (...), tests <pass/fail>, UPDATES rows M`; one-paragraph reply; never call AskUserQuestion; never print or commit credentials.
