SOLVER EXPERIMENT solvEX (parent worker; Opus; cap $15; disk only, no hosts, no subagents). Started 24 Sept 2026 19:47 UTC by the parent (cipher-lab-7b, session_01K7ZbE95o1pUW5gof8VA5PR) under the owner's goal of unique results. Question: does a model in the loop, proposing plaintext for partially decoded stretches, shift the matched-control curve of tools/homophonic_anneal.py, and by how much? Measured on controls first (rule 3); the target is touched only if the control gain is real.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md (rules 3, 4, 6, 10, Usage 8), LESSONS.md, `python3 tools/homophonic_anneal.py --help`, ciphers/fr2933-salviati-1525/NOTES.md sections "Solver (LANE R4 I)" and "Code+mark control curve (LANE R4 P)", ciphers/beinecke-mellon29-elia/NOTES.md "Solver (LANE R4 R)", ciphers/jan-van-nassau-1572-75/NOTES.md "J7 crib cryptanalysis". ROOM claim line "solvEX: model-in-the-loop crib rounds on matched controls -- files tools/homophonic_anneal.py (option only), tools/crib_rounds.py, tools/tests/, ciphers/fr2933-salviati-1525/control/solvex/, ciphers/beinecke-mellon29-elia/control/solvex/".

Method (each round is one script run plus one reading by you; at most 4 rounds per control; you never see the control's plaintext or key until the scoring step, which the script does):
1. `tools/crib_rounds.py` (new; `--help`; offline test on a 300-letter fixture in tools/tests/): wraps homophonic_anneal.py. `--control PLAIN --signs K --length N --corpus ...` makes the control cipher and hides the key in a JSON you do not open; `--round R --cribs FILE` re-anneals with the cribs fixed (the tool's --fix option), writes the best decode as a spaced letter stream with the current per-symbol confidence (agreement across restarts), and `--score` compares the decode to the hidden plaintext and prints letter accuracy and token accuracy. Do not add a language model to the tool: the model in the loop is you reading the decode.
2. Controls: (a) Italian, N=720, K=36 (Salviati f.54r's own N and K; corpus the same it16 collections as worker I, plaintext held out); (b) German nomenclator-like, the 5549 control of J7 if reproducible from j7/, else German N=540 K=82; (c) Italian N=244 K=31 (Mellon's). Baseline = the tool's blind result at the same settings, 3 seeds (already on file for a and c; rerun if the files lack the decode). Then rounds: read the decode as 16th-century Italian or German, propose at most 12 cribs per round as sign=letter pairs where you are confident from the language (not from a guess at the topic), re-anneal, score. Report per round: accuracy before and after, cribs proposed, cribs right, cribs wrong. Stop a control when a round gains under 2 points or a crib set is more than a third wrong.
3. Result table in NOTES.md of ciphers/fr2933-salviati-1525 (section "Model-in-the-loop crib rounds (solvEX)") and one line each in the Mellon and 5549 NOTES: the curve with and without the loop at each N and K. Verdict wording: "the loop adds X points at N, K on a matched control" or "no gain"; never a claim about the target.
4. Target step, only if control (a) gains 15 points or more: run the same rounds on the pooled Salviati ciphertext (f.54r + f.54v, vi design as in worker P) with the same crib limit, report the score per symbol against the control's, and stop. No reading is claimed; grades stay H0 C0 S0 unless the decode reads as Italian at the control's score, in which case grade S with the control numbers and post ROOM "for LANE V5: reading ready".
Cost: check get_session on yourself after the tool and test, after each control, before the target step; stop at $15 with what is on disk pushed. Push with `MSG=... python3 tools/room.py --push <paths>`; ROOM done line with the table's headline numbers; final paragraph of five lines; stop.
