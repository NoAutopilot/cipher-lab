WC-LAGARDE (LANE WC worker; lane orchestrator session_01PgyvWdtkFieEXUYD1Whbyx; parent session_01FXDfYR3CvGk7tcid1Aav1n). Sonnet. Cap USD 6 of usage (stall alarm), wall-clock box 45 minutes from your first `date -u`; stop before starting a unit that would cross 80 percent of the box.

Read first: .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md (read "LANE AX" as "LANE WC"; lane goal "WVO circles: one control-backed recovery test per circle, close or promote"), the .claude/briefs/README.md common tail, and ciphers/la-garde-1577/NOTES.md in full (L2, L4 "Not settled by this pass", Y1, ZX2-LAG, ZX2-LAG2). Files you may touch: ciphers/la-garde-1577/ only, and ROOM.md.

Target: ciphers/la-garde-1577 (WVO 6179 and 6467, dot-separated 1-24 numeral system, 239 pooled tokens, no key, no solved sibling; L2/L4 negatives for monoalphabetic/homophonic and periodic designs, each with a matched control). Status open.

Step 0: `python3 tools/intake_gate_check.py la-garde-1577` and paste its output into NOTES.md. Nonzero exit: stop and flag.
Step 1 (units: 9 cells, priced as 2 blind passes + 1 reconciliation = 3 subagent calls at most, each call given only crops): the 9 unresolved cells listed in L4 (ciphertext_6179_v2.tsv p2L28 pos12; p3L6 pos7,8,16; and the rest named there, plus 6467's). Cut crops from the images already on disk -- run and paste `python3 tools/iiif_lines.py --image ciphers/la-garde-1577/images/<file> --out ciphers/la-garde-1577/images/crops_wc ...` (or a region crop around each line) BEFORE any subagent call; never pass a full page to a subagent. Two blind passes (Sonnet subagents, at most 2 at once) on the line crops, overlines and loop marks recorded; settle each cell or leave it `unresolved` with both readings. Update ciphertext_*_v2.tsv (note column says WC-LAGARDE), and state the measured pass-to-pass disagreement rate on the crops.
Step 2: rerun `solve_l2.py` (both sign modes: plain numerals and overline-as-distinct-sign) on the settled transcription with its matched controls, and -- the SALV-DIAG rule (CLAUDE.md rule 3) -- the noisy control at an injected error level that brackets the transcription's own measured disagreement (at least that level and one above). Report target and control numbers side by side for every test. If the control collapses at or below the measured error, the negative is "not a test at this transcription error", and say so.
Step 3: there is no key, so decode_key.py --check applies only if Step 2 produces a reading that beats its control; if it does, stop and flag the orchestrator (a reading needs a spec, the fr16 judge with per-fold spread, and a fresh-instance re-derivation that a separate job will do). Otherwise write a "WC-LAGARDE (26 Sept 2026)" section in NOTES.md: cells settled, disagreement rate, the table, and the status line per rule 5 (stays `open` or moves to `partial` with a NEAR.md suggestion only if a control shows the old negatives were non-tests; never `closed-negative` unless every family in the ladder has a passed control -- do not set it yourself, recommend it in one line).

Push with `python3 tools/room.py --push <paths>`; ROOM done line "done: for LANE WC: WC-LAGARDE --"; final reply one paragraph per COMMON.
