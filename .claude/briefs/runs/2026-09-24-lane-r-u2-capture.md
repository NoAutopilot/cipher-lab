ROLE: ACCESS + TRANSCRIPTION, Huntington capture for two targets (LANE R worker R1). Model Sonnet, cap $9.
Targets: ciphers/huntington-blathwayt-madrid-1728 (U2) and ciphers/huntington-luzerne-destouches-1781 (U1). You are the ONLY LANE R worker on hdl.huntington.org; fetch everything both targets need in this one session, then stop hitting the host.
Step 1 (host): Huntington CONTENTdm API per CLAUDE.md Access playbook (CISOSEARCHALL form; dmGetItemInfo/ALIAS/POINTER/json for the full note; compound objects via dmGetCompoundObjectInfo; page images via the IIIF image API /iiif/2/ALIAS:POINTER/full/max/0/default.jpg or /digital/api/singleitem/image/... whichever the site serves). Locate and fetch every page of: U2 mssBLA 179, 184, 185, 186, 187, 188, 189, 190, 191, 194 (the cipher-bearing items in NOTES.md's table; include each item's deciphered French sheet), and U1 mssDE 68, 108(A), 37, 55 (37 and 55 are catalogued as deciphered in a contemporary hand; fetch all pages). Save to each target's images/ with manifest.json (item, page, pointer, URL, bytes, sha1, catalogue note verbatim). Keep each folder under 30 MB (JPEG quality/scale down the reference copy only if needed; record native URLs). Budget: at most 250 requests, >=1.5 s apart. Record the route that worked in each NOTES.md "Access notes".
Step 2 (look, don't transcribe the plaintext): for each item write one row in images/inventory.tsv: item, page, what is on it (cipher / French decipherment / interlinear decipherment / clear), cipher type seen (numeric groups, letters, symbols), approx token count. Push. Post ROOM `note: for LANE R: images and inventory pushed` at this point.
Step 3 (passes, U2 only, cipher pages only): two blind Sonnet subagent passes (passA.tsv, passB.tsv; columns item, page, line, idx, token) of every cipher passage in U2, from the images; the French decipherment sheets are transcribed separately, once, as plain text (decipherment_<item>.txt, line by line, as written). Run tools/reconcile_passes.py; do NOT settle disagreements (the Opus reconciler does). Push after each pass. U1 passes are a separate worker; for U1 stop after Step 2.
Do not build keys or decode. Report: items found/fetched, pages, token counts per item, pass agreement.
