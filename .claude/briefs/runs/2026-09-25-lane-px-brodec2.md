PX-BRODEC2 (Sonnet, cap $6 as a stall alarm; the orchestrator watches your cost, you cannot; at most 2 subagents). Target: ciphers/antt-msliv0638-brochado-1712. Job: a proper control for the period key, then (if it passes) the letter-134 decode, spec, judge and fresh re-derivation. Parent: LANE PX orchestrator session_01KapVpHVzNpnnQce5C8c3LY. Read the lane COMMON, .claude/briefs/solver.md, NOTES.md sections PX-BROKEY2, PX-BROGLYPH, PX-BROBODY, PX-BRODEC; key.tsv, scripts/, decode.json, reading_body_tokens.tsv. No network.

Why this job: PX-BRODEC's control gate (68.4% / 50.0%) compared body decodes with the appendix's Deciffrada glosses stripped of abbreviations. The period decipherer abbreviated and paraphrased (Carta 81: 23 codes against the gloss "O S.d± Luis the Communica±", 19 letters; Carta 80: the body decode reads "...despropp_ito" where the gloss has "do proposito"). That measures the scribe's shorthand as much as the key. The gate below measures the key.
1. Leave-one-out control (scripts/11_loo_control.py): for each appendix entry with aligned pairs (scripts/03 output, letter count = token count), rebuild the key from all other entries (same majority rule as scripts/04), decode the held-out entry's aligned spans, and score letter agreement on those spans only. Report per entry and pooled (tokens, agreement, and the share of held-out tokens whose code is unkeyed without that entry). Matched control (rule 3): run the same procedure on a synthetic cipher built by scripts: a random homophonic key of the same shape (40 codes, the same code-frequency profile) applied to the appendix's own plaintexts, with the same abbreviation-free spans; report both numbers side by side.
2. Re-state the m0179/m0180 comparison as information, not a gate: align each body decode against its gloss with the gloss's abbreviations marked (S.d±, V., ±) and list every disagreement as one of: key value (homophone minority), transcription (M token or the known extra tokens), gloss abbreviation/paraphrase. No threshold.
3. Gate: if pooled leave-one-out agreement on the target is >= 80% and within 10 points of the synthetic control, do PX-BRODEC's brief steps 3-5 (.claude/briefs/runs/2026-09-25-lane-px-brodec.md): letter 134 (m0275-r1 + m0276-r1 + m0276-r2 as one run) decoded with per-token C/M/I/U, Portuguese in period spelling + English gloss, I-grade completions kept apart; specs/antt-msliv0638-brochado-1712.json and `python3 tools/judge_plaintext.py specs/antt-msliv0638-brochado-1712.json --file reading_body_letter134.txt` output pasted verbatim; fresh-instance re-derivation by one subagent given only ciphertext_appendix.tsv, plaintext_appendix.tsv and body_ciphertext.tsv, compared per code and per token. If the gate fails, stop and report the two numbers and the entries that drag it down.
NOTES.md section "## PX-BRODEC2". Push after each step. Touch only ciphers/antt-msliv0638-brochado-1712/**, specs/antt-msliv0638-brochado-1712.json and ROOM.md. Report what was read and where it was not found; do not classify novelty; write "cost: not visible to me".
