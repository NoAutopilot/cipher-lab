LANE R6 ORCHESTRATOR (recovery and deep work; successor to LANE R5 and heir to YX's 13 gate-passing targets). Opus. Cap $80 own usage; hand off at $80 or 500k context. Started 25 Sept 2026 15:42 UTC by parent 7b (session_01K7ZbE95o1pUW5gof8VA5PR). Window: allowed on this account; `rejected` on any session stops everything.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`, LESSONS.md, STATUS.md sections "LANE R5 handoff" (written by the closer, may land after you start; else the R5 state in status.json lanes and ciphers/fr2933-salviati-1525/NOTES.md), "LANE YX handoff", "LANE CX handoff", "LANE OX handoff"; `python3 tools/room.py --digest "2026-09-25 13:00"`. ROOM claim "LANE R6 orchestrator: Salviati remaining leaves and code+mark; YX's 13 gate-passing targets by lead class". Workers get `.claude/briefs/runs/2026-09-24-lane-r4-common.md` plus the 25 Sept COMMON addendum plus a job brief `.claude/briefs/runs/2026-09-25-lane-r6-<job>.md` with a wall-clock box. At most 8 live workers; Sonnet for passes and captures, Opus for reconciliation, key reading and cipher reasoning; check-ins every 30-40 minutes via send_later, reading get_session cost on every worker.

Jobs, in order:
1. Salviati 1525 (`ciphers/fr2933-salviati-1525`): finish f.55v (pass B resumes at line 12 from passB_f55v.tsv, H1's commit-every-six-lines cadence), then f.56r, f.56v, f.57r, f.57v (one Sonnet worker per leaf, cap $12 and 60 minutes, the box-keyed method that passed on f.55r at 84.8 percent; check what H3 and H4 pushed before re-doing a leaf), each gated at 80 percent; then one Opus worker (cap $10) runs `control/codemark_curve.py` under cm at the real pooled N with the control first, and the crib loop on top only if the blind control reads above 45 percent; a reading is reported only with the judge output and a fresh-instance re-derivation; ROOM "for LANE V6: reading ready".
2. YX's 13 gate-passing targets (the list and each one's route are in the LANE YX and LANE CX handoffs): take them in lead-class order (COMMON item 4), one Sonnet worker per target at cap $6 and 45 minutes for the cheapest route named, Opus only where the route is a key reading; a target whose route is a copy order or a local-runner row goes to ASKS/LOCAL-QUEUE and is not worked.
3. From N4's handoff: PP-04 ANTT Costa Cabral 1865 (interlined plaintext: capture and key as a dataset, Sonnet $5) and KT-01 Villeroy-Bongars 1604 only if Tomokiyo's Bongars paper becomes readable (one attempt through the open indexes, else stays held).
Not this lane: the Nevers key no.60 set (blocked on the hand, no more spend), Seure (blocked), the other account's targets, anything without a passing intake gate.
After every worker: LEDGER row with the get_session cost (ledger_check.py), ASSIGNMENTS, archive, status.json results row when a dataset or reading lands, ROOM line. Handoff "LANE R6 handoff" at the top of STATUS.md's handoffs; LEDGER row for yourself; ROOM done line; stop.
