LANE B7 ORCHESTRATOR (breadth; successor to LANE B6, whose handoff is STATUS.md "LANE B6 handoff (session_0125z25Ujw4ehAavXcyAfwXz)", closed 04:30 UTC 26 Sept 2026). Opus. Cap $25 own usage; hand off at $25 or 300k context (B5 and B6 each reached 290k in about 90 minutes with a dozen workers: write your "LANE B7 handoff" section in STATUS.md from the first worker report, keep its heading fixed, re-check it after every push). Started 26 Sept 2026 at the 04:45 UTC clock read by parent 7e (session_01SvjMDFfJZJ3uK47RYxAQrM). Authorised by CLAUDE.md Pipeline 3a, rule 5 and the intake gate. Window allowed; `rejected` anywhere stops everything.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md Pipeline 3 and 3a, rules 1, 3, 4, 5, Usage 6 (crop tool mandatory for every transcription pass; a single-leaf image job is priced at about $5, not $2-3 -- four B6 workers ran 1.45-2.6x their caps on image work), `.claude/briefs/breadth.md`, `.claude/briefs/runs/2026-09-26-lane-b6-orchestrator.md` and its common file (reuse as yours, header changed to B7), the LANE B6 handoff and its table, NEAR.md, UPDATES.md tail; `python3 tools/room.py --digest "2026-09-26 04:20"`. ROOM claim "LANE B7: breadth, B6's successor". Never touch LANE AX/AX2's targets (WVO Nassau pool, Brochado, pro3055) or the GOLD lane's (koehler, debosnys, kaliningrad).

Jobs, in the B6 handoff's order:
1. szembek-bk1560 (the one cheap recovery on the board): all three leaves carry a period interlinear gloss over nearly every Latin nomenclator group (about 300-350 groups). Intake gate exit 0 pasted (bSZEM wrote the spec; run a check-solved worker first if the gate fails -- Bourdeau's szembek notes and the Polish/Latin editions whole-volume). Then one Sonnet worker per leaf (cap $5, 45 min, line crops through tools/iiif_lines.py or the equivalent for the host, never full-page passes): transcribe code group + gloss word into pairs, then tools/interlinear_align.py to build key.tsv at grade C; control: a shuffle-consistency check (each group gets the same gloss wherever it recurs, against shuffled pairings). Reading through tools/decode_key.py with a decode.json; judge with an era-matched corpus (la_repo for Latin, flag the register). If the key reads the groups, post "for LANE V8: ciphers/szembek-bk1560 reading ready" with the search log.
2. matignon-mayenne-1586: revert bMAT1C's S grades (or re-run M with U held) so the committed reading is back to -1.371 / cover 0.814, then NEAR step 1d: U signs as code words against the period decipherments; numbers into the NEAR row.
3. malsburg-hessen-1636: pass B of ff.3/12 (line crops) plus the IC test against German masc/homophonic/nomenclator controls through tools/family_run.py ($4; fetch done).
4. QUEUE.md "Scored backlog for LANE B6, second pass" rows 2, 4, 6, 7 (Siena, Zeschau, Hessian 1824, Hesse-Kassel/Denmark 1672): intake gate, then the first cheap test with its control, $3 each ($6 with an image fetch and two blind passes).
5. lope-hurtado-1522: Kolosova 2017 and 2024 are unread -- one LOCAL-QUEUE.tsv row for the owner's machine, no worker. 6. untersberg-code: symA against the other Herzog witnesses ($3) only after 1-4 are dispatched.
Rules: intake gate output pasted in every brief; every family through tools/family_run.py; every done line carries target and control numbers; a control-backed gap or a non-test negative is `partial` and a NEAR.md row (you write it; workers never edit NEAR.md, LEDGER.md, ASSIGNMENTS or status.json); never `closed-negative` without every family in the ladder logged with a passed control; readings reported with judge output pasted and the corpus era named; rule 10 wording only; no AskUserQuestion (the owner reads ASKS.md). Spawn with create_session giving source_url https://github.com/NoAutopilot/cipher-lab and source_revision main, prompt opening with the job brief file and the one-line job. Job briefs to `.claude/briefs/runs/2026-09-26-lane-b7-<job>.md`; ledger every report with the get_session cost and a tools/ledger_check.py code; ASSIGNMENTS line; archive after ledgering; run `python3 tools/ledger_check.py` before your own self-ledger row at close; check-ins with send_later (30 minutes while workers run) re-armed before ending each turn; "for the parent: ..." in ROOM.md with cost and context at every check-in. Hand-off: LANE B7 handoff section with a results table like B6's, ledger yourself, ROOM handoff line, stop.
