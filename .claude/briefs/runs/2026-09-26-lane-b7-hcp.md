JOB bHCP (LANE B7, Sonnet, cap USD 5, wall-clock box 45 min; hosts: api.hcportal.eu (images + record API, >= 2 s apart, <= 40 requests), github.com shallow clone, api.openalex.org, api.semanticscholar.org): two HCPortal Marburg items from QUEUE.md "Scored backlog for LANE B6, second pass", in this order:
  A. row 7 -- Hesse-Kassel/Denmark letter with enciphered passages, 4 May 1672, HStAM 4 f Dänemark Nr. 125, ff.2-4, HCPortal record 494 ("Partially solved", unattributed). Slug hessen-daenemark-1672.
  B. row 6 -- Hessian polyalphabetic message, 20 Feb 1824, HStAM 9 a Nr. 259 f.249, HCPortal record 513 ("not solved", polyalphabetic). Slug hessen-1824.
Intake gate (orchestrator, 05:00 UTC 26 Sept 2026): neither folder exists (`no such target directory`, exit 1) -- so for each, step 0 is the minimal check-solved of `.claude/briefs/breadth.md` "Intake step" (both solver repos in fresh shallow clones: Bourdeau CATALOGUE.md #340/#341 and any folder; HCPortal record JSON `api.hcportal.eu/api/cryptograms/<id>` incl. its solution/attachments; one OpenAlex + one S2 query), NOTES.md lines 1-2 in the gate's format, gate re-run pasted; proceed only on exit 0. See ciphers/malsburg-hessen-1636/NOTES.md for the HCPortal route bMAL used.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b7-common.md` (it governs).
A (first, ~USD 2.5): fetch the 3 images once (manifest.json). Find what HCPortal's "partial" solution is (record JSON, attachments); cut line crops of the enciphered passages (tools/iiif_lines.py --image ... --debug, pasted), count signs, describe the system; if a partial key is attached, apply it (tools/decode_key.py with decode.json) and say what share it reads, grade H for the attached key's values. The first cheap test is that: what the existing partial reads and what is left. Control only if you make an S claim.
B (~USD 2): fetch the one image; crops; one blind transcription; IC and periodic IC (Friedman/Kasiski periods 2-20) vs a German de20 control of the same N under a periodic Vigenere of the best period (tools/family_run.py specs/hessen-1824.json --family periodic_vigenere --seeds 3 --control-only first; the target only if the control reads, else CONTROL BELOW GATE is a non-test). Write specs/hessen-1824.json with a de20 judge block and the era note.
Stop before starting B if A has taken > 60% of cap or box; push A.
Output per target: ciphers/<slug>/NOTES.md, specs/<slug>.json with cheap_test_done. ROOM done line <= 600 chars covering both. Report what was found; do not classify novelty. Final reply at most five lines, requests per host.
