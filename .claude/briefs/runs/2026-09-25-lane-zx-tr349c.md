ZX-TR349C (Sonnet, wall-clock box 90 minutes, also a minimum working time; at most 2 subagents at once). Target: ciphers/clair349-este-guise-1556. Parent: LANE ZX orchestrator session_01MxueEQJUGF9PWJiYcVyvBM. Kind: recovery (key source `period`, fr.20974; plus a contemporary interlinear decipherment on the ciphertext leaf itself). Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md), .claude/briefs/transcription.md, NOTES.md sections "ZX-349", "ZX-349B" and "ZX-KEY349" in full, images/atlas/atlas.tsv (S01-S80). Intake gate: `partial`, exit 0 (orchestrator 15:44 UTC 25 Sept 2026; ZX-KEY349 17:12 UTC). No network: every image needed is on disk.

ZX-KEY349 finished the key and found the letter carries a small interlinear gloss above its codes. This job produces the settled-enough ciphertext and the gloss, with no decode.
1. Recut the 33 cipher lines from images/clair349_f9_right_full.jpg with enough top margin to include the gloss band above each code line (the current images/lines crops cut part of it off); same line boundaries as images/lines (bands 4-36 of the old detection), write images/lines_g/line01..33.jpg, check each by eye at least at the first, middle and last line, record the recipe in images/manifest.json. Keep images/ under 30 MB (you may delete images/atlas/xq_crops and sign_crops; their sheets remain).
2. Two fresh blind passes (subagents), each given only the lines_g crops, images/atlas/sheet.jpg and atlas.tsv (codes and shapes, no meanings), and these rules: digits as digit groups, and the z-shaped mark is the digit 2 (write 12, 62, 22, never a separator); a vertical bar or slash is `|` (a word divider, its own token); every other mark is an atlas code S01-S80, or X? with a description; keep S34 (hooked A + o), S69 (A + hooked v) and S76 (plain A) apart; the small letters written ABOVE a code are its gloss and go in a separate `gloss` column on that token's row (blank if none, `?` if present but illegible), never as a cipher token; "2429"/"3" on line 1 and any BnF stamp are not text. Columns: line, position, token, kind (digit|sign|divider|X?), gloss, grade (H/M), note. Write passE.tsv, passF.tsv. Wait for each subagent's completion report before committing its file (ZX-349's lesson).
3. Reconcile the token stream with tools/reconcile_passes.py passE.tsv passF.tsv --crops images/lines_g (dividers included as tokens); report pooled and per-line agreement against the 60% gate, beside ZX-349's 39.3%. Separately report gloss agreement (on aligned tokens where both passes wrote a gloss). Write ciphertext_draft.tsv with both columns (token, gloss) and H where the passes agree, M where the reconciler took one side.
4. Key-vs-gloss consistency, as a check on both: for every aligned token with an agreed gloss, compare the gloss with key_alpha.tsv/key_nomen.tsv's value(s) for that code; report agreement per code (a TSV) and list every code where gloss and key disagree more than once. Do not decode, do not write reading.txt, do not settle key homographs; that is the next job.
If the gate passes, write the next job's line (settle disagreements from the image, decode with tools/decode_key.py, compare against the gloss, fr judge with the fr16 corpus, re-derivation). If it fails, write the per-line table and the likeliest cause.
NOTES.md gets one section "## ZX-TR349C (25 Sept 2026)". Status stays `partial`. Touch only ciphers/clair349-este-guise-1556/** and ROOM.md. Push after each step. Report what was found and where it was not found; do not classify novelty; write "cost: see the lane ledger". Never call AskUserQuestion.
