YX-WMDICT (Sonnet, wall-clock box 45 minutes; at most 2 subagents). Target: ciphers/wellington-maitland-1812 (Wellington to Maitland, 2 Sept 1812; dictionary code, plaintext known at grade C from Gurwood; the printed dictionary edition is unidentified). Parent: LANE YX orchestrator session_01PHaEdHeQY2FtMHLo9yGeoe. Kind: recovery (identify the codebook). Read the lane COMMON, the target NOTES.md "Next step", DICTIONARY.md in full (sections 6 and 8 list what is tested and what remains), codebook.tsv, check.py, and tools/htrc_ef_headwords.py --help. Never re-test an edition DICTIONARY.md lists as tested.

Job: one round on the editions still untested, cheapest route first.
1. From DICTIONARY.md, list the remaining candidates (Scott 1797/1799 and later Scott, London Entick editions 1801-1811, Dublin Entick reprints, Jones 1800, Fulton and Knight 1802-1808, London Perry, others it names). For each, find a copy: Google Books full-view (GOOGLE_BOOKS_KEY, &country=US, filter=full), Internet Archive (advancedsearch), HathiTrust Bibliographic API then HTRC Extracted Features (tools/htrc_ef_headwords.py). Record per edition: found where, or not found and where searched.
2. Test each found copy against codebook.tsv's constraints (headword -> page, and column position where known) with the existing scripts (check.py, htrc_ef_headwords.py); add an option to a tool rather than a private copy. Report per edition the maximum page residual, as the HathiTrust pass did, so a near-miss is visible.
3. Append the results to DICTIONARY.md as a new dated section (tested, residual, route) and one line in NOTES.md. If an edition passes (every constraint within a page), save its identifier and the pages that fix the constraints, and stop; the lane orchestrator briefs the decode.
Hosts: Google Books and IA 1.5 s apart; HTRC >= 1.6 s; at most 60 requests per host. Touch only ciphers/wellington-maitland-1812/**, tools/ options with their tests, and ROOM.md. Push after each step. Report what was found and where it was not found; do not classify novelty; "cost: see the lane ledger"; request counts per host.
