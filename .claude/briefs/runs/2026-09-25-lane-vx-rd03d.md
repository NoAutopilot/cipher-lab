VX-RD03D (Sonnet, time box 40 min, at most 2 subagents). Final worker on ciphers/na-suriname-map-1781 (VX-N01, partial: 17-sign homophonic key C; 'van' read out of sample in 2039's cartouche; blocker: the loop/hook glyph family; з contradiction). Kind: recovery (key source period). Parent: LANE VX orchestrator session_01EYwyYDeAaQReWy7ZuQcoxe. Read .claude/briefs/runs/2026-09-25-lane-vx-COMMON.md (wall-clock box, no self-reported cost, host budgets are hard limits), .claude/briefs/solver.md, and the target NOTES.md in full; build on the files on disk, never redo a finished pass. Rule 7: judge output pasted into NOTES.md (a FAIL reported as a FAIL, never a threshold change), decode.json + `tools/decode_key.py <folder> --check`, one fresh-instance re-derivation subagent for anything newly keyed. Do not classify novelty. This is the FINAL worker on this target in LANE VX: finish with the status line (partial or solved), a "State at close" paragraph in NOTES.md (what is read, grade counts, coverage, the single best next step for a future lane), and stop.
Steps: (1) fetch the loop/hook glyph family's occurrences on 2007A (and 2061) at native resolution through service.archief.nl's IIIF region API (info.json first; region crops only; <=15 requests, hard), settle each occurrence against 2007B's plain text, and resolve or re-state з. (2) count, per target map (2039, 2046, 2077), which signs it uses and how many are keyed now. (3) decode each target as far as the key allows (label by label, unseen signs [?]); per-label table; judge on the concatenated target text (say so); fresh-instance re-derivation of any new signs and of the target readings.
Touch only ciphers/na-suriname-map-1781/**, specs/na-suriname-map-1781.json, ROOM.md.
