PARENT WORKER OUTBOX (Sonnet, wall-clock box 45 minutes, no subagents, no network except git). Job: the owner said the open asks are "way too much blocked on me". Collapse everything a person must do into one file, OUTBOX.md at the repo root, so the owner can act in one sitting. Read CLAUDE.md, ASKS.md (every row whose last cell is open), JSTOR-QUEUE.tsv (queued rows), LOCAL-QUEUE.tsv (queued rows), tools/local_runner_brief.md, tools/jstor_runner_brief.md, and every REQUEST.md and outreach/*.md draft the open rows cite.
Write OUTBOX.md with exactly these sections:
1. "One local runner session" -- a single paragraph telling the owner how to start one session on his own computer (Claude Desktop or `claude remote-control` in a clone; per tools/local_runner_brief.md) and the one-line prompt to give it, which must make that session work through every queued LOCAL-QUEUE.tsv row AND every queued JSTOR-QUEUE.tsv row, unattended, pushing results. Then the list of rows it will cover (id, target, one line each) so he can see what disappears from his plate.
2. "Emails ready to paste" -- one block per email, with To, Subject, and the full body, sign-off "[owner's name]" as a placeholder, merging asks to the same recipient into one email: BnF manuscrits (ASKS 49, 54, clair571 row), Hessisches Staatsarchiv Marburg, NARA College Park (ASKS 55a), Adirondack History Center Museum (ASKS 52, with the address-confirmation note), Koninklijk Huisarchief, AGR Brussels (ASKS 60), Clements Library (ASKS 57), Spencer Research Library (Stair 1710). Reuse the drafts on file; do not invent facts; every folio and reference copied from its REQUEST.md.
3. "Paid orders, ranked" -- a table of every open copy order (TNA batch row 58 and the SP 78 row, BL batch row 56, Arsenal microfilm, Heinsius NA, Monck BL quote) with: cost as the rows state it, what arrives, and an expected-value line in one sentence (how many check-solved-open targets it unblocks and whether a period key or decipherment is known to sit in the same box). Rank by unblocked targets per pound. Recommend the top three.
4. "Decisions, one line each" -- DECODE login retry, FNMT root certificate, Whitworth 1707, GitHub Pages, collaborators, and any "run or waive" outreach-gate row: for each, the default you recommend and what happens if the owner says nothing.
5. "Nothing needed from the owner" -- rows the parent has already reassigned to cloud workers (IA-BORROW items ASKS 53, 55b, 59, L7, Dorabella) and any row you find that a cloud worker can in fact do; list them so they are not re-asked.
Then update ASKS.md: add one row at the end "OUTBOX.md consolidates rows <list>; act from OUTBOX.md" and do not alter the other rows. Post a ROOM claim first and a done line at the end as `parent worker OUTBOX (Sonnet, <session id>)` with tools/room.py; commit, fetch and rebase, push (never force). Never call AskUserQuestion.
