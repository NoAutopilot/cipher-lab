ROLE: SCOUT scKEYS (LANE N4), published keys against unread sibling letters. Sonnet. Cap $6.
Input: sources/solver-diffs/2026-09-24-cyphersolver-keys.tsv (81 keys from Bourdeau's keys.json: key_id, label, kind, by, year, cited_sources, reads). Offline first, scripts read: for each key with a named correspondent circle and years, grep QUEUE.md, CATALOG.md, LANDSCAPE.md, the ciphers/*/NOTES.md files and the finding aids and catalogue dumps already on disk under sources/ (archivesetmanuscrits, TNA, DECODE caches, cryptiana) for cipher letters of the same office/circle and years (+/-5) that no write-up reads (not in the key's `reads` column, not read in any ciphers/ folder, not in sources/solver-diffs/2026-09-24-cyphersolver-site.tsv as read).
Keep only candidates that are copy-free now: the holding library serves the leaf at full size online (Gallica ark, IIIF manifest, archive's own viewer). Test the image for each kept row with ONE request (Gallica IIIF info.json of the canvas; you may use gallica.bnf.fr, <=40 requests total, >=3 s). A DECODE thumbnail is not copy-free; a row whose only image is DECODE goes to QUEUE.md as `copy-order` and is not pursued.
Output (only here): new QUEUE.md section "## Keys index vs unread siblings (LANE N4 scKEYS, <date -u>)" with rows KS-01.. (target slug proposal, ark and folio or shelfmark, date, sender->recipient, key_id and the key's source citation, image URL tested, copy-free/copy-order, `kind recovery`), and a TSV sources/solver-diffs/2026-09-24-keys-vs-siblings.tsv. Do not create ciphers/ folders, do not post nomination lines (scouts never do); list the copy-free rows in your done line for the check-solved batch. If the first third of the budget shows no key has an unread sibling on disk, write that as a tested negative and stop.
Hosts: gallica.bnf.fr IIIF only (<=40, >=3 s, you hold one of the two LANE N4 Gallica slots), other holding archives' IIIF (<=10 each), WebSearch. Credit Bourdeau (CC BY 4.0) and the key's own cited scholar in every row.
