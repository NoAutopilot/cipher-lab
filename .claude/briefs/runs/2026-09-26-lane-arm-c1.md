JOB ARM-C1: family C -- build the `nomenclator` family for tools/family_run.py from ciphers/armstrong-madison-1808/design/family_C_spec.md and run its matched control; the target runs only if the control clears its gate (the tool enforces this). Fable (claude-fable-5-1). Stop and push at USD 15 or 100 minutes (units: build + offline test 35 minutes; control 1 at 3 seeds about 20 minutes; control 2 (15 Feb letter blind) 10 minutes; shuffled-target 10 minutes; target 10 minutes only if gated; margin 15; do not start a unit that would cross 80 percent of either figure; do not run a background computation beside a foreground unit -- Usage 6, GOLD-K3). At most 2 subagents, Sonnet, for scripted runs only. Lane: LANE ARM orchestrator session_011gEKqukZAxE6FjtQCzsUQ8. Written 26 Sept 2026 by LANE ARM.

INTAKE_GATE_OUTPUT (live, 26 Sept 2026 06:46, TOMO-REPLY): ciphers/armstrong-madison-1808: open (line 1) -- edition/page or full-text-search citation found within 6 lines; EXIT:0.

Read first: `.claude/briefs/runs/2026-09-26-lane-arm-COMMON.md` (binding; you are on Fable for this job), ciphers/armstrong-madison-1808/design/family_C_spec.md (your spec -- follow it, and log any deviation with the reason), HYPOTHESES.md (ARM-CODES, ARM-DESIGN, ARM-A2 sections), `python3 tools/family_run.py --help` and the source of one existing family in tools/families/ (the interface you implement), tools/data/en18/README.md, tools/data/uscodes-1800/README.md. No network.

Do:
1. Build `tools/families/nomenclator.py` and register it in family_run.py (an option added to the shared tool, Usage 8), with an offline test in tools/tests/ that encodes and solves a tiny synthetic letter. Tokens are whitespace-separated integers; `*`, `**`, `***` and `<..>` are an OOV wildcard. The control generator follows the spec's control 1 exactly: held-out en18 file (Jefferson IX or Gallatin I, excluded from the LM), 369 tokens, particle block 1-99 cold-started, family book with the target's slot frequencies, about 37 dropped OOV words. Recovery = fraction of control tokens whose recovered word equals the true word (report particles and book separately as well as blended -- rule 3's unbalanced-class paragraph).
2. Control 1, `--control-only --seeds 3`, gate 0.6. Report per seed, never averaged away. If the mean is above 0.95, stop and say the control is near ceiling (rule 3): the design needs a harder control before the target means anything.
3. Control 2 (label it design-mismatched): Armstrong's 15 Feb 1808 letter (tools/data/uscodes-1800/decodes/) blind, without the THE=972 key.
4. Control 3: `--shuffle-target` noise floor.
5. Target, only if control 1 cleared: the tool runs it; write the best decode to ciphers/armstrong-madison-1808/families/, paste the judge line (en18; carry ARM-EN18's 14-15 percent FN / 0.26-0.27 spread caveat beside it), and the target's model score against control 3's floor. Never describe decoded text as a reading; first 40 tokens as salad only. A judge PASS: write "for LANE ARM: judge PASS on family C, re-derivation owed" in ROOM and stop.
Append "ARM-C1 nomenclator" to HYPOTHESES.md (family_run writes its rows; add per-seed, per-class and floor numbers), the spec's cheap_test_done entry "C1" with both numbers, one paragraph in NOTES.md.
Files: tools/families/nomenclator.py, tools/family_run.py (registration only), tools/tests/test_nomenclator*.py, ciphers/armstrong-madison-1808/{HYPOTHESES.md,NOTES.md,families/**}, specs/armstrong-madison-1808.json (cheap_test_done only), ROOM.md.
Done line (ROOM, starting `done: for LANE ARM: ARM-C1`): control 1 per seed (particles / book / blended, mean, gate met or not); control 2; floor; target score and judge or "not run: control below gate"; no cost figure.
