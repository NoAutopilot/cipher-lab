PARENT WORKER RETRO-APPLY-Q (Sonnet, cap $6 of usage, wall-clock box 40 minutes, no subagents, no network except git; the container is a depth-50 clone). Job: apply the brief- and tool-only proposals of RETRO-2026-09-26e.md (retrospective q, 26 Sept 2026 07:41 UTC) for parent 7g (session_01Jb4UHR8so3yBUG1NTMdf6C, "LIVE parent 7g · Orchestrator 8"). Authorised by CLAUDE.md "Improvement loop" (the orchestrator applies changes that only touch briefs, tools or workflows; anything that changes the goal, the spend or the owner's asks goes to ASKS.md). Written 26 Sept 2026 at the 08:12 UTC clock read.

First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker RETRO-APPLY-Q (Sonnet, <session id>)`; read CLAUDE.md, RETRO-2026-09-26e.md in full, then each target file before editing it. Edits minimal, in the file's voice. Apply each of the four proposals as the retro's diff, in order: (1) NEAR.md hessen-1824 row -- the parent already moved the row from under "## Closed rows" into the active table at 08:12 (verify with `python3 tools/near_check.py`); apply only the tool half: make `tools/near_check.py` distinguish the active table from the closed table (a 6-column row under the Closed heading, or a 3-column row above it, is a nonzero exit naming the row), with a test case in tools/tests/ (run test files directly; pytest is not installed). (2) A brief that specifies a summed scoring objective names the objective's own degenerate optimum and the control that would expose it -- into the brief template the retro names (.claude/briefs/README.md common tail or the recovery/breadth brief), one paragraph, plus the AX2-4612S example in one sentence. (3) Orchestrator self-ledger rows against get_session -- into .claude/briefs/parent.md and .claude/briefs/README.md's lane-orchestrator addenda as the retro proposes (a lane's self-ledgered close cost is provisional until the parent replaces it in place from get_session; the parent's ASSIGNMENTS done row records both figures), and if the retro proposes a mechanical check, put it in tools/ledger_check.py with a test rather than a new script. (4) A scout's "named cheap test" is checked against its own cited source before it reaches a breadth brief -- into .claude/workflows/scout.js's brief text or .claude/briefs/scout.md as the retro names, and the breadth orchestrator brief's intake step (one line: the orchestrator opens the cited source line before pricing the test). One UPDATES.md row per instituted change (`By` = "RETRO-APPLY-Q for parent 7g", clock-read time). Never touch BUDGETS.md or rate-limit rules; never touch status.json or any ciphers/ folder. Commit by explicit path, `python3 tools/room.py --push <paths>`; ROOM done line `done: RETRO-APPLY-Q: N of 4 items applied (...), tests <pass/fail>, UPDATES rows M`; one-paragraph reply; stop. Never call AskUserQuestion; never print or commit credentials.
