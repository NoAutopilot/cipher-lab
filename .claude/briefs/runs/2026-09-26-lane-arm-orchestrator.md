LANE ARM ORCHESTRATOR (standing campaign lane on Armstrong to Madison, Paris, 20 Feb 1808 -- owner's decision 26 Sept 2026 about 06:40 UTC, "let's go hard at the one he called out", after S. Tomokiyo named it his preferred target for us and suggested the sibling-code vocabulary route; QUEUE.md row 27; CATALOG.md line 160; LANDSCAPE.md line 53). Opus. Cap $100 own usage; hand off at $100 or 500k context by writing "LANE ARM handoff" in STATUS.md (families tried, control and target numbers side by side, what ARM2 runs first) so the parent starts ARM2 from it: this lane runs continuously until the owner stops it or every family in its ladder is logged with a passed control. Started 26 Sept 2026 by parent 7f (session_01J57XZYekjroNoFt7SoSS3W, "LIVE parent 7f · Orchestrator 7"). Window: allowed on this account; `rejected` on any session stops every lane (BUDGETS.md).

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md in full (rules 1, 3, 4, 7, 10; Usage 1-8; the intake gate; the session-title rule in .claude/briefs/parent.md "Handing over": every session you create is titled `LIVE ...` and renamed `ARCHIVED ... (done <time>, $cost code)` before archive_session), `.claude/briefs/README.md` common tail, `.claude/briefs/runs/2026-09-25-lane-gold-orchestrator.md` (the pattern: hypothesis families, sibling-first controls, HYPOTHESES.md, a consolidator per cycle), LESSONS.md, UNSOLVED-SURVEY.md, QUEUE.md row 27, CATALOG.md line 160, LANDSCAPE.md line 53, `sources/cryptiana/blog/` (Tomokiyo's June 2026 Armstrong post, snapshotted by TOMO-REPLY) and Bourdeau's armstrong page snapshot; `python3 tools/room.py --digest "2026-09-26 06:30"`. ROOM claim "LANE ARM orchestrator: armstrong-madison-1808, standing campaign". Write `.claude/briefs/runs/2026-09-26-lane-arm-COMMON.md` (copy the shape of 2026-09-26-lane-ax-COMMON.md, one copy of each paragraph; `python3 tools/tests/test_common_briefs.py` must pass).

Adopt: parent worker TOMO-REPLY (session_01EMqeFqnLTHBZwamtfdfGJ8, Sonnet, cap $6, brief 2026-09-26-parent-tomokiyo-reply.md, started 06:34) is writing ciphers/armstrong-madison-1808/NOTES.md (check-solved intake), ciphertext.txt (Bourdeau's feb20_ciphertext.txt with provenance) and specs/armstrong-madison-1808.json, and pastes `tools/intake_gate_check.py` output. Read its ROOM done line and get_session; ledger it (get_session cost), ASSIGNMENTS done row, retitle ARCHIVED, archive. If the gate exit is nonzero, your first worker is the check-solved fix, not cryptanalysis (CLAUDE.md Pipeline 2). Paste the gate output in every deep-work brief.

What is known going in (verify each against the snapshots and the editions before relying on it): 369 numeric code groups; a code Armstrong used with Madison that Bourdeau's page calls unique (no other letter in it found so far, POOLS.tsv 599: pool of 1); a claimed solution (AFIO) adjudicated and rejected (LANDSCAPE.md); Krajcovic's crib from Armstrong's 15 Feb 1808 letter to Jefferson (QUEUE row 27: place it against the opening groups and score with a shuffled control); the Papers of James Madison edition team wants it read (Tomokiyo, 26 Sept 2026, "coveted by the editorial team of a prestigious publication project" -- confirm which project from his June post; it is the eventual outreach recipient, through every gate in CLAUDE.md Outreach, never before AUDIT.md). Founders Online (founders.archives.gov) is the curl-reachable full-text edition for the Madison, Jefferson and Monroe papers; Ralph E. Weber, United States Diplomatic Codes and Ciphers 1775-1938 (Chicago 1979; check IA/HathiTrust EF for reachability) reproduces the period's State Department and private diplomatic codes and is the reference for sibling codes.

How this lane works (GOLD's pattern): one append-only ciphers/armstrong-madison-1808/HYPOTHESES.md with a section per family, CONTROL and TARGET numbers side by side, written by tools/family_run.py where a family exists there and by the worker's own script otherwise; sibling first -- no family runs on the target until its matched control (same N=369, same group-value range, same design: a one- or two-part numeric code of the 1800s, English diplomatic prose) has run and read at or above its gate; a Fable consolidator (cap $15) per cycle (about four hours, or when a family's workers have all reported) rewrites the top of HYPOTHESES.md, decides continue/pivot/park per family and writes the next round's briefs; machine gate only (judge_plaintext.py output pasted -- note tools/data/en/README.md's unknown fold reliability, and build `tools/data/en18` from 1800-1815 American diplomatic prose on Founders Online, about 12 minutes per V6-PTCORP, before trusting any en judge on this target); a PASS gets a fresh Sonnet rule-7 re-derivation, then "for LANE V8: reading ready" in ROOM; never solved, new, first, unpublished (rule 10); status words open/partial/found-solved/blocked only; a near solve is a NEAR.md row, never closed-negative (rule 5).

Families, in order (each a job brief with cap, box sized per unit, control and gate named):
A. Crib placement (Sonnet, cap $4): Krajcovic's crib against the opening groups; score = fraction of crib words whose code groups are consistent (one group, one word) across the letter; control = the same crib against 200 shuffled-order versions of the ciphertext (order matters for a positional crib, so this control CAN differ -- check rule 3's orthogonality paragraph before running). Both numbers into the spec's cheap_test_done and HYPOTHESES.md.
B. Sibling-code vocabulary (Sonnet, cap $6 for the corpus; Fable, cap $12 for the design): every published early-US diplomatic code and nomenclator with its plaintext vocabulary (Weber 1979's reproductions; the Madison, Jefferson and Monroe codes on Founders Online, where editors print keys or decodes; Bourdeau's armstrong page and any key he publishes in full or part; DECODE records for 1800-1815 US codes) into `tools/data/uscodes-1800/` as value->word tables with a README of sources; from them a vocabulary prior (word frequencies, name and place lists, the alphabetical ordering property of one-part codes). Design question for the Fable worker: is Armstrong's code one-part (group order follows alphabetical order of the plaintext, which makes value adjacency informative) or two-part; test it on two sibling codes first as the control.
C. Nomenclator solver with the vocabulary prior (Fable design, cap $15; Sonnet runs): tools/family_run.py gains a `nomenclator` family if it lacks one (an option added to the shared tool, never a private copy: Usage 8): hill-climb or anneal over group->word assignments scored by an n-gram model over words plus the prior from B; control = a known sibling letter of about 369 groups enciphered under its own published code, solved blind from the same prior with the true key held out -- report the control's blind read rate first (rule 3's ceiling caution: if the control reads above 95 percent blind, the prior adds nothing measurable and a harder control is needed).
D. Model-in-the-loop reading (Fable, cap $20), only if C's control clears its gate and the target reads partially: candidate groups from C's best restarts, a Fable session proposing words for unread groups under the vocabulary prior, judged by the same script gate; control = the same loop on the sibling letter with its true key held out, gain over C's blind read reported (rule 3's gain gate).
E. Recovery (Sonnet, cap $5, in parallel with A-B): is Armstrong's key or a decode extant? NARA RG 59 (Despatches from US Ministers to France, M34), the Armstrong papers, the Madison papers at LOC (loc.gov JSON API), the editors' own notes in the Papers of James Madison (Presidential Series and Secretary of State Series) on this letter and the 30 Aug 1808 postscript Bourdeau also lists; a copy order goes to ASKS.md and REQUEST.md, never to a worker.
Not this lane: any other target (LANE B8 keeps the breadth specs; LANE V8 verifies; LANE AX2 on the owner account holds the Nassau pool). Hosts: the good-citizen rule and the Access playbook table; one worker per host; Founders Online, IA full text, loc.gov, DECODE listing each at most 150 requests per session, 1.5 s apart.

After every worker: LEDGER row with the get_session cost (run tools/ledger_check.py before your own self-ledger row), ASSIGNMENTS row, retitle ARCHIVED, archive_session, ROOM line; the spec's cheap_test_done and UNSOLVED-SURVEY.md row when a test is logged; a NEAR.md row the moment a family beats its control by a reproducible margin; status.json results row only when V8 has classed a reading. Report to the parent only through ROOM lines ("for the parent:" and "flag"). Check-in every 45 minutes by send_later while workers run; get_session on any transcription or large-unit worker every 15 minutes. Never call AskUserQuestion; never print or commit credentials.
