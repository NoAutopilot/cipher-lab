JOB bMAL28 (LANE B9, Sonnet, cap USD 12, wall-clock box 75 min; hosts: none -- images on disk): malsburg-hessen-1636 record 507, leaf f.28 (hstam_4_h_1411_0028.jpg), full cipher transcription, two blind passes, reconciled, with f.30 (record 508, the same text copied) as the settling witness, then the per-leaf pool gate.
Intake gate: run and paste `python3 tools/intake_gate_check.py malsburg-hessen-1636` (orchestrator 08:17 UTC: exit 0, partial). Nonzero = stop.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b9-common.md` (it governs); NOTES.md sections bMAL3, bMALC (leaf extent), bMALDUP + "Orchestrator check", and "Glyph conventions (bMALG)" -- its convention paragraph goes verbatim into every pass subagent's prompt, and its glyph_map.tsv is passed to reconcile with `--sign-map`.
Units and pricing (Usage 6, per pass): f.28 has ~52 cipher lines after ~3-4 clear lines; L05-L14 already have two passes (transcription/pass_{a,b}_0028.tsv) -- do not redo them, re-reconcile them with the sign map. Remaining ~42 lines = 5 blocks of <= 10 lines; each block x 2 blind passes = 10 Sonnet (claude-sonnet-5) subagent calls at about USD 0.9 each (bMALDUP's rate), plus settling. Stop before starting a call that would cross 80% of cap or box; push after every block.
Steps: (1) crops: `python3 tools/iiif_lines.py --image ciphers/malsburg-hessen-1636/images/hstam_4_h_1411_0028.jpg --out ciphers/malsburg-hessen-1636/crops/0028 --debug` (paste the output; check the overlay; the committed L05-L14 crops must keep their names) and the same for _0030 into crops/0030 (witness only). A pass call gets only its block's crop paths, never a full leaf. (2) passes A and B per block, blind (pass B never sees A), TSV per bMAL2's line/pos/sign format, committed per block. (3) `python3 tools/reconcile_passes.py ... --sign-map ciphers/malsburg-hessen-1636/glyph_map.tsv --crops crops/0028 --out-dir recon_0028` over all lines. (4) Settle disagreements.tsv rows (at most 60; beyond that leave M): for each, open the f.28 crop AND the corresponding f.30 line crop (align by the neighbouring agreed groups) as small zooms of the one group; take the reading both leaves support at H, one leaf only at M. Record which rows the witness settled. (5) Gate: `python3 tools/leaf_pool_gate.py --leaf ciphers/malsburg-hessen-1636/recon_0028/ciphertext_draft.tsv --pool ciphers/malsburg-hessen-1636/ciphertext.txt --json recon_0028/gate.json` (pre-registered by the orchestrator at 08:19 UTC: M <= 0.15, off-form <= 0.05, frequency cosine > relabel p95; the orchestrator admits, not you).
Output: NOTES.md section "Record 507 f.28 (bMAL28)": lines, signs, pass agreement before/after settling, rows settled by the witness, grade counts, the gate line pasted. ROOM done line <= 600 chars: N signs, H/M, agreement, gate line. Final reply at most five lines. Do not edit NEAR.md or ciphertext.txt.
