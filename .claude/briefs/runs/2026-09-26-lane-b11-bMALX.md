bMALX -- malsburg-hessen-1636: cribs from record 509's codes-in-clear-text and the clear clauses on ff.16/23/24/28 (LANE B11 job 3). Sonnet. Cap USD 5, box 45 minutes from your first `date -u`.

Read first: `.claude/briefs/runs/2026-09-26-lane-b11-common.md` (governs), CLAUDE.md rule 3 in full (in particular: a control must be able to fail differently from the target; the per-leaf merge gate) and rule 4, ciphers/malsburg-hessen-1636/NOTES.md (item 4 and the bMALK "Fetch" paragraph for the HCPortal route; the extent notes on 509; "Glyph conventions (bMALG)"; bMALN's section), pool/POOL.md, pool/pooled.tsv, clear_0016.txt / clear_0023.txt / clear_0024.txt / clear_0028.txt, HYPOTHESES.md (bMALK, bMALN rows).

Intake gate (orchestrator ran it 12:24 UTC): `malsburg-hessen-1636: partial (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

Why: letter, block and syllable tables are excluded at the pooled N (bMALH, bMALN) and every family fails the same way, pointing at a larger code (word/name entries). Record 509 (ff.32-33, "around 1637") writes code groups inside mostly clear German/French prose, and ff.16/23/24/28 carry clear clauses beside cipher: the only known-plaintext context this fond has. Note: this is not an interlinear decipherment (no clear text repeats the cipher), so tools/interlinear_align.py does not apply as-is; say so in one line if you consider it.

Units and pricing: 2 leaves of mostly clear prose with about 100 code tokens (extent.tsv: ~60 on f.32, ~40 on f.33). Priced per pass (CLAUDE.md Usage 6): pass A = one Sonnet subagent call per leaf on line crops (about USD 1 each), pass B = your own read of the code tokens only on the same crops (about USD 0.8 a leaf), reconcile, then the crib analysis (about USD 1). Stop before starting a unit that would cross 80% of cap or box.

Steps:
1. Fetch f.32 and f.33 (HCPortal record 509) through the route in NOTES.md (browser UA, `Origin: https://crypto.hcportal.eu`, api.hcportal.eu only, 1.5 s apart, at most 6 requests; images/hstam_4_h_1411_0032.jpg, _0033.jpg, manifest.json entries with URL, sha1, size). Keep the folder under 30 MB (it is about 27 MB: resize to <= 2400 px longest side if needed and record it).
2. Crop step, a command to run and paste before any subagent call: `python3 tools/iiif_lines.py --image ciphers/malsburg-hessen-1636/images/hstam_4_h_1411_0032.jpg --out ciphers/malsburg-hessen-1636/crops/0032 --debug` (same for 0033); check the debug overlay. The subagent gets crop paths only, never the full leaf.
3. Pass A (subagent) and pass B (you), blind to each other, into transcription/0032_passA.tsv etc.: for every code token: line id, position in line, the code as read (bMALG glyph conventions), confidence, and the clear words immediately before and after it (up to 5 each side, as read, with [?] for doubtful words). Reconcile the code tokens (tools/reconcile_passes.py where it fits); settle only disagreements on the crop. Commit after each leaf.
4. Crib table (cribs.tsv): one row per code occurrence -- code, leaf, line, left context, right context, candidate reading class (person, place, title, number/date, function word, noun, verb, unclear) and, where the syntax forces it, a candidate word, each with your confidence. Do this per occurrence without looking at the code's other occurrences (sort the rows by leaf/line, not by code, while you write them).
   Add the clear clauses on ff.16/23/24/28: where a cipher group sits directly against a clear word in those files and pool/pooled.tsv, add the same kind of row.
5. Control (must be able to fail): consistency statistic = among codes seen 2+ times, the share of pairs of occurrences whose candidate class (and word, separately) agree. Shuffle control: permute the candidate readings against the code positions 1,000 times (keep the multiset of readings and of codes), recompute; report real vs shuffle mean and p95, per class, with N of repeated codes. A real/shuffle margin is the gate; a tie is non-discriminating (rule 3), not a pass.
6. Also report: how many of 509's codes occur in pool/pooled.tsv's sign set (same number range and forms? system-match check), and the frequency ranks of the shared ones in the pool.
7. HYPOTHESES.md: one row "bMALX crib consistency" with both numbers side by side; NOTES.md section "## bMALX: cribs from record 509 and the clear clauses (26 Sept 2026)" with the table of codes that are both repeated and consistent above the shuffle (the candidates for job 4), each graded M (no key source) -- never H or C, and no key claim. Count how many codes have a consistent candidate WORD (not just class) across 2+ occurrences: the orchestrator opens a nomenclator family only at 3 or more.
Do not edit NEAR.md, LEDGER.md, ASSIGNMENTS or status.json. Commit by explicit path; `python3 tools/room.py --push <paths>`. ROOM claim first; done line at most 600 chars (codes read, repeated codes N, consistency real vs shuffle mean/p95, system-match share, consistent-word count). Final reply at most five lines. Report what was found and where it was not found; do not classify novelty.
