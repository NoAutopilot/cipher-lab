ROLE: SETTLER (key-consistent, scripted), Lodewijk van Nassau targets (LANE R worker R20). Model Sonnet, cap $5. No subagents.
Target: ciphers/lodewijk-van-nassau-1573-74. Read NOTES.md sections R13 and R18, key.tsv (letter cipher: five numbers per letter in blocks from n; >120 names and nulls), recon/ disagreements and drafts for 4610, 4611, 4612, 4616, decode.json.
1. Script settle.py (--check): for each disagreement, decode each variant with key.tsv and keep the one that makes the surrounding French plausible, scored with a French letter n-gram model built from the sibling plaintexts (plaintext_4613.txt, plaintext_4615.txt) plus any French text already in the repo (e.g. ciphers/fr2980-gramont readings); take a variant only when its score beats the other by a set margin, else M. Log every decision (settle_log.tsv).
2. Open image crops only for up to 30 unresolved disagreements in stretches where one group changes a word; record them.
3. ciphertext.tsv per letter; tools/decode_key.py --check exits 0; per-letter C/M/I/U counts; reading_<nr>.txt with French word breaks as far as the text allows (no context fills).
4. NOTES.md "R20: targets settled": counts, a short French rendering per letter with gaps, what is left. ROOM `for LANE V: ciphers/lodewijk-van-nassau-1573-74 reading ready (counts)`. Check your cost after each step; stop at the cap with what is pushed. Do not classify novelty.
