PARENT WORKER IA-BORROW (Sonnet, wall-clock box 60 minutes, no subagents). Job: take five Internet Archive lending-only page checks off the owner's plate by doing them from the cloud with tools/ia_borrow.py (IA_USER/IA_PASS are in the environment; login was confirmed working 23 Sept 2026, the borrow and page-image steps have never been exercised -- you are the first). Read CLAUDE.md (Access playbook, Internet Archive paragraph; the good-citizen rule; credential handling: never print a credential, never `env`, never `curl -v` or `set -x`), then tools/ia_borrow.py in full.
Items, in this order; one loan at a time, return each loan before the next, stop the whole job after the first hard failure of the borrow step and write what failed:
1. `correspondancede0006jose` (Cuvelier-Lefevre, Correspondance de la Cour d'Espagne VI, 1937): p.647 and any index entries for Mercy -> paste the substance into ciphers/espagnol142-mercy-1648/NOTES.md (ASKS 59; a Fable verifier already read no.1499 in substance -- confirm or correct it from the page image).
2. `sim_cryptologia_1981-04_5_2` (Kahn, Cryptologia April 1981) pp.65-66: read the six five-letter groups listed in ASKS.md row 53 and the msg-3 header/length -> ciphers/koehler-1944/NOTES.md and ciphertext-variants.tsv (do not change ciphertext.txt; LANE GOLD2 owns the target -- post a ROOM line "for LANE GOLD2").
3. `supplementaryrep0000grea` (HMC Supplementary Report 1932) pp.70-74 and the 1650 entries -> ciphers/hamilton-1650/NOTES.md (LOCAL-QUEUE L7).
4. `edwardelgarmemor0000mrsr` (Elgar memoir) the appendix and pp.8-9 range named in specs/cheap-tests/dorabella-1897/test1-letter-search.md -> that log.
5. Farago, The Game of the Foxes (1971): find the IA identifier by search, borrow, read the Koehler chapter for how the Paris messages were enciphered -> ciphers/koehler-1944/ARCHIVE.md (ASKS 55b), ROOM line for LANE GOLD2.
For each item that succeeds: update the ASKS.md row or LOCAL-QUEUE.tsv row status to done with the date and what was read; save page images under the target's images/ only if under 2 MB each. If the borrow step fails on item 1, try item 2 once (a different item may behave differently), then stop and write the exact failure (HTTP status, JSON reason, no credentials) into CLAUDE.md's Internet Archive paragraph as a dated line. Post a ROOM claim first and a done line at the end as `parent worker IA-BORROW (Sonnet, <session id>)` with tools/room.py; commit, fetch and rebase, push (never force). Never call AskUserQuestion.
