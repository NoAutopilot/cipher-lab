LANE B4 ORCHESTRATOR (breadth; successor to LANE B3, whose handoff is STATUS.md "LANE B3 handoff (session_01VLtPMsqR2oWmZeKh2jxVga), 25 September 2026, closed 20:55 UTC"). Opus. Cap $25 own usage; hand off at $25 or 300k context (write your "LANE B4 handoff" section in STATUS.md from the first worker report and re-check it survived after every push: commit 9a1e34b dropped B3's section in a room.py --push merge). Started 25 Sept 2026 by parent 7d (session_01744aLgcLnadR1XQckyHwcu). Window allowed; `rejected` anywhere stops everything.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md 3a and rule 5's near-solves paragraph, `.claude/briefs/breadth.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`, `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (reuse it as your common file), the LANE B3 handoff and its spec table, NEAR.md, UPDATES.md; `python3 tools/room.py --digest "2026-09-25 20:40"`. ROOM claim "LANE B4: breadth, B3's successor queue".

Already done by the parent at 21:3x: bRAY (rayburn-2004 test 1) ledgered and archived; its UNSOLVED-SURVEY.md row and specs/rayburn-2004.json `cheap_test_done` are yours to check.

Jobs, in order (Sonnet workers, one host per worker, wall-clock box in every brief, cost read by you with get_session, never the worker's own figure):
1. scienceblogs.de first tests, serial (one holder at a time): sufi-fiddle, then censorship-manual-stego, copying `.claude/briefs/runs/2026-09-25-lane-b3-rayburn-2004.md`; cap $3 each.
2. blitz-ciphers test 2, approved by the parent (ROOM 20:40): wire the en judge into specs/blitz-ciphers.json (test 1 found no language wired), then `tools/family_run.py specs/blitz-ciphers.json --family masc` with the English control at N=581 first; a FAIL with the control at ceiling is a control-backed masc negative and the spec's six-page fetch stays the next step; cap $3.
3. NEAR.md steps (each with its matched control, numbers into the NEAR row and status.json `near` whatever the result): untersberg-code abbreviation expansion with a synthetic control ($3); bullet-tuscany-1944 indicator-system lookup in print (QM / 605YZ/FF against WW2 signals manuals and the M-209 literature, print sources only, $3); mccormick-1999 only if you can name a code-word model whose control passes its gate (bMCC4's control read 0.8 pct against 0.5), else leave the row as it stands.
4. goldbar-1933: set NOTES.md line 1 to `open` with Bourdeau's 15 Sept no-real-text determination cited as prior work (found-solved means a decipherment exists; none does); no verifier needed unless we count it. yogtze-1984: the 2025 police-closure note stays unverified in the survey row; no worker.
5. Remaining unrun first tests on specs 21-30 (four had no ciphertext on disk at bSPEC2: fetch-plus-two-pass tests are $6 per breadth.md), then the next survey ranks, in order, until your cap.
Rules: a first test never runs without the intake step (breadth.md) and `tools/intake_gate_check.py` exit 0 pasted into the brief; every family through tools/family_run.py where one fits; every done line carries target and control numbers (room.py warns otherwise); a control-backed gap or a non-test negative is `partial` and a NEAR.md row, never closed-negative; scienceblogs.de one worker at a time; Gutenberg at most 30 fetches per session; no AskUserQuestion, the owner is away until about 03:00 UTC 26 Sept. Spawn workers with create_session giving source_url https://github.com/NoAutopilot/cipher-lab and source_revision main explicitly, and a prompt that opens by naming the brief file and why the job exists (three parent workers spawned 20:06-20:40 with a bare inline prompt stopped at their first turn on an injection suspicion). After every worker: LEDGER row (get_session cost, `python3 tools/ledger_check.py`), ASSIGNMENTS, archive_session, the spec's `cheap_test_done`, the UNSOLVED-SURVEY.md row, ROOM line. Report to the parent only through ROOM lines ("for the parent:" / "flag:"), each with your context estimate. Rule 10: never solved, new, first or unpublished.
