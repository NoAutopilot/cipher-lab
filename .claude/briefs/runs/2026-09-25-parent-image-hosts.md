PARENT WORKER HOSTS (Sonnet, stall alarm $5, at most 1 subagent, NO network fetching at all). Job: one table of every image and catalogue host this project has used, in CLAUDE.md's Access playbook, so workers stop rediscovering routes. You report to the parent, session_01FXDfYR3CvGk7tcid1Aav1n, through ROOM.md.

Read CLAUDE.md (Access playbook in full), then harvest from the repository only: every QUEUE.md section header and its method paragraph (the scouts wrote how they reached each host), sources/*/NOTES.md and README.md, tools/*.py and tools/*.js docstrings (gallica_folio.py, iiif_lines.py, digitarq_fetch.py, browser_fetch.js, ia_*.py, htrc_*.py, decode_*.{sh,js}), ROOM.md lines mentioning 403, Cloudflare, challenge, Anubis, TLS, 429 or "dead", and LESSONS.md.

Write a section `### Image and catalogue hosts (table, 25 Sept 2026)` at the end of the Access playbook in CLAUDE.md, one row per host, columns: host | what it serves (IIIF manifest / image API / JSON search / PDF / OCR text) | auth or key (variable name, or none) | works from the cloud? (yes / challenge / 403 / dead, with the date last seen) | route or tool that works | rate rule | where it is documented (file and section). Cover at least: Gallica and BnF archivesetmanuscrits, BL IIIF, e-codices, e-manuscripta, Bavarikon/BSB, ONB, Europeana (note that the project uses the public api2demo key), DigitArq/ANTT, BNP purl.pt, PARES, Huygens (WVO, Heinsius), Nationaal Archief, archieven.nl, Internet Archive (full-text API, lending), HathiTrust (bibliographic, HTRC EF, full-text), Google Books, JSTOR, OpenAlex, Semantic Scholar, DECODE, Library of Congress, NARA, Bodleian, CUDL, Beinecke, Folger, Leiden, KB, TCD, NLS, Antenati, academia.edu, Banco de Portugal. Say "not recorded" where the repository has nothing; never guess. Below the table, one short list: hosts that need the owner's machine (LOCAL-QUEUE.tsv), and free keys the owner could add that the record shows would have helped (with the variable name).

Touch only CLAUDE.md (that section) and ROOM.md. Commit by path, push with tools/room.py --push, ROOM done line, final one-paragraph report (first line: host count and how many work from the cloud), stop.
