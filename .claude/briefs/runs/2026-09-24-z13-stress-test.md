Z13 STRESS TEST (parent worker z13; Opus; cap $8; disk plus at most three fetches). Started 24 Sept 2026 20:27 UTC at the owner's request ("for fun"): a forum poster claims the Zodiac Killer's 13-character "My name is" cipher (Z13) yields ARTHUR LA through a typewriter-keyboard walk, and asks where the mechanism fails. Our job is the stress test they asked for, not a solve. Rules 3 and 10 apply: no verdict on the cipher, only on the mechanism.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md rules 3, 6, 8, 10. ROOM claim line "z13: stress test of the ARTHUR LA keyboard mechanism -- files ciphers/zodiac-z13-stress/". Fetch once (browser User-Agent, 1.5 s apart, at most three requests): the Z13 transcription from Wikipedia's Zodiac Killer ciphers page or zodiackillerciphers.com, and save it to ciphers/zodiac-z13-stress/ciphertext.txt with the source URL and date; do not fetch anything else. The poster's text is in the brief file `ciphers/zodiac-z13-stress/CLAIM.md` (write it there from the parent's message below, verbatim, attributed as "a forum post quoted by the owner, 24 Sept 2026", no poster name).

Work, all in `ciphers/zodiac-z13-stress/`:
1. `mechanism.py`: implement the claimed procedure as code with every choice made explicit as a parameter: board layout (QWERTY three rows), the "reverse track" order, the anchor rule (which cipher letters are copied literally), the "move up then pass one, take next" step, the eight-count on the 8-like symbols (count start convention, direction, boundary reflection at Q and M), and the pass-one/take-next after a count. Run it on Z13 exactly as the poster describes and confirm it yields ARTHUR LA. Then list, per output letter, which parameter values were required, and count the distinct rules and free choices used across the 13 positions (the poster applies different operations to A, E, N, K, the 8-like symbols, and the final N and M).
2. Degrees of freedom: enumerate the rule space (direction up or down, pass 0 to 3 keys, count 6 to 10 with either start convention, reflect or wrap at the boundary, literal or moved for each plain letter) and run every combination on the unchanged Z13. Score every output against a name list (build it on disk from the names already in the repository's corpora plus a 2,000-entry list of common US given names and surnames you write from memory; no fetch). Report how many combinations produce a recognisable given name plus surname fragment of 7 or more letters, and list the ten best-scoring other names the same rule family produces.
3. Matched control (rule 3): generate 1,000 random 13-symbol strings with Z13's own alphabet mix (9 letters, 3 eight-like symbols, 1 other symbol, same positions for the symbols), run the poster's exact fixed rule set on each, and report how often a 6-plus-letter dictionary name appears. Then allow the rule set the same per-position freedom the poster used and report the rate again.
4. `NOTES.md` (status line `closed-negative` if the mechanism fails the control, `partial` only if it survives): what the mechanism is, where it is sequential and where it is chosen per position, the degrees-of-freedom count, the control rates, the other names the same family produces, and one plain answer to the poster's question "where exactly does this mechanism fail". Rule 10 wording: nothing about who the Zodiac was; Arthur Leigh Allen is a public historical figure and may be named as the poster names him. `results.tsv` with the enumeration and control numbers.
Cost: get_session on yourself after step 2; stop at $8 with what is on disk pushed. Push with `MSG=... python3 tools/room.py --push ciphers/zodiac-z13-stress`; ROOM done line with the three headline numbers (rules used, share of rule combinations giving a name, control rate); final paragraph of five lines in plain language for the owner to paste back to the forum if they wish; stop.
