JOB GOLD-K2: Köhler family B' (keyed-tableau running key), the three untried cheap variants, control first each time. Sonnet (claude-sonnet-5). Stop and push at $6 or 75 minutes, whichever first. NO subagents. Lane: LANE GOLD orchestrator session_01DKDynpdEwZK5EokxtjCM3P. Written by the cycle-1 consolidator GOLD-CONS1 (Fable, session_017PRh6MAX17rgp5ayuycZ6c), 25 Sept 2026 19:35 UTC; the orchestrator fills the gate line and the spawn time.

INTAKE_GATE_OUTPUT: <orchestrator pastes `python3 tools/intake_gate_check.py koehler-1944` output and exit code here before spawning>

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), the top block and the "Family B'" section of `ciphers/koehler-1944/HYPOTHESES.md`, `python3 tools/family_run.py --help`, the docstring of `tools/families/keyed_running_key.py`. Claim in ROOM (`tools/room.py`). Read the clock with `date -u` before any date.

What the last round learned (GOLD-2C, Fable, 25 Sept 18:26-19:03 UTC), pasted so you do not rediscover it:
- Family B' = a running (book) key through a mixed tableau c = S3(S1(p) + S2(k)). GOLD-2C ran ONE corner: vig arithmetic, Dutch key model (nl20), one keyword-mixed alphabet drawn from de20+nl20 words (11,437 alphabets x 4 placements = 45,749 candidates), stage 1 ranks by the pooled unigram multinomial likelihood, stage 2 beam-decodes the top 30 (order 6, beam 300, LM_p de20, LM_k nl20).
- CONTROL (German plaintext at 237/178/140/140/229 under a Dutch book key through a keyword tableau, seeds 1-3): 79.1 / 74.0 / 64.7 pct plaintext letters recovered, mean 72.6, gate 0.5 met; the true tableau ranked 1-27 of 45,749 at stage 1 and won stage 2 by 0.35-0.5 nats per letter over its near-misses.
- TARGET: winner `waarvan:plain` pooled joint ll -3.534 per letter, inside the full-pipeline one-time-key noise band (-3.507 to -3.544 on two uniform-random texts of the same lengths); best-of-30 on message 1 -3.470 vs noise -3.477 / -3.492; judge FAIL. Control-backed negative for that corner only.
- The decoder is deterministic on the target (one run per configuration; seeds apply to controls). Stage-1 noise band over 45,749 candidates on 20 uniform texts: min -5.5, median 0.1, max 6.9 nats; the target's stage-1 best was 11.8 (`zustimmung:key`) and did not survive stage 2.
- Lesson logged: never `git rebase --autostash` (or `tools/room.py --push`, which rebases) while a decoder is writing a log inside the tree. Write logs to your scratchpad directory and copy them in after the process exits. Memory: each decoder process takes up to 3-4 GB at order 8; at order 6 / beam 300 (this job's settings) run at most three at once.

Do, in this order, each variant control-first through `tools/family_run.py` (gate 0.5 on the control's mean plaintext-letter recovery over 3 seeds; a variant whose control fails its gate is NOT run on the target: write the CONTROL BELOW GATE row, say so, move on):
1. Beaufort arithmetic, Dutch key:
   `python3 tools/family_run.py specs/koehler-1944.json --family keyed_running_key --corpus tools/data/de20 --param kcorpus=tools/data/nl20 --param arith=beau --param top=30 --param beam=300 --param order=6 --param spaces=1 --seeds 3 --gate 0.5 --label "GOLD-K2 B' beau, Dutch key, keyword-mixed, control before target"`
2. German key (both streams German; under vig the streams are exchangeable, so read the either-stream recovery on the control and report both strict and either-stream):
   same command with `--param kcorpus=tools/data/de20` and `--param arith=vig`, label "GOLD-K2 B' vig, German key". If the module's control cannot hold the key book out when pcorpus and kcorpus coincide, say so and use `--param kcorpus=tools/data/de20` with `--corpus` pointing at a copy of de20 minus one book (make the copy in your scratchpad, not in tools/data).
3. English keyword list (an agent trained for the US may have carried an English word): add `--param wordcorpus=DIR[,DIR]` to `tools/families/keyed_running_key.py` (default unchanged: the words come from corpus + kcorpus), one case in `tools/tests/test_keyed_running_key.py` that the word list changes with the option, then run variant 1's command with `--param arith=vig --param wordcorpus=tools/data/en16_repo,tools/data/de20,tools/data/nl20`. If step 3's tool change would not fit the box, log it as "not run, tool option owed" and stop after 2.
4. For every target run: the family_run row lands in HYPOTHESES.md by itself. Append one short section "Family B' variants (GOLD-K2)" under the append-only part of `ciphers/koehler-1944/HYPOTHESES.md` with, per variant: the control's three numbers, the target's stage-1 best (nats over uniform) and stage-2 best-of-30, the winner's pooled joint ll against the noise band -3.507 to -3.544 (same decoder settings as GOLD-2C), the judge line, and the first 40 letters of both decoded streams of message 1 (documenting what noise looks like, never as a reading). Add the variant to `specs/koehler-1944.json` `cheap_test_done` as one line with both numbers.
5. A candidate reading exists only if `tools/judge_plaintext.py specs/koehler-1944.json --file <decode>` prints PASS; then paste the judge output, write "for LANE GOLD: judge PASS on <variant>, re-derivation owed" in ROOM, and stop. A target pooled joint ll above -3.40 (0.1 nats above the top of the noise band) without a PASS is also a ROOM flag line ("flag: GOLD-K2 <variant> target -x.xxx above the noise band, judge FAIL"), because the consolidator wants to see it.

Hosts: none (disk and CPU only). Requests per host in the done line: 0.
Done line (ROOM, starting `done: for LANE GOLD: GOLD-K2`): per variant "control a/b/c pct (gate 0.5 met/not), target pooled ll x vs noise -3.507/-3.544, judge PASS/FAIL"; variants not run and why; files touched; no cost figure (the orchestrator reads it); no reading described unless the judge passed. Rule 10: never solved, new, first, unpublished; NOTES.md status stays `open`.
