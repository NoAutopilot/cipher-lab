ROLE: SOLVER with matched control, printed cipher (LANE R worker R3). Model Opus, cap $15.
Target: ciphers/oxenstierna-gustav-adolf-1632 (W1): Gustav II Adolf to Axel Oxenstierna, Nürnberg 23 July 1632, printed in Styffe's Oxenstierna series letter 602, pp.821-822 (IA rikskanslerenax00styfgoog). Read NOTES.md, ciphertext.txt, tokens.tsv, extract.py, REQUEST.md, keys/manifest.json. The key is not public (two undigitised Riksarkivet candidates in REQUEST.md; do not chase them).
Step 1 (image over OCR, rule 2): archive.org is LANE T's host; you may make at most 6 requests there: fetch the two printed page images (pp.821-822; find the leaf via the item's _page_numbers.json or scandata, then /download/<id>/page/n<leaf>.jpg or the BookReader image URL) and, if the djvu text cache sources/ia-fulltext/rikskanslerenax00styfgoog_djvu.txt is absent, the djvu text once (gitignored). Correct the OCR token list against the page image into ciphertext_verified.tsv (every [?] row settled or left M; record each change). Push.
Step 2 (design): characterise the system from the verified tokens: are cipher groups whole words/names (code), syllables, or letters (count groups per gap between clear words, value range, repeats, the 91 values' distribution, whether low values behave like letters and high like nomenclator entries); use the clear-text context and the flanking plaintext letters of 21 July and 1 Aug 1632 (Konung Gustaf II Adolfs skrifter, already cited in NOTES.md; the djvu is on IA -- count it inside your 6 requests or use sources/ if cached) for cribs. Language: Swedish with German, 1632 orthography.
Step 3 (attack + control): build a matched synthetic control first: a plaintext from Styffe's other 1632 letters in the same volume (same language mix), encoded in the design you inferred (same token count, same number of distinct values, same share of clear words, same homophone/nomenclator ratio), and run the same solver on it; then on the target. Report both numbers (control accuracy, target score vs the control's null). Any reading: key.tsv + decode.json, tools/decode_key.py --check exits 0, grade per token (S only where the control supports it; M otherwise). A clean negative with the control's number is a result too; write it as such in NOTES.md.
Search log for LANE V if anything reads. Do not classify novelty.
