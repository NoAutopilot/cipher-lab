bMALK -- malsburg-hessen-1636: the Marburg cipher keys HStAM 4 d Nr. 1219-1224 against the pool (LANE B10 job 2). Sonnet. Cap USD 4, box 45 minutes from your first `date -u`. No blind leaf passes of the letters.

Read first: `.claude/briefs/runs/2026-09-26-lane-b10-common.md` (governs); ciphers/malsburg-hessen-1636/NOTES.md (sources items 1 and 4: Bourdeau's lead and the HCPortal API route), pool/POOL.md, HYPOTHESES.md; the clear-text files clear_0016.txt, clear_0023.txt, clear_0024.txt, clear_0028.txt.

Intake gate (orchestrator ran it 10:47 UTC): `malsburg-hessen-1636: partial (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

Lead (Bourdeau, dbourdeau/cyphersolver `oldest/scan_2026-09-23/hard_targets.md` #4, credit it): "Check whether any of the Marburg keys in 4 d Nr. 1219-1224 (HCPortal 519-523) fits before a blind attack" (Antal and Mirka, HistoCrypt 2022, "Wrong Design of Cipher Keys ... Marburg").

Units: 5 HCPortal records (519-523), 1-4 key images each. Estimate per record: about USD 0.6 (metadata + images + reading the key table from the image + the overlap test). Stop before starting a record that would cross 80% of cap or box.
1. For each record 519-523: GET `https://api.hcportal.eu/api/cryptograms/<id>` (browser UA + `Origin: https://crypto.hcportal.eu`), save the JSON to ciphers/malsburg-hessen-1636/keys/hcportal_<id>.json; record shelfmark, date, description, solution field, any attached transcription/solution files. Fetch each key image once (one request at a time, 1.5 s apart) to keys/ as JPEG, longest side at most 2400 px (keep the folder under 30 MB; a parallel worker bMALS is shrinking images/ -- touch only keys/ and your NOTES section), manifest keys/manifest.json with URL, sha1, size.
2. Before reading an image, cut it into row strips with `tools/iiif_lines.py --image <key image> --out ciphers/malsburg-hessen-1636/keys/crops_<id> --debug` and paste the command and output; read the strips, not the full image. Read each key's inventory into keys/key_<id>.tsv: code (as written), plaintext value, confidence H/M. Note the key's design: code range (e.g. 1-99, 100-999), letters vs syllables vs nomenclator words, homophones per letter, nulls.
3. Test each key against the pool: (a) sign-set overlap -- pool/pooled.tsv has 1,828 signs, 173 distinct numeric values; report |pool values covered by the key| / 173 and token coverage / 1,828, beside a control: the same statistic for 1,000 random code sets of the key's size drawn from the key's own numeric range (report mean and p95); (b) if coverage beats the control p95 and exceeds 0.8 of tokens, decode the pool with tools/decode_key.py (a decode.json + key.tsv in keys/<id>/), grade per token, and read the result for German/Latin/French words and the clear-clause context of clear_0016/0028 (does the cipher continue the clear sentence sensibly?). Paste `python3 tools/judge_plaintext.py specs/malsburg-hessen-1636.json --file <reading>` if the spec's judge block is wired (de16, era flag).
4. Record every key in HYPOTHESES.md: one row per key, overlap numbers beside the control. A key that fits (coverage past the control, readable German) is a recovery: `decode_key.py --check` must exit 0; then ROOM `flag` to the LANE B10 orchestrator "reading ready for rule-7 re-derivation" (the orchestrator, not you, posts to V9). A key that does not fit is a logged negative for that key only; the target stays partial.
Hosts: api.hcportal.eu only, at most 30 requests; report the count. On 403/429/challenge stop the host (one retry after a pause). Commit and push after each record (`python3 tools/room.py --push <paths>`). ROOM claim first; done line at most 600 chars with the per-key coverage vs control numbers. Final reply at most five lines. Report what was found and where it was not found; do not classify novelty.
