# LANE G2 worker G: fr5160 1653 band, constrained cryptanalytic solve of f1 + f9 (Opus, cap $10, disk only)

Target: ciphers/fr5160-letellier-1653. Read NOTES.md "Folio 1-2 letter reconciled", "1653 band: natives and folio 9 passes" and "1653 band:
folio 9 reconciled and key trial" (worker A's result: key_1659 beats its character-level control on both 1653 letters, z 3.4/3.7, on codes
36=de, _6=ques, 61=le, _16=se; about 40% of the text is in letter-like signs no table covers). Tools: tools/nomenclator_anneal.py,
tools/subst_hillclimb.py (read --help; add an option rather than a private copy if you need one).

1. Control first: build a synthetic 1653-like ciphertext (same length 754, same token-frequency profile, French plaintext of the period
   from a letter already read in this repo, a nomenclator with the same share of syllable codes) and show what your solver recovers on it
   (percent of tokens correct). If the control reads under ~60%, report that the real text is below what the method can do, and stop.
2. Real run: f1 + f9 together (ciphertext_f1.tsv, ciphertext_f9.tsv; add passA_f11/passB_f11 or a reconciled f11 block only if worker B
   has committed it by the time you start), seeded with the four supporting key_1659 codes, the rest free, the letter-like signs as unknowns,
   the clear phrases as context (they give the sentence frames on each side of a cipher run). Several restarts; report the best, the spread,
   and which values are stable across restarts.
3. Grade: S only for values that are stable across restarts AND whose stretches read as French in context; everything else M. Write key_1653.tsv
   (code, value, grade, evidence), a decode.json for tools/decode_key.py --check (rule 7), and a NOTES.md section "1653 band: constrained
   solve (24 Sept 2026)" with control vs real numbers and per-token grade counts. If nothing is stable, that is a clean negative with its
   control: say so.
COMMON (every LANE G2 worker, 24 Sept 2026). Parent: LANE G2 orchestrator session_015NqJ9uu5Ef3Bo6QaRiGcGp.
- Read CLAUDE.md in full, then this brief, then the target's NOTES.md sections named below; the last 30 lines of ROOM.md.
- First action: `python3 tools/room.py --start`, then a claim line with `python3 tools/room.py "<role> (<model>, cap $N, for LANE G2)" "claim: <target> -- files: ..."`.
  Last action: a `done:` line with the commit hash and a two-line result. Push with `python3 tools/room.py --push <paths>`; stage by explicit path.
- Run every long job in the FOREGROUND (never background a solver or a fetch loop; two workers went idle that way). Commit and push after each
  pass or step, so a rate-limit stop loses nothing.
- Stop at your cap (the session metadata's cost figure). At the cap, push what you have, write a 'progress' paragraph in NOTES.md, post done.
- Rule 4 grades per token, rule 7 a script that regenerates any reading (prefer tools/decode_key.py with decode.json), rule 3 matched controls for
  any negative. Rule 10: no "new", "first", "unpublished", "previously unread"; say "read at grade X" and "not found in <source>, searched <date>".
  Absolute dates; read `date -u` before writing any time.
- Good-citizen rule: one request at a time per host, >= 1.5 s apart, UA `cipher-lab research script (contact via repository)`, IIIF images only on
  gallica.bnf.fr (.texteBrut is altcha-walled), stop a host on 429/403/challenge and log it. Report requests per host in your done line.
- Keep committed images under 30 MB per folder; above that, keep a manifest and put working copies in your scratchpad.
- End: a short report paragraph in the target's NOTES.md (what was found, where it was not found); do not classify novelty.
