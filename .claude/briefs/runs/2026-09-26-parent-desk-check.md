DESK-CHECK (Sonnet, cap $5 of usage, box 40 minutes, no network except git; no subagents). Parent worker for parent 7h (session_01GT3Yv4kUZrBR8pc2s5EToH), at the owner's ask of 26 Sept 2026 16:33 UTC ("make this systematic as well"): CLAUDE.md Usage 8a, rules become tools. Written at the 16:33 UTC clock read.

The incident: outreach/bodleian-rawl-a24-p4.md (ASKS 30) sat on the owner's desk at `status: ready` with a body that told the owner to search Digital Bodleian himself and said the catalogue "has no item record for these leaves", after the desk runner had already established (LOCAL-QUEUE L19, PR 22, 26 Sept 2026) that the volume is catalogued in two parts, not digitised, with item-level records per folio. The draft was stale relative to a landed answer on its own ask, and nothing flagged it; the parent rewrote it by hand at 16:31. Related: the board (tools/build_dashboard.py, `load_asks`, `desk_item`) shows every `status: ready` draft as actionable with no freshness signal.

Build `tools/desk_check.py` with an offline test in tools/tests/test_desk_check.py, and wire it:
1. The tool reads every outreach/*.md header (`status:` line, and any `asks:`/`targets:`/`row` reference in the header or body), ASKS.md, LOCAL-QUEUE.tsv, JSTOR-QUEUE.tsv, SECOND-OPINIONS-QUEUE.tsv and the git log, and prints one line per problem, exit nonzero if any:
   (a) STALE DRAFT: a draft at `ready` or `drafted` whose ASKS row (matched by row number in the draft, or by target folder) has a LOCAL-QUEUE/JSTOR-QUEUE/SO row set `done` or `bounced`, or a NOTES.md/AUDIT.md commit on the draft's target folder, dated after the draft's own status date (parse the date in the status line; fall back to the file's last commit) -- name the newer fact (row id or commit) so the fixer knows what to read.
   (b) DESK MISMATCH: an ASKS row whose status is `open` while a draft for it reads `sent`/`held`, or a draft at `ready` while its ASKS row reads `done`/`queued as ...`/`waiting`.
   (c) BODY CONTRADICTION (cheap heuristics only): a `ready` draft whose body still contains "open https://digital", "search \"", "cannot reach", "has no item record" or "if not imaged" while its ASKS row or a LOCAL-QUEUE row for the same target is `done` -- the runner has done that step.
   (d) NO DATE: a `ready`/`drafted` status line carrying no absolute date (CLAUDE.md rule 6).
   `--fix-suggest` prints, per problem, the one-line status change or the file to re-read; the tool never edits.
2. Wiring: (a) `.claude/briefs/parent.md` duty 6 ("Board and desk") gets one sentence: run `python3 tools/desk_check.py` at every check-in and act on every line before republishing the board; (b) `.claude/briefs/runs/2026-09-26-parent-pr-land-3.md` and the "our side of the loop" section of tools/local_queue_runner_prompt.md get one sentence: after landing a row, run desk_check and, for any STALE DRAFT it names on that row's target, rewrite the draft's body to the landed finding and reset its status date (LADDER-TOOL is editing the same two files right now -- `git pull --rebase` immediately before each edit, append one sentence, commit that file alone, push; on a conflict keep both); (c) tools/build_dashboard.py `desk_item`: show the draft's status date next to "ready to send" and a small "check" chip when desk_check flags it (read a `desk-check.json` the tool writes with `--json`; the board must still build when the file is absent); (d) CLAUDE.md Usage 8a precedent list gains `tools/desk_check.py` in one clause; (e) one UPDATES.md row per instituted change (By = "DESK-CHECK for parent 7h", clock-read time). Run the test file directly (pytest is not installed) and run the tool once on the live repo, pasting its output in your done line. Rule 10 wording; never touch status.json or any ciphers/ folder; never print or commit credentials; never call AskUserQuestion.
First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker DESK-CHECK (Sonnet, <session id>)`. Finish: commit by explicit path, `python3 tools/room.py --push <paths>`, ROOM done line "done: for parent 7h: DESK-CHECK -- tool + N tests pass, live run: K lines (<summary>), wired into <files>"; one-paragraph reply; stop.
