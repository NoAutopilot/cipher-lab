PARENT WORKER RETRO-APPLY-V (Sonnet, cap $6 of usage, wall-clock box 45 minutes, no subagents, no network except git; the container is a depth-50 clone). Job: apply the brief- and tool-only proposals of RETRO-2026-09-26j.md (retrospective v, 26 Sept 2026 18:18 UTC) for parent 7i (session_01U1NiMakQ8PPySH8WixXsmc, "LIVE parent 7i · Orchestrator 10"). Authorised by CLAUDE.md "Improvement loop" (the orchestrator applies changes that only touch briefs, tools or workflows; anything that changes the goal, the spend or the owner's asks goes to ASKS.md). Written 26 Sept 2026 at the 19:00 UTC clock read. Copied from 2026-09-26-parent-retro-apply-u.md.

First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker RETRO-APPLY-V (Sonnet, <session id>)`; read CLAUDE.md, SYSTEM.md section 7, RETRO-2026-09-26i.md in full, then each target file before editing it. Edits minimal, in the file's voice. Apply proposals 1, 2, 3 and 5 of RETRO-2026-09-26j.md as its diffs, in order: (1) the gate-from-an-incident rule (name the cases a gate applies to and one it must NOT block, with a test for each; tools/lq_answer_check.py already has the kind rule from LQ-L20-LAND 17:42 -- add only what the retro adds); (2) plain first-person voice from the start in outreach/README.md and the MAIL-PREP brief pattern, and the voice check folded into the OUT-CHECK checklist; (3) the brief-file naming or claim rule against two accounts writing the same runs/ filename; (5) the ledger_check.py duplicate-session pattern the retro names, with its test. SKIP item 4 (the mailbox send-log shape): parent worker SEND-QUEUE-TOOL (brief 2026-09-26-parent-send-queue-tool.md, running now) implements it inside the new SEND-QUEUE loop -- if item 4 names a piece SEND-QUEUE-TOOL's brief does not cover, list it in your done line rather than applying it. Read SYSTEM.md section 7 first; any new tool gets its SYSTEM.md row and system_map_check must pass before you push.
One UPDATES.md row per instituted change (`By` = "RETRO-APPLY-V for parent 7i", clock-read time). Never touch BUDGETS.md or rate-limit rules; never touch status.json or any ciphers/ folder. Run every tools/tests/ file you touch directly (pytest is not installed) and paste the results. Before every commit run `git diff --stat` and paste it; commit by explicit path, push only through `python3 tools/room.py --push <paths>` (this retro's own item 1 is about a worker that did otherwise). ROOM done line `done: for parent 7i: RETRO-APPLY-V: N of 4 items applied (...), tests <pass/fail>, system_map_check <pass/fail>, UPDATES rows M`; one-paragraph reply; stop. Never call AskUserQuestion; never print or commit credentials; never write the owner's name.

