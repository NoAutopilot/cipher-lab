LANE B2 breadth worker bPOL: pollaky-1865-1875. Sonnet (claude-sonnet-5). Cap $3 or 30 minutes, whichever first. No subagents, no second pass.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b2-common.md` (read it first; it governs) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Spec: specs/pollaky-1865-1875.json. Run `cheap_tests_in_order[0]` only: fetch the four ad images linked from Cipherbrain post 29 (URLs in sources/schmeh/posts/29-pollaky.html; at most 5 requests to scienceblogs.de or the image host named there, >=1.5 s apart, browser UA as bMOU used) into ciphers/pollaky-1865-1875/images/ with manifest.json, and do ONE blind transcription pass yourself (letters and digits as text; an invented-symbol ad as sign codes with a short sign table). Report per ad: N, distinct K, IC, and for letter ads the IC of an English text at the same N and of a uniform random string at the same K (tools/data, no fetch). Mark every count 'single pass' (rule 2). Put ciphertext.txt (single pass, draft), NOTES.md (status open, check-solved not run: say so) in the folder and replace the spec's ciphertext_pending with a pointer. A second pass is test 2, not yours. At $3 or 30 minutes push what is done.
