LANE B3 breadth worker bMCC4: mccormick-1999 token/nomenclator test (NEAR.md row's named next step; spec cheap_tests_in_order[2]). Sonnet (claude-sonnet-5). Cap $3 or 30 minutes, whichever first. No subagents, no network.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (read it first; it governs; its INTAKE STEP applies) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Intake gate first (paste output). Read the NEAR.md mccormick row, ciphers/mccormick-1999/HYPOTHESES.md and cheap_test_done 1-3. Test: tokenize both notes as written (split on the separators the notes use, keep hyphens/slashes as the token boundaries the spec documents), treat each distinct multi-letter token (NCBE, the -RSE family, WLD, ...) as a code word and each single letter as itself, and search for a token->English-word assignment that maximises English word/bigram likelihood (tools/data English corpus; a simple anneal over the token vocabulary actually present is enough; if tools/ has a nomenclator anneal that runs without numpy, use it, else write specs/cheap-tests/mccormick-1999/token_anneal.py). Matched control: synthetic English of the same token count, written in the same scheme (frequent words replaced by 4-letter code tokens at the target's code-token share, the rest vowel-dropped or kept as the target's letter share suggests -- document the choice), 3 seeds: report the fraction of code tokens the solver recovers. Also the target's own tokens shuffled in order (3 shuffles) as the false-positive floor. Gate: if the control recovers < 0.5, it is not a test (partial). Report control recovery, target score, shuffled score side by side; judge the target decode with judge_plaintext.py. cheap_test_done.4. Do not edit NEAR.md. A judge PASS is only 'for LANE B3: candidate', never solved.
