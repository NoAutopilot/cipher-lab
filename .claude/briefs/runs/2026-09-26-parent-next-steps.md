PARENT WORKER NEXT-STEPS (Sonnet, cap USD 4, box 45 minutes, no network except git). Parent: the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n); context: OPTIMIZATION-2026-09-26.md section (c) -- 63 of 111 open or partial target folders end with a written next step nobody has run; lanes are opened from scout picks, so that backlog is invisible. Job: make it visible and make lanes pull from it.
1. tools/next_steps.py: scan every ciphers/*/NOTES.md whose first line status is open, partial or blocked; extract the folder's named next step -- the last paragraph or bullet that contains "next step", "Next:", "next job", "for whoever picks this up", "successor" or "follow-up" (case-insensitive; take the most recent such paragraph in the file); classify a blocker type from its wording: needs-image (copy order, REQUEST.md, not digitised, no image), needs-key (no key, below unicity, key source), needs-person (owner, LOCAL-QUEUE, ASKS, HathiTrust page, JSTOR), needs-edition (check-solved, edition volume), runnable (anything else, including crops, second pass, atlas, alignment, control re-run); estimate a cost band from .claude/briefs/README.md "Per-unit pricing precedents" (S under 3, M 3-8, L over 8 USD) by keywords (crops/pass/leaf -> M; alignment/key -> M; full transcription -> L; grep/check -> S); note the NEAR.md row if the folder has one and the last ledger date touching the folder (grep LEDGER.md for the folder slug). Output NEXT-STEPS.tsv at the repo root: folder, status, blocker, cost_band, near_row (y/n), last_touched, next_step (one line, 200 chars), plus a summary line count by blocker. Idempotent; `--check` exits nonzero if the file is stale against the folders. Offline test in tools/tests/ with two fixture NOTES files.
2. tools/build_dashboard.py: a "Next steps" strip in the Readings view or a new view: counts by blocker and the top 15 runnable rows with folder links; run the existing board tests.
3. .claude/briefs/parent.md: one paragraph under the lane-opening duty: a lane's job 1 is the top runnable row of NEXT-STEPS.tsv for the account's own targets; a scout is spawned only when no runnable row remains; a folder is not left open with a runnable next step for more than a day without a ledger row saying why. Add the same sentence to .claude/briefs/README.md's common tail for lane orchestrators. One UPDATES.md row per instituted change (clock-read time). CLAUDE.md Usage 8a: add tools/next_steps.py to the precedent list in one line.
Never touch BUDGETS.md, status.json or any ciphers/ folder. Post a ROOM claim first and a done line at the end (first line: counts by blocker) as `parent worker NEXT-STEPS (Sonnet, <session id>)` with tools/room.py (two positional arguments); add your LEDGER.md row; commit by explicit path, fetch and rebase, push (never force; keep both sides on shared-file conflicts). Never call AskUserQuestion.
