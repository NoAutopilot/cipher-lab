LANE ZX2 ORCHESTRATOR (deep work on the CX/CX2 gate-passing targets the other account's LANE R6 does not hold). Opus. No dollar budget (owner: cost is not a concern). Opened by the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n) at 19:15 UTC 25 Sept 2026 under parent 7c's rule (ROOM 18:18): every CX or CX2 gate-passing target without an R6 ROOM claim by 19:00 UTC is this lane's, under its triage route; R6 keeps espagnol142-mercy-1648, fr2933-salviati-1525, berthier-napoleon-1812, antt-fcc-costacabral-1865 and rah-xiquena-1868. Read `rate_limit_info` before each dispatch and follow BUDGETS.md as written.

First: `python3 tools/room.py --start` (it prints the last UPDATES.md rows and the NEAR.md targets; read both files in full), CLAUDE.md, the LANE CX, CX2, YX and ZX handoffs in STATUS.md, the R6 claim line (ROOM 18:52), .claude/briefs/README.md's common tail, and this lane's COMMON (2026-09-25-lane-zx2-COMMON.md). Rate limit, check-ins (every 30-45 minutes with send_later), worker count (at most 6 live Sonnet workers; Opus or Fable only where the job is a key reading, a tool, or a design decision), first steps, ledgering and handoff: as in 2026-09-25-lane-lx-orchestrator.md with LANE ZX2 for LANE LX. ROOM claim line "LANE ZX2 orchestrator: CX/CX2 hand-offs". Keep a 'LANE ZX2 handoff' section in STATUS.md current from the first worker report. Read each worker's cost and elapsed time with get_session; interrupt past its box (a box is also a minimum working time). Both accounts are live: never touch R6/V6/B2/GOLD targets or ZX's (clair349, brochado, barriere); re-check ROOM.md for a claim in the last 6 hours before each assignment; fetch and rebase before every push.

Pool: the gate-passing hand-offs listed in the LANE CX and LANE CX2 handoffs (STATUS.md) minus R6's five, each with the route those handoffs and ciphers/_triage/2026-09-25-lane-yx-triage.md give it. Order: recovery routes with a copy-free next step first (clair1161-avis-flandre-1688 transcription from the image on disk; ormond-arran-1678 Cipher 1 table from HMC Ormonde vol. 5; la-garde-1577 WVO 6467 lead; clairambault296-paget-1713 sequential canvas sweep past the 129 checked; destaing-gerard-1779; hellen-frederick-1752; then the rest), then cryptanalysis routes only through `tools/family_run.py` (control first; a control below its gate stops the run) with `specs/<target>.json` carrying a real judge block (language with a wired corpus, or cribs); targets whose next step is a copy order or a local-runner row get an ASKS.md or LOCAL-QUEUE.tsv row and are not worked.
Rules that changed today (UPDATES.md): a control-backed gap or a non-test negative is `partial` with a NEAR.md row, never `closed-negative`; every cryptanalytic run goes through family_run.py and leaves a HYPOTHESES.md row; judge_plaintext.py fails closed on a spec with no language/corpus/cribs; transcription is two blind passes at the 60% gate, sign passes need a shared atlas first; done lines never carry a dollar figure.
Reading chain per target: transcription -> solver (per-token grades, judge with a corpus of the right era, fresh-instance re-derivation with --check) -> a ROOM line 'for LANE V6: reading ready' (the other account's verification lane holds verifiers) -> for N3 or better, a second adversarial audit and a second-opinion prompt in SECOND-OPINIONS-QUEUE.tsv.
For the parent: each reading that passes judge and re-derivation, each AUDIT.md verdict, each NEAR.md row you open, and a count of targets started/closed at each check-in.
