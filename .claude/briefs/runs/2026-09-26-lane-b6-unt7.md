JOB bUNT7 (LANE B6, Sonnet, cap USD 3, wall-clock box 40 min): untersberg-code NEAR step (4): a second BLIND transcription pass of Hs. 2398 opening 11 (image on disk: ciphers/untersberg-code/images/hs2398_opening11_inscription_leaf.jpg), then settle line 6 ("loqui"/"vult") and the other rows where pass 1 (bUNT6) and pass 2 disagree.
Intake: `python3 tools/intake_gate_check.py untersberg-code` -- paste output in NOTES.md; exit nonzero = stop.
Steps: (1) Do NOT read bUNT6's transcription, Herzog's print or witnesses.tsv before your pass is on disk. Cut line crops if useful (local PIL crop of the on-disk image; no network). Transcribe line by line into ciphers/untersberg-code/pass_b7.tsv (line, token, confidence); commit and push it. (2) Only then reconcile against bUNT6's pass with tools/reconcile_passes.py; settle each disagreements.tsv row on the image; report the character agreement pass1-vs-pass2 and each pass vs Herzog 1929 p.28 diplomatic (same normalisation as bUNT6, 75/109 baseline). (3) Line 6: state what the image supports, with the crop file name. No expansion or reading this job -- step (5), the abbreviation-list expansion, is a later job; write it as a one-line suggestion. No hosts.
Output: NOTES.md dated section "Second blind pass (bUNT7)" with both agreement numbers side by side; ROOM done line with pass1/pass2/Herzog numbers.
Common rules: read `.claude/briefs/runs/2026-09-26-lane-b6-common.md` in full; it governs. Report what was found and where it was not found; do not classify novelty. Final reply at most five lines.
