V8-SO16 (Sonnet, cap $4, wall-clock box 40 minutes from your first `date -u`: stop and push at $4 or 40 minutes, whichever first; at most 1 Sonnet subagent). Citation check of one second-opinion pull request: PR 16 "Add SO-MERCY-F22 second opinion" (SECOND-OPINIONS-QUEUE.tsv row SO-MERCY-F22, target ciphers/espagnol142-mercy-1648). Parent: LANE V8 orchestrator session_01YRuw3TCf7d1w85DLmYNnw4. Authorised by CLAUDE.md "Operating model" (second opinions: a verifier checks every citation) and LANE V8 brief job 3. Pattern: `.claude/briefs/runs/2026-09-25-lane-v6-socheck.md` (same method, one PR). Read `.claude/briefs/verifier.md` section "Second opinions from outside models", CLAUDE.md rule 10, and ciphers/espagnol142-mercy-1648/AUDIT.md.

Fetch: `git fetch origin 'refs/pull/16/head:pr16'`; the PR adds ciphers/espagnol142-mercy-1648/second-opinions/chatgpt-2026-09-26.md. Do not merge, close or comment on GitHub.
Task: list every factual claim and citation in the answer (edition, page, archive reference, DOI, URL, person, date). Trace each to the source itself (archive.org full text / be-api fts, Google Books with &key=$GOOGLE_BOOKS_KEY&country=US >=3 s, HathiTrust bibliographic API, OpenAlex with Bearer $OPENALEX_KEY, Semantic Scholar x-api-key $S2_KEY 1.1 s, CrossRef, the publisher page). Mark each confirmed (with what you read), not confirmed (searched, not found), or unreachable. Does any confirmed lead show prior print or prior decipherment that would move the target's N3 in AUDIT.md? If yes, post "flag: for LANE V8 and the parent: espagnol142-mercy-1648 class may move, see <file>" and do not change the class yourself. Note: the owner's JSTOR runner answered this target's JSTOR rows in PR 17 (jstor-runs/2026-09-26-0455.tsv on that PR); read them, do not re-query JSTOR. Write ciphers/espagnol142-mercy-1648/second-opinions/CHECK-SO-MERCY-F22.md (claims table, verdict merge/close: merge when the answer is faithful with its unconfirmed citations listed; close when mostly invented); copy the PR's answer file itself to main at its own path; append a "Second-opinion claims not confirmed (SO-MERCY-F22)" list to AUDIT.md (append only, class line untouched); set the SECOND-OPINIONS-QUEUE.tsv row to status `checked`, pr `16`, outcome a one-line summary with confirmed/unconfirmed counts. Commit by explicit path to main (no PR), push with tools/room.py --push. JSTOR-only leads go as rows in JSTOR-QUEUE.tsv.
First `date -u`, `python3 tools/room.py --start`, last 30 lines of ROOM.md, claim with tools/room.py (role first). No AskUserQuestion. Never print a key. Hosts >=1.5 s apart, one at a time. ROOM done line "for LANE V8: PR 16 checked, <merge|close>" with confirmed/unconfirmed counts and requests per host, no cost figure. Five-line report, stop.
