LANE AX2 AX2-SHRINK -- lodewijk-van-nassau-1573-74: folder hygiene, 80 MB against the 30 MB rule (Sonnet, wall-clock box 40 minutes). Parent: LANE AX2 orchestrator session_016sbTkVT8HGNJ6JGjJNmCuM. Read and follow .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md (read "LANE AX2" for "LANE AX") and the common tail of .claude/briefs/README.md. First: `date -u`; `python3 tools/room.py --start`; last 30 lines of ROOM.md. Claim with tools/room.py before other work; never AskUserQuestion; no dollar figures for yourself.

CLAUDE.md Access playbook: "Keep committed images under 30 MB per folder; for more, keep the manifest and a sample and note where the rest can be re-fetched." ciphers/lodewijk-van-nassau-1573-74 is about 80 MB: images/ 31 MB (full-page PNG renders plus L-crops of 4610/4611/4612/4613/4615/4616), images_wv2/ 44 MB (crops_4612 15 MB, crops_comp 20 MB, crops_gloss, crops_rederiv, page JPEGs).
OUT OF BOUNDS (live workers use them): images_wv2/crops_4612/** (AX2-4612) and images_wv2/crops_comp/05801_* (AX2-5801). Do not move, re-encode or delete them; a later pass takes them.
Job:
1. Inventory (`du`, and grep every NOTES.md/AUDIT.md/tsv/json/py in the folder for each image path) into images_manifest_full.tsv at the folder root: path, bytes, sha1, source (WVO PDF URL + page + render command, or the parent image + box for a crop), and "cited by" (files that name it).
2. Write regen_images.sh: re-fetches the WVO PDFs from resources.huygens.knaw.nl and re-renders the full-page PNGs with the same tool and resolution the manifests record (read images/manifest.json and images_wv2/manifest.json for the settings); re-cuts crops with tools/iiif_lines.py from the recorded boxes. Test it on ONE page and ONE crop in your scratchpad (<= 2 PDF fetches, 2 s apart), compare sha1 or, if a renderer is not byte-identical, pixel dimensions and a mean-abs pixel difference, and record the result.
3. Remove from the working tree (git rm; history keeps them, never rewrite history) what regen_images.sh rebuilds and nothing cites as the ONLY copy of evidence: first the full-page PNG renders; then, if still over 30 MB, downscale the remaining crops that are cited (JPEG quality 80, at most 1600 px wide) and note in the manifest that the original is in git history at commit <sha>. Keep a sample (one page and its crops per letter). Target: folder under 30 MB counting the out-of-bounds files (report the number with and without them).
4. NOTES.md: one short section "## AX2-SHRINK: images relocated (26 Sept 2026, LANE AX2)" with before/after sizes, what was removed, the commit that still holds the originals, and how to regenerate.
ROOM done line: "for LANE AX2: folder <before> -> <after> MB (<with/without> out-of-bounds files); regen test <result>".
Files: ciphers/lodewijk-van-nassau-1573-74/{images/**,images_wv2/** except the two out-of-bounds sets,images_manifest_full.tsv,regen_images.sh,NOTES.md own section}. Hosts: resources.huygens.knaw.nl <= 2.
