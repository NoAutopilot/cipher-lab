PARENT WORKER RETRO-APPLY-M (Sonnet, cap $6 of usage, wall-clock box 40 minutes, no subagents, no network except git). Job: apply the brief- and tool-only proposals of RETRO-2026-09-26a.md (retrospective m, 26 Sept 2026 00:08 UTC) for parent 7e (session_01SvjMDFfJZJ3uK47RYxAQrM, noautopilotytbiz account). Authorised by CLAUDE.md "Improvement loop": the orchestrator applies changes that only touch briefs, tools or workflows, records them in the ledger, and puts anything that changes the goal, the spend or the person's asks to the person. Written 26 Sept 2026 at the 00:53 UTC clock read.

First: `date -u`; `python3 tools/room.py --start`; ROOM claim line `parent worker RETRO-APPLY-M (Sonnet, <session id>)` with tools/room.py; read CLAUDE.md, then RETRO-2026-09-26a.md in full, then each target file before editing it. Every edit minimal and in the file's existing voice.

Apply, in order:
1. Proposal 1: `.claude/briefs/parent.md` "Opening a lane" -- the "Size the opening brief for a shift, not a wave" paragraph, as the retro's diff.
2. Proposal 2: `.claude/briefs/breadth.md` -- the "Before re-scouting, score what is already scouted" paragraph, as the diff. Note in the paragraph that SCOUT-RERANK (QUEUE.md "Re-rank for LANE B5", 26 Sept 00:03) ranked the github-held and scPOOL rows but the VX-*/KX-*/KT-*/PX-* rows are still unscored.
3. Proposal 3: `tools/room.py start()` -- prefix the two "keys:" lines with the tool name, as the diff. Run `python3 tools/room.py --start` afterwards and paste its two key lines into your done paragraph.
4. Proposal 4: `tools/room.py push()` -- snapshot STATUS.md/QUEUE.md headings from `origin/main` (fetched fresh) before the commit, via a new `headings_from_ref(ref, relpath)`; add the pre-commit local-edit test case to `tools/tests/test_room_headings.py`; run `python3 -m pytest tools/tests -q` and keep it green. If the change cannot be made in one screen without breaking the existing conflict-retry loop, document the gap in the docstring and say so in the done line instead of forcing it.
5. Proposal 5: CLAUDE.md Usage item 6, the background-CPU-contention paragraph after the RETRO-2026-09-25l unit-loop paragraph, as the diff.
6. Near-solve review notes (RETRO-2026-09-26a "Near solves"): in NEAR.md's fr2933-salviati-1525 row, add to the "What would settle it" column the retro's one-line diagnostic (extend R8-DSN's syllabary control error sweep from 5-7% to 10-14%, Sonnet about $2, to find where the control itself crosses into the target's failing range, before funding the word-level scorer). Do not edit any other NEAR.md row; the Brochado row was touched by LANE AX's AX-BRO3 at 00:37 (owner account) and is theirs.
7. One UPDATES.md row per instituted change (the file's format, clock-read time, `By` = "RETRO-APPLY-M for parent 7e"), and one LEDGER.md-style note is NOT yours -- the parent ledgers you.
Never touch BUDGETS.md or any rate-limit rule; anything in the retro that changes the goal, the spend or the owner's asks goes to ASKS.md as a row, not applied. Commit by explicit path, `python3 tools/room.py --push <paths>` (never force). ROOM done line `done: RETRO-APPLY-M: N of 5 proposals applied (...), tests <pass/fail>, UPDATES rows M`; final reply one paragraph, first line the count. Never call AskUserQuestion (the owner is away until about 03:00 UTC). Never print or commit credentials.
