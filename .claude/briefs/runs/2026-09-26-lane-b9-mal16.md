JOB bMAL16 (LANE B9, Sonnet, cap USD 15, wall-clock box 75 min; hosts: none -- images on disk): malsburg-hessen-1636 record 503, leaf f.16 (hstam_4_h_1411_0016.jpg), full cipher transcription, two blind passes, reconciled, then the per-leaf pool gate.
Intake gate: run and paste `python3 tools/intake_gate_check.py malsburg-hessen-1636` (orchestrator 08:17 UTC: exit 0, partial). Nonzero = stop.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b9-common.md` (it governs); NOTES.md sections bMAL3, bMALC (leaf extent: f.16 opens with ~6 clear lines, then dense cipher ~38 lines; the image is a two-page opening and its right-hand panel (a clear name/docket list) is a separate item: crop only the left page (pass `--region` to iiif_lines if needed)), and "Glyph conventions (bMALG)" -- its convention paragraph goes verbatim into every pass subagent's prompt, and its glyph_map.tsv is passed to reconcile with `--sign-map`.
Units and pricing (Usage 6, per pass): ~38 cipher lines = 6 blocks of <= 7 lines x 2 blind passes = 12 Sonnet (claude-sonnet-5) subagent calls; bMAL23 ran about USD 0.4 a cipher line all in (16.27 for 39 lines, 1.6x its cap) -- that is the rate this cap is sized from, plus settling. Stop before starting a call that would cross 80% of cap or box; push after every block. Transcribe the clear lines too as plain text in a separate file (clear_0016.txt, one pass, you read them) -- they are the letter's crib context.
Steps: (1) crops: `python3 tools/iiif_lines.py --image ciphers/malsburg-hessen-1636/images/hstam_4_h_1411_0016.jpg --out ciphers/malsburg-hessen-1636/crops/0016 --debug` (paste the output; check the overlay). A pass call gets only its block's crop paths, never a full leaf. (2) passes A and B per block, blind (pass B never sees A), TSV per bMAL2's line/pos/sign format, committed per block. (3) `python3 tools/reconcile_passes.py ... --sign-map ciphers/malsburg-hessen-1636/glyph_map.tsv --crops crops/0016 --out-dir recon_0016`. (4) Settle disagreements.tsv rows on the crop as small zooms of the one group (at most 50; beyond that leave M). (5) Gate: `python3 tools/leaf_pool_gate.py --leaf ciphers/malsburg-hessen-1636/recon_0016/ciphertext_draft.tsv --pool ciphers/malsburg-hessen-1636/ciphertext.txt --json recon_0016/gate.json` (pre-registered by the orchestrator at 08:19 UTC: M <= 0.15, off-form <= 0.05, frequency cosine > relabel p95; the orchestrator admits, not you).
Output: NOTES.md section "Record 503 f.16 (bMAL16)": lines, signs, pass agreement before/after settling, grade counts, the gate line pasted. ROOM done line <= 600 chars: N signs, H/M, agreement, gate line. Final reply at most five lines. Do not edit NEAR.md or ciphertext.txt.
