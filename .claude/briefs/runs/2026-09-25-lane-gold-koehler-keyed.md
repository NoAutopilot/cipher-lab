JOB GOLD-2C: Köhler family B', keyed-tableau running key: design, cheap exclusion test, control, target. Fable (claude-fable-5-1). Stop and push at $15 or 90 minutes, whichever first. Lane: LANE GOLD orchestrator session_01DKDynpdEwZK5EokxtjCM3P.

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), `ciphers/koehler-1944/HYPOTHESES.md` in full (Bourdeau's six excluded families; GOLD-2A's family-B control and target numbers for the standard tabulae), `ciphers/koehler-1944/NOTES.md`, `tools/running_key.py --help`, `specs/koehler-1944.json`. Claim in ROOM.
Intake gate: INTAKE_GATE_OUTPUT

State of play: Bourdeau (15 Sept 2026) excluded the standard-tableau running key with a unigram likelihood test (true 924-letter running-key ciphertexts score >= +7.5 nats over uniform in 95 percent of trials; the target scores below uniform, with c rare and z frequent) and left open (a) a running key or book key over a mixed/keyed tableau and (b) a hand-made one-time key. GOLD-2A built a two-stream beam decoder for the standard tabulae and passed its control gate. Your job is the next family, designed by you:
1. Cheap exclusion test first (the analogue of Bourdeau's, made permutation-invariant): under a keyed tableau (e.g. c = S3(S1(p) + S2(k)) for unknown alphabet permutations, and the common special cases -- one keyed alphabet on the plain side, or a Vigenère over a keyword-mixed alphabet, or a Porta/Beaufort variant), the ciphertext letter distribution is a permutation of a convolution of the plaintext and key distributions. Compare the target's sorted frequency profile (and its IC and entropy) with the sorted profiles of simulated keyed-tableau running-key ciphertexts of the same five lengths (German and Dutch plaintext and key from tools/data/de20, nl20; random permutations; 1000 trials) and of one-time-key ciphertexts (uniform key). Report where the target falls in both distributions (percentile), with Bourdeau's standard-tableau number beside it. This is the family's gate: if the target sits outside the keyed-tableau 99 percent band, the family is excluded at this cost and you log it and stop at step 1.
2. If not excluded: choose the attack you think has the best chance (e.g. anneal the permutation(s) with the two-stream beam of tools/running_key.py as the inner scorer, restricted to keyword-mixed alphabets first because that is what an agent could carry), add it to tools/running_key.py as an option (with --help and a case in tools/tests/), and run its MATCHED CONTROL first: German plaintext under a Dutch/German book key through a random keyword-mixed tableau, same five lengths, 3 seeds; gate = at least 50 percent of plaintext letters recovered pooled on 3 seeds. Report control and target numbers side by side. Do not run the target on an attack that fails its control; say what would fix it.
3. Append a section "Family B', keyed-tableau running key" to HYPOTHESES.md (method, parameters, seeds, corpora, CONTROL and TARGET numbers side by side, partial decodes of 15+ letters if any, dead ends); append the step-1 test to `specs/koehler-1944.json` cheap_test_done with both numbers. Any stretch where both streams read above the one-time-key noise band goes to ROOM as `flag:` with numbers, and no plaintext is claimed without `tools/judge_plaintext.py specs/koehler-1944.json --file <reading>` output (the spec's judge is now on de20).
Done line: step-1 percentiles (keyed-tableau band, one-time-key band), gate result, control a/b and target c/d if run, no cost figure. Rule 10 wording.
