ZX-349B (Sonnet, wall-clock box 60 minutes, also a minimum working time; at most 2 subagents). Target: ciphers/clair349-este-guise-1556 (Clairambault 349 f.9, key transcribed from fr.20974). Parent: LANE ZX orchestrator session_01MxueEQJUGF9PWJiYcVyvBM. Kind: recovery. Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md), NOTES.md sections "YX-TR349", "YX-TR349B" and "ZX-349", and images/atlas/atlas.tsv. Intake gate: `partial`, exit 0 (orchestrator, 15:44 UTC 25 Sept 2026). No network unless step 1 needs one key-leaf image (Gallica IIIF, one at a time, >=1.5 s).

State: two blind passes against the key-derived 26-sign atlas agree 39.3 pct (gate 60), and 252 (passC) and 341 (passD) tokens matched no atlas sign (X?). Before any third pass, answer: does this key fit this letter?
1. Key-fit test. Cut 40 random X?-token crops (fixed seed) from images/lines and cluster them by eye into shape types (write atlas/xq_types.tsv: type, example crops, count, description). For each type, look for it on the key leaf (every row of the fr.20974 key image on disk, including the nomenclature word list and any row YX-TR349 did not transcribe). Report: the share of X? types found on the key leaf (and where), the share not found. Do the same for 40 random tokens the passes DID code with an atlas sign (does the letter's S-sign really look like the key's S-sign?).
2. Control for step 1: the same 40+40 procedure on a letter whose key is known to fit, if one with a sign alphabet is on disk (ciphers/dupuy468-carpi-1520 is the pattern); if none is, say so and report step 1 alone.
3. Verdict in numbers. If most X? types are on the key leaf (rows missing from the atlas), extend atlas.tsv with them (codes S27+, crops from the key, no meanings), and write the next brief's line: two new blind passes against the extended atlas. If most X? types are absent from the key, write that the fr.20974 key may not be this letter's key (or covers only its alphabet part) with the counts, and stop: no further passes.
NOTES.md gets one section "## ZX-349B (25 Sept 2026)". Status stays `partial`. Touch only ciphers/clair349-este-guise-1556/** and ROOM.md; keep images/ under 30 MB (crops for the atlas only, small). Push after each step. Write "cost: see the lane ledger". Never call AskUserQuestion.
