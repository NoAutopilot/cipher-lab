bMALN -- malsburg-hessen-1636: a code/table family at the pooled N (LANE B10 job 4). Opus (design and judgement; runs are scripts). Cap USD 8, box 60 minutes from your first `date -u`.

Read first: `.claude/briefs/runs/2026-09-26-lane-b10-common.md` (governs); CLAUDE.md rule 3 in full (matched design, not only N and K; the judge-floor sentence: a judge PASS on the family's decode of the SHUFFLED target voids the judge as a gate; a control that cannot vary on the statistic is a non-test; a re-brief that only turns one knob); ciphers/malsburg-hessen-1636/NOTES.md sections "Glyph conventions (bMALG)", "Marburg keys ... (bMALK)", pool/POOL.md, HYPOTHESES.md (every row); tools/family_run.py --help; tools/families/{block_homophonic,homophonic,syllabary,nomenclator}.py docstrings.

Intake gate (orchestrator ran it 10:47 UTC): `malsburg-hessen-1636: partial (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

What is known: pool/pooled.tsv N=1,828 signs, K=173 (111 numeric: 97 values 0-99 carry 1,629 tokens, 14 values >= 100 carry 15; about 184 tokens are letters/off-form or clear words). Letter-level homophonic with profile=target is excluded at this N with a narrow-pass control (control 0.637 vs gate 0.6; target -1.635 vs real_p05 -0.435; the shuffled-pool decode -1.724). The period Marburg key 519 (c.1607, bMALK keys/key_519.tsv) is a German homophone table on 11-99 (no multiples of 10) -- a design sibling, not this key; the pool uses 10, 20, 30 ... freely. Embedded clear clauses on ff.16/23/24/28 (clear_*.txt) and record 509's scattered codes in clear text are cribs.

Job, in order, stopping at the first unit that would cross 80% of cap or box (units: A about USD 2.5, B about 4, C about 1; state the estimates in your claim line):
A. block_homophonic on the pool (values 0-99; drop >=100 and non-numeric, say how many): control first through family_run at the pool's N and the family's block count, widths 3, 4 and 5 (offset 0; one extra offset per width only if the control passes), gate 0.6, 3 seeds; target only where the control passes. Also test the block hypothesis directly before annealing: under a block design, adjacent values share a letter, so their bigram-context profiles should be closer than non-adjacent pairs -- compute adjacent-pair context cosine vs a random-pair control (can differ from the target: yes, it is computed on the same data under a permutation of labels) and report both.
B. If A excludes or cannot test: a letter+syllable homophonic design (a code value = a letter or a common German bigram/syllable, e.g. en, er, ch, sch, st, und, der), matched to key 519's era and to the pool's value range. If no family covers it, add it as an option to an existing family (preferred: homophonic.py `units=` param) with an offline test in tools/tests/, rather than a private script. Control first at N=1,828, K matched, the composed_enhg corpus bMALH used, gate 0.6.
C. For every target run that reaches the judge: the same family on the shuffled pool (`--shuffle-target 1`) through the same judge; a PASS on the shuffle voids the judge for that family. Try the crib only as a check on a candidate decode (do the codes adjacent to a clear clause read as a sensible continuation?), never as a tuning target.
Log every run with both numbers in HYPOTHESES.md (family_run does this). A control below gate is a non-test, logged as such -- not a negative. Rule 5: never write closed-negative; the target stays partial. A decode that PASSes the judge, beats its shuffle floor and reads German at a crib: `decode_key.py --check` exit 0, then ROOM flag to the LANE B10 orchestrator "reading ready for rule-7 re-derivation".
No network. Commit and push after each unit (`python3 tools/room.py --push <paths>`). NOTES.md section "bMALN: code/table families at pooled N (26 Sept 2026)". ROOM claim first; done line at most 600 chars with each family's control and target numbers side by side. Final reply at most five lines. Report what was found and where it was not found; do not classify novelty.
