LANE KX ORCHESTRATOR (key reuse: what else can the keys we hold read?). Opus. Opened 25 Sept 2026 by the parent (owner-account orchestrator, session_01FXDfYR3CvGk7tcid1Aav1n) on the owner's question "we have this key, what else could it solve". Mechanics exactly as .claude/briefs/runs/2026-09-25-lane-lx-orchestrator.md (rate limit, check-ins, at most 4 live Sonnet workers, stall alarms, intake gate, ledgering, job 6, handoff). Workers get 2026-09-25-lane-kx-COMMON.md plus a job brief you file first as 2026-09-25-lane-kx-<job>.md. ROOM claim line "LANE KX orchestrator: key reuse across the repository".

Gap this lane fills: scKEYS and scTOMO (QUEUE.md, 24 Sept) ran other people's published keys against unread siblings. Nobody has run the keys we built ourselves: about 60 key tables in 30 target folders (find ciphers -maxdepth 3 -iname 'key*.tsv' or 'key*.txt', excluding drafts, candidates, atlas and pass files), plus the published key tables already transcribed on disk (sources/solver-diffs, the Bourdeau key46/key60 tables, Tomokiyo transcriptions). Offices reused keys for years and sometimes across embassies.

Jobs:
1. Mechanical cross-match (Sonnet, alarm $8): write tools/key_crossmatch.py. Load every key table on disk into one normal form (code -> plaintext, with its source folder, office, years, language). Load every unread or partly read ciphertext on disk (ciphertext*.tsv/.txt in targets whose status is open, partial, blocked or closed-negative, and printed-ciphertext passages already transcribed), grouped by sign system. Filter key-ciphertext pairs by compatibility (sign type: digits, letters, symbols; code-length distribution; overlap of the key's code set with the ciphertext's signs, report the share covered). For each compatible pair decode and score with tools/judge_plaintext.py in the key's language against two controls: the same key with codes shuffled, and the ciphertext decoded by an unrelated key of the same design. Positive control first: every key must rank its own letter first (for example the Lodewijk key on WVO 4610), or the tool is wrong -- fix it before trusting any other row. Output KEY-CROSSMATCH.tsv (ciphertext target, key source, coverage, score, z against each control, verdict), ranked. Commit the tool and the table.
2. Office map (Sonnet, alarm $6, can run beside job 1): for each key, its office, correspondents and years from the folder's NOTES.md, then unread letters of the same office and years elsewhere in the repository's lists (QUEUE.md, POOLS.tsv, sources/decode/*.tsv metadata, sources/wvo, sources/huygens, CATALOG.md) and in the same manuscript volume's neighbouring folios. Rows go to a new QUEUE.md section "Key reuse candidates (LANE KX, 25 Sept 2026)", KX-01 onward, never re-listing a row already there, each naming the key and whether images are online.
3. Check-solved (intake gate) on every cross-match hit (z clearly above both controls, coverage high enough to read) and every copy-free office-map row, then readings: apply the key, per-token grades (H from the key), judge output, fresh-instance re-derivation; verifier per reading with the key source; job 6.
Hits on another lane's or the other account's targets (R5: Salviati, Seure, Nevers, fr5761; PX, TX, OX files): post a ROOM note for that lane with the numbers; do not work them.
For the parent: the cross-match's positive-control result, its hit count, each reading that passes its judge, each AUDIT.md.
