JOB bMAL2 (LANE B7, Sonnet, cap USD 6, wall-clock box 45 min, no hosts -- images are on disk): malsburg-hessen-1636: blind pass B of ff.3 and 12, reconcile with pass A, write the spec, then the first cheap test (IC + family_run masc and homophonic against German controls).

Intake gate (orchestrator, 04:50 UTC 26 Sept 2026):
    $ python3 tools/intake_gate_check.py malsburg-hessen-1636
    malsburg-hessen-1636: open (line 1) -- edition/page or full-text-search citation found within 6 lines
    exit 0

Read first: common file `.claude/briefs/runs/2026-09-26-lane-b7-common.md` (it governs); ciphers/malsburg-hessen-1636/NOTES.md; transcription/pass_a_0003.tsv and pass_a_0012.tsv (FORMAT only -- pass B must not read their tokens before it is written).
Steps (price: ~USD 2 per leaf for pass B, ~USD 1.5 for the tests; stop before a unit that would cross 80% of cap or box):
1. Crop step, mandatory, pasted into NOTES: `python3 tools/iiif_lines.py --image ciphers/malsburg-hessen-1636/images/hstam_4_h_1411_0003.jpg --out ciphers/malsburg-hessen-1636/crops/0003 --debug` (same for 0012; pip install numpy pillow scipy if missing); check the overlay.
2. Pass B, blind, from crops only (you, or ONE Sonnet subagent given crop paths only): transcription/pass_b_0003.tsv, pass_b_0012.tsv in pass A's columns. Commit per leaf.
3. `python3 tools/reconcile_passes.py pass_a pass_b --crops ...` per leaf; settle only disagreements.tsv rows on the crop; write ciphertext.txt (reconciled, as transcribed; unsettled rows marked) and agreement rate per leaf.
4. Write specs/malsburg-hessen-1636.json (model on specs/szembek-bk1560.json and another spec with a wired judge: ciphertext source and date, alphabet, N, K, constraints, cheap tests in order, judge block `language: de16`, min_word_cover 0.5, and the corpus-era note: de16 vs a 1637 chancery letter).
5. First cheap test: IC and sign-frequency profile of the reconciled stream vs German plain (de16), vs a German masc control and a German homophonic control at the target's N and K (compute from the spec), then `python3 tools/family_run.py specs/malsburg-hessen-1636.json --family masc --seeds 3` and `--family homophonic --seeds 3` (control first; a CONTROL BELOW GATE is a non-test and goes in the done line as such). If the numeral stream looks like a nomenclator (K > ~60, many singletons), say so and run homophonic only with --param profile=target. Record both numbers in the spec's cheap_test_done and in HYPOTHESES.md (the tool writes it).
Output: NOTES.md dated section "Pass B + first test (bMAL2)"; ROOM done line <= 600 chars: agreement rates, N/K, IC target vs controls, family_run target vs control per family. Do not edit NEAR.md. Report what was found; do not classify novelty. Final reply at most five lines.
