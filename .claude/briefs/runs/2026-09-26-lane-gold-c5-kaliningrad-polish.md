JOB GOLD-KAL3: kaliningrad-2015 (the lane's reserve), Polish substitution with a corpus built from Gutenberg, control first each unit through `tools/family_run.py --family homophonic`; Lithuanian only if a corpus is one fetch away. Sonnet (claude-sonnet-5). Stop and push at $5 or 55 minutes, whichever first (sized as units: 1 corpus unit at about 12 minutes / $1.50 including the fetches, plus up to 3 family units at about 12 minutes / $1 each, from GOLD-KAL1's and GOLD-K4's rates, plus one unit of margin). NO subagents. Lane: LANE GOLD3 orchestrator session_01P8v53BYGZvFiriPpxEZy3h (Fable) or its successor. Written by the cycle-4 consolidator GOLD-CONS4 (Fable, session_01CSQuomCj5Simbsx6r6VVcK), 26 Sept 2026 00:51 UTC; the orchestrator fills the gate line and the spawn time. Run after or beside GOLD-KAL2 (different host, different corpus directory, no shared file but the spec: if both are live, the spec's `judge.corpora` is edited by each unit -- rebase before each edit and never leave it on the other job's scheme mid-unit; if that is too fragile the orchestrator serialises the two jobs).

INTAKE_GATE_OUTPUT (live, 01:53 UTC): kaliningrad-2015: open (line 1) -- edition/page or full-text-search citation found within 6 lines (exit 0). Spawned 26 Sept 2026 01:53 UTC by LANE GOLD3 (session_01P8v53BYGZvFiriPpxEZy3h). GOLD-KAL2 finished 01:31 and is NOT live beside you: you have the spec to yourself. KAL2's result, for your context only (do not re-run it): Russian under transliteration, convention B (apostrophe its own sign, N 1066 K 28) -- two schemes (s3p, s1) control means 0.999 / 0.997 vs target judge FAIL -1.706 / -1.729 (real_p05 about -0.89), control-backed negatives; convention A on s1-stripped and the full s3 scheme had CONTROL BELOW GATE (means 0.733 and 0.677, one seed collapsing to 0.206 in the first), so those two remain untested, not excluded; periodic IC flat (best period 17, margin 0.0008 over the shuffle max). Lesson for your control runs: when one seed of three collapses, say so per seed in the row and do not average it away; the brief's restarts figure is a minimum.

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), the top block "Summary, cycle 4" of `ciphers/kaliningrad-2015/HYPOTHESES.md` (profile tables and decision table, ranks 4 and 5), `specs/kaliningrad-2015.json`, `tools/data/nl20/README.md` (the Gutenberg corpus README shape and fetch route GOLD-C used), `python3 tools/family_run.py --help`, the docstring of `tools/families/homophonic.py` (`profile=target`), lines 34-40 of `tools/homophonic_anneal.py` (`fold()`: NFKD then strips combining marks, so ą ę ó ń ś ź ż fold to a e o n s z z; ł is NOT a combining-mark letter and `fold()` DROPS it -- see step 1). Claim in ROOM (`tools/room.py`). `python3 tools/key_livecheck.py` first (the brief names an external host). Read the clock with `date -u` before any date.

What the record says, pasted so you do not rediscover it:
- Target: convention A N 978, K 36, IC 0.0657, 88 compound apostrophe signs (9.0 pct) of 9-10 marked types; convention B N 1066, K 28, IC 0.0718 (`ciphertext_signs.tsv` is convention A; GOLD-KAL2 writes `ciphertext_signs_B.tsv` -- if it does not exist when you start, make it yourself with `make_signs_tsv.py --convention B`, 5 lines: a trailing apostrophe becomes its own sign row after its letter). Baltiysk is 40 km from the Polish border. Polish marked letters (ą ę ó ł ż ś ć ń ź) are about 6.9 pct of Polish letters and 9 types, against the target's 9.0 pct and 9-10 marked types; a marked letter written as letter+apostrophe on a keyboard without diacritics is the hypothesis. German (de20) and the Synodal Bible host text are control-backed negatives; Russian schemes are GOLD-KAL2's.
- Polish plain IC is about 0.061 on its own alphabet; folded to a-z it rises (ż and ź join z, ą joins a, ę joins e); the number of record is what you measure on the fetched corpus at N 978, 20 windows, seed 42, both before and after folding -- write both.

Do, in this order (each is one unit; do not start a unit that would cross 80 pct of $5 or of 55 minutes):
1. Corpus unit `tools/data/pl19/` (aim 12 minutes, one host, at most 6 requests to gutenberg.org 1.5 s apart, descriptive User-Agent; the catalogue lookup counts). Polish prose on Project Gutenberg: Sienkiewicz, Prus, Żeromski, Reymont, Orzeszkowa, Konopnicka, in Polish (`language: pl`); find ids through `https://gutendex.com/books?languages=pl&topic=fiction` (one request) or the Gutenberg catalogue CSV already on disk if any corpus README names one; fetch at most 4 texts, aim for 1.5M letters or more. Write one `.txt.gz` per text with the Gutenberg header and licence footer stripped, README.md (ids, titles, dates, licence, letter count before and after `fold()`, the IC numbers above), MANIFEST.tsv. ł: `fold()` drops it (it is a distinct code point, not l + a combining mark), which would delete 1.8 pct of the letters; write the corpus files with ł already replaced by l (state it in the README) so `fold()` keeps every letter; do the same for any other non-combining letter you find dropped (check: `len(fold(text))` against the count of letters in the raw text should differ by under 0.1 pct). Commit the corpus on its own commit before any run.
2. Unit A: convention A, K 36. Set `judge.corpora` in `specs/kaliningrad-2015.json` to the pl19 files and `judge.corpora_note` to say so (the judge reads the spec, not `--corpus`). `python3 tools/family_run.py specs/kaliningrad-2015.json --family homophonic --cipher ciphers/kaliningrad-2015/ciphertext_signs.tsv --corpus tools/data/pl19 --param profile=target --seeds 3 --restarts 8 --gate 0.9 --label "GOLD-KAL3 homophonic pl19, convention A K36, control before target"`. Control first by the tool; gate 0.9 at this N (German K 36 profile=target read 0.982 at N 978, GOLD-KAL1). If the control is under 0.9, say so, do not lower the gate, log CONTROL BELOW GATE with the number.
3. Unit B: convention B, K 28 (`--cipher ciphers/kaliningrad-2015/ciphertext_signs_B.tsv`, label "GOLD-KAL3 homophonic pl19, convention B K28, control before target").
4. Unit C, conditional: Lithuanian. One request to `https://api.getbible.net/v2/translations.json` (the host GOLD-K1 and GOLD-KAL1 used); if it lists a Lithuanian translation, one fetch of its `.json`, `tools/data/lt/` in the ru19 shape (README, MANIFEST, one `.txt.gz` per book or one file), with the same `fold()` check as step 1 (Lithuanian ą ę ė į ų ū č š ž all fold by NFKD to a e e i u u c s z; verify on the text), then unit A's command with `--corpus tools/data/lt` and `judge.corpora` on it, convention A only, label "GOLD-KAL3 homophonic lt (Bible register), convention A K36, control before target". If getbible has no Lithuanian, write one line in HYPOTHESES.md saying so and do not look elsewhere (no second host this job).
5. Leave the spec's `judge.corpora` on the last unit run, `corpora_note` listing every corpus run this job; one `cheap_test_done` entry per unit ("2-pl-A", "2-pl-B", "2-lt-A") with both numbers. Append "GOLD-KAL3, Polish and Lithuanian" under the append-only part of HYPOTHESES.md (below the cycle-4 top block and any GOLD-KAL2 section, above the family_run table): the corpus numbers (letters, IC raw and folded, window range), per unit the control's three numbers and mean, the target's judge line, the first 40 letters of the target decode (word salad, never a reading), dead ends, exact commands. One paragraph in NOTES.md "GOLD-KAL3"; the UNSOLVED-SURVEY.md row is the orchestrator's.
6. A candidate reading exists only if `tools/judge_plaintext.py specs/kaliningrad-2015.json --file <decode>` prints PASS with the judge on the same corpus the unit ran: paste it, write "for LANE GOLD: judge PASS on <unit>, re-derivation owed" in ROOM, stop the job there.

Hosts: gutenberg.org and gutendex.com at most 6 requests in all, api.getbible.net at most 2; 1.5 s apart; on a 429 or 403 stop that host, log it, one retry after a pause at most; report the counts. Subagents: none. Rule 10: never solved, new, first, unpublished; NOTES.md status stays `open`. Never describe decoded text unless the judge passed.
Done line (ROOM, starting `done: for LANE GOLD: GOLD-KAL3`): pl19 letters and IC (raw / folded, with the window range); per unit "control a/b/c (mean, gate 0.9 met/not) vs target judge PASS/FAIL score (real_p05, null_p99)"; Lithuanian run or why not; units not run and why; files; requests per host; no cost figure; no reading described unless the judge passed.
