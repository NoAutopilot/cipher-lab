LANE B6 ORCHESTRATOR (breadth; successor to LANE B5, whose handoff is STATUS.md "LANE B5 handoff (session_01A4jfQZGS8SUuDamZK19KZq), 26 September 2026, 00:55-02:45 UTC"). Opus. Cap $25 own usage; hand off at $25 or 300k context (B5 reached 289k in 110 minutes with 12 workers: write your "LANE B6 handoff" section in STATUS.md from the first worker report, keep its heading fixed, and re-check it survived after every push). Started 26 Sept 2026 at the 03:05 UTC clock read by parent 7e (session_01SvjMDFfJZJ3uK47RYxAQrM). Authorised by CLAUDE.md Pipeline 3a (a spec and one first cheap test with its matched control before any campaign), rule 5 (near solves), and the intake gate. Window allowed; `rejected` anywhere stops everything.

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md Pipeline 3 and 3a, rules 1, 3, 5, `.claude/briefs/breadth.md`, `.claude/briefs/check-solved.md` (whole volume, never one term or one page range), `.claude/briefs/runs/2026-09-26-lane-b5-orchestrator.md` and `2026-09-26-lane-b5-common.md` (reuse it as your common file, header changed to B6), the LANE B5 handoff and its table, NEAR.md, RETRO-2026-09-26b.md (proposals 2 and 3 are about this lane's own failure modes: ranking candidates Bourdeau had already done, and check-solved corner-cuts), UPDATES.md tail; `python3 tools/room.py --digest "2026-09-26 02:40"`. ROOM claim "LANE B6: breadth, B5's successor". The other account's LANE AX holds the WVO Nassau pool, antt-msliv0638-brochado-1712 and pro3055-clinton-1779; LANE GOLD3/GOLD4 holds koehler-1944, debosnys-1883, kaliningrad-2015: never touch them.

First wave (Sonnet workers, one host per worker, wall-clock box in every brief, cost read by you with get_session):
0. Two check-solved workers first, cap $3 each, from `.claude/briefs/check-solved.md`: matignon-mayenne-1586 (V7-QA5 gap: bMAT searched IA full text for one place name only; sweep the Matignon and Mayenne printed correspondence, the Lettres of Henri III, Tomokiyo's pages, DECODE, both solver repositories, by date and both correspondents) and lope-hurtado-1522 (bLOP re-cited Bourdeau's CSP Spain vol. II search instead of opening the edition: open Bergenroth's Calendar of State Papers, Spain, vol. II on archive.org and search it whole). Each rewrites the target's check-solved verdict; `python3 tools/intake_gate_check.py <slug>` exit 0 pasted before any later test on either.
1. matignon-mayenne-1586 NEAR step: the M/U word-model beam (1,648 M and 1,272 U tokens on 13 leaves), designed by the strongest model (Fable or Opus, cap $5, 45 min), shuffled-line-order control first through tools/family_run.py or a scratch spec with the fr16 judge; numbers into the NEAR row whatever the result.
2. untersberg-code NEAR step: a second blind pass of opening 11 of Hs. 2398 (Sonnet, cap $3) to settle line 6 (loqui/vult), then a period Latin/German abbreviation list; numbers into the NEAR row.
3. A fresh scout (Sonnet, cap $6, `.claude/briefs/scout.md`): rank the github-held candidates against each solver repository's HEAD (fresh clones, `tools/solver_repo_diff.py`), not QUEUE.md's snapshot -- four of nine re-rank rows and 16 of 17 backlog rows were already done in dbourdeau/cyphersolver (B5 lesson; RETRO-2026-09-26b proposal 2). Output: a QUEUE.md section "Scored backlog for LANE B6, second pass" with, per row, the solver-repo HEAD commit checked and the date. It never promotes or solves.
Reserve wave (start when the first wave has fewer than two live workers):
4. The scout's top rows, one first cheap test each with its control ($3, or $6 with an image fetch and two blind passes), intake gate first.
5. bPOOL0's two open remainders only if a route exists that does not need DECODE full-size images (account-blocked): ASV doss. 346 (bagno1652) and Barb.lat 6956's 69 unrun records under the published Pallotto key; otherwise an ASKS.md row, no worker.
6. blitz-ciphers homophonic German K=48 at N=581 through family_run.py ($3) -- parent's yes given here; pages 1-6 add no letters.
Rules: a first test never runs without the intake step and the gate output pasted in the brief; every family through tools/family_run.py where one fits; every done line carries target and control numbers; a control-backed gap or a non-test negative is `partial` and a NEAR.md row (you write it; workers never edit NEAR.md, LEDGER.md, ASSIGNMENTS or status.json); never `closed-negative` without every family in the ladder logged with a passed control; a reading is reported with tools/judge_plaintext.py output pasted and its era-matched corpus named; rule 10 wording only; no AskUserQuestion (anything for the owner is an ASKS.md row). Spawn workers with create_session giving source_url https://github.com/NoAutopilot/cipher-lab and source_revision main explicitly, and a prompt that opens by naming the job brief file and the one-line job. Write each job brief to `.claude/briefs/runs/2026-09-26-lane-b6-<job>.md` before spawning; ledger every report (LEDGER.md row with the get_session cost and a tools/ledger_check.py outcome code) and a hub-seed/ASSIGNMENTS.md line; archive_session after ledgering; check-ins with send_later (30 minutes while workers run), re-armed before ending each turn; "for the parent: ..." in ROOM.md with your own cost and context estimate at every check-in. A reading ready for a verifier goes to ROOM as "for LANE V8: ciphers/<t> reading ready" with the search log named. Hand-off: LANE B6 handoff section with a results table like B5's, ledger yourself, ROOM handoff line, stop.
