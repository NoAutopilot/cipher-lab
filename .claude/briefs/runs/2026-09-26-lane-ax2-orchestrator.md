LANE AX2 ORCHESTRATOR (owner account; successor of LANE AX on the Lodewijk/Jan van Nassau letters). Opus. No dollar budget (owner: cost is not a concern as long as each worker moves a verdict). Opened by the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n) at 05:20 UTC 26 Sept 2026 from the LANE AX handoff (STATUS.md "LANE AX handoff", closed 04:31). Read `rate_limit_info` before each dispatch and follow BUDGETS.md as written.

First: `python3 tools/key_livecheck.py`, then `python3 tools/room.py --start` (read UPDATES.md rows since 04:00 and NEAR.md in full), CLAUDE.md, the LANE AX handoff in full (its "Open leads" list is your queue, in its order), ciphers/lodewijk-van-nassau-1573-74/{NOTES.md,AUDIT.md} (V8 and V8.2 sections, revision log v3), .claude/briefs/README.md's common tail, and this lane's COMMON (2026-09-26-lane-ax-COMMON.md, reused; post a ROOM note that AX2 uses it). Rate limit, check-ins (every 30-45 minutes with send_later or create_trigger; re-arm before you end every turn), worker count (at most 6 live Sonnet workers; Opus only for a key reading or a design decision), first steps, ledgering and handoff (at 400k context, not later): as in 2026-09-26-lane-ax-orchestrator.md with LANE AX2 for LANE AX. ROOM claim line "LANE AX2 orchestrator: Lodewijk 4612 and 5801, remaining 5797 blanks, folder shrink". Keep a 'LANE AX2 handoff' section in STATUS.md current from the first worker report. Both accounts are live: the other account's parent is 7f, its lanes V8 (verifiers; it holds V8-NA172 on the 5797 third blank) and B7 (breadth); never touch their targets; re-check ROOM.md for a claim in the last 6 hours before each assignment; fetch and rebase before every push.

Lessons from AX that are rules here: (1) every transcription pass call gets line crops under 2500 px (tools/iiif_lines.py conventions), never a full page -- the two full-page workers cost 101 dollars for worse agreement; (2) compare passes by content alignment (difflib), never by position; (3) size a worker's box by the job (AX workers finished in 6-24 minutes of 40-120 boxes); (4) a held-out gate on a null-heavy map cannot fail -- gate per class or on known answers; (5) every reading goes through tools/decode_key.py --check, a shuffled-key control with the gate written before the run, the language judge with its per-fold caveat, and a fresh-instance rule-7 re-derivation before any "for LANE V8: reading ready" line; (6) the harvest pool of 13 Nassau letters is spent (triaged 24 Sept, printed in clear in Groen) -- do not reopen it.

Jobs, in order:
1. 4612 (Lodewijk to Orange, unread fourth letter): AX-4612TR2's v2 transcription scores French-word share 68.6 percent under key.tsv vs shuffle max 61.3. A Sonnet worker checks legibility run by run against the 300-dpi crops, settles the 16 unsettled numerals, then decodes under key_full v3 with tools/decode_key.py --check, a 20-shuffle control, and the fr16 judge (per-fold caveat); if the reading beats its control, a fresh-instance re-derivation, then "for LANE V8: reading ready". The earlier block/homophonic negatives ran on a 47.8 percent transcription and are not tests; say so in NOTES.md.
2. 5801 (28 May 1573, period decipherment attached pp.1-5, clear copy pp.7-9): the table 4612/5799 would need if job 1 fails. Line crops are already cut (images_wv2/crops_comp/05801_*). tools/interlinear_align.py for the key; then apply to 5799 and 4612 with controls.
3. Remaining 5797 blanks (182 at p7_spot6, 156 at p8_spot7): 5552/5557 leaves next; 7205 did not carry them.
4. Folder hygiene: ciphers/lodewijk-van-nassau-1573-74 is 80 MB against the 30 MB rule -- keep manifests, downscale or relocate the 300-dpi 4612 renders and full-page crops (a Sonnet worker, before job 2's crops add more).
5. Brochado letter 134 NEAR step (re-segment the 34 drifted appendix entries from images so 69 undecidable thin-code occurrences can be read) only after 1-3 are dispatched.
Do not touch pro3055 (parked on archive images) or the 5549 body (low). Handoff at 400k context or when the queue is spent; ledger every worker from get_session usage; post 'for the parent' lines at each check-in. Never call AskUserQuestion.
