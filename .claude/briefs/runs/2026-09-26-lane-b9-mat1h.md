JOB bMAT1H (LANE B9, Opus orchestrating one blinded subagent, cap USD 4, wall-clock box 40 min; hosts: none, NO gallica fetch): matignon-mayenne-1586 NEAR step (1h) -- a pre-registered high-confidence gate on a third fresh seed. The last step on this target unless it passes (USD 17 spent in LANE B8 alone).
Intake gate (orchestrator 08:20 UTC): `matignon-mayenne-1586: partial (line 1) -- ...`, exit 0. Paste your re-run; nonzero = stop.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b9-common.md` (it governs); NOTES.md sections (1e) bMAT1E, (1g) bMAT1G; align1e/ (make_masked.py, score_control.py), align1g/ (f78_corrected.txt).
PRE-REGISTERED GATE (fixed by the orchestrator at 08:20 UTC before any run; do not change it after seeing data): on the corrected passage (align1g/f78_corrected.txt), same density-matched draw of 20 H codes as bMAT1E/1G, a FRESH seed = 8620 (not 1586, not 7806), one blinded Opus subagent that sees only the masked passage and the two cribs, asked to mark each answer high- or low-confidence BEFORE scoring. PASS iff the subagent gives >= 8 high-confidence answers AND at least ceil(0.9 x n_high) of them are right (8/8, 9/10, 10/11 ...). Anything else is FAIL. Also report k/20 overall, for the record only; it does not gate.
Degenerate optimum to avoid: the gate rewards abstention -- a subagent marking only its 8 surest answers high passes trivially if those are the easy ones. So report which 20 codes were drawn and each code's frequency in the passage; if every high-confidence answer is a code occurring 3+ times in the cribs, say so in the verdict line.
If PASS: run the 17-U-sign target under the same protocol, commit only the high-confidence U meanings at grade C (source "f.79r period decipherment, hand alignment bMAT1H"), decode_key.py --check exit 0, fr16 judge beside the shuffled control (-1.78), both pasted. If FAIL: stop; write in NOTES.md "NEAR step (1h)" what would settle the target now (the f.78v/79r margin line order from native crops, or a period key not yet found) as a one-paragraph next step, and nothing else.
Output: NOTES.md section "NEAR step (1h) (bMAT1H)"; align1h/. ROOM done line <= 600 chars: seed, n_high, right among high, k/20, PASS/FAIL, U signs given C if any. Do not edit NEAR.md. Final reply at most five lines.
