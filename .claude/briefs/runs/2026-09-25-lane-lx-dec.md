LX-DEC (Sonnet, cap $5 as a stall alarm, at most 2 subagents). Target: ciphers/antt-linhares-chave. Job: decode the 26 groups of m0002 against the identified dictionary. Kind: recovery. Parent: LANE LX orchestrator session_01UXTpujdthrPiBDUG57oNwf. Template: .claude/briefs/solver.md (read it) plus the lane COMMON (.claude/briefs/runs/2026-09-25-lane-lx-COMMON.md).

Read first: ciphers/antt-linhares-chave/BOOK.md (the book, the parse rule, the method, the leaf-offset warning), NOTES.md, ciphertext.tsv, key_example.tsv.

Book: Vieyra, A New Pocket Dictionary of the Portuguese and English Languages, Part I (Portuguese-English), London 1809, archive.org `newpocketdiction00viey` (Google Books 0mESAAAAIAAJ). Fetch the page images with the BookReaderImages route BOOK.md used, one at a time, >=1.5 s apart, into images/book/ with manifest.json entries. Read the printed page number on every fetched leaf; never assume an offset (it drifts 10-18). The OCR text may locate a leaf; the page/column/rank is read only from the image.

Steps:
0. Fix key_example.tsv group 5 to 23812 only after you look at m0003 yourself and agree (BOOK.md's reading); note who read what.
1. For each group in ciphertext.tsv: page, column, rank from the image; the headword; apply the trim (from the end, as the worked example settles; if the end trim gives nonsense and a front trim gives a word, record both and grade M). Group 829011 (starts with 8) is a null under the key: record it as a null and say what the key's null rule implies here (the key says a message *starting* with nulls uses "o Diccionario Inglez", i.e. Part II; this one is mid-letter). Count rank with the key's own convention as the worked example fixes it (headwords only; say whether sub-entries/run-ons were counted).
2. Write key.tsv (group -> headword, trim, token, book page/col/rank, grade) and decode.json so `python3 tools/decode_key.py ciphers/antt-linhares-chave --check` regenerates reading.txt and exits 0; if decode_key.py lacks an option this scheme needs, add it to the tool with a test in tools/tests/ (CLAUDE.md Usage 8), never a private decode.py.
3. reading.txt: the Portuguese as read, pages 2 and 3 of the letter, with an English gloss below each line. Grades per token: H (read from the book page), M uncertain, I inferred or repaired. Give the counts.
4. Judge: no spec exists for this target. Write specs/antt-linhares-chave.json following specs/README.md (ciphertext as transcribed with source and date, alphabet, the key rule as the constraint, a `judge` block suited to Portuguese of c.1805-1812); run `python3 tools/judge_plaintext.py specs/antt-linhares-chave.json --file ciphers/antt-linhares-chave/reading.txt` and paste its output into NOTES.md. If the judge has no Portuguese model, say so in NOTES.md and paste what it printed.
5. Fresh-instance re-derivation: one Sonnet subagent that sees ONLY the key rule (as a paragraph you write), the fetched book page images and ciphertext.tsv, never your key.tsv or reading, re-derives every token. Report the agreement count; any token it reads differently becomes M unless you settle it from the image and say how.
6. NOTES.md: status stays `open` or becomes `partial` (letter pages 1 and 4 are missing, so not `solved`); a "Reading (LX-DEC)" section with counts, judge output, re-derivation agreement and what was not found. Push after steps 2, 3 and 5.

Touch only ciphers/antt-linhares-chave/**, specs/antt-linhares-chave.json, tools/decode_key.py and its test (if needed), and ROOM.md. Report what was read and where it was not found; do not classify novelty, do not run a print check or a verifier's search (a separate session will). Hosts: archive.org only, count requests.
