YX-BARB (Sonnet, wall-clock box 50 minutes; at most 2 subagents). Target: ciphers/thurloe-barriere-1654 (Barriere to Conde, 20 Nov 1654, Birch's Thurloe State Papers II pp.721-722; word-per-code nomenclator, 368 tokens / 42 runs, sparse printed gloss). Parent: LANE YX orchestrator session_01PHaEdHeQY2FtMHLo9yGeoe. Read the lane COMMON, .claude/briefs/transcription.md, the target NOTES.md in full (section 8, the negative), passA.tsv, key_gloss.tsv, coverage_test.py, matched_control.py, specs/thurloe-barriere-1654.json, and the LEDGER row TX-BARRT (its pass B was interrupted before landing). Intake gate: tools/intake_gate_check.py exits 0.
QA (25 Sept 2026) found the matched-control negative (gloss key coverage 29.3% vs control 33.4%, 10 seeds) rests on one unreconciled pass. Job:
1. Pass B, blind: one Sonnet subagent transcribes the same page images (images/ on disk; if absent, fetch once from the IA copy NOTES.md names, 1.5 s apart, at most 10 requests) without seeing passA.tsv, in the same TSV layout. Give it crops of the two printed pages, not the whole volume.
2. Reconcile with tools/reconcile_passes.py passA.tsv passB.tsv; report agreement. If under 60%, stop: the negative stays provisional, say why. Otherwise settle disagreements from the image yourself (not by vote), write ciphertext.tsv as the reconciled transcription, and grade rows H/M.
3. Re-run coverage_test.py and matched_control.py on the reconciled ciphertext with the same seeds; report target and control side by side, before and after (rule 3). Update the spec's cheap_test_done and the status note YX-FIX added (provisional -> stands, or changed, with the numbers).
4. The next named test (permutation z-test on the gloss positions) is not this job; one line in NOTES.md.
NOTES.md gets one section "## YX-BARB (25 Sept 2026)". Touch only ciphers/thurloe-barriere-1654/**, specs/thurloe-barriere-1654.json and ROOM.md. Push after each step. Report what was found and where it was not found; do not classify novelty; "cost: see the lane ledger"; request counts per host.
