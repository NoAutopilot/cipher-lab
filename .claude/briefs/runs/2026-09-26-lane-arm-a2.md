JOB ARM-A2: family A2 -- direct transfer of every published sibling table to the 20 Feb 1808 groups, against a structure-preserving shuffled-table control. Sonnet (claude-sonnet-5). Stop and push at USD 3 or 35 minutes (units: one per table at about 4 minutes, plus a write-up unit). No subagents, no network. Lane: LANE ARM orchestrator session_011gEKqukZAxE6FjtQCzsUQ8. Written 26 Sept 2026 by LANE ARM.

INTAKE_GATE_OUTPUT (live, 26 Sept 2026 06:46, TOMO-REPLY): ciphers/armstrong-madison-1808: open (line 1) -- edition/page or full-text-search citation found within 6 lines; EXIT:0.

Read first: `.claude/briefs/runs/2026-09-26-lane-arm-COMMON.md` (binding), ciphers/armstrong-madison-1808/{NOTES.md,HYPOTHESES.md}, tools/data/uscodes-1800/README.md, specs/armstrong-madison-1808.json, `python3 tools/judge_plaintext.py --help`.

Do, per table T in tools/data/uscodes-1800/*.tsv (script ciphers/armstrong-madison-1808/a2/transfer.py, offline, seeded):
1. Positive control first: decode one sibling letter that T is known to read (a decode in tools/data/uscodes-1800/decodes/, re-encoded under T) -- coverage and judge score, to show the pipeline reads a true key. If no letter exists for T, say so.
2. Target: coverage (tokens and distinct values T defines), the decode, a word-bigram score under en18 (or en if en18 is absent; say which), and the judge line (`tools/judge_plaintext.py specs/armstrong-madison-1808.json --file <decode>`).
3. Control, which CAN differ from the target: 200 tables made from T by permuting its plaintext column WITHIN alphabetical blocks of 20 consecutive values (keeps the one-part structure and value range, destroys the value->word pairing); the same bigram score on the target under each; report the target's percentile. Also the reverse check: T on 200 shuffled-order copies of the target (bigram score depends on order, so this control can differ too).
4. Append "ARM-A2 transfer sweep" to HYPOTHESES.md: one row per table (positive control coverage/judge; target coverage, bigram score, percentile vs permuted tables, percentile vs shuffled order, judge verdict). Never quote decoded words as a reading unless the judge passes; paste the first 30 decoded tokens as salad only. One paragraph in NOTES.md; the spec's cheap_test_done gets one entry "A2" with both numbers.
Done line (ROOM, starting `done: for LANE ARM: ARM-A2`): per table "target coverage x, percentile vs permuted p, vs shuffled q, judge V"; no cost figure.
