ROLE: CHECK-SOLVED FOLLOW-UP worker H (LANE S). Read .claude/briefs/check-solved.md and the NOTES.md of each folder below first; you continue their logged searches, you do not redo them.
1. ciphers/cotton-tudor-cluster/ (N40): check the 9 volumes batch B left unchecked, each against its Letters and Papers Henry VIII / CSP Spanish / CSP Venetian / CSP Scotland / State Papers Henry VIII entry by date (archive.org full text; HTRC Extracted Features test where HathiTrust is the only copy). Per volume: found-solved (with the page) or open. Update the folder's verdict table.
2. ciphers/sp105-paget-1693/ (N36): the SP 106 key search by TNA Discovery API (records search restricted to SP 106, terms Paget, Dresden, cipher, key, 1690-1695), at most 40 calls, >=3 s apart. You are the only LANE S worker on Discovery.
3. ciphers/sp87-further-1712/ (N28) and every 'GB queries pending' list written today by LANE S check-solved workers (grep 'GB queries pending' in ciphers/*/NOTES.md): run them. YOU HOLD LANE S's Google Books slot: append &key=$GOOGLE_BOOKS_KEY&country=US, never print the key, >=2 s apart, at most 120 calls; record each query and hit in the folder's NOTES.md.
4. ciphers/sp90-raby-whitworth-1705/ (N27): look up BL Add MS 37373-37389 (Whitworth's retained papers) in searcharchives.bl.uk JSON (at most 15 calls) for a decipher or key matching SP 90/7-8; one line in NOTES.
Change a status word only when the evidence supports it, quoting the printed page for any found-solved. Update the QUEUE.md rows with an appended note.
COMMON RULES (LANE S worker; parent LANE S orchestrator session_01PE7TAF6Hsp3MHtFEkDPP4a):
- First: `date -u` (use that clock for every timestamp); `git fetch origin main && git checkout -B main origin/main`. Read CLAUDE.md in full (rules 1, 3, 10; the good-citizen rule; the Access playbook), LESSONS.md sections 1-2, and the last 30 lines of ROOM.md. Append a ROOM.md `claim` line naming you, the role, the rows and the files you will touch, commit and push it BEFORE other work. Append a `done` line when you stop; use `flag` for anything the lane orchestrator must see.
- Git: commit by explicit path only; before every push `git fetch origin main && git rebase --autostash FETCH_HEAD`; ROOM.md is append-only; on a conflict in QUEUE.md/ROOM.md keep both sides.
- Hosts (good-citizen rule): one request at a time per host, >=1.5 s apart (archive.org: >=3 s, at most 60 requests this session, other workers share it); descriptive User-Agent `cipher-lab research script (contact via repository)` unless the playbook says a site needs a browser UA; on 429/403/Cloudflare/challenge stop that host, log it in NOTES.md and ROOM.md, one retry after a pause at most. NEVER fetch gallica.bnf.fr or archivesetmanuscrits.bnf.fr (another lane owns them). Google Books only if this brief says you hold the Google Books slot. TNA Discovery API only if this brief allows it. Report your request count per host.
- Rule 10: never write new, unpublished, unread, first, never printed, previously unknown. Report what was found and where it was not found. Do not classify novelty. Never promote to the board, never solve, never start a target your brief does not name. No copy orders or payments: if a target needs a copy, draft ciphers/<slug>/REQUEST.md (no personal data) and say so in the report.
- Credentials: never print them; no unfiltered env/printenv, no curl -v / set -x on a credentialed call. DECODE login is known broken (ASKS row 1): do not attempt it; use DECODE's public pages/cached catalogue only (sources/ and the solver repos' DECODE sweeps).
- Usage: Sonnet; cap $10 of usage (check your own cost; stop at the cap even if unfinished, and push what you have). At most 4 subagents at once, on Sonnet. Scripts read, models judge: fetch once to disk, grep, give the model the hits.
- Finish: commit, rebase, push; ROOM `done` line; then your final reply is ONE paragraph, first line the answer (per-row verdicts/new row ids), then files touched, request counts per host, cost. Then stop.
