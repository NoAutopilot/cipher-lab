YX-DEC2754 (Sonnet, wall-clock box 45 minutes; at most 2 subagents). Target: ciphers/decode-2754-bnf-baluze156-1636 (BnF Baluze 156 f.157r, Sabran circle, 1636). Parent: LANE YX orchestrator session_01PHaEdHeQY2FtMHLo9yGeoe. Kind: recovery (published sibling key). Read the lane COMMON, .claude/briefs/solver.md, the target NOTES.md in full (two control-backed negatives already on file with the first Sabran key: numeral 0/31 vs control 19.1%; letter trial 5.649 bits/char vs positive control 3.066), sources/cryptiana/web/GL.htm section "Melchior de Sabran (1631)", and the YX triage entry. Intake gate passes (six-source sweep logged).

Job: test Lasry's second Sabran-circle key (the Baluze 156 f.40 break) on f.157r.
1. Locate the key. On disk first (sources/cryptiana/web/GL.htm and louisxiii.htm; any linked image under sources/cryptiana/web/), then Tomokiyo's live page (cryptiana.web.fc2.com, one or two requests), then Lasry's publications (search engine; an open-access paper or preprint only; no academia.edu, it is blocked). If only "solved by George Lasry" is printed and no key table or reading of f.40 is published, stop here: write where you looked, and one line naming the next route (the f.40 leaf itself on Gallica, to rebuild the key from a known plaintext if Tomokiyo prints one).
2. If a key is found: write it to key_sabran2.tsv with its source (URL, figure, date), apply it to f.157r's committed tokens (reuse the target's existing trial scripts; add an option rather than a copy), and score with the same two statistics as the earlier trials, against the same matched controls (the positive control enciphered with this key, and the shuffle range). Target and control numbers side by side (rule 3). If the key reads it (control-matched), write the reading with per-token grades H/M/U, and do not run a judge or re-derivation in this job; the lane orchestrator briefs those.
Touch only ciphers/decode-2754-bnf-baluze156-1636/** and ROOM.md. Push after each step. Report what was found and where it was not found; do not classify novelty; "cost: see the lane ledger"; request counts per host.
