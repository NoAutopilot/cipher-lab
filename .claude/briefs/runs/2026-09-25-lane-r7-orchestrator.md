LANE R7 ORCHESTRATOR (recovery and deep work; successor to LANE R6, whose handoff is STATUS.md "LANE R6 handoff (session_018MWpKL71WnBxA8k4ejVkBS), 25 September 2026, 19:25 UTC"). Opus. Cap $80 own usage; hand off at $80 or 400k context (RETRO-2026-09-25k: R6 ran to 505k against 500k), keeping a "LANE R7 handoff" section in STATUS.md current from the first worker report. Started 25 Sept 2026 by parent 7c (session_01H4AdRiu9g44F1oCBVNzbpx); the parent hands over to 7d within the hour, so report through ROOM lines only. Window allowed; `rejected` anywhere stops everything.

First: `date -u`; `python3 tools/room.py --start` (prints UPDATES.md and NEAR.md); read CLAUDE.md rules 3, 5 (near solves), 7, 10 and Usage 8a; `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`; `.claude/briefs/runs/2026-09-24-lane-r4-common.md` (worker common); the LANE R6 handoff in full (its table's "next" column is your queue); NEAR.md; UPDATES.md; ciphers/espagnol142-mercy-1648/AUDIT.md; `python3 tools/room.py --digest "2026-09-25 19:00"`. ROOM claim "LANE R7: NEAR rows Mercy and Salviati first, then R6's claimed targets". At most 6 live workers; Sonnet for passes and searches, Fable for hypothesis design and reading a decode against history; every worker brief carries a wall-clock box and never a get_session self-read; every cryptanalytic run goes through tools/family_run.py; check-ins every 30-40 minutes with send_later, cost read by you with get_session.

Jobs, in order:
1. Mercy 1648 (NEAR.md row; N3 after one audit, key ours). The second adversarial audit is LANE V6's; your part is the leads the verifier owes: (a) Lonchay 1896 p.445 cites a 15 April 1648 sibling in Brussels, Archives generales du Royaume, SEE t.LXIV f.16, in clear -- a Sonnet worker (cap $5) establishes whether that leaf is digitised or printed and whether its text overlaps the 6 June instruction (a crib that would lift M tokens and settle the sender); (b) Cuvelier-Lefevre, Correspondance de la Cour d'Espagne VI (1937) p.647 is an owner read: one ASKS.md row with the exact citation; (c) after V6's second audit reaches N3, write ciphers/espagnol142-mercy-1648/second-opinions/PROMPT-chatgpt.md and a `queued` row SO-MERCY-1648 in SECOND-OPINIONS-QUEUE.tsv (tools/second_opinion_runner_prompt.md). NOTES.md first line is `partial` (set by the parent 25 Sept 20:0x); never `solved` from this lane.
2. Salviati 1525 (NEAR.md row). CM2 showed the error-tolerant solver reads its own 10%-noise control at 58% against a 60% gate and 40% at 20%, so the lever is the transcription error, not the solver: a glyph-atlas re-pass (tools/glyph_atlas.py, one Sonnet worker per leaf, cap $6, the two lowest-agreement leaves f.55v and f.57v first) that merges near-duplicate types and re-scores the pooled type count; then re-run the CM control ladder only if the type count drops by a fifth or more. Record every number in the NEAR.md row.
3. R6's claimed targets, in the order of its handoff table, each by the cheapest route named there (mercy siblings, berthier spec and first test through the breadth rule, clair1161 and clairambault296 only if a new locating signal exists, the sp87/TNA items only as print leads -- copy orders go to ASKS.md, never to a worker).
4. Any target the owner-account's ZX2 claimed after 19:00 is theirs: check ROOM before every claim.
After every worker: LEDGER row (get_session cost), ASSIGNMENTS, archive_session, NEAR.md and status.json `near` touched when a row's target moved (`python3 tools/near_check.py` must stay clean), ROOM line. Report to the parent only through ROOM ("for the parent:" / "flag:").
