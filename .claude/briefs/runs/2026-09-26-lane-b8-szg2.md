JOB bSZG2 (LANE B8, Sonnet, cap USD 8, wall-clock box 50 min; hosts: none -- crops are on disk): szembek-bk1560, second gloss pass on leaves 65 and 67 in the aligner's input format, with the per-leaf control first.
Intake gate (orchestrator, run between 06:51 and 06:54 UTC): `szembek-bk1560: solved (line 1) -- already terminal, nothing to gate` (exit 0). Paste your own re-run.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b8-common.md` (it governs); CLAUDE.md rule 3's per-leaf merge-gate paragraph (the Szembek one) and Usage 6's passes-per-unit paragraph; ciphers/szembek-bk1560/NOTES.md sections bSZL65, bSZL67, bSZM (merge) and AUDIT.md (V8-SZEM, 06:30); leaf65/pairs.tsv, leaf67/pairs.tsv (the aligner's input: plain_line, plain_raw, cipher_line, cipher_raw), leaf65/crops, leaf67/crops.
Why: the committed reading is C 384 / M 50. Leaves 65 and 67 were first glossed per-group (word runs left blank), so their own shuffle-consistency controls tied (0.0 vs 0.0; 0.429 vs 0.429 -- non-tests at that N). A letter-level pass on those two leaves, like leaf 66's, should let each leaf clear or fail its own control.
Pricing (Usage 6, per pass): 2 leaves x 1 new blind pass each = 2 Sonnet subagent calls, each given ONE leaf's line-crop paths only (never the full leaf image; `ls leaf65/crops leaf67/crops` pasted before the first call), estimate about USD 2 a call; plus reconciliation of disagreements from crops at one crop each, cap 20 crops. Stop before starting a unit that would cross 80% of cap or box.
Steps: (1) blind pass B per leaf: for every line, cipher_raw and the interlinear gloss written letter-by-letter above each code (plain_raw), in pairs.tsv format -> leafNN/pairs_b.tsv, commit per leaf. (2) tools/reconcile_passes.py pass A (existing pairs.tsv) vs pass B; settle disagreements.tsv rows on the crop; -> leafNN/pairs_r.tsv. (3) PER-LEAF CONTROL FIRST: for each leaf alone, run tools/interlinear_align.py on that leaf's reconciled pairs, compute same-code -> same-letter agreement over recurring codes vs 1000 shuffles (seed 20260926), exactly bSZM's build_key.py statistic, and report real / shuffle mean / p95 and the per-class breakdown. (4) Merge: rebuild pairs.tsv from the three leaves (leaf 66 unchanged) and rerun build_key.py; a code attested ONLY on a leaf whose own control tied or failed is held at M. decode_key.py --check exit 0. Report the new C/M/U counts beside the old (C 384 / M 50) and list every token whose letter changed.
If the reading changes in any token: update AUDIT.md's counts line and any SECOND-OPINIONS-QUEUE.tsv szembek row (CLAUDE.md rule 10 last paragraph), and post "for LANE V8: ciphers/szembek-bk1560 reading revised (bSZG2), N-class re-check" in ROOM.md. Do not change NOTES.md line 1 (status is the parent's call, flagged by V8-SZEM).
Output: NOTES.md section "Second gloss pass, leaves 65/67 (bSZG2)". ROOM done line <= 600 chars: per-leaf control numbers side by side, new C/M. Final reply at most five lines. Do not classify novelty.
