LANE R5 ORCHESTRATOR (recovery; successor to LANE R4 from its 18:22 handoff). Opus. Cap $60 own usage; hand off at $60 or 600k context.

Owner decision, 24 Sept 2026 18:48 UTC, relayed by the parent (cipher-lab-7b, session_01K7ZbE95o1pUW5gof8VA5PR): the seven-day window reads `allowed_warning` (resets Sat 26 Sept 13:00 UTC) and the owner said to keep chasing momentum. So: `allowed_warning` does NOT stop you; `rejected` on any session does: interrupt every worker, write the handoff, stop. Check `rate_limit_info` on yourself at every check-in (every 30-45 min via send_later; R4's lesson: 45 minutes is enough at this pace, and an orchestrator at $66 own usage for seventeen workers was the cost of 30-minute check-ins). At most 6 live workers; Sonnet (claude-sonnet-5) for passes and image work, Opus only for reconciliation, key reading and cipher reasoning. R4's phase-2 rule for every worker brief: "do it yourself, at most one pass-B subagent, check cost every page" (every phase-2 worker finished under its cap after it).

First: `date -u`; `python3 tools/room.py --start`; read CLAUDE.md, LESSONS.md, BUDGETS.md, STATUS.md sections "Parent handoff", "LANE R4 handoff" (results table, open items, lessons), "LANE N3 handoff"; `python3 tools/room.py --digest "2026-09-24 18:00"`. Post a ROOM claim line "LANE R5 orchestrator: recovery (Salviati code+mark, Seure coarse atlas, M36 key leaves, N4 nominations as they post)". Workers get `.claude/briefs/runs/2026-09-24-lane-r4-common.md` (with your session id in place of R4's) plus a job brief you write to `.claude/briefs/runs/2026-09-24-lane-r5-<job>.md`. Every brief names the output place (the target folder) and states the cap.

Jobs, in priority order (spawn 2-3 at once, then the rest as they close):
1. **Salviati 1525 f.55r-f.57v** (`ciphers/fr2933-salviati-1525`, NOTES sections "Leaves f.54v-f.57v (LANE R4 J)" and "Code+mark control curve (LANE R4 P)"): the code+mark homophonic model needs ~2,800 signs; f.54r-v gave 719. Price at the first leaf: one Sonnet worker (cap $8) does f.55r with J's exact box-keyed procedure (classify against the shared atlas, strips, pass A itself, one blind pass-B subagent, gate >=80% on base codes, settle from the strips), reports cost per leaf and agreement. If the gate passes and the cost is near $6.5 a leaf, run the remaining leaves two workers at a time (each cap $8, two leaves each, disk only: the images are on disk, no refetch). Then an Opus worker (cap $6): `control/codemark_curve.py` on the pooled ciphertext under `cm` at the real N (control first, rule 3; the control must read above 85% at that N before the target is run), and, if the target scores like the control, a reading with grades and `tools/decode_key.py --check`. If the target does not read: one line in NOTES.md and a control-first recommendation on which design to try next, not another capture. Keys to look for in parallel (one Sonnet worker, cap $4, search only): a Salviati nunciature key of 1525-26 (ASV Segreteria di Stato, Strozziane at the Archivio di Stato di Firenze, Tomokiyo's vatican.htm and spanish pages in sources/cryptiana/web/, Bourdeau's keys index sources/solver-diffs/2026-09-24-cyphersolver-keys.tsv); report where it was and was not found.
2. **Seure 1558 f75L** (`ciphers/fr3151-seure-1558`, NOTES "Re-segmentation and passes f75L (LANE R4 O)"): O's option (a), no new fetch: one Sonnet worker (cap $5) merges the hook/loop family from 9 codes to 2-3 shape buckets, re-runs the two passes on the same three lines against the gate. Gate passes → an Opus reconciler (cap $12, the R4 handoff costed the whole of item 40 at about $30: stop at three more lines and report the per-line cost before committing the rest) reads the image for the codes. Gate fails again → closed-negative-for-now line in NOTES.md ("not box-keyable at this image quality; needs a different capture or a key") and stop; no hand-settling.
3. **M36 1519 election key** (`ciphers/fr5761-election-1519`, R4 open item 6): Sonnet (cap $5): f.105-f.110 of the key with the same atlas and the three-pass method that gave f.104 its 37 rows; extend key.tsv as a dataset. Then a grep of QUEUE.md, sources/solver-diffs/*.tsv and the fr.5761 finding-aid text on disk for 1519-20 letters the key might open (none in fr.5761 carries 'chiffre'; look at fr.5761's neighbours in the same series). A key is a dataset result even with no letter read.
4. **Nominations from LANE N4** as they post in ROOM ("nomination: ... | copy-free"): take any with an archive route and a design a control can test; control-first ($2-3) before any capture over $5.
5. Not this lane: Marie de Medicis 1610 (waits on ASKS 43), Posthius (N0, closed), 5549 and Bowes (need sources outside the repo; one line each if a key source turns up in job 1's search).

Do not touch other targets. After every worker report: LEDGER row (with session id; run `python3 tools/ledger_check.py` before pushing LEDGER.md), ASSIGNMENTS row, archive, status.json results row when a reading or dataset lands, ROOM line. Readings go to the parent for verification ("for LANE V5: reading ready" in ROOM); do not verify. Handoff: "LANE R5 handoff" at the top of STATUS.md's handoffs (results table, open items with costs, lessons), LEDGER row for yourself, ROOM done line, stop. Rule 10 wording throughout.
