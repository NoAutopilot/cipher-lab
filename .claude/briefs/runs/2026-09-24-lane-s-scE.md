ROLE: SCOUT, catalogue sweep Europeana non-English + RAH (LANE S). Read .claude/briefs/scout.md, the scoring rubric in .claude/workflows/scout.js, README "What counts as a result", STATUS.md's lane table, and QUEUE.md sections "Candidates not on DECODE" and "Digitised candidates outside the BnF (scout of 24 September 2026)" (what those sweeps already reached, what was blocked, and the noise patterns found: 'cipher' as zero/foliation, Pepys, WWII, Voynich, Founders Online). Do not re-sweep a host that section reports as exhausted.
Goal: find cipher correspondence (letters, despatches, registers with ciphertext; keys beside letters) 1450-1900 in the catalogues below that neither solver project reads, with a strong preference for items with a PUBLIC IMAGE ONLINE (copy-free). Copy-order items are logged but score lower (lane rule: 13 copy-order targets already wait on the person). Query in the catalogue's own languages (cipher, in cipher, partly in cipher, deciphered, key; chiffre, en chiffre, déchiffrement; cifra, cifrado, in cifra, cifrata; Chiffre, chiffriert, Geheimschrift; cijfer, in cijfers, gecodeerd; chiffer, chiffrerad, i chiffer; kryptografi, chifferskrift; Latin 'notis arcanis', 'litteris occultis').
Hosts: api.europeana.eu (JSON API, wskey=api2demo; the only worker on it; query it, esp. Italian, German, Dutch, Latin, Portuguese, French terms, with filters for TYPE:TEXT and dates 1450-1900 where the API allows, and look for member archives neither solver project reads); bibliotecadigital.rah.es (Real Academia de la Historia: a dedicated pass on its 9/76xx-9/79xx and other correspondence series for 'cifra', 'cifrada', 'clave', 'cifrado'; the on-site search needs the browser tool driving the real form: NODE_PATH=$(npm root -g) node tools/browser_fetch.js ... --type ... --selector ...; at most 150 requests, >=2 s apart). The two RAH rows already in QUEUE.md (Morillo 1817-20, Cañada 1869) are being checked by another worker; exclude them.
For every candidate: normalise (holder, reference, date, sender, recipient, extent, description, image URL if any); exclude anything already named in QUEUE.md, CATALOG.md, LANDSCAPE.md, ciphers/, sources/cryptiana/, or fresh shallow clones of github.com/dbourdeau/cyphersolver and github.com/aaymeloglu/unsolved-ciphers (grep holder+shelfmark and names), and anything whose catalogue already says deciphered/decoded/with decipherment unless a sibling letter stays undeciphered (then it is a recovery candidate). View the record page (and the image's first leaf if public) before scoring, to confirm ciphertext is present; never transcribe. Score every axis of the rubric including unread and kind (cryptanalysis / recovery / contribution; drop editions).
Output: a NEW QUEUE.md section "Europeana and Real Academia de la Historia digitised candidates (LANE S scout of <date>)" with an intro (hosts reached, queries, blocked hosts with the one attempt logged, raw/kept/digitised counts), a table with row ids E1, E2, ... (this prefix is reserved for you; at most 25 rows, best first), and a caveats paragraph; raw hits and exclusions to sources/solver-diffs/<date>-lane-s-europeana-rah.tsv; append rows to QUEUE-scores.json under a key "lane_s_europeana-rah" (read its structure first; rebase immediately before editing). Report per host: raw, kept, digitised (copy-free) count.
No Google Books calls. No TNA Discovery calls unless your host list allows them.

COMMON RULES (LANE S worker; parent LANE S orchestrator session_01PE7TAF6Hsp3MHtFEkDPP4a):
- First: `date -u` (use that clock for every timestamp); `git fetch origin main && git checkout -B main origin/main`. Read CLAUDE.md in full (rules 1, 3, 10; the good-citizen rule; the Access playbook), LESSONS.md sections 1-2, and the last 30 lines of ROOM.md. Append a ROOM.md `claim` line naming you, the role, the rows and the files you will touch, commit and push it BEFORE other work. Append a `done` line when you stop; use `flag` for anything the lane orchestrator must see.
- Git: commit by explicit path only; before every push `git fetch origin main && git rebase --autostash FETCH_HEAD`; ROOM.md is append-only; on a conflict in QUEUE.md/ROOM.md keep both sides.
- Hosts (good-citizen rule): one request at a time per host, >=1.5 s apart (archive.org: >=3 s, at most 60 requests this session, other workers share it); descriptive User-Agent `cipher-lab research script (contact via repository)` unless the playbook says a site needs a browser UA; on 429/403/Cloudflare/challenge stop that host, log it in NOTES.md and ROOM.md, one retry after a pause at most. NEVER fetch gallica.bnf.fr or archivesetmanuscrits.bnf.fr (another lane owns them). Google Books only if this brief says you hold the Google Books slot. TNA Discovery API only if this brief allows it. Report your request count per host.
- Rule 10: never write new, unpublished, unread, first, never printed, previously unknown. Report what was found and where it was not found. Do not classify novelty. Never promote to the board, never solve, never start a target your brief does not name. No copy orders or payments: if a target needs a copy, draft ciphers/<slug>/REQUEST.md (no personal data) and say so in the report.
- Credentials: never print them; no unfiltered env/printenv, no curl -v / set -x on a credentialed call. DECODE login is known broken (ASKS row 1): do not attempt it; use DECODE's public pages/cached catalogue only (sources/ and the solver repos' DECODE sweeps).
- Usage: Sonnet; cap $10 of usage (check your own cost; stop at the cap even if unfinished, and push what you have). At most 4 subagents at once, on Sonnet. Scripts read, models judge: fetch once to disk, grep, give the model the hits.
- Finish: commit, rebase, push; ROOM `done` line; then your final reply is ONE paragraph, first line the answer (per-row verdicts/new row ids), then files touched, request counts per host, cost. Then stop.
