LANE AX AX-4612TR2 -- lodewijk-van-nassau-1573-74: settle AX-4612TR's four passes of WVO 4612 against the images, write ciphertext_4612_v2.tsv and run the two checks (Sonnet, wall-clock box 60 minutes; also a minimum). Parent: LANE AX orchestrator session_01VzK62xX92yKfnUD93zprD8. Read and follow .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md and the common tail of .claude/briefs/README.md, CLAUDE.md rules 2-4 and Usage 6. First: `date -u`; `python3 tools/room.py --start`; last 30 lines of ROOM.md. Claim with tools/room.py before other work; never AskUserQuestion; no dollar figures for yourself.

Read NOTES.md section "AX-4612TR" and ax4612tr/{passA_p1,passB_p1,passA_p2,passB_p2}.tsv, ax4612tr/agreement.py, images_wv2/crops_4612/src_04612_p{1,2}.png. No new passes and no network: the passes exist.
Steps. (1) p2: realign the two passes by content, not position (sequence alignment of the numeral streams per page, e.g. difflib or tools/reconcile_passes.py on the joined stream), and report the true agreement. (2) Cut line crops under 2500 px from the two source PNGs (row ink profile, tools/iiif_lines.py conventions, --debug overlay checked), one crop per line, and settle each disagreement by viewing only the crop that holds it (never a full page in one read); log each settlement in ax4612tr/settle.tsv (page, line, pos, A, B, settled, why). Units: disagreements, batched per line; stop before starting a batch that would cross 80% of the box and write v2 with the unsettled positions marked '?'.
(3) Write ciphertext_4612_v2.tsv (same columns as ciphertext_4612.tsv; clear words with '='). (4) Checks, both numbers side by side with the old transcription: (a) key.tsv via decode_4612_v2.json (tools/decode_key.py --check): share of values 1-120 tokens that fall inside French words of >=3 letters (a script against tools/data fr16 wordlist), old vs v2, and the same statistic on 20 value-shuffled key.tsv copies (this statistic CAN move under a value shuffle -- say so); (b) IC of values 1-120 grouped in consecutive blocks of 5 (offsets 0-4, max), v2 vs old vs 4610/4611 (0.080) vs a random-block null (median, p95; 200 draws).
(5) Section "## AX-4612TR2: 4612 v2 settled (26 Sept 2026, LANE AX)". ROOM done line "for LANE AX: 4612 v2 <n> numerals (<u> unsettled), p2 true agreement <p>%, key.tsv word share old <a> / v2 <b> / shuffles <c>, block IC v2 <d> (null p95 <e>)".
Files: ciphers/lodewijk-van-nassau-1573-74/{ciphertext_4612_v2.tsv,decode_4612_v2.json,reading_4612_v2*,ax4612tr/**,images_wv2/crops_4612/**,NOTES.md own section}.
