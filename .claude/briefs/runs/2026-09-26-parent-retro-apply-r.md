PARENT WORKER RETRO-APPLY-R (Sonnet, cap $6 of usage, wall-clock box 40 minutes, no subagents, no network except git; the container is a depth-50 clone). Job: apply the brief- and tool-only proposals of RETRO-2026-09-26f.md (retrospective r, 26 Sept 2026 09:11 UTC) and the portable items of LEARN-2026-09-26-0906.md (LEARN-7, 09:07 UTC) for parent 7g (session_01Jb4UHR8so3yBUG1NTMdf6C, "LIVE parent 7g · Orchestrator 8"). Authorised by CLAUDE.md "Improvement loop" (the orchestrator applies changes that only touch briefs, tools or workflows; anything that changes the goal, the spend or the owner's asks goes to ASKS.md) and parent.md duty 10. Written 26 Sept 2026 at the 09:53 UTC clock read.

First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker RETRO-APPLY-R (Sonnet, <session id>)`; read CLAUDE.md, RETRO-2026-09-26f.md and LEARN-2026-09-26-0906.md in full, then each target file before editing it. Edits minimal, in the file's voice; where a retro proposal and a LEARN item say the same thing, apply it once and cite both. Items, in order: (1) retro r proposal 1 plus LEARN-7 item 1 -- the non-test shape (a second or third attempt at an unchanged approach varying one knob, with the control far below gate and insensitive to that knob; and a tool that has failed its own known-answer control three times is retired for that hypothesis, logged in HYPOTHESES.md as untested-by-this-tool): CLAUDE.md rule 3, one paragraph, citing hessen-1824 bHCP2/bHCP3, matignon 1e-1h and AX2-4612S/S2/S3, and the same line into .claude/briefs/README.md's common tail; if the retro proposes a mechanical check (a HYPOTHESES.md row count per tool and hypothesis), put it in tools/family_run.py or tools/ledger_check.py with a test rather than a new script. (2) retro r proposal 2 -- a lane's "please change X" ask names the commit it read the file at: .claude/briefs/README.md lane-orchestrator addenda and tools/room.py's warnings_for() as a non-blocking WARNING when a signal contains "please change" or "still says" without a 7-hex commit id, with a test in tools/tests/. (3) retro r proposal 3 plus LEARN-7 item 3 -- check-solved reads the solver repository's own stated next step (its NOTES/README plan text, not only its readings) and a live overlap is a duplicate-effort penalty: .claude/briefs/check-solved.md and .claude/workflows/check-solved.js's brief text, plus the scoring line in .claude/briefs/scout.md; and LEARN-7 item 2 -- a scout re-reads each surviving candidate's current NOTES.md and diffs against this repository's own tracking files (POOLS.tsv, QUEUE.md) before writing a row: .claude/briefs/scout.md and .claude/briefs/breadth.md, one line each. (4) retro r proposal 4 -- a typed clock time in a ROOM line's signal that differs from the stamp: tools/room.py warnings_for() (non-blocking WARNING when the signal contains an HH:MM UTC time more than 3 minutes from the stamp), with a test. (5) .claude/briefs/parent.md "Handing over" and the lane-orchestrator addenda in README.md: a lane session cannot archive itself -- it retitles ARCHIVED and self-ledgers, and the parent runs archive_session at its next check-in (owner-account parent's ROOM line 09:42, AX2's close); one sentence each. (6) From ARM-C1's flag (09:04): before a judge PASS is used as a gate for a solver family, the family's decode of the shuffled target is scored through the same judge; a PASS on that decode voids the judge as a gate for that family at that N (the en18 judge passed a nomenclator decode of shuffled armstrong-madison-1808): CLAUDE.md rule 3 one sentence next to the shuffled-null paragraph, and tools/family_run.py's docstring or --help if it has a judge step. One UPDATES.md row per instituted change (`By` = "RETRO-APPLY-R for parent 7g", clock-read time). Never touch BUDGETS.md or rate-limit rules; never touch status.json or any ciphers/ folder. Run every tools/tests/ file you touch directly (pytest is not installed). Commit by explicit path, `python3 tools/room.py --push <paths>`; ROOM done line `done: RETRO-APPLY-R: N of 6 items applied (...), tests <pass/fail>, UPDATES rows M`; one-paragraph reply; stop. Never call AskUserQuestion; never print or commit credentials.
