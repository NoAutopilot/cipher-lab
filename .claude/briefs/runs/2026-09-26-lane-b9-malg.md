JOB bMALG (LANE B9, Sonnet, cap USD 2, wall-clock box 30 min; hosts: none -- everything on disk): malsburg-hessen-1636 glyph conventions, before any further transcription of the fond.
Intake gate (orchestrator 08:20 UTC): `malsburg-hessen-1636: partial (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0. Paste your re-run; nonzero = stop.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b9-common.md` (it governs); ciphers/malsburg-hessen-1636/NOTES.md sections bMAL3 (settled rows, digit-shape descriptions), bMALDUP and "Orchestrator check of bMALDUP's verdict"; ciphertext.txt; recon_0003/, recon_0012/, recon_0028/, recon_0030/.
Why: ff.3/12 (H-grade, N=352) have no lowercase `i` or `z` sign; bMALDUP's blind passes on ff.28/30 wrote `i` and `z` often (16<->i6, 11<->ii, zz, 1z), and pass agreement fell to 66/77%. Every later leaf pass needs one fixed convention or the pool fills with reader noise.
Units (stop before one that crosses 80% of cap or box): U1 script-only census: every sign in the eight pass TSVs (transcription/pass_{a,b}_*_r.tsv) containing a letter, counted, with the crop and column it sits in. U2 at most 12 small PIL zooms (a single group, never a full leaf or full line; you look, no subagent) comparing the `i`/`z` shapes against agreed `1`/`2`/`3`/`7` shapes in the same line, per bMAL3's method. U3 write `glyph_map.tsv` (pass_reading -> canonical sign, evidence crop, count, grade H if shape-settled else M) and add `--sign-map FILE` to tools/reconcile_passes.py (applied to both passes before alignment) with an offline test in tools/tests/; re-run the reconcile on 0028 and 0030 with the map and report agreement before/after. Capital marks (D, G, H, L, N, O, S, W, X, Y, #) stay marks; say whether case (D/d, V/v, N/n) is significant or noise.
Degenerate optimum to avoid: a map that sends every letter to one digit raises agreement by construction. Each mapping needs a shape citation; report agreement also with only the H-grade mappings applied.
Output: NOTES.md section "Glyph conventions (bMALG)" (the table, the before/after agreement, one paragraph a pass subagent can be handed verbatim as its convention instruction); glyph_map.tsv; the tool option and test. ROOM done line <= 600 chars: mappings (H/M counts), agreement 0028/0030 before -> after. Final reply at most five lines. Do not edit NEAR.md.
