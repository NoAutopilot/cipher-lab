JOB bSCOUT6 (LANE B6, Sonnet, cap USD 6, wall-clock box 60 min): a fresh scout pass per `.claude/briefs/scout.md` (read it whole) that ranks candidates against each solver repository's HEAD, not QUEUE.md's snapshot. It never promotes to the board and never solves.
Why: four of nine SCOUT-RERANK rows and 16 of 17 backlog rows were already done in dbourdeau/cyphersolver (LANE B5; RETRO-2026-09-26b proposal 2).
Steps: (1) Fresh shallow clones of dbourdeau/cyphersolver and aaymeloglu/unsolved-ciphers; record each HEAD commit hash and date. (2) `python3 tools/solver_repo_diff.py` against the clones (read its --help; queue mode) over QUEUE.md and specs/; (3) harvest candidates from the clones that the repos themselves mark unsolved / partly read / key unknown (Bourdeau's per-target READMEs, Aymeloglu's catalogue -- cite, never copy Aymeloglu's code), plus Tomokiyo's on-disk pages (sources/cryptiana/) for items neither repo holds; drop anything already in ciphers/ or specs/ or NEAR.md or on a live claim in ROOM.md (last 200 lines), and anything LANE AX or GOLD3/GOLD4 holds. (4) Score per scout.md with Pipeline 3's expected-value rule and the pools-first rule (2,000+ signs per sender/key family), kind set, and for each row the cheapest first test with its matched control named. (5) Write QUEUE.md section "Scored backlog for LANE B6, second pass" (at most 15 rows, ranked): per row slug, source/shelfmark, kind, signs, language/era corpus available in tools/data, the solver-repo HEAD commit checked and the date, fraction read by either repo (from the diff), first test + control, estimated cost. Scores to QUEUE-scores.json. Rebase before writing QUEUE.md (shared file); keep other sections intact. Hosts: github.com clones only, plus de-crypt.org listing via tools/decode_list.py if needed (<=40 requests). Delete clones after.
Common rules: read `.claude/briefs/runs/2026-09-26-lane-b6-common.md` in full; it governs. Report what was found and where it was not found; do not classify novelty. Final reply at most five lines.
