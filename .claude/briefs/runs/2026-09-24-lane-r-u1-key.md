ROLE: KEY RECOVERY, La Luzerne code 1781 (LANE R worker R2). Model Opus, cap $12.
Target: ciphers/huntington-luzerne-destouches-1781 (U1). Read its NOTES.md in full, and sources/cryptiana/blog/2021_09_decoded-but-not-identified-code-of.html (Tomokiyo, 23 Sept 2021: La Luzerne letter of 8 Jan 1781, Rochambeau Papers, Beinecke, brbl-dl.library.yale.edu/vufind/Record/4528532; code figures to ~1199; he had the decoded text but not the figure assignment). Also grep the local shallow clones or fresh shallow clones of dbourdeau/cyphersolver (destaing/NOTES.md line 13 names this code) for 'Luzerne' -- read only the hits; cite, and copy nothing from aymeloglu (no licence).
Step 1 (no host): harvest every figure and its plaintext Tomokiyo prints (and any figure/plaintext pair in Bourdeau's notes) into ciphers/huntington-luzerne-destouches-1781/key_tomokiyo.tsv (figure, plaintext, source, grade C for a pair aligned from a printed decoded text, M if the alignment is uncertain). State how the code is ordered if the pairs show it (one-part alphabetical vs two-part), since a one-part code lets you interpolate (grade S with a control).
Step 2 (Yale, one host, <=40 requests): if the Beinecke item has public images of the 8 Jan letter and its decoded copy, fetch once to images/yale/ with manifest.json and extend the pair list from the image (grade C). If the host blocks (Cloudflare/403), try the Wayback route once, then log and go on.
Step 3: LANE R worker R1 is fetching the Huntington images (mssDE 68, 108(A), 37, 55) to images/ with images/inventory.tsv; `git fetch` and check. If present: mssDE 37 and 55 are catalogued as deciphered in a contemporary hand -- if either is in the same code with the decipherment beside it, align them into key.tsv (grade H for figures read from the contemporary decipherment, C from Tomokiyo's). Then two blind Sonnet subagent passes of the cipher in mssDE 68 and 108(A) (passA.tsv/passB.tsv), tools/reconcile_passes.py, settle disagreements on the image, apply key.tsv with tools/decode_key.py and a decode.json; grade every token; --check exits 0. Coverage extension by homophone/context interpolation only with a matched control (a synthetic letter of the same length encoded in a one-part code of the same size, same partial-key coverage): report control accuracy beside the target's figures. If R1's images are not pushed when you finish Step 2, write what you have, push, and stop (the orchestrator will re-brief).
Search log for LANE V: list what you checked for a prior decipherment of mssDE 68/108(A) (sources, method, date) and where it was not found. Do not classify novelty.
