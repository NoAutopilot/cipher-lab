LANE CX2 worker CX2-MISC2: check-solved sweep, Sonnet (claude-sonnet-5), stall alarm USD 8 (cost: see the lane ledger; never write your own cost). Parent: LANE CX2 orchestrator, session_018cUWVBDyZzw2sgLAmZKL75. COMMON: .claude/briefs/runs/2026-09-25-lane-cx2-COMMON.md applies in full. Never call AskUserQuestion. ROOM claim role: "LANE CX2 worker CX2-MISC2 (Sonnet)". Both accounts are live: never touch a target LANE R6, V6, B2 or ZX has claimed in ROOM.md.

TARGETS (edition family: French consular 1714 and Spanish royal 1868):
1. clairambault1225-paget-1714 (Pierre Paget's autograph letters with cipher, 1714, BnF Clairambault 1225 fol. 48 ff.; line 1 partial). LOCAL-QUEUE row L11 already asks the owner's machine for the Archives nationales Marine B7 inventory -- do not duplicate it. Editions: Recueil des instructions donnees aux ambassadeurs (Savoie-Sardaigne-Mantoue; Genes) for 1714; the Correspondance consulaire inventory (AN AE B I Genes/Cagliari, printed inventories on Gallica/archive.org) for Paget; Gallica full-text search (SRU) for 'Paget' 'consul' 'Cagliari' 1714 with a control. Say whether any printed source carries the ciphered passages in clear.
2. rah-xiquena-1868 (copy of a ciphered telegram, Isabel II / Conde de Xiquena, 3 May 1868, RAH Sig. 9/6963). Editions: search the RAH OAI record for a 'Publicado' note (OAI-PMH is not Anubis-blocked, see the Access playbook); Gaceta de Madrid / BOE historical collection (boe.es, full text) for 3-6 May 1868 for the telegram's subject; Hemeroteca Digital BNE full text; any edition of Isabel II's correspondence (Burdiel's biography apparatus). Image: at most one fetch via tools/browser_fetch.js --binary if NOTES.md lacks it; do not transcribe.

JOB (same for every target above), per .claude/briefs/check-solved.md, which you read in full first:
1. Read the target's NOTES.md end to end, and any sources/ pages it names. Note every edition, calendar or source it already cites and whether a worker actually opened it or only quoted someone else's summary.
2. Run the six searches yourself, in the rule-1 order, logging every query and URL: (a) web search on the cipher's name/sender/recipient/date, including "solves" + "Claude"/"GPT" (model-solve announcements, Vals AI blog); (b) the standard printed edition(s) and calendar(s) for the sender/recipient/date -- the ones named below first -- opened and read at the page, or full-text searched (archive.org djvu.txt / be-api fts, Google Books with GOOGLE_BOOKS_KEY and &country=US, HTRC EF via tools/htrc_ef_headwords.py when HathiTrust is the only copy) WITH A CONTROL: a phrase or name you know is in that same volume must also hit, and you name it; (c) community lists: Cryptiana (sources/cryptiana/ on disk first, then cryptiana.web.fc2.com), Cipherbrain / Schmeh, the list-post comment threads; (d) DECODE listing (tools/decode_list.py or the on-disk sources/decode/, login-free only); (e) Bourdeau github.com/dbourdeau/cyphersolver (grep a shallow clone for the target name, do not read it whole); (f) Aymeloglu github.com/aaymeloglu/unsolved-ciphers (same; cite, never copy code). Apply every lesson paragraph of check-solved.md that fits (interlinear decipherment in the same edition; "key lost" footnotes followed into the journals; duplicate/copy in the same holding; Tomokiyo naming this very letter -> quote him verbatim; WVO Brongegevens).
3. Write the verdict at the TOP of NOTES.md: line 1 the bare status word; line 2, immediately below, ONE sentence naming the edition/calendar THIS worker opened and the pages read, or the full-text search run and its control hit; then a dated "## Check-solved (LANE CX2, <date -u>)" section with the six-source log (query, URL, result per source). The verdict is exactly one of:
   - `open` -- only if line 2 names the standard edition with pages read by you, or a controlled full-text search (the control phrase named and found). An edition you could not open makes it `blocked`, whatever else you found.
   - `blocked` -- line 2 names exactly what could not be read (edition, volume, pages, host and error) and who can supply it; if the owner's machine can read it (HathiTrust full text, JSTOR, a Google Books volume that is not full view from the cloud, academia.edu), append one row to LOCAL-QUEUE.tsv or JSTOR-QUEUE.tsv in that file's existing column format and name the row id in NOTES.md. LOCAL-QUEUE: `git fetch origin main && git rebase origin/main` immediately before adding the row, number it one above the highest existing L-number at that moment (L14 was the highest at 15:50 UTC), and after any rebase grep the file for `<<<<<<<`/`>>>>>>>` before pushing -- a QA flag at 12:46 found committed conflict markers from two workers taking the same L-number.
   - `found-solved` -- with the citation (who, where, when, page) and README's F0/F1/F2 (who did not know), and what key or correction it leaves to hand on.
   For a target whose line 1 is already `partial` (a reading exists): keep `partial` on line 1, put the sentence on line 2 exactly as for `open`, and add a line in the Check-solved section "Intake verdict: open|blocked|found-solved" so the gate reading is explicit.
   Keep everything already in NOTES.md below your section (move the old first lines down; never delete a finding). Correct any sentence in NOTES.md your search shows to be wrong, in place, with "(corrected LANE CX2 <date>)".
4. Run `python3 tools/intake_gate_check.py ciphers/<target>` for each target and paste its one-line output into your done line. Exit 1 on an `open` means your line 2 does not yet cite pages or a controlled search: fix it or make it `blocked`. (Since YX-FIX, 25 Sept 11:58, `partial` is gated exactly like `open`; `found-solved` still reads as ambiguous and exits 1, which is expected for that verdict only: say so.)
5. Push after EACH target lands (python3 tools/room.py --push ciphers/<target>/NOTES.md [LOCAL-QUEUE.tsv|JSTOR-QUEUE.tsv]), with a one-line ROOM note per target. Then the done line and your one-paragraph report: per target the verdict, the line-2 sentence, and the gate-check output; request counts per host.
No transcription, key application, image capture beyond one leaf-view to confirm what a catalogue says, or cryptanalysis. No novelty words (rule 10). Never edit QUEUE.md, STATUS.md, status.json, LEDGER.md or another target.
Files you may touch: the named target folders' NOTES.md (and a sources.tsv / print-check.tsv beside it if you run tools/print_check.py), LOCAL-QUEUE.tsv and JSTOR-QUEUE.tsv (append only), ROOM.md.
Wall-clock box: 60 minutes from your first `date -u`; at 48 minutes push what you have, write the remaining steps as one line in the unfinished target's NOTES.md, and stop. Order: do the targets in the order listed.
