LANE AX2 AX2-4612S3 -- lodewijk-van-nassau-1573-74: third and last key_repair iteration (Sonnet, wall-clock box 45 minutes). Parent: LANE AX2 orchestrator session_016sbTkVT8HGNJ6JGjJNmCuM. Read and follow .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md (read "LANE AX2" for "LANE AX") and the common tail of .claude/briefs/README.md, CLAUDE.md rules 3, 4, 7, 10 and Usage 8. First: `date -u`; `python3 tools/room.py --start`; last 30 lines of ROOM.md. Claim with tools/room.py before other work; never AskUserQuestion; no dollar figures for yourself.
Read: NOTES.md sections AX2-4612S and AX2-4612S2; the brief .claude/briefs/runs/2026-09-26-lane-ax2-4612s2.md (units 2-3 are repeated here unchanged); tools/key_repair.py; ax2_4612s/**.
Where it stands: objective=excess fixed the NULL bias (null control 100 -> 53 false changes; known-answer 0/8 -> 5/8) but multi-letter candidates still win because summed excess over more above-average characters beats a correct single letter (AX2-4612S2's own diagnosis).
Fix, in the tool (new option, previous objectives kept):
- `--objective paired`: a candidate replaces the current value only if BOTH (a) its summed excess gain over all the code's occurrences exceeds --margin AND (b) its mean excess per emitted character across those occurrences is higher than the current value's -- a longer value must be better per character, not just longer. Also `--min-occ` (default 3): codes seen fewer times are never changed.
- A unit test: on a clean synthetic French text enciphered with a known key, null control proposes 0 changes.
Units (stop before one that would cross 80% of the box; push after each):
1. Tool option + test (about 10 min).
2. Controls FIRST, exactly AX2-4612S2's two controls and gates (null control <= 2 false; known-answer >= 6/8 recovered, bigram codes reported separately, <= 2 false), gates copied verbatim into NOTES.md before running. Below gate = CONTROL BELOW GATE: stop, report the null control's changed codes, and write one line in NOTES.md naming the H-S hypothesis as untested by this tool family (so it is not re-briefed a fourth time without a new idea).
3. Only if unit 2 passed: targets 4612 v3 and 5799 exactly as AX2-4612S2 unit 3; flag a candidate for re-derivation if a target reaches the 0.85x-of-5811 word-share gate; no "for LANE V8" line.
Section "## AX2-4612S3: key repair, paired objective (26 Sept 2026, LANE AX2)". ROOM done line "for LANE AX2: null <n>; known-answer <x/8, bigram y/2, false z> (gate met/not); 4612 <..>; 5799 <..>; verdict". Files: tools/key_repair.py, tools/tests/test_key_repair.py; ciphers/lodewijk-van-nassau-1573-74/{ax2_4612s/**,key_4612_repair.tsv,key_5799_repair.tsv,decode_*_repair.json,reading_*_repair*,HYPOTHESES.md (append),NOTES.md own section}. No network, no images.
