bSALR2 -- fr2933-salviati-1525: finish LANE B11 job 1, the three variants bSALR did not reach. Sonnet. Cap USD 4, box 75 minutes from your first `date -u`. Disk only, no hosts, no subagents, no image reading.

Read first: `.claude/briefs/runs/2026-09-26-lane-b11-common.md` (governs), `.claude/briefs/runs/2026-09-26-lane-b11-bSALR.md` (the parent brief: same rules, judge, row format), ciphers/fr2933-salviati-1525/NOTES.md section "## bSALR" in full (its exact commands and the table you extend), CLAUDE.md rule 3.

Intake gate (orchestrator ran it 12:24 UTC): `fr2933-salviati-1525: open (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

Units: three control+target runs, each about 15-18 minutes of CPU at 24 restarts (bSALR's own estimate); the model cost is small, the box is the limit. Run in the foreground one at a time; do not start a unit that would cross 80% of the box (60 min). Order:
 1. DSN regular at 24 restarts (not 6), `--measured-error 0.064 --param err=0.064`, 3 control seeds, target on seeds 1, 2 and 3 (three target runs, `--seed` 1..3; if family_run only runs the target on --seed, call it three times with --target-only-if-gated and record each) -- settles bSALR's point (ii), whether the 6-restart single target seed was a solver collapse.
 2. DSN2 V2 (marks=mixed, boundary=1), 24 restarts, err=0.064, 3 control seeds, target seed 1.
 3. DSN irregular (assign=irregular), 24 restarts, err=0.064, 3 control seeds, target seed 1.
 `--shuffle-target 1` only for a run whose target judge PASSes or whose target score/symbol reaches the control's range.
Extend bSALR's table in NOTES.md (new rows, label bSALR2; keep bSALR's rows as written) and add a two-line verdict: which R8 families are now control-backed negatives at 6.4% and which remain non-tests. A decode that PASSes with a non-PASS shuffle floor: reading_bsalr2.txt + regeneration script, S/M grades, ROOM "flag: salviati reading candidate". Do not edit NEAR.md, LEDGER.md, ASSIGNMENTS or status.json. Commit by explicit path and `python3 tools/room.py --push <paths>` after each unit. ROOM claim first; done line at most 600 chars with control mean vs target score/symbol per run. Final reply at most five lines. Report what was found and where it was not found; do not classify novelty.
