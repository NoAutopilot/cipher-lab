ZX-BRO2 (Sonnet, wall-clock box 45 minutes, also a minimum working time; at most 1 subagent). Target: ciphers/antt-msliv0638-brochado-1712. Parent: LANE ZX orchestrator session_01MxueEQJUGF9PWJiYcVyvBM. Kind: recovery (key source `period`). Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md) and NOTES.md sections "YX-BRO79", "YX-PTJUDGE judge re-run" and "ZX-BRO" (Step 4 especially: two held-out controls at 42 and 66 letters FAIL the pt judge like letter 134). Intake gate: `partial`, exit 0 (orchestrator, 15:44 UTC 25 Sept 2026). No network needed.

Question: is letter 134's judge score (-1.443) typical of a same-procedure decode of this material at its length, or in the bottom tail? One control per length is not enough; score the whole bucket.
1. With scripts/14_loo_judge_control.py, LOO-decode EVERY appendix entry (and, for entries longer than 70 letters, every non-overlapping 65-letter window of the LOO decode) and score each with `python3 tools/judge_plaintext.py specs/antt-msliv0638-brochado-1712.json --file <f>`, the same spec and corpus (language "pt" = tools/data/pt17, Vieira 1648-1697, the nearest period to 1712; also score each with a copy of the spec's judge block set to "pt18" (1780-1830) in a scratch spec, and report both, since LANE V6 just landed pt18). Write judge_bucket.tsv (entry, letters, LOO accuracy vs the gloss, score pt17, score pt18).
2. Report: the distribution of scores for 40-70-letter LOO decodes (n, median, 5th/25th percentile), the correlation of score with LOO accuracy, and letter 134's empirical percentile in that distribution under each corpus. Also its percentile against shuffled letter 134 (the judge's own null).
3. Verdict in numbers: if letter 134 sits inside the bulk of same-procedure decodes (above their 10th percentile), write "judge-limited at this length; letter 134 scores like a correct decode of this material" and the parent decides whether it goes to a verifier; if it sits in the bottom tail with the known-good decodes clearly above it, write "letter 134's candidate fails the bucket control" and name the likeliest cause (a mis-keyed code, a word boundary, a code with 1-2 observations) from its per-token grades.
NOTES.md gets one section "## ZX-BRO2 (25 Sept 2026)". Status stays `partial`. Touch only ciphers/antt-msliv0638-brochado-1712/** and ROOM.md (never tools/ or specs/ except a scratch spec inside the target folder). Push after each step. Report what was found and where it was not found; do not classify novelty; write "cost: see the lane ledger". Never call AskUserQuestion.
