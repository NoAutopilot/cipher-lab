ZX-BAR (Sonnet, wall-clock box 45 minutes; at most 1 subagent). Target: ciphers/thurloe-barriere-1654 (Barriere to Conde, London 20 Nov 1654, Birch Thurloe II pp.721-722; word-per-code nomenclature with a sparse printed interlinear gloss). Parent: LANE ZX orchestrator session_01MxueEQJUGF9PWJiYcVyvBM. Kind: cryptanalysis (the gloss is period known plaintext, grade C). Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md), LESSONS.md "Controls, always", CLAUDE.md rule 3, NOTES.md sections 8 and "YX-BARB", specs/thurloe-barriere-1654.json. Intake gate run by the lane orchestrator at 15:44 UTC 25 Sept 2026: `ciphers/thurloe-barriere-1654: open (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0. Work from disk only (ciphertext.tsv, key_gloss.tsv, coverage_test.py, matched_control.py, tools/data/fr16); no network.

The one named next test: a permutation z-test on the letter's real gloss positions. The question it answers: does the gloss-derived key carry information about the UNglossed tokens of this letter beyond what any assignment of the same code frequencies would?
1. Write permutation_test.py (--help, fixed seeds, 1000 permutations). Statistic, stated in NOTES.md before running: for each glossed token held out in turn (leave-one-out over the C-graded gloss positions in key_gloss.tsv), predict its value from the key built from all OTHER gloss positions; the statistic is the count of held-out tokens predicted correctly (exact word after lower-casing and long-s/u-v/i-j folding on both sides). Null A (key shuffle): permute the code->word assignments among the key's codes, keeping each word's code count; null B (token shuffle): permute the order of code tokens within each run, keeping the gloss words in place. Report the real statistic, each null's mean, sd, z and empirical p.
2. Run the same test, same script, same seeds, side by side on a matched control: matched_control.py's construction (same 33-run/400-token layout, same gloss-reveal positions, same conflict pattern, fr16 prose with known ground truth), 10 control seeds, reporting each control's real z. This shows what z a real word-per-code nomenclature of this design gives the test.
3. Verdict, stated as numbers: if the target's z sits inside the control's z range, the letter's key behaves like a real nomenclature of this design and the test cannot extract more from the gloss than coverage did; if the target's z is far below the control range (near 0), the gloss positions carry no transferable key (a clean negative that closes the cryptanalytic route on this letter alone, pending a key or the 1655 cipher lead). Either way add the test and both numbers to the spec's cheap_tests_in_order and a second cheap_test_done entry (keep the first).
NOTES.md gets one section "## ZX-BAR (25 Sept 2026)". Status line: stays `open` unless the verdict is the clean negative, in which case `closed-negative` with a one-sentence reason and both numbers in the first lines. Touch only ciphers/thurloe-barriere-1654/**, specs/thurloe-barriere-1654.json and ROOM.md. Push after each step. Write "cost: see the lane ledger". Never call AskUserQuestion.
