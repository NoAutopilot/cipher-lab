PARENT WORKER DESK-CAP (Sonnet, cap USD 4, box 45 minutes, no network except git). Parent: the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n); context: OPTIMIZATION-2026-09-26.md section (a): 46 open or waiting ASKS rows sit on one person; requests to a role in ROOM.md can go unanswered for hours with no signal. Job, three changes, both accounts' parents use them:
1. Desk cap. In .claude/briefs/parent.md, a "Desk" paragraph: the parents keep at most five items on the owner's desk at any time; an item is one action, one sentence, with a paste-ready text or a single click; every other ASKS row carries `status: backlog` with a one-line expected value; the parents re-rank daily at a named check-in and record what they demoted in STATUS.md. Implement the state: add a `desk` marker the parents put in an ASKS.md row's status cell (`desk` / `backlog` / `waiting` / `done`), migrate the current 46 open rows: the five for the desk are ASKS 68 (RAH order, drafts delivered), 67 (KHA route, waiting on Huysman -- waiting, not desk), 69 (done: 7i created), the CORE key on the other account, and the two mailbox sign-offs; everything else `backlog` with its one-line value; tools/desk_check.py gains a `--cap 5` check that fails when more than five rows carry `desk`. Board: "Your desk" shows desk rows only, with a collapsed backlog count.
2. Dropped-item detector. tools/orphan_check.py: a new class (d): any ROOM.md line whose text contains "for <role>" (for the parent, for parent 7i, for LANE X, for the owner-account parent, for LANE VO1, for the verifier) with no later line from that role (role name as the second column's prefix) within two hours is reported with its age; exclude "done:" lines that carry "for the parent" as a report rather than a request (treat lines starting with "done:" or "check-in" as reports); test with fixtures. Wire it into the parent check-in prompts by a sentence in parent.md duty 3 (the orphan check now includes dropped requests).
3. Owner-side rule in parent.md and outreach/README.md: nothing handed to the owner requires reading the repository; if an ask needs context, the ask is rewritten until it does not.
One UPDATES.md row per instituted change (clock-read time). Never touch BUDGETS.md, status.json or ciphers/. Post a ROOM claim first and a done line at the end (first line: desk rows and backlog count; second: detector findings on the live ROOM.md) as `parent worker DESK-CAP (Sonnet, <session id>)` with tools/room.py (two positional arguments); add your LEDGER.md row; commit by explicit path, fetch and rebase, push (never force; keep both sides on ASKS.md/ROOM.md conflicts). Never call AskUserQuestion.
