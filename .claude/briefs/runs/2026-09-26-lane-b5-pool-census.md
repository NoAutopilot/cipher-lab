JOB bPOOL0 (LANE B5 reserve, Sonnet worker). One line: a disk/listing census of QUEUE.md re-rank rows 5-9 (RAH 9/23 + 9/24-25; BAV Barb.lat 6956; ARA Brussels Secretairerie/Carpio-Fuenmayor; ASV SdS France 17-18; ASV SdS Spain 364C) from the DECODE login-free listing and the two solver repositories, so the per-host sampling workers know which records exist, which already have transcriptions (sign counts for free), and which host serves page images.

Read first: `.claude/briefs/runs/2026-09-26-lane-b5-common.md` (and the 7b COMMON it names); QUEUE.md "Re-rank for LANE B5" rows 5-9 and the scPOOL "real pools" table (grep "The real pools"); CLAUDE.md Access playbook table rows for DECODE, RAH (bibliotecadigital.rah.es), and the good-citizen rule.

Cap and box: $3 or 35 minutes from your first `date -u`, whichever first. Hosts: de-crypt.org listing only through `tools/decode_list.py` (no login), >= 1.5 s apart, at most 150 requests; github (git clone) for the two solver repositories. No image fetches in this job.

Steps:
0. `date -u`; `python3 tools/room.py --start`; ROOM claim (tools/room.py) naming specs/cheap-tests/pools-b5/.
1. From DECODE's listing, per pool: record ids, dates, pages, language field, status, whether a thumbnail or a transcription/document is listed. Write specs/cheap-tests/pools-b5/records.tsv (pool, record, date, pages, lang, status, has_transcription, notes).
2. Clone shallow both `dbourdeau/cyphersolver` and `aaymeloglu/unsolved-ciphers`; grep each for the record ids and shelfmarks (RAH 9/23, 9/24, 9/25 including 9/26's Lope Hurtado neighbours; Barb.lat 6956 / Barb. lat. 6956; Carpio / Fuenmayor; ASV/AAV Segreteria di Stato Francia 17-18; Spagna 364C). Note per record whether either repo holds a transcription, a key or a reading (cite folder and commit; Aymeloglu: cite only, never copy). Where a transcription exists, count cipher signs from it (script, not by eye) -> signs column. Delete the clones.
3. Per pool, one line in specs/cheap-tests/pools-b5/README.md: letters, pages, signs known from transcriptions, signs estimated (pages x a stated per-page figure, marked as estimate), pool yes/no/unknown against the 2,000-sign threshold, already worked by Bourdeau or Aymeloglu (and how far), and the image host a sampling worker would need (RAH Anubis via tools/browser_fetch.js --binary; digi.vatlib.it IIIF for BAV; search.arch.be for ARA; ASV has none known -- say what the listing shows) with one reachability probe per host (`curl -sS -o /dev/null -w "%{http_code}"`, one request each).
4. No specs written in this job (the sampling workers do that). Push; done line with the five pool lines in short; report in five lines.
