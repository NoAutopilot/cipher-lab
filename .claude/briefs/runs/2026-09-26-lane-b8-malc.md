JOB bMALC (LANE B8, Sonnet, cap USD 4, wall-clock box 45 min; hosts: none): malsburg-hessen-1636 -- before pooling the fond, find the N at which the matched homophonic control reads, and count the remaining leaves' cipher signs.
Intake gate (orchestrator, run between 06:51 and 06:54 UTC): `malsburg-hessen-1636: partial (line 1) -- edition/page or full-text-search citation found within 6 lines` (exit 0). Paste your re-run.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b8-common.md` (it governs); ciphers/malsburg-hessen-1636/NOTES.md (bMAL, bMAL2, bMAL3 sections) and HYPOTHESES.md (homophonic profile=target, K=95, N=352: control 0.392 below gate 0.6).
Why: the plan is to transcribe the other HCPortal records 502-509 (14 leaves on disk in images/, about USD 5 a leaf per pass, two passes each) to pool the fond. That is only worth it if the matched control can read K=95 at the pooled N.
A (tool, about USD 1.5): tools/family_run.py overwrites params N with the target's length, so a control at a projected N cannot be run. Add `--control-n N` (valid only with --control-only; the control is built at N with the target's K and profile scaled to N; the HYPOTHESES.md row says "projected N"), with an offline test in tools/tests/. Then run `--family homophonic --param profile=target --control-only --seeds 3` at N = 1000, 2000, 3000 (restarts as bMAL3). Serialize the runs (Usage 6: no concurrent CPU work in one box). Paste the three control means.
B (about USD 2): cipher extent per remaining leaf (records 502-509 and ff.4, 13): for each image, `tools/iiif_lines.py --image <leaf> --out ciphers/malsburg-hessen-1636/crops/<leaf> --debug` (paste), then count cipher lines vs clear lines from the debug overlay and ONE low-resolution look per leaf by one Sonnet subagent (overlay only, no transcription) -> extent.tsv (leaf, record, date, cipher lines, est. signs, notes). Sum the estimated pool N.
Stop before B if A has taken > 60% of cap or box. Output: NOTES.md section "Pooled-N control and leaf extent (bMALC)", the tool change + test, extent.tsv. ROOM done line <= 600 chars: control mean at each N, estimated pooled N, and which leaves to transcribe first. Final reply at most five lines. Do not edit NEAR.md.
