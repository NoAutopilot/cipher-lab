PX-BROKEY (Sonnet, cap $9 as a stall alarm, at most 2 subagents). Target: ciphers/antt-msliv0638-brochado-1712 (QUEUE PX-01; check-solved `open` 25 Sept 2026 by PX-CS01B, intake gate passed: Dória 1944 read by Google Books search-within, London cipher material absent). Job: rebuild the period key from the volume's own contemporary decipherments, then report which ciphered passages in the volume that key reads. Kind: recovery (key source `period`). Parent: LANE PX orchestrator session_01KapVpHVzNpnnQce5C8c3LY. Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-px-COMMON.md), .claude/briefs/transcription.md, .claude/briefs/solver.md, and the target's NOTES.md in full (the leaf inventory of m0289-m0293).

Material: ANTT PT/TT/MSLIV/0638, docId 7049343e7af44ba7b4ec18e74e1b2cd7, 306 images, CC BY-SA 4.0; images/filelist.json and five full-res leaves are already on disk. tools/digitarq_fetch.py (--full --names for leaves, --thumbs for a sweep). DigitArq: one request at a time, >=3 s apart, at most 60 requests this session (no other worker is on this host now; say so in ROOM before starting).

Steps:
1. Appendix extent: fetch full res for the appendix leaves not yet on disk (start m0284-m0288 and m0294-m0306; stop at the first two leaves on each side with no "Deciffrada"/cipher groups). Record in NOTES.md's inventory every entry: image, letter number or "Passage", cipher groups present, decipherment present.
2. Transcribe every appendix entry in two blind passes (two Sonnet subagents from the images, per transcription.md), each giving per entry the cipher groups exactly as written and the Deciffrada plaintext exactly as written (period spelling, abbreviations kept in brackets). Reconcile with tools/reconcile_passes.py where it fits; settle disagreements from crops. Write ciphertext.tsv (entry, letter_no, image, position, token, grade H/M) and plaintext_period.tsv (entry, the Deciffrada text).
3. Key: align each entry's cipher tokens with its decipherment (first decide the system from the data: letter substitution with homophones, syllabary, nomenclator codes for names, nulls; state it with the evidence). Write key.tsv (code, value, n_occurrences, entries, grade C when fixed by >=2 independent entries or one unambiguous alignment, M otherwise) and conflicts.tsv. A decode.json so `tools/decode_key.py ciphers/antt-msliv0638-brochado-1712 --check` regenerates reading.txt; check that the key re-reads each appendix entry to its own decipherment and report the per-entry agreement.
4. Ciphered passages without decipherment: from the scout's and CS01's notes and a --thumbs sweep at a stride that fits the request budget, list body leaves carrying cipher groups (image, letter number if visible). Fetch at most 8 at full res, transcribe the cipher runs in two passes, and decode them with the key, per-token grades (C from key entries fixed by the appendix, M uncertain, I inferred). Say which body passages match an appendix entry (the same letter number: then it is a control, not a reading) and which have no decipherment in the volume.
5. Spec and judge: write specs/antt-msliv0638-brochado-1712.json on the pattern of specs/antt-linhares-chave.json (language pt, judge block), and paste `python3 tools/judge_plaintext.py specs/antt-msliv0638-brochado-1712.json --file <reading of the undeciphered body passages>` into NOTES.md (a FAIL is reported as a FAIL).
6. Fresh-instance re-derivation: one subagent given only ciphertext.tsv, plaintext_period.tsv and the body transcription (not key.tsv, not your reasoning) rebuilds the key and decodes the body passages; report agreement per code and per token. A difference beyond the M-graded tokens goes in NOTES.md as unresolved.
Report what was read (counts H/C/M/I per rule 4) and where it was not found; do not classify novelty. Touch only ciphers/antt-msliv0638-brochado-1712/**, specs/antt-msliv0638-brochado-1712.json and ROOM.md. At 80% of the cap push what you have, write the remaining steps as one line in NOTES.md, and stop.
