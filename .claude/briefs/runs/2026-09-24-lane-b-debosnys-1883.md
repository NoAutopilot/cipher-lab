LANE B breadth worker: debosnys-1883. Sonnet (claude-sonnet-5). Cap $3.
Common rules: `.claude/briefs/runs/2026-09-24-lane-b-common.md` (read it first; it governs).

Spec: specs/debosnys-1883.json. Run `cheap_tests_in_order[0]` and its matched control, nothing else. Write both numbers into the spec's `cheap_test_done` (with date, method, cost). Judge any candidate plaintext with `tools/judge_plaintext.py` and paste its output. Cap $3. Report in five lines; do not run test 2; rule 10 wording; a negative's done line carries the target and control numbers side by side.

cheap_tests_in_order[0] here is the image fetch and two-pass transcription. Fetch the images from Schmeh's post (image URLs are in sources/schmeh/posts/03-debosnys.html; at most 4 requests to scienceblogs.de, >=1.5 s apart) into ciphers/debosnys-1883/images/ with manifest.json. Pass A yourself, pass B by ONE Sonnet subagent writing its TSV to disk, reconcile with tools/reconcile_passes.py; report symbol counts, IC and line structure per cryptogram with pass agreement. Control for this test is the pass agreement itself and an IC of an English and a French text of the same N (same script). Put ciphertext.txt and a NOTES.md (status line open) in ciphers/debosnys-1883/, and point the spec's ciphertext field at it. If agreement is under 60%, stop and report the blocker (glyph atlas), per the PROCESS memo.
