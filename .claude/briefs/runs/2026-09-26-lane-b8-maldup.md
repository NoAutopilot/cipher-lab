JOB bMALDUP (LANE B8, Sonnet, cap USD 6, wall-clock box 50 min; hosts: none -- images on disk): malsburg-hessen-1636, are records 507 and 508 (ff.28 and 30, both 28 Mar 1637, same postscript) the same text enciphered twice?
Intake gate: run and paste `python3 tools/intake_gate_check.py malsburg-hessen-1636` (orchestrator's run 06:51-06:54 UTC: exit 0, partial). Nonzero = stop.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b8-common.md` (it governs); NOTES.md sections bMAL2/bMAL3 (transcription conventions: how groups are split, the TSV format in transcription/ and recon_0003/), "Pooled-N control and leaf extent (bMALC)", extent.tsv.
Why: extent.tsv says ff.28-29 and 30-31 are near-duplicates. If the digit groups are identical, one is a copy (transcribe once, half the cost). If the groups differ but the structure lines up, the same plaintext is under two homophone choices: every aligned pair of different groups is a free equivalence (same meaning), which is the strongest cheap lever on a K=95 nomenclator.
Pricing (Usage 6, per pass): the first 10 cipher lines of f.28 and of f.30 only; 2 leaves x 2 blind passes = 4 Sonnet subagent calls, about USD 1 each; one crop per disagreement, at most 15. Stop before starting a call that would cross 80% of cap or box.
Steps: (1) crops: `python3 tools/iiif_lines.py --image ciphers/malsburg-hessen-1636/images/hstam_4_h_1411_0028.jpg --out ciphers/malsburg-hessen-1636/crops/0028 --debug` and the same for _0030 (paste output; check the overlay; pip install numpy pillow if missing). (2) two blind passes per leaf on the first 10 cipher-line crops only (crop paths only, never the full image), TSV per bMAL2's format, committed per leaf; reconcile with tools/reconcile_passes.py; settle disagreements on the crop. (3) Compare f.28 vs f.30 group sequences: global alignment (edit distance on groups), share of identical groups in aligned positions, and the list of aligned differing pairs. CONTROL (can differ from the target by construction): the same alignment of f.28's stretch against an equal-length stretch of the already-reconciled ff.3/12 text (recon_0003, recon_0012) and against 200 shuffles of f.30's groups; report target identity share vs control mean and p95.
Verdict: copy (identity near 1), same text re-enciphered (identity well above control, differing pairs structured), or different texts (at control). For "re-enciphered", write equivalences.tsv (group_a, group_b, count, positions) as grade S candidates -- not a key.
Output: NOTES.md section "507/508 duplicate test (bMALDUP)", transcription/0028_*, 0030_*, dup_align.tsv. ROOM done line <= 600 chars: identity share target vs control, verdict, pair count. Final reply at most five lines. Do not edit NEAR.md.
