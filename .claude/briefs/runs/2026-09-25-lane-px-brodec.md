PX-BRODEC (Sonnet, cap $6 as a stall alarm; the orchestrator watches your cost, you cannot; at most 2 subagents). Target: ciphers/antt-msliv0638-brochado-1712. Job: decode the body cipher runs with the period key, controls first, then spec, judge and a fresh-instance re-derivation (rule 7). Kind: recovery (key source `period`). Parent: LANE PX orchestrator session_01KapVpHVzNpnnQce5C8c3LY. Read the lane COMMON, .claude/briefs/solver.md, NOTES.md sections PX-BROKEY, PX-BROKEY2, PX-BROPASSB, PX-BROGLYPH, PX-BROBODY, PX-BROBODY2; key.tsv, decode.json, body_ciphertext.tsv, body_letter_map.tsv. No network needed.
1. Extend decode.json (or add a second config) so `python3 tools/decode_key.py ciphers/antt-msliv0638-brochado-1712 --check` also regenerates reading_body.txt and reading_body_tokens.tsv from body_ciphertext.tsv + key.tsv, grading every token: C (code fixed at grade C in key.tsv and token graded H in the transcription), M (code at M, or token graded M), U (unkeyed, shown as _). Treat m0275-r1 + m0276-r1 + m0276-r2 as one run (letter 134), with the clear context words either side.
2. Controls first: decode m0179-r1 and m0180-r1 (letters 80 and 81) and compare letter by letter with plaintext_appendix.tsv's Carta 80 and 81 decipherments; report agreement per run. If either control is under 80%, stop after step 2 and report why (key, transcription, or the appendix copy itself).
3. Letter 134 (m0275-m0276): decode, give the Portuguese with period spelling and a word-by-word English gloss; mark every U and M position; propose word completions only as grade I, one alternative per gap at most, never folded into the C reading. Per-token counts C/M/I/U (rule 4).
4. Spec specs/antt-msliv0638-brochado-1712.json on the pattern of specs/antt-linhares-chave.json (ciphertext source and date, alphabet, constraints: homophonic letter substitution inside clear Portuguese, key rebuilt from the volume's own Deciffrada appendix; judge block language pt). Run `python3 tools/judge_plaintext.py specs/antt-msliv0638-brochado-1712.json --file reading_body_letter134.txt` and paste its output verbatim into NOTES.md (a FAIL is reported as a FAIL).
5. Fresh-instance re-derivation: one Sonnet subagent given only ciphertext_appendix.tsv, plaintext_appendix.tsv and body_ciphertext.tsv (no key.tsv, no scripts, no NOTES.md) rebuilds a key and decodes letter 134. Compare per code (on codes both keys have) and per token of letter 134; a difference beyond the M/U positions goes in NOTES.md as unresolved.
NOTES.md gets one section "## PX-BRODEC" with the control numbers, the letter-134 reading table, the judge output and the re-derivation comparison. Status line stays as it is unless the key reads the body run fully (then `partial` or `solved` by rule 5, with the reason on the line below). Push after each step. Touch only ciphers/antt-msliv0638-brochado-1712/**, specs/antt-msliv0638-brochado-1712.json and ROOM.md. Report what was read and where it was not found; do not classify novelty; write "cost: not visible to me".
