JOB bCAS (LANE B5, Sonnet worker). One line: open ciphers/castelcicala-1816/, run check-solved, then hand-read the un-glossed 1816 despatches against Bourdeau's 162 confirmed code values and the Decazes/Florida crib themes (the "retry" step his escalation log marks not-done).

Read first: `.claude/briefs/runs/2026-09-26-lane-b5-common.md` (and the 7b COMMON it names), `.claude/briefs/breadth.md`, CLAUDE.md rules 1-5, 7, 8, 10. Source row: QUEUE.md "Re-rank for LANE B5" rank 3 and QUEUE.md G3.

Cap and box: $4 or 40 minutes from your first `date -u`, whichever first. Disk and git only; no DECODE login, no image hosts.

Steps:
0. `date -u`; `python3 tools/room.py --start`; ROOM claim (tools/room.py) naming ciphers/castelcicala-1816/ and specs/castelcicala-1816.json.
1. Clone once `git clone --depth 1 https://github.com/dbourdeau/cyphersolver <scratch>/cs`; read only `castelcicala1816/` (NOTES.md, key.tsv, reading_partial.txt, transcription.txt, cipher/, transcripts/, profile.json). Record HEAD commit and date. First establish from NOTES.md, as of that commit, which records are glossed, which read and whether the retry step is still not-done; if Bourdeau has done it, write that (found-solved or partial for this job) and stop.
2. Check-solved (rule 1, breadth.md intake), minimal: Tomokiyo (cryptiana, Castelcicala), the Neapolitan diplomatic editions / Archivio di Stato di Napoli inventories reachable by Internet Archive full-text search (be-api fts: "Castelcicala" 1816 dispacci), DECODE listing (tools/decode_list.py, login-free) for R9553-R9589, aaymeloglu/unsolved-ciphers (clone shallow, grep, delete), one OpenAlex + one Semantic Scholar query. NOTES.md lines 1-2 in the gate's format; `python3 tools/intake_gate_check.py castelcicala-1816` output pasted; proceed only on exit 0.
3. Test: copy (credit: Bourdeau, dbourdeau/cyphersolver castelcicala1816, commit, date, CC BY 4.0/MIT) the key and the un-glossed 1816 records. Apply the 162 values (tools/decode_key.py via decode.json; add a tool option with an offline test if needed) to show coverage per record. Then the hand read: using the known values as anchors and the Decazes / Florida themes of the glossed letters as cribs, propose values for open groups only where the same group fits the same word in at least two independent contexts (grade S with that two-context control; one context is M). Control for the coverage number: the same key on the records with group order shuffled (3 seeds) -- report target vs control. No it16 judge verdict: it is 16th-century against an 1816 target (300-year era gap); if you paste a judge line, flag the mismatch beside it and grade by eye.
4. Write specs/castelcicala-1816.json with cheap_test_done (date, method, both numbers, count of new S values and M values). Finish per the common file; ROOM "for LANE V7: ciphers/castelcicala-1816 reading ready" only if a reading beyond Bourdeau's exists. Report what was found and where it was not found; do not classify novelty.
