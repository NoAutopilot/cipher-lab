VX-SCGAL (Sonnet). LANE VX job 1 scout (a): Gallica and BnF archivesetmanuscrits. Row prefix VX-G01, VX-G02 ...
Kind: scout (never solves, never promotes, no ciphers/ folder, no check-solved, no decoding). Parent: LANE VX orchestrator session_01EYwyYDeAaQReWy7ZuQcoxe. Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-vx-COMMON.md) first, then CLAUDE.md's host table ("Image and catalogue hosts") and follow its per-host route and rate rule exactly.

Target pattern (LESSONS.md section 1: "the key was in the archive beside the letter"): a volume, file or correspondence in which UNDECIPHERED cipher letters or passages sit beside (i) a key sheet / table of the cipher, (ii) a decipherment of a sibling letter of the same correspondence and period, or (iii) an interlinear or marginal decipherment of part of the run. Precedents on disk: WVO 4613/4615 -> Lodewijk van Nassau; WVO 74 -> Saxony 57; Blathwayt same-page keys; the Brochado letterbook's own Deciffrada appendix (ciphers/antt-msliv0638-brochado-1712). A cipher letter with no key or decipherment in reach is NOT a row for this lane (at most a one-line lead).

Copy-free only: for every keeper, test a full-size image URL (native-resolution IIIF or the host's full-res image) and look at the leaf yourself: it must show cipher (digit/symbol groups, a key table, an interlinear decipherment). Record the tested URL and the leaf. A description hit whose image shows no cipher is dropped with one line.

Drop-check every keeper against QUEUE.md (grep shelfmark, sender, recipient), CATALOG.md, LANDSCAPE.md, ciphers/ folder names and NOTES.md, sources/wvo and sources/huygens (already classed there = not yours), and ROOM.md claims of the last six hours and every target held by lanes PX, TX, KX, OX, LX, DX (this account) or R5, N4, B, V2 (other account): never re-list a row already in QUEUE.md; if an existing QUEUE row matches but never noted the key/decipherment beside it, write one line under "Adds to existing rows" naming that row and the key source instead of a new row.

Output: append rows to QUEUE.md under the section "## Key beside the letter (LANE VX, 25 Sept 2026)" (create it at the end of QUEUE.md if absent; three scouts write here, so `git pull --rebase` immediately before editing, keep other scouts' rows, and number yours from the prefix given below, never colliding). Columns: Row | Holding / shelfmark | Date(s) | Sender -> recipient | Undeciphered material (how many letters/lines) | Key or decipherment beside it (what, and exactly where: folio/canvas) | Image URL tested (full size) + leaf | Copy-free (yes/no) | Scout note (why EV is high/low; transcription on disk? printed edition to check?). Then a short method paragraph (queries run and totals per host) and a list of non-copy-free leads (not numbered rows).
Touch only QUEUE.md (that section) and ROOM.md. Stall alarm $6; at most 2 Sonnet subagents. Report: rows found, how many copy-free, requests per host, cost.

Host family (a) only: gallica.bnf.fr (SRU search, IIIF manifests, tools/gallica_folio.py for canvas<->folio) and archivesetmanuscrits.bnf.fr (finding aids; page 1 by curl, pagination needs the browser tool, run twice). Never touch another scout's hosts. Finding-aid words: "déchiffrement", "déchiffré", "en chiffres avec le déchiffrement", "avec le déchiffrement", "chiffre", "clef du chiffre", "table de chiffre", "contre-chiffre", "interligné"; ambassadors' registers and dépêches volumes (Français, Nouvelles acquisitions françaises, Clairambault, Cinq-Cents de Colbert, Dupuy, Mélanges de Colbert, Italien/Espagnol fonds) 1500-1800. Prefer volumes where the finding aid says some letters are deciphered and others not. Budget: <=80 Gallica requests (1-2 s apart, browser UA per the table), <=40 archivesetmanuscrits requests.
