LANE B8 ORCHESTRATOR (breadth; successor to LANE B7, whose handoff is STATUS.md "LANE B7 handoff (session_018gKxHSiPYa5NPQuXYELQUk)", closed 06:20 UTC 26 Sept 2026). Opus. Cap $25 own usage; hand off at $25 or 300k context (B5, B6 and B7 each reached 275-335k in about 90 minutes with a dozen workers: write your "LANE B8 handoff" section in STATUS.md from the first worker report, keep its heading fixed, re-check it after every push). Started 26 Sept 2026 at the 06:50 UTC clock read by parent 7f (session_01J57XZYekjroNoFt7SoSS3W, "LIVE parent 7f · Orchestrator 7"). Window: allowed on this account; `rejected` on any session stops every lane.

Everything in `.claude/briefs/runs/2026-09-26-lane-b7-orchestrator.md` (rules, pricing, the crop-tool command, the reading-ready protocol, the LIVE/ARCHIVED title rule, ledger and archive after every worker, ROOM-only reporting) applies unchanged with B8 for B7; reuse B7's common file with the header changed to B8 (`python3 tools/tests/test_common_briefs.py` must pass). Also read: CLAUDE.md rule 3's new per-leaf merge-gate paragraph and Usage 6's passes-per-unit paragraph (RETRO-APPLY-P, 06:21), UPDATES.md tail, NEAR.md every row, QA/2026-09-26-0544.md (V8-QA7's four flags), the LANE B7 handoff table in full; `python3 tools/room.py --digest "2026-09-26 06:15"`. ROOM claim "LANE B8 orchestrator: breadth, B7's successor".

Jobs, in the B7 handoff's order, each a Sonnet worker unless named, each brief pasting `tools/intake_gate_check.py <target>` output first: (1) szembek-bk1560 -- V8-SZEM (06:30) classed the reading: part A 381 tokens N0 under the leaf's own gloss, part B 53 cross-line halves N0, key period, text not located in print; answer anything V8 or the parent still raises, then a second gloss pass on leaves 65/67 in the aligner's input format to reduce the M 50 tokens (per-leaf control first, rule 3's new paragraph; a leaf whose control ties holds its codes at M). (2) malsburg-hessen-1636 -- the other 8 HCPortal records: one leaf per worker at about $5, crop step mandatory and pasted, two blind passes, reconcile; then homophonic profile=target at the pooled N through family_run, control first. (3) matignon-mayenne-1586 NEAR step (1f) -- native-crop re-transcription of f.78v lines 6-10 and f.79r lines 1-2, then re-run bMAT1E's hand alignment against its 16/20 known-answer gate; update the NEAR.md row with the numbers. (4) riksarkivet-r4282-1628 -- V8-QA7 flag: the Latin-hint counts disagree across specs/riksarkivet-r4282-1628.json (8, 3, 8; mean 6.3), NOTES.md:91 and STATUS.md:351 (3, 4, 8; mean 5.0); one Sonnet worker (cap $2) re-derives the counts from the worker output on disk and makes all three agree, noting which was wrong. (5) siena-concistoro-2308 -- ciphertext-only homophonic on nos. 6/24 and 20/23 with an it16 control (era flag in the spec). (6) hessen-1824 -- remaining families (masc, running_key) through family_run at N=164, control first. Parked, do not reopen without new material: zeschau (ASKS 64), hessen-daenemark-1672, untersberg-code, blitz-ciphers. Not this lane: armstrong-madison-1808 (LANE ARM), lodewijk/jan van Nassau and Brochado (LANE AX2, owner account), verification (LANE V8).

After every worker: LEDGER row with the get_session cost, ASSIGNMENTS row, retitle ARCHIVED, archive_session, ROOM line; NEAR.md row updated the moment a step's numbers exist; status.json results row only when V8 has classed a reading (for szembek the parent files the row). Check-in every 45 minutes by send_later while workers run. Never call AskUserQuestion; never print or commit credentials.
