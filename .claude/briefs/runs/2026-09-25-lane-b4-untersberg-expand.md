LANE B4 breadth worker bUNT2: untersberg-code NEAR.md step (1), abbreviation expansion with a synthetic control. Sonnet (claude-sonnet-5). Cap $3 or 40 minutes from your first `date -u`, whichever first. No subagents. Disk and CPU only, except as below. Parent: LANE B4 orchestrator session_01V2WMavWGMeAUYELUpmyEcX (written 25 Sept 2026 21:34 UTC).
Common rules: `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (it governs; read "LANE B3" as "LANE B4") and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Intake gate, run by the orchestrator 21:33 UTC: `untersberg-code: open (line 3) -- edition/page or full-text-search citation found within 6 lines`, exit 0. Re-run and paste.
Why: bUNT (test 1) found 36 period-closed short tokens against 12.0 (5-20) in synthetic strings from the target's own unigrams -- the text is shaped like scribal abbreviation, not a cipher of meaningless periods (NEAR.md row). The named next step is to try to expand it, with a control that says whether an expander could succeed at all at this length.
Steps, control FIRST (rule 3; match the design, not only the length):
(1) Control. From tools/data/de16/composed_enhg.txt (Early New High German; state that Latin has no wired corpus and that you checked the era, rule 3's corpus paragraph), take 5 windows of real text and abbreviate each to the target's own shape: same token count, same share of 1-letter, 2-letter, 3-letter and longer tokens, same share of digits (map digit tokens to a numeral rule you state), using suspension (initial letter(s) + period) as the scheme. Build an expander: a beam search that proposes a full word for each abbreviated token from a word list with a word-bigram (or trigram) model trained on the rest of de16 (never on the window itself). Score = share of tokens expanded to the true word. Gate: mean >= 0.30 across the 5 windows. Report per window. Also report a floor: the same expander on 5 windows abbreviated from shuffled word order.
(2) Only if the control meets its gate, run the same expander on the target's six lines (spec `ciphertext`, as transcribed; rule 2), and judge the top expansion with `python3 tools/judge_plaintext.py specs/untersberg-code.json --file <expansion>` (paste the output; the spec's judge is de; say plainly that a de judge on an expansion the model itself chose is circular and counts for little). Grade every token M or I (rule 4): no H/C/S is possible here.
(3) If the control is below its gate, stop: that is a non-test (rule 5), and the numbers are the result.
Write both numbers into specs/untersberg-code.json `cheap_test_done` (as NEAR step 1, not a spec test number), scripts and outputs in specs/cheap-tests/untersberg-code/. Do not touch NEAR.md (the orchestrator does). No Latin corpus build, no network fetch; if you conclude a Latin 16th-c. corpus is the missing piece, say so as a one-line suggestion.
Done line: control mean (per window) and floor vs target result (or CONTROL BELOW GATE).
