OUT-CHECK-3 (Opus, cap USD 6, box 40 minutes; network for the cited sources and the institution contact pages only, one request per host, 1.5 s apart). Parent worker for the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n), CLAUDE.md Outreach gate 7. You are the checker, never the drafter: your job is to falsify. Job: one draft the owner approved for sending after this check -- outreach/tomokiyo-unsolved-list.md with outreach/mailbox/tomokiyo-unsolved-list.json. Check every factual sentence against CITATIONS.md (the two Tomokiyo rows: the unsolved.htm credit of 24 Sept and the Raince blog post of 26 Sept -- fetch both pages and confirm the credit and the post exist and say what the draft says), outreach/bowes-tomokiyo.md and bowes-tomokiyo-thanks.md (the existing thread, tone and what was already said), CONTRIBUTIONS.md, LICENSE (MIT), and CLAUDE.md rule 10. The draft claims nothing about any reading; check that it does not. Confirm the Spanish and German texts say the same as the English (a native-quality read; correct grammar and register in place, keep the sentences). Rule 10 sweep on every body (no solved, new, first, unpublished, unread, never printed beyond what the audit's own wording allows; "sin descifrar" quoting the Índice is fine). Check that the first paragraph carries the AI-disclosure sentence (outreach/README.md rule 1) and that the body is unwrapped (rule 6). For each draft: correct the json body and the md body in place (minimal edits, keep the [SIGN-OFF] line), fill the Dresden `to:` line and json "to" from the archive's own page with the date read, add `checked: <UTC time> by OUT-CHECK-3 -- <n> sentences verified, <m> corrected: <list>` to the md header; if a claim cannot be verified from the repo or the source, rewrite it to what is verifiable or cut it and say so. Never add claims. Never write the owner's name. Commit by explicit path, `python3 tools/room.py --push <paths>`.
First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker OUT-CHECK-3 (Opus, <session id>)`. Finish: ROOM done line "done: for the owner-account parent: OUT-CHECK-3 -- 2 drafts checked, corrections: <one clause each>, addresses <verified/changed>"; add your LEDGER.md row; one-paragraph reply; stop. Never call AskUserQuestion; never print or commit credentials.
