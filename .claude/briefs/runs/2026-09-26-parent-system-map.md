SYSTEM-MAP (Opus, cap $8 of usage, box 45 minutes, no network except git; at most 2 subagents for reading). Parent worker for parent 7h (session_01GT3Yv4kUZrBR8pc2s5EToH), at the owner's ask of 26 Sept 2026 16:58 UTC ("a clear diagram of how our work is done and the different levers ... sometimes we add something and then it's lost in the ether and our other agent account can't see it"). Authorised by CLAUDE.md Usage 8a (rules become tools) and the Improvement loop. Written at the 16:58 UTC clock read.

Build `SYSTEM.md` at the repository root: the one current-state map of how the work is done, for both accounts' parents and for a new collaborator. Sources: CLAUDE.md in full (Operating model, Pipeline, Collaborators, Workers, Usage, Access playbook, Improvement loop, Outreach with today's gate 7), .claude/briefs/parent.md, .claude/briefs/README.md, ONBOARDING.md, README.md, tools/*.py `--help` and docstrings, tools/*_runner_prompt.md, tools/*_runner_brief.md, outreach/README.md, the queue files' headers, STATUS.md "Lane structure" table and the 26 Sept parent handoff lines, UPDATES.md (every instituted change: each must appear on the map or be marked retired). Sections, each a table with columns (name, what it does, defined in, enforced or run by, who may change it): (1) Roles: owner, the two parents (one per account, invisible to each other, coordinating only through git), lane orchestrators (B breadth, V verification, GOLD standing, target lanes), workers, verifiers (two hats), retrospective and RETRO-APPLY, LEARN, PR-LAND, MAIL-PREP, OUT-CHECK, tool builders; the lineage-depth constraint of 26 Sept 2026 (parent.md "Lineage depth") and who creates the next parent. (2) Loops outside the repo: the three ChatGPT runners (Verifier / JSTOR / Desk runner) with their queue file, PR title prefix, what lands them and which gate; the project mailbox (parent drafts, gate 7 check, owner sends, replies logged). (3) Gates, one row per mechanical check in tools/ (intake_gate_check, family_run control-first, near_check, ledger_check, orphan_check, room.py warnings/refusals, lq_answer_check, desk_check, judge_plaintext + rule-7 re-derivation, key_probe/key_livecheck, print_check before reading-ready), each with the rule it enforces and where it runs. (4) Registers and state files: status.json + the board artifact, NEAR.md, ASKS.md, KEYS.md/KEYS-STATUS.md, CONTRIBUTIONS.md, CITATIONS.md, LEDGER.md, ROOM.md, STATUS.md, hub-seed/ASSIGNMENTS.md, hub-seed/SUCCESSOR-PROMPT.md, UPDATES.md, the three queues, specs/, ciphers/<t>/ files, outreach/. (5) Cadences and levers: check-in interval, QA cadence, retrospective trigger (12 rows or USD 60), caps per brief, rate-limit rule, hand-over points, what only the owner can move (money, sends, keys, the next parent). (6) A diagram: a Mermaid `flowchart` block (owner, runners, parents, lanes, workers, gates, registers, mailbox) that renders on GitHub, plus a 30-line ASCII version for terminals. (7) "How to add something": the rule that any new tool, gate, loop, register or runner gets its row here in the same commit, and that `tools/system_map_check.py` enforces it.
Then build `tools/system_map_check.py` with an offline test in tools/tests/test_system_map_check.py: every `tools/*.py` (except tests), every `tools/*_runner_prompt.md` and `*_runner_brief.md`, every `*.tsv` queue at the root, every register file listed above, and every gate name that appears in CLAUDE.md's Usage 8a precedent list must be mentioned in SYSTEM.md, else nonzero with the missing names; `--help` documents it. Wire it: one sentence in .claude/briefs/parent.md "On start" (SYSTEM.md first in the reading list) and duty 3a (run system_map_check at every check-in), one in ONBOARDING.md (read SYSTEM.md first), one in .claude/briefs/retro-apply's common text or README.md common tail (a RETRO-APPLY that adds a tool adds its SYSTEM.md row and runs the check), one clause in CLAUDE.md Usage 8a naming the tool, a "System" link in tools/build_dashboard.py's header pointing at https://github.com/NoAutopilot/cipher-lab/blob/main/SYSTEM.md, and one UPDATES.md row per instituted change (By = "SYSTEM-MAP for parent 7h", clock-read time). Run the test directly and the check once on the live repo (it must pass). Rule 10 wording; never name the owner (rule 9); never touch status.json or any ciphers/ folder; never print or commit credentials; never call AskUserQuestion. Also post one ROOM line "for the owner-account parent: SYSTEM.md is the current map; read it first at your next start".
First: `date -u`; `python3 tools/room.py --start`; ROOM claim `parent worker SYSTEM-MAP (Opus, <session id>)`. Finish: commit by explicit path, `python3 tools/room.py --push <paths>`; ROOM done line "done: for parent 7h: SYSTEM-MAP -- SYSTEM.md N rows in 7 sections, system_map_check passes (M names), wired into <files>"; one-paragraph reply; stop.
