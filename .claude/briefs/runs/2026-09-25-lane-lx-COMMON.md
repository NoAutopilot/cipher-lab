COMMON RULES (LANE LX worker; parent LANE LX orchestrator, session id in your job brief or ROOM.md; lane goal: read ciphers/antt-linhares-chave, the Linhares dictionary code, and any sibling letter under the same key; kind recovery):
- First: `date -u` (use that clock for every timestamp; never estimate it); `python3 tools/room.py --start`. Read CLAUDE.md in full (rules 1, 2, 3, 7, 10; the good-citizen rule; the Access playbook), LESSONS.md sections 1-2, ciphers/antt-linhares-chave/NOTES.md, and the last 30 lines of ROOM.md. Claim with `python3 tools/room.py "LANE LX <role> (Sonnet, <your session id if known>)" "claim: <job> -- files: <paths>"` BEFORE other work. Done line the same way when you stop; `flag:` for anything the lane orchestrator must see.
- Scope: touch only the paths your job brief names (normally ciphers/antt-linhares-chave/** and ROOM.md). Never edit QUEUE.md, STATUS.md, status.json, LEDGER.md or another target; the lane orchestrator and the parent do those.
- Git: commit by explicit path only (never `git add -A`); push with `python3 tools/room.py --push <paths>` (rebase-and-retry loop). ROOM.md is append-only. Never rewrite history, never force-push, never a pull request. Push after each piece of work lands, not once at the end.
- Hosts (good-citizen rule): one request at a time per host, >=1.5 s apart (DigitArq >=3 s, <=60 per session; its JSON search past 10 results is broken, see QUEUE.md 'PARES / DigitArq'); descriptive User-Agent `cipher-lab research script (contact via repository)` unless the playbook says a site needs a browser UA; on 429/403/Cloudflare/challenge stop that host, log it in NOTES.md and ROOM.md, one retry after a pause at most. PARES is dead from the cloud; do not retry. Report your request count per host.
- Rule 10: never write new, unpublished, unread, first, never printed, previously unknown. Report what was found and where it was not found. Do not classify novelty; the verifier does, in AUDIT.md. Never promote to the board.
- Rule 7: a claimed reading is reported only with `tools/judge_plaintext.py`'s output pasted in where a spec exists, and stands only after a fresh-instance re-derivation from the key and the book. A reading the judge failed is a candidate that failed check X, not a reading.
- Rule 3: a negative carries target and control numbers side by side (for a book test: the five test pairs and what the candidate page actually shows), or it is not a negative.
- Credentials: never print them; no unfiltered env/printenv/set, no curl -v / --trace / set -x on a credentialed call; test presence with `test -n`.
- Usage: Sonnet. Nothing is billed (Max plan); the cap in your job brief is a stall alarm, not a budget. Check your own cost after every major step; at 80 percent of the cap push what you have, write the remaining steps as one line in NOTES.md, and stop, so the lane orchestrator can brief a successor if the work is still producing. At most 2 subagents at once, on Sonnet, and only for the passes or searches your brief names. Scripts read, models judge: fetch once to disk, grep, give the model the hits. If you see `rejected` on the rate limit anywhere, push what you have and stop.
- Finish: commit, push; ROOM `done` line; then your final reply is ONE paragraph, first line the answer, then files touched, request counts per host, cost from the session metadata. Then stop.
- Intake gate (25 Sept 2026): before any deep work (transcription, key application, cryptanalysis) on a target, read its check-solved verdict against .claude/briefs/check-solved.md: an `open` whose sentence does not name the standard edition and the pages or full-text search actually read, or that names an edition it could not open, is `blocked`, whatever word it uses. Send a check-solved worker first. (Linhares, 25 Sept: an `open` with the sender-family edition unread went to deep work; the verifier held it at N3 for that reason.) If your job brief sends you to deep work on a target whose verdict fails this, stop and `flag:` it.
