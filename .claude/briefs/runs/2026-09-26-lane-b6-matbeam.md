JOB bMATBEAM (LANE B6, Opus -- designs and runs, cap USD 5, wall-clock box 45 min from your first `date -u`): matignon-mayenne-1586 NEAR step (1b): resolve the M/U Cipher-1 codes with an fr16 language-model search, matched control first.
Intake: `python3 tools/intake_gate_check.py matignon-mayenne-1586` -- paste output (after bCSMAT's rewritten verdict) into NOTES.md; nonzero exit = stop.
Material on disk (no hosts needed): ciphers/matignon-mayenne-1586/ reading_tokens.tsv (12,994 tokens: H 10,074 / M 1,648 over 13 codes with 2-3 candidates each in key.tsv / U 1,272 over 63 unkeyed signs), spans.tsv, resolve_mu.py (bMAT2's two-context rule, a control-backed negative: S 41 vs shuffled 40-45 -- read its section first), judge corpus fr16 as wired in specs/matignon-mayenne-1586.json.
Design (orchestrator's sketch; improve it if you see better, say why in NOTES.md):
 - Model: character n-gram LM (order 5-6, Kneser-Ney or Witten-Bell) trained on tools/data/fr16 with spaces removed (cipher has no word division), letters folded to a-z (u/v, i/j merged if the key merges them -- check key.tsv).
 - M codes: chosen PER OCCURRENCE among their own key.tsv candidates (homophone-ambiguity is contextual), by Viterbi/beam over each line with H letters fixed.
 - U signs: one GLOBAL value each from a-z plus `null` (and a nomenclator `[TOKEN]` placeholder that scores as a word break), by hill-climbing / coordinate ascent over the 63 signs with the M beam in the inner loop; restarts >= 5.
 - Nothing else in the key changes; H letters are fixed.
Controls (both before the target, rule 3; numbers side by side):
 (A) KNOWN-ANSWER synthetic control, the one that licenses any value: take a held-out fr16 passage (a file or span NOT used to train the LM) of the target's length, encipher it with the target's own key structure -- same H inverse map, the same 13 M codes each hiding its true letter among the same candidate sets, 63 U signs standing for letters at the target's U frequency and sign-count profile, nomenclator boxes at the target's rate -- then run the identical search and score M-occurrence accuracy and U-sign accuracy against the truth. 3 seeds. Gate: U-sign accuracy >= 0.6 and M accuracy above the frequency-baseline (always the likeliest candidate) by >= 10 points; state the gate before running.
 (B) Shuffled-line-order null: the target's tokens shuffled within each line (3 seeds), the same search, to show the LM gain on the real text is not what the optimiser buys on any text: report LM log-lik per char before/after on target and on each shuffle.
Target only if (A) meets its gate; else CONTROL BELOW GATE, stop, report the (A) numbers (still useful for the NEAR row).
If the target runs: write the resolved values as grade S candidates in ciphers/matignon-mayenne-1586/mu_beam_results.json (per sign: value, support count, margin over second-best); commit NO key.tsv/exceptions.tsv change unless a sign's value is stable across all restarts and its margin exceeds the largest margin seen for any WRONG sign in control (A). Re-judge: `python3 tools/judge_plaintext.py specs/matignon-mayenne-1586.json --file <new reading>` (fr16; state the corpus and its era -- Lettres de Catherine de Médicis, same decade) vs the shuffled controls; paste output in NOTES.md. Script `mu_beam.py` beside the others, reproducible from the repository root (rule 7).
Output: NOTES.md dated section "NEAR step (1b), M/U beam (bMATBEAM)": gate stated, control (A) accuracies x3 seeds, control (B) gains, target gain, judge line, grade counts (H/S/M/U). ROOM done line <= 600 chars with target and control numbers side by side. Workers never edit NEAR.md.
Common rules: read `.claude/briefs/runs/2026-09-26-lane-b6-common.md` in full; it governs, except that you are Opus and do the work yourself (no image reading needed). Report what was found and where it was not found; do not classify novelty. Final reply at most five lines.
