LANE NX job NX-MOR4 (Sonnet). Parent: LANE NX orchestrator, session_01RDXRYmATas1Xy6K1Jjwtvo. COMMON rules: .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md, read it and follow it, with "LANE NX" for "LANE AX" and lane goal "copy-free candidates with a key route, one control-backed cheap test each".
Target: ciphers/rah-morillo-1817. Item 3 (Herrera to Morillo, Guanare, 7 Nov 1820) now has a grade-H key, key_5186.tsv (17 numeral signs, from the leaf's own interlinear period decipherment; NX-MOR2), re-derived with 0 differences (NX-MOR3). Question for this job: does any OTHER cipher text in the Morillo papers at the RAH (Coleccion Pablo Morillo, Sig. 9/7650-9/7690 range, 1815-1821) carry numeral ciphertext that key_5186 can be applied to? Item 2's own key table is not preserved (NX-MOR), so item 2 cannot be the test unless its leaf carries ciphertext (it does not, per NX-MOR).
Box: 90 minutes from your first date -u; stall alarm USD 12. Units: search (about 20 minutes), then at most 4 candidate records' images (2 browser fetches each), then at most 2 transcriptions (2 passes each on crops, about USD 1.5 per call). Stop before a unit that would cross 80 percent of box or alarm.
Files: ciphers/rah-morillo-1817/**, ROOM.md.
Steps:
1. Find candidates: RAH's own search takes a plain POST (QUEUE.md line ~1760: curl -X POST --data-urlencode busq_general=<term> to resultados_busqueda.do, cookie jar only to keep a result id alive): terms cifra, cifrada, cifrado, clave, "en cifra", Herrera, plus Morillo with each. Also the Contreras 1988 Catalogo de la Coleccion Pablo Morillo (Google Books, key + country=US; search-inside for "cifra"/"cifrada") for entries described as cipher letters. List every candidate (signature, folio, date, sender, recipient, record id) in rederiv_transfer/candidates.tsv. Hosts: bibliotecadigital.rah.es >= 3 s apart, at most 60 requests; Google Books at most 20.
2. For up to 4 candidates dated 1818-1821 and nearest to Herrera/Guanare or Morillo's own headquarters, fetch the page images (OAI-PMH didl + tools/browser_fetch.js --binary, the NX-MOR route) and say which carry numeral ciphertext, how many groups, and whether any interlinear decipherment is present (a second glossed leaf is itself a result: it would test key_5186 as known plaintext).
3. For at most 2 leaves that carry numeral cipher: line crops (tools/iiif_lines.py --image <file> --out ...; paste the command), two blind transcription passes, reconcile. Then gates written in NOTES.md BEFORE decoding: (a) coverage: share of cipher tokens whose value is in key_5186 (report; a different numeral range means a different key -- say so and stop); (b) reading: Spanish-word share of the decode vs 20 shuffles of key_5186's values over the same tokens (shuffling values CAN change this statistic; it cannot change coverage -- rule 3, say so); target must exceed the shuffle max. If the leaf is glossed, the known-answer test replaces (b): exact agreement of key_5186's reading with the gloss, per token, vs the same 20 shuffles.
4. HYPOTHESIS to test, not to adopt: NX-MOR3's blind pass read item 3's line-wrapped groups as "Bolivar", implying code 26 = v (the leaf gloss has not settled 26). Test it only on the new text: does setting 26 = v produce correct Spanish words at every occurrence of 26 there (list each occurrence with its context), versus 26 = each other unused letter (a small control)? Report the counts; do not edit key_5186.tsv.
5. If no candidate carries numeral ciphertext, write that plainly in NOTES.md with the search log (terms, hit counts, records viewed) -- that is a result: key_5186 then stays a single-letter dataset.
Rule 10: no novelty words; do not classify novelty. Stop; the orchestrator sends any reading to a fresh re-derivation.
Finish per COMMON.
