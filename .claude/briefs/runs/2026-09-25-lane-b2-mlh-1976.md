LANE B2 breadth worker bMLH: mlh-1976. Sonnet (claude-sonnet-5). Cap $3 or 30 minutes, whichever first. No subagents, no second pass.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b2-common.md` (read it first; it governs) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Spec: specs/mlh-1976.json. Run `cheap_tests_in_order[0]` only, as re-scoped here: fetch the image(s) named in the spec's ciphertext_pending (scienceblogs.de only, at most 4 requests, >=1.5 s apart, browser UA as bMOU used) into ciphers/mlh-1976/images/ with manifest.json, and do ONE blind transcription pass yourself (letters/digits as text; invented symbols as sign codes with a short sign table and crop ids). One image. Catalogue distinct shapes with counts and any grouping; say where a shape could be a rebus/picture rather than a letter sign.
Report: N, distinct K, IC with a script, line/word structure, and the controls: IC of an English text at the same N and of a uniform random string at the same K (tools/data, no fetch), side by side. Mark every count 'single pass' (rule 2). Put ciphertext.txt (single pass, draft) and NOTES.md (status open; check-solved not run: say so) in ciphers/mlh-1976/, and replace the spec's ciphertext_pending with a pointer; write cheap_test_done. A second pass is test 2, not yours. At $3 or 30 minutes push what is done.
