LANE ZX ORCHESTRATOR (the three partial targets LANE YX left). Opus. No dollar budget (owner: cost is not a concern). Opened by the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n) at 15:50 UTC 25 Sept 2026, window allowed. Read `rate_limit_info` on yourself before each dispatch and follow BUDGETS.md as written.

Rate limit, check-ins (every 30-45 minutes with send_later), worker count (at most 6 live Sonnet workers), first steps, ledgering and handoff: exactly as in .claude/briefs/runs/2026-09-25-lane-lx-orchestrator.md, with LANE ZX for LANE LX and this lane's COMMON (2026-09-25-lane-zx-COMMON.md). ROOM claim line "LANE ZX orchestrator: Brochado, Clair 349, Barriere". Read each worker's cost and elapsed time with get_session at check-ins; interrupt past its wall-clock box. Paste `python3 tools/intake_gate_check.py ciphers/<target>` before briefing any deep work.

Both accounts are live on this repository. The other account's parent 7b opened LANE R6 (Salviati and the 13 gate-passing targets in the LANE CX handoff), LANE V6 (Portuguese judge corpus, Linhares re-run, bl-charles-digby verifier, verifiers for readings, rolling QA) and LANE B2 (breadth) at 15:39; their briefs are .claude/briefs/runs/2026-09-25-lane-{r6,v6,b2}-orchestrator.md. Never touch their targets or jobs; re-check ROOM.md for a claim in the last 6 hours before every assignment; fetch and rebase before every push; never force-push.

Pool (from the LANE YX handoff in STATUS.md; these are YX's own partials, not the 13 R6 holds):
1. antt-msliv0638-brochado-1712: check by image the other entries that drag the leave-one-out control (Carta 15, 80, Passage 2a m0284/m0294, Carta 101), normalizing both texts to one convention (CLAUDE.md rule 3), re-run the gate; if it passes, the letter 134 decode with the pt judge (tools/judge_plaintext.py; use LANE V6's pt18 corpus once it lands, pt17 until then) and a fresh re-derivation.
2. clair349-este-guise-1556: re-pass f9's 33 lines with the key's own atlas codes as a shared vocabulary for both blind passes (YX's lesson: arbitrary-sign passes need a shared atlas before agreement means anything); decode only past the 60% gate.
3. thurloe-barriere-1654: a permutation z-test on the gloss positions (YX's named next step), target and control side by side; a clean negative closes it.
Reading chain per target: transcription (two blind passes, the 60% gate) -> solver (per-token grades, judge output with a corpus for that language, fresh-instance re-derivation) -> a ROOM line 'for LANE V6: reading ready' (the other account's verification lane holds verifiers this window) -> for N3 or better, a second adversarial audit and a second-opinion prompt in SECOND-OPINIONS-QUEUE.tsv.
For the parent: each reading that passes judge and re-derivation, each AUDIT.md verdict.
