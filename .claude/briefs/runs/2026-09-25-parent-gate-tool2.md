PARENT WORKER TOOLS-GATE2 (Sonnet, wall-clock box 30 minutes). tools/intake_gate_check.py: QA/2026-09-25-1740.md failure 2 -- the verdict regex knows only open|partial|blocked|found-solved, so a NOTES.md whose status word is `solved`, `closed-negative` or `offline-only` exits 1 as ambiguous (antt-fcc-costacabral-1865, status solved since 17:03). Make those three words recognised terminal verdicts that exit 0 with their own label and need no citation (the tool's own comment says they are ungated); everything else unchanged. Add test cases for all three plus a synthetic file whose only verdict word is inside prose (must still be ambiguous), keep all existing tests passing, run the tool over every ciphers/*/NOTES.md before and after and list every target whose exit code changed. Report to the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n) in ROOM.md. Touch only tools/intake_gate_check.py, its test, ROOM.md. ROOM lines with tools/room.py or single quotes. Never call AskUserQuestion.
