JOB ARM-TR2: finish the manuscript transcription -- frame 0033 (the last ~37 groups, fetched by ARM-POOL), the page-1 lines 6-13 span where 54 Founders groups have no ms counterpart, and the page2_L02/L06 crops ARM-S1 found nearly empty of their claimed marks. Sonnet (claude-sonnet-5). Stop and push at USD 7 or 60 minutes. Priced per pass (Usage 6): frame 0033 about 2 pages x 2 blind passes = 4 subagent calls, page-1 L6-13 re-read 2 calls (8 crops), page-2 L02/L06 recrop check 1 call = 7 calls at about USD 0.9 each, plus reconcile/diff unit; stop before a call that would cross 80 percent of either figure. At most 2 subagents at once, Sonnet, crop paths only, never a full frame. No network. Lane: LANE ARM orchestrator session_011gEKqukZAxE6FjtQCzsUQ8. Written 26 Sept 2026 by LANE ARM.

INTAKE_GATE_OUTPUT (live, 26 Sept 2026 06:46, TOMO-REPLY): ciphers/armstrong-madison-1808: open (line 1) -- edition/page or full-text-search citation found within 6 lines; EXIT:0.

Read first: `.claude/briefs/runs/2026-09-26-lane-arm-COMMON.md` (binding), ciphers/armstrong-madison-1808/{NOTES.md (ARM-TR, ARM-POOL, ARM-S1 sections),images/layout.md,tr/**,ciphertext_ms.txt,images/shorthand/INVENTORY.tsv}, `python3 tools/iiif_lines.py --help`, `python3 tools/reconcile_passes.py --help`. pip install numpy pillow if missing.

Do:
1. Crops, MANDATORY and pasted into NOTES.md before the first subagent call: `python3 tools/iiif_lines.py --image ciphers/armstrong-madison-1808/images/M34-014-0033.jpg --region <one page at a time> --out ciphers/armstrong-madison-1808/images/crops_0033 --debug`; check the overlays. Re-cut page2 L02/L06 from 0031/0032 and check them against the overlay (ARM-S1's flag: crop or bookkeeping error).
2. Two blind passes per 0033 page (as ARM-TR did: groups in order; marks counted as runs `*n`; `?` for unreadable digits); reconcile with tools/reconcile_passes.py; settle disagreements from the crop yourself.
3. Page-1 L6-13: two fresh blind passes on ARM-TR's crops of those lines only, with the instruction to separate numeral groups from marks carefully (ARM-S1: marks sit in runs of up to 19 glued to line starts/ends; superscript ticks above numerals on two lines); then align against ciphertext.txt's corresponding span and classify each of the 54 unmatched Founders groups: present in ms under a different reading / absent from ms (Founders error or a mark misread as digits) / ms illegible.
4. Update ciphertext_ms.txt to cover the whole letter (header states frames, passes, method, date); rerun the ARM-TR diff (subs/ins/del vs ciphertext.txt) and the units-digit table on the full letter. Never edit ciphertext.txt.
Append "ARM-TR2 manuscript completion" to HYPOTHESES.md and one paragraph to NOTES.md.
Files: ciphers/armstrong-madison-1808/{images/crops_0033/**,images/**manifest*,tr/**,ciphertext_ms.txt,HYPOTHESES.md,NOTES.md}, ROOM.md.
Done line (ROOM, starting `done: for LANE ARM: ARM-TR2`): 0033 groups and pass agreement; the 54-group span classified (counts per class); page2 crop issue cause; full-letter diff counts and units-digit table ms vs Founders; no cost figure.
