ZX-KEY349 (Opus, per CLAUDE.md Usage 1: key reading runs on the strongest model; wall-clock box 75 minutes, also a minimum working time; at most 2 subagents, Sonnet, only for blind cross-check reads of single key cells). Target: ciphers/clair349-este-guise-1556. Parent: LANE ZX orchestrator session_01MxueEQJUGF9PWJiYcVyvBM. Kind: recovery (key source `period`: BnF fr.20974 no.15, pages 57 and 69, the key Tomokiyo's guise.htm shows as an image). Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-zx-COMMON.md), NOTES.md sections "YX-TR349", "ZX-349" and "ZX-349B", key_alpha.tsv, key_nomen.tsv, images/atlas/atlas.tsv and images/atlas/xq_types.tsv. Intake gate: `partial`, exit 0 (orchestrator 15:44 UTC and ZX-349B 16:38 UTC, 25 Sept 2026). Images on disk: images/fr20974_p57_native.jpg, fr20974_p69_native.jpg (native Gallica resolution; if a cell is still illegible, fetch that region once at full size through the Gallica IIIF image API, canvases 30 and 37 of ark:/12148/btv1b9062131g, one request at a time >=1.5 s, at most 10, recorded in images/manifest.json).

Two blind passes against a 26-sign atlas agreed on 39.3% (gate 60). ZX-349B found the shortfall is mostly the key's own unfinished transcription: the L-Z alphabet, the Doubles/Nulles rows, the Monosillabes rows and the word lists are graded M or unresolved, and two recurring ciphertext shapes (to-ligature, 35; R-loop, 25) sit in those rows. Finish the key.
1. Read every cell of both tables (p57 alphabet, Doubles, Nulles, Monosillabes, word lists; p69 nomenclator) at native resolution, one crop per cell into images/atlas/key_cells/ (small crops only; keep images/ under 30 MB, pruning ZX-349B's xq_crops/sign_crops if needed, since their sheets remain). For each cell: value (letter, word or null), code (digit group or sign), grade H (legible and, for a sign, drawn unambiguously) or M, and a note. Where a cell is M, one Sonnet subagent reads that cell's crop blind (value hidden) and you record whether it agrees. Update key_alpha.tsv and key_nomen.tsv in place, same columns, and add a corrections table in NOTES.md (row, old, new, reason).
2. Extend images/atlas/atlas.tsv and sheet.jpg with every sign in the key not already coded (S27 onward), crops from the KEY image only, shape descriptions only, no meanings. Record in NOTES.md which ciphertext types from xq_types.tsv now have an atlas code.
3. Answer three questions with evidence: (a) are the separator_vertical_bar (157) and separator_z_curl (162) shapes in the key's Nulles row or anywhere else in the key? (b) is the Ao-ligature (39, about once a line) anywhere in the key; if not, is it a plaintext abbreviation (check three instances against the letter's own plain portions and the hand of the key)? (c) what share of the ciphertext's recurring sign types now has an atlas code (compare with dupuy452's 99.8 pct as ZX-349B did)?
4. Stop there: no transcription passes and no decode in this job. Write the next job's line: two fresh blind passes against the extended atlas (and, if (a) says the separators are nulls, instructions to code them as such).
NOTES.md gets one section "## ZX-KEY349 (25 Sept 2026)" with per-table H/M counts before and after. Status stays `partial`. Touch only ciphers/clair349-este-guise-1556/** and ROOM.md. Push after each step. Write "cost: see the lane ledger". Never call AskUserQuestion.
