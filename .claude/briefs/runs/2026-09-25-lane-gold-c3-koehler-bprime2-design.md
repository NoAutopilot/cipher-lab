JOB GOLD-B2D: Köhler family B'' design -- a general permuted tableau, first sub-family B''-c (a free permutation on the cipher side of a standard running-key square), as a `tools/families/` module with an offline test, a matched control and two noise bands. Fable (claude-fable-5-1): the model is the instrument here. Stop and push at $12 or 90 minutes, whichever first. NO subagents (one module, one control; nothing to fan out). Lane: LANE GOLD2 orchestrator session_013JXDgLDkW2y5Ldi2gWzTkY (Fable). Written by the cycle-2 consolidator GOLD-CONS2 (Fable, session_01SPixjGTp23T4YyxPvHwkWs), 25 Sept 2026 21:16 UTC; the orchestrator fills the gate line and the spawn time.

INTAKE_GATE_OUTPUT: (orchestrator pastes `python3 tools/intake_gate_check.py koehler-1944` here, with the clock time)

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), the whole top block ("Summary, cycle 2") of `ciphers/koehler-1944/HYPOTHESES.md` and its sections "Family B, running key, detail (GOLD-2A)" (the decoder: `running_key.LM`, `beam_decode`, `mixed_tabula` dict tabulae accepted by `key_of` and `encipher`, the control construction, the memory limits) and "Family B', keyed-tableau running key (GOLD-2C)" (step 1's statistic and the stage-1 identifiability numbers), `tools/families/__init__.py` (the family interface: `make_control`, `solve`, `score_recovery`, `draw_window`), `tools/families/keyed_running_key.py` as the template, `tools/tests/test_keyed_running_key.py`, `python3 tools/family_run.py --help`, and the sibling brief `2026-09-25-lane-gold-c3-koehler-bprime-cipher-side.md` (GOLD-K3 may be adding a `cipher` mode to `mixed_tabula` at the same time: if it has pushed, reuse it; if not, do NOT add it yourself -- build your tabula from an explicit permutation list, see step 1). Claim in ROOM (`tools/room.py`). Read the clock with `date -u` before any date.

What cycle 2 learned, pasted:
- The target's letter statistics are inside the keyed running-key band on 6/6 statistics and outside the uniform one-time-key band on 5/6: a non-uniform key stream through at least one unknown alphabet permutation. The standard tableau (Bourdeau 4, GOLD-2A at two key registers) and the keyword-mixed tableau in modes plain/key/both/full (GOLD-2C, K1, K2: vig, beau, Dutch novel, Dutch Bible, German key) are closed with controls at 67-90 pct and targets at noise. Stage 1's sorted-profile statistic cannot rank general permutations: the target sits at the keyed band's MEDIAN (percentile 0.41-0.52), i.e. a random permutation pair reproduces its profile. So the objective that discriminates is the two-stream decoder: in every control it put the true tableau 0.35-0.5 nats per letter above its near-misses, and controls' pooled scores (-3.01 to -3.18) sit 0.35-0.5 above the noise band (-3.507 / -3.544 at order 6, beam 300; -3.57 to -3.64 at order 8, beam 1000).
- Decoder cost: about 1-3 minutes for stage 2 on 30 candidates on message 1 at order 6 / beam 300, i.e. 2-6 seconds per candidate decode of 237 letters. An anneal needs thousands of evaluations, so the anneal objective must be cheaper (see step 2) and the winner re-scored at the standard settings. Memory: at most three decoder processes at order 6; log to your scratchpad, copy in after exit; never `room.py --push` with a live log in the tree.
- Symmetry to respect in the general case: (S1 + t, S2 - t) gives the same ciphertext for every shift t, and under vig with same-language models the two streams swap; B''-c (S1 = S2 = identity) has neither problem, which is why it goes first.

Deliverable: `tools/families/permuted_tableau.py` registered in `tools/family_run.py` (one line in its families list and help), an offline test `tools/tests/test_permuted_tableau.py` that runs in under 30 s, the control on 3 seeds through `family_run.py`, both noise bands, and the target run only if gated and the box allows.

1. Model and tabula. c = S3(S1(p) + S2(k)) mod 26 (`--param arith=vig|beau`); B''-c fixes S1 = S2 = identity and searches S3, a permutation of 26 letters. Write `perm_tabula(S1, S2, S3, arith)` returning the same dict shape `mixed_tabula` returns (`enc`, `kof`, `name`, `alphabet`) so `running_key.key_of`, `encipher` and `beam_decode` accept it unchanged. Equivalent view for the reader: c' = S3^-1(c) is a standard-tableau running-key ciphertext.
2. Objective. The pooled two-stream joint log-likelihood per letter of `running_key.beam_decode` under LM_p = de20, LM_k = nl20 (params `corpus`, `kcorpus` as in keyed_running_key), evaluated at ANNEAL settings you choose by measuring cost: aim at or under 2 s per evaluation (order 5, beam 60-100, spaces on, the first 120-160 letters of message 1; state the settings and the measured seconds per evaluation in the docstring). The chain's winner is re-scored at GOLD-2C's settings (order 6, beam 300, all five messages) so its number is comparable to the -3.507 / -3.544 band and to K3's ten-text beau band. Optional and second: a cheap proxy (the bigram counts of c' = S3^-1(c) against the convolution of the de20 and nl20 bigram tables) used only as a proposal filter, and only if it raises control recovery when tested on the control; if you try it, report the control number with and without it.
3. Search. Simulated annealing over S3: moves are a swap of two letters (80 pct) or a 3-cycle (20 pct); start from the sort-match permutation (sorted ciphertext counts matched to the sorted predicted conv(de20 unigram, nl20 unigram) distribution, GOLD-2C step 1's statistic) for chain 1 and from uniform-random permutations for chains 2-3; temperature schedule set from the measured spread of objective deltas on the control (say so); 1500-3000 evaluations per chain, a chain stops after 400 consecutive non-improving proposals; the best of three chains is the run's S3. Side number on the control: letters of S3 placed correctly (of 26) by the sort-match start alone and by the anneal.
4. Control (rule 3), built in `make_control`: per seed, German plaintext windows at 237 / 178 / 140 / 140 / 229 from one de20 book, key windows from one held-out nl20 book, S3 drawn uniformly at random from the 26! permutations (S1 = S2 = identity), both books out of the models (the keyed_running_key template does the hold-out). The control's number is the pooled share of plaintext letters recovered over the five messages after decoding under the annealed S3 at the standard settings, exactly as B' scores. Gate 0.5 mean on seeds 1-3. If the control reads under 0.5, fix the tool (more evaluations, the proxy filter, a better start, a cheaper objective that lets more evaluations fit), never the target; if it still reads under 0.5 at the box, the family_run row says CONTROL BELOW GATE and your section names, from the control's confusions, which letters the anneal cannot place (expect those of near-equal count) and what a 26!-search would need.
5. Noise bands, both, after the control clears: (a) the false-positive floor with the target's own counts, `family_run.py ... --shuffle-target S` for seeds 1-3 (stage 1's sort-match start is identical for target and shuffle; the anneal on order is the only difference); (b) three uniform-random texts of the five lengths through the same anneal (a short script as GOLD-2C's `keyed_pipeline_noise_*.out`). A target pooled score is a ROOM `flag:` line only if it exceeds the maximum of both bands by 0.1 nats without a judge PASS; a reading exists only on `tools/judge_plaintext.py specs/koehler-1944.json --file <decode>` PASS (paste it, "for LANE GOLD: judge PASS, re-derivation owed", stop).
6. Target: `python3 tools/family_run.py specs/koehler-1944.json --family permuted_tableau --corpus tools/data/de20 --param kcorpus=tools/data/nl20 --param arith=vig --seeds 3 --gate 0.5 --label "GOLD-B2D B''-c cipher-side permutation anneal, control before target"` runs the control and then the target by itself. Run it only when the control has cleared on 3 seeds AND at least 25 minutes of the box remain; otherwise write the exact command, the measured minutes per seed and the two bands' commands into your section as "run owed to a Sonnet box (cap $8, 60 minutes)", which the next consolidator briefs. Beau arithmetic: only if vig has run and time remains; otherwise owed.
7. The general case (S1, S2 free), design paragraph only, no code: from the control's S3-letters-correct number, say whether a three-permutation anneal has a chance at 924 letters (the objective is the decoder alone; 26!^2/26 more states; the stream swap under vig), and recommend continue / park with that number. Then append "Family B'', permuted tableau (GOLD-B2D)" under the append-only part of `ciphers/koehler-1944/HYPOTHESES.md`: method, settings, seconds per evaluation, control table (seed, letters of S3 correct at start and at the end, pooled recovery, pooled score), both bands, the target row if run, dead ends, and the exact commands to reproduce; one line in `specs/koehler-1944.json` `cheap_test_done` and in NOTES.md "Next steps".

Stopping rule for the job: the module, its test, the control on 3 seeds and the two bands are the deliverable; a target run is a bonus, not a debt. Push the module and test on their own commit as soon as the test passes, before the control runs, so a stopped box leaves a usable tool.
Hosts: none (disk and CPU only). Requests per host in the done line: 0. Subagents: none.
Done line (ROOM, starting `done: for LANE GOLD: GOLD-B2D`): seconds per evaluation and settings; control a/b/c pct and S3-letters-correct per seed (gate 0.5 met/not); shuffle band and uniform band (min/max); target pooled score and judge line if run, else "target owed, command in HYPOTHESES"; the general-case recommendation in six words; files; no cost figure; no reading described unless the judge passed. Rule 10: never solved, new, first, unpublished; NOTES.md status stays `open`.
