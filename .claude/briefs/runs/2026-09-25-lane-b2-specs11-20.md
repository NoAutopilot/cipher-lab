LANE B2 spec writer bSPEC: ten specs from UNSOLVED-SURVEY.md ranks 11-20. Sonnet (claude-sonnet-5). Cap $6 or 45 minutes, whichever first.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b2-common.md` (read it first; it governs) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Job: write `specs/<slug>.json` for survey ranks 11-20 (Copenhagen 1835-painting note; Pollaky ads; Catokwacopa 1875 -- already `ciphers/catokwacopa-1875/`, partial, spec it from that folder and say so; T. E. Wood / Thouless 1950; Cylob c.1995; Scorpion S1/S5 1991; McCormick 1999; Rubin 1953; MLH c.1976; Bullet cryptogram Tuscany 1944). Model on the existing specs (read specs/README.md and two of the survey's specs, e.g. specs/lima-1916.json and specs/somerton-1948.json): slug, rank, schmeh_top50, name, date, language_candidates, ciphertext (as transcribed, with ciphertext_source and date; or `ciphertext_pending` naming exactly where it is and what one fetch would get it), alphabet, constraints, cheap_tests_in_order (three, cheapest first, each with its matched control and an est. cost; the first must be runnable by a $3 Sonnet worker in 30 minutes), matched_control, judge block (language, letters_min/max, min_word_cover, cribs if any, control_samples), value, model, status line dated 25 Sept 2026, schema_note, written. Sources: the survey row, `sources/schmeh/posts/NN-*.txt` already on disk (read the .txt, not the .html), LANDSCAPE.md, LESSONS.md. No network except, only where the ciphertext is not on disk, nothing: write ciphertext_pending instead. Do NOT run any test. Each file must load with `python3 -c "import json;json.load(open(...))"` and `python3 tools/judge_plaintext.py specs/<slug>.json --help`-level sanity (run `tools/judge_plaintext.py` on a two-line dummy file for each spec and confirm it reads the judge block without error; paste nothing into the specs). Add a "Spec" pointer to each row in UNSOLVED-SURVEY.md's "Working on it" column only by appending ` spec: specs/<slug>.json` (fetch+rebase right before; keep others' edits). Rule 10 wording throughout: a proposed test is not a claim. Push after every three specs. Done line: the ten slugs and which have ciphertext on disk vs pending.
