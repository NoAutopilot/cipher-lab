ROLE: DETECTOR, HathiTrust-only printed cipher (LANE N). Read .claude/briefs/detector-test.md, sources/ia-fulltext/NOTES.md (rounds 1-4 on archive.org: method, thresholds, controls, pre-filters, the interlinear-decipherment lesson), tools/htrc_ef_headwords.py (how the Bibliographic API and the HTRC Extracted Features API are called), and QUEUE.md's four "Printed ciphertext" sections (P, Q, X and round-4 rows: everything already judged is excluded).
Goal: editions that print ciphertext and that archive.org lacks, found through HathiTrust's Extracted Features (per-page token counts, which include numeral tokens). Build sources/htrc/editions-ht1.tsv: 150-250 HathiTrust volume ids of 1830-1928 diplomatic editions and calendars NOT on archive.org (check each with one IA advancedsearch call? NO -- you do not hold the IA slot; instead use the identifier lists already in sources/ia-fulltext/editions*.tsv and titles in QUEUE.md to exclude what rounds 1-4 covered). Candidate series: Négociations/Correspondance diplomatique volumes (Documents inédits), Nuntiaturberichte aus Deutschland (all Abteilungen), Documenti di storia italiana, Calendar of State Papers Venetian/Spanish/Milan/Rome volumes, Colección de documentos inéditos (CODOIN), Archives de la maison d'Orange-Nassau, Correspondance de Granvelle, Recueil des instructions, Lettres de Catherine de Médicis, Acta Tomiciana, Monumenta Poloniae Vaticana, Scottish History Society and Camden volumes with ciphered letters. Get ids from the Bibliographic API by OCLC number (Open Library search API gives OCLC numbers; catalog.hathitrust.org/api/volumes/brief/oclc/N.json with a full Chrome User-Agent string).
Detector: for each volume, fetch EF pages once (data.htrc.illinois.edu/ef-api/volumes/HTID/pages?pos=false, cache under your scratchpad, not the repo) and score every page for numeral tokens (count and distinct numeral types of 1-4 digits, excluding years 1400-1930 and the page's running numbers) against prose tokens; flag pages where numeral tokens >= 25, distinct numerals >= 12 and a repeat rate >= 0.3, then drop the last 8% of a volume (indexes) and runs of ascending numerals (tables of contents). Write the detector as tools/htrc_numeral_pages.py with --help and an offline test in tools/tests/ (fixture: a small saved EF JSON).
CONTROLS FIRST (report both numbers): Thurloe, A collection of the state papers, vol. 1 (a HathiTrust copy) and Rommel 1840 Correspondance inédite de Henri IV avec Maurice le Savant (a HathiTrust copy) must flag their known cipher pages; and a plain-prose control volume of the same era must flag nothing or near nothing. If a positive control fails, tune once and report; if it still fails, stop and report.
Judging: EF has no word order, so a flagged page is only a lead. For each flagged cluster record the page sequence and the top numeral tokens and whether prose words like 'deciphered', 'déchiffré', 'dechiffrement', 'cifra', 'descifrado', 'entziffert', 'chiffre', 'cipher' occur on the same or the neighbouring pages (a probable printed decipherment). Grep sources/cryptiana/, the solver repo clones, QUEUE.md and ciphers/ for the edition and any identifiable letter. Output: sources/htrc/NOTES.md (method, controls, per-series counts, precision of a 30-page hand sample judged from the EF token lists), and a NEW QUEUE.md section "Printed ciphertext, HathiTrust detector (LANE N, <date>)" with rows HT1, HT2, ... (prefix reserved for you; at most 20; edition, htid, page seq, tokens, decipherment-word nearby yes/no, next step = page image check by a person or a later worker).
Hosts: data.htrc.illinois.edu and catalog.hathitrust.org API (yours alone in LANE N), openlibrary.org search API (>=1.5 s). No babel.hathitrust.org page images (Cloudflare), no archive.org, no Google Books. At most ~600 HTRC requests, >=1.5 s apart. Cap: $10.
COMMON RULES (LANE N worker; parent LANE N orchestrator session_01W4z8JhXJYHRjorPC1Qkpiy; lane goal: stage-2 nominations, at least half copy-free):
- First: `date -u` (use that clock for every timestamp; never estimate it); `python3 tools/room.py --start`. Read CLAUDE.md in full (rules 1, 3, 5, 10; the good-citizen rule; the Access playbook), LESSONS.md sections 1-2, .claude/briefs/check-solved.md (the Thurloe, Raince, Bowes and M9 lessons), and the last 30 lines of ROOM.md. Claim with `python3 tools/room.py "LANE N <role> (Sonnet, <your session id if known>)" "claim: <rows/target> -- files: <paths>"` BEFORE other work. Done line the same way when you stop; `flag:` for anything the lane orchestrator must see.
- Git: commit by explicit path only (never `git add -A`); push with `python3 tools/room.py --push <paths>` (rebase-and-retry loop, keeps both sides of ROOM conflicts). ROOM.md is append-only; on a conflict in QUEUE.md keep both sides. Never rewrite history, never a pull request.
- Hosts (good-citizen rule): one request at a time per host, >=1.5 s apart; descriptive User-Agent `cipher-lab research script (contact via repository)` unless the playbook says a site needs a browser UA; on 429/403/Cloudflare/challenge stop that host, log it in NOTES.md and ROOM.md, one retry after a pause at most. Use ONLY the hosts your brief names plus: WebSearch/WebFetch, github.com (shallow clones of dbourdeau/cyphersolver and aaymeloglu/unsolved-ciphers, grep only), and the files already in the repo (sources/cryptiana/, sources/solver-diffs/). NEVER gallica.bnf.fr or archivesetmanuscrits.bnf.fr (LANE G), never Google Books (LANE V). archive.org: only if your brief gives you the IA slot. DECODE (de-crypt.org): only the DECODE worker logs in; everyone else uses sources/decode/ files and the solver repos' DECODE sweeps. Report your request count per host.
- Quality over quantity. A nomination later found solved costs more than none. Before writing "open": (1) the recipient's and sender's standard printed edition and the calendar for the date (Raince lesson); (2) a printed or interlinear decipherment in the same edition or the catalogue note (Thurloe lesson); (3) quote verbatim any sentence in Tomokiyo, a solver README/NOTES or an edition that names this very letter (M9 lesson).
- Rule 10: never write new, unpublished, unread, first, never printed, previously unknown. Report what was found and where it was not found. Do not classify novelty. Never promote to the board, never solve or transcribe beyond confirming ciphertext is present, never start a target your brief does not name. No copy orders or payments: a target that needs a copy gets ciphers/<slug>/REQUEST.md (no personal data).
- Credentials: never print them; no unfiltered env/printenv/set, no curl -v / --trace / set -x on a credentialed call; test presence with `test -n`.
- Usage: Sonnet; cap stated in your brief (check your own cost; stop at the cap even if unfinished, and push what you have). At most 4 subagents at once, on Sonnet. Scripts read, models judge: fetch once to disk, grep, give the model the hits.
- Finish: commit, push; ROOM `done` line that ends with `nominations: <n> (<k> copy-free)` when your brief is a check-solved; then your final reply is ONE paragraph, first line the answer (per-row verdicts or new row ids), then files touched, request counts per host, cost. Then stop.
