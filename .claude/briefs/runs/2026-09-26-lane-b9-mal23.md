JOB bMAL23 (LANE B9, Sonnet, cap USD 10, wall-clock box 70 min; hosts: none -- images on disk): malsburg-hessen-1636 record 505, leaf f.23 (hstam_4_h_1411_0023.jpg), full cipher transcription, two blind passes, reconciled, then the per-leaf pool gate.
Intake gate: run and paste `python3 tools/intake_gate_check.py malsburg-hessen-1636` (orchestrator 08:17 UTC: exit 0, partial). Nonzero = stop.
Read first: common file `.claude/briefs/runs/2026-09-26-lane-b9-common.md` (it governs); NOTES.md sections bMAL3, bMALC (leaf extent: f.23 ~8 clear lines then ~44 cipher lines), and "Glyph conventions (bMALG)" -- its convention paragraph goes verbatim into every pass subagent's prompt, and its glyph_map.tsv is passed to reconcile with `--sign-map`.
Units and pricing (Usage 6, per pass): ~44 cipher lines = 5 blocks of <= 10 lines x 2 blind passes = 10 Sonnet (claude-sonnet-5) subagent calls at about USD 0.9 each, plus settling. Stop before starting a call that would cross 80% of cap or box; push after every block. Transcribe the clear lines too as plain text in a separate file (clear_0023.txt, one pass, you read them) -- they are the letter's crib context.
Steps: (1) crops: `python3 tools/iiif_lines.py --image ciphers/malsburg-hessen-1636/images/hstam_4_h_1411_0023.jpg --out ciphers/malsburg-hessen-1636/crops/0023 --debug` (paste the output; check the overlay). A pass call gets only its block's crop paths, never a full leaf. (2) passes A and B per block, blind (pass B never sees A), TSV per bMAL2's line/pos/sign format, committed per block. (3) `python3 tools/reconcile_passes.py ... --sign-map ciphers/malsburg-hessen-1636/glyph_map.tsv --crops crops/0023 --out-dir recon_0023`. (4) Settle disagreements.tsv rows on the crop as small zooms of the one group (at most 50; beyond that leave M). (5) Gate: `python3 tools/leaf_pool_gate.py --leaf ciphers/malsburg-hessen-1636/recon_0023/ciphertext_draft.tsv --pool ciphers/malsburg-hessen-1636/ciphertext.txt --json recon_0023/gate.json` (pre-registered by the orchestrator at 08:19 UTC: M <= 0.15, off-form <= 0.05, frequency cosine > relabel p95; the orchestrator admits, not you).
Output: NOTES.md section "Record 505 f.23 (bMAL23)": lines, signs, pass agreement before/after settling, grade counts, the gate line pasted. ROOM done line <= 600 chars: N signs, H/M, agreement, gate line. Final reply at most five lines. Do not edit NEAR.md or ciphertext.txt.
