PARENT WORKER TOOLS-GATE (Sonnet, wall-clock box 40 minutes). Two fixes to tools/intake_gate_check.py named in the LANE CX handoff (STATUS.md): (1) a `found-solved` verdict with a citation nearby exits 0 with the label found-solved; without a citation, exit 1; (2) an `open` verdict whose citation window says the edition was NOT read ('unread', 'not read', 'could not open', 'paywalled') exits 1, not 0 (fr4687-paleologue-nevers before its correction is the example). Extend tools/tests/test_intake_gate_check.py with cases for both and keep every existing case passing (antt-linhares-chave blocked; clair349-este-guise-1556 partial passes). Run the tool over every ciphers/*/NOTES.md before and after; paste the count of exit-0 targets both times in your done line and list any target whose result changed. Report to the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n) in ROOM.md. Touch only tools/intake_gate_check.py, its test, and ROOM.md. ROOM lines with tools/room.py or single quotes. Never call AskUserQuestion.
