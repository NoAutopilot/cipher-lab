OX-PAGS solver, one cryptanalysis attempt (Sonnet, cap $8 as a stall alarm, at most 2 subagents). Parent: LANE OX orchestrator, session_01BE3g8tWbS4T24KXMpShHt4. COMMON: .claude/briefs/runs/2026-09-25-lane-ox-COMMON.md applies in full; .claude/briefs/solver.md; CLAUDE.md rules 3, 4, 7. Never call AskUserQuestion.
Target: ciphers/clairambault1225-paget-1714 (Paget, Genoa, 8 Apr and 28 Aug 1714; French; ciphertext.tsv 2340 tokens, 500 cipher tokens, 122 distinct codes 2-692, 38 shared between the letters; contemporary interlinear glosses over some runs in letter 1, e.g. 145.31.67 = l'abbé [Lomeliny], 192.46.87.147.46.146 = de l'Isle). Read OX-PAG's and OX-PAGT's NOTES.md sections first.
Job: recover as much of the nomenclator as the glosses and the clear French context fix, and test whether the remaining codes yield to a crib-anchored attack -- one attempt.
1. Cribs: list every gloss (code run -> plaintext, grade C) and derive what each implies (syllable vs letter codes: e.g. does 145.31.67 = l'-ab-bé or l-a-b-b-e? use run lengths across all glosses). State the system hypothesis with the evidence.
2. key.tsv from the glosses (C) and from forced context (a code whose every occurrence sits in a clear-French slot that admits one word; grade S only with the control below, else M). decode.json + `tools/decode_key.py --check`; reading.txt with unknown codes as [code].
3. Matched control (rule 3, match the DESIGN): build a synthetic French nomenclator cipher of the same shape -- same number of cipher tokens (500) and distinct codes (~122), the same mix of syllable/letter/name codes your hypothesis gives, embedded in clear French at the same density, with the same number of glossed runs revealed -- from a French text of the period (tools/data if it has one; else a public-domain early-18th-c. French letter text), and run the identical procedure on it. Report the control's recovered-code share and token accuracy beside the target's.
4. If a judge spec exists (specs/clairambault1225-paget-1714.json) run tools/judge_plaintext.py on reading.txt and paste the output; if none, say so; do not write a spec.
5. Fresh-instance re-derivation of any C/S code by a subagent that sees only ciphertext.tsv, the gloss list and your one-paragraph system description.
6. NOTES.md "Cryptanalysis attempt 1": numbers side by side (target vs control), codes keyed by grade, what stays unread. A failure is a result with its control numbers, not a stop to retry: no second attempt in this session.
Report what was found and where it was not found; no novelty wording. No host fetches (images on disk). Files: ciphers/clairambault1225-paget-1714/**, ROOM.md.
