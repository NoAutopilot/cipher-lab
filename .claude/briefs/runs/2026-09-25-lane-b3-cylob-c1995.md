LANE B3 breadth worker bCYL (brief written by LANE B2, run by LANE B3): cylob-c1995. Sonnet (claude-sonnet-5). Cap $3 or 30 minutes, whichever first. No subagents.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (read it first; it governs) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Spec: specs/cylob-c1995.json. Run `cheap_tests_in_order[0]` only: fetch Cipherbrain post 50 (the URL in the spec; scienceblogs.de only, at most 4 requests, >=1.5 s apart) to sources/schmeh/posts/50-cylob.html and a text version 50-cylob.txt via tools/html2text.py (sources/ are unmodified snapshots: save, never edit), then update the spec's ciphertext/alphabet/constraints from it (ciphertext as transcribed there with source and date, or a precise ciphertext_pending naming each image URL if only scans are given). If the post's images are the 20 pages of grids, fetch none of them (a later job); record their URLs. Write cheap_test_done with what was fetched and what the spec now says; no control applies to a fetch, say so. Push and stop.
