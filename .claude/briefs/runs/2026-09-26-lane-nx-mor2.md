LANE NX job NX-MOR2 (Sonnet). Parent: LANE NX orchestrator, session_01RDXRYmATas1Xy6K1Jjwtvo. COMMON rules: .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md, read it and follow it, with "LANE NX" for "LANE AX" and lane goal "copy-free candidates with a key route, one control-backed cheap test each".
Target: ciphers/rah-morillo-1817 item 3 only (RAH 9/7666, record 5186, f.420r, Herrera to Morillo, Guanare 7 Nov 1820). NX-MOR (08:57) found about 21 numeral cipher groups on f.420r WITH an interlinear period decipherment under each group (read its "NX-MOR" section in NOTES.md; crop images/crop_5186_p1_cipher.jpg, full page images/10088713.jpg).
Box: 60 minutes from your first date -u; stall alarm USD 6 (2 transcription calls on one crop + edition search). Files: ciphers/rah-morillo-1817/**, specs/rah-morillo-1817.json (new), ROOM.md.
Steps:
1. Intake (the item-3 verdict was "open pending t.4"): Rodriguez Villa, El teniente general don Pablo Morillo, t.4 (documents after 1818). Look for it once more with the keyed Google Books API (key + country=US, CLAUDE.md), the HathiTrust bibliographic API and HTRC Extracted Features (tools/htrc_ef_headwords.py for "Romerito", "Guanare", "Herrera"), and archive.org advancedsearch. If t.4 prints this letter with its deciphered passage, stop: write found-solved for item 3 with the page. Otherwise log every route tried, and write the item-3 line in NOTES.md naming what was read. Then run python3 tools/intake_gate_check.py rah-morillo-1817 and paste the output (fix NOTES line 2 if the tool needs the citation there -- line 1 must stay the single word partial).
2. Transcribe the cipher block: two blind passes (Sonnet subagents, each given only the crop path; if the crop is over 2500 px wide cut it into two halves first) recording per group: the cipher values in order, and the interlinear word under it. Compare by content alignment; reconcile from the image.
3. Key: build key_5186.tsv (value -> letter, count, the words it was seen in), grade H (period decipherment on the leaf). Consistency control, gate written before: every value that recurs must map to one letter; report conflicts. Shuffle control (rule 3, the Szembek lesson): permute the gloss words across groups 20 times and compute the same consistency score; report real vs shuffle mean and p95 -- if the shuffle equals the real score the control is non-discriminating, say so.
4. Decode: decode.json + python3 tools/decode_key.py ciphers/rah-morillo-1817 --check (the reading of the cipher block from the key must reproduce the interlinear words; list any group where the period decipherer's word disagrees with the key, as a data point, not a correction). Write specs/rah-morillo-1817.json (judge block, language es; say which corpus in tools/data you used, es17/es17c are 17th-century -- the letter is 1820: say the corpus is not era-matched, per rule 3) and paste the judge output.
5. Also note in one line whether this numeric letter-value system is compatible with item 2's description ("alterando la numeracion que da el valor a las letras").
Rule 10: no novelty words. Do not classify novelty. Stop; the orchestrator sends a fresh re-derivation.
Finish per COMMON.
