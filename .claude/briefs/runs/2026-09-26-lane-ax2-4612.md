LANE AX2 AX2-4612 -- lodewijk-van-nassau-1573-74: WVO 4612 (Lodewijk to Orange, Meer, 6 March 1574): settle the 16 open numerals, decode under key_full v3 with controls, then a key-seeded anneal if the straight decode is not legible (Sonnet, wall-clock box 90 minutes). Parent: LANE AX2 orchestrator session_016sbTkVT8HGNJ6JGjJNmCuM. Read and follow .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md (read "LANE AX2" for "LANE AX") and the common tail of .claude/briefs/README.md (both override anything below they conflict with), CLAUDE.md rules 3, 4, 7, 10 and Usage 6/8. First: `date -u`; `python3 tools/room.py --start`; last 30 lines of ROOM.md. Claim with tools/room.py before other work; never AskUserQuestion; no dollar figures for yourself.

Read: NOTES.md sections AX-4612, AX-4612TR, AX-4612TR2 and AX-MERGE3; ax4612tr/{settle.tsv,build_v2.py,word_share_check.py}; specs/lodewijk-4612.json; key_full.tsv; axnames/build_key_full.py (--check).

Why this job: under key.tsv, v2 decodes to runs such as "yusarbesxs", "zurdnt", "yusest", "yusen": recurring clusters ("yus" where "vous" would sit, "zur" where "pour" would sit) that look like a small, consistent set of codes differing from the 1574 table (or a consistent misreading of a few digit shapes), not noise. v2's French-word share is 68.6% against a 20-shuffle mean of 41.5% and max of 61.3%. The earlier block/homophonic negatives (AX-4612) ran on the 47.8%-agreement transcription and are not tests of the letter; write one line in NOTES.md saying so.

Units, in order (stop before a unit that would cross 80% of the box; push after each):
1. Settle the 16 `?` numerals in ax4612tr/settle.tsv (about 20 min). Crop step, mandatory, run and paste its command line into NOTES.md before looking: `python3 tools/iiif_lines.py --image images_wv2/crops_4612/src_04612_p1.png --region x,y,w,h --out <scratchpad dir> --debug` for each needed region (find the region by matching the row's neighbouring tokens against the existing p1_L*_s* crops; crops under 2500 px). Read the crops yourself; if you use a subagent, give it crop paths only, never src_04612_p*.png. Where a digit stays ambiguous, keep both readings in `alt` at grade M. Write ciphertext_4612_v3.tsv (same columns as v2) with a build script beside build_v2.py.
2. Decode v3 under key_full.tsv v3 with a new decode_4612_v3.json through `tools/decode_key.py ciphers/lodewijk-van-nassau-1573-74 --check` (reading_4612_v3.txt). Gate, written into NOTES.md BEFORE the numbers are computed: "4612 v3 reads under key_full if its French-word share (word_share_check.py method, fr16) is above the max of 20 value-shuffles of key_full AND at least 0.85 of the share the same statistic gives 5811's own ciphertext (known reading) cut to 4612's N." Also run tools/judge_plaintext.py specs/lodewijk-4612.json --file <plain letters of v3> and on the 5811 cut (positive control); paste both, with the per-fold caveat (fr16 FAILed key.tsv's true 5811/4610 readings at N=775: blended FN 25.7%, folds 7.5/67.5/2.0). Report both numbers side by side.
3. If unit 2's gate fails (or passes but the text has non-French clusters like "yus"/"zur"): a key-seeded anneal. Add `--init KEY.tsv` to tools/homophonic_anneal.py (start from that sign->letter map instead of random; default behaviour unchanged) with an offline test in tools/tests/. Matched control FIRST: 5811's real ciphertext cut to 4612's N, started from key_full with a random 20% of codes 1-120 reassigned (3 seeds); gate written before the run: control recovers >= 0.90 of 5811's key_full token reading. Only if the control passes: run 4612 v3 from key_full, 3 seeds, plus one `--shuffle`-order target control (token order shuffled, same start). Report: codes the anneal changes (value, key_full letter, new letter, count), whether the changed codes pair up as digit-shape confusions (1/7, 3/8, 5/6, 4/9, 2/7) -- which would point to transcription, not key -- the decoded text, judge line, and solver scores target vs shuffled.
No reading is claimed by you: if unit 2 or 3 produces legible French, grade per token (rule 4), commit reading_4612_v3*.txt via decode_key.py (a changed key goes in a new key_4612.tsv, never into key_full.tsv or key.tsv), and write ROOM "flag: 4612 candidate ready for re-derivation" -- the orchestrator briefs a fresh instance; do NOT write a "for LANE V8" line.
Section "## AX2-4612: 4612 v3, key_full decode and key-seeded anneal (26 Sept 2026, LANE AX2)". ROOM done line: "for LANE AX2: 4612 v3 <n> numerals settled; key_full word share <x> vs shuffle max <y> vs 5811 control <z>; judge <line>; anneal control <c>, target <changed codes>; verdict <reads / candidate / not a test / negative with numbers>".
Files: ciphers/lodewijk-van-nassau-1573-74/{ciphertext_4612_v3.tsv,decode_4612_v3.json,reading_4612_v3*.txt,reading_4612_v3*_tokens.tsv,key_4612.tsv,ax4612tr/**,ax2_4612/**,HYPOTHESES.md (append),NOTES.md own section}, specs/lodewijk-4612.json (cheap_test_done append only), tools/homophonic_anneal.py (--init only), tools/tests/test_homophonic_anneal_init.py. Do not touch images_wv2/crops_4612 contents (AX2-SHRINK leaves them alone for you); new crops go to your scratchpad, and only the ones you cite are committed under ax2_4612/crops/ (JPEG, under 2 MB total). No network.
