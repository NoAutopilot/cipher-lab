LANE AX2 AX2-4612S2 -- lodewijk-van-nassau-1573-74: re-run tools/key_repair.py with a length-neutral objective (Sonnet, wall-clock box 60 minutes). Parent: LANE AX2 orchestrator session_016sbTkVT8HGNJ6JGjJNmCuM. Read and follow .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md (read "LANE AX2" for "LANE AX") and the common tail of .claude/briefs/README.md, CLAUDE.md rules 3, 4, 7, 10 and Usage 8. First: `date -u`; `python3 tools/room.py --start`; last 30 lines of ROOM.md. Claim with tools/room.py before other work; never AskUserQuestion; no dollar figures for yourself.

Read: NOTES.md sections AX2-4612 and AX2-4612S in full; .claude/briefs/runs/2026-09-26-lane-ax2-4612s.md (the predecessor brief, whose hypothesis H-S and units 2-3 you repeat); tools/key_repair.py and its test; ax2_4612s/**.
What went wrong (orchestrator's error, ledgered): the objective was the TOTAL log-probability of the decoded stream, so a code set to NULL (zero characters) always wins, and multi-letter values always lose. Null control: 100 of 110 codes changed.
Fix, in the tool (default behaviour switchable, test updated):
- `--objective excess` (make it the default): score = sum over decoded characters of (log p(c | context) - mu), where mu is the fr16 order-5 model's mean log-probability per character on held-out fr16 text (compute once, print it). A candidate value now gains only if its characters are better than average French in context, so neither deletion nor lengthening is rewarded by construction. Keep `--objective total` for reference.
- `--no-null-below N` (default 121): codes below N may not take NULL (the 1574 table's letter range; AX-NAMES found the nulls at 121-149).
- Add a unit test that a null control on a clean synthetic French text proposes 0 changes.
Units (stop before one that would cross 80% of the box; push after each):
1. Tool fix + tests (about 15 min).
2. Controls FIRST (about 20 min), same design as AX2-4612S (null control on 5811 cut to 833; known-answer SWAP control, 3 seeds, k=8), gates copied verbatim into NOTES.md before running: ">= 6 of 8 altered codes recovered on average AND <= 2 false changes in the null control and on untouched codes". Also run the known-answer control with 2 of the 8 altered codes given a French bigram value (the H-S shape) and report those two separately. A control below gate = CONTROL BELOW GATE; stop and say so; if it fails, report which codes the null control changes and to what (so the orchestrator can see the next bias).
3. Targets (about 15 min, only if unit 2 passed): 4612 v3 and 5799, exactly as AX2-4612S unit 3 (changes table, word share vs 20 shuffles vs 5811 cut, judge beside the 5811-cut control, order-shuffled target control; key_4612_repair.tsv / key_5799_repair.tsv; readings via tools/decode_key.py --check). If a target reaches the 0.85x-of-5811 word-share gate or reads as French prose, write ROOM "flag: <letter> repaired-key candidate ready for re-derivation"; do NOT write a "for LANE V8" line.
Section "## AX2-4612S2: key repair, length-neutral objective (26 Sept 2026, LANE AX2)". ROOM done line: "for LANE AX2: mu <x>; null control <n> false; known-answer <x/8, bigram codes y/2, false z> (gate met/not); 4612 <..>; 5799 <..>; verdict".
Files: tools/key_repair.py, tools/tests/test_key_repair.py; ciphers/lodewijk-van-nassau-1573-74/{ax2_4612s/**,key_4612_repair.tsv,key_5799_repair.tsv,decode_*_repair.json,reading_*_repair*.txt,reading_*_repair*_tokens.tsv,HYPOTHESES.md (append),NOTES.md own section}. No network, no images.
