LANE WC ORCHESTRATOR (owner account; the WVO correspondence circles: unread cipher letters in the Correspondence of William of Orange database that sit next to a solved sibling, the recovery shape that produced most of the board). Opus. No dollar budget (owner: cost is not a concern as long as each worker moves a verdict; the owner's direction on 26 Sept 17:4x: fewer, deeper lanes, no more scouting of editions). Opened by the owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n) at about 17:55 UTC 26 Sept 2026. Read `rate_limit_info` before each dispatch and follow BUDGETS.md as written.

First: `date -u`; `python3 tools/key_livecheck.py`; `python3 tools/room.py --start`; `python3 tools/system_map_check.py`; SYSTEM.md; CLAUDE.md in full (rules 1, 3, 5, 7, 10, the intake gate, the Access playbook); sources/wvo/NOTES.md and sources/wvo/cipher-letters-2026-09-24.tsv (92 rows: briefnr, date, direction, correspondent, place, archive codes, cipher extent, solution status, printed edition, pdf url, already nominated) -- the 73 uncovered letters grouped by correspondence circle, five circles with an unread letter, four of them with a solved sibling in the same run; QUEUE.md rows WV1-WV4 and the section "Willem van Oranje correspondence: unsolved cipher letters"; the NOTES.md of every existing WVO-circle folder at status open, each of which ends with a named next step: ciphers/la-garde-1577 (9 unresolved cells, needs crops), ciphers/willem-van-hessen-1567 (the Hessen letters remain unread, edition check named), ciphers/jan-van-nassau-1572-75 (fetch and image the remaining six letters; locate 5198/5199), ciphers/wvo-hessen-1564 (parked below unicity: do not reopen without a key), ciphers/oldenbarnevelt-brederode-1605 and ciphers/borssele-heinsius-1714 (first step named in each); the LANE AX, AX2 and NX handoffs in STATUS.md (their lessons are rules here: the Nassau pool 4612/5799 is exhausted, do not reopen it); .claude/briefs/README.md's common tail; this lane's COMMON (2026-09-26-lane-ax-COMMON.md, reused; post a ROOM note that WC uses it with lane goal "WVO circles: one control-backed recovery test per circle, close or promote"). Rate limit, check-ins (every 30-45 minutes with send_later or create_trigger; re-arm before you end every turn), worker count (at most 6 live Sonnet workers; Opus only for a key reading or a design decision), ledgering and handoff (at 400k context): as in 2026-09-26-lane-nx2-orchestrator.md with LANE WC for LANE NX2. Every create_session passes source_url https://github.com/NoAutopilot/cipher-lab. Titles: LIVE/ARCHIVED convention; a lane archives its own workers, the parent archives the lane. ROOM claim line "LANE WC orchestrator: WVO correspondence circles (recovery shape), circles <names>". Keep a 'LANE WC handoff' section in STATUS.md current from the first worker report. Both accounts are live: the other account's parent is 7i (session_01U1NiMakQ8PPySH8WixXsmc), its lanes B12 (Salviati, malsburg, spec backfill) and its verifiers; never touch their targets; re-check ROOM.md for a claim in the last 6 hours before each assignment; fetch and rebase before every push.

Rules: (1) intake gate per letter before deep work -- tools/intake_gate_check.py exit 0 with a check-solved verdict that grepped the whole edition volume the WVO record names for the letter's date and correspondents (the clair349 lesson), Tomokiyo's pages and both solver repositories; the WVO 'solution status' column is a lead, not a verdict; (2) the sibling first: for each circle, the solved sibling's decipherment (printed in Groen, Gachard, Nepveu, Japikse, or interlinear on the leaf) is aligned to its ciphertext with tools/interlinear_align.py (leave-one-line-out control, gate 0.60) to rebuild the key; only a key that passes its control is applied to the unread letter; (3) transcription by line crops under 2500 px, two blind passes, content-aligned agreement, gate 80 percent for a reading, 60 percent for a key alignment; (4) every reading goes through tools/decode_key.py --check, a shuffled-key control, the language judge with its per-fold caveat (de16/fr16/nl16 as the letter's language: report the spread), tools/print_check.py on decoded phrases, and a fresh-instance rule-7 re-derivation before a "reading ready" line; (5) never self-class; the reading-ready line goes "for LANE VO1 (owner-account verifier) and for parent 7i's verifier" -- whichever claims first verifies, the other may countersign; (6) unseen witnesses (minutes, drafts, later copies the WVO record lists, the KHA Collectie Japikse) are named in every summary; (7) size boxes by the job; a control below its gate is "not a test", never a negative.

Jobs, in this order (dispatch 1 and 2 in parallel; 3 and 4 after):
1. WC-MAP (Sonnet, cap 4, box 40 min): from the 92-row table, list the four recovery circles with, per circle, the unread letters (briefnr, date, cipher extent, pdf url), the solved sibling(s) and where the solution is (edition and page, or on the leaf), whether a folder already exists and its named next step, and the cheapest control-backed test; write it as QUEUE.md's "WVO circles work list (LANE WC, 26 Sept 2026)" and as the lane's dispatch order; no images, no transcription.
2. WC-LAGARDE (Sonnet, cap 6, box 45): ciphers/la-garde-1577's named next step (the 9 unresolved cells: crops and a second pass; then decode_key --check and the controls); if the reading passes, re-derivation and the reading-ready line.
3. Per circle from WC-MAP's order (Sonnet, cap 6 each; Opus cap 10 only for a key alignment that needs judgement): sibling alignment, key, application, controls, re-derivation. Hessen 1567 (the Willem van Hessen letters with the 1127 key on file) and Jan van Nassau (image the remaining six letters first, Groen's printed decipherments as the crib) are the two the harvest and the NB rows rate highest.
4. Do not reopen wvo-hessen-1564 (parked below unicity) or the Nassau 4612/5799 rows; log them as excluded in the work list.
Handoff at 400k context or when the queue is spent; ledger every worker from get_session usage; post 'for the parent' lines at each check-in. Never call AskUserQuestion.
