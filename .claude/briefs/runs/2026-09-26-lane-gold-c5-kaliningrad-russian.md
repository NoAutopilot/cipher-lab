JOB GOLD-KAL2: kaliningrad-2015 (the lane's reserve), Russian substitution under transliteration, control first each unit through `tools/family_run.py --family homophonic`. Sonnet (claude-sonnet-5). Stop and push at $5 or 60 minutes, whichever first (sized as units: 1 corpus unit at about 10 minutes / $1.50 plus 3 family units at about 12 minutes / $1 each, from GOLD-KAL1's 17-minute two-family run and GOLD-K4's $3.16 for 51 minutes of family_run units, plus one unit of margin). NO subagents. Lane: LANE GOLD3 orchestrator session_01P8v53BYGZvFiriPpxEZy3h (Fable) or its successor. Written by the cycle-4 consolidator GOLD-CONS4 (Fable, session_01CSQuomCj5Simbsx6r6VVcK), 26 Sept 2026 00:51 UTC; the orchestrator fills the gate line and the spawn time.

INTAKE_GATE_OUTPUT (live, 01:14 UTC): kaliningrad-2015: open (line 1) -- edition/page or full-text-search citation found within 6 lines (exit 0). Spawned 26 Sept 2026 01:14 UTC by LANE GOLD3 (session_01P8v53BYGZvFiriPpxEZy3h). GOLD-KAL3 (Polish) is NOT live beside you: the orchestrator serialised the two jobs because both edit the spec's judge.corpora; you have the spec to yourself.

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), the top block "Summary, cycle 4" of `ciphers/kaliningrad-2015/HYPOTHESES.md` (the two profile tables and the decision table are what this job tests; read nothing else of the file), `specs/kaliningrad-2015.json` (judge block, cheap_test_done), `tools/data/ru19/README.md`, `python3 tools/family_run.py --help`, the docstring of `tools/families/homophonic.py` (`profile=target`), lines 34-40 of `tools/homophonic_anneal.py` (`fold()`: NFKD, strips combining marks, j->i, v->u, keeps a-z only -- every plaintext letter of a scheme must survive that), `ciphers/kaliningrad-2015/scripts/make_signs_tsv.py` (convention A; you add convention B). Claim in ROOM (`tools/room.py`). Read the clock with `date -u` before any date.

What the record says, pasted so you do not rediscover it:
- Target: convention A (`ciphertext_signs.tsv`, letter+apostrophe one sign) N 978, K 36, IC 0.0657; convention B (apostrophe its own sign) N 1066, K 28, IC 0.0718; 88 apostrophes, always in-word after one of nine consonant-like signs (n' 29, t' 16, x' 12, d' 11, l' 5, f' 5, s' 4, m' 3, z' 2). Under convention A the IC sits inside the window range of every transliteration scheme below; under B it sits above every scheme's maximum. The Synodal Bible as host text is a control-backed negative (GOLD-KAL1); Russian as the language has never been run. German homophonic K 36 is a control-backed negative (control 0.982, judge FAIL -1.605).
- Tokenisation discrepancy to settle first (5 minutes): NOTES.md's counts (n 92, n' 42, x 62, s 61, from `ic_analysis.py` as LANE B2 ran it) differ from the committed TSV's (n 105, n' 29, x 65, s 62) at the same N 978 and K 36 -- 13 apostrophes assigned differently. Find which rule `tokenize_signs` applies at a double apostrophe (`t't'`, `n'n'`) and at an apostrophe before a dot, state it in one sentence in HYPOTHESES.md, and use the TSV as it stands unless it is wrong by that rule (then regenerate it with the script, commit the diff, and say so). Never repair the transcript.
- `fold()` keeps 24 letters (a-z minus j and v, which it merges into i and u). A transliteration scheme may therefore use only: a b c d e f g h i k l m n o p q r s t u w x y z. Never use j or v on the plaintext side.

Transliteration schemes (letter by letter; `ё` as `е` throughout, Soviet print convention; `ъ` dropped, 0.01 pct of letters; `й` and `ы` both `y`, as practical Soviet-era transliteration did):
- S1, scientific with digraphs: а a, б b, в w, г g, д d, е e, ё e, ж zh, з z, и i, й y, к k, л l, м m, н n, о o, п p, р r, с s, т t, у u, ф f, х kh, ц ts, ч ch, ш sh, щ shch, ъ (dropped), ы y, ь q, э e, ю yu, я ya. 23 Latin letters; q is the soft sign (1.5 pct of letters).
- S1-stripped: S1 with q removed entirely (22 letters; softness lost).
- S3', phonemic partial: S1, except that when a paired consonant (б в г д з к л м н п р с т ф х) is followed by я, ю, ё or ь, write the consonant, then q, then the vowel as a, u, o (nothing for ь); я ю ё elsewhere stay ya yu e. q is then 2.9 pct of letters.
- S3, phonemic full: S3', and also q before е and и after a paired consonant (the vowel then e, i). q is 12.6 pct of letters.
`tools/translit_ru.py --scheme s1|s1s|s3p|s3 IN_DIR OUT_FILE` implements all four (shared tool, Usage 8: `--help`, an offline test in `tools/tests/test_translit_ru.py` on a 200-letter Cyrillic fixture asserting the exact output for each scheme and that no j or v appears, under 2 s). Output: one `.txt.gz` per scheme, `tools/data/ru19_lat/<scheme>.txt.gz` (Cyrillic lower-cased verse text from `tools/data/ru19/*.txt.gz`, chapter markers stripped, one letter stream), plus `tools/data/ru19_lat/README.md` (source ru19, the four schemes spelled out, letter counts, date) and `MANIFEST.tsv` (scheme, file, letters). Expected letter counts about 3.2-3.5M per scheme (digraphs add letters). Commit the corpus on its own commit before any run. No network: the corpus is on disk.

Do, in this order (each is one unit; do not start a unit that would cross 80 pct of $5 or of 60 minutes):
0. Five-minute steps, no unit: (a) the tokenisation discrepancy above; (b) `make_signs_tsv.py --convention B` writing `ciphertext_signs_B.tsv` (a trailing apostrophe becomes its own sign row, immediately after its letter; K should read 28, N 1066; print both); (c) periodic IC (spec test 3, never run): a 15-line `scripts/periodic_ic.py` printing the mean coset IC for periods 2-30 on the convention-A sign sequence and, as its control, the same on three shuffles of that sequence (seeds 1-3); a period whose coset IC exceeds every shuffle's maximum at that period by 0.01 or more is a ROOM `flag:` line, otherwise one sentence in HYPOTHESES.md ("flat: best period p at x vs shuffle max y").
1. Corpus unit: `tools/translit_ru.py` + test + the four files + README + MANIFEST, committed.
2. Unit A (rank 1 of the consolidator's table): convention B, scheme S3'. Set `judge.corpora` in `specs/kaliningrad-2015.json` to `["tools/data/ru19_lat/s3p.txt.gz"]` and `judge.corpora_note` to say which scheme the judge is on and why (the judge reads the spec, not `--corpus`; the pairing of corpus and judge is what makes the row a test). Then `python3 tools/family_run.py specs/kaliningrad-2015.json --family homophonic --cipher ciphers/kaliningrad-2015/ciphertext_signs_B.tsv --corpus tools/data/ru19_lat/s3p.txt.gz --param profile=target --seeds 3 --restarts 8 --gate 0.9 --label "GOLD-KAL2 homophonic ru S3-partial, convention B K28, control before target"`. Control first by the tool; gate 0.9 at this N (calibration: German K 36 profile=target read 0.982 at N 978, GOLD-KAL1; masc reads 0.994 at N 581, blitz). If the control is under 0.9, say so, do not lower the gate, and log CONTROL BELOW GATE with the number.
3. Unit B: convention B, scheme S1 (`judge.corpora` -> `s1.txt.gz`, `--corpus tools/data/ru19_lat/s1.txt.gz`, label "GOLD-KAL2 homophonic ru S1, convention B K28, control before target").
4. Unit C: convention A, scheme S1-stripped (`judge.corpora` -> `s1s.txt.gz`, `--cipher ciphers/kaliningrad-2015/ciphertext_signs.tsv`, `--corpus tools/data/ru19_lat/s1s.txt.gz`, label "GOLD-KAL2 homophonic ru S1-stripped, convention A K36, control before target").
5. Unit D only if the 80 pct rule allows a fourth family unit: convention B, scheme S3 (`s3.txt.gz`). Otherwise log it as owed with the command.
6. Leave the spec's `judge.corpora` on the last unit run, with a `corpora_note` listing every scheme run this job and the unit each served; one `cheap_test_done` entry per unit ("2-ru-s3p-B", "2-ru-s1-B", "2-ru-s1s-A", "2-ru-s3-B") with both numbers. Append a section "GOLD-KAL2, Russian transliteration schemes" under the append-only part of HYPOTHESES.md (below the cycle-4 top block, above the family_run table): the tokenisation finding, the periodic-IC line, per unit the control's three numbers and mean, the target's judge line, the first 40 letters of the target decode (word salad, never a reading), dead ends, exact commands. One paragraph in NOTES.md "GOLD-KAL2"; the UNSOLVED-SURVEY.md row is the orchestrator's.
7. A candidate reading exists only if `tools/judge_plaintext.py specs/kaliningrad-2015.json --file <decode>` prints PASS with the judge on the same scheme the unit ran: paste it, write "for LANE GOLD: judge PASS on <unit>, re-derivation owed" in ROOM, stop the job there.

Hosts: none (disk and CPU only); requests per host in the done line: 0. Subagents: none. Rule 10: never solved, new, first, unpublished; NOTES.md status stays `open`. Never describe decoded text unless the judge passed.
Done line (ROOM, starting `done: for LANE GOLD: GOLD-KAL2`): the tokenisation rule found; periodic IC best period and shuffle max; per unit "control a/b/c (mean, gate 0.9 met/not) vs target judge PASS/FAIL score (real_p05, null_p99)"; units not run and why; files; requests per host 0; no cost figure; no reading described unless the judge passed.
