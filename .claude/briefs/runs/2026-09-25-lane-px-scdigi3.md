PX-SCDIGI3 (Sonnet, cap $6 as a stall alarm, at most 2 subagents). Job: DigitArq deep harvest for copy-free Portuguese cipher letters and key sheets. Kind: scout (never solves, never promotes). Parent: LANE PX orchestrator session_01KapVpHVzNpnnQce5C8c3LY. Read the lane COMMON (.claude/briefs/runs/2026-09-25-lane-px-COMMON.md) first.

Read first: QUEUE.md sections "PARES / DigitArq cipher letters" (scPARES, scDIGI, scDIGI2: the working JSON API, the broken pagination of /api/docs/search past 10 rows, the empty children endpoint, the parent/rootParent trick, tools/digitarq_fetch.py for file lists, thumbnails and full resolution). Rows PP-01..PP-10 and antt-linhares-chave (maco 86, CLNH/0086) are already listed or taken: never re-list them.

Budget: digitarq.arquivos.pt one request at a time, >=3 s apart, at most 60 requests this session. Plan before spending: roughly 30 search/details calls, 30 image calls.

Method (past the ten-result limit, by fonds and unit description, not keyword pages):
1. Narrow queries that combine a cipher word with a fonds or a correspondent, so each returns <=10 rows fully: cipher words cifra, cifrado, cifrada, "em cifra", chave, decifrado, algarismos, "por cifra", contracifra; fonds/people: Condes de Linhares (CLNH, other macos than 86), Ministerio dos Negocios Estrangeiros (MNE), Manuscritos da Livraria (MSLIV), Miscelaneas (MISC), Casa de Palmela, Condes da Ponte, Marques de Pombal, D. Luis da Cunha, Sousa Coutinho, Aracaty, Anadia, Vila Nova de Cerveira, Azevedo e Araujo; ambassador names for Madrid, Paris, London, The Hague, Rome, Vienna 1640-1830. Use each hit's details parent/rootParent to find sibling units; narrow again on a sibling's own title words.
2. Keep only units with hasImages:true (copy-free). For each, pull the file list and 1-3 thumbnails or one full-resolution leaf to confirm cipher on the page (digit or symbol groups, a key table). A description hit whose images show no cipher is dropped with one line.
3. Drop-check each keeper against QUEUE.md, CATALOG.md, LANDSCAPE.md and ciphers/ (grep the reference code and names).

Output: append rows to QUEUE.md under a new section "## Portuguese holdings and dictionary codes (LANE PX, 25 Sept 2026)" (create it at the end of the file if absent; the dictionary scout PX-SCDICT writes to the same section, so rebase immediately before editing and number your rows PX-01, PX-02 ... continuing after the highest PX number already there). Columns: Row | Archive / signatura | Date | Description (archive's note, cipher words bolded) | Image URL tested (fileViewer URL and the leaf) | Copy-free | Kind (recovery if a key, decipherment or sibling in clear exists; else cryptanalysis) | Scout note. Also a short method paragraph with the queries run and their totals, and a list of non-digitized hits as copy-order leads (not numbered rows).
Touch only QUEUE.md (that section) and ROOM.md. No ciphers/ folder, no check-solved, no decoding. Report: rows found, how many copy-free, requests per host.
