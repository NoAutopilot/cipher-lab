LANE NX job NX-WVO174 (Sonnet). Parent: LANE NX orchestrator, session_01RDXRYmATas1Xy6K1Jjwtvo. COMMON rules: .claude/briefs/runs/2026-09-26-lane-ax-COMMON.md, read it and follow it, with "LANE NX" for "LANE AX" and lane goal "copy-free candidates with a key route, one control-backed cheap test each".
Target: ciphers/wvo-hessen-1564 (WVO briefnr 1109, Orange to Wilhelm IV of Hessen, cipher enclosure f.23, 18 Sept 1564; about 16 lines, mixed clear German and invented signs). Intake gate exit 0 (its NOTES.md). NX-WVO1109 (09:01) found the sign repertoire resembles willem-van-hessen-1567's briefnr 174 (9 Apr 1567, same Orange->Hessen direction), which has an extracted nomenclator key at ciphers/willem-van-hessen-1567/siblings/key_174_nomenclator.tsv. The test: does that key read 1109? Read willem-van-hessen-1567/NOTES.md for how the 174 key was built and graded; do not edit that folder.
Box: 75 minutes from your first date -u; stall alarm USD 8 (about 16 lines: 2 passes over line crops, batched a few lines per call, at most 6 calls, plus decoding).
Files: ciphers/wvo-hessen-1564/**, specs/wvo-hessen-1564.json (new), ROOM.md.
Steps:
1. Crops: line crops of the enclosure with python3 tools/iiif_lines.py --image <the rendered page file> --out ciphers/wvo-hessen-1564/images/crops --debug (paste the command; check the overlay). Subagents get crop paths only.
2. Transcribe: two blind passes, signs named with the SAME sign names key_174_nomenclator.tsv uses (give the subagents the 174 key's sign list and its reference images if the 174 folder has them, as the inventory -- not its values); clear German words as written. Compare by content alignment; reconcile from the crops. Report pass agreement.
3. Gates written in NOTES.md before decoding: (a) coverage -- share of 1109 cipher tokens whose sign exists in the 174 key (report it; below 70 percent means the key is a different system: stop and say so); (b) reading -- decode with the 174 key via decode.json + python3 tools/decode_key.py ciphers/wvo-hessen-1564 --check, and compare a German-word share (or the judge score) against 20 shuffles of the key's values over the same tokens (target must exceed the shuffle max). The shuffle is of VALUES, which can change the statistic; coverage is not tested by it (rule 3: say so).
4. Write specs/wvo-hessen-1564.json (judge block, language de, corpus tools/data/de16 -- 16th-century; report its per-fold spread or say it is not on file) and paste the judge output. Grade per token: tokens read by the 174 key are S at best (a sibling key, cryptanalytic transfer) unless 174's own values were graded H/C, then say what grade they carry and why.
5. If (a) or (b) fails, the target stays open: "park, below unicity, 174 key does not transfer", both numbers side by side.
Rule 10: no novelty words; do not classify novelty. Stop; the orchestrator sends a fresh re-derivation if (b) passes.
Finish per COMMON.
