LANE B3 breadth worker bBUL2: bullet-tuscany-1944 test 2 (approved by parent 7c, 19:08 UTC). Sonnet (claude-sonnet-5). Cap $3 or 30 minutes, whichever first. No subagents, no network.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (read it first; it governs; its INTAKE STEP applies) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Run `cheap_tests_in_order[1]` of specs/bullet-tuscany-1944.json (44-letter body; QM header and 605YZ/FF footer as candidate indicator/key material). Order:
(a) Intake step, then repair the spec's judge block first: add `min_word_cover` (0.5) and keep language en; add the crib list (GRENADE, PIN, PINS, REINFORCEMENT(S), PULL, THROW, ENEMY, POSITION, ATTACK, TANK, MG) as `cribs` if judge_plaintext.py supports it (read its --help).
(b) `python3 tools/family_run.py specs/bullet-tuscany-1944.json --family periodic_vigenere --seeds 3 --gate 0.6` once per tabula (vig, beau, varbeau via --param tabula=...), periods 1-8 (add a period-range --param with a test if the family lacks one). Control N=44 English, built by the tool. A CONTROL BELOW GATE (exit 3) is an expected, reportable result -- record it, do not tune the gate.
(c) Whatever (b) gives: the deterministic keyed decrypts -- key QM, key MQ, the letters of 605YZ/FF (YZFF, and digits mapped A=1/A=0), each under vig/beau/varbeau and Caesar shifts 0-25 -- scored by tools/judge_plaintext.py and by crib hits; matched control: the same key list and scoring on 3 synthetic 44-letter English military sentences enciphered under one of those keys (the true key must rank 1), and the same scoring on 3 random 44-letter strings (the false-positive rate).
(d) A crib drag of each crib over every offset under vig/beau/varbeau, reporting any offset whose derived key fragment is periodic at period <=8; control: the same drag on a synthetic 44-letter enciphered sentence containing the crib (true offset rank) and on random text.
Write specs/cheap-tests/bullet-tuscany-1944/test2/ (scripts, outputs, README) and cheap_test_done.2 with target and control numbers side by side. Rule 5: if the control is below gate, the verdict is 'not a test at this N' (partial), not a negative. Done line: per step, target vs control.
