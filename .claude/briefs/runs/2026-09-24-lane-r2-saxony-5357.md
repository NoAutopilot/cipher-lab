ROLE: TRANSCRIBER + SOLVER, Orange-Saxony 53 and 57 (LANE R2 worker S1). Model Opus, cap $9. Up to 2 Sonnet subagents for the passes only. No network: use the committed images (images/00053_p1.png, 00057_p1-p4.png); if a native-resolution crop is essential, write a flag line in ROOM instead of fetching.
Target: ciphers/august-van-saksen-1561-64. Read NOTES.md (R9, R14, R21 sections), glyphs/atlas.md, key_74.tsv, key_98.tsv. R21: 53's postscript shares System A's sign shapes but not its values; 57 was not opened (57 p3 has two separately signed cipher blocks).
1. Two blind Sonnet passes (atlas codes, one row per sign: letter, page, line, idx, code, doubt) over 53 p1 postscript and 57's cipher blocks; each pass writes its TSV to disk line by line and you commit+push after each block. Reconcile with tools/reconcile_passes.py; you settle only disagreements.tsv rows, on the image crop. ciphertext_53.tsv, ciphertext_57.tsv.
2. Test first whether 57 (or 53) decodes under key_74 or key_98 (tools/decode_key.py with a decode.json variant): report the share of plausible German.
3. If not, cryptanalysis: German, monoalphabetic substitution with homophones and possible nomenclator signs, System A shapes. Build a matched synthetic control first (German text of the same period and length, same symbol count and homophone profile, enciphered the same way) and report the solver's score on the control and on the target (rule 3, both numbers). A reading only if the control reads and the target gives connected German; grade S per token with M for doubtful, decode_key.py --check 0.
4. NOTES.md "S1: 53 and 57": counts or the negative with both control numbers. ROOM `for LANE V2: ...` if read (with the search log: what you checked, e.g. the printed Groen/Kluckhohn texts named in NOTES, by grep of what is on disk), else a done line. Check your cost after each step; stop at the cap with what is pushed. Do not classify novelty.
