LANE B3 breadth worker bRUB2: rubin-1953 test 2 (approved by parent 7c, 19:08 UTC: masc through family_run at N=305). Sonnet (claude-sonnet-5). Cap $3 or 30 minutes, whichever first. No subagents, no network.
Common rules: `.claude/briefs/runs/2026-09-25-lane-b3-common.md` (read it first; it governs; its INTAKE STEP applies) and `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Transcription: ciphers/rubin-1953/ciphertext.txt (one blind pass, bRUB 18:20; say so -- rule 2: any negative is conditional on it). (a) Intake step (the gate should pass; paste its output). (b) Repair the spec's judge block: set letters_min/max to the letter passage's real size (N=305, e.g. 280-330), add `min_word_cover` 0.5, language en; check it with judge_plaintext.py on a dummy file. Make the target the letter passage only (Blocks A+D, as test 1 pooled them): if family_run reads the whole ciphertext, write the letters-only passage to ciphers/rubin-1953/letters_AD.txt with the script that cuts it and pass it with --cipher. (c) `python3 tools/family_run.py specs/rubin-1953.json --family masc --seeds 3 --gate 0.6 --cipher ciphers/rubin-1953/letters_AD.txt` (English control at N=305 built by the tool). Also one run with the clear words DULLES and CONANT removed from the passage if they sit inside Blocks A+D (say which). (d) If the target decode is judge FAIL while the control passes: that is a control-backed negative for simple substitution of English on this transcription; say it in those words. If the control is below gate: not a test (partial). Write cheap_test_done.2 (target vs control, judge line pasted) and the HYPOTHESES.md rows the tool writes. Done line: control mean (range) vs target judge verdict.
