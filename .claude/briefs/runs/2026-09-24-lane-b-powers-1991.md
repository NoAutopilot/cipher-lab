LANE B breadth worker: powers-1991. Sonnet (claude-sonnet-5). Cap $3.
Common rules: `.claude/briefs/runs/2026-09-24-lane-b-common.md` (read it first; it governs).

Spec: specs/powers-1991.json. Run `cheap_tests_in_order[0]` and its matched control, nothing else. Write both numbers into the spec's `cheap_test_done` (with date, method, cost). Judge any candidate plaintext with `tools/judge_plaintext.py` and paste its output. Cap $3. Report in five lines; do not run test 2; rule 10 wording; a negative's done line carries the target and control numbers side by side.

The Thomas article on thefreelibrary returns 403 to the cloud: try it once; on 403 add a JSTOR-QUEUE.tsv-style owner-machine row (or an ASKS.md row) naming the URL, and do the open-index half: OpenAlex (OPENALEX_KEY header) and Semantic Scholar (S2_KEY header), and Crossref, for Powers + Gold Bug Variations + dedication/cryptogram/code; one host at a time. Control for a print check: the same queries for a known-solved dedication puzzle are not needed; instead report hits for the query terms and what each hit says about the dedication. A print hit that states a decipherment ends the test as candidate found-solved (verifier to confirm).
