bSALC -- fr2933-salviati-1525 transcription pass C on the remaining disagreement boxes (LANE B10 job 3). Opus worker orchestrating Sonnet subagents (claude-sonnet-5). Cap USD 8, box 60 minutes from your first `date -u`.

Read first: `.claude/briefs/runs/2026-09-26-lane-b10-common.md` (governs; its cost-discipline line: you, Opus, never read page images to transcribe -- Sonnet subagents read the crops; you open a crop only for a named three-way split). ciphers/fr2933-salviati-1525/NOTES.md sections on pass C for f55v (L1c, line ~908) and f57v (L5c, ~1259), the f56r settle (~997), the table with "519 / 3,554 = 14.6%" (~1491) and "SALV-DIAG" (~1766); NEAR.md fr2933 row.

Intake gate (orchestrator ran it 10:47 UTC): `fr2933-salviati-1525: open (line 1) -- edition/page or full-text-search citation found within 6 lines`, exit 0.

Why: SALV-DIAG found the syllabary control crosses into the target's failing range at about 8-9% injected error, while passes A/B disagree on 14.6% of boxes (519 / 3,554 over seven leaves). Every design-family negative so far is a non-test at that error. Pass C gives each disagreement box a third blind read, the protocol L1c used on f55v (87 rows, USD 3.19) and L5c on f57v (78 rows, 2.35).

Units (state them in your claim line): the leaves whose recon_box_<leaf>/disagreements.tsv rows have no third read yet -- expected f54v (68), f55r (61), f56v (61), f57r (85) = 275 boxes; check f56r (79): if its settle was one reader against 5x recrops rather than an independent third pass, it is unit 5, run only if cap allows. f55v and f57v already have pass C: reuse their votes. Per leaf: cut the crops with the existing blind script (adapt crop_passC.py / crop_passC_f56r.py / montage_passC.py by leaf; they read only line/pos, never the A/B calls) -- run and paste the command before the first subagent call; split the leaf's boxes into two montage batches; one Sonnet subagent call per batch, given only montage paths plus the code atlas (glyphs/ and the code list the passes used), writing passC_<leaf>.tsv to disk; commit and push after each leaf. Estimate about USD 0.7 per subagent call (8 calls) plus your own overhead; stop before starting a leaf that would cross 80% of cap or box.
Then per leaf: a settle_passC script (adapt settle_passC.py) -- 2-of-3 base-code+mark majority wins; three-way splits listed, and you may settle them on the crop yourself (grade M).
Report, over all seven leaves (3,554 boxes): (a) residual disagreement = boxes with no 2-of-3 majority after pass C / 3,554; (b) the pass-C-vs-majority dissent rate on the formerly disputed boxes; (c) the implied per-box error estimate of the settled text, with the method in one sentence. Compare to SALV-DIAG's 8-9% crossover.
If the estimated error falls below 8%: rebuild the per-leaf ciphertext_<leaf>.tsv from the settled votes and the spec ciphertext with build_spec.py (show the spec diff counts), and flag the orchestrator "R8 design families re-runnable" -- do NOT re-run any family. If not: say so, with the numbers; the orchestrator writes the owner-side high-resolution request into the NEAR row.
Write NOTES.md section "bSALC: pass C, remaining leaves (26 Sept 2026)". Do not commit passC_crops/ (working copies). The folder is already 36 MB, over the 30 MB line: add nothing large. No network needed (crops from glyphs/crops/ on disk); if you need a host, stop and flag. ROOM claim first; done line at most 600 chars with (a), (b), (c). Final reply at most five lines.
