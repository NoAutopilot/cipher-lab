JOB GOLD-KAL4: kaliningrad-2015 (the lane's reserve), cycle 6: (a) re-run the four scheme/convention pairings whose homophonic CONTROL fell below the gate on a single collapsed seed with more restarts and more seeds, then the target if the gate is met; (b) the Latin-alphabet sweep at convention B, one language per unit, only if the 80 pct rule allows. Sonnet (claude-sonnet-5). Stop and push at $6 or 80 minutes, whichever first (sized as units: 5 restart units at about 10 minutes / $0.70 each -- KAL2 ran 4 units in 17 minutes at restarts 8, seeds 3, and this job runs restarts 20, seeds 5, about 2.5x the control work -- plus up to 7 sweep units at about 4 minutes / $0.35 each, plus one unit of margin; do not start a unit that would cross 80 pct of either figure). NO subagents. Lane: LANE GOLD3 orchestrator session_01P8v53BYGZvFiriPpxEZy3h (Fable). Written 26 Sept 2026 02:36 UTC by the orchestrator from the GOLD-KAL2 (01:31) and GOLD-KAL3 (02:18) done lines.

INTAKE_GATE_OUTPUT (live, 02:36 UTC): kaliningrad-2015: open (line 1) -- edition/page or full-text-search citation found within 6 lines (exit 0). Spawned 26 Sept 2026 02:36 UTC by LANE GOLD3. No other worker is live on this target or this spec.

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), the top block "Summary, cycle 4" of `ciphers/kaliningrad-2015/HYPOTHESES.md` and the two sections below it, "GOLD-KAL2" and "GOLD-KAL3" (the tables with the per-seed control numbers; read nothing older), `specs/kaliningrad-2015.json` (judge block, cheap_test_done), `python3 tools/family_run.py --help` (--seeds, --restarts, --corpus, --cipher, --gate, --param profile=target), the READMEs of `tools/data/ru19_lat`, `tools/data/pl19`, `tools/data/lt`. Claim in ROOM (`tools/room.py`). Read the clock with `date -u` before any date.

What the record says, pasted so you do not rediscover it:
- Two ciphertext views on disk: convention A (letter+apostrophe one sign, N 978, K 36, `ciphers/kaliningrad-2015/ciphertext_signs.tsv`) and convention B (apostrophe its own sign, N 1066, K 28, `ciphertext_signs_B.tsv`). Every control so far at convention B reads 0.99+ (Russian s3p 0.999, s1 0.997; Polish 0.996) and every target under it is a judge FAIL (control-backed negatives, in HYPOTHESES.md). At convention A the German control read 0.982 (KAL1) but three later controls each lost one seed of three to a collapse (Russian s1s-A 0.998 / 0.206 / 0.997; Polish A 0.988 / 0.973 / 0.003; Lithuanian A 0.467 / 0.444 / 0.987) and Russian s3 at convention B (K 28 over a 37-letter scheme) read 0.498 / 0.751 / 0.780. These four are CONTROL BELOW GATE rows: untested, not excluded. The question this job answers is whether they are a restarts problem (the anneal's local optimum at 8 restarts) or a real limit; the cure is more restarts and more seeds, never a lower gate.
- The judge reads `judge.corpora` from the spec: set it to the unit's corpus directory before each unit (the files, not the directory name, as the existing entries do; `corpora_note` names the unit), rebase before each edit, and RESTORE it to the seven `tools/data/de20/*.txt.gz` files (the GOLD-KAL1 state) as your last spec edit, whatever unit ran last. KAL3 left it on tools/data/lt.
- `homophonic_anneal.fold()` keeps a-z only; pl19 ships with l for ł; lt folds cleanly; ru19_lat schemes carry ь/ъ as q/w. Nothing to fetch: every corpus is on disk. Hosts: none; requests per host in the done line: 0.
- Rule 10: never solved, new, first, unpublished; no decoded text described unless the judge prints PASS; NOTES.md status stays open.

Do, in this order (one unit each; family_run.py control first, `--param profile=target --restarts 20 --seeds 5 --gate 0.9`, label "GOLD-KAL4 <unit>, restarts 20 seeds 5, control before target"):
1. 2-ru-s1s-A: `--corpus tools/data/ru19_lat/s1s.txt.gz --cipher ciphers/kaliningrad-2015/ciphertext_signs.tsv` (spec judge.corpora -> that file).
2. 2-ru-s3-B: `--corpus tools/data/ru19_lat/s3.txt.gz --cipher ciphers/kaliningrad-2015/ciphertext_signs_B.tsv`.
3. 2-pl-A: `--corpus tools/data/pl19 --cipher ciphertext_signs.tsv`.
4. 2-lt-A: `--corpus tools/data/lt --cipher ciphertext_signs.tsv`; then 5. 2-lt-B (never run): `--corpus tools/data/lt --cipher ciphertext_signs_B.tsv`.
   For each: if the 5-seed mean meets 0.9 the tool runs the target and the judge; paste the judge line. If the mean misses because of one collapsed seed (four seeds at 0.9+ and one under 0.5), run the same unit once more with `--seeds 6` (not a lower gate) and report both rows; if it still misses, the row says CONTROL BELOW GATE at restarts 20 and the unit is recorded as a limit of the anneal at this N and K, with the per-seed numbers. Never average away a collapsed seed: every HYPOTHESES.md line carries all five (or six) seed values.
6. Sweep at convention B (K 28), only if at least 25 pct of the cap and the box remain per unit as you go, in this order (CONS4's identity-profile L1 ranking, nearest first): nl20, da19, en, fr19, it16, es17c, pt18; `--corpus tools/data/<dir> --cipher ciphertext_signs_B.tsv --restarts 8 --seeds 3 --gate 0.9` (the convention-B controls have not needed more), spec judge.corpora set to that corpus's files per unit. A language whose corpus dir holds the plain .txt files (tools/data/en) is passed as the directory, as family_run's --corpus accepts. Log units not reached as "not run, box" in one line each.
7. Append "GOLD-KAL4, restarts and sweep (26 Sept 2026)" under the append-only part of `ciphers/kaliningrad-2015/HYPOTHESES.md`: one table row per unit (unit, corpus, convention, K, per-seed control values, mean, gate met/not, target judge line or "not run"), the family_run rows the tool appends, and one sentence per unit saying restarts problem / real limit / control-backed negative. One `cheap_test_done` entry per unit in the spec (test ids "2-ru-s1s-A-r20", "2-ru-s3-B-r20", "2-pl-A-r20", "2-lt-A-r20", "2-lt-B", "2-<lang>-B" for the sweep) with both numbers. One paragraph in NOTES.md "Next steps". Restore judge.corpora to de20 last.
8. A judge PASS is pasted, "for LANE GOLD: judge PASS on <unit>, re-derivation owed" goes to ROOM, and the job stops there.

Done line (ROOM, starting `done: for LANE GOLD: GOLD-KAL4`): per unit "control s1/s2/s3/s4/s5 (mean, gate met/not) vs target judge PASS/FAIL score x (real_p05 y)" or "CONTROL BELOW GATE at restarts 20, per-seed ..."; sweep units run and not run; whether any convention-A or Russian-s3 pairing turned out to be a restarts problem; files; requests per host 0; no cost figure; no reading described unless the judge passed.
