# LANE G3 worker G: transcribe the M36 fr.5761 1519 key (Sonnet, cap $4, Gallica fetcher; ONE target)

Read .claude/briefs/transcription.md and ciphers/fr5761-election-1519/NOTES.md. Ark btv1b52510561g; key heading on canvas f104 (folio 50v),
continuing toward folio 53 (canvases ~104-110; the manifest pins labels). Natives of the key leaves only (images/manifest.json; < 30 MB).
Two blind Sonnet subagent passes of the key into key_passA.tsv and key_passB.tsv with columns: section (alphabet / nulls / names / words),
plain, cipher sign (glyph atlas code if invented symbols: build one shared atlas first, see ciphers/dupuy452-carpi-1520/glyphs/), notes.
Commit after each pass; reconcile with tools/reconcile_passes.py where it fits, else a row diff; write key.tsv with a grade per row (H where both
passes agree and the image is legible, M otherwise). Record which correspondents the key names (these tell a later worker whose letters to
look for). No canvas walk of the rest of fr.5761, no solving. NOTES.md section 'Key transcription (24 Sept 2026)'. The key is a dataset:
report row counts and agreement.
