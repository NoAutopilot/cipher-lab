JOB GOLD-K4: Köhler family B', the two owed runs that complete the keyword-mixed tableau record: (a) the last two `mixed_tabula` placements `plaincipher` and `keycipher` (vig, Dutch key), (b) the English keyword list on the default four placements (vig, Dutch key), control first each through `tools/family_run.py`. Sonnet (claude-sonnet-5). Stop and push at $8 or 75 minutes, whichever first. NO subagents. Lane: LANE GOLD2 orchestrator session_013JXDgLDkW2y5Ldi2gWzTkY (Fable) or its successor. Written by the cycle-3 consolidator GOLD-CONS3 (Fable, session_01UBBcnhMmQEpPpdNQgDfsTE), 25 Sept 2026 23:10 UTC; the orchestrator fills the gate line and the spawn time.

INTAKE_GATE_OUTPUT:

Read first: `.claude/briefs/runs/2026-09-24-lane-r4-common.md`, `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md` (binding), the top block ("Summary, cycle 3") and the sections "Family B' variants (GOLD-K2)" (step 3 there is the English-list recipe; `wordcorpus=DIR[,DIR]` is the option) and "Family B'-c, cipher-side placements (GOLD-K3)" of `ciphers/koehler-1944/HYPOTHESES.md`, `python3 tools/family_run.py --help`, the docstring of `tools/families/keyed_running_key.py`. Claim in ROOM (`tools/room.py`). Read the clock with `date -u` before any date.

What cycle 3 learned, pasted so you do not rediscover it:
- Six of the seven `mixed_tabula` placements (plain, key, both, full at vig and beau; cipher at vig and beau) are run with controls at 72.6-89.6 pct plaintext letters recovered (gate 0.5 met every time) and targets inside the same pipeline's noise band (two-text -3.507 / -3.544; ten-text beau band min -3.548, median -3.531, max -3.494), judge FAIL every time; the record's one excess is K2's beau -3.478, 0.016 nats above the ten-text max, under the 0.1-nat flag line. B''-c (a free cipher-side permutation) is unidentifiable at 924 letters (control 9.5 pct). This job closes the family's record: seven of seven placements and a third keyword list. It is bookkeeping with a small chance (the consolidator put it at about 0.05); run it as such.
- Decoder facts: deterministic on the target; seeds apply to controls. Memory: at most THREE decoder processes at order 6 / beam 300; run nothing in the background this time (K3's background band pushed it to 59 of 75 minutes and over its cap). Never `git rebase --autostash` or `tools/room.py --push` while a decoder writes a log inside the tree: log to your scratchpad, copy in after exit. One control+target unit (3 control seeds + target) took K3 about 24 minutes with contention, about 18-20 without; budget on that.

Do, in this order (each is one unit; do not start a unit that would cross 80 pct of $8 or of 75 minutes):
1. `python3 tools/family_run.py specs/koehler-1944.json --family keyed_running_key --corpus tools/data/de20 --param kcorpus=tools/data/nl20 --param modes=plaincipher,keycipher --param arith=vig --param top=30 --param beam=300 --param order=6 --param spaces=1 --seeds 3 --gate 0.5 --label "GOLD-K4 B'-c plain+cipher and key+cipher, vig, Dutch key, control before target"` (the control's keyword and mode are drawn from the two modes given, so it tests exactly the placement set the target search covers).
2. The English keyword list on the default modes: the same command with `--param modes=plain,key,both,full --param wordcorpus=tools/data/en` (`tools/data/en`: three Gutenberg novels, 1880-1925 English; `en16_repo` is 17th-century readings, too small and the wrong register, do not use it; the option is `wordcorpus=DIR[,DIR]`, keyed_running_key.py line 29), label "GOLD-K4 B' English keyword list, vig, Dutch key, control before target". The control's keyword is drawn from the English list, so the control tests the list.
3. Only if the 80 pct rule allows a third unit: unit 1 with `--param arith=beau`. Otherwise log it as not run with the reason.
4. Append "Family B', owed runs (GOLD-K4)" under the append-only part of HYPOTHESES.md: per unit the control's three numbers, the target's stage-1 best and stage-2 best-of-30 on message 1, the pooled joint ll per letter against BOTH bands (two-text and ten-text, with the ten-text's min/median/max copied), the judge line, the first 40 letters of both decoded streams of message 1 (word salad, never a reading), and one sentence stating whether the seven-placement record is now complete. One line per unit in `specs/koehler-1944.json` `cheap_test_done`; one line in NOTES.md "Next steps".
5. A candidate reading exists only if `tools/judge_plaintext.py specs/koehler-1944.json --file <decode>` prints PASS: paste it, write "for LANE GOLD: judge PASS on <unit>, re-derivation owed" in ROOM, stop. A target pooled joint ll above -3.40, or above the ten-text band's max (-3.494) by 0.1 nats, without a PASS, is a ROOM `flag:` line.

Hosts: none (disk and CPU only); requests per host in the done line: 0. Subagents: none. Rule 10: never solved, new, first, unpublished; NOTES.md status stays `open`.
Done line (ROOM, starting `done: for LANE GOLD: GOLD-K4`): per unit "control a/b/c pct (gate 0.5 met/not), target pooled ll x vs two-text -3.507/-3.544 and ten-text -3.548/-3.531/-3.494, judge PASS/FAIL"; units not run and why; whether the B' record is complete (seven of seven placements, three keyword lists); files; no cost figure; no reading described unless the judge passed.
