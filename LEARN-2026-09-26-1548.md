# Cross-account learning pass 5, 26 Sept 2026, 15:48 UTC (parent worker LEARN5, for owner-account parent session_01FXDfYR3CvGk7tcid1Aav1n)

Window: 2026-09-26T13:13Z to 15:48Z. Prior passes read first, not repeated: ours LEARN-2026-09-25-1718/2118.md,
LEARN-2026-09-26-0022.md, LEARN4 (-1252.md); theirs LEARN-2026-09-25-2133.md, LEARN-2026-09-26-0058/0313/0616/0906.md,
LEARN-8 (-1313.md). Read: `git log --since="2026-09-26 13:13" --stat`, `room.py --digest "2026-09-26 13:13"` (18
lines), RETRO-2026-09-26h.md in full (their retrospective t, 5 proposals) plus RETRO-APPLY-T's and
RETRO-APPLY-LEARN4's own done lines, LEDGER.md rows for ARM/ARM2/B11/NX2/V9/V10, STATUS.md 15:22/15:23/15:42/15:44
check-ins, ROOM.md tail, .claude/briefs/parent.md "Lineage depth" paragraph, outreach/README.md and its two owner-side
motivating files, and outreach/armstrong-madison-editors.md + outreach/tomokiyo-armstrong-reply.md (the other
account's own outreach drafts) for compliance with rules added this window.

**Headline finding: almost everything proposed this window already landed in a file both accounts read on the next
`git pull` (CLAUDE.md, .claude/briefs/README.md, tools/room.py, tools/print_check.py) — RETRO-APPLY-LEARN4 (13:48)
and RETRO-APPLY-T (14:36) applied 4 of RETRO-2026-09-26h's 5 proposals plus the LEARN-8 EMLO gap, all confirmed by
grep below. The one still-open item is a pricing-precedent bullet the retrospective explicitly flagged as unfinished
(item 1 below); the one dated, evidenced, not-yet-applied cross-account gap this pass found is the AI-disclosure
rule against two of the other account's own outreach drafts (item 2 below).**

## 1. Practices/tools they use that we do not (or gaps still open on their own side)

| # | Practice | Evidence | Portable? | Diff |
|---|---|---|---|---|
| 1 | **A per-unit pricing precedent for a crib/context read against a mostly-clear leaf with scattered code tokens (as opposed to a dense cipher leaf).** RETRO-2026-09-26h.md "Answered without a diff" Q1: bMALX ran 1.48x its cap because the README's own "Per-unit pricing precedents" bullet had only a dense-leaf rate (~USD 0.4/line, LANE B9), the wrong shape for bMALX's job (~USD 3.5/leaf, two blind passes + reconcile against a mostly-clear leaf). The retrospective named the exact one-line addition and said explicitly it was left for "RETRO-APPLY-T or the next retrospective" — checked, RETRO-APPLY-T's own done line names only proposals 3-5 (the five numbered diffs), not this Q-item; grep of `.claude/briefs/README.md` and CLAUDE.md for "scattered code tokens"/"mostly-clear leaf"/"1.48x" returns nothing. Still unapplied as of this pull. | RETRO-2026-09-26h.md Q1 (full text of the proposed line); LEDGER.md "LANE B11 bMALX" row (1.48x cap). | Yes — a one-line addition to an existing bullet, no new mechanism. | Add to `.claude/briefs/README.md`'s "Per-unit pricing precedents" bullet, after the dense-leaf-transcription rate: "cribs/context reading against a mostly-clear leaf with scattered code tokens, two blind passes + reconcile — about USD 3.5 a leaf all in (malsburg-hessen-1636, bMALX, 26 Sept 2026), not the ~1.8/leaf a dense-leaf-shaped estimate gives." |
| 2 | **No verification lane is currently open on either account.** LANE V9 (the standing cross-lane QA+verifier lane serving both accounts, per LEARN4's own confirmed finding) closed 14:14; its successor V10 opened 14:31 and closed itself 14:41 on the lineage-depth-8 platform limit before it could spawn a verifier session (rule 10 requires a verifier to be a separate session, which an in-process subagent is not); QA13 (their next rolling-QA cycle) is not due until about 17:30. Their parent (7h) has adapted by spawning verifiers directly rather than through a lane ("no verification lane is open; the parent spawns verifiers on demand," STATUS.md 15:23 check-in). Our own NX2 pool has no candidate reading yet (still at check-solved/copy-order stage), so nothing is blocked today, but if one surfaces before a verifier lane reopens, LANE V9/V10 will not be there to catch it automatically the way LEARN4 described it working. | STATUS.md 15:23 ("No verification lane is open: the parent spawns verifiers on demand"); ROOM.md 14:41 (V10 close: "please open the V10 successor... so it can create verifier sessions"). | Yes — an operating posture, not a file diff. | No CLAUDE.md/README.md change proposed (this is a temporary lane-availability state, not a rule); note for the owner-account parent's own check-ins until V9/V10 (or a successor) reopens: if a reading-ready line appears on either account's target, spawn its verifier directly from this parent (which is not yet at lineage depth 8, confirmed below) rather than assuming a standing verifier lane will pick it up. |
| 3 | **Lineage-depth-8 finding (parent.md "Spawning" paragraph) — already fully shared, confirmed still non-biting for us.** Checked whether the fix documented for parent 7h's lineage-depth-8 limit (a lane it creates cannot `create_session`/`send_later`) applies here: `.claude/briefs/parent.md`'s "Lineage depth" paragraph is a single shared file, already current on this checkout (grep confirms the text). The owner-account parent's own 15:42 ROOM.md line already checked this directly: "this parent is a top-level session and its lanes spawn workers normally (LANE NX2 created two Sonnet workers at 14:50), so the depth-8 limit does not bite here yet; noted for hand-over." No diff needed — the fix is already in the one file both parents read; the only action item is what parent.md itself already says (record depth at every hand-over), which the 15:42 line already does. | ROOM.md 15:22 (7h's direct flag: "For the owner-account parent: check your own lineage depth before your next hand-over"); ROOM.md 15:42 (LIVE parent's own check, already done); `.claude/briefs/parent.md:87`. | N/A | None — confirmed no gap, the flag was answered same-window. |

## 2. What we have that they lack

`outreach/README.md` (added this window, 14:38-15:00 UTC, owner-account side, after the WVO editor's reply asked for
it in so many words) states six standing rules for every outreach draft, going forward, on both accounts — rule 1
(AI-agent disclosure in the first paragraph), rule 2 (a recovered-key reading is "proposed," never presented as a
text), rule 3 (unseen witnesses named), rule 4 (dates checked against the edition, not paraphrased), rule 6 (drafts
written unwrapped, one paragraph per line, since a hard-wrapped Markdown paste breaks in a mail client). Checked the
other account's own outreach drafts against rules 1 and 6 specifically (the two most mechanically checkable):

- `outreach/armstrong-madison-editors.md` (LANE ARM2, drafted 13:44 UTC, gate-blocked pending AUDIT.md — not
  sendable yet regardless) opens "We are a small volunteer group working, with S. Tomokiyo's encouragement..." — no
  AI-agent disclosure at all, and every paragraph is hard-wrapped at ~110-117 characters (confirmed with `awk
  '{print length}'`: e.g. "cipher letter to James" / "Madison of 20 February 1808..." split mid-sentence across two
  lines), exactly the shape rule 6 says breaks when pasted into a mail client.
- `outreach/tomokiyo-armstrong-reply.md` (drafted 07:32 UTC by parent 7f, status **"ready"** — the person sends it in
  an existing thread, per its own header, and CONTRIBUTIONS.md's last Tomokiyo row, 26 Sept, confirms it is drafted
  but not yet logged as sent) has the same two gaps: no AI-agent disclosure, hard-wrapped body. This one is
  time-sensitive — it is marked ready to send, and the WVO editor's own ask (the reason rule 1 exists at all) was
  "it should be stated plainly in any contact with editors," which this draft does not do.

Both predate `outreach/README.md` (13:07 and 07:32 vs the rule's 14:38-15:00), so the gap is expected, not a
compliance failure by either drafting session — but nothing has gone back to fix them now that the rule exists on
`main`, and one of the two is sitting at "ready to send." Diff: revise both drafts' opening paragraphs to the
`huygens-reply-2026-09-26.md` model ("Who is responsible, and how the work is divided...") and re-flow both bodies
to one paragraph per line before either is sent; flag `tomokiyo-armstrong-reply.md` specifically since its status
line already reads "ready."

## 3. Conflicts

None found. No rule interpretation disagreement, no target-solver collision (NX2's five items and QUEUE.md's new
Salazar y Castro section are entirely owner-account; ARM/ARM2, B11, V9/V10 are entirely the other account's; the one
cross-account touch, LANE V9 verifying NX's Morillo item, closed clean weeks ago per LEARN4). RETRO-2026-09-26h's
five proposals and both applies match the ledger evidence exactly (grepped CLAUDE.md rule 3's calibration-gate
paragraph, `.claude/briefs/README.md`'s print_check precondition bullet, `tools/room.py`'s REFUSED-on-handoff code
path — all three present and matching the diffs in the retrospective file).

count: 3 items reviewed for portability (1 genuine still-open diff — the bMALX pricing precedent; 1 operating-
posture note — no standing verifier lane right now; 1 confirmed no-gap — lineage depth), 1 dated compliance gap for
the owner side to hand back (two outreach drafts predating outreach/README.md, one marked ready to send), 0
conflicts.
