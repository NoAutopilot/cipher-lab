# goldbar-1933

found-solved
Minimal check-solved, 25 Sept 2026 (LANE B3 worker bGLD), before any deep work (intake step, `.claude/briefs/breadth.md`): Cipherbrain post 49 (Schmeh, 11 Feb 2017, scienceblogs.de/klausis-krypto-kolumne, already on disk at `sources/schmeh/posts/49-goldbar.html`/`.txt`, page 1 of 2, read in full including its 11-comment thread) relays former IACR president Kevin McCurley's 1996 IACR web page (iacr.org/misc/china/), the only primary source found by Schmeh or by this pass -- seven gold bars allegedly issued to a "General Wang" in Shanghai in 1933 as bank-deposit certificates, a legal dispute over the deposit claim, and doubts already raised in the thread (Nick Pelling: the depicted airplane, a Boeing 247 class, did not fly until 1934-35; both named 1996 contacts, Bin J. Tao and lawyer Peter Bisno, gave no useful reply, Bisno's firm saying only "we no longer represent the client"). Comment #10 (Bret Bowen, 10 Sept 2020) flags a probable transcription error (`UGMNCBXCFLDBEY`, printed length 14, should likely be `UGMNCBXCFLDBY`, length 13) and reports a frequency count of "exactly ten of every letter... except I, O, S and T" (13, 9, 11, 9). Comment #11 ("BREAKER", 28 Jun 2025) claims "ciphertext is broken" via a self-published Facebook page and an "Old English Box Cipher"/"Rotating Grille" reading tying the bars to the Lindbergh kidnapping, the Black Dahlia case and Yardley -- unreviewed, no verifiable primary-source method, not corroborated anywhere else found this pass; not treated as a solve (rule 10 -- and it does not read as a credible one either).

Both solver repositories grepped via shallow clone (github.com/dbourdeau/cyphersolver, github.com/aaymeloglu/unsolved-ciphers; cloned to /tmp, grepped, deleted, not committed). **dbourdeau/cyphersolver carries a dedicated, reproducible result already on file** (`goldbar/NOTES.md`, `goldbar/analyse.py`, `goldbar/readings.py`, dated 15 Sept 2026, independent-corroboration section added 15 Sept 2026 -- both stale relative to "today" per this repo's rule 6, but the content is theirs, not ours, and pre-dates this pass): the 263-letter corpus (as McCurley published it) has 21 of 26 letters occurring exactly ten times, chi-squared 1.251 against a uniform distribution on 25 degrees of freedom (P = 9.3e-13, "about 1 in a trillion"), and 0 of 200,000 Monte Carlo random 263-letter strings drawn from the same letter multiset reached this uniformity -- meaning the flatness is a property of the *deliberately assembled letter pool*, not of a shuffle of it, which rules out simple substitution or transposition of natural-language plaintext (those preserve the plaintext's own frequency skew, IC ~0.066) and is inconsistent with a long-key/OTP reading too (which would show chi-squared *near* 25 under random sampling, not near 1). Bourdeau's note goes further: it checked the transcription against McCurley's own photographs (7 of 15 images carry legible Latin lettering) and confirmed 14 of 16 strings letter-for-letter, found line 8 genuinely reads 13 letters on the clearest cast (agreeing with Bret Bowen's comment), and left one further single-letter doubt (line 11, `KOWVRSRKWTMLDH` vs `KOWVRSRWTMLDH`) unresolved from the casts. Under every one of the five readings this implies, chi-squared stays between 1.2 and 1.5 (P between 3e-12 and 1e-11) -- the conclusion is reading-independent. dbourdeau's repo lists the item in `SOLVED_CATALOGUE.md` and `SOLVED_RANKING.md` (#2: "Chinese gold bar cryptograms ... Letter counts flatter than any cipher of a real text; no message"), i.e. already closed there, credited to Bourdeau 15 Sept 2026 per CLAUDE.md rule 8 (code MIT, text CC BY 4.0; cited here, not copied -- the frequency count and control below are an independent re-run on this repo's own transcription, not a copy of `analyse.py`). aaymeloglu/unsolved-ciphers agrees independently: `SHORTLIST.md` line 105 groups "Scorpion, Blitz, Chinese gold bars, Hampton, Penitentia, McCormick" under "Hoax risk, no context, or no real system". OpenAlex (api.openalex.org/works?search=, `Authorization: Bearer $OPENALEX_KEY`) for "Chinese gold bar cryptogram solved" returned 29 hits, none relevant (top results: a 1998 public-key cryptosystem paper, a 2023 forgery-authentication paper, an unrelated Sherlock Holmes literary-studies paper). Semantic Scholar (api.semanticscholar.org/graph/v1/paper/search, `x-api-key: $S2_KEY`) for "Chinese gold bar cryptogram" returned 92 hits, none relevant (bio-bar-code assays, gold-market policy papers, Han-dynasty funerary-goods archaeology). No standard printed edition applies; McCurley's IACR page is the only primary source located by Schmeh, by this pass, or by Bourdeau's repo.

Status: this repo did not solve it and is not the first to close it -- **dbourdeau/cyphersolver already reached and published this determination on 15 Sept 2026** (named per CLAUDE.md rule 8; key class `published`, not ours). UNSOLVED-SURVEY.md row 25 and this target's own `specs/goldbar-1933.json` status line already noted "Bourdeau 15 Sept 2026: letter counts flat, no real text" before this pass; this NOTES.md is the target folder's own record of that determination plus an independent re-run of the frequency/control test (below) confirming it. The item remains on public "unsolved" lists (Schmeh's Top 50 #49, aaymeloglu's own top-level framing) because "not an encryption of a message" is a different kind of answer than a decrypted plaintext, and per this repo's rule 10 nobody here may call it "solved" for us -- but per CLAUDE.md rule 5 status vocabulary `found-solved` is the correct word for a target whose disposition question is already answered elsewhere, with a citation, before we touched it.

`python3 tools/intake_gate_check.py goldbar-1933` output (25 Sept 2026, after the paragraph above stabilised):
```
goldbar-1933: found-solved (line 3) -- edition/page or full-text-search citation found within 6 lines
exit: 0
```

## Cheap test 1: per-letter frequency vs random-sampling control (25 Sept 2026, LANE B3 worker bGLD)

Per `specs/goldbar-1933.json` `cheap_tests_in_order[0]` ("confirm or refute the commenter's 'every letter ~10 times' claim independently... report the design implication") and the lane brief's control-size instruction (1000 draws, vs Bourdeau's own 200,000, run independently on a from-scratch script rather than reusing his `analyse.py`, per rule 8).

**Control-design correction found this pass, logged rather than silently fixed:** the brief and the spec both describe the control as "a random permutation of the same letter multiset." That is a *degenerate* control for a whole-corpus letter-frequency chi-squared statistic -- any permutation of a fixed multiset has, by construction, the identical letter counts and therefore the identical chi-squared value. First run of `freq_test.py` confirmed this empirically: control mean == control min == control max == target statistic, 1000/1000 shuffles identical. The statistic that actually needs a random-sampling null (and the one that answers the spec's real question, "is this flatness the kind of thing chance alone produces") is: 1000 *fresh* N-letter strings, each letter drawn i.i.d. uniformly from the 26-letter alphabet, NOT constrained to the observed multiset. That is exactly Bourdeau's own Monte Carlo design (his NOTES.md: "200,000 random 263-letter strings"), just at 1000 draws instead of 200,000. Fixed and re-run; both versions of the script and the corrected output are in `specs/cheap-tests/goldbar-1933/`.

Script: `specs/cheap-tests/goldbar-1933/freq_test.py` (stdlib only, no numpy/scipy dependency, disk-only, no network). Ran on both readings named in the spec: as-printed (263 letters, McCurley/Schmeh transcription, `ciphertext.txt` as committed here) and with the Bret-Bowen/Bourdeau line-8 correction applied in memory only (262 letters, `UGMNCBXCFLDBEY` -> `UGMNCBXCFLDBY`; `ciphertext.txt` itself is left untouched per rule 2, no manuscript/photograph check was performed by this worker, image fetch is test 3, not run this pass).

Statistic: chi-squared of the 26 letter counts against the uniform expectation (N/26 each, 25 df). Control: 1000 fresh i.i.d.-uniform random N-letter strings (seed 20260925, recorded in the output JSON for reproducibility); same chi-squared statistic computed per draw; target's percentile rank reported against the resulting empirical null.

| reading | N | target chi-squared (25 df) | control mean | control [min, max] | control [p5, p95] | target percentile |
|---|---|---|---|---|---|---|
| as-printed | 263 | 1.251 | 24.92 | [8.17, 52.86] | [14.30, 36.84] | 0/1000 (below every draw; 0.0 pct) |
| Bowen/Bourdeau line-8 correction | 262 | 1.176 | 24.76 | [8.52, 55.36] | [14.27, 37.50] | 0/1000 (below every draw; 0.0 pct) |

Both readings: the target's own chi-squared (~1.2) sits below every one of 1000 fresh random draws of the same length (control mean ~24.8-24.9, essentially the textbook expectation of 25 for 25 df; control minimum still 8.2-8.5, nowhere near 1.2). This independently reproduces Bourdeau's finding (his 1.251/1.374/1.176 range across five readings, P ~1e-12 to 1e-13 against 200,000 draws) on a smaller control (1000 draws here vs 200,000 there) and a from-scratch script: **a corpus this flat essentially never arises from chance letter selection of this length; the near-uniform distribution is a designed property of the letter pool, not a coincidence of a natural process.** This is inconsistent with any encryption (substitution, transposition, short-key Vigenere, or long-key/OTP) of natural-language plaintext, all of which would preserve either the plaintext's own letter-frequency skew or the full random-sampling variance around uniform (chi-squared centred near 25, not near 1).

judge_plaintext.py: not run. The spec's `judge` block deliberately omits `language` (schema_note: a language-corpus check would FAIL any real decode by construction here, given the near-uniform design, so it would be actively misleading rather than uninformative) and this test produced no candidate plaintext line to judge.

Verdict for the spec: **closed-negative on "this is a substitution/transposition/short-key-Vigenere/OTP encryption of natural-language plaintext"**, control-backed (rule 3: matched control is a fresh i.i.d.-uniform random draw of the same N, not a fresh-English-text control, because the design question here is "does chance alone produce a corpus this flat", not "does this read as English/Chinese-pinyin prose"). Not a NEAR-candidate: this is not a control-backed *gap* (a technique that came close to the gate) but a control-backed floor -- the target sits outside the control band on the side that argues against any of these encryption designs, not inside/near it. Per rule 4, no H/C/S tokens are claimed (no key, no known plaintext, no cryptanalytic reading) -- this is a structural/statistical result about the ciphertext alone, same grade as Bourdeau's own (not applicable for token grading).

Files: `specs/cheap-tests/goldbar-1933/freq_test.py`, `specs/cheap-tests/goldbar-1933/freq_test_output.json` (raw counts, chi-squared values, and the 1000-draw control statistic summary for both readings; seed fixed and recorded for reproducibility).

Test 2 (exact-duplicate/substring scan) and test 3 (image fetch) not run, per brief ("run `cheap_tests_in_order[0]` only... Do not run test 2").

Requests: scienceblogs.de 0 (already-fetched pages reused from disk), api.openalex.org 1, api.semanticscholar.org 1, github.com 2 shallow clones (deleted after grep, not committed). No subagents.
