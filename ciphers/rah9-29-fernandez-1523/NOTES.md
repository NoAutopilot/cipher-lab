found-solved
No CSP Spain edition volume opened by this worker specifically for these folios this pass; dbourdeau/cyphersolver's sessa1524/key_working.md (which already quotes and uses the R9834 crib below) read by this worker from a fresh shallow clone (25 Sept 2026).

# Luis Fernández (Duke of Sessa) letters, 1523, RAH Signatura 9/29 — QUEUE.md row D8

DECODE ids (row D8): R9838, R9839, R9840, R9842, R9843 (Non-decrypted) / R9841, R9834 (Decrypted).

## Verdict: his lane, found-solved

QUEUE.md's own note for D8 says "same hand as Bourdeau's 9/28 and 9/30 targets, so expect him to reach 9/29" and
scores it "read only if DECODE opens before he gets there; otherwise a duplicate". He has already reached it: his
`sessa1524/key_working.md` names **R9834 directly** as a crib already used to fix a key value —

> `z y` = **-on** (3rd pl. preterite): R9834 (Sessa, 8 Sep 1523, **9/29 ff. 33–39**, cipher f. 35v, clear "D Claro"
> f. 38): *dilataron lo* = `… ott α ꝍ z y kef`; *concedieron* = `tac ttt g ꝑ3 x ꝍ z y`; also *se alargaba* =
> `dim α mus ꝍ # α bad`, *y no sin interesse* = `ꝑ lex cob do ꝑ do ott g ꝍ x dim`

R9834 is exactly D8's own "Decrypted" neighbour id. So the crib QUEUE.md flagged as needing a DECODE login has
already been read and mined by Bourdeau, feeding the same working key that spans 9/28, 9/29 (via this pair),
9/30, 9/31 and 9/33-9/34 (see rah9-31-fernandez-1524 and rah9-34-fernandez-1525). R9838, R9839, R9840, R9842,
R9843 and R9841 (D8's remaining ids) are not individually named in his repository, but they sit inside the same
9/29 folio range (ff. 33-39 is where R9834 lives) as a live target of the same campaign that has already used
this exact volume once.

## Search log (25 Sept 2026)

1. Bourdeau: same fresh shallow clone as D2/D3. Grepped "9/29", "A-29" (excluding the shared catalogue-harvest
   dump, which is raw scraped metadata, not a reading) and every D8 id individually. Only R9834 hits, in
   `sessa1524/key_working.md` (quoted above); no folder or file for the other five ids.
2. Aymeloglu: same fresh shallow clone. No 9/29-specific row beyond the DECODE-catalogue metadata already on
   our own disk (`sources/solver-diffs/2026-09-23-decode-neighbours-annotated.tsv`, the lines that produced
   QUEUE.md row D8 in the first place).
3. Editions: not opened this pass for 9/29 specifically (cap; D2's CSP Spain vol.2 April 1524 check and D3's
   CSP Spain vol.3 index check used the time budget for this brief). CSP Spain vol.2 covers 1519-1525 and would
   very likely calendar at least R9834 and R9841 (both "Decrypted", i.e. historically significant enough to be
   copied out) under Salazar A.29 folio citations, the same way vol.2 calendared the A.31 siblings for D2 —
   **flagged as the concrete next print-check**, not run here for lack of budget, not because it looks empty.
4. Community: WebSearch found nothing beyond Bourdeau's own site for this cluster specifically. No Cryptiana/
   Cipherbrain hit. `sources/decode/NOTES.md` carries no mention of these ids.

## What a recovery worker would need

Nothing from this lane, for the same reason as D2/D3: the crib id is already read and its output is already
folded into an active working key on the immediately adjacent volumes. If the orchestrator wants the other five
9/29 ids checked independently, the DECODE login worker would need R9838, R9839, R9840, R9841, R9842, R9843 —
not requested here, no action taken.

Images: not fetched by this worker (no DECODE login; playbook forbids it for this role).

Hosts this pass: WebSearch 1, github.com 2 shallow clones (shared with D2/D3, grep only, not counted twice).
