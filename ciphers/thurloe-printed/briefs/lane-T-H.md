LANE T worker H: align the printed decipherments of P3 (Butler) and P2 (Stouppe) (Sonnet, solver brief at best case N0, cap $5).

Context: ciphers/thurloe-printed/ NOTES.md sections 13 and 14 and index.tsv. P3 is John Butler, informant in Holland, c. 22 Sept 1656, vol. 2 (collectionofstat02thur) djvu 47926-48004, pp.575-577; Tomokiyo (sources/cryptiana/web/thurloe.htm, "John Butler (1656)", E=3/5/405) cites "deciphered text and ciphertext (Page 575)" for this letter; the body looks interlined; the old window (47994-48006) is the postscript, whose decipherment was not seen. P2 is Stouppe to the prince of Tarente, 25 Aug 1654, djvu 46954-47060, pp.565-566, where Birch prints "Deciphered thus:" with an English translation after a French/numeral passage.

Job: (1) P3: run tools/interlinear_align.py over the letter's full extent (P3_pairs.tsv), build key_butler.tsv (C by vote, H for Tomokiyo's stated values), and say exactly which groups, including the postscript's, the print leaves without a decipherment; if the postscript has none, decode it with the key and grade per token (C/M/U). (2) P2: say what the "Deciphered thus:" text covers (which cipher tokens, whether it is a translation rather than a letter-for-group decipherment) and, if the groups can be matched to words of the translation, write P2_pairs.tsv and key_stouppe.tsv; otherwise say why not. (3) decode_butler.py (and decode_stouppe.py if built) with --check (rule 7). (4) NOTES.md section "17. P2, P3 printed decipherments (LANE T worker H)" with grade counts and any groups still unread. Files you may write: ciphers/thurloe-printed/{P3_pairs.tsv, key_butler.tsv, decode_butler.py, reading_P3.txt, P2_pairs.tsv, key_stouppe.tsv, decode_stouppe.py, reading_P2.txt, NOTES.md (your section only)}, ROOM.md.


First action: `date -u`; `git fetch origin main && git status`; if you have no local commits, `git reset --hard origin/main`. If ROOM.md says 'swap starting', push what you hold and stop until 'swap done', then `git fetch origin && git reset --hard origin/main`. Read CLAUDE.md in full and the last 30 lines of ROOM.md first; append a claim line to ROOM.md (name yourself as the brief title, parent "LANE T session_01EwdS3bprjRaK49MrSERfA2"), a line before editing a shared file, and a `done` line when you stop; use `flag` for anything LANE T must see. The four Thurloe volumes' OCR text is committed gzipped at sources/ia-fulltext/thurloe-gz/ (read its README): restore with zcat to sources/ia-fulltext/<id>_djvu.txt; do NOT fetch archive.org or any other host unless this brief names you as the network worker. Do not edit index.tsv, thurloe-check.tsv, status.json, STATUS.md, LEDGER.md or QUEUE.md: put proposed index corrections in your NOTES section as a TSV block and LANE T applies them. Write only in the files this brief names. Cap: about $5 of usage (session metadata cost); stop at it. No subagents. Commit by explicit path, `git fetch origin main && git rebase --autostash FETCH_HEAD && git push origin main`, keeping both sides of any ROOM.md conflict (append-only). Report in one short paragraph (first line: the answer), with per-host request counts, then stop. Per rule 10, report what was found and where it was not found; never new, unpublished, first, unread; do not classify novelty. Do not start other targets. Never print or commit credentials.
