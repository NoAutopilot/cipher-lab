LANE T worker K: align the printed decipherments of P14, P9 and P10 from the page-image transcription (Sonnet, solver brief at best case N0, cap $6).

(Revised 24 Sept 2026 04:37 UTC after worker I's section 18: P14 does carry its own interlinear decipherment in the print; the OCR hid it. The earlier version of this brief, a cryptanalytic reading of P14, is withdrawn.)

Context: ciphers/thurloe-printed/ NOTES.md sections 11 (worker B) and 18 (worker I), images/manifest.json, P14/image_transcription.tsv (p.101 lines 44-65, p.102 lines 1-22), P9/image_transcription.tsv (p.611-612), P10/image_transcription.tsv (p.620), decode.py (worker B's refactor: extended_from_pairs for P9/P10 into key_blake_extended.tsv, P15 into the Montagu key; P14 currently a mechanical reading), tools/interlinear_align.py.

Job: (1) Build P14_pairs.tsv, and rebuild P9_pairs.tsv and P10_pairs.tsv, from the image transcriptions (plain/interlinear line paired with its cipher line), keeping the djvu line references where they exist. (2) Extend decode.py so P14 goes through the same aligned path as P15 (Montagu system; Tomokiyo values H, print-aligned C, single-occurrence M) and P9/P10 through the Blake path using the new pairs; P11-13's committed grades must not change; `python3 decode.py --check` must pass. (3) Regenerate reading_P9.txt, reading_P10.txt, reading_P14.txt, key_blake_extended.tsv, key_montagu_extended.tsv. (4) Per-letter grade counts before and after. (5) NOTES.md section "20. P9, P10, P14 aligned from the page image (LANE T worker K)" with counts, the groups the print leaves without a gloss (e.g. P10 p.620 line 10), and a proposed index.tsv status line per row. Worker I also saw a Lockhart letter (Chauny, 19 June 1656) on p.101 above P14 with its own coded numerals and glosses; do not transcribe or align it; one line in your section noting it is enough. No anneal, no control (nothing cryptanalytic), no network. Files you may write: ciphers/thurloe-printed/{decode.py, P9_pairs.tsv, P10_pairs.tsv, P14_pairs.tsv, key_blake_extended.tsv, key_montagu_extended.tsv, align_*.tsv, reading_P9.txt, reading_P10.txt, reading_P14.txt, reading_P15.txt, NOTES.md (your section only)}, ROOM.md. Report what was found; do not classify novelty.


First action: `date -u`; `git fetch origin main && git status`; if you have no local commits, `git reset --hard origin/main`. If ROOM.md says 'swap starting', push what you hold and stop until 'swap done', then `git fetch origin && git reset --hard origin/main`. Read CLAUDE.md in full and the last 30 lines of ROOM.md first; append a claim line to ROOM.md (name yourself as the brief title, parent "LANE T session_01EwdS3bprjRaK49MrSERfA2"), a line before editing a shared file, and a `done` line when you stop; use `flag` for anything LANE T must see. The four Thurloe volumes' OCR text is committed gzipped at sources/ia-fulltext/thurloe-gz/ (read its README): restore with zcat to sources/ia-fulltext/<id>_djvu.txt; do NOT fetch archive.org or any other host unless this brief names you as the network worker. Do not edit index.tsv, thurloe-check.tsv, status.json, STATUS.md, LEDGER.md or QUEUE.md: put proposed index corrections in your NOTES section as a TSV block and LANE T applies them. Write only in the files this brief names. Cap: about $6 of usage (session metadata cost); stop at it. No subagents. Commit by explicit path, `git fetch origin main && git rebase --autostash FETCH_HEAD && git push origin main`, keeping both sides of any ROOM.md conflict (append-only). Report in one short paragraph (first line: the answer), with per-host request counts, then stop. Per rule 10, report what was found and where it was not found; never new, unpublished, first, unread; do not classify novelty. Do not start other targets. Never print or commit credentials.
