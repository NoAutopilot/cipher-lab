LANE T worker F: real-data benchmark of our homophonic solver on the Fauconberg letters, scored against Birch's printed decipherment (Opus, solver brief, cap $15).

Why: the 1654 inline pool (P4-P7, worker C) prints numerals with no decipherment; before any solver runs on it, rule 3 needs a matched control. A real 1658 letter-level homophonic cipher with its printed decipherment is a better control than synthetic text. NOTES.md section 8 shows the Fauconberg-to-Henry-Cromwell cipher (P19, P21, P22, P23, and per LANE T's heading scan also P16, P17, P18, P20, P24, all vol.7) is a letter substitution with a few homophones (11=e, 13=e, 23=l, 27=n, 6=d, 26=o, 39=t, 41=t, 16=h) plus single-capital name codes, and that Birch prints the decipherment on the neighbouring line.

Job: (1) Ground truth: from the extraction files (ciphers/thurloe-printed/<Pn>/ciphertext.txt) take only the cipher lines whose neighbouring printed decipherment has exactly as many letters as the line has groups (check_interlinear.py's exact matches), giving (group, letter) pairs; that is the scoring key, not an input. Worker A is aligning the full letters at the same time into key_fauconberg.tsv; you may use it for scoring when it lands but must not wait for it. (2) Blind run: feed tools/subst_hillclimb.py and tools/nomenclator_anneal.py (read both; tools/english_score.py for the language model) the group sequences only, with no crib, on (i) all Fauconberg groups together and (ii) length-matched subsets of about 300, 600 and 1,200 groups, three seeds each. Score: share of tokens and of distinct values read correctly against the ground truth. (3) The same runs on a synthetic control of the same length and homophone structure made from clear 1650s English out of the same volume, so real and synthetic can be compared. (4) Write ciphers/thurloe-printed/benchmark/ (script, results TSV with every run, seed and score; `--check` or a README command that regenerates the TSV), and a NOTES.md section "15. Solver benchmark on Fauconberg (LANE T worker F)": a table of length vs accuracy, real vs synthetic, and the smallest length at which the solver reads at least 80 percent of tokens. That number decides whether the 1654 pool is solver-ready. Tool fixes are allowed if a tool fails on this input; keep them minimal and say what changed. Files you may write: ciphers/thurloe-printed/benchmark/*, tools/subst_hillclimb.py and tools/nomenclator_anneal.py (minimal fixes only), NOTES.md (your section only), ROOM.md.

First action: `date -u`; `git fetch origin main && git status`; if you have no local commits, `git reset --hard origin/main`. If ROOM.md says 'swap starting', push what you hold and stop until 'swap done', then `git fetch origin && git reset --hard origin/main`. Read CLAUDE.md in full and the last 30 lines of ROOM.md first; append a claim line to ROOM.md (name yourself as the brief title, parent "LANE T session_01EwdS3bprjRaK49MrSERfA2"), a line before editing a shared file, and a `done` line when you stop; use `flag` for anything LANE T must see. The four Thurloe volumes' OCR text is committed gzipped at sources/ia-fulltext/thurloe-gz/ (read its README): restore with zcat to sources/ia-fulltext/<id>_djvu.txt; do NOT fetch archive.org or any other host unless this brief names you as the network worker. Do not edit index.tsv, thurloe-check.tsv, status.json, STATUS.md, LEDGER.md or QUEUE.md: put proposed index corrections in your NOTES section as a TSV block and LANE T applies them. Write only in the files this brief names. Cap: about $15 of usage (session metadata cost); stop at it. No subagents. Commit by explicit path, `git fetch origin main && git rebase --autostash FETCH_HEAD && git push origin main`, keeping both sides of any ROOM.md conflict (append-only). Report in one short paragraph (first line: the answer), with per-host request counts, then stop. Per rule 10, report what was found and where it was not found; never new, unpublished, first, unread; do not classify novelty. Do not start other targets. Never print or commit credentials.
