LANE T worker D: consolidate the remaining isolated rows P2, P3, P8 (Sonnet, consolidation only, cap $6).

Context: ciphers/thurloe-printed/ NOTES.md sections 1, 4, 5 and index.tsv. P2 (vol.2 collectionofstat02thur, window 46981; index "General Fleetwood" but LANE T's heading scan finds only "Monsieur Bordeaux to his son the French ambassador at London" at 46913 and a letter opening "to your highnefs" at 46956 above it), P3 (vol.2 window 47994; index "Mr. Bradshaw, resident at Hamburgh", but the lines above are Scots, "quhat ... zou", and the P3 excerpt signs "John Butler"; its values are 400-series code groups mixed with small numbers: 412 60 40 411 405 ...), P8 (vol.3 window 45228, "Lord chief baron Steele to secretary Thurloe" at 45193; check_interlinear.py finds a clear line matching the group count for 12 of its 15 cipher lines, so Birch may print a decipherment here).

Job, no cryptanalysis: for each row (1) confirm sender, recipient, dateline and printed page from the print (heading above, signature below, not the previous letter's signature), and the full extent of its cipher; (2) say whether the print carries a decipherment (interlinear or inline) and, if it does for P8, run tools/interlinear_align.py and write P8_pairs.tsv plus key_steele.tsv (grade C) and a decode_steele.py with --check (rule 7); (3) otherwise write <row>/tokens.tsv (order, djvu line, raw, cleaned, doubtful flag, clear words around) and a note on the system's shape (value range, code vs letter groups, repeats) and whether it resembles any other row in index.tsv or Tomokiyo's sections in sources/cryptiana/web/thurloe.htm (local mirror, no fetch); (4) a NOTES.md section "13. P2, P3, P8 (LANE T worker D)" with attribution table, counts, verdict per row (printed decipherment / inline undeciphered / too short) and a proposed index.tsv correction block. Files you may write: ciphers/thurloe-printed/{P2,P3,P8}/tokens.tsv, P8_pairs.tsv, key_steele.tsv, decode_steele.py, reading_P8.txt, NOTES.md (your section only), ROOM.md. Do not touch other rows.

First action: `date -u`; `git fetch origin main && git status`; if you have no local commits, `git reset --hard origin/main`. If ROOM.md says 'swap starting', push what you hold and stop until 'swap done', then `git fetch origin && git reset --hard origin/main`. Read CLAUDE.md in full and the last 30 lines of ROOM.md first; append a claim line to ROOM.md (name yourself as the brief title, parent "LANE T session_01EwdS3bprjRaK49MrSERfA2"), a line before editing a shared file, and a `done` line when you stop; use `flag` for anything LANE T must see. The four Thurloe volumes' OCR text is committed gzipped at sources/ia-fulltext/thurloe-gz/ (read its README): restore with zcat to sources/ia-fulltext/<id>_djvu.txt; do NOT fetch archive.org or any other host unless this brief names you as the network worker. Do not edit index.tsv, thurloe-check.tsv, status.json, STATUS.md, LEDGER.md or QUEUE.md: put proposed index corrections in your NOTES section as a TSV block and LANE T applies them. Write only in the files this brief names. Cap: about $6 of usage (session metadata cost); stop at it. No subagents. Commit by explicit path, `git fetch origin main && git rebase --autostash FETCH_HEAD && git push origin main`, keeping both sides of any ROOM.md conflict (append-only). Report in one short paragraph (first line: the answer), with per-host request counts, then stop. Per rule 10, report what was found and where it was not found; never new, unpublished, first, unread; do not classify novelty. Do not start other targets. Never print or commit credentials.
