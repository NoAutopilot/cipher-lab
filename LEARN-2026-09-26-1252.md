# LEARN4: cross-account learning pass, window 2026-09-26 09:06-12:52 UTC

Parent worker LEARN4 (Sonnet, session_017LaWfuA5LcF7UaVNnpvxkq), for owner-account parent session_01FXDfYR3CvGk7tcid1Aav1n.
Prior passes read first, not repeated: ours LEARN-2026-09-25-1718/2118.md, LEARN-2026-09-26-0022.md; theirs
LEARN-2026-09-25-2133.md, LEARN-2026-09-26-0058/0313/0616/0906.md. Window covers parent 7g/7h, LANE V9 (QA9/QA10/MOR),
LANE B9/B10/B11, LANE ARM/ARM2, retrospectives r/s and their applies (RETRO-APPLY-R/S), SALV-DIAG.

**Note first:** most of this window's proposed practice changes (RETRO-2026-09-26f.md, -g.md) were already applied
directly to the *shared* files -- CLAUDE.md, `.claude/briefs/README.md`'s common tail, `tools/leaf_pool_gate.py`,
`tools/room.py` (`warnings_for`), `tools/key_livecheck.py`, `tools/family_run.py` -- by RETRO-APPLY-R (09:53-09:59)
and RETRO-APPLY-S (11:33-11:41), both committed to `main` before this pull. Since both accounts read the same
`main`, those items need no porting: cite-what-you-read, typed-clock-time self-check, the "third unchanged attempt"
rule-3 paragraph, per-token H-only leaf pooling, `--measured-error` family gate, key-probe retry-before-flip, the
per-unit pricing register, and "a lane cannot archive itself" are already live on this side. Confirmed by grep:
README.md:101 (archive-itself), README.md:104-107 (pricing register), CLAUDE.md (measured-error paragraph) all
present in this checkout.

## 1. What they use that we do not (real gaps, not yet shared)

| What | Evidence | Diff to port | If not: why |
|---|---|---|---|
| QA cycle mechanically re-runs `tools/near_check.py`, `tools/ledger_check.py`, `tools/intake_gate_check.py` on every touched target, not just a by-eye rule read | V9-QA9/QA10 room lines ("near_check.py 11/11 in step", "ledger_check.py duplicates are pre-existing legacy rows only"), QA/2026-09-26-0942.md, -1127.md | Add an explicit checklist item to `.claude/briefs/runs/2026-09-25-parent-quality-audit.md` (our rolling-QA brief): "7. Run `tools/near_check.py`, `tools/ledger_check.py` and `tools/intake_gate_check.py <each touched target>`; paste output; a nonzero exit or an unresolved flag is a failure." Our brief currently only names rules 1/3/7/10 by number, not the tools that already mechanize checking them. | Cheap, no reason not to; propose to the parent. |
| ARM-C1's finding that a context-only scoring objective (family C, nomenclator, N=369) does not have the true key at its own maximum -- it drifts to 0.59 under the objective even with real correspondences known -- is a second confirmed instance of the same failure shape as AX2-4612S's `key_repair.py` (a tool/objective whose own known-answer control does not recover the truth) | LANE ARM ARM-CONS1 room line 09:54 UTC ("C2 (stronger objective) REJECTED on paper... only more same-code ciphertext lowers the singleton share"); ARM-C1 brief | CLAUDE.md rule 3 and item 8a already generalize "a tool whose own known-answer control fails three times" (AX2-4612S) but not "an unsupervised objective whose optimum is provably not the truth even given correct auxiliary information" as its own check. Suggest one sentence added to `tools/family_run.py`'s docstring: before a family's blind score is used to judge an unknown target, run it once with the *known* key/plaintext of a matched control and confirm the true key ranks at or near the top of the objective, not just that the mean score clears a gate. | Not urgent (ARM's own consolidator already caught and parked it on paper); worth a docstring line, not a new tool. |

Not ported, and why: the bMALK "control drawn from the key's own exhausted value-range is a non-test" finding is a
third instance of a pattern CLAUDE.md rule 3 already generalizes (bCAS, AX-5799) -- no new rule needed, it is
already covered by the existing paragraph. LANE V9 itself (a standing cross-lane QA+verifier lane) is not a gap to
port: it is already serving both accounts directly -- V9-MOR classified rah-morillo-1817 item 3, an **owner-account
NX target**, per NX orchestrator brief's own rule 4 ("a reading... goes to re-derivation then V9"). That
cross-account routing is working as designed, not something to duplicate on our side.

## 2. What we have that they lack

| What | Evidence | Diff for their side | Status |
|---|---|---|---|
| `create_session` must pass `source_url`/`source_revision` explicitly (inheritance is unreliable) | `.claude/briefs/parent.md`, "Spawning (25 Sept 2026, UPDATES.md)" paragraph, already names the exact SCOUT-OWN-2 failure mode ("three workers on 25 Sept got no repository and stopped at their first turn") | None needed | `parent.md` is the single shared template both parents read -- already sufficient per the brief's own check; SCOUT-OWN-2 (26 Sept, this account) just reconfirmed it working as designed (the worker correctly refused). No diff to write. |
| A Google Books phrase sweep for an editor's own "not deciphered" annotation (e.g. "cifra no descifrada", "cifra non decifrata") finds candidates the Internet Archive numeral-run detector structurally cannot see (an edition can omit the cipher's digits from print while still noting in prose that a passage was in cipher) | SCOUT-OWN-3-2026-09-26.md (this account, 12:07 UTC): surfaced D1 (Doria/Don Juan de Austria 1896 ed.) and D2/D3 this way after QUEUE.md's numeral-run rounds were exhausted | Not in `.claude/briefs/scout.md`, `tools/print_check.py`, or `.claude/briefs/check-solved.md` on either side (grepped, no hits). Genuinely portable to their scout/check-solved process and to `print_check.py`'s search step: add an editorial-annotation phrase query (per-language: "non déchiffré", "not deciphered", "cifra no descifrada", "non decifrata", "unentziffert") alongside the existing full-text numeral search, independent of whether digits appear in the OCR. | Portable; the other account runs check-solved verdicts too and would benefit the same way. |

## 3. Conflicts

None found this window. The one place the two accounts' processes meet directly -- LANE V9 verifying an
owner-account (NX) target -- is working, not conflicting: grade counts differed slightly between the solver's
AUDIT-adjacent count and `decode_key.py --check`'s count for rah-morillo-1817 item 3 (C86/M5/U6 vs 91H... already
flagged and reconciled by V9-MOR/V9 orchestrator itself at 12:00 UTC, "the status.json row should quote AUDIT" --
no action needed here, already self-corrected).

Verdict: 2 portable items (1 tool/checklist diff, 1 technique diff), 1 docstring-level suggestion, 0 conflicts,
everything else already shared via `main`.
