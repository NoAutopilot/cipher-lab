# Cross-account learning pass 3, 26 Sept 2026, 00:22 UTC (parent worker LEARN, owner account)

Window: 2026-09-25 21:18 UTC onward, on top of passes 1-2 (`LEARN-2026-09-25-1718.md`, `-2118.md`) and the other
account's `-2133.md` (all three read first, items not repeated). Scope read: `git log --since="2026-09-25 21:18"
--stat` (this pushed commit included), `RETRO-2026-09-26a.md` (parent 7d's retrospective m, LEDGER.md rows 838-863
and its own 5 diffs), LEDGER.md rows in that range, `.claude/briefs/runs/2026-09-25-lane-gold-c4-*.md` and
`-2026-09-26-lane-gold-consolidator-c4.md`, `tools/room.py`, `.claude/briefs/parent.md`, `.claude/briefs/breadth.md`,
CLAUDE.md, and the owner-account `.claude/briefs/runs/2026-09-26-lane-ax-COMMON.md`. `date -u` read 00:15:41
(start) and 00:22 (this line). Not applied; the parent applies brief/tool-only diffs.

**Headline: this window's "cross-account learning" surface is mostly one file — RETRO-2026-09-26a.md, written by
the other account's own retrospective m at 00:08 UTC, twelve minutes before this pass started, with five diffs
already fully specified against files both accounts share (`parent.md`, `breadth.md`, `tools/room.py`, CLAUDE.md
Usage 6).** None of its five diffs are on disk yet (checked each literally: `grep` for its added sentences in the
target files, zero matches). Under the pattern passes 1-2 established (whichever parent applies a retrospective's
brief/tool-only diffs first, both accounts get it), this is squarely something the owner-account parent can apply
directly right now rather than wait for the other account's own RETRO-APPLY to get to it — it is not a "port their
practice into ours" item so much as "their retrospective is done and unapplied; either parent applying it closes
the loop faster." Reported as item 1 below with the diffs already written (copied from RETRO-2026-09-26a.md, not
re-derived), plus one gap found in this pass's own checking that RETRO-2026-09-26a did not catch.

## 1. Practices/lessons not yet in a shared file

| # | Practice / lesson | Evidence (file:line) | Portable? | Diff |
|---|---|---|---|---|
| 1 | **RETRO-2026-09-26a's five proposals are unapplied as of this pass** (reserve-batch sizing for lane-opening briefs; scoring QUEUE.md's already-scouted VX-/KX-/KT-/PX- rows before a new breadth scout pass; labeling the two `room.py --start` "keys:" lines by tool; comparing STATUS.md/QUEUE.md headings against `origin/main` fetched fresh rather than the local post-edit working tree; pricing a background computation that shares a per-unit box's CPU as its own box). All five diffs are already written out in full in `RETRO-2026-09-26a.md` sections 1-5 — this pass just confirms none are on disk yet. | `RETRO-2026-09-26a.md:41-59` (parent.md reserve-batch diff), `:84-99` (breadth.md scoring diff), `:125-141` (room.py key-label diff), `:169-190` (room.py origin-fetch heading-guard diff), `:219-233` (CLAUDE.md Usage 6 background-CPU diff); confirmed unapplied by `grep -n "Size the opening brief for a shift" .claude/briefs/parent.md`, `grep -n "Before re-scouting, score what is already scouted" .claude/briefs/breadth.md`, `grep -n "key_probe.py --sync:" tools/room.py`, `grep -n "origin_watched" tools/room.py`, `grep -n "competes for CPU" CLAUDE.md` — all five zero matches at 00:22 UTC. | Yes — these are shared-file diffs already fully drafted by a retrospective, the exact shape pass 1/2 found propagates the moment either parent lands it. | Apply RETRO-2026-09-26a.md's five diffs verbatim (or have a RETRO-APPLY worker do it), same as the owner side already does for its own retrospectives (RETRO-APPLY-LEARN2, RETRO-APPLY-L). Check first whether the other account's own apply job has claimed it in ROOM.md to avoid the duplicate-apply LEDGER.md already logged once (RETRO-APPLY-L: "both parents applying the same LEARN file in the same hour is a duplicate to avoid"). |
| 2 | **`tools/room.py --push`'s path-list argument has no flag validation, so a stray flag (e.g. `-m "message"`) passed after `--push` is treated as a literal git path and silently drops the real paths from the commit** — a live, self-reported incident, and a different bug from the "unknown leading option" fix that landed this window. | LEDGER.md (PR-LAND row, 25 Sept): "a stray -m flag to room.py --push drops the paths; the tool should refuse unknown flags." Checked `tools/room.py`'s `main()`: the `if a[0] == "--push": return push(msg, a[1:])` branch passes everything after `--push` straight to `push()` as `paths`, with `push()`'s only later use being `sh("git", "add", "--", *paths)` — no check that each path exists or doesn't start with `-`. The unknown-leading-option fix that did land this window (`f421d8a`, V7-QA4) only fires when `a[0]` itself (the very first CLI argument) is an unrecognized flag; it never runs for `--push`, since that branch matches and returns before the generic `a[0].startswith("-")` check is reached. `git log --all --oneline \| grep -i "stray\|room.py"` confirms only `f421d8a` and its own ROOM note landed — the `--push` path-list gap is still open. | Yes — same class of tool-hygiene fix as the leading-option guard just added, and the same tool. | `tools/room.py`'s `push(message, paths)`: before `git add`, reject any `paths` entry starting with `-` (or that doesn't exist on disk after `git add --` fails) with a clear error rather than silently including it as a pathspec. One line: `bad = [p for p in paths if p.startswith("-")]` then `if bad: print(f"room.py --push: refusing flag-like path(s) {bad!r} -- pass paths only, e.g. tools/room.py --push file1 file2"); return 2` before the `git add` call. Add a `tools/tests/test_room_push_flag.py` case mirroring `test_room_unknown_flag.py`'s shape. |
| 3 | **The "Cost and time" paragraph duplicated verbatim (items 14/15) has now recurred a third time**, copied forward into a fresh lane's COMMON file across three consecutive dates. Passes 1 and 2 each found it once (`-lane-zx-COMMON.md`, then `-lane-zx2-COMMON.md`) and explicitly declined to escalate it ("worth a one-line fix next time that file is touched, not a LEARN finding"). It has now recurred a third time in `-2026-09-26-lane-ax-COMMON.md`, meaning "next time that file is touched" never happened — each new lane's COMMON file is drafted by copying the previous one forward, carrying the duplication along. | `.claude/briefs/runs/2026-09-26-lane-ax-COMMON.md` (two identical "Cost and time (added 09:05 UTC 25 Sept 2026...)" paragraphs, back to back); same duplication independently confirmed still present in `-lane-zx-COMMON.md` and `-lane-zx2-COMMON.md` (`grep -c` returns 2 for all three files, 6 total). | Yes, and per CLAUDE.md Usage 8a ("a rule that the ledger shows broken twice gets a mechanical check... and its prose shrinks to one line"), a copy-paste bug caught twice by name and still recurring a third time is exactly the threshold that rule sets for a mechanical check rather than a third passive note. | Two moves: (a) fix `-2026-09-26-lane-ax-COMMON.md` now (delete the duplicate paragraph) since this pass found it live; (b) add a one-line mechanical check alongside the other Usage-8a precedents (`tools/intake_gate_check.py`, `tools/room.py`, `tools/ledger_check.py`, `tools/family_run.py`, `tools/near_check.py`) — a small script that greps each `.claude/briefs/runs/*-COMMON.md` for any paragraph appearing twice verbatim (a duplicate-line detector over blank-line-separated blocks) and exits non-zero, run once when a new dated COMMON file is committed. Cheaper than the tool option: since these files are template-copied, a would-be fix could also just be "diff a new dated COMMON file against its most recent same-lane-type predecessor before committing it" — but that is a habit, not a mechanism, which is exactly what recurred here twice already. |

## 2. What we have that they lack

Checked the owner-account's `-2026-09-26-lane-ax-COMMON.md` and `parent.md` against the other account's current
lane briefs (GOLD3, GOLD-c4 job briefs): nothing owner-account-only found this window beyond item 3's own COMMON-
file bug (which is a liability, not an asset). `tools/key_livecheck.py` (owner-account tool, live per-credential
test calls) remains ahead of the other account's `key_probe.py` (presence-only) in capability, but this is not new
— RETRO-2026-09-26a itself documents both were kept by design with no functional overlap (its item 3), so this is
confirmed-mutual-knowledge, not a fresh gap to report.

## 3. Conflicts

None found. RETRO-2026-09-26a's five proposals are all extensions or fixes to shared tooling, not disagreements
with anything on the owner-account side; no target overlap surfaced (GOLD3 holds koehler-1944/debosnys-1883/
kaliningrad-2015, LANE AX holds the WVO Nassau pair, antt-msliv0638-brochado-1712, pro3055-clinton-1779 — disjoint,
as every prior pass has also found).

count: 3 portable items (1 apply-a-pending-retrospective diff bundle, 1 clear tool-hygiene diff, 1 recurring-bug
diff with a mechanical-check option per Usage 8a), 0 reverse-direction gaps, 0 conflicts.
