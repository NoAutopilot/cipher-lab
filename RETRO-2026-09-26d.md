# Retrospective p, 26 Sept 2026 (Sonnet, cap $10 / 40 min, parent 7f)

Window: LEDGER.md rows after "Retrospective o: 14 ledger rows since retro n" (RETRO-2026-09-26c.md, line 926) through
the last row at this session's start -- 23 rows, roughly USD 135 of worker usage across both accounts (LANE AX's
close and its last workers AX-REDERIV2 3.62 and AX-COMP2 16.07 at 2x cap, LANE B6's twelve workers and close, LANE
B7 opening and six workers so far, LANE V8's two audits (V8-NA5797-2 3.86, N4 held) and V8-NA172 (172 N4), parent
workers RETRO-APPLY-O 1.42, DECODE-ACCESS 2.43, PR-LAND-2 1.99, and parent 7e's 34.38 covering the hand-over
incident). `date -u` read 05:22:18 UTC (start) and 05:33:26 UTC (this line). `python3 tools/ledger_check.py` run
fresh (not run by retro o or RETRO-APPLY-O since their own window): flags four duplicate-session-id pairs at or
before line 934 (B5/V7/GOLD3, already covered by RETRO-2026-09-26c item 4 and its LANE-B6-orchestrator twin at
930/934, all written before RETRO-APPLY-O's fix landed at line 935) and nothing new after it -- the ledger-check-
before-self-ledger rule has not yet been tested by a lane close in this window (LANE AX, LANE B6, LANE V8 all
closed/checked in after 935 without a duplicate), so "does it hold" stays open, not confirmed, for the next
retrospective to check. ROOM.md digest from 03:55 (35 of 2604 lines, `python3 tools/room.py --digest`); no QA file
newer than QA/2026-09-26-0308.md (QA7 is scheduled 05:40, after this session); UPDATES.md rows since 03:55 (the
04:20 RETRO-APPLY-O rows and the 05:17 LIVE/ARCHIVED title rule); NEAR.md all 9 active rows; the LANE B6 (closed),
LANE B7 (live), LANE AX (closed) and STATUS.md "Parent handoff" 7f section (line 655) handoffs; hub-seed/
SUCCESSOR-PROMPT.md's 7e block was not separately reachable as a distinct "block" heading in the working copy --
its content is covered instead by STATUS.md line 655's own account of the take-over and the ROOM.md lines at
04:48-05:17 that narrate the same incident, cited below.

## 1. The hand-over gap: archive-on-takeover is now prescribed but unverified, and the ROOM-only ask to
   `tools/orphan_check.py` will not survive past this one worker unless it is in the tool's own checklist

STATUS.md line 655 shows parent 7f took over at 04:46-04:48 UTC, before the owner's 05:00-05:10 messages into the
retired 7e -- so the gap was not a slow hand-over, it was that no rule yet existed requiring 7f to archive 7e as
its first act (the LIVE/ARCHIVED title-and-archive-on-takeover rule was written at 05:17, *after* this incident, in
response to it: UPDATES.md, parent.md "Handing over"). That fix is now prescriptive ("as its first act after its
ROOM take-over line renames its predecessor... and archives it") but has no verification step: nothing confirms
the `archive_session` call actually took effect before the successor moves on, so a failed or skipped call would
reproduce the same gap silently, with the same "an archived session is read-only" assumption doing the work
untested. Separately, `tools/orphan_check.py` (brief `.claude/briefs/runs/2026-09-26-parent-orphan-tool.md`,
ORPHAN-TOOL session_01LV9zxH7aPvDF6oEzJuKNDe, in flight, not yet landed as of this session) was asked by a ROOM.md
aside (05:17, parent 7f: "Add one check if cheap: a non-archived session whose title does not start with LIVE...")
to cover exactly this -- but that ask lives only in ROOM.md, not in the brief's own item-2 checklist (a-e) or in an
offline-test fixture requirement; if the worker runs short of budget before adding it, the next orchestrator swap
gets no benefit, because Usage 8a's "rules become tools" pattern only holds once the check is *in* the tool, not
asked of one instance of it.

```diff
--- a/.claude/briefs/parent.md
+++ b/.claude/briefs/parent.md
@@ ## Handing over
 **Session titles say LIVE or ARCHIVED (owner, 26 Sept 2026, 05:1x UTC, after replying into the retired parent 7e by mistake; both accounts).** Every session a parent or lane creates carries the prefix `LIVE ` in its title from `create_session`. The session that archives it renames it first: `ARCHIVED <old title> (done <clock time>, $<cost> <code>)`, then `archive_session`. A parent taking over titles itself `LIVE parent <x> · Orchestrator <N> (talk to this one)`, and as its first act after its ROOM take-over line renames its predecessor `ARCHIVED parent <w> · Orchestrator <N-1> (handed over <clock time>; do not message)` and archives it -- an archived session is read-only, so a message typed into it by mistake cannot start a second parent. A non-archived session whose title lacks `LIVE ` is a flag for the orphan check (`tools/orphan_check.py`). Lane orchestrators do the same for their workers.
+
+**Verify the archive took (26 Sept 2026, RETRO-2026-09-26d).** The rename-and-archive step above is prescriptive,
+not self-checking: `archive_session` can fail or be skipped with no error the successor notices. As part of the
+same first act, the successor re-reads the predecessor's session state (`list_sessions` or `get_session` on its
+id) and confirms the title now starts `ARCHIVED`; if it does not, the successor writes a ROOM.md flag naming the
+still-live predecessor immediately, rather than assuming the call worked. This is the gap the LIVE/ARCHIVED rule
+itself does not close: the owner replied into 7e at 05:00-05:10 UTC before this rule existed at all, so no session
+had a chance to confirm 7e's state either way -- closing the verification half now means the next hand-over is
+caught by a session, not by the owner noticing.
```

```diff
--- a/.claude/briefs/runs/2026-09-26-parent-orphan-tool.md
+++ b/.claude/briefs/runs/2026-09-26-parent-orphan-tool.md
@@ ADDENDUM 05:12 UTC (owner, 05:10): the check applies to every account. [...]
+
+ADDENDUM (RETRO-2026-09-26d, 05:33 UTC): make the 05:17 ROOM.md ask ("add one check if cheap") a formally named
+item, not only a message this one worker instance might or might not reach before its own cap: item 2 gets check
+(f) TITLE MISMATCH -- a non-archived session whose title does not start with `LIVE ` (parent.md's own convention),
+or an archived session whose title does not start with `ARCHIVED`. Add a fixture for it to
+tools/tests/test_orphan_check.py before landing, and fold the count into the summary line ("orphans: N sessions, M
+triggers, K claims, J unledgered, L title-mismatches"). If ORPHAN-TOOL already added this per the 05:17 ask, this
+addendum only confirms the fixture and summary-line wording match; it is not a conflicting instruction.
```

Motivating rows: STATUS.md line 655 ("7f, took over; clock 04:45-04:48"); ROOM.md 05:08/05:09/05:15/05:17 lines (the
incident narrative and the 05:17 verbal ask to ORPHAN-TOOL); UPDATES.md 05:17 row; `.claude/briefs/runs/
2026-09-26-parent-orphan-tool.md` item 2 (checks a-e, no title check named).

## 2. Worker session ids hand-typed into a ROOM.md role field are unchecked, though the check is one environment
   variable away -- confirmed against this session's own id, not guessed

`CLAUDE_CODE_REMOTE_SESSION_ID` is set in this container as `cse_01Gny4j6pxVrzWP5F9wXjDdE`; this session's own
attribution footer (and this session's own ROOM.md claim/done lines, e.g. the 05:22 line "retrospective p spawned
(session_01Gny4j6pxVrzWP5F9wXjDdE)") confirm the mapping is exactly `cse_<id>` -> `session_<id>`, the same
`session_...` form used throughout ROOM.md and LEDGER.md. A ROOM.md role field is self-referential by convention
("parent worker X (session_...)", "retrospective o (Sonnet, session_...)"), so any `session_...` id it names should
equal the appending session's own id -- but `tools/room.py`'s append path takes the role string as free text and
checks nothing about it. DECODE-ACCESS's done line (26 Sept 2026, LEDGER.md: "ROOM done line carried the wrong
session id (7e's)") named `session_01SvjMDFfJZJ3uK47RYxAQrM` (7e, the parent) instead of its own
`session_01AQwk5zWLCGvxyuPFhwaHDD` -- exactly the shape this check catches at append time, before it reaches
LEDGER.md or a successor's hand-over prompt. The second incident named in this session's brief (7e's
SUCCESSOR-PROMPT carrying DECODE-ACCESS's id with one character dropped) is a different file, not a ROOM.md role
field, and is a single occurrence there -- per this repo's own "a rule broken twice gets a tool" threshold (Usage
8a), it does not yet earn a mechanical fix; the honest next step if it recurs is to have that field copy-pasted
from the source ROOM/LEDGER row rather than retyped, not a new tool now.

```diff
--- a/tools/room.py
+++ b/tools/room.py
@@ def warnings_for(role, signal):
-def warnings_for(role, signal):
+_UNSET = object()
+ROLE_SESSION_RE = re.compile(r"\bsession_[0-9A-Za-z]+\b")
+
+
+def _own_session_id():
+    """This session's own id in the 'session_...' form used throughout ROOM.md/LEDGER.md, derived from
+    CLAUDE_CODE_REMOTE_SESSION_ID ('cse_<id>' -> 'session_<id>'; confirmed 26 Sept 2026, RETRO-2026-09-26d,
+    against this session's own attribution footer, not guessed). None when the variable is absent or in an
+    unexpected form -- callers must then skip the check, not treat it as a mismatch."""
+    v = os.environ.get("CLAUDE_CODE_REMOTE_SESSION_ID", "")
+    return "session_" + v[4:] if v.startswith("cse_") else None
+
+
+def warnings_for(role, signal, own_id=_UNSET):
     """Warnings printed before a line is appended (25 Sept 2026, UPDATES.md; the line is still appended).
 
     - a done line that reports a test, negative or FAIL without the word control: CLAUDE.md rule 3 (a negative
       means nothing without a matched control number beside it; nine done lines broke this on 25 Sept 2026)
     - a dollar figure anywhere in the line: cost figures come from the orchestrator's get_session, not the
       worker's own sense of it (README common tail item 1), and `$8` inside double quotes vanishes anyway.
     - a done line over 1,200 characters: CLAUDE.md Usage rule 5 ("a short markdown table with a five-line
       report") -- 17 of 23 done/flag lines this window (26 Sept 2026, retro n) exceeded 800 characters and one
       (GOLD-CONS4) reached 4,021, and a lane orchestrator now reads every one of these before its own context
-      handoff line, so a verbose line has a direct, measurable cost in lane-handoff overhead, not just legibility."""
+      handoff line, so a verbose line has a direct, measurable cost in lane-handoff overhead, not just legibility.
+    - a role field naming a session_... id that is not this session's own (CLAUDE_CODE_REMOTE_SESSION_ID, when
+      set): the role field is self-referential by convention, so any session id it names should be this
+      session's; DECODE-ACCESS's done line named 7e's id instead of its own (26 Sept 2026, LEDGER.md), which this
+      would have caught at append time. `own_id` is for the offline test only; real callers get it from the
+      environment."""
+    if own_id is _UNSET:
+        own_id = _own_session_id()
     w = []
     sig = signal.strip()
     if sig.lower().startswith("done:") and re.search(r"\btests?\b|\btested\b|\bnegatives?\b|\bfail(s|ed|ing)?\b|closed-negative",
                                                      sig, re.I) and not re.search(r"\bcontrols?\b", sig, re.I):
         w.append("WARNING: done line reports a test or negative without a control number (rule 3)")
     text = f"{role} | {signal}"
     if re.search(r"\$\s?\d|\bdollars\b|\bUSD\b", text, re.I):
         w.append("WARNING: cost figures in ROOM lines are the orchestrator's to read (COMMON item 1)")
     if len(text) > 1200:
         w.append(f"WARNING: done line is {len(text)} chars, over Usage rule 5's five-line report -- move detail "
                   f"to NOTES.md/HYPOTHESES.md and leave a short summary + pointer here")
+    if own_id:
+        named = ROLE_SESSION_RE.findall(role)
+        if named and own_id not in named:
+            w.append(f"WARNING: role names {named[0]}, not this session's own id ({own_id}) -- a role field is "
+                      f"self-referential; check before appending (DECODE-ACCESS, 26 Sept 2026)")
     return w
```

New file `tools/tests/test_room_session_id.py`:

```python
#!/usr/bin/env python3
"""Offline test for tools/room.py's role-session-id check (26 Sept 2026, RETRO-2026-09-26d): a ROOM.md role
field is self-referential ('parent worker X (session_...)'), so any session_... id it names should be the
appending session's own, taken from CLAUDE_CODE_REMOTE_SESSION_ID ('cse_<id>' -> 'session_<id>', confirmed
against this retrospective's own id). DECODE-ACCESS's done line (26 Sept 2026, LEDGER.md) named a different
session's id instead of its own, undetected until a human read the ledger. Run:
python3 tools/tests/test_room_session_id.py"""
import os, sys

ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.path.insert(0, os.path.join(ROOT, "tools"))
import room  # noqa: E402


def test_own_session_id():
    old = os.environ.get("CLAUDE_CODE_REMOTE_SESSION_ID")
    try:
        os.environ["CLAUDE_CODE_REMOTE_SESSION_ID"] = "cse_01Gny4j6pxVrzWP5F9wXjDdE"
        assert room._own_session_id() == "session_01Gny4j6pxVrzWP5F9wXjDdE"
        os.environ["CLAUDE_CODE_REMOTE_SESSION_ID"] = "not-a-cse-id"
        assert room._own_session_id() is None
        del os.environ["CLAUDE_CODE_REMOTE_SESSION_ID"]
        assert room._own_session_id() is None
    finally:
        if old is None:
            os.environ.pop("CLAUDE_CODE_REMOTE_SESSION_ID", None)
        else:
            os.environ["CLAUDE_CODE_REMOTE_SESSION_ID"] = old


def test_warnings_role_session_mismatch():
    W = room.warnings_for
    own = "session_01AQwk5zWLCGvxyuPFhwaHDD"
    ws = W("parent worker DECODE-ACCESS (Sonnet, session_01SvjMDFfJZJ3uK47RYxAQrM)", "done: verdict account-wide",
           own_id=own)
    assert any("session_01SvjMDFfJZJ3uK47RYxAQrM" in w and own in w for w in ws), ws
    assert W(f"parent worker DECODE-ACCESS (Sonnet, {own})", "done: verdict account-wide", own_id=own) == []
    assert W("LANE B7 orchestrator", "for the parent: check-in", own_id=own) == []
    assert W("parent worker X (session_ANYTHING)", "done: x", own_id=None) == []
    print("ok room session-id check: 4 cases + _own_session_id 3 cases")


if __name__ == "__main__":
    test_own_session_id()
    test_warnings_role_session_mismatch()
```

Motivating rows: LEDGER.md DECODE-ACCESS row ("ROOM done line carried the wrong session id (7e's)"); STATUS.md line
655 ("the successor prompt's id had a dropped character, corrected"); this session's own environment
(`CLAUDE_CODE_REMOTE_SESSION_ID=cse_01Gny4j6pxVrzWP5F9wXjDdE`) and ROOM.md 05:22 line, checked directly, not
assumed.

## 3. A per-leaf recovery gate does not survive the merge step -- Szembek BK 1560's own numbers show two of three
   leaves tie their control, and the merge (bSZM) is live now, not hypothetical

Three leaves of szembek-bk1560 were each transcribed and gated independently against the same rule-3
shuffle-consistency check (does a recurring code get the same gloss wherever it recurs, against 1000x shuffled
pairings). Leaf 66 clears its control decisively: real consistency 0.918 vs shuffle mean 0.306 (p95 0.341), on 87
of 168 tokens glossed (bSZL66, 26 Sept 05:04). Leaves 65 and 67 do not: leaf 65 reads real 0.0 vs shuffle mean
0.0/p95 0.0 on 21 occurrences (bSZL65, 05:02), and leaf 67 reads real 0.429 vs shuffle 0.429 on 18 occurrences
(bSZL67, 05:06) -- both a tie with their own control, the "non-discriminating control" shape CLAUDE.md rule 3
already names for bMAT2's M/U rule, not a pass. LANE B7's orchestrator dispatched the merge worker (bSZM) at 05:19
UTC with no per-leaf gate named in either the LANE AX/B7 brief line ("one Sonnet worker per leaf... control: a
shuffle-consistency check... If the key reads the groups, post...") or a merge-specific instruction: nothing stops
a code attested only on leaf 65 or 67 from entering the shared key.tsv at the same grade C as a leaf-66 code, though
only leaf 66's gloss has been shown to be more than chance. This is flagged live in ROOM.md (this session, 05:33
UTC) for LANE B7 to act on directly; the mechanical fix is a rule-3 addition so the next recovery brief with a
multi-unit merge states the same requirement without needing a live flag to catch it.

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ rule 3, after the "control that cannot vary on the same axis" paragraph (ends "...the test is a non-test, not a negative.")
+   A per-unit (leaf, page) recovery signal merged into one shared key inherits the weakest unit's unchecked
+   status unless each unit clears its own control before the merge. Lesson of 26 Sept 2026 (Szembek BK 1560,
+   bSZL65/66/67, LANE B7's bSZM merge in progress): three leaves' interlinear-gloss pairs were gated by the same
+   rule-3 shuffle-consistency check on the recurring-code/recurring-gloss pairing -- leaf 66 clears it decisively
+   (real 0.918 vs shuffle mean 0.306, p95 0.341, 87/168 glossed) but leaves 65 and 67 TIE their own shuffle
+   control exactly (0.0 vs 0.0 on 21 occurrences; 0.429 vs 0.429 on 18) -- non-discriminating at that N, the
+   bMAT2 shape above, not a pass. The recovery brief's merge step (interlinear_align.py -> key.tsv) named no
+   per-leaf gate: nothing stops a code attested only on a leaf that tied its control from entering the shared
+   key.tsv at the same grade as a leaf that beat its control decisively. Before folding a unit's glosses into a
+   shared key, check that unit's own control result first, the same way family_run.py already checks a family's
+   control before running the target (Usage item 8) -- a code attested only on a unit whose own control tied or
+   failed is held pending more occurrences or corroboration from a unit that cleared, not merged as equally
+   supported.
```

Motivating rows: ROOM.md 05:02 (bSZL65), 05:04 (bSZL66), 05:06 (bSZL67), 05:19 (LANE B7 orchestrator, "merge bSZM
live"), 05:33 (this session's live flag); `.claude/briefs/runs/2026-09-26-lane-ax-orchestrator.md` line 6 (the
merge instruction as written, no per-leaf gate).

## 4. A per-unit box priced by page count, not by subagent-call count, undercounts by the passes-per-unit
   multiplier -- AX-COMP2's overrun was a dollar-costing gap, not a time-pacing failure

AX-COMP2's brief priced six pages at an $8 cap and "about 12 minutes per page," and correctly applied this
section's own GOLD-4D/AX-4612TR fix (one page's line crops per subagent call, never a full page). Its actual cost
was 16.07, 2x the cap, ledgered D-. Time pacing was not the problem: the worker ran about 50 of its 90-minute box
(04:05-04:55 UTC), well under the 80% self-stop line throughout -- the wall-clock box did its job. The gap is
dollars: the transcription protocol for each page is two independent blind passes, so finishing six pages took 11
Sonnet subagent calls (one page's second pass already on file from the prior worker), not six. 16.07 / 11 = 1.46
dollars per call -- almost exactly what the $8-for-6-pages estimate implied per page (1.33) understated by the
2x-passes-per-page factor the brief's cost line never carried, because it counted the unit as "page," not "pass."
This is the same undercounting shape this section already names for GOLD-4D/AT55V/MEYE/GOLD-K2 (a box sized in
round units without pricing the thing that actually drives cost) but a new instance of it: here the driver is
passes-per-unit, not image resolution or verbosity, and it is invisible to time-based pacing specifically because
two concurrent Sonnet calls can finish inside the same wall-clock window that one call would have taken, while
still costing twice.

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ Usage rule 6, after the GOLD-K3 CPU-sharing paragraph, before the AX-4612TR/bUNT7 crop-tool paragraph (or after it; both are unit-costing lessons)
+   A per-unit box priced by page or leaf count undercounts by the passes-per-unit factor when a unit's own
+   protocol calls for more than one subagent call per unit. Lesson of 26 Sept 2026 (AX-COMP2, lodewijk-van-
+   nassau-1573-74 7205): the brief priced six pages at an $8 cap and "about 12 minutes per page" -- a page-count
+   estimate -- and correctly scoped each subagent call to one page's line crops (this section's own GOLD-4D/
+   AX-4612TR fix). But each page needs two independent blind passes, so six pages took 11 Sonnet subagent calls,
+   not six: cost was 16.07 (2x the $8 cap), while time pacing was fine throughout (about 50 of a 90-minute box,
+   under the 80% self-stop line at every point) -- the wall-clock box cannot catch this, because passes-per-unit
+   is a multiplier on dollars a worker cannot see mid-session, not on time it can (16.07 / 11 = 1.46 dollars per
+   call, matching the $8-for-6-pages estimate's implied per-page rate almost exactly once the 2x factor is
+   applied). When a unit's own protocol requires N independent passes (a reconciliation, a second blind eye),
+   this section's own "state the unit count and the per-unit estimate in the brief itself" already requires a
+   number -- that number must be per pass (subagent call), not per page or leaf, before multiplying by the
+   planned count.
```

Motivating rows: LEDGER.md AX-COMP2 row (16.07, D-, "2x its 8-dollar cap on a job that was mostly transcription");
ROOM.md 04:05 (claim) and 04:55 (done: "all 6 pages transcribed... 11 blind-pass Sonnet subagents, at most 2 at
once"); `.claude/briefs/runs/2026-09-26-lane-ax-comp2.md` ("about 12 minutes per page").

## 5. JSTOR verifier rows ran only the sender+date+cipher-keyword query family, never the quoted-phrase family
   the verifier brief's own step 2 already names -- and the keyword family found nothing on two targets

JSTOR-QUEUE.tsv rows 88-91 (lodewijk-van-nassau-1573-74) and 80-83 (espagnol142-mercy-1648) all combine 4-6 ANDed
clauses -- name variants, a year, a place, and a cipher keyword (Chiffre/entziffert/cipher/cijfer, or
Spain/Leopold-style terms) -- and came back "no relevant hit... none about the letter" on every row (PR-LAND-2,
26 Sept 05:17). JSTOR's own search is exact-phrase; requiring a cipher keyword in the same result as a name and a
year assumes the secondary literature calls the correspondence a cipher at all, which a work that merely quotes or
discusses the letter's content need not do. CLAUDE.md's verifier brief template step 2 already names "quoted
phrases" as one of four search axes (alongside date, sender+recipient, and ciphertext words/identifiers) and this
target's own team already uses phrase-search-with-a-positive-control successfully elsewhere for the very same
letters (V8-NA5797, 26 Sept 04:09: "Phrase search with positive control (Groen text found): 0 hits on the name
phrases" against archive.org/be-api; V8-NA172, 05:17: "Phrase search control found Groen p6") -- but neither target
has run that same method as a JSTOR row. The JSTOR-specific instruction in item 2(g) only names the sender/keyword
family, so a verifier reaches for it and never writes the phrase-only row.

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ Verifier brief (template), step 2(g)
-   Scholar API with the keys of access playbook item 3, Persée, HAL, CrossRef, Google Scholar when reachable) and, for JSTOR, a row per query appended to
-   `JSTOR-QUEUE.tsv` for the owner's local runner; a queued JSTOR row never blocks N3 or N4 on its own (24 Sept 2026,
-   the owner is not the bottleneck at fifty sessions). Log each family
-   as searched or unreachable, with what was searched.
+   Scholar API with the keys of access playbook item 3, Persée, HAL, CrossRef, Google Scholar when reachable) and, for JSTOR, a row per query appended to
+   `JSTOR-QUEUE.tsv` for the owner's local runner, in two families per target: (i) sender/recipient/date/place
+   ANDed with a cipher keyword, and (ii) a distinctive phrase quoted exactly from the plaintext or from the
+   printed edition's own wording, with NO cipher keyword required -- JSTOR's search is exact-phrase, and a
+   secondary work that discusses or quotes the letter may never call it a cipher at all. Lesson of 26 Sept 2026:
+   JSTOR-QUEUE rows 88-91 (lodewijk-van-nassau-1573-74) and 80-83 (espagnol142-mercy-1648) ran only family (i) and
+   came back with no hit about either letter; neither target had run a bare quoted-phrase row, though the same
+   phrase-with-positive-control method already works for these exact letters against archive.org/be-api
+   (V8-NA5797, V8-NA172). A queued JSTOR row never blocks N3 or N4 on its own (24 Sept 2026,
+   the owner is not the bottleneck at fifty sessions). Log each family
+   as searched or unreachable, with what was searched.
```

Motivating rows: JSTOR-QUEUE.tsv rows 80-83, 88-91; ROOM.md 05:17 (PR-LAND-2, "no relevant hit about the letter"
for both clusters); ROOM.md 04:09 (V8-NA5797) and 05:17 (V8-NA172), the phrase-with-positive-control method already
in use against archive.org for these same targets.

## Near solves (review, per brief)

All 9 active NEAR.md rows read.

**matignon-mayenne-1586: the row's own bookkeeping columns are stale even though the underlying work is current --
a data-hygiene gap, not a staleness-of-substance one.** bMAT1D (26 Sept 04:59, LANE B7) ran both open next steps
named in the row's "What would settle it" column: (1c') reverted bMAT1C's S grades (judge back to -1.371, cover
0.814) and (1d) tested the U signs as nomenclator code words against the crib pair, which failed its own
known-answer control (6/20, 4/20 vs a 16/20 gate) -- a closed non-test, not a negative. The row's Evidence cell was
updated with this (the "**Revert + U code words (B7 bMAT1D...)**" paragraph is on file), but the "What would settle
it" column still lists (1c') and (1d) as open work with cost estimates, the "Lane / owner" column still reads "next
breadth lane (B6)" though the work ran in B7, and the "Last touched" column still reads "26 Sept 2026 02:45,"
before bMAT1D ran at 04:59. `tools/near_check.py` does not catch this: its staleness check compares "Last touched"
against now (>48 hours), not against the latest dated paragraph actually embedded in the row's own Evidence text,
so a row can be internally inconsistent (a next step both "still open" and "already run" in the same cell) without
tripping any check, so long as the wall-clock gap from the (wrong) Last-touched value to now stays under 48 hours.
Not proposed as a sixth diff (the cap is five and items 1-5 above are stronger-evidenced); worth a mechanical check
in near_check.py (parse the latest bolded "(... , DATE TIME)" marker in each row's Evidence cell and flag a
Last-touched value earlier than it) if a second row shows the same drift. The correct next step for this row today
is (1e), unchanged from before bMAT1D: hand-align f.78v/79r's 17 U signs against the period decipherment with a
20-H known-answer control first, in LANE B7 (not B6).

**untersberg-code**: touched this window (bUNT8, 26 Sept 04:14: reconciled 76/110 vs Herzog, symA confirmed at all
5 positions, matches no Cappelli form); its own next step (symA against the other witnesses) is now dispatched as
bUNT9 (claimed 05:21, this session's tail). Not stale; moving as named.

**antt-msliv0638-brochado-1712 letter 134**: unmoved across two prior retrospectives' worth of window (last
activity AX-BRO3, 25 Sept 00:37), but as of this session's tail (05:22 UTC) LANE AX2 dispatched AX2-BRO4 ("thin
codes x/z/d/f/16/9 by chunk alignment, interlinear_align.py") -- close to but not verbatim the row's own named next
step ("re-segment the 34 drifted appendix entries"); too recent to grade (claimed one minute before this line was
written). Flag for the next retrospective to confirm AX2-BRO4's numbers land in the row.

**espagnol142-mercy-1648, fr2933-salviati-1525, koehler-1944, mccormick-1999, bullet-tuscany-1944, blitz-ciphers**:
none touched this window; none stale under the 48-hour rule (koehler correctly PAUSED per GOLD3's standing
idle-hold, ASKS 53/55 unchanged; the other four last moved 25 Sept, all still same-day-adjacent). fr2933's
diagnostic-first next step (extend R8-DSN's error sweep to 10-14%) has now gone three retrospectives untouched
(retro m, n, and this one) -- worth a worker dispatch, not a fourth mention, if a fifth retrospective finds it
still open.

## What did not need a change

Rules 1, 2, 6, 7, 8, 9, 9a, and the Outreach gates: untouched by anything else in this window. Rule 4 held
everywhere checked (bMAT1D's H/M/U counts and grade discipline on the failed control; V8-NA5797/V8-NA172's H/M
grading with sourced gloss citations). Rule 10: clean everywhere checked (V8-NA5797/V8-NA5797-2/V8-NA172's safe
sentences all use "no prior decipherment located" and name the key source per the 25 Sept amendment; AX-MERGE3's
"period decipherment" language is about the 4614 source gloss, not a claim about our own work). Credentials: no
mishandling found in this window's ROOM.md/NOTES.md diffs; DECODE-ACCESS's addressee fix (decode@stp.lingfil.uu.se
-> beata.megyesi@ling.su.se) is a public staff-page address, not personal data. Absolute dates: `date -u` read at
this session's own start and re-checked before this line.

Retrospective p done: 5 proposals, file RETRO-2026-09-26d.md.
