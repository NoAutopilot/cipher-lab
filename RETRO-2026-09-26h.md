# Retrospective t, 26 Sept 2026 (Sonnet, cap $10 / 40 min, parent 7h)

Window: LEDGER.md rows after "retrospective s" (line 1044, 10:51) through the last row at this session's start
(line 1065, LEARN-8) plus LANE ARM2's ARM-S3 (not yet ledgered at start; its own ROOM done line 13:37 and 7h's
13:40 line, get_session 12.67 against a 6 cap, used in place of a ledger row per the brief). `date -u` read
13:43:11 UTC. `python3 tools/room.py --start`: key_probe.py --sync 16 set; key_livecheck.py 9 present/6 working,
last probe 13:40 UTC. `git fetch --deepen=400` run (depth-50 clone). ROOM.md digest from 10:40 (34 of 2923 lines)
read in full, extended to the tail (2926 lines) for the 13:37-13:43 lines the digest's own pattern list does not
catch (ARM-S3's done line, bSALR2's claim, 7h's 13:40/13:42 check-in lines, this session's own claim). LEDGER.md
rows 1045-1065 read directly (STATUS.md handoffs and ROOM digest both cross-checked against the same figures).
QA/2026-09-26-1127.md and -1335.md read in full. RETRO-2026-09-26g.md read in full (applied by RETRO-APPLY-S
11:33-11:41; not repeated). NEAR.md's fr2933-salviati-1525 and malsburg-hessen-1636 rows read directly (grep, not
the whole file -- both rows are current as of this window's own last edits, bSALR/bMALX). STATUS.md's LANE ARM,
NX, V9, B11 and B10 handoffs read in full; LANE ARM2's own STATUS.md row not yet written (still live) --
its state is read from ROOM.md and the ARM handoff's "What ARM2 runs first" list instead. LEARN-2026-09-26-1252.md
and -1313.md (LEARN4, LEARN-8) read in full. `tools/ledger_check.py` and `tools/room.py`/`tools/print_check.py`/
`tools/intake_gate_check.py` read directly for the diffs below. ASKS.md grepped for "armstrong" (0 hits).

## 1. LEARN4's editor's-note phrase sweep (SCOUT-OWN-3) is confirmed portable and still unapplied to `scout.md`

SCOUT-OWN-3 (12:07 UTC, this window) found two candidates (D1, D2/D3) by searching Google Books for an editor's own
"not deciphered" annotation phrase ("cifra no descifrada" and its cognates) rather than for numerals in OCR --
LEARN4 (12:54) confirmed this is a genuinely different, reusable harvesting axis, not yet in `scout.md`,
`tools/print_check.py`, or `check-solved.md` on either account (grepped, no hits), and proposed porting it.
Nobody has applied the diff; the brief for this retrospective names it explicitly for inclusion. This is a scout-
side (candidate-finding) technique, distinct from `print_check.py`'s role (verifying *our own* decoded phrases
against print) -- D1-CHECK's own close (12:57) used exactly this method by hand, not through a script, so the
port is to `scout.md`'s harvest instructions, naming the query family and the languages already tested.

```diff
--- a/.claude/briefs/scout.md
+++ b/.claude/briefs/scout.md
@@ (after "A Top-50 item enters the queue only with its survey rank and the named cheap test...")
+**An editor's own "not deciphered" annotation is a harvest axis the numeral-run detector cannot see (26 Sept
+2026, SCOUT-OWN-3 -> LEARN4).** A printed edition can note in prose that a passage was "in cipher" or "not
+deciphered" while omitting the cipher's own digits or symbols from the typeset page entirely -- the Internet
+Archive numeral-run detector (CLAUDE.md item "Printed ciphertext detector") structurally cannot find these, since
+there is no numeral run in the OCR to match. Run a Google Books phrase sweep, per target language, for the
+editor's own annotation wording alongside the existing numeral-run harvest: "cifra no descifrada" / "cifra
+descifrada" / "cifra no adivinada" (Spanish), "cifra non decifrata" (Italian), "non déchiffré" (French),
+"unentziffert" (German), "not deciphered" (English). A hit is scored like any other candidate (edition, page,
+kind) but flagged "digits may be absent from print, verify with a page-image check before transcription" -- the
+annotation alone does not tell you whether the ciphertext itself survived typesetting (D1, 26 Sept 2026: three of
+three marker phrases showed continuous prose with zero digits across four scans).
```

Motivating: SCOUT-OWN-3-2026-09-26.md; LEARN-2026-09-26-1252.md item "What they use that we do not" row 2;
D1-CHECK's own close (ROOM.md 12:57); this retrospective's own brief, item (6).

## 2. LEARN-8's EMLO manifestation-level Solr note is a stronger check-solved signal than the volume-title
   inference, and is not yet in `check-solved.md` or the Access playbook table

EM3-CHECK (12:56, this window) queried `emlo.bodleian.ox.ac.uk/solr/all/select` directly for the per-letter
`bibo_Note` field and got a verbatim "deciphered interline, and the key to the cipher used" note for two specific
letters in a volume where a third item's own note reads only "implicit cryptographic features" -- a per-item
signal the collection-title method (`williams-1690`'s EM5) cannot distinguish. LEARN-8 confirmed no `emlo`/`solr`
hit in either `check-solved.md` or CLAUDE.md's Access playbook table.

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ (Access playbook host table, after the Bodleian row)
+| EMLO / Bodleian Solr (emlo.bodleian.ox.ac.uk/solr/all/select) | per-manifestation catalogue notes (`bibo_Note`), stronger than the collection-title inference | none | yes, plain GET, reliable | `emlo.bodleian.ox.ac.uk/solr/all/select?q=...` -- query the item's own manifestation record for `bibo_Note`, not just the finding-aid's volume title | ~1.5s apart | QUEUE.md "EM3 check-solved, 26 Sept 2026"; LEARN-2026-09-26-1313.md |
```

```diff
--- a/.claude/briefs/check-solved.md
+++ b/.claude/briefs/check-solved.md
@@ (after the Trew Briefsammlung / aerztebriefe.de paragraph)
+For a target sourced from EMLO (Early Modern Letters Online, emlo.bodleian.ox.ac.uk), query the manifestation's
+own Solr record (`bibo_Note` field) before falling back to the collection-level finding-aid title: the two can
+disagree within one volume (EM3-CHECK, 26 Sept 2026 -- two letters in Bodleian MS Eng. misc. c.382 carry EMLO's
+own note "deciphered interline, and the key to the cipher used" while a third item in the same volume reads only
+"implicit cryptographic features"). A found-solved verdict from this source is grade M (catalogue-based, no
+image online), not H or C, unless an image or transcription is independently opened.
```

Motivating: LEARN-2026-09-26-1313.md item 1; QUEUE.md "EM3 check-solved, 26 Sept 2026"; EM3-CHECK's ROOM done line
(12:56); this retrospective's own brief, item (6).

## 3. ARM-S3 spent four candidate subagent calls after its own calibration step already showed the drift was
   over tolerance -- a calibration check that only writes a caveat, rather than stopping the job, buys nothing

ARM-S3's brief (`2026-09-26-lane-arm2-s3.md` step 2) already required a fresh Taylor calibration run before the
four candidates, with a 0.1-tolerance check against ARM-S2's numbers "if this session's Taylor score differs...
by more than 0.1... every candidate result is reported as non-test at this drift, not an exclusion or an
identification" -- correct instinct, wrong place in the sequence. The freq_score drift was 0.211, over double the
tolerance, discovered at step 2, but step 3 (four candidate subagent calls, about USD 3.6 of the session's 12.67)
ran anyway, because the brief told the worker what to *write* about a failed calibration, not to *stop* before
spending on it. This is the same shape `tools/family_run.py` already enforces mechanically for a statistical
family (control runs first; the target run is refused, not merely caveated, when the control is below gate,
CLAUDE.md Usage item 8) -- ARM-S3's calibration-against-a-known-negative is a control in exactly that sense, just
run by a subagent rather than a script, and nothing generalizes the family_run.py discipline to that shape of job.

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ (rule 3, appended after "not a further family attempt at the same noise levels." and before rule 4)
+   A per-session calibration re-run against a known negative (the family_run.py "control first" discipline,
+   Usage item 8, applied to a job scored by a subagent rather than a script) stops the job before any candidate
+   is scored when the calibration itself misses its own pre-registered tolerance -- the same control-before-
+   target order `tools/family_run.py` already enforces mechanically, not a caveat written after the candidates
+   are scored anyway. Lesson of 26 Sept 2026 (ARM-S3, armstrong-madison-1808): the brief's own step 2 already
+   required a Taylor re-run and a 0.1 tolerance check before the four candidate calls, and the freq_score drift
+   (0.211) was known at that point to be over twice the tolerance -- but the brief told the worker to caveat the
+   result, not to stop, so all four candidate calls ran anyway (about USD 3.6 of the session's 12.67, 2.1x its
+   cap) to produce scores nobody could use. A job brief that scores several candidates against a per-session
+   calibration states the calibration step as a gate ("if the drift exceeds tolerance, stop here, log
+   'non-test at this drift' for the family, and do not run the candidate calls"), not as a caveat to attach to
+   candidate results computed regardless.
```

New test case (for whichever tool later mechanizes shorthand-symbol-match scoring, e.g. an extension of
`score_summary.py` into a small driver script): given a calibration drift above a passed `--tolerance`, the
driver exits before invoking any candidate scoring call and prints "non-test at this drift" -- mirroring
`tools/tests/test_family_run.py`'s existing "CONTROL BELOW GATE" test shape.

Motivating: `.claude/briefs/runs/2026-09-26-lane-arm2-s3.md` step 2; ARM-S3's own done line (ROOM.md 13:37, "shape
drift 0.045 OK, freq drift 0.211 OVER the brief's 0.1 tolerance... every candidate this session is non-test");
this retrospective's own brief, item (2).

## 4. Nothing requires a solver to run `print_check.py` on its own decoded phrases before a "reading ready" line
   -- the verifier found the Morillo print in minutes, after the solver had not looked

V9-MOR (11:37, prior window but the lesson is restated in this window's LANE NX/V9 handoffs and LEDGER row) found
rah-morillo-1817 item 3's plaintext already in print (2021) through `print_check.py`'s global IA full-text pass on
three decoded phrases -- a run the solver lane had not made before posting "reading ready." The tool already
exists and is cheap (`ia-global` needs no per-item setup); the gap is procedural, not technical: nothing in
README.md's common tail or a solver/recovery brief template makes it a precondition of the ROOM line, so it only
ran because V9 happens to run it routinely as a verifier step. A solver posting "reading ready" for a target that
turns out to be already in print costs a verifier's full search-log pass to discover what a five-minute script
run would have shown first.

```diff
--- a/.claude/briefs/README.md
+++ b/.claude/briefs/README.md
@@ (common tail, after the "Per-unit pricing precedents" bullet)
+- **Run `print_check.py` on your own decoded phrases before posting "reading ready" (26 Sept 2026, V9-MOR,
+  rah-morillo-1817 item 3).** A verifier finding the plaintext already in print through `tools/print_check.py`'s
+  `ia-global` pass on the solver's own decoded phrases, when the solver never ran it, wastes the verifier's full
+  search-log pass on something a cheap script check would have shown first. Before the ROOM line that says
+  "reading ready" (or the equivalent status change to a candidate reading), run `tools/print_check.py <target>`
+  on 2-4 distinctive decoded phrases and cite the `ia-global` (and, where a key is present, `gbooks`/`openalex`)
+  result in the done line -- a "no hit" is still worth stating (it is evidence for the verifier's own search
+  log, CLAUDE.md rule 10), not just a "found it" worth reporting.
```

```diff
--- a/tools/print_check.py
+++ b/tools/print_check.py
@@ (docstring, after the existing usage/route description)
+Precondition for a "reading ready" ROOM.md line (26 Sept 2026, V9-MOR): run this on the decode's own distinctive
+phrases before posting that a reading is ready for a verifier, not only at the verifier step -- `--phrases` can
+point at a small ad hoc file of 2-4 decoded phrases when `phrases.txt` does not yet exist for the target.
```

New test case: none needed for the docstring change; the README bullet is a process requirement, checked by
`tools/tests/test_readme_common_tail.py` if one exists (grepped, none does) or, more cheaply, by QA's existing
by-hand item ("reading ready" lines checked for a `print_check.py`/`print-check.tsv` citation nearby) -- a
mechanical version is left as a future retrospective's job if this is missed a second time (CLAUDE.md Usage item
8a's own "twice" bar), since a script gate here would need to parse ROOM.md's free-text done lines, which is a
heavier build than this window's evidence (one instance) justifies.

Motivating: LEDGER.md "LANE V9 V9-MOR" row ("run print_check on decoded phrases before the reading-ready post, not
only at the verifier"); STATUS.md LANE V9 handoff's own "Lesson" line; LEARN-2026-09-26-1313.md item 2 (confirms
the practice is *not yet* a precondition, only an already-adopted habit at the verifier step); this retrospective's
own brief, item (5).

## 5. Two consecutive parent take-over/handoff lines typed an estimated clock time despite the successor prompt's
   own "read the clock first," even after `key_livecheck`'s sibling WARNING already existed to catch it

`room.py`'s `warnings_for` already flags a typed time that disagrees with the line's own auto-generated stamp
(RETRO-APPLY-R, from RETRO-2026-09-26f), and it worked exactly as designed this window: 7h's 13:07 line ("took
over... at 13:11 UTC") was caught and self-corrected one line later. But this is the *second* instance of the
identical mistake in successive parent hand-overs on the same day (7g's own take-over line wrote "at 07:41 UTC"
against its own 07:37 stamp, per the WARNING's own docstring) -- the WARNING catches the error after the line is
already in ROOM.md, requiring a second line to correct it every time, rather than preventing the wrong line from
being written at all. A take-over/handoff line is exactly the shape where no typed digit is ever needed (the
stamp is the timestamp): the successor session has no other content in that line that legitimately needs a clock
time written in words.

```diff
--- a/tools/room.py
+++ b/tools/room.py
@@ def warnings_for(role, signal, own_id=_UNSET):
     m = TYPED_TIME_RE.search(sig)
     if m:
         typed_minutes = int(m.group(1)) * 60 + int(m.group(2))
         stamp_minutes = int(time.strftime("%H", time.gmtime())) * 60 + int(time.strftime("%M", time.gmtime()))
         if abs(typed_minutes - stamp_minutes) > 2:
-            w.append(f"WARNING: signal text says 'at {m.group(1)}:{m.group(2)} UTC' but this line's own stamp is "
-                      f"{time.strftime('%H:%M', time.gmtime())} UTC -- the stamp is authoritative, drop the typed "
-                      f"time or use '(clock read)' with no digits (26 Sept 2026, parent 7g takeover line)")
+            msg = (f"signal text says 'at {m.group(1)}:{m.group(2)} UTC' but this line's own stamp is "
+                   f"{time.strftime('%H:%M', time.gmtime())} UTC -- the stamp is authoritative, drop the typed "
+                   f"time or use '(clock read)' with no digits (26 Sept 2026, parent 7g takeover line)")
+            if re.match(r"\s*(took over|take-?over|handoff|hand-off|hands over)\b", sig, re.I):
+                # 26 Sept 2026, RETRO-2026-09-26h: a WARNING here still let 7g's AND 7h's take-over lines into
+                # ROOM.md wrong, each needing a second line to correct -- a take-over/handoff line never needs a
+                # typed digit (the stamp is the only clock it has), so this is a refusal, not a warning.
+                raise SystemExit(f"REFUSED: {msg} -- a take-over/handoff line carries no typed digits, only "
+                                  f"'(clock read)'; rewrite and re-run")
+            w.append(f"WARNING: {msg}")
     return w
```

New test case for `tools/tests/test_room_*.py` (mirroring the existing typed-time test): a signal starting "took
over from ..." with a mismatched typed time raises `SystemExit` rather than returning a warning string; the same
mismatch on a signal starting "done:" still returns the existing warning list, unchanged behaviour. A
"handoff"/"took over" signal whose typed time matches the stamp (or carries no typed time) is unaffected either
way.

Motivating: `tools/room.py`'s own `warnings_for` docstring (both the RETRO-APPLY-R paragraph and the "parent 7g's
own take-over line" example); ROOM.md 13:07 (7h) and 13:07 correction line; V9-QA11 item 51 (confirms the WARNING
mechanism worked as designed, which is why this proposal escalates rather than replaces it -- the WARNING stays
for every other role's typed time); this retrospective's own brief, item (7).

## Answered without a diff

**(Q1) bMALX ran 1.48x its USD 5 cap on a job the README's own "Per-unit pricing precedents" bullet (added by
RETRO-APPLY-S, 11:41, before this brief was written at 12:21) could have priced better.** The bMALX brief's own
"Units and pricing" section estimated "pass A... about USD 1 each, pass B... about USD 0.8 a leaf" as a round
number, with no reference to the precedents bullet at all -- and the bullet's one existing precedent (dense-leaf
transcription, USD 0.4/line) is not actually the right shape for bMALX's job (a crib/context read against a
*mostly-clear* leaf with about 100 scattered code tokens, not a dense cipher leaf), so even a careful brief-writer
checking the bullet would not have found a matching rate. This is a genuine gap but a narrower one than "the
bullet was ignored": it needed a second precedent, not just better reading discipline. No diff is proposed this
window (the five-proposal budget is spent on items the brief flagged as needing inclusion, items 1-2, plus 3-5);
the concrete follow-up for RETRO-APPLY-T or the next retrospective is a one-line addition to the pricing-precedents
bullet: "cribs/context reading against a mostly-clear leaf with scattered code tokens, two blind passes + reconcile
-- about USD 3.5 a leaf all in (malsburg-hessen-1636, bMALX, 26 Sept 2026), not the ~1.8/leaf a dense-leaf-shaped
estimate gives." Flagged here so it is not lost; not written as one of this window's five diffs.

**(Q3) Is LANE ARM2 idle-standing for armstrong-madison-1808 now, and did the 13:42 check-in apply the rule
correctly?** The target has genuinely exhausted its cheap steps (Livingston, the Jefferson channel, the docket, and
ten shorthand systems all checked with no pool and no identification), so idle-standing is the right *state*. But
the parent's own 13:42 ROOM.md line ("ARM2 idle-standing after ARM-S3") does not follow the idle-standing entry
procedure CLAUDE.md itself names: "the lane orchestrator writes exactly one ROOM.md line, 'LANE <X>: idle-standing,
blocked on ASKS <rows>'" -- no ASKS row is named, and a direct grep of ASKS.md for "armstrong" returns zero rows.
STATUS.md's own ARM handoff names the correct next step ("idle-standing on an ASKS row asking the Papers of James
Madison editors... that is outreach, so AUDIT.md-gated; draft only") but the row itself was never filed. This is a
compliance gap in this window's own action, not a rule gap (GOLD3's idle-standing entry the same day names its
ASKS rows correctly, "ASKS 52, 53 or 55" -- the procedure works when followed). No diff: this is a one-off
correction for the parent to make now (file the drafted ASKS row and re-post the idle-standing line naming it),
not a rule the ledger shows broken twice.

**(Q4) Is bSALR's V1/V3 "control-backed negative at 6.4% error" a rule-3 negative yet, or does it need an
independent error measurement first, and what should the NEAR row say?** NEAR.md's own bSALR entry already gets
this right in substance ("the first syllabary negatives drawn from an error level the transcription can back
(conditional on the 6.4% model estimate)") but the word "conditional" is doing real work that a future reader
could miss: 6.4% is pass C's *model-based* estimate (from majority-vote agreement across three reads), not a
directly *measured* second-transcription disagreement rate the way the 14.6% pass-A/B figure was measured. The
margin against the SALV-DIAG crossover (8-9%) is 1.5-2.5 points -- real, but thin enough that a model-based
estimate sitting close to a design-family exclusion threshold is a weaker basis than a measured one (this is the
same distinction CLAUDE.md's SALV-DIAG paragraph itself draws between the two kinds of number, just not yet
applied to bSALR's own row). DSN regular's control is separately weak (0.646 with one seed collapsed at 6
restarts) and already has its own re-run in flight (bSALR2, 24 restarts). Proposed NEAR row wording, for the
parent or LANE B11's close to paste in once bSALR2 finishes: *"V1/V3 are provisional control-backed negatives,
conditional on the model-based 6.4% error estimate (not yet independently measured); DSN regular's control is
weak (one seed collapsed at 6 restarts) and is being re-run at 24 restarts (bSALR2). The syllabary family is
logged excluded at the measured error only once (a) bSALR2's DSN-24 control is decisive and (b) a third,
independently-read sample of f.56r's 79 still-single-read boxes gives a measured (not model-based) error figure
in place of the 6.4% estimate -- until then this stays a strong-but-provisional result, not a closed family."*
This is advisory text for NEAR.md, not a brief/tool/CLAUDE.md change, so it is not one of this window's five diffs.

**(Q8) LEDGER.md still carries four exact-duplicate-session-id pairs from earlier the same day (lines 900/903 LANE
B5, 902/904 LANE V7, 909/917 LANE GOLD3, 930/934 LANE B6) that CLAUDE.md's own Improvement-loop paragraph already
names and already says to fix ("edit the existing row in place... unless the new row demonstrably describes
different workers or a different total spend") -- none of the four differs in workers or total spend from its
pair, only in wording and a corrected cost figure, so by the rule's own text these should have been merged, not
left standing.** This is not a rule gap (the rule already exists, in CLAUDE.md, written specifically for this
shape) and not a tool gap (`tools/ledger_check.py` already flags all four correctly) -- it is an unfinished
housekeeping action from before this window. LEDGER.md itself is data, not a brief/tool/workflow/CLAUDE.md file,
so merging these four pairs in place is not one of this retrospective's five proposals; it is a straightforward
edit for the parent (or a short parent worker) to make directly, citing this paragraph. Separately, and more
usefully: this window's three lane closes (LANE B10, NX, ARM) all show the *correct* pattern -- each has exactly
one LEDGER row, with the parent's own check-in lines confirming an in-place cost correction from `get_session`
("LANE B10 archived (3.89 by get_session, ledger row edited from 3.60)"; "LANE NX archived (10.33... edited from
8.54)"; ARM's single row shows 10.39 against the orchestrator's own "8.81 at close" in STATUS.md, again a
same-row correction) -- so the discipline this rule asks for is now the parent's routine for new closes, and the
four legacy pairs are exactly that: legacy, from before it became routine, not evidence the routine is failing.

**(Q9) Is the roughly two-hour QA cadence still earning its cost while the live lanes are breadth-only (no reading
candidates surfacing)?** QA10 and QA11 both returned 0 new flags across 45-46 checklist items each, at USD 2.85
and (not yet ledgered, but comparably scoped) cost, during a window whose lanes (B10, B11, ARM, ARM2, NX) produced
control-backed negatives and screening results but no new "reading ready" line to verify and no status.json class
change. Two consecutive clean audits during breadth-only work is weak but real evidence that the fixed two-hour
cadence is running ahead of anything new to check. This is a lane-orchestrator operating parameter (LANE V9's own
QA brief authors its own window and cadence at each check-in already), not a CLAUDE.md rule or a fixed brief
template value, so it is answered here as a recommendation rather than a diff: lengthen the idle cadence to
roughly 3-3.5 hours when the prior QA cycle found 0 new flags AND no reading-ready/class-change line posted in the
interim, but keep (or shorten) the immediate-trigger case -- a QA cycle should still run right after any lane
posts "reading ready" or a status.json class change, regardless of how recently the last cycle ran, since that is
exactly the event a QA cycle exists to catch (V9-MOR's own lesson, item 4 above, is a case where sooner would have
helped, not later).

**(Q10) Did the last three lane closes (B10, NX, ARM) run `ledger_check.py` before self-ledgering, and is the
parent's `get_session` replacement now routine?** Yes to both, per the same evidence read for Q8: all three closes
produced exactly one LEDGER row each, with the parent's own ROOM.md check-in lines explicitly naming the
`get_session`-corrected figure against the orchestrator's own provisional one in each case. This is the
Improvement-loop rule working as intended for new closes; see Q8 for the contrast with the four still-unmerged
legacy pairs from earlier the same day.

## Near solves (review, per brief)

Read directly from NEAR.md (not the digest) for the two rows that moved this window.

**fr2933-salviati-1525**: moving (bSALR, bSALR2 running) -- see item Q4 above for the proposed row wording once
bSALR2 finishes; not stale.

**malsburg-hessen-1636**: moving (bMALX this window: cribs from record 509, class-consistency TIE against its
shuffle control, non-discriminating, correctly not entered as support; job 4 not opened) -- not stale.

**matignon-mayenne-1586, espagnol142-mercy-1648, koehler-1944, mccormick-1999, bullet-tuscany-1944,
untersberg-code, blitz-ciphers, antt-msliv0638-brochado-1712, hessen-1824, fr3625-lauriere-1593**: no ledger rows
touch any of these this window; none stale under the 48-hour rule read against this session's `date -u` of
13:43:11 UTC 26 Sept 2026 (fr3625-lauriere-1593 was last touched 11:13, well inside the window; the rest are
older still, per NEAR.md's own "Last touched" column, and none crosses 48 hours).

## What did not need a change

Rules 1, 2, 6 (except item 5 above, which is a stronger enforcement of an existing check, not a new rule), 8, 9,
9a, and the Outreach gates: untouched by anything else this window. Rule 3 held everywhere else checked (bMALX,
bSALR, NX/ARM2 screening results all report target and control, or "TIE, non-discriminating" language, correctly).
Rule 4 held (no new H-vs-H conflict). Rule 5 held (malsburg, salviati stay `partial`; no target closed
`closed-negative` without every family in its ladder). Rule 7 held (Morillo's re-derivation from a prior window
still stands; no new reading claimed this window to re-derive). Rule 10 held everywhere checked (bMALX, bSALR,
ARM-S3, ARM-LIV, ARM-JEF, D1-CHECK, EM3-CHECK all report found/not-found and search method; EM3-CHECK explicitly
declines to assert N0, and V9-QA11 confirmed this). Credentials: no mishandling found; `key_livecheck.py`'s own
retry-before-flip fix (RETRO-APPLY-S) had nothing to catch this window (no flip reported). Absolute dates: `date
-u` read at this session's own start (13:43:11 UTC) and used throughout; no typed-time WARNING fired on this
session's own lines.

## For the owner

None of this window's findings need the owner's decision, spend or a change to the goal. The QA-cadence
recommendation (Q9) is a lane-orchestrator judgment call within existing budgets, not a spend change, so it is not
raised here as a decision to make.

Retrospective t done: 5 proposals (items 1-5), file RETRO-2026-09-26h.md.
