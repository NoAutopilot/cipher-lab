# Asks

Anything blocked on a human, from any project, any account. An agent that gets blocked writes a row here,
because nobody can see into another account's sessions. Clear a row by changing its status to `done` with
the date; do not delete it.

Status: `open`, `done`, `dropped` (with a reason), `waiting` (with who and since when).

| # | Raised | Project | What is needed | Exact action | Who can do it | Status |
|---|---|---|---|---|---|---|
| 1 | 20 Sept | cipher-lab | DECODE login rejected, password also leaked into a transcript; rotated password (set ~21 Sept) tested once and also rejected ("Incorrect user name or password") — blocks record 8725 (intercepted-royalist-1646 f.104). Update 23 Sept: R413 and R4930 no longer needed, Boswell 1628 was solved in 2021 (DECODE upload, Woodard) and Randolph 1570 has its decipherment on f.278; only 8725 still depends on this login. **Owner reports the DECODE account fixed, 23 Sept 2026 ~23:30 UTC**, and the variables set in the ytbiz environment; not visible in the running container | Next fresh session on the ytbiz account: presence test, then ONE login attempt with `tools/decode_fetch.sh 8725 ciphers/intercepted-royalist-1646/decode/` (the script never echoes credentials); on success set this row to done and start ASSIGNMENTS row 7; on failure set it back to open with the exact signal and stop | Orchestrator, next session | open, 23 Sept 2026 21:41 UTC: the single test ran (`tools/decode_fetch.sh 8725`, exit 4): the server re-rendered the login form at HTTP 200 with `"IS_LOGGEDIN":false`, the script's rejection signal; csrf pair present, markup unchanged; no retry. Observation for the owner: the login form's only identity field is labelled "User Name" (placeholder), while DECODE_USER holds an address with an `@` (21 Sept diagnostic). Check whether DECODE wants the account's user name rather than its email, and confirm the pair by logging in once by hand at de-crypt.org/decrypt-web/login before it is set again. Retried 24 Sept 2026 00:47 UTC per the owner's 23 Sept ~23:30 UTC report that the account is fixed: DECODE_USER and DECODE_PASS both confirmed set (presence test only, `test -n`). The single attempt (`tools/decode_fetch.sh 8725`, exit 4) gave the same non-secret signal as 23 Sept: login re-rendered at HTTP 200 with `"IS_LOGGEDIN":false`. No retry made, no credential printed. The User Name/email question above is still unresolved — recommend the owner confirm the pair by logging in once by hand at de-crypt.org/decrypt-web/login |
| 2 | 20 Sept | cipher-lab | archive.org login rejected (`account_not_found`: no account existed under the address in `IA_USER`) | **Closed 23 Sept**: the person registered the account; `services/xauthn/?op=login` now returns `{"success": true}`. Unblocks the three lending-only reads: `supplementaryrep0000grea` (hamilton-1650), `lettersdispatche0004marl`/`0005marl` (stair-townshend-1710), and the Whitworth biography (whitworth-1707). Borrow verified 23 Sept 2026 21:43 UTC: `browse_book` opens a one-hour SESSION_LOAN and `return_loan` returns it; the page-image step reaches the image server but a lending item is served obfuscated for archive.org's own reader (`shouldProtectImages`), which the script now stops on rather than decode, so the pages of a lending-only book must be read by the person in the reader (row 18) | the owner | done, 23 Sept 2026 (login works; see the action cell) |
| 3 | 20 Sept | cipher-lab | Spink lot 1184: does it include a pocket dictionary? (Sale day is 23 Sept; the 19 catalogue photographs are already saved in `ciphers/wellington-maitland-1812/images/`, so only the question lapses, not the images) | One email before the 23 Sept sale, contact on the lot page | the owner | waiting: enquiry sent 23 Sept 2026 (outreach/nls-20769-enquiry.md); NLS reply pending |
| 4 | 20 Sept | cipher-lab | Stair 1710 scans, about $20 | Order four TIFFs per `ciphers/stair-townshend-1710/REQUEST.md`. Send to: the Spencer Research Library reproduction request route named in that file | the owner (payment) | open |
| 5 | 20 Sept | cipher-lab | NLS MS 20769 quote and five-leaf sample | Copy enquiry per `ciphers/nls-20769/REQUEST.md`. Send to: manuscripts@nls.uk, or the form at auth.nls.uk/copy-enquiry-form/ | the owner, or a teammate if authorised | open |
| 6 | 20 Sept | cipher-lab | British Library quote for Add MS 32093 f.423 | Imaging Services enquiry per `ciphers/monck-1660/REQUEST.md`. Send via: the BL digitisation order form (Order now on bl.uk/services/digitisation) | the owner, or a teammate if authorised | open |
| 7 | 21 Sept | cipher-lab | Whitworth 1707: is one unread clause worth a Kew order? | A decision, then the order if yes | the owner | open |
| 8 | 21 Sept | cipher-lab | Board is published to a private artifact; the team cannot see it | Enable GitHub Pages, main branch, `/docs` folder | the owner (repo settings) | open |
| 9 | 21 Sept | cipher-lab | Teammates need repository access | Add collaborators with write access | the owner (repo settings) | open |
| 10 | 23 Sept | cipher-lab | The noautopilotytbiz account's cloud environment (`env_01EKcbReEPpDXpAoLWEYLGv8`) has none of the seven credential variables set: `DECODE_USER`, `DECODE_PASS`, `IA_USER`, `IA_PASS`, `GOOGLE_BOOKS_KEY`, `JSTOR_USER`, `JSTOR_PASS` are all unset there (checked with `test -n` only, 23 Sept 2026). Blocks ASSIGNMENTS row 2 (Google Books print checks for Cobham 1588 and Rupert 1645) and any verifier JSTOR check run from this account | Add the same seven variables to that environment in the claude.ai environment settings, at least `GOOGLE_BOOKS_KEY` (working) and `JSTOR_USER`/`JSTOR_PASS`; DECODE and IA only once rows 1 and 2 are resolved | the owner (environment settings) | done, 23 Sept 2026 21:41 UTC: all seven variables set in the fresh ytbiz container (`test -n` only, no values printed) and de-crypt.org, archive.org, googleapis, jstor.org reachable (HathiTrust babel/catalog still 403). GOOGLE_BOOKS_KEY works (used for the Bowes calendar read the same session); IA_USER/IA_PASS log in (row 2); DECODE pair rejected (row 1); JSTOR pair untested because the host challenge-blocks the login page (row 17) |
| 11 | 23 Sept | cipher-lab | The noautopilotytbiz account's cloud environment denies outbound access to every archive host: archive.org, web.archive.org, bl.digirati.io, gallica.bnf.fr, catalog/babel.hathitrust.org, data.htrc.illinois.edu, en.wikisource.org and other Wikimedia hosts, gutenberg.org, de-crypt.org, scienceblogs.de, loc.gov, books.google.com all answer `000` / CONNECT 403 (checked 23 Sept 2026). Only github.com, pypi.org and www.googleapis.com are reachable. No access, print-check or image work can run from this account | In the session's title bar, open the cloud environment menu, Edit, and change Network access: either a broader access level or add those domains to the allowed list (levels described at https://code.claude.com/docs/en/claude-code-on-the-web). Then add the seven credential variables (row 10) in the same place | the owner (environment settings) | done, 23 Sept 2026 ~23:00 UTC: archive.org, gallica, bl.digirati.io, wikisource, gutenberg, de-crypt.org, scienceblogs.de, TNA Discovery all reachable from the running container |
| 12 | 23 Sept | cipher-lab | Mornington despatches to Dundas 1798-1800, BL Mss Eur D623: nine despatches unprinted in six editions, key catalogued in the same file (D623/41). Stage 2 | Submit the BL Imaging Services quote request per `ciphers/mornington-1798/REQUEST.md` (first order: D623/41 both copies plus D623/4 as a legibility test; the other eight follow if the key reads). Submitted 23 Sept 2026 via the BL digitisation order form: D623/41 both copies plus D623/4, photography, digital transfer. Ingram 1970 check still to run (ASSIGNMENTS row 22) | the owner | waiting: BL Digitisation Services, since 23 Sept 2026 |
| 13 | 23 Sept | cipher-lab | William Courten's cipher diary, BL Add MS 4956 (66 ff.), with Madden's key at f.66 and an 18th-century key at Sloane MS 4019 f.79; no published reading found. Stage 2, conditional on a JSTOR pass the next session can run in minutes | Submitted 23 Sept 2026 via the BL digitisation order form: test batch Add MS 4956 f.66 and ff.1-3 both sides, plus Sloane MS 4019 f.79 both sides, photography, digital transfer. JSTOR pass still to run in the credential session | the owner | waiting: BL Digitisation Services, since 23 Sept 2026 |
| 14 | 23 Sept | cipher-lab | Seven Years War cipher run, TNA SP 87/36-44: 98 cipher-flagged items, three of them paired with a contemporary decipherment in the same box (SP 87/36/9-10, 40/76-77, 40/121-124). Stage 2 | Decide the first step: (a) a TNA digital copy order for the three paired items (a few pounds per page; the worker's NOTES.md lists the exact item numbers), or (b) check first whether State Papers Online (Gale, paywalled) covers SP 87, which would make copies unnecessary if you have library access | the owner (decision, then order) | open |
| 15 | 23 Sept | cipher-lab | France despatches in cipher 1642-57, TNA SP 78/111/93, /135 and SP 78/113/57 (+1): unprinted in Thurloe vi, no sibling decipherment, not digitised. Stage 2 | TNA record copying per `ciphers/sp78-france-1583/REQUEST.md`: two page checks (SP 78/113 first, then SP 78/111), 9.92 GBP each, then the quotes. Attempted 23 Sept 2026: TNA's daily page-check cap was already reached; slots are first come, so submit early in the UK morning | the owner (order) | open |
| 16 | 23 Sept | cipher-lab | Three more TNA cipher runs verified unsolved at stage 2 on 23 Sept: SP 90/2 (Raby and Reichart 1704, five items, all 'partly in cipher'), SP 87/13 (Newcastle to Carteret 1743, three items), SP 87/23 (Chesterfield and Cumberland 1747, three items; Dobrée's edition unread, lending-only). REQUEST.md not yet drafted | Decide whether to batch these into one TNA copy order with row 15; a worker drafts the three REQUEST.md files on the next wake (ASSIGNMENTS row 12) | the owner (decision) | open |
| 17 | 23 Sept | cipher-lab | JSTOR pass for Courten (four queries in ciphers/courten-diary/NOTES.md) and Bowes 1583 (four queries in AUDIT.md section 3) cannot run from the cloud container: www.jstor.org answers both curl and the headless Chromium of `tools/browser_fetch.js` with a "Client Challenge" page on the login URL and on a `doBasicSearch` URL (one retry after a 20 s pause, 23 Sept 2026 21:50 UTC; host not hit again per the good-citizen rule). JSTOR_USER/JSTOR_PASS themselves untested | Run the eight queries in a browser on the JSTOR account and paste the hit list (title, author, journal, year, page) into the two files, or say the JSTOR check is waived for these two targets | the owner | open |
| 18 | 23 Sept | cipher-lab | Hamilton 1650 print check: HMC Supplementary Report (1932), archive.org `supplementaryrep0000grea`, pp.70-74 ("passages in italics ... in cipher in the original", two 1648 letters to Lanark) and the 1650 entries. The account can borrow it (row 2) but the page images are served protected for the archive.org reader and are not decoded by our tools | Borrow the item at archive.org/details/supplementaryrep0000grea, read pp.70-74 and the August-September 1650 entries, and note whether any 1650 cipher passage is printed deciphered; one hour, no cost | the owner | open |
| 19 | 23 Sept | cipher-lab | The repository is private (GitHub shows 404 signed out), so every outreach link and the Pages board would fail for recipients; CLAUDE.md rule 9 assumes it is public | Made public 23 Sept 2026; addresses, the owner's name and restricted images removed the same day; drafts link the folders again | the owner | done, 23 Sept 2026 |
| 20 | 23 Sept | cipher-lab | Bowes 1583 sign table, a contribution to Tomokiyo's list (F2). Draft ready: `outreach/bowes-tomokiyo.md` | Send the draft. Send to: the contact address on S. Tomokiyo's Cryptiana site (cryptiana.web.fc2.com, contact page); attach key.tsv, reading.tsv, AUDIT.md or link the folder now the repo is public | the owner | done: sent 23 Sept 2026; reply pending |
| 21 | 23 Sept | cipher-lab | Dupuy 468 f.28 catalogue correction for the BnF (F1). Draft ready in French and English: `outreach/dupuy468-bnf.md` | Send the draft to manuscrits@bnf.fr (Département des Manuscrits; confirmed 23 Sept 2026 on bnf.fr). Fallback: the SINDBAD form https://www.bnf.fr/fr/une-question-pensez-sindbad. The notice at archivesetmanuscrits.bnf.fr has no error-report link; an earlier version of this row said it did | the owner | done: sent 23 Sept 2026 to manuscrits@bnf.fr; reply pending |
| 22 | 24 Sept | cipher-lab | Raince to Madame, 25 Oct 1525 (Dupuy 452 ff.28-29): the reconciled ciphertext has 5,725 signs in 50 types, so it fails the gate set for the solver (fewer than 40 types). 28 types are common, 22 occur 1-33 times each; the reconciler says no further pass can merge them, only a solver judging by frequency and context (11 allograph merges would give 39). A third pass on the half-lines pass A skipped is running for the count half of the gate | Decide: (a) let the solver start with 50 types, trying Raince's 1526 key first (a key comparison settles allographs directly) and the annealer with a matched 50-sign control; or (b) keep the gate and hold the target. Orchestrator recommends (a) | the owner | done, 24 Sep 2026: (a), decided by the parent orchestrator under the owner's standing instruction of 24 Sept 2026 to decide without them. Solver starts on the 50-type inventory: Raince's 1526 key first, then the annealer with a matched 50-sign control; the 22 rare types are carried as their own symbols, never merged by hand; every token graded; report what was found and where it was not; no novelty class |
| 23 | 24 Sept | cipher-lab | Two retrospective items that change policy rather than briefs (RETRO-2026-09-24.md): (1) CLAUDE.md's Improvement loop should say the retrospective runs at 12 ledger rows or $60 of worker usage, checked after every worker (the orchestrator brief already says so, applied 24 Sept); (2) proposal 4 wanted your Raince gate (fewer than 40 sign types) written into the transcription template for every symbol-alphabet target, which would block any nomenclator, since they run to hundreds of signs. The orchestrator applied only the shared glyph-atlas step | Approve (1) as worded in the retro; for (2) say whether the type ceiling is Raince-only (recommended) or general | the owner | done, 24 Sep 2026: (1) approved and written into CLAUDE.md's Improvement loop; (2) agreed, the 40-type gate stays a per-target choice and is not in the transcription template |
| 24 | 24 Sep 2026 | cipher-lab | History purge, final step (your decision of 23 Sept; CLAUDE.md Git section). Branch `purged-main-2` on GitHub is the rewritten history, cut from main at 9866425: same files, 259 commits, no addresses, no first name, no credential-length line, no BL/Spink/KHA images anywhere in its past. Sessions cannot force-push or delete branches under their permission policy, so this is four clicks of yours, any time, no need to wait for a quiet moment | On github.com/NoAutopilot/cipher-lab: (1) Settings > General > Default branch > switch to `purged-main-2`. (2) Branches page: delete `main`, `purged-main` and `preserve/f1fe9b1-unrelated-local-history-2026-09-23`. (3) Rename `purged-main-2` to `main`. (4) Optional, later: ask GitHub support to purge unreachable commits. Then nothing else: the next orchestrator check-in sees the new root, runs `tools/finish_history_swap.sh` to carry over whatever was pushed after 9866425, and writes 'swap done' in ROOM.md | the owner | open |
| 25 | 24 Sept 2026 | cipher-lab | Gramont f.29r second reader: 14 sign changes plus 4 uncertainty marks proposed in `ciphers/fr2980-gramont/passC_proposed_changes.tsv` (image basis per row; agreement with the first reader 91.9%). This session was not permitted to write them into the shared `ciphertext.txt`, so the committed reading is unchanged | Decide whether the changes are applied (orchestrator or you): apply the table to ciphertext.txt, run `python3 decode.py`, commit ciphertext.txt, reading.txt and reading_tokens.tsv together. Preview: H 533 / M 30 / U 5 of 568 (now H 538 / M 26 / U 5 of 569) |
