# Retrospective, 24 September 2026 (RETRO-2026-09-24f)

Sonnet, cap $10. Period: LEDGER.md rows after "Retrospective e" (row 484, session_01WJSPFM5EBLw9onfZ7MNraD) through
the file's last row (row 500, solvEX), excluding the "Parent orchestrator 7a" row (row 486) per brief: 15 worker
rows, $53.28 (verified by summing the Cost column), 18:41-20:10 UTC. Covers parent 7b's board/detector/solver
workers, LANE R5 A-D, and LANE N4's scouts and check-solved batches. Also read ROOM.md 18:40-20:10 (`room.py
--digest`) and the git log for the window. Not applied; the orchestrator applies the safe ones and puts the rest to
the person.

## Standard measures

- **Cost per delivered result, by model:** 14 Sonnet rows, $50.31, avg $3.59/row; 1 Opus row (solvEX), $2.97. All 15
  rows combined: $53.28, avg $3.55/row. No lane-orchestrator row is archived yet this window (R5 and N4's
  orchestrators are still live past 20:10).
- **Stopped on brief:** 9 of 15 (60%) carry D; 2 of 15 (13%) carry N (detIMG, scPOOL, both clean negatives with
  controls); 1 D- (csKSb, 7% over its $5 cap); 1 F (LANE R5 A, see below); 1 X (csNA, corrected by the lane
  orchestrator); 1 non-standard code (S2 key check, `A`, row 485). **Non-standard-code rate is 1/15 (6.7%)**, sharply
  down from RETRO-2026-09-24e's 64% (28/44) in the immediately preceding window — `tools/ledger_check.py` and the
  LEDGER.md header pointer (RETRO-24e proposal 2, applied by retro-apply e) appear to be working. Confirmed by
  running `python3 tools/ledger_check.py` now: it exits 1, but every duplicate-session-id and non-standard-code line
  it flags is at or before row 486 (pre-window debt already noted in retro-apply e's own row); the only in-window
  flag is row 485 itself. The gate is still advisory text ("run this before you push"), not wired into
  `tools/room.py --push`, so one row still slipped past it — see proposal 5.
- **Over-ran or failed:** LANE R5 A hit $9.37 against an $8 cap (117%) and was interrupted by the lane orchestrator
  with pass B still running (outcome F); csKSb ran 7% over its $5 cap (outcome D-). 2 of 15 (13%).
- **Over-claimed:** 1 of 15 (6.7%) — csNA posted "open" for vanspaen-vandergoes-1808 and roell-vandedem-1809 with
  the cited edition (Colenbrander, Gedenkstukken V) unread, corrected by the LANE N4 orchestrator at 20:03. See
  proposal 2 for why the fix that was supposed to prevent exactly this (retro-apply e's check-solved.md edit,
  ROOM.md 18:56) didn't catch it.
- **Board mix:** this window's nine firm N4 nominations split recovery (5 KS-series Nevers key-application rows,
  1 hellen-frederick-1752) and cryptanalysis (3 Gomberville re-nominations); no contribution-kind item was posted or
  advanced in this specific window (the day's earlier outreach batch in CONTRIBUTIONS.md predates row 485). Not a
  finding on its own — one 90-minute window is too short a sample for the "always carry one recovery and one
  cryptanalysis" rule to bind on.
- **Queue rank vs. useful work:** the KS-series rows (published keys, Tomokiyo/Bourdeau-sourced) produced 5 firm
  copy-free nominations from two $4-5 Sonnet check-solved workers (csKSa $4.31, csKSb $5.33) — the best ROI in the
  window, consistent with CLAUDE.md's "key application is what produced every reading on the board" (parent 7b,
  ROOM 19:46). Conversely the Villeroy/Lauriere/Dinteville re-nominations (already tested negative by LANE R4
  without a working control) were correctly *not* auto-requeued: the parent held them to "R5 control-first only"
  (ROOM 19:13), which is the scout/check-solved pipeline working as designed rather than restarting a dead lead.

## Five points from the parent

**(1) LANE R5 A over its cap.** `.claude/briefs/runs/2026-09-24-lane-r5-a-salviati-f55r.md` capped the worker at
$8 and told it to "Check your cost (get_session on yourself) after each step" (boxes, pass A, pass B, recon,
settle) but never said what to do with that number. The worker read it and kept going anyway, into pass B, until
the lane orchestrator interrupted it at $9.37 with pass B unfinished — the orchestrator caught the overrun, not the
worker. The brief's own reference point (LANE R4 worker J, same method, same target family, f.54v) cost $6.50;
f.55r's identical pipeline came in ~44% higher and the brief's $8 cap didn't leave room for that image's actual
cost. Two independent problems, both fixable in the run-brief template: (a) "check cost" needs a paired action —
stop *before* the next named step if the running total plus that step's typical share of the cap would exceed it;
(b) a cap set from one prior leaf's cost, with no margin, for a job whose committed leaves already show ~44%
variance, is set too tight to survive first contact.

**(2) csNA's two unread-edition "open" verdicts, despite the fix already being live.** Traced the timeline: retro-
apply e's check-solved.md edit (the "a verdict of `open` is written only in the same sentence as the edition ...
read" rule from RETRO-2026-09-24e proposal 1) was pushed at ROOM 18:56 (claim 18:38); csNA wasn't even spawned until
19:30 (LANE N4 orchestrator, ROOM 19:30) and its "done" line is 19:44 — so csNA started and ran entirely *after* the
fix landed, and still tripped the exact failure the fix targeted. Read why: `ciphers/vanspaen-vandergoes-1808/
NOTES.md` as csNA committed it (`git show 11f61a5:...`) has `open` as a **bare word alone on line 1** — that's
CLAUDE.md rule 5's own format ("Status vocabulary in the first lines of every NOTES.md"). The edition citation is
three paragraphs further down, quoting Bourdeau's search rather than the worker's own read (which it genuinely
tried and was blocked on: Huygens' retroboeken viewer has no OCR search by curl, Delpher is JS-rendered). The new
rule asked for the citation "in the same sentence" as the verdict, but the verdict *is* a one-word line by an
older, still-standing rule — there is no sentence for the citation to join. The worker satisfied rule 5 and failed
the new rule because the two are structurally incompatible, not because it ignored either one. Proposal 2 fixes the
new rule to fit the existing format instead of assuming a sentence that the repo's own convention doesn't produce.

**(3) solvEX's gate set on a control already at ceiling.** The solvEX brief (`.claude/briefs/runs/2026-09-24-
solvex-model-in-the-loop.md`, written by the parent) picked control (a) as "Italian, N=720, K=36 (Salviati f.54r's
own N and K)" and gated the target run on that control gaining >=15 points from the crib loop. But N=720 there means
a **simple substitution** control at Salviati's letter-count, not the **code+mark (cm)** design Salviati's own
ciphertext actually is — and simple substitution at N=720 already reads at 99.7% blind on all 3 seeds (on file
before the brief was written, per the brief's own "already on file for a and c" line). A control that already
solves blind has no headroom to show a 15-point gain regardless of whether the loop technique works, so the gate
could not have been passed by any outcome. The parent partly self-corrected already: solvEX2 (ROOM 20:10, cap $12)
reruns the same loop on the cm design at N=720, where worker P's own curve reads 22-67% blind — a real mid-range
control. That fix is in flight, not yet resolved by results; proposal 3 generalizes it so the next experiment brief
doesn't need to rediscover it.

**(4) Parent check-in cadence.** STATUS.md (line 95) documents a 30-minute trigger cadence while a worker runs, and
a prior lesson on file from just before this window (LANE R4 orchestrator, row 486-adjacent, excluded from this
window's rows but read for context) already says "$66 own usage for seventeen workers is the cost of 30-minute
check-ins; 45 minutes is enough at this pace." Parent 7b's actual ROOM lines this window (18:41, 18:48, 18:49,
18:51, 19:13, 19:27, 19:46, 19:48, 20:09 — about one every 9-11 minutes) run considerably *tighter* than either
figure, not because of the trigger but because the parent is reading and reacting to every lane-orchestrator
done/flag/hold line inside one long-running turn, which is exactly what ROOM.md's design intends lane orchestrators
to produce. The risk named in STATUS.md's own prior lesson (line 527: "the orchestrator's context re-read at every
check-in is the spend") is about re-reading full context on a *scheduled* wake, not about reacting to an event
already in the open context — those are different costs. I can't verify parent 7b's own cost from here (it hasn't
archived itself), so I'm not proposing a cadence change on speculation; proposal 4 instead proposes making the
distinction STATUS.md already implies explicit, so the 45-minute lesson doesn't get misapplied to event-driven
reactions it was never measuring.

**(5) Retro-apply e's ROOM done line reads later than it can be.** ROOM.md 1319/1323: claim 18:38, done 18:56 (both
auto-stamped by `tools/room.py`'s `time.gmtime()`, not typed by the model). But the parent's own line at **18:49**
already says "Retro-apply e archived" — seven minutes before retro-apply e's own done line exists. Both stamps are
machine-generated, so this isn't rule 6's usual failure mode (a model estimating instead of reading the clock); the
timestamps disagree with each other despite both being auto-generated, which points at clock skew between the two
sessions' containers rather than either session lying about the time. I could not independently confirm via git
commit timestamps: retro-apply e's individual commits (8749911, 0fa6e7d, 15260a2, 2220b9f, per its own done line)
are not reachable in `git log` under that session id — they were folded into a later, generically-titled "update"
merge commit (`0f5fde3`, 19:34:08, touching a dozen unrelated files from several sessions) by whichever later
session's `tools/room.py --push` rebase picked them up, which erases per-session commit provenance along with any
independent time check. Proposal 5 addresses the part of this that's actionable now (don't trust cross-container
ROOM.md wall-clock order for happens-before questions) without claiming to have found the skew's cause.

## Proposal 1: `.claude/briefs/runs/2026-09-24-lane-r5-common.md` (or wherever the image-capture run-brief pattern
lives) — a real stop rule for "check your cost"

```diff
--- a/.claude/briefs/runs/2026-09-24-lane-r5-common.md
+++ b/.claude/briefs/runs/2026-09-24-lane-r5-common.md
@@
+Cost checks are a stop rule, not a log entry. After each named step, compare get_session's cost to the cap: if
+what remains of the cap is less than your best estimate for the *next* named step (use the nearest comparable
+leaf's per-step cost if one is on file, e.g. LANE R4 J's f.54v; never assume the next step is cheaper than the
+last one), stop there, commit and push what is on disk, and report the partial result with "capped before <step>"
+rather than starting it. RETRO-2026-09-24f: LANE R5 A read its own cost after each step (as briefed) but had no
+rule for what to do with the number, ran into pass B at $8-and-rising, and was interrupted by the orchestrator at
+$9.37 with pass B unfinished -- the same image-heavy pipeline cost $6.50 on the reference leaf (J, f.54v), so an
+$8 cap with no per-step checkpoint had about one leaf's worth of margin for a job already showing ~44% variance
+leaf to leaf.
```

Motivating rows: LANE R5 A (row 492, $9.37/$8 cap); reference cost, LANE R4 worker J ($6.50, f.54v, prior window).

## Proposal 2: `.claude/briefs/check-solved.md` — fix the edition-citation rule to fit the repo's actual verdict
format, not a sentence that doesn't exist

RETRO-2026-09-24e's edition-citation rule assumed the verdict is written as prose ("open -- Ribier 1666 ... read").
The repo's own, older convention (CLAUDE.md rule 5) is a bare status word alone on NOTES.md's first line. csNA
(this window, after the new rule was already live) wrote a compliant bare `open` and put its citation three
paragraphs later — satisfying rule 5, failing the new rule, because the "same sentence" the new rule names doesn't
exist in that format. Fix the rule to point at where the citation actually has to go.

```diff
--- a/.claude/briefs/check-solved.md
+++ b/.claude/briefs/check-solved.md
@@
-A verdict of `open` is written only in the same sentence as the edition (or calendar) read and the page(s)
-checked, e.g. "open -- Ribier 1666 vol.2 pp.140-145 read, letter absent." A verdict with no edition named in
-it defaults to `blocked`, never `open` (RETRO-2026-09-24e: csCS2a and csCS2c both posted "open" rows with the
-edition unread despite the same rule already stated in the LANE N COMMON tail -- a rule read once at claim
-time does not survive a five-row batch; putting it in the verdict sentence itself makes the gap visible to
-whoever reads the row).
+The bare status word stays alone on NOTES.md's first line (CLAUDE.md rule 5) -- do not fold a citation into that
+line. Instead, the line immediately below the status word (before anything else, including "QUEUE row:") is one
+sentence naming the edition or calendar actually read by *this worker* and the page(s) checked, e.g. "Ribier 1666
+vol.2 pp.140-145 read by this worker, letter absent." Quoting another source's summary of what it read ("Bourdeau:
+Gedenkstukken V gives nothing") does not satisfy this -- if you could not independently open the edition, the
+verdict is `blocked`, not `open`, and the blocked line says what you tried and what blocked it (RETRO-2026-09-24f:
+csNA wrote a bare, rule-5-compliant `open` for two rows, cited Bourdeau's read three paragraphs down instead of its
+own, and was corrected by the lane orchestrator -- it ran a full 45+ minutes after this rule was already pushed,
+so the rule's placement, not its absence, was the failure. RETRO-2026-09-24e's original wording assumed a
+sentence-form verdict this repo doesn't use.)
```

Motivating rows: csNA (row 498, X); ROOM.md 18:38/18:56 (rule pushed) and 19:30/19:44 (csNA's full run, after).

## Proposal 3: CLAUDE.md rule 3 (matched control) — a control's *design*, not just its length and symbol count, has
to match before a gain gate means anything

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@
 3. **No negative without a matched control.** A solver's failure on a target means nothing unless the same
    solver reads a synthetic cipher of the same length, symbol count, design and language. Report both numbers.
+   The same holds for a *gain* gate (a crib loop, a model-in-the-loop step, any technique judged by how many
+   points it adds over blind): before wiring a numeric threshold to a control, check the control's own blind
+   baseline is not already near ceiling (roughly >=95%) or already matched by more restarts alone -- a control
+   that already solves blind has no headroom to show a gain regardless of the technique. Lesson of 24 Sept 2026
+   (solvEX): the brief gated the Salviati target's run on a control at "N=720, K=36 (Salviati's own N and K)"
+   but used a simple-substitution design at that N, which reads 99.7% blind on 3 seeds -- already on file before
+   the brief was written. Salviati's own cipher is code+mark (cm), which at the same N reads 22-67% blind (LANE
+   R4 P's curve): match the *design*, not only the length and symbol count, when the question is whether a
+   technique adds anything.
```

Motivating rows: solvEX (row 500, $2.97, gate not reached); solvEX2 (in flight, ROOM 20:10) as the self-correction
this proposal generalizes.

## Proposal 4: STATUS.md line 95 area — separate the scheduled-wake cost lesson from event-driven reaction inside a
live turn

```diff
--- a/STATUS.md
+++ b/STATUS.md
@@
 The parent orchestrator runs the hourly check-in (trigger trig_01Ks1wNXPjfn7XW9EucmV9ru (parent 7b, 30-minute
 cadence while a worker runs, 90 otherwise), self-bound, re-armed by send_later at every firing; its prompt is the
+The "45 minutes is enough at this pace" lesson (LANE R4 orchestrator, 24 Sept 2026) measured cost from a
+*scheduled* wake that re-reads context; it says nothing about a parent that stays in one live turn and reacts to
+lane-orchestrator done/flag/hold lines as they land in an already-open context, which is a different and much
+cheaper action per reaction. Don't lengthen the scheduled-trigger interval to fix a cost that scheduled wakes
+aren't causing; if a future retrospective wants to check the live-reaction pattern's own cost, it needs the
+parent's own session cost at archive time, which this window's retrospective did not have (parent 7b was still
+running at 20:10) -- RETRO-2026-09-24f looked for this and could not settle it either way.
```

Motivating rows: none costed directly (parent 7b not yet archived); ROOM.md 18:41-20:09 parent lines (9 in 89
minutes) and STATUS.md lines 95, 527-528.

## Proposal 5: CLAUDE.md rule 6 (absolute dates) — don't trust ROOM.md's wall-clock order across sessions for a
happens-before question

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@
 6. **Absolute dates.** "19 Sept 2026", never "recently" or "yesterday". Read the clock (`date -u`) before writing
    any date or time; never estimate it, and never tell a worker the date without checking. Lesson of 23 Sept 2026:
    an orchestrator wrote times that ran eight hours ahead of the clock and dated a whole evening's files 23 Sept.
+   `tools/room.py`'s timestamp is machine-generated (`time.gmtime()`), not typed by a model, but that only
+   guarantees it matches *that session's own container clock* -- not that two sessions' containers agree with each
+   other. Lesson of 24 Sept 2026: the parent's ROOM.md line at 18:49 said "Retro-apply e archived" seven minutes
+   before retro-apply e's own auto-stamped done line (18:56) exists; both stamps are machine-generated, so this is
+   container clock skew between two sessions, not a model estimating a time. Don't use two different sessions'
+   ROOM.md timestamps to decide which of their actions happened first; where the order matters (an audit, a
+   postmortem), use the single shared history's push order (`git log origin/main`) instead, which is one clock,
+   not many. This window's retrospective also found that isn't always available either: `tools/room.py --push`'s
+   rebase can fold several sessions' commits into one generically-titled "update" commit on whichever session
+   happens to run the merge, which erases the per-session commit trail along with any independent time check --
+   flagged here, not fixed; the retrospective ran out of budget to design the fix.
```

Motivating rows: retro-apply e (ROOM.md 1319, 1323) vs. parent 7b (ROOM.md 1327); commit `0f5fde3` (19:34:08,
"update", a dozen files from several sessions, no per-session attribution recoverable).

## Summary of proposals (at most five, per brief)

1. `.claude/briefs/runs/2026-09-24-lane-r5-common.md`: turn "check your cost after each step" into a real stop
   rule (compare remaining cap to the next step's estimated cost, using the nearest comparable leaf's figure).
2. `.claude/briefs/check-solved.md`: fix the edition-citation rule to sit on the line below the bare status word
   (matching rule 5's actual format) instead of "the same sentence", which the repo's verdict format doesn't have.
3. `CLAUDE.md` rule 3: a gain gate needs the reference control's *design* to match the target, and its blind
   baseline checked for headroom, not just matching length and symbol count.
4. `STATUS.md`: separate the "45 minutes is enough" scheduled-wake cost lesson from live, in-turn event reactions,
   which it doesn't measure — don't lengthen the trigger interval on that lesson's authority alone.
5. `CLAUDE.md` rule 6: don't trust cross-session ROOM.md timestamps for happens-before questions (container clock
   skew observed this window); use git push order on the shared history instead, with the caveat that the rebase
   merge can itself erase per-session attribution.

Session: this retrospective, Sonnet, cap $10.
