# Cross-account learning pass 6, 26 Sept 2026, 06:16 UTC (parent worker LEARN-6, for parent 7f)

Window: 2026-09-26T03:13:00Z onward. Read LEARN-2026-09-26-0022/-0058/-0313.md first (nothing repeated). Clone
was shallow (`is-shallow-repository` true); `git fetch --deepen=400 origin main` run per LEARN-0313's own lesson,
though `--since` still returned commits fine this window (no squash this time). Skipped this account's own
sessions (parent 7f, LANE V8, B7, GOLD3, RETRO-APPLY-O, retrospective p, ORPHAN-TOOL, PR-LAND-2, DECODE-ACCESS,
LEARN-5) and read LANE AX's close (04:31) plus LANE AX2 (from 05:14: AX2-4612, AX2-5801, AX2-172, AX2-BLANKS,
AX2-BRO4/5, AX2-SHRINK), AX-REDERIV2/AX-COMP2 before AX's close, and the owner-account parent's own check-in
lines. Not applied; the parent applies.

## 1. Practices/lessons not yet in a shared file

| # | Practice / lesson | Evidence | Portable? | Diff |
|---|---|---|---|---|
| 1 | **A key conflict between two H-grade period decipherments tracked correspondence *direction*, not transcription error.** AX2-172 (06:07 UTC) found codes >=145 split into two disagreeing name lists: letters from the brothers to Willem (4613-4615, 4614, 5550) read one way ("list A"), letters from Willem to the brothers (7205, 7206) read the other ("list B") -- 172 (le Conte Jean / Lumbres), 217, 339 and 202 all disagree between the lists, each side read by eye against its own clear copy. Rather than treat the second sighting as an error to adjudicate, the worker classed the letter in question (5797, Jan+Lodewijk->Willem) by its own direction (list A) and left 7205/7206 out of key_full's codes >=145, closing a live audit (V8-NA172) that would otherwise have downgraded the reading on a false conflict. | `ciphers/lodewijk-van-nassau-1573-74/NOTES.md` AX2-172 section; ROOM.md 06:07 ("Name codes >=145 differ by direction... The two lists disagree at 172... 217... 339... and 202"); `axmerge4/proposal.tsv` (4 KEEP_NOTE lines). | Yes -- a general cryptological pattern (a shared numeral table carrying direction-keyed name sublists) worth naming before the next merge of two period sources into one key_full/key.tsv. | Add to CLAUDE.md rule 4 (grading), after the H/C/S/M/I line: "Lesson of 26 Sept 2026 (AX2-172, lodewijk-van-nassau-1573-74): a code conflict between two H-grade period decipherments from different correspondence directions may mean the table carries two direction-keyed name lists, not a transcription error -- check whether the disagreeing letters group by sender/recipient direction before picking one occurrence over the other or averaging them into one key_full.tsv row." |
| 2 | **A folder-shrink pass for an over-30MB target folder used a manifest with a `cited_by` column and a `regen_images.sh` script covering both full-page renders and every recorded crop, verified by a byte-identical regen test on a sample before deleting anything.** AX2-SHRINK cut `lodewijk-van-nassau-1573-74` from 79.5 MB to 57.4 MB (removed 11 of 19 regenerable full-page PNGs, converted 8 kept/cited pages PNG->JPEG q80) with `images_manifest_full.tsv` (path/bytes/sha1/kind/source/**cited_by**) recording all 395 non-out-of-bounds files, not just the removed ones. CLAUDE.md's existing 30 MB rule ("keep the manifest and a sample... note where the rest can be re-fetched") does not currently name a manifest schema or require a regen script; `tools/iiif_lines.py`'s own manifest (`images/manifest.json`) has no `cited_by` field. | `ciphers/lodewijk-van-nassau-1573-74/images_manifest_full.tsv:1-3` (header + first two rows, `cited_by` column); `regen_images.sh:1-30` (page/crop/all subcommands, sources both manifests); ROOM.md 05:32/06:00 ("manifest + regen_images.sh, sha1-identical regen test"). | Yes, as a worked-example precedent to cite (not yet a second occurrence, so no new shared tool per Usage 8a's "broken twice" threshold). | Add one sentence to the Access playbook's 30 MB-per-folder paragraph (after the `tools/iiif_lines.py` line): "For shrinking an already-overgrown folder rather than a fresh crop pass, `ciphers/lodewijk-van-nassau-1573-74/images_manifest_full.tsv` + `regen_images.sh` (AX2-SHRINK, 26 Sept 2026) is the worked example: a manifest column naming what cites each file, and a regen script proven byte-identical on a sample before anything is deleted." |
| 3 | **`tools/homophonic_anneal.py` gained a `--init` mode (key-seeded anneal, starting from a known partial key instead of a random map) landed directly in shared `tools/`, not a private copy** -- AX2-4612 needed it for settling 16 open numerals against an otherwise-known key_full and added the flag with an offline test rather than writing a target-local anneal script. Already available to both accounts since it is a tracked shared-tools commit; no port needed, just awareness for whichever lane next needs a key-seeded anneal (this account's B7/V8 lanes have not needed it yet). | `tools/homophonic_anneal.py` (commit `ec8edbd`, "Starts the anneal from a given sign->letter key file instead of a random map"); `tools/tests/test_homophonic_anneal_init.py`. | N/A -- already shared and usable, not a diff. | No diff; note only, so a future worker with a partially-known key doesn't reinvent it. |

## 2. What we have that they lack / rule adoption check

The owner-account parent (session_01FXDfYR3CvGk7tcid1Aav1n) picked up both of this account's 05:17 UPDATES.md
additions during this window: **LIVE/ARCHIVED titling** is confirmed applied ("LANE AX2 orchestrator... 06:00:
Titles LIVE/ARCHIVED applied," ROOM.md line 2643). **`tools/orphan_check.py`** is not yet confirmed run by them:
their 05:15 check-in (before ORPHAN-TOOL landed at 05:23) did the orphan sweep by hand and said "I run
tools/orphan_check.py at every check-in once ORPHAN-TOOL lands" (ROOM.md line 2594); no owner-account
parent-level check-in after 05:23 appears in this window's log to confirm a first real run -- for the
owner-account parent: your first post-05:23 check-in is the one to actually run it and say so, not just the
hand-sweep intent from before the tool existed.

## 3. Conflicts

None found on rule content this window. Item 1's finding is a data conflict (two period sources disagreeing),
not a disagreement between the two accounts' own rules or tooling.

## 4. Overlaps on targets

Confirmed no overlap. LANE AX2's window (4612, 5801, 172, BLANKS, BRO4/5, SHRINK) touches only
`ciphers/lodewijk-van-nassau-1573-74/` and `ciphers/antt-msliv0638-brochado-1712/`; grepped this account's window
commits for any B5/B6/B7 lane touching those two folders directly -- zero matches. LANE V8 is the only one of
this account's lanes that touches lodewijk-van-nassau-1573-74 this window (V8-NA172, V8-NA172-2, V8-QA7), and
strictly as verifier (N-classing, gate-2 audits, a QA staleness flag), consistent with "LANE V8 verifies for both
accounts by design" already recorded in STATUS.md -- no solver-role collision.

count: 2 portable practice items (1 cryptological key-merge lesson for CLAUDE.md rule 4, 1 folder-shrink worked
example for the Access playbook), 1 shared-tool awareness note (already landed, no diff), 1 rule-adoption
confirmation (LIVE/ARCHIVED: yes; orphan_check.py: pending their next check-in), 0 conflicts, 0 target overlaps.
