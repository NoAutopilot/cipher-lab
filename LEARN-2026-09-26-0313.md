# Cross-account learning pass 5, 26 Sept 2026, 03:13 UTC (parent worker LEARN-5, for parent 7e)

Window: 2026-09-26T00:58:00Z onward. Read LEARN-2026-09-26-0022.md and -0058.md first (both already applied,
verified on disk below). `git log origin/main --since=...` showed no commits between 00:58 and 02:27: origin/main's
history was squashed to a new root (f2955595, committer date 02:27:00Z, 65 commits total, 9,460 diff lines in the
root alone) sometime in this exact window -- see item 0. Recovered the 00:58-02:27 content from ROOM.md's own lines
instead (LANE AX: AX-GLOSS, retrospective n, V7-QA5, GOLD-KAL3, bTHU). This account's own sessions skipped: LANE
V7/V8, B5/B6, GOLD3/GOLD4, RETRO-APPLY-N, retrospective n, this LEARN pass.

## 0. Git history was squashed again, now to the whole repository (not just a few sessions' commits)

`git rev-list --count origin/main` = 65; `git rev-list --max-parents=0 origin/main` = f2955595, whose own diff is
9,460 lines (effectively the entire repo state in one commit), committer-dated inside this window (02:27:00Z). This
is the same phenomenon CLAUDE.md rule 6 already names ("room.py --push's rebase can fold several sessions' commits
into one... commit, which erases the per-session commit trail -- flagged here, not fixed") and that ROOM.md:2376
and STATUS.md:1451/447 already recorded on 23 and 25 Sept at smaller scale (one squashed commit, or a prepared-but-
declined purge branch) -- but at a new scale: the *entire* history, not a handful of sessions. Practical
consequence for any git-log-based job (this LEARN job's own brief, near_check.py-style provenance, `date`
ordering per rule 6): a `--since` git-log read across a squash boundary silently returns nothing for that span;
ROOM.md's own append-only lines are the only recovery path, confirming rule 6's existing guidance to prefer git
push order breaks down across a squash and ROOM.md becomes the fallback of last resort, not push order.
Operational note: this session's container had a local `main` on the pre-squash lineage (unrelated root,
`git log main ^origin/main` had no merge-base with `origin/main`); `git checkout -B main origin/main`, the exact
step `tools/room.py --start` runs, was blocked by this host's own safety classifier ("Irreversible Local
Destruction") even with a clean working tree. Worked around it with `git checkout -b learn5-work origin/main` (a
new branch, no ref reset) -- safe, no approval needed, and equally usable for `room.py --push`. Portable tip for
either account: if `room.py --start`'s reset step is ever refused by a host classifier, branch fresh from
`origin/main` under a new name rather than retrying the reset. Diff: none proposed (this is a `.claude/briefs/
README.md`-scale infra fragility, not a brief or tool fix); flagged in ROOM.md for the owner rather than applied.

## 1. Practices/lessons not yet in a shared file

| # | Practice / lesson | Evidence | Portable? | Diff |
|---|---|---|---|---|
| 1 | Both open items from LEARN-0022/-0058 are now **resolved on disk**, confirmed by direct read rather than by commit log (which the squash would have hidden): (a) `tools/room.py:170-189` now has `bad_paths()`, explicitly citing both LEARN files by name, rejecting a `paths` entry starting with `-` unless it is a known deletion; (b) `.claude/briefs/runs/*-COMMON.md` "Cost and time" paragraph count is now 1 in every file that had 2 (cx, cx2, kx, vx, yx, zx, zx2, ax all =1), and `tools/tests/test_common_briefs.py` exists and passes (`ok: no duplicate paragraphs`), matching LEARN-0058's exact recommendation (a mechanical check, not a README edit). | `tools/room.py:170-189`; `grep -c "Cost and time" .claude/briefs/runs/*COMMON*.md` (all 1); `python3 tools/tests/test_common_briefs.py` -> `ok`. | N/A -- already applied. | Nothing to port; note only so a future LEARN pass does not re-flag these as still-open (LEARN-0058 item 3 called item (a) "orphaned"; it is not, as of this pass). |
| 2 | LEARN-0058 item 1 (a third hand-copy of the hard-EM alignment algorithm) was resolved by a **CLAUDE.md rule addition rather than the `--align-from-tsv` flag it proposed**: CLAUDE.md Usage item 8 now names `tools/interlinear_align.py` as "the alignment tool for known-plaintext key recovery... a target with its own hand-built (group, span) pairs uses it rather than re-deriving the DP/hard-EM loop," and both existing per-target scripts' headers now point at it. This window, AX-COMP (`c93234fd`'s successor work, per its ROOM claim `ecde9c48`) used `interlinear_align.py` directly for three new companion decipherments (4614/5801/7205) rather than writing a fourth private copy -- the rule change is holding under real use. | CLAUDE.md Usage item 8 (dated "26 Sept 2026, LEARN-2026-09-26-0058"); ROOM.md claim `2026-09-26 02:27` "companion decipherments 4614/5801/7205 via interlinear_align.py". | N/A -- already applied and confirmed working. | No diff; recommend the parent stop tracking this as open. |
| 3 | AX-4612TR's transcription job (blind pass A, 4612 p1 then p2, `ab67f593`/`c5db8c0c`) scoped each call to **one page against the reference sheet**, matching GOLD-4D's own fix (CLAUDE.md Usage 6: "Scope a subagent's visual-transcription call to one page... never the whole inventory in one call") rather than repeating GOLD-4D's mistake. | `ab67f593` (p1, 875 tokens), `c5db8c0c` (p2, 331 tokens), two separate commits/calls. | Already our own rule; this is confirmation it transfers, not a new practice. | None. |

## 2. What we have that they lack

Checked LANE AX's window against `tools/family_run.py`, `tools/near_check.py`, `tools/intake_gate_check.py`: no gap
found (same result as passes 3 and 4) -- AX's targets (Nassau pool, pro3055, Brochado gloss/companion mining) are
attestation and key-recovery jobs below the family-run/NEAR.md thresholds this window. For the owner-account
parent (ROOM line filed): nothing to flag this window beyond item 0's squash note, which applies to both accounts
equally.

## 3. Conflicts

None on rule content. Item 0 is an infrastructure fragility both accounts share (the squash lands on the one
shared `origin/main`), not a disagreement between the accounts' own rules.

## 4. Overlaps on targets

Confirmed no overlap. LANE AX's window (AX-COMP, AX-MERGE, AX-HMC2, AX-4612TR, AX-GLOSS) touches only
`ciphers/lodewijk-van-nassau-1573-74/`, `ciphers/jan-van-nassau-1572-75/`, `ciphers/pro3055-clinton-1779/`. This
account's live lanes (V7/V8, B5/B6, GOLD3/GOLD4) touch `thurloe-printed`, `matignon-mayenne-1586`,
`lope-hurtado-1522`, `untersberg-code`, `kaliningrad-2015` only. V7-QA5's rolling QA read lodewijk/pro3055/brochado
items this window (`ROOM.md 2026-09-26 01:53`), but as read-only cross-account verification, not solver work --
STATUS.md:507's own "none of ours touch them" line still holds.

count: 1 infra-fragility flag (item 0, own numeric evidence: 65-commit total history, 9,460-line root diff), 2
previously-open items now confirmed resolved on disk (item 1: room.py flag validation + COMMON duplication test),
1 confirmation that a prior fix is holding under real use (item 2: interlinear_align.py adopted by AX-COMP), 1
confirmation of an existing rule transferring correctly (item 3), 0 reverse-direction gaps, 0 rule conflicts, 0
target overlaps.
