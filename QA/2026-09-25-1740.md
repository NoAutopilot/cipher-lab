# QA run V6-QA1 -- window 2026-09-25 12:45 UTC -> 17:52 UTC

Worker V6-QA1 (Sonnet, session_01NJrQQJbT1gTtjwFJJsy6fq), for LANE V6 orchestrator (session_01V2BHwhVh1k72qSYuBFCyGd).
Brief: `.claude/briefs/runs/2026-09-25-lane-v6-qa.md`. Cap $5 / 40 min wall clock (`date -u` 17:42:24 -> 17:52:37 at
write-up, well under). Scope: every ROOM.md line and commit 12:45-17:52 UTC across LANE R6, B2, V6, ZX, CX2, GOLD,
and parent 7b/7c workers (repo was a shallow clone at start; `git fetch --unshallow` run first so the window diff
is real, not a rebase-fold artifact -- see LESSONS.md's own warning about this). Checked against CLAUDE.md rules
1, 3, 7, 10 (+ key source), the Pipeline intake gate, `.claude/briefs/check-solved.md`, `verifier.md`, and the LANE
V6 QA additions (`.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`). Previous run: QA/2026-09-25-1245.md, both of
whose flags (LOCAL-QUEUE.tsv markers, clair349 gate regression) are confirmed cleared this window (ROOM 13:55,
live re-check below) and are not re-flagged.

## Table

| # | target / item | check | pass/fail | file : line |
|---|---|---|---|---|
| 1 | LOCAL-QUEUE.tsv, JSTOR-QUEUE.tsv | shared-file hygiene: no conflict markers repo-wide; no duplicate row IDs | PASS | `grep` sweep this pass, clean; LOCAL-QUEUE.tsv id column unique L1-L16 |
| 2 | status.json, QUEUE-scores.json | valid JSON | PASS | parsed this pass |
| 3 | antt-linhares-chave judge PASS (V6-PTCORP/orchestrator) | rule 7 + item 10: live re-run of the exact pasted command reproduces the pasted numbers; pt18 is the era-correct corpus (1808-1819 letter) | PASS, exact match | `python3 tools/judge_plaintext.py specs/antt-linhares-chave.json --file ciphers/antt-linhares-chave/reading.txt` -> `score=-1.051, null_p99=-1.452, real_p05=-1.122 ... PASS`, byte-identical to NOTES.md:829-831 |
| 4 | antt-linhares-chave status.json qa_flag | item 8 stale-state: flag cleared, not left stale | PASS | `qa_flag` absent from status.json (grep, 0 hits); `results` entry mentions the pt18 PASS |
| 5 | bl-charles-digby AUDIT.md (V6-DIGBY) | rule 10 verdict: N0, key source recorded, safe/unsafe sentences correct, citations check out | PASS | ciphers/bl-charles-digby/AUDIT.md:1-30 (Ellis 1858 letter + BM 1849 Index + Groen 1859 all named and dated correctly) |
| 6 | bl-charles-digby status.json `results` entry | item 8 / rule 10 addendum: parent copies the AUDIT.md key source into the entry's `key` field | **FAIL** | status.json (results array, Digby entry) -- see failure 1 |
| 7 | antt-fcc-costacabral-1865 AUDIT.md (V6-COSTA) | rule 10 verdict: N0, key `period` (corrected from solver's `ours`), status corrected found-solved->solved with a stated reason, search log covers the families | PASS | ciphers/antt-fcc-costacabral-1865/AUDIT.md:1-45 |
| 8 | antt-fcc-costacabral-1865 rule 7 reproduction | `tools/decode_key.py --check` reproduces C 63 M 19 U 2 | PASS | live run this pass: `tokens 84: C 63, M 19, U 2 / reading up to date / EXIT: 0` |
| 9 | antt-fcc-costacabral-1865 live intake gate | item 9: NOTES.md status word is now `solved` (AUDIT.md task 3) -- live gate check | **FAIL** | see failure 2 |
| 10 | vanbeuningen-dewitt-1657, jan-van-nassau-1572-75 AUDIT.md (V6-SOCHK PRs 10-12 + parent 7c's follow-up) | corrections V6-SOCHK raised but did not apply are logged, not silently dropped or silently applied | PASS | ciphers/antt-linhares-chave/NOTES.md +8 lines (trim direction), ciphers/jan-van-nassau-1572-75/NOTES.md +9 lines (Pfalzgraf Christoph), ciphers/vanbeuningen-dewitt-1657/AUDIT.md:304-310 (penmingem misquote) -- all commit ebe77c5e / pre-existing |
| 11 | SO-CLOSE: PRs 10-12 landed files, closed without merge | one file per PR, in its own second-opinions/ folder, nothing else touched, PR not merged | PASS | `git show 502f64a6 --stat`; PR states confirmed `merged:false` per SO-CLOSE's own done line (ROOM 17:10) |
| 12 | thurloe-barriere-1654 closed-negative (ZX-BAR2) | rule 3: target and control numbers side by side; reproduces live | PASS | live run this pass: `mark_shuffle_test.py` -> `real: 8/13 ... shuffle null ... mean=8.265 sd=1.516 z=-0.175 p=0.7080`, matches NOTES.md:2-4 exactly |
| 13 | thurloe-barriere-1654 status history | item 8: orchestrator's 16:25 correction (closed-negative->open, methodology flaw: marks ignored) and ZX-BAR2's later closed-negative are both kept in NOTES.md, not overwritten | PASS | NOTES.md:1-8, both corrections present in order |
| 14 | espagnol142-mercy-1648 Y8 flag ("moved it", homophonic anneal) | rule 3 gain-gate: control's own blind baseline not near ceiling; numbers check out | PASS | cheap_test_1/*.json: target -1154.3 (2 seeds, reproducible), 5 control seeds -1321.7 to -1356.6 (share correct 0.355-0.814, genuinely spread, not ceiling-saturated) -- matches NOTES.md Y8 section; hedged correctly (no rule-10 wording, status stays `open`, flagged for orchestrator review not claimed as a reading) |
| 15 | koehler-1944 (GOLD-0K) transcription-discrepancy flag | rule 2: not silently repaired | PASS | ciphertext.txt keeps `nogex`/`ayddg` (the discrepant readings), discrepancy table only in NOTES.md |
| 16 | Breadth-lane cheap tests: kaliningrad-2015, moustier-altars, pollaky-1865-1875, debosnys-1883 | Pipeline intake gate: deep work (here, cryptanalysis: 2-pass transcription + IC/anneal against matched controls) begins only after a passing check-solved verdict | **FAIL** (3 of 4; 1 out-of-order) | see failure 3 |
| 17 | jan-van-nassau-1572-75 live intake gate | item 9, pre-existing-state check | informational, not a failure -- see note below the table |
| 18 | Rule 10 wording sweep, 45 real (non-rebase-artifact) NOTES.md/AUDIT.md files changed in-window, plus targeted greps for "first decipherment/never printed/unpublished plaintext/newly recovered/previously unread/unread cipher/we solved" | no over-claim about our own work | PASS -- 0 violations. Every hit is a compliant "did we first-decipher? no" answer, an "unsafe sentence" disclaimer, or "key never printed" (credentials) | grep sweep, see report |
| 19 | GOLD-4A debosnys cryptogram-4 pass agreement | rule 3 gate-fail path: below 80% gate, ciphertext.txt left unchanged, no reading claimed | PASS | ROOM 17:45; `ciphertext_draft.tsv` not promoted |

## Failures (3)

**1. status.json `results` entry for `bl-charles-digby` has no `key` field**, unlike every other 25-Sept N-class
board entry written the same day. AUDIT.md (V6-DIGBY) states plainly: "Key source: `published`" and "`text: known`"
-- rule 10's 25-Sept addendum says "every AUDIT.md verdict also records whose key read the item... and the parent
copies it to the result's `key` field in status.json". The other three N-class entries added today all have it
(`antt-fcc-costacabral-1865`: `"key": "period"`; `antt-linhares-chave` and both `jan-van-nassau-1572-75`/
`vanbeuningen-dewitt-1657` entries: `"key": "ours"`). Digby's grade string mentions "key published" in prose but the
structured field the addendum asks for is missing, so anything reading `status.json` mechanically (a board
publisher, a script) misses it. Fix: add `"key": "published"` to the Digby entry in `status.json`. For the parent
(whoever next edits `status.json`'s Digby row).

**2. `tools/intake_gate_check.py` does not recognise `solved` (or `closed-negative`, `offline-only`) as a verdict
word, so a target that has actually finished -- `antt-fcc-costacabral-1865`, NOTES.md line 1 now reads `solved`
per V6-COSTA's own instruction -- gate-checks live to **exit 1, "no open/partial/blocked/found-solved verdict word
found in NOTES.md -- ambiguous, treat as blocked"**. The tool's own comment at line 66 says "every other status
word (solved, closed-negative, offline-only) is untouched", i.e. the intent is clearly to leave these alone /
not gate them, but the implementation has no pass-through case for them: `find_verdict()` only matches
`VERDICT_RE = (open|partial|blocked|found-solved)`, so a `solved` line simply never matches and the function falls
through to "no verdict word found", which the caller treats as `blocked`. Effect: the Pipeline rule ("paste
`tools/intake_gate_check.py`'s output before briefing any deep-work worker; a nonzero exit blocks the brief") would
tell the next person that a target the repo's own board already lists as N0/solved is "blocked" -- confusing at
best, and a real risk if anyone automates on the exit code. This is the same tool TOOLS-GATE (15:50 UTC, this
window) just fixed for `found-solved`; `solved` was not covered by that fix or its 22-case test suite (checked:
`tools/tests/test_intake_gate_check.py` has no `solved`/`closed-negative`/`offline-only` case). Reproduced live
this pass: `python3 tools/intake_gate_check.py antt-fcc-costacabral-1865` -> exit 1. Fix: add a pass-through branch
in `check()`/`find_verdict()` for `solved`, `closed-negative`, `offline-only` that returns exit 0 with a
"nothing to gate" message, mirroring the existing `blocked` branch, then add the three missing synthetic cases to
the test suite. For the parent (tool owner; this is the same file LANE CX2's "gate-tool gap" note at 17:06 UTC
was already flagging for a different reason -- found-solved/partial reading as FAIL -- so route both to the same
fix session).

**3. Breadth-lane cheap tests ran cryptanalysis before (or without) a passing check-solved verdict**, contrary to
the Pipeline's Intake gate rule ("before any deep work (transcription, key application, cryptanalysis) on a
target, read its check-solved verdict... an `open` whose sentence does not name the standard edition and the
pages or full-text search actually read... is `blocked`... Send a check-solved worker first"). `.claude/briefs/breadth.md`
(the shared brief every LANE B2/GOLD breadth worker runs from) has no intake-gate or check-solved step at all --
it goes straight from "Spec: `specs/<slug>.json`" to "Run `cheap_tests_in_order[0]`". Concretely, this window:
  - `kaliningrad-2015` (bKAL, 16:58) and `moustier-altars` (bMOU, 16:28) ran a 2-pass transcription plus an
    Index-of-Coincidence/matched-control cryptanalysis test with **no check-solved verdict ever recorded** --
    both NOTES.md files carry only a bare `status: open` / `Status: **open**` self-description, no search log, no
    edition or full-text citation of any kind (confirmed: live `tools/intake_gate_check.py` reads both as
    "no verdict word found -- ambiguous, treat as blocked", because their status line isn't in the plain
    bare-word form the tool -- and CLAUDE.md rule 5 -- expects; even read charitably as prose-`open`, item 1's
    "names the standard edition and the pages or full-text search actually read" is simply absent from either
    file).
  - `pollaky-1865-1875` (bPOL, 17:41) is explicit about it in its own NOTES.md: "check-solved has not been run on
    this target (it entered the board via the breadth lane, spec-first, per CLAUDE.md 3a)" -- correctly disclosed,
    but the cheap test (image fetch, transcription, IC vs. English/random controls) still ran first.
  - `debosnys-1883` (bDEB) is an out-of-order case rather than a missing one: bDEB's cheap-test-1 cryptanalysis
    (sign inventory, IC vs. matched controls) committed at 16:42:51 UTC, seventeen minutes **before** GOLD-0D's
    check-solved sweep committed at 16:59:51-17:00:13 UTC (git log timestamps, both confirmed this pass). The
    verdict came back `open` so no harm resulted here, but the order the Pipeline specifies (check-solved, then
    deep work) was not followed.
  None of these four is a lane-orchestrator slip repeated by chance -- they trace to the same shared brief file
  having no gate step, so the same gap will keep recruiting new breadth targets as LANE B2/GOLD scale up. Not
  itself a board-affecting error (all four stayed `open`/no reading claimed), but it is exactly the class of gap
  CLAUDE.md's 25 Sept Intake-gate paragraph was written to close, on the newest lane. Fix: add a
  `tools/intake_gate_check.py <target>`-or-check-solved-verdict step to `.claude/briefs/breadth.md` before "Run
  `cheap_tests_in_order[0]`", or explicitly amend Pipeline 3a to say a first cheap test is exempt from the intake
  gate (an intentional-and-documented carve-out is fine; a silent one is not). For LANE B2 and LANE GOLD
  (own the workers) and whoever maintains `.claude/briefs/breadth.md` (the actual fix).

## Note, not a failure

`jan-van-nassau-1572-75`'s NOTES.md also live-gate-checks to exit 1 ("open... with no standard-edition citation...
within 6 lines") -- but this predates the window: re-run against the file's state at commit `7fd9de7d` (12:40:48
UTC, before this window) gives the identical exit code and message, so nothing this window's SOCHK-correction edit
(appended ~1000 lines below, well outside the 6-line window) caused or changed it. The target reached N3 with two
audits and an `ours` key well before this gate rule existed; deep work is long done and not gated on this. Listed
for completeness (item 9 asks for a live re-run on every NOTES.md that changed) but not flagged, since flagging it
would send a lane chasing a citation-formatting fix on a target that is already finished and on the board, per the
brief's "a flagged item does not count toward any total until its lane clears it" -- there is nothing here for a
lane to clear.

## Totals

Items checked: 19 (table) + 4 breadth targets folded into failure 3 + previous run's 2 flags re-verified cleared.
Failures: 3 (2 tooling/status.json gaps, 1 shared-brief process gap spanning 4 targets). No failure touches a
board result: Digby and Costacabral's own classes, keys and readings are all correct and reproduce live: 1 is a
missing structured field for automation, 2 is a tool gap that would misreport an already-finished target as
blocked (not become one), 3 affected only breadth first-tests that all came back `open`/no-claim regardless.

Requests this session: none (disk/git only, one `git fetch --unshallow`). No subagents used (0 of the brief's cap
of 2). No credentials touched.
