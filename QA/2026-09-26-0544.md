# V8-QA7 rolling QA sweep

Window: 2026-09-26 03:08 UTC (V8-QA6's start, previous run's cutoff) to 2026-09-26 05:44 UTC (this session's
start, clock read). Source: ROOM.md lines 2492-2619, `git log --since "2026-09-26 03:08:00"`, and the touched
files. Previous run: QA/2026-09-26-0308.md (V8-QA6; 3 findings, all routed -- see below for follow-up). Items
12-17 from earlier briefs are re-checked only where still open in that file; none were. Disk + git log + a
handful of tool re-runs, no subagents, no network.

## Row-by-row

| Target / item | Check | Result | File:line |
|---|---|---|---|
| thurloe-printed | 19. RETRO-APPLY-O's heading-comma fix | **PASS** -- `tools/intake_gate_check.py thurloe-printed` now returns `partial (line 2)` (the real rule-5 status word), no longer matching the line-15 subheading "**Open, for the next owner..." that QA6 caught. Confirmed live re-run. | tools/intake_gate_check.py; ciphers/thurloe-printed/NOTES.md:1-2 |
| matignon-mayenne-1586 | 20. bMAT1C/bMAT1D revert consistency | **PASS** -- `reading.txt` header reads `H 10074, M 1648, U 1272` (S 0, the pre-bMAT1C grades); `python3 tools/decode_key.py ciphers/matignon-mayenne-1586 --check` exits 0, "reading up to date"; NOTES.md:1 = `partial`. NEAR.md's row does quote bMAT1C's numbers (-1.545, cover 0.714) but only as a labelled, superseded step immediately followed by "**Revert + U code words (B7 bMAT1D...)**: bMAT1C reverted, judge back to -1.371 / cover 0.814" -- a historical log entry, not a live claim; no file cites -1.545/0.714 as the current state. | ciphers/matignon-mayenne-1586/{NOTES.md:1,reading.txt:1}; NEAR.md:21 |
| riksarkivet-r4282-1628 | 21. QA6's control-mean flag: fixed, or carried to B7? | **finding, still open** -- see Failures section. `specs/riksarkivet-r4282-1628.json` was rewritten to a *different* set of Latin-hint counts ("8, 3, 8, mean 6.3") that is now internally self-consistent but disagrees with `NOTES.md:91` and `STATUS.md:351`, which both still say "3, 4, 8, mean 5.0". Not mentioned anywhere in LANE B6's close (04:27-04:30) or LANE B7's open/check-ins (04:45-05:19) -- not carried forward, not re-owned. | specs/riksarkivet-r4282-1628.json (control field); ciphers/riksarkivet-r4282-1628/NOTES.md:91; STATUS.md:351 |
| szembek-bk1560 | 22. Leaf 65/67 non-test framing; leaf 66 gloss-consistency framing | **PASS on the per-leaf files, open concern at the merge** -- leaf65/NOTES.md:155 and leaf67/NOTES.md:254-260 both explicitly call their own 0.0-vs-0.0 and 0.429-vs-0.429 results "non-test", never a negative or a PASS; leaf66's 0.918 vs 0.306/0.341 is correctly framed as "gloss is a working key" (a consistency check), not a decipherment claim. But retrospective p (05:33, ROOM.md:2616) flagged, essentially at this window's close, that the merge (bSZM, 05:31-05:32) pools all three leaves' codes into one grade-C key.tsv without holding leaf-65/67-only codes to a lower grade than leaf 66's -- confirmed still true in the current `key.tsv`/`consistency_by_code.tsv` (pooled stat mixes all 22 codes with no per-leaf split). Already self-flagged in this window; folded into the `solved` finding below rather than re-flagged separately. | ciphers/szembek-bk1560/{leaf65/NOTES.md:155,leaf67/NOTES.md:254-260,NOTES.md:292-303}; ROOM.md:2616 |
| espagnol142-mercy-1648 (SO PRs) | 23. PRs 16-19 picked up? | **PASS, fully picked up** -- PR-LAND-2 (05:12-05:17) landed and closed PRs 16-20: JSTOR rows 80-83/88-91 marked done (no relevant hit), LOCAL-QUEUE L4/L5 blocked-as-answer, L14 answered. PR 16 (SO-MERCY-F22) was then citation-checked by V8-SO16 (05:10-05:16): verdict **merge**, 9 of 13 claims confirmed against source, 4 access-gap citations not independently re-tried, N3 class unchanged, appended to AUDIT.md. Not acted on further per the brief (report only). | ROOM.md:2597,2596; ciphers/espagnol142-mercy-1648/AUDIT.md:308-325 |
| lodewijk-van-nassau-1573-74 | 18. status.json vs current AUDIT.md/qa_flag | **finding** -- see Failures section. status.json's WVO 5797 row is stale on four points relative to the AUDIT.md now on disk. | status.json:1077-1087; ciphers/lodewijk-van-nassau-1573-74/AUDIT.md:698-716,835-838,974-1056 |
| lodewijk-van-nassau-1573-74 | 18. Overclaim check ("fully read", name values as "ours") | **PASS** -- no file calls the p5/p7 blanks fully read (AUDIT.md:623,706,936 and NOTES.md:1029,1031,1363 all correctly show p5 still carrying 136 M / 146 U, and p7 still carrying 146 U); AUDIT.md:835 and :974 both correctly split the key source as `period` for the name values (5550/4614 glosses) and `ours` only for the letter table -- no file attributes the name values themselves to `ours`. | ciphers/lodewijk-van-nassau-1573-74/{AUDIT.md:623,706,835,936,974; NOTES.md:1029,1031,1363} |
| lodewijk-van-nassau-1573-74 | 18. Folder size (report only) | 58 MB now (`du -sh`), down from the 78-80 MB QA6/AX2 both reported, after AX2-SHRINK (05:32, this window). Still over the 30 MB rule; AX2-SHRINK's own figure (24 MB excluding live workers' in-progress crop folders) is the number that will hold once AX2-4612/AX2-5801 finish and their scratch crops are cleared. Report only, per the brief -- not fixing. | ciphers/lodewijk-van-nassau-1573-74/ (`du -sh`) |
| lodewijk-van-nassau-1573-74 | 3/7. Rule-7 re-derivation, rule-3 numbers | **PASS** -- AX-REDERIV2 (04:32-04:41) re-derived key_full v3 fresh-instance, 0 differing tokens on all four letters (5797 73, 4610 1545, 4611 1393, 4616 261), matching a live re-run: `python3 tools/decode_key.py ciphers/lodewijk-van-nassau-1573-74 --check` exits 0, "reading up to date" now too. AX-MERGE3's four conflict resolutions (127, 57, 129, 123, 107, 95) each report the real-text gate numerator/denominator beside the shuffled/dual-path comparison it was tested against. | ciphers/lodewijk-van-nassau-1573-74/ (`decode_key.py --check` live re-run); ROOM.md:2543,2563-2564 |
| lodewijk-van-nassau-1573-74 (AX-4612TR/TR2, AX-COMP2) | 6. Transcription two-pass gate, honesty about below-gate pages | **PASS** -- AX-4612TR2 caught and fixed a second content-drift in AX-4612TR's own p1 figure (80.1% -> true 88.3%) before using it; AX-COMP2's 7205 transcription reports p1 36.8%/p3 45.6% honestly as *below* the 60% gate with a stated reason (faint/bled ink, both blind passes independently flagged low confidence) and then visibly restricts what it will settle from those two pages (six named codes only, eye-checked against the crop, not a blanket read) rather than treating the low-agreement pages as if they had cleared the gate. | ciphers/lodewijk-van-nassau-1573-74/NOTES.md:1913-1925 (AX-COMP2 table) |
| malsburg-hessen-1636 (bMAL3) | 3. Rule 3 design-match discipline | **PASS, exemplary** -- bMAL3 ran `family_run.py --family masc` (control 0.987, gate met) but explicitly labelled it "masc's control is built over K=22 letters, not the target's real K=95 -- not a matched design", ran it "anyway for the record" without letting the resulting target FAIL count as a real negative, then ran the actually-matched `homophonic --param profile=target` design at K=95, which failed its OWN control gate (0.392 < 0.6, CONTROL BELOW GATE) -- correctly reported as "the anneal doesn't have enough signal... not a negative on the target" rather than forcing a verdict. This is CLAUDE.md rule 3's own headline lesson (Salviati, design not just length/K) applied correctly and by name. | ciphers/malsburg-hessen-1636/NOTES.md (tail, "First cheap test" section); HYPOTHESES.md (both rows) |
| Various new negatives this window (bSIE, bBLZ6, bMATBEAM, bMAT1D, bZES-blocked, bUNT8/bUNT9, hessen-1824) | 3. Numbers side by side | **PASS, spot-checked** -- every one reports target and control (or control-below-gate) numbers together in the same sentence (e.g. bSIE: "no09/key14 real -1.562 vs scrambled mean -2.141"; bUNT9: "1/5 naive... vs 200-draw random-position control mean 1.07/5 p95 3/5"; hessen-1824: "target FAIL at auto period 16... control 1.000 vs de20 (gate met)"). | ROOM.md:2527,2580,2582,2585,2612,2539 |
| Nine check-solved/intake verdicts written this window (matignon, lope-hurtado, szembek, siena, malsburg, hessen-1824, hessen-daenemark-1672, zeschau-seebach-1841, untersberg-code) | 1. Intake gate live re-run | **8 PASS, 1 soft-warning false positive** -- see Failures section for the lope-hurtado tool false-positive. All eight others (matignon `partial`, szembek `solved`/terminal, siena `open`, malsburg `open`, hessen-1824 `open`, hessen-daenemark-1672 `partial`, zeschau-seebach-1841 `blocked`/terminal, untersberg-code `open`) return a clean exit 0 with no soft warning. | tools/intake_gate_check.py (live re-run, all nine targets) |
| szembek-bk1560 | 5. Rule-5 status word: `solved` | **finding** -- see Failures section. | ciphers/szembek-bk1560/NOTES.md:1 |
| Shared files | 7. Conflict markers | PASS -- none found repo-wide (`<<<<<<<`/`=======`/`>>>>>>>` on their own lines, .md/.tsv/.json) | -- |
| Shared files | 7. status.json / QUEUE-scores.json parse | PASS -- both valid JSON | -- |
| Shared files | 7. Duplicate IDs, LOCAL-QUEUE.tsv / JSTOR-QUEUE.tsv | PASS -- no duplicate `id` values in LOCAL-QUEUE.tsv; JSTOR-QUEUE.tsv has no `id` column (col 1 is `target`, which legitimately repeats across queued queries for the same folder) -- not a duplicate-ID violation | -- |
| NEAR.md / status.json | near_check.py | PASS -- "9 NEAR.md rows, 9 status.json near entries, in step, none closed-negative, none stale" | tools/near_check.py output |
| ROOM.md | 15. Role names since the 00:20 guard | PASS -- no line in the window (2492-2619) has a `--flag`-shaped or otherwise unknown leading role field | ROOM.md:2492-2619 |
| Rule 10 wording, all targets touched this window | 5. No new/first/unpublished/unread/never-printed language about our own work | PASS -- every hit for these phrases in the window's touched files is inside an AUDIT.md "unsafe example" list (guidance, not a claim); no live claim found anywhere | ciphers/lodewijk-van-nassau-1573-74/AUDIT.md (grep, all six hits are unsafe-example lists) |

## Failures / findings requiring a flag

1. **status.json's WVO 5797 (lodewijk-van-nassau-1573-74) row is stale on four points against the AUDIT.md now on disk.**
   (a) `qa_flag` reads "one audit only (V8-NA5797); second adversarial audit and a clean rolling-QA window pending" --
   the second adversarial audit (V8-NA5797-2) completed at 05:09 UTC, HELD N4 on both blanks; this QA7 window is the
   "clean rolling-QA window" the flag is waiting on, so the text should now read as ready to lift for the 153/161
   spots specifically. (b) `"key": "period"` -- AUDIT.md (both the A2 second audit, line 835, and the V8C spot audit,
   line 974) explicitly records the key source as **two** values, `period` for the name values and `ours` for the
   locating table; status.json drops the `ours` half. (c) There is no `"text"` field on this row at all; AUDIT.md
   says `text: known` (Groen IV CDXLIV, N0) explicitly and every comparable row elsewhere in status.json carries this
   field. (d) The row's prose still describes the third blank (172, le Conte Jean) as "not in the audited claim" --
   true when the row was written, but V8-NA172 has since audited it (05:10-05:17, N4) with **one** audit only (not
   two, unlike 153/161), so it is not yet at the same bar. flag: for LANE V8/the parent -- refresh the row's
   `qa_flag`, `key` (add `+ours`), add `text: known`, and either add 172 as its own entry noting one audit, or note
   it explicitly as pending a second.

2. **riksarkivet-r4282-1628: QA6's control-mean flag was not fixed -- the spec now disagrees with NOTES.md and
   STATUS.md on the underlying numbers, not just the mean.** QA6 (03:18) flagged `specs/riksarkivet-r4282-1628.json`'s
   "mean 6.3" as an arithmetic error against the values {3, 4, 8} (correct mean 5.0), matching NOTES.md:91. As of
   this window's end, the spec's `cheap_test_done.control` field reads "Latin-hint counts 8, 3, 8 (mean 6.3)" --
   6.3 is now the *correct* mean of {8, 3, 8}, but {8, 3, 8} is not the same set of numbers as NOTES.md:91's
   "B: 3, 4, 8; mean 5.0" or STATUS.md:351's "synthetic keys 3/4/8". Someone edited the spec's arithmetic without
   reconciling it against the other two authoritative records, which still agree with each other and disagree with
   the spec. The crib verdict is unaffected either way (the real crib's count, 4, sits inside/below both ranges).
   Not mentioned in LANE B6's close (STATUS.md, 04:27-04:30) or anywhere in LANE B7's opening/check-ins
   (ROOM.md 04:45-05:19) -- the QA6 flag was not carried forward by B6 before it closed, and B7 has not adopted the
   target. flag: for the parent -- assign an owner for riksarkivet-r4282-1628 and reconcile the three-way
   disagreement (which triple of numbers is actually right -- re-run the synthetic-key control if unclear).

3. **szembek-bk1560/NOTES.md:1 was set to `solved` (bSZM, 05:32) with no rule-7 fresh-instance re-derivation on file
   and no verifier/AUDIT.md N-class yet, and while a same-session flag (retrospective p, 05:33) about the merge's
   grading is still open.** `decode_key.py --check` exiting 0 on the worker's own commit is not the rule-7
   fresh-instance re-derivation rule 7 requires before a target moves toward a terminal status (a separate session
   re-deriving from only the spec and the key); none has run for this target. No AUDIT.md exists for szembek-bk1560
   at all, so no N-class has been assigned (rule 10) despite the reading containing a distinctive unmatched name,
   Pergenbeio, that print_check already searched with 0 hits everywhere -- exactly the shape of claim rule 10 says a
   verifier, not the solver, gets to close. Separately, retrospective p flagged (05:33, three minutes after the merge)
   that the merged `key.tsv` grades all 22 pooled codes C uniformly, without holding the codes attested only on
   leaves 65/67 (whose own per-leaf consistency checks were non-discriminating at low N -- 0.0 vs 0.0 and 0.429 vs
   0.429) to a lower grade than leaf 66's codes (which do clear a real control, 0.918 vs 0.306/0.341); this is
   confirmed still true in the current `key.tsv`/`consistency_by_code.tsv`. Every comparable target in this repo
   with an H/N4-graded partial reading (lodewijk-van-nassau-1573-74, matignon-mayenne-1586) stays `partial` until a
   verifier has looked at it; `solved` here looks premature on both grounds. flag: for LANE B7/the parent -- consider
   holding szembek-bk1560 at `partial` ("reading ready") until a fresh re-derivation and a verifier pass are on file,
   and until the per-leaf grading question retrospective p raised is settled one way or the other.

4. **Informational, tool false positive, not a lane failure: `tools/intake_gate_check.py`'s reused-search soft
   warning fires on lope-hurtado-1522 even though the citation is not reused.** `REUSED_SEARCH_RE` matches the bare
   word "re-cited" anywhere in the context window; NOTES.md:5's sentence is "correcting the prior verdict, which
   re-cited Bourdeau's 2026-09-21 search instead of opening the edition" -- describing what the *earlier, now-fixed*
   verdict did, not this pass. bCSLOP's own text and ROOM.md done line (03:18) both independently confirm it opened
   CSP Spain II's `_djvu.txt` directly (31/101/24/168 term hits reported, one `archive.org` request logged), which
   is exactly the citation check.solved.md asks for. No content or gate action needed on lope-hurtado-1522 itself.
   flag: for the parent -- `REUSED_SEARCH_RE`'s soft warning has a false-positive mode when the flagged word appears
   inside a "correcting X" sentence about a past verdict; worth a narrower regex or a second look before RETRO
   proposes fixes to this check again.

## Not re-flagged (already covered)

- Items 12-17 (from QA5/QA6 briefs): none were still open in QA/2026-09-26-0308.md at this window's start, so none
  were re-checked here per the brief's instruction.
- QA6's own three findings (riksarkivet control mean/`partial` label, thurloe-printed status word): the status-word
  fix is confirmed landed (see PASS row above); the control-mean fix attempt is confirmed *not* to have resolved the
  underlying disagreement (see Failure 2, a new/updated finding, not a re-flag of the old one -- the old "6.3 vs
  5.0" framing no longer describes the current state).
- szembek-bk1560 leaves 65/67's own non-test framing and leaf 66's gloss-consistency framing: both PASS on the
  per-leaf files; the merge-level grading concern was already self-flagged by retrospective p at 05:33 and is
  carried into Failure 3 above rather than raised twice.

No other items failed. Rule 3 (control numbers), rule 6 (transcription honesty about below-gate pages), rule 7
(reproducibility) and rule 10 (novelty wording) discipline were otherwise clean everywhere spot-checked this
window, including one exemplary application of rule 3's design-matching headline lesson (malsburg-hessen-1636,
bMAL3) that is worth citing as a positive example in a future retrospective or brief.
