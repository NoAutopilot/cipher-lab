# QA rolling audit, 25 Sept 2026 21:42-21:47 UTC

Worker V6-QA3 (Sonnet), for LANE V6 orchestrator session_01V2BHwhVh1k72qSYuBFCyGd. Window: ROOM.md lines and
commits from 2026-09-25 19:42 UTC to 21:42 UTC (43 ROOM lines this window: LANE R6/B2/B3/GOLD/ZX/ZX2/R7/V6
closing and opening, parent 7c/7d check-ins). Previous run: QA/2026-09-25-1940.md; nothing it flagged is
still open (no follow-up needed). Brief: `.claude/briefs/runs/2026-09-25-lane-v6-qa.md` +
`.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`. Priorities set by the launch prompt: (1) Mercy
Cleues->Eleues propagation; (2) LANE GOLD cycle-2 numbers/family_run.py; (3) rule 3 fold-count amendment;
(4) near_check.py + shared-file hygiene.

## 1. Mercy (espagnol142-mercy-1648) Cleues -> Eleues propagation -- PASS, all four files

R7-MREV (5b08322, 20:51 UTC) moved the reading from the non-blind "Cleues" to the blind-majority "Eleues"
(exceptions.tsv 5 -> 2, S 496/M 26 of 522). Checked every place the priority named:

| file | check | result |
|---|---|---|
| `status.json` line 1889-1891 (board line) | no longer asserts Cleves as read; names the correction explicitly ("the place name read as Cleves earlier rests on a glyph three blind passes read otherwise, R7-MREV 20:51") | PASS |
| `status.json` line 2153-2155 (near-solve row) | "blind majority applied (S 496 M 26, Cleues -> Eleues)" | PASS |
| `NEAR.md` line 21 | full R7 20:45/20:51 sequence logged inline, Cleues->Eleues stated as history | PASS |
| `ciphers/espagnol142-mercy-1648/AUDIT.md` line 308 | correction paragraph appended: "The Cleves name is therefore no longer part of the reading; read every mention of Cleves/Cleues above as the pre-correction text... do not name Cleves in any outward sentence" | PASS |
| `ciphers/espagnol142-mercy-1648/second-opinions/PROMPT-chatgpt.md` line 1 | correction note prepended 21:08 UTC: "Treat every mention of Cleves below as an unconfirmed guess" | PASS |
| `ciphers/espagnol142-mercy-1648/reading.txt` line 17 | text itself reads ELEUES, not CLEUES | PASS |
| `python3 tools/decode_key.py ciphers/espagnol142-mercy-1648 --check` | live: "tokens 522: M 26, S 496" | matches claim exactly, exit 0 |
| `python3 tools/judge_plaintext.py specs/espagnol142-mercy-1648.json --file reading.txt` | live: FAIL -1.031 | byte-identical to the claimed number |

No outward sentence anywhere in these five files asserts Cleves as the current reading; every remaining
mention is either a historical search-log entry (AUDIT.md section 4's search table, NOTES.md's dated
sections) or an explicit correction note. No "first"/"new"/"unpublished"/"never printed" found about our
work in any of the five files. **No flag.**

Second audit (V6-MERCY2, 2159f63, 20:39 UTC, before the correction) and status.json both state N3 kept,
key `ours`, gate 2 (outreach) not met -- JSTOR-QUEUE rows 80-83 (verified: file lines 80-83 are exactly the
four queued Mercy rows, `status: queued`, matches the claim) awaiting the owner. Written as written; no
over-claim.

## 2. LANE GOLD cycle-2 -- PASS on numbers, one informational tooling note

`ciphers/koehler-1944/HYPOTHESES.md` cycle-2 summary table (GOLD-CONS2, fff3352): every row carries
CONTROL (mean, range, seeds, gate) and TARGET (pooled joint ll, noise band) side by side -- 6 corners,
6 complete rows. `ciphers/debosnys-1883/HYPOTHESES.md` (GOLD-D1): same pattern, controls-only this cycle,
every row labelled. **PASS.**

`tools/family_run.py` usage: GOLD-K2 (keyed_running_key, family B') and GOLD-D1 (homophonic) both ran
through `family_run.py`, rows appended by the tool itself (`<!-- family_run.py table -->` markers intact,
not hand-edited). **PASS.**

Family B (standard running-key tableau, GOLD-K1's nl_dev reruns) used `tools/running_key.py` directly, not
`family_run.py --family running_key`. Checked why: `tools/families/running_key.py`'s `solve()` takes one
`--corpus` list for both plain and key streams; Family B's design needs two *different*-language corpora
(plain=de20, key=nl20/nl_dev), which the wrapper does not support. This is a tool capability gap, not a
rule-3 violation -- GOLD-K1 still ran the matched control first and reported both numbers side by side by
hand, same as the tool would. Informational, not flagged against LANE GOLD; worth a `family_run.py`
two-corpus option for the retrospective (rule 8a, "rules become tools") so Family B doesn't have to bypass
the mechanical control-first gate by convention alone.

## 3. Rule 3 fold-count amendment (new 20:01 UTC this window) -- 1 gap found, systemic

Amendment: "report the per-fold spread alongside the blended rate... treat a corpus with under ~5 source
files... as a FAIL/PASS of unknown reliability." Checked every judge-language corpus against its file count
(`tools/data/*`, excluding README/MANIFEST):

| corpus | files | fold-spread documented anywhere? |
|---|---|---|
| es17c | 4 | yes (CLAUDE.md rule 3 lesson: 10.0/21.0/39.5% per volume) |
| pt18 | 4 | yes (`tools/tests/test_judge_plaintext_lang_pt18.py`, README) |
| fr18 | 6 | n/a (>=5) |
| **en** (`LANG_CORPORA["en"]`, judge_plaintext.py line 37) | **2** (pg1661_holmes, pg2701_mobydick) | **no** -- no fold/false-negative computation exists anywhere in the repo for this corpus |
| es17, de16, fr16, da19, es_repo, la_repo, pt_repo, nl_repo | 1-3 each | no (not used for a language-judge FAIL/PASS verdict in this window, so not flagged individually) |

Two in-window judge verdicts used the 2-file `en` corpus for a FAIL and did not state a per-fold spread:

- `ciphers/blitz-ciphers/NOTES.md` line 140-142 (bBLZ2, 21:40 UTC, pushed 228c28a): `FAIL language: score=-1.685` against `LANG_CORPORA["en"]`. Reproduced live, byte-identical. No fold-spread stated.
- `ciphers/mccormick-1999/NOTES.md` cheap test 4 (bMCC4, 20:30 UTC, pushed 882ae5e): judge PASS/FAIL against the same `en` corpus via its word pool. No fold-spread stated.

This is not an individual worker's error -- no per-fold false-negative number for `en` exists anywhere to
paste; only es17c/pt18/fr18 have had that analysis built. `en` is the repo's single most-used judge
corpus (the tool's own default for English), so this gap sits under a large share of every English-language
verdict in the project, not just these two rows. **Flagged to the parent** (cross-lane, not LANE
B3/B4-specific): build a leave-one-file-out fold check for `en` (or accept that a 2-file corpus cannot
produce a meaningful fold spread and say so explicitly in `LANG_CORPORA`'s own comment, the way `es17c`'s
README already states its reliability caveat), then backfill the caveat sentence into any live FAIL/PASS
that cites it, per the amendment's own instruction.

## 4. near_check.py and shared-file hygiene -- PASS

`python3 tools/near_check.py`: `ok: 7 NEAR.md rows, 7 status.json near entries, in step, none closed-negative,
none stale.` (The room-start banner's "8 near-solve rows" counts NEAR.md's closed-rows table too --
pollaky-1865-1875 left the active table 25 Sept 19:08 and near_check.py correctly excludes it; not a
discrepancy.)

- Conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`) in tracked `.md`/`.tsv`/`.json`: none found.
- `status.json`, `QUEUE-scores.json`: both parse as valid JSON.
- `LOCAL-QUEUE.tsv` `id` column: no duplicates.
- `JSTOR-QUEUE.tsv`: no exact target+query duplicate rows; row range "80-83" cited by AUDIT.md/status.json
  for Mercy verified against the actual file (lines 80-83 are exactly the four queued Mercy rows).

One prior-window hygiene incident (LANE B3 orchestrator, 20:23, flagged commit 9a1e34b dropping the LANE B3
STATUS.md section in a `room.py --push` rebase) was self-caught and self-restored by the same orchestrator
in the same ROOM line -- no residual damage found in the current `STATUS.md`.

## Live gate re-runs (check 9)

`tools/intake_gate_check.py` re-run on every target that started or continued deep work this window:
untersberg-code, goldbar-1933, yogtze-1984, erba-2006, blitz-ciphers, fair-game-2010, mccormick-1999,
rayburn-2004, ss-radio-lippert-1944, espagnol142-mercy-1648, koehler-1944, debosnys-1883,
fr2933-salviati-1525 -- all exit 0, all verdicts match the file/ROOM claim. No discrepancy.

## Reproducibility spot-checks (rule 7 / check 10)

- Mercy: `decode_key.py --check` (S496/M26) and judge FAIL (-1.031) both reproduce live, byte-identical.
- clair349 (LANE ZX, ZX-TR349E): `decode_key.py --check` reproduces H29/C260/M611/I98/U22 exactly; status
  correctly stays `partial` (not claimed as a full reading).
- blitz-ciphers judge FAIL (-1.685) reproduces live, byte-identical.

## Summary

Items checked: 1 (Mercy propagation, 5 files) + 1 (GOLD numbers/tool table) + 2 (fold-count corpora) +
1 (near_check) + 4 (hygiene sub-checks) + 13 (live intake-gate re-runs) + 3 (reproducibility spot-checks)
= **25 items**. Failures: **1** (rule 3 fold-count amendment not yet satisfiable for the `en` judge corpus;
systemic, flagged to the parent, not lane-specific). Everything else checked this window -- the Mercy
correction's propagation, LANE GOLD's cycle-2 reporting discipline, `near_check.py`, and shared-file
hygiene -- passed cleanly. Cost/time: well under cap ($5/40 min), no subagents, no network hosts (disk and
git only).
