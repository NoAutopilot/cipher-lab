# Parent worker QA -- rolling quality audit

Session: parent worker QA (Sonnet, session_01LkyvEf8FXwnfxbjbXnP1kn), reporting to the owner-account parent
(session_01FXDfYR3CvGk7tcid1Aav1n). Brief: `.claude/briefs/runs/2026-09-25-parent-quality-audit.md`.
Window: 2026-09-25 06:38-08:25 UTC (where `QA/2026-09-25-0638.md` ended, up to this session's start), lanes PX,
TX, KX, VX and parent workers AUD2/LX-QAFIX/the parent's own check-ins. `date -u` read at start (08:19:21) and
again while writing this file (08:29:30).

Method: read the full ROOM.md window directly (lines 1634-1701, `tools/room.py --digest "2026-09-25 06:38"` cross-
checked, 39/68 lines matched its filter), direct reads of every NOTES.md/AUDIT.md/spec touched in the window,
independently re-ran `decode_key.py --check` (antt-linhares-chave, jan-van-nassau-1572-75's `decode_5551.json`)
and `tx/decode_p10_line10.py --check` (thurloe-printed) myself rather than trust the prose, and ran
`judge_plaintext.py` directly against antt-linhares-chave to confirm the "vacuous" judge claim still holds after
this window's new `tools/data/pt_repo` corpus (it does -- `judge_plaintext.py` isn't wired to read that
directory, so `checks: {}` regardless).

Extra attention per the brief: the WVO 5551 N3 claim's rule 7 judge status and its `ours` key-source label;
Linhares after LX-QAFIX; and worker `done` lines claiming to be under cost cap without a figure.

## Row-by-row

| # | Item | Check | Target | Pass/fail | File : line |
|---|---|---|---|---|---|
| 1 | Intake gate | `open` names edition + pages/full-text search | na-schonenberg-1678-1716 (VX-CS01) | **PASS** | ciphers/na-schonenberg-1678-1716/NOTES.md:2 (26-page finding aid read in full and grepped; Huygens Heinsius retroboeken full-text search with a live-hit control: 413 "Schonenberg" hits vs 0 "Albanilla" hits) |
| 2 | Intake gate | as above | na-oldenbarnevelt-2442-1605 (VX-CS03) | **PASS** | ciphers/na-oldenbarnevelt-2442-1605/NOTES.md:2 (Lonchay & Cuvelier vol. I, full 690pp djvu OCR read/grepped; Bescheiden Oldenbarnevelt Deel 1-3 full-text search, both named editions read directly) |
| 3 | Intake gate | as above | thurloe-barriere-1654 (TX-BARR) | **PASS** | ciphers/thurloe-barriere-1654/NOTES.md:2 (Birch's *State Papers of Thurloe* vol. 2, specific pages, read from page images; BHO edition read in full; Aumale vol. 6 TOC + full-text search; CSPD 1654 full-text search) |
| 4 | Deep work gated on item 1 | worker began only after a compliant `open` | VX-RD01 (schonenberg job 3, key from interlinear gloss) | **PASS** | claimed 08:07, after VX-CS01's 07:24 open verdict; still running at window end, no output to check yet |
| 5 | Deep work gated | as above | VX-CT03 (oldenbarnevelt breadth cryptanalysis, matched control) | **PASS** | claimed 07:53 after VX-CS03's 07:26 open verdict; only `specs/na-oldenbarnevelt-2442-1605.json` written in-window (no `cheap_test_done` yet), nothing further to check |
| 6 | Deep work gated | as above | TX-BARRT (barriere transcription, key_gloss, spec, first test) | **PASS** | claimed 08:15 after TX-BARR's own check-solved in the same job; still running at window end |
| 7 | Rule 7, reproducibility | claimed reading regenerates and exits 0 | antt-linhares-chave (post LX-QAFIX) | **PASS, reproduced** | re-ran `decode_key.py ciphers/antt-linhares-chave --check` myself: "tokens 26: H 24, M 2 / reading up to date", exit 0 -- matches NOTES.md:678 exactly |
| 8 | Rule 7, fresh-instance re-derivation | matches the *current* reading, not a stale one | antt-linhares-chave (LX-QAFIX, clearing QA 0638 row 8) | **PASS** | ciphers/antt-linhares-chave/NOTES.md:609-692: new fresh Sonnet subagent given only the key's parse rule, the 26 raw digit groups and the page images (no prior reading, no key.tsv) -- 24/26 agreement; the one H-graded disagreement (`3241315`/justa) downgraded H->M per the brief's rule rather than argued back up; the other (already M) left unresolved and recorded, not forced. Judge correctly not re-run: still vacuous (verified myself, see Method) |
| 9 | Rule 7 | judge output required only where a spec exists; correctly skipped otherwise | jan-van-nassau-1572-75 / WVO 5551 (no spec in `specs/`) | **PASS** | confirmed no `specs/*nassau*` or `*5551*` file exists; NOTES.md and AUDIT.md both say "no spec, so no judge" rather than fabricate one |
| 10 | Rule 7, reproducibility | as above | jan-van-nassau-1572-75 / WVO 5551 | **PASS, reproduced** | re-ran `decode_key.py ciphers/jan-van-nassau-1572-75 --config decode_5551.json --check` myself: "tokens 32: C 23, I 2, M 1, U 6", matches AUDIT.md:212 exactly |
| 11 | Rule 7, reproducibility | as above | thurloe-printed P10 L10 | **PASS, reproduced** | re-ran `tx/decode_p10_line10.py --check` myself from inside the folder: "5/14" agree, "0/14" unread by either -- matches TX-VERP10's AUDIT.md:752-760 figures exactly |
| 12 | Rule 10, key-source label | `ours`/`period`/`published` correctly distinguished | jan-van-nassau-1572-75 / WVO 5551, key `ours` (Lodewijk's 1574 table, rebuilt by aligning WVO 4613/4615's own ciphertext to their imaged **period** decipherment) | **PASS on closer check, not a violation** | AUDIT.md:293,305,307,316. Checked this against the same-day precedent set by AUD2 for antt-linhares-chave/AUDIT.md:333-341, which the parent's own key-source rule presumably means to apply consistently: even though the *underlying artifact* there (a "Chave de uma cifra" sheet) is period, AUD2 classed the key `ours` because identifying/aligning the missing piece (there, the codebook; here, the figure-to-letter mapping across two sibling ciphertexts using their own period plaintexts) is itself "a plain-copy alignment", which rule 10's own wording places under `ours`. The Lodewijk table was built the same way: two independent transcription passes of 4613/4615's ciphertext AND their period decipherment sheets, reconciled and aligned by this project (`ciphers/lodewijk-van-nassau-1573-74/NOTES.md`), not simply copied from an already-tabulated period key. `period` would fit a case where a period key sheet or cipher book already gives the mapping directly and we only transcribe it -- no instance of that class was found anywhere in the repo this pass (`grep` for an assigned `period` key-source class returns zero rows outside of unrelated prose use of the word "period"). Not flagged; recording the reasoning here so a future audit does not have to re-derive it. |
| 13 | Rule 10, key-source correction | over-claim caught and fixed within the window | thurloe-printed P10 L10 (TX-VERP10) | **PASS, good example** | brief proposed `ours`; verifier found Powell 1937 already prints a connected reading through the gap (confirmed by an identical clear-text sentence following the cipher in both Birch and Powell) and correctly reclassed N0, key `published`, not `ours` (AUDIT.md:802-828). Correction appended to NOTES.md s.22 (:1991-2002) pointing to AUDIT.md; the older P9/P10/P14/P15 section is explicitly left as "under-reporting, not over-claiming" (still true on its own narrow point) rather than silently wrong -- acceptable, not flagged |
| 14 | Rule 10, banned words | no "first"/"new"/"unpublished"/"unread"/"never printed" about our own work | every window-touched folder (antt-msliv0638-brochado-1712, jan-van-nassau-1572-75, thurloe-printed, thurloe-barriere-1654, na-schonenberg-1678-1716, na-oldenbarnevelt-2442-1605, na-janssens-java-1811, na-suriname-map-1781, na-raad-azie-1800, alvin-gustav3-chiffre, fr5160-letellier-1653, colbert26-lathuillerie-1644, oldenbarnevelt-brederode-1605, antt-linhares-chave) | **PASS** | every hit for the tight banned phrases ("first decipherment", "previously unread", "newly recovered", "unpublished plaintext", "never printed/read/deciphered") is either a credential-never-printed note, an explicit unsafe-sentence citation, or (thurloe-printed AUDIT.md:825) a negation ("...is not the first reading of this passage") -- no live over-claim found |
| 15 | Rule 3, matched control | target/control reported side by side | KX-XMATCH2 mechanical positive-control repair (tools/key_crossmatch.py) | **PASS** | KEY-CROSSMATCH.md / ROOM.md:1688 -- "8 of 35... 0 false positives on 8 scored negative pairs", the real-text floor's failure against a corpus-register gap documented rather than papered over |
| 16 | Transcription, own stricter gate | correctly halted rather than force a rebuild | antt-msliv0638-brochado-1712 pass B (PX-BROPASSB) | **PASS** | ciphers/antt-msliv0638-brochado-1712/NOTES.md "PX-BROPASSB" section: 85.84% agreement against the job's own 90% gate (stricter than the general 60% floor) -- stopped, `disagreements.tsv`/`agreement.tsv` written, `key.tsv` correctly left unrebuilt, root cause (one recurring glyph-shape confusion, 47.5% of disagreements) isolated rather than blamed diffusely |

**Items checked: 16. Failures: 0 live.** Two already self-corrected within the window by the orchestrators
themselves before this audit ran (see below, not re-flagged).

## Findings not tied to a single checklist row

**Cost-cap "well under" lines with a literally blank dollar figure (VX-CS03 ROOM.md:1661, VX-SCNORD ROOM.md:1675,
VX-CS05 ROOM.md:1700, and pre-window parent worker AUD2 ROOM.md:1629): "well under [blank] stall alarm" /
"well under the [blank] stall-alarm cap" / "well under both jobs' [blank] stall alarm combined" (two spaces where
a `$N` figure belongs, confirmed against the raw file, not a rendering artifact) -- the dollar figure is missing
entirely, not merely unverified.** This is distinct from (and in addition to) the already-known and
already-fixed problem the parent named at ROOM.md:1701 ("a worker's own 'under cap' line is not evidence") after
PX-BROKEY (3.4x its $9 alarm) and PX-BROKEY2 (1.8x its $8 alarm) both wrote unverified "well under cap" lines
this window and were caught and ledgered by their own orchestrator (LEDGER.md:577,587) before this audit started
-- those two are real overruns, already actioned, not re-flagged here. The blank-figure instances above are a
narrower, template-level bug: whichever brief text these four workers copied their closing line from has an
unfilled `$N` placeholder, distinct from the get_session-monitoring fix the parent already ordered for the
accuracy problem. All four workers' actual costs were later reconciled to reasonable figures by their
orchestrators (VX-CS03 $3.52, VX-SCNORD $8.14, per ROOM.md:1679; AUD2's own cost not separately ledgered in this
window), so no target's result is at risk -- this is a brief/template fix, not a target-level failure.

## Failures flagged to ROOM

None from the six checklist items this pass -- flagging the template-blank cost-figure pattern above instead,
since it is cross-lane (VX and a parent worker both hit it) rather than one lane's error.

## Not covered

`na-janssens-java-1811`, `na-suriname-map-1781`, `na-raad-azie-1800`, `alvin-gustav3-chiffre` (VX-CS04/CS05
check-solved-only rows, no deep work yet) were read for intake-gate wording but not deep-audited beyond that --
no key or decode work has started on any of them. `colbert26-lathuillerie-1644` and `fr5160-letellier-1653`
(KX-COLB26P1, KX-LATHKEY) were spot-read for the same reason: leaf-map/check-solved only, no claimed reading yet.
`oldenbarnevelt-brederode-1605` (TX-BRED) was only a claim line at window end, nothing to audit. LANE PX's
body-sweep worker (PX-BROBODY) and KX-LATHKEY were both still running past window end and are next pass's work.
