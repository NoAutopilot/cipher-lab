# V9-QA8 rolling QA sweep

Window: 2026-09-26 05:44 UTC (V8-QA7's start/cutoff) to 2026-09-26 07:35 UTC (this session's start, clock read).
Source: ROOM.md lines 2619-2712, `git log --since "2026-09-26 05:44:00"` (54 commits), and the touched files.
Previous run: QA/2026-09-26-0544.md (V8-QA7; 3 findings + 1 informational, all routed). Per this brief, re-checked
only QA7 items still open there (findings 1-3); items 12-17/19-27 from earlier briefs were not still open at QA7's
close and are not re-litigated. New items for this window: 28 (bSZG2 per-leaf control replacement), 29
(hessen-1824 partial + NEAR row), 30 (armstrong-madison-1808 en18 caveat). Disk + git log + tool re-runs only, no
subagents, no network.

## Re-check of QA7's open findings

| # | Finding | Result | File:line |
|---|---|---|---|
| 1 | lodewijk-van-nassau-1573-74 status.json WVO 5797 row stale (qa_flag/key/text/172) | **PASS, fixed** -- row now reads `"key": "period + ours"`, `"novelty": "N4"`, carries a `"text": "known (Groen IV no. CDXLIV...)"` field, and 172 is correctly kept out of the N4 grade (described as N2/contested, grade M, "no licensed value") rather than folded into the row's N4 count. AX2-172's list A/list B direction-of-correspondence finding is recorded in NOTES.md:2512-2632 (rule 4's own worked example). | status.json:1077-1087; ciphers/lodewijk-van-nassau-1573-74/NOTES.md:2512-2632 |
| 2 | riksarkivet-r4282-1628 control-count disagreement (spec 8,3,8 vs NOTES/STATUS 3,4,8) | **PASS, fixed** -- bRIKFIX (06:56-06:57) re-derived via `crib_test.py --check`, confirmed the spec's 8,3,8 (mean 6.3) was the correct triple, and corrected NOTES.md:91-92 and STATUS.md:319 to match. All three now agree; verdict (crib count 4 sits inside/below the range) unchanged. | ciphers/riksarkivet-r4282-1628/NOTES.md:91-99; STATUS.md:319; specs/riksarkivet-r4282-1628.json:27 |
| 3 | szembek-bk1560 set `solved` with no rule-7 fresh re-derivation and no verifier N-class | **PASS, fixed** -- bSZR (05:49-05:52, a separate worker session, spec+key only) re-derived rule-7: exit 0, 434 tokens C384/M50, 0 value/grade diffs, 10/10 spot-check gloss matches. V8-SZEM (verifier, 06:18-06:40, separate session from every solver worker) wrote AUDIT.md: whole item N0, key `period`, text not in print, safe/unsafe sentence lists provided. NOTES.md:1 `solved` is now supported by both a fresh re-derivation and a verifier class, matching the bar set by lodewijk/matignon. Retrospective p's merge-grading concern (codes attested only on leaves 65/67 graded equally to leaf 66's) is addressed by item 28 below, closing the loop. | ciphers/szembek-bk1560/AUDIT.md:1-19; NOTES.md:1-6,355 (bSZR section) |

## New items (28-30)

| # | Item | Result | File:line |
|---|---|---|---|
| 28 | bSZG2 (07:13): szembek leaves 65/67 per-leaf controls replaced (tied -> clearing) | **PASS** -- NOTES.md:445-504 states both the old non-test numbers (0/18=0.0 vs 0.0; 0.429 vs 0.429) and the new letter-level per-leaf numbers (leaf 65: 119/135=0.881 vs shuffle mean 0.253/p95 0.281; leaf 67: 79/98=0.806 vs 0.298/0.337) side by side, explicitly framed as replacing a non-test per rule 3's control-must-differ paragraph. `key.tsv`'s note field for the three thin codes (19, 24, 48) cites bSZG2 by name and explains the downgrade is independent of the per-leaf gate. Reading unchanged (`decode_key.py --check` exit 0, same C384/M50 as before). | ciphers/szembek-bk1560/NOTES.md:445-504; key.tsv:7,11,20 |
| 29 | hessen-1824 moved to `partial` with a NEAR row (B8 07:31) | **PASS** -- NOTES.md:1 = `partial`; NEAR.md:37 carries the row with all four family numbers (periodic_vigenere control 1.000/target FAIL; masc control 0.823/target FAIL; homophonic profile=target control 0.878/target FAIL; running_key control 0.372 vs gate 0.6, CONTROL BELOW GATE) and a named next step; status.json:2261-2266 has a matching `near` entry with the same summary. `tools/near_check.py`: "11 NEAR.md rows, 11 status.json near entries, in step, none closed-negative, none stale" (live re-run). | ciphers/hessen-1824/NOTES.md:1; NEAR.md:37; status.json:2261-2266; tools/near_check.py (live) |
| 30 | armstrong-madison-1808 en18 judge (ARM-EN18, 07:07): per-fold spread caveat (0.26-0.27) | **finding, partial miss** -- see Failures section. The build note itself (NOTES.md:193-208, ROOM.md:2696) states the caveat correctly and completely. But ARM-A2 (26 Sept, in-window, `eedb4de`), which cites the en18 judge's FAIL verdicts as the decisive signal for a negative ("no table transfers... all four FAIL `tools/judge_plaintext.py`'s language check outright"), does not repeat or cross-reference the 0.26-0.27 per-fold-spread caveat anywhere in its own NOTES.md section (259-282) or its HYPOTHESES.md section (74-120+), even though HYPOTHESES.md explicitly flags a different, unrelated judge caveat (the syllable-code positive-control artifact) in the same passage. | ciphers/armstrong-madison-1808/NOTES.md:259-282; HYPOTHESES.md:74-98 |

## Other spot checks (rules 1, 3, 6, 7, 10, shared-file hygiene)

- **Intake gate, live re-run, all seven targets touched this window** (lodewijk, szembek, riksarkivet, matignon,
  malsburg, siena, hessen-1824): all exit 0, no soft warnings, status words match each target's NOTES.md:1. PASS.
- **Rule 3, spot-checked new negatives this window** (bHCP2/hessen-1824, bSIE2/siena, ARM-A2/armstrong,
  bMALC+bMAL3/malsburg pooled control): every one reports target and control (or control-below-gate) numbers
  together in the same sentence (e.g. bSIE2: "control mean 0.985 (6+24) / 0.842 (20+23)... target judge FAIL both
  (score -1.372/-1.384 vs real_p05 -0.925/-0.954)"; bMALC: "0.928 / 0.844 / 0.976 for N = 1000/2000/3000 (gate
  0.6)"). PASS.
- **Rule 6, transcription two-pass gate**: bSZG2's pass B on leaves 65/67 is the second blind pass this window;
  reconciliation log (`disagreements_b.tsv`) settles every disagreement against the crop image, one clear-text
  word fixed, no code value changed. PASS.
- **Rule 10 wording**: grepped every touched cipher target folder (lodewijk, szembek, riksarkivet, matignon,
  malsburg, siena, hessen-1824, armstrong, zeschau-seebach-1841, untersberg-code) and ROOM.md lines 2685-2712 for
  "first decipherment", "previously unread", "newly recovered", "unpublished plaintext", "never printed", "never
  read", "first to decode/decipher/read" -- every hit is inside an AUDIT.md "unsafe example" list (guidance, not a
  live claim); ROOM.md has none. PASS.
- **Shared-file hygiene**: no conflict markers (`<<<<<<<`/`=======`/`>>>>>>>`) repo-wide; `status.json` and
  `QUEUE-scores.json` both parse; no duplicate `id` in LOCAL-QUEUE.tsv; JSTOR-QUEUE.tsv has no `id` column (its
  repeating `target` column is not a duplicate-ID violation). PASS.
- **ROOM.md role names**: every line in the window (2619-2712) carries a real role/session identifier as its
  second field; none match a leading-flag shape. PASS.
- **status.json/near_check.py**: 11 rows, in step, none closed-negative, none stale (live re-run). PASS.

## Failures / findings requiring a flag

1. **armstrong-madison-1808/ARM-A2 cites the en18 judge's FAIL verdict as its decisive language-check signal
   without the 0.26-0.27 per-fold-spread caveat that the same file's own ARM-EN18 build section (NOTES.md:193-208)
   established minutes earlier in the same window.** The caveat is real and documented once, but rule 3's EN-FOLDS
   amendment (CLAUDE.md) requires it wherever a verdict is drawn from the judge, not just where the corpus is
   introduced -- the precedent is QA6's finding on the plain `en` corpus, which flagged exactly this shape of gap.
   ARM-A2's negative does not rest solely on the language judge (coverage and percentile-vs-control numbers are
   also reported, and the verdict would likely be unchanged), so this is a documentation gap, not grounds to
   revisit the negative itself. flag: for LANE ARM -- add one sentence to ARM-A2's NOTES.md/HYPOTHESES.md section
   noting the en18 per-fold spread (0.26-0.27, still above the 0.05 gate) alongside the FAIL verdicts it reports.

## Not re-flagged (already covered / resolved this window)

- QA7 findings 1-3: all three fixed this window (lodewijk status.json, riksarkivet counts, szembek re-derivation +
  verifier) -- see table above, not re-flagged.
- QA7's informational item 4 (`intake_gate_check.py`'s reused-search false positive on lope-hurtado-1522): no new
  information this window; not re-flagged, matching the brief's "do not re-flag items already flagged unless still
  open" and this one being informational only.

No other items failed. Rule 1 (intake gate), rule 3 (control numbers), rule 6 (transcription honesty), rule 7
(reproducibility) and rule 10 (novelty wording) discipline were clean everywhere spot-checked this window,
including three closed loops from the previous run's open findings (lodewijk status.json, riksarkivet counts,
szembek re-derivation/verifier) landing correctly in this one.
