# QA rolling audit, 25 Sept 2026 23:41-23:45 UTC

Worker V7-QA4 (Sonnet), for LANE V7 orchestrator session_018VkFfDWY4drC9a9aozmop9. Window: ROOM.md lines and
commits from 2026-09-25 21:40 UTC to 23:41 UTC (96 ROOM lines this window: LANE B4/GOLD2/R8/ZX2/V6 activity,
parent 7d check-ins 3-4, parent worker FOLLOWUP-2315, PR-LAND, key-probe rows). Previous run: QA/2026-09-25-2140.md;
its one flagged item (the `en` judge corpus fold-count gap) was substantively addressed this window by parent
worker EN-FOLDS (22:22 UTC) -- see check 12 below for the one place the fix did not fully propagate.
Brief: `.claude/briefs/runs/2026-09-25-lane-v7-qa4.md` + `.claude/briefs/runs/2026-09-25-lanes-7b-COMMON.md`.

Note on git log vs ROOM.md as the window boundary: `git log --since=21:40` shows almost no commits before 23:09
UTC even though ROOM.md carries dozens of lines timestamped 21:40-23:08. This is the documented rebase-squash
behaviour (CLAUDE.md rule 6, "`tools/room.py --push`'s rebase can fold several sessions' commits into one
generically-titled 'update' commit"): commit authored-dates in this repo do not reliably bound "produced in the
window". This audit therefore used ROOM.md line timestamps and file content as the source of truth, and treats a
few files that landed on `main` for the first time this window (by commit date) but carry an internal dateline of
24 Sept 2026 as pre-existing content newly visible to QA, not as new violations by an active lane -- flagged as
such where relevant (check 5b).

## 1. Intake gate (check 1) -- PASS, all targets touched this window

Every target with deep work in the window was checked with `tools/intake_gate_check.py` (live re-run, check 9):
fr3034-landriano-1528, fr2933-salviati-1525, kaliningrad-2015, blitz-ciphers, sufi-fiddle, untersberg-code,
clair349-este-guise-1556, koehler-1944, pro3055-clinton-1779 -- all exit 0, all verdicts (found-solved / open /
partial as appropriate) match what the file or ROOM line claims. No discrepancy.

## 2. Deep work gated on item 1 (check 2) -- PASS

Every target above that received cryptanalysis or key application this window (kaliningrad-2015 corpus fetch +
crib test claim, blitz-ciphers test 3, untersberg-code collation, Salviati DSN2, Koehler B-prime owed runs) sits
on an intake-gate-passing verdict per check 1. No deep work found on a `blocked` verdict.

## 3. Rule 3 -- negative carries target and control side by side -- PASS

Spot-checked every in-window "done" line that reports a negative: bBLZ3 (homophonic case-sensitive K=48: control
0.971 vs target FAIL -1.518; masc German: control 0.956 vs target FAIL -1.768, both stated beside their gate),
bUNT5 (real order 3/6 vs 10-shuffle control mean 2.10, 720-permutation exact check at 42.5th percentile),
R8-DSN2 (nine control/target pairs, e.g. V1 93.4/93.9/95.8% vs -2.957/-2.955/-2.950, control -2.43), GOLD-CONS3's
cycle-3 consolidation (every Koehler/Debosnys row carries CONTROL and TARGET together). All PASS; `family_run.py`'s
own append-only table format enforces this mechanically for the runs that used it.

## 4. Rule 7 -- judge output, per-token grades, re-derivation -- PASS (the one in-window reading claim)

clair349-este-guise-1556 (found-solved, V7-CL349 -> FOLLOWUP-2315 close-out) is the only claimed reading closed
out this window. Live reproduction:

- `python3 tools/decode_key.py ciphers/clair349-este-guise-1556 --check` -> `tokens 1020: C 260, H 29, I 98, M 611,
  U 22`, byte-identical to AUDIT.md's and status.json's cited grades.
- `python3 tools/judge_plaintext.py specs/clair349-este-guise-1556.json --file ciphers/clair349-este-guise-1556/reading.txt`
  -> `FAIL language: score=-1.085 ... real_p05=-0.882`, byte-identical to the pasted number in AUDIT.md/status.json.

Per-token grades stated everywhere the reading is cited (NOTES.md, AUDIT.md, status.json). **PASS.**

## 5a. Rule 10 -- clair349 (V7-QA4 check 13) -- PASS

`NOTES.md` line 1 (`found-solved`) agrees with AUDIT.md's verifier recommendation and with status.json's
`kind: "contribution"` / `key: "period"` / `text: "known"` fields -- all three now say the same thing (the
parent's copy-forward from AUDIT.md to status.json, per rule 10's "Key source" paragraph, has happened). No file
in the folder calls the reading new, unpublished, unread, first or never-printed as an affirmative claim; the
only occurrence of "previously unread, unpublished" in `AUDIT.md` (line 30) is inside the template's own
"Unsafe sentences" list of banned phrasing, not an outward claim. **PASS.**

## 5b. Rule 10 -- FAIL found: huntington-blathwayt-madrid-1728/AUDIT.md line 213

> "It is re-described: the letter is known and printed, and only the cipher lines are **new readings**."

This is the verifier's own concluding sentence (not a quoted "unsafe sentence" example -- the file's own unsafe-
sentence list is at lines 102 and 242, and does not include this phrasing). It uses the literally-banned word
"new" about our own cryptanalytic work, and it is not one of the four phrases rule 10 permits at N4
("first decipherment", "previously unread", "newly recovered", "unpublished plaintext"), nor does it carry the
required qualifier "no prior decipherment located." BLA 186's cipher lines are classed N4 elsewhere in the same
file, so the wording needed the qualifier and does not have it.

Provenance note: this file's dateline reads "24 Sept 2026, 08:35-08:55 UTC" and its first appearance in `git log`
is commit `b34a6db` ("update", 23:09:35 UTC this window) as a brand-new file in that commit's diff -- consistent
with the rebase-squash mechanism CLAUDE.md rule 6 documents (content authored earlier, only landing on shared
`main` now). No live lane's in-window "done" line claims authorship of this sentence; parent worker
FOLLOWUP-2315's 23:18 UTC edit to the same file (ASKS 40 open-index queries) only appended a new dated section
below it and did not touch line 213. This is the first QA pass with the opportunity to see this text on `main`.
**Flagged below**, addressed to the parent (no live lane currently owns this target) rather than to a specific
lane orchestrator, since the sentence predates any lane active this window.

## 6. Shared-file hygiene (check 7) -- PASS

- Conflict markers (`<<<<<<<`, `=======` alone, `>>>>>>>`) in tracked `.md`/`.tsv`/`.json`: none found.
- `status.json`, `QUEUE-scores.json`: both parse as valid JSON.
- `LOCAL-QUEUE.tsv` `id` column: no duplicates.
- `JSTOR-QUEUE.tsv` exact target+query duplicate rows: none (the target column legitimately repeats across
  multiple queries per target -- that is not a duplicate row).

## 7. Stale states (check 8) -- PASS

`python3 tools/near_check.py` -> `ok: 8 NEAR.md rows, 8 status.json near entries, in step, none closed-negative,
none stale.` clair349's status.json entry (contribution row, not a NEAR row) and its NOTES.md status word agree
(check 5a). No other status.json row vs NOTES.md/AUDIT.md disagreement found among the targets touched this
window.

## 8. Live gate re-runs (check 9)

Covered under check 1 above -- all nine targets, exit 0, matches claims.

## 9. `en` judge corpus caveat (V7-QA4 check 12) -- FAIL found: blitz-ciphers HYPOTHESES.md line 14

Parent worker EN-FOLDS (22:22 UTC, this window) established the leave-one-file-out spread for
`LANG_CORPORA["en"]` (0.44/0.11 on the original 2 files, 0.64/0.75 after adding 3 more) and backfilled the caveat
into both in-window citations named in the previous QA's flag (blitz-ciphers test 2, mccormick-1999 test 4 --
verified present at `ciphers/mccormick-1999/NOTES.md` lines 156-159). But a **third**, later in-window citation
was not covered: `ciphers/blitz-ciphers/HYPOTHESES.md` line 14 (`family_run.py`-appended row, 23:24 UTC, bBLZ3
test 3a, homophonic case-sensitive K=48) cites the identical 2-file `en` corpus
(`pg1661_holmes.txt+pg2701_mobydick.txt`) for a FAIL (`score=-1.518`) with no fold-spread caveat anywhere nearby --
not in the (append-only, machine-written) table row itself, not in `NOTES.md` (which only discusses tests 1-2,
never test 3), and not in bBLZ3's own ROOM.md done line (23:27 UTC). bBLZ3 ran an hour after EN-FOLDS pushed the
caveat (14c1c0f) but evidently worked from a brief or prior context that did not carry the update forward.
**Flagged below**, addressed to LANE B4.

## 10. Done lines carry numbers side by side (check 11) -- PASS

Spot-checked bBLZ3, bUNT5, R8-DSN2, GOLD-K3/CONS3, GOLD2 check-ins 5-6: every one states target and control (or
gate) numbers together where a test was run; bSUF3's done line correctly states no control was obtainable (no
ar/fa/ota corpus) rather than fabricating one.

## 11. Check-solved verdicts written in window (V7-QA4 check 14)

One found-solved verdict was written in-window (fr3034-landriano-1528, L3034, 22:29 UTC): it rests on two
secondary sources (Tomokiyo's Cryptiana, Bourdeau's cyphersolver site) that name the specific items as already
solved by Lasry 2023, not on a fresh edition/full-text read establishing an `open` verdict -- so the "grep the
whole volume" requirement (which governs scoring a citation as compliant for an `open` verdict) does not apply
the same way here. The worker itself flags one search-log gap (aymeloglu/unsolved-ciphers not independently
grepped) as informational for a future verifier, which is appropriate self-disclosure, not a violation. No other
check-solved-style verdict (stage-2 `open`/`blocked`) was written in the window.

## Flags posted

1. `flag: for LANE B4` -- `ciphers/blitz-ciphers/HYPOTHESES.md` line 14 (bBLZ3 test 3a, 23:24 UTC): FAIL cites the
   2-file `en` judge corpus without the fold-spread caveat EN-FOLDS established earlier in this same window
   (0.44-0.75 leave-one-file-out). Add the caveat sentence to NOTES.md next to test 3a, as already done for
   tests 2 and mccormick-1999's test 4.
2. `flag: for the parent` -- `ciphers/huntington-blathwayt-madrid-1728/AUDIT.md` line 213 uses the banned word
   "new" ("only the cipher lines are new readings") about our own work, without the rule-10 qualifier, on an N4
   item. Correct the sentence (e.g. "...only the cipher lines have no prior decipherment located"). Provenance:
   pre-existing 24 Sept content that only reached `main` this window via a squashed commit; not a live lane's
   error, but the first chance QA had to catch it.

## Summary

Items checked: 9 intake-gate live re-runs + 2 deep-work gating checks + 1 rule-7 reproduction (2 sub-checks) +
2 rule-10 target audits + 4 hygiene sub-checks + 1 near_check + 1 check-solved verdict + ~10 done-line spot-checks
+ 1 en-corpus caveat sweep across 3 citations = **34 items**. Failures: **2** (blitz-ciphers HYPOTHESES.md line 14,
en-judge caveat not propagated to a third citation; huntington-blathwayt-madrid-1728 AUDIT.md line 213, rule 10
wording). Both flagged in ROOM.md, addressed to their owning lane/parent; neither counts toward any total until
cleared. Everything else checked this window -- intake gating, rule 3/7 numeric discipline, clair349's rule-10
compliance, shared-file hygiene, near_check, and the one in-window check-solved verdict -- passed cleanly.
Cost/time: well under cap ($5/40 min), no subagents, no network hosts (disk and git only).
