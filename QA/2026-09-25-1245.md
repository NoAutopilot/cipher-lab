# QA run 4 -- window 2026-09-25 10:36 UTC -> 12:45 UTC

Parent worker QA (Sonnet, session_019SR3eNrE7asnrdpLuLphEY). Brief:
`.claude/briefs/runs/2026-09-25-parent-quality-audit.md`. Scope: every ROOM.md line and commit in the window
(lanes YX, CX; parent workers RETRO-APPLY-H and the parent's check-ins; KX and VX closing handoffs). Checked
against CLAUDE.md rules 1, 3, 7, 10 (key source) and the Pipeline intake gate, per `.claude/briefs/check-solved.md`
and `.claude/briefs/verifier.md`. Not audited: `ciphers/antt-linhares-chave`'s judge question (parent worker
LX-JUDGE holds it) -- only checked that the FAIL re-run pasted into its NOTES.md reproduces byte-for-byte.

Note on method: one push this window (`81c6cbc`, 11:51:17) committed literal unresolved git-stash conflict
markers into `LOCAL-QUEUE.tsv` (see failure 1) -- found by grepping every tracked `.md`/`.tsv`/`.json` file for
`<<<<<<<`/`=======`/`>>>>>>>`; no other file in the window carries this. Separately, one push this window
(`d6e205b`, 11:50:02, CX-FR16's) shows ~20 long-lived files (AUDIT.md, briefs, etc.) as pure additions in `git
show --stat`, the same rebase-fold artifact CLAUDE.md's 24 Sept lesson and QA run 3 both already document
(a session's local branch replays intervening history as new commits); spot-checked one (`jan-van-nassau-1572-75/
AUDIT.md`) and its content is unchanged and correct, so this is not re-flagged as a new failure, only noted so a
future session does not mistake `git log --since` on these paths for a real change list.

## Table

| # | target / item | check | pass/fail | file : line |
|---|---|---|---|---|
| 1 | LOCAL-QUEUE.tsv | shared-file hygiene: no unresolved conflict markers, no duplicate row IDs | **FAIL** | LOCAL-QUEUE.tsv:14,16,18 |
| 2 | clair349-este-guise-1556 (YX-TR349) | intake gate: `tools/intake_gate_check.py` exit code matches the file's own pasted claim, right now, for a target with deep work still queued (steps 2-3) | **FAIL** | ciphers/clair349-este-guise-1556/NOTES.md:1-9 |
| 3 | thurloe-barriere-1654 (YX-BARB) | rule 3: re-reported control after pass B keeps target and control numbers side by side, before and after | PASS | ciphers/thurloe-barriere-1654/NOTES.md:400-403 |
| 4 | thurloe-barriere-1654 | rule 7: `coverage_test.py` and `matched_control.py --seeds 10 [--ctpath ciphertext.tsv]` reproduce the committed 30.0%/31.1%/33.4% figures exactly | PASS | ciphers/thurloe-barriere-1654/coverage_test.py, matched_control.py (run this pass, see report) |
| 5 | thurloe-barriere-1654 | QA run 3 flag ("negative rests on one unreconciled pass") cleared: pass B landed, reconciled against the image, negative confirmed unchanged (29.3%->30.0% vs control 33.4%->31.1%) | PASS | ciphers/thurloe-barriere-1654/NOTES.md:2-6 |
| 6 | colbert26-lathuillerie-1644 (YX-FIX) | QA run 3 flag ("Kind section not corrected after 09:59 retraction") cleared | PASS | ciphers/colbert26-lathuillerie-1644/NOTES.md:149-156 |
| 7 | colbert26-lathuillerie-1644 | QA run 3 flag (canvas 21 57.6%, below the 60% gate, folded in unflagged) cleared: held back, excluded from "reconciled" wording in NOTES.md and the spec, pre-existing cheap-test result annotated (not deleted, not silently reused) as resting partly on unsettled data | PASS | ciphers/colbert26-lathuillerie-1644/NOTES.md:349-355; specs/colbert26-lathuillerie-1644.json (`ciphertext`, `cheap_test_done.tokens_used`) |
| 8 | tools/intake_gate_check.py | offline test suite passes, `partial` gated like `open`, Linhares stays `blocked` (nearby-blocked-wins logic) | PASS (see #2 for a live-file regression this fix does not catch) | tools/tests/test_intake_gate_check.py (run this pass, 1 failure -- clair349, matching #2) |
| 9 | CX check-solved verdicts, sampled by hand (14 of ~21) | intake gate item 1: `open`/`partial` verdict names a real standard edition + real pages/full-text search actually read by the worker, not a stale/borrowed citation | PASS, all 14 | la-garde-1577, birago-nevers-1571, ormond-arran-1678, bl-james-1669, sp53-22-f52, moray-wood-1568, courten-diary, clair1161-avis-flandre-1688, clairambault296-paget-1713, espagnol142-mercy-1648, clair571-estrades-1645, decode-4450-bnf-fr20506-1525, bl-sacchetti-nunzio-1623, bl-gualterio-1700 -- each NOTES.md:1-8 |
| 10 | fr16092-maisse-1582, sp53-16-78, sp53-16-79, fr4687-paleologue-nevers | CX `blocked` verdicts state what was tried and what blocked it (not a bare word) | PASS, all 4 | each NOTES.md:1-2 |
| 11 | fr4687-paleologue-nevers | intake-gate self-correction: CX-FR16 wrote `open` with Ferrari 1999 unread; CX orchestrator caught and corrected to `blocked` same window, LEDGER graded X | PASS (breach self-caught before reaching the board) | ROOM.md:1842; LEDGER.md (CX-FR16 row, grade X); ciphers/fr4687-paleologue-nevers/NOTES.md:4 |
| 12 | bl-charles-digby (CX-STUART) | found-solved (F1) reclassification: citation (Wheatstone 1862 pamphlet pp.321-330 + BL catalogue record 032-003442981) is real, four-point comparison is accurate to both sources, hedged as circumstantial not certain, explicitly held for a verifier before any outward claim, no rule-10 wording used | PASS | ciphers/bl-charles-digby/NOTES.md:1-62; ROOM.md:1826,1839 |
| 13 | Rule 10 wording sweep, every NOTES.md touched in the window | no "new/first/unpublished/unread/never printed/newly recovered/previously unread" used as a claim about our own reading or decipherment | PASS -- 0 violations. All hits are benign ("first test", "first pass", "new sign", "new lead", "no new intake-relevant material", explicit rule-10 disclaimer sentences) or describe a real access/search gap ("Rommel... remain unread -- flagged as a genuine gap") | grep sweep, ~17 NOTES.md files in the window (see report) |
| 14 | YX-PTJUDGE pt17/it16 corpus wiring | rule 7: judge re-run FAILs pasted into brochado-1712 and linhares-chave NOTES.md reproduce exactly on live re-run; offline test passes | PASS | ciphers/antt-msliv0638-brochado-1712/NOTES.md:1391-1406; ciphers/antt-linhares-chave/NOTES.md:698-709; tools/tests/test_judge_plaintext_lang.py (run this pass, ok) |
| 15 | moray-wood-1568 (CX-MARY) | rule 8 credit: Aymeloglu's partial cryptanalytic reading (119/134 S, z=17.0) quoted and credited, not adopted as this repo's own; status set to `partial` (not `open`, not `found-solved`) per check-solved.md's M9/fr.2996 precedent | PASS | ciphers/moray-wood-1568/NOTES.md:1,13,15,17 |
| 16 | na-janssens-java-1811, na-suriname-map-1781 (VX closing) | rule 10 wording sweep on this window's additions; no forced reading where coverage/gate falls short | PASS -- 0 violations; both closed `partial` with a named next step, no reading forced | ciphers/na-janssens-java-1811/NOTES.md; ciphers/na-suriname-map-1781/NOTES.md |
| 17 | STATUS.md / ROOM.md check-ins this window | rule 10 wording; cost-per-solve box present; no blank-`$` lines (QA run 2's fixed flag, regression check) | PASS | STATUS.md:127; ROOM.md (window lines) |
| 18 | status.json, QUEUE.md, JSTOR-QUEUE.tsv | shared-file integrity (valid JSON, no conflict markers) | PASS | (checked this pass, clean) |

## Failures (2)

1. **LOCAL-QUEUE.tsv:14-18** -- literal unresolved git-stash conflict markers are committed to `main`
   (`<<<<<<< Updated upstream` / `=======` / `>>>>>>> Stashed changes`), introduced in commit `81c6cbc`
   ("update", 11:51:17 UTC). CX-BRIT (`e0f4c13`) and CX-MARY (11:51 ROOM note) each independently appended a
   row with id **L13** to this shared file within the same minute -- CX-BRIT's for catokwacopa-1875
   (bna-verify), CX-MARY's for sp53-16-78/sp53-16-79 (hathitrust) -- and whichever session ran
   `tools/room.py --push` (or a plain `git stash`/rebase) second left the markers in the committed file instead
   of resolving the merge. Effect right now: the file has two different rows both titled `L13` (a real ID
   collision the owner's local runner will hit), plus three lines of git markup that break any TSV parser and
   would visually confuse the owner reading the file directly. This is exactly the shared-file hazard CLAUDE.md's
   Collaborators section warns about ("fetch and rebase immediately before editing... when a row conflicts, keep
   both facts rather than overwriting") -- both facts needed to be kept, but as two well-formed rows, not as a
   raw conflict block. Fix (for the owning lane, not this QA session, per brief): renumber one row to L14,
   delete the three marker lines, keep both instruction rows.

2. **ciphers/clair349-este-guise-1556/NOTES.md:1-9** -- `tools/intake_gate_check.py clair349-este-guise-1556`
   returns exit 1 (`blocked`) right now, contradicting the file's own embedded transcript at lines 5-8 claiming
   `EXIT: 0`. YX-TR349 ran the gate check before editing the file (correctly, and it did pass then), pasted that
   output as the file's first content after the bare `partial` status word, and then did its own deep-work
   write-up in the same section -- which pushed the real citation sentence (still present, just further down,
   from YX-CS349's original check-solved pass) outside the tool's 6-line lookahead window. The regression is
   live and reproduces on this session's own re-run (see #2's row above); `tools/tests/test_intake_gate_check.py`
   now shows 1 failure for exactly this target, where QA run 3's own record (and YX-FIX's landing note) both
   describe it passing. Not a citation problem -- the underlying edition/page evidence this repo requires is
   still in the file -- but the Pipeline rule ("run `tools/intake_gate_check.py <target>` and paste its output
   before briefing any deep-work worker; a nonzero exit blocks the brief") means anyone briefing YX-TR349's own
   planned steps 2-3 next, or any other target-level lane orchestrator, who runs this mechanical check as
   instructed gets `blocked` for a target that in substance still qualifies `open`. Fix (for the owning lane):
   move the verdict-adjacent citation sentence (or a short restatement of it) back within 6 lines of the bare
   status word, or extend the tool's context window for a growing NOTES.md -- either fix, not this QA session's
   call, per brief.

Neither failure touches a counted board result: LOCAL-QUEUE.tsv is an offline-runner queue with no board effect
beyond corrupting one row pair, and clair349-este-guise-1556 has no reading, no AUDIT.md and is not on the board
(`partial`, cryptanalysis-pending). Both are process/tooling-integrity failures, flagged so a lane clears them
before either file is relied on again.

## Findings not tied to a single checklist row

None additional. The two QA run 3 flags (colbert26-lathuillerie-1644 Kind/canvas-21, thurloe-barriere-1654
single-pass negative) are both confirmed cleared this window (rows 6-7, 3-5 above) and drop off the running
total, per the brief's "a flagged item does not count toward any total until its lane clears it" rule.
