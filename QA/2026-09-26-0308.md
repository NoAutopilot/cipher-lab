# V8-QA6 rolling QA sweep

Window: 2026-09-26 01:42 UTC (V7-QA5's start, previous run's cutoff) to 2026-09-26 03:08 UTC (this session's
start). Source: ROOM.md lines 2449-2497, `git log` in that window, and the touched files. Previous run:
QA/2026-09-26-0142.md (2 findings, both routed to LANE B5 -- see item 16 below for follow-up). Disk + git log
only, no subagents, no network.

## Row-by-row

| Target / item | Check | Result | File:line |
|---|---|---|---|
| riksarkivet-r4282-1628 (bRIK) | 1. Intake gate: `open` names sources checked | mechanical PASS (exit 0); bRIK cloned dbourdeau/cyphersolver and aaymeloglu/unsolved-ciphers fresh and read Bourdeau's own NOTES.md in full itself (not re-citing another party's summary without opening it, the lope-hurtado failure mode from QA5) | ciphers/riksarkivet-r4282-1628/NOTES.md:1-14 |
| riksarkivet-r4282-1628 | 2. Deep work only after gate PASS | PASS -- crib test (cryptanalysis) ran in the same job as the check-solved verdict, the sanctioned breadth-lane pattern (CLAUDE.md 3a), after the verdict read `open` | same |
| riksarkivet-r4282-1628 | 3. Rule 3 negative w/ control, numbers correct | **finding** -- arithmetic error: ROOM.md:2454 and specs/riksarkivet-r4282-1628.json `cheap_test_done` both report the same-shape synthetic-key control mean as "6.3" for values {3,4,8} (actual mean 5.0); the target's own NOTES.md:91 has the correct "mean 5.0" for the identical three numbers. Verdict unaffected (target's 4 sits inside/below 3-8 either way) but two of the three authoritative records disagree with the third. flag: for LANE B6/whoever owns this target -- fix the mean in specs/riksarkivet-r4282-1628.json (ROOM.md itself is not edited after the fact) | ROOM.md:2454; specs/riksarkivet-r4282-1628.json cheap_test_done; ciphers/riksarkivet-r4282-1628/NOTES.md:91 |
| riksarkivet-r4282-1628 | 8. Stale/disagreeing status word | **finding** -- NOTES.md:1 (the rule-5 status line) reads `open` and is consistent with the crib test *not* beating its control (no near-solve per rule 5's amendment); but bRIK's own ROOM.md:2454 done line ("Status: partial (rule 5 amended)"), the spec's `cheap_test_done` verdict text ("not closed-negative (rule 5 as amended) -- partial"), and STATUS.md's LANE B5 handoff table (line ~298, "crib hypothesis excluded; partial; no NEAR row") all call it `partial` -- but rule 5's "partial" reclassification is paired with a NEAR.md row for a genuine near-solve, and the LANE B5 orchestrator correctly declined a NEAR.md row ("not near", ROOM.md:2465), so calling it `partial` anywhere is inconsistent with that same decision. NOTES.md's own line 1 is the one that stayed right; the prose elsewhere should not have said `partial`. No downstream action was taken on the mislabel (no NEAR.md row was actually added), so this is a wording inconsistency, not a broken gate. flag: for LANE B6 -- no file edit needed to NOTES.md, but STATUS.md's B5 handoff row overstates the target's status and could mislead a future reader into treating it as a near-solve | ciphers/riksarkivet-r4282-1628/NOTES.md:1; STATUS.md:298; ROOM.md:2454,2465 |
| thurloe-printed | 9. Live gate re-run vs. rule 5's bare-status-word convention | **finding** -- `tools/intake_gate_check.py thurloe-printed` returns `open (line 15)` and exits 0, but line 15 is `**Open, for the next owner (LANE W...` -- a subheading, not a rule-5 status word. Checked: thurloe-printed/NOTES.md has *no* line anywhere that is exactly `open`/`partial`/`solved`/`blocked`/`found-solved`/`closed-negative`/`offline-only` alone (grep for the exact rule-5 vocabulary on its own line: 0 hits), unlike every other pooled target touched this window (lodewijk-van-nassau-1573-74, pro3055-clinton-1779, kaliningrad-2015 all correctly carry a bare status word on line 1). The mechanical gate is passing this target by accident, matching prose that happens to start with the capitalized word "Open." Not previously flagged (checked QA/*.md). Low severity for now -- P25-P28's own N-class sits in AUDIT.md, not gated by this line -- but the folder's overall rule-5 status word is missing, and the live-gate check that's supposed to catch a citation gap here would not actually be exercised. flag: for the parent -- thurloe-printed/NOTES.md should get a real top-of-file rule-5 status word (its pooled items are individually classed in AUDIT.md; a folder-level word like the other pools have would both satisfy rule 5 and stop this false match) | ciphers/thurloe-printed/NOTES.md:1,15 |
| thurloe-printed P25-P28 | 17. status.json / NOTES.md / AUDIT.md agreement | PASS -- all three agree: N0 x4, key `period`, text known. AUDIT.md:870-873 gives the corrected pages (P25 vol.5 pp.100-101, P26 vol.3 pp.382-383, P27 vol.3 p.383, P28 vol.3 p.409); status.json's 26 Sept row (~line 1908) cites the identical four page references and the same C177/M63 count; NOTES.md s.23 (:2069-2076) carries the same corrections and sender/recipient fix. No stale copy found. | ciphers/thurloe-printed/AUDIT.md:855-925; status.json:1905-1914; ciphers/thurloe-printed/NOTES.md:2069-2076 |
| thurloe-printed P25-P28 | 5/10. Rule 10 wording + judge/count reproduction | PASS -- V7-TH25 reproduces C177/M63 exactly (AUDIT.md:924-925); no "new/first/unpublished/unread/never printed" language about our own work anywhere in the new AUDIT.md section (:855-945) or NOTES.md s.23; "misattributed headings" correctly attributed to this repo's own script trap, not a claim about the source | ciphers/thurloe-printed/AUDIT.md:855-945 |
| jan-van-nassau-1572-75 | 9. Live gate re-run | confirmed **exit 1** (`open`, no page/full-text citation within 6 lines) -- exactly as AX-MERGE self-flagged at 02:39 (ROOM.md:2487): the 5549 postscript is J5S-cited with pp.140-148 further down in the file, outside the gate's citation window. Not yet cleared by LANE AX orchestrator as of window's end (03:08); LANE AX's last check-in in this window was 02:26 with a next check-in at 03:05, so the fix is not yet due. No new failure -- the worker's own flag already covers this; not re-flagging | ciphers/jan-van-nassau-1572-75/NOTES.md:1 |
| lodewijk-van-nassau-1573-74 | 1/9. Intake gate live re-run | PASS (exit 0, `partial`) | ciphers/lodewijk-van-nassau-1573-74/NOTES.md:1 |
| lodewijk-van-nassau-1573-74 (AX-4612, done 02:12) | 3. Rule 3 negatives, numbers side by side | PASS -- H1/H2/H3 each report control range and target FAIL score side by side, plus real-letter positive controls (5811 0.871, 4610 0.948) and two further negative extension tests (digit-permutation 3/3, affine/shift/reversal 4/4); fr16 judge-cannot-decide caveat correctly flagged on the folder's own known-good readings (5811, 4610) rather than treated as a target failure | ROOM.md:2467 |
| lodewijk-van-nassau-1573-74 (AX-MERGE, AX-NAMES2, AX-GLOSS) | 5. Rule 10 wording | PASS -- "reading ready for re-derivation" (not yet claimed as a finished reading), no new/first/unpublished language; AX-GLOSS's one tentative find was itself withdrawn on a second blind-pass disagreement, correctly not committed | ROOM.md:2458,2473,2487 |
| lodewijk-van-nassau-1573-74 | 6. Transcription two-pass gate (AX-4612TR) | N/A this window -- only "blind pass A" for pages 1-2 committed (ab67f59, c5db8c0); no second pass yet, job's own 75-min box runs past this window's end; correctly not yet reported as a completed transcription | git log ab67f59, c5db8c0 |
| kaliningrad-2015 (GOLD-KAL3, GOLD-KAL4) | 3. Rule 3, numbers side by side | PASS -- every unit (restarts-20 re-runs and the 7-language convention-B sweep) reports control mean + seeds + target judge score + real_p05 together; CONTROL BELOW GATE units (lt-A, pt-B) correctly report the target as not run. Spot-checked arithmetic on 6 units (ru-s1s-A, ru-s3-B, pl-A, lt-A, and the fr/it/es sweep means): all reproduce to the stated precision from the per-seed numbers given | ROOM.md:2497; specs/kaliningrad-2015.json cheap_test_done (22 rows) |
| kaliningrad-2015 | 12. en-judge fold-spread caveat | PASS -- GOLD-KAL4's `en` sweep unit explicitly carries "the en judge corpus's own 0.44-0.64 fold spread makes this FAIL unknown-reliability per EN-FOLDS" | ROOM.md:2497 |
| kaliningrad-2015 | 5. Rule 10 | PASS -- no reading described, no judge PASS claimed, NOTES.md status stays `open` | ROOM.md:2497 |
| pro3055-clinton-1779 (AX-HMC, AX-HMC2) | 1/9. Intake gate | PASS (exit 0, `partial`) | ciphers/pro3055-clinton-1779/NOTES.md:1 |
| pro3055-clinton-1779 | 5. Rule 10 wording | PASS on inspection -- the one "new" hit (GBOOKS worker, "key never printed") is about a credential, not a plaintext claim; AX-HMC's and AX-HMC2's search-log prose ("not yet found", "discrepancy stays open") makes no first/new/unpublished claim about our own work | ciphers/pro3055-clinton-1779/NOTES.md:27 (grep hit, benign) |
| matignon-mayenne-1586 (bMAT2, bMAT3) | 3. Rule 3, numbers side by side | PASS -- bMAT2's unread-span re-judge (-1.483 vs controls -1.81/-1.83, cover 0.771 vs 0.66) and two-context M/U rule (S 41 vs control range 40-45) both give target/control side by side; bMAT3 correctly reports a provenance finding (no magenta ink on the native Gallica image) rather than a numeric test, and explicitly declines to commit an ambiguous sign-by-sign key rather than force a guess (rule 3/4 cited by name) | ROOM.md:2457,2484 |
| matignon-mayenne-1586 | 5. Rule 10 | PASS -- bMAT3 grades the Tomokiyo composite labels M throughout and credits Tomokiyo; no new/first language | ROOM.md:2484 |
| LANE B5's two QA5 check-solved gaps (matignon single-term IA search; lope-hurtado re-cited Bourdeau search) | 16. Follow-up after B5 closed | **answered** -- LANE B5's own handoff (STATUS.md line ~319, "(0) two check-solved workers first, for the V7-QA5 citation gaps") names both by target; LANE B6, opened immediately after B5 closed, claimed both as its first wave at 03:07 (ROOM.md:2495, "First wave: check-solved matignon-mayenne-1586 and lope-hurtado-1522"). No done line for either check-solved worker yet as of window's end (03:08) -- expected, B6 just opened. Not re-flagging; report only, per the brief | STATUS.md:319; ROOM.md:2489,2495 |
| Shared files | 7. Conflict markers | PASS -- none found repo-wide | -- |
| Shared files | 7. status.json / QUEUE-scores.json parse | PASS -- both valid JSON | -- |
| Shared files | 7. Duplicate IDs, LOCAL-QUEUE.tsv / JSTOR-QUEUE.tsv | PASS -- no duplicate `id` values in LOCAL-QUEUE.tsv, no duplicate rows in JSTOR-QUEUE.tsv | -- |
| NEAR.md / status.json | 8. near_check.py | PASS -- "9 NEAR.md rows, 9 status.json near entries, in step, none closed-negative, none stale" (matches `tools/room.py --start`'s corrected banner, confirming the QA5 room.py fix from 02:12 holds) | tools/near_check.py output |
| ROOM.md | 15. Role names since the 00:20 guard | PASS -- no line in the window (2449-2497) has a `--flag`-shaped role field | ROOM.md:2449-2497 |

## Informational (not a per-lane flag): commit-squash erases cited hashes

Confirmed a concrete recurrence of the already-documented, unfixed CLAUDE.md rule 6 lesson ("tools/room.py
--push's rebase can fold several sessions' commits into one generically-titled 'update' commit... erases the
per-session commit trail"). `git log` shows **zero** individual commits between 01:42:45 and 02:27:45 UTC in
this window, despite ~15 substantive ROOM.md done lines in that span (bMAT2, bRIK, bPOOL0, AX-NAMES2, AX-GLOSS,
AX-HMC, GOLD-KAL3, LANE B5/AX check-ins). All of that work landed in a single commit `000618a` ("update",
2026-09-26T02:27:45Z) confirmed via `git log --follow` on spans.tsv, riksarkivet's NOTES.md and
specs/cheap-tests/pools-b5/records.tsv (all three show `000618a` as their only commit). Worse than the
already-documented symptom: the *specific commit hashes* three workers cited in their own ROOM.md done lines
(bMAT2 "Pushed 370fcc3", bPOOL0 "Pushed e932e59", bRIK "commit 8f210c7") are not valid git objects anywhere in
this repository (`git cat-file -t <hash>` fails on all three) -- these citations are now permanently dangling,
not merely relabeled. This weakens rule 7's reproducibility promise (a verifier cannot check out the exact
commit a worker says it pushed) and partially defeats rule 6's own fallback advice (using push order in git log
when ROOM timestamps skew between sessions) for anything inside a squashed range, since the individual push
order is gone too. Not filing this as a new per-lane flag -- CLAUDE.md already records the general problem as
"flagged here, not fixed," and no lane can repair already-squashed history -- but surfacing the concrete,
now-broken-hash instance since the retrospective that logged the general issue said it "ran out of budget to
design the fix." flag: for the parent, informational only.

## Failures / findings requiring a flag

1. **riksarkivet-r4282-1628: control mean miscalculated (6.3 vs actual 5.0) in two of three records.**
   ROOM.md:2454 and specs/riksarkivet-r4282-1628.json's `cheap_test_done` both say the three same-shape
   synthetic-key controls {3, 4, 8} have "mean 6.3"; the correct mean is 5.0, which is what the target's own
   NOTES.md:91 already says. Doesn't change the verdict (target's 4 is inside/below 3-8 either way). flag: for
   LANE B6 (current owner of the target) -- fix the mean in specs/riksarkivet-r4282-1628.json.

2. **riksarkivet-r4282-1628: `partial` used inconsistently with the rule-5 near-solve amendment.**
   bRIK's own done line, the spec's verdict text, and STATUS.md's B5 handoff table all label the target
   `partial` after a crib-test negative that did *not* beat its control -- but rule 5's `partial` reclassification
   is meant for a near-solve that gets a NEAR.md row, and the LANE B5 orchestrator correctly declined one ("not
   near"). NOTES.md's own line 1 stayed `open`, which is right; the `partial` language elsewhere is not.
   No downstream action was taken on the mislabel. flag: for LANE B6 -- STATUS.md's closed B5 handoff row
   overstates this target's status for any future reader.

3. **thurloe-printed/NOTES.md has no rule-5 bare status word, and `tools/intake_gate_check.py` is passing it by
   accident.** The tool's "open (line 15)" result matches the word "Open" inside the subheading "**Open, for the
   next owner (LANE W...**", not a genuine rule-5 status line -- this folder has no line anywhere that is exactly
   one of the seven rule-5 words alone, unlike every other pooled target touched this window. Not previously
   flagged. flag: for the parent -- add a folder-level rule-5 status word to thurloe-printed/NOTES.md.

4. Informational, not a lane flag (see section above): commit-hash citations from three workers in the
   01:42-02:27 window (bMAT2, bPOOL0, bRIK) are dangling after a `tools/room.py --push` rebase squash -- a
   concrete recurrence of the already-documented, unfixed rule 6 lesson. flag: for the parent.

No other items failed. jan-van-nassau-1572-75's live-gate exit 1 is a real, currently-open gap but was already
self-flagged by its own worker (AX-MERGE, 02:39) and is not yet due for the orchestrator's fix -- not re-flagged.
LANE B5's two QA5 citation gaps (matignon, lope-hurtado) were answered by a handoff row and picked up as LANE
B6's first wave -- reported per item 16, not a new finding.
