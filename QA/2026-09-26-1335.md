# V9-QA11 rolling quality audit

Window: 11:27 UTC to 13:35 UTC, 26 Sept 2026 (previous run QA/2026-09-26-1127.md, 0 open flags).
Lanes live in window: LANE ARM2, LANE B11, LANE V9 (this audit); LANE B10/NX closed at window start;
parent 7g -> 7h handover at 13:05/13:07/13:10; parent workers SCOUT-OWN-3, EM3-CHECK, D1-CHECK, LEARN4, LEARN-8, RETRO-APPLY-S.

## Items checked

| # | Item | Target/File | Result | Notes |
|---|---|---|---|---|
| 46a | Morillo finer split in key/decode files | ciphers/rah-morillo-1817/{decode.json,key_5186.tsv} | INFORMATIONAL, not carried | `tools/decode_key.py --check` -> `C 91, U 6` (mechanical, no M grade). AUDIT.md's per-token grade is `C 86 / M 5 / U 6` (5 tokens where the gloss disagrees with the key at the letter level, rows r2g2/r4g2/r5g1/r5g2). decode.json's own note says "Grade C throughout" -- the tool has no M category, so the finer A/B group split (AUDIT.md lines 61-62) and the 5-token disagreement are not, and cannot yet be, represented in the mechanical decode output. Already surfaced by V9-QA10 (12:00 parent line) and by AUDIT.md itself ("of the key/decode files ... outside this verifier's brief"); not a new gap, not re-flagged. |
| 46b | status.json quotes AUDIT.md's class/key/text/print | status.json:1936-1946 | PASS | `"class": "N0"`, `"key": "period"`, `"text": "known"`, grade line quotes `C 86 / M 5 / U 6 of 97`, cites *Portuguesa en Carabobo* p.37 n.100 verbatim. |
| 46c | No rule-10 words about our own work | ciphers/rah-morillo-1817/{NOTES.md,AUDIT.md}, status.json | PASS | AUDIT.md explicitly states "no file calls the reading new, first or unread" and gives the unsafe sentence only as a **counter-example**. Instances of "unread"/"unpublished" found are legitimate uses (describing unread cipher *signs*, or item 2's un-printed archival key table, pre-dating this window) -- none claim novelty of our decipherment. |
| 47 | bMALX class-consistency TIE logged as non-discriminating, not support; no crib code entered a shared key | ciphers/malsburg-hessen-1636/HYPOTHESES.md:17-18, pool/pooled.tsv, keys/key_519.tsv | PASS | HYPOTHESES.md: "REAL overall class-agreement share: 0.812. SHUFFLE (N=1000) mean 0.780, p95 0.836. Real sits *inside* the shuffle p95 -- TIE, non-discriminating (CLAUDE.md rule 3), not a pass." Per-class breakdown also given (function word/noun/unclear), "No candidate WORD (only class) was evidence." `git log` on the bMALX commit (b91d2eec, 12:42) touches only cribs.tsv/HYPOTHESES.md/NOTES.md/transcription/crops -- `pool/pooled.tsv` and `keys/key_519.tsv` untouched. |
| 48a | bSALR: control/target side by side at measured error 0.064 | ciphers/fr2933-salviati-1525/HYPOTHESES.md:55-58 | PASS | Four rows (DSN-reg, DSN-reg shuffle floor, DSN2 V1, DSN2 V3), each with control accuracy (0.646/0.609/0.854/0.850) and target judge score, gate column "yes (gate 0.6)" confirming control-first ordering (rule 3 / family_run.py). |
| 48b | Error-bracket paragraph honoured | same | PASS | All four rows run at `err=0.064`, matching the target's own measured error (6.4%/sign) rather than a lower, easier level -- the exact fix the SALV-DIAG lesson (CLAUDE.md rule 3) calls for. |
| 48c | Salviati folder under 30MB with regen test | ciphers/fr2933-salviati-1525/ | PASS | `du -sb` = 27.87 MB (actual; `du -sh` rounds to 30M on block size). bSALS done line + NOTES.md record a byte-identical regen test (2 Gallica fetches) before deletion; glyphs/atlas_review left in place with the gap disclosed, not silently dropped. |
| 49 | EM3-CHECK found-solved names its source and grade M, not asserted as N0 | QUEUE.md "EM3 check-solved, 26 Sept 2026" | PASS | Names the exact source (EMLO Solr manifestation-level `bibo_Note`, specific uuids for Austen/Sedley) verbatim quoting "deciphered interline, and the key to the cipher used." Explicit: "Grade: M, not H -- this rests on EMLO's own cataloguer's description ... not grade H or C." Explicit: "This print-check gap means the rule-10 N-class is not settled by this pass" and "no novelty wording used (rule 10 -- N-class not assigned by this pass, that is a verifier's job)." No AUDIT.md exists for this item; no N0 claimed. |
| 50 | D1-CHECK closed notes-only, three marker phrases shown | ciphers/_triage/doria-donjuan-1568.md | PASS | All three markers quoted with surrounding snippet text (`cifra no descifrada` p.90; `cifra descifrada`, extended ~40 words across 3 scans; `cifra no adivinada`, a phrasing not previously recorded) -- each shows continuous plain prose, no digits. Verdict: "NO -- notes only ... Close D1." No `ciphers/` folder opened, filed under `_triage/` per brief. |
| 51 | Rule 6/clock: typed time contradicting own stamp | ROOM.md 13:07 (parent 7h) | SELF-CORRECTED, not re-flagged | Parent 7h's take-over line typed "at 13:11 UTC" against its own 13:07 stamp; the *next* line (same timestamp) is a self-correction: "the take-over time is this line's predecessor stamp (13:07 UTC, clock read), not 13:11 -- the typed figure was an estimate ... room.py's new typed-time WARNING caught it." Mechanism worked as intended within the same window; no outstanding contradiction. No other typed-time contradictions found in the window (13:03/13:04 and 13:29/13:30 pairs are ≤1 min, consistent with "checked in at X, logged at X+1", not a computed-clock error). |

## Core/hygiene checks (light pass, no new material warranting deep review beyond the above)

- Intake gate: no new `ciphers/` deep-work target opened in the window without a prior check-solved verdict. EM3/D1 correctly stayed as check-solved passes (QUEUE.md / `_triage/`), no folder promotion. ARM2's ARM-LIV/ARM-JEF/ARM-S3 are screening passes within the already-gated armstrong-madison-1808 target, not new targets.
- Rule 3: bMALX and bSALR negatives both carry target and control numbers together (see 47, 48a). ARM-LIV/ARM-JEF report "all clear text, no coded passage" as a search result, not framed as a control-backed negative -- correctly so (no cryptanalysis was run).
- No conflict markers (`<<<<<<<`/`=======`/`>>>>>>>`) in any tracked `.md`/`.tsv`/`.json`.
- `status.json` and `QUEUE-scores.json` both parse clean.
- No duplicate IDs in `LOCAL-QUEUE.tsv`/`JSTOR-QUEUE.tsv`.
- `tools/near_check.py`: 12/12 NEAR.md rows in step with status.json, none closed-negative, none stale.
- `tools/ledger_check.py`: duplicate session ids are all pre-existing legacy rows (line numbers <1000); none among this window's sessions (ARM2/B11/V9/parent workers, lines 500+).
- A large "update" merge commit (4e8acab, 13:05) initially looked like it touched ~200 unrelated `ciphers/*/NOTES.md` files per a naive `git log --name-only` since 11:27; checked directly and it in fact touches only `fr2933-salviati-1525/NOTES.md` (1 line) -- the apparent breadth was `e962e44` (LANE B11's own opening commit, which legitimately carries `.claude/briefs/` file additions) aggregating unrelated history via the known rebase-fold behaviour (CLAUDE.md rule 6, "flagged here, not fixed"). Informational only; no new conflict or content problem found.

## Failures

None. 0 new flags this window.

## Summary

6 items checked (46a/46b/46c/47/48a/48b/48c/49/50/51 -- 11 sub-checks across 6 numbered items) plus a light hygiene pass (conflict markers, JSON parse, ledger/near_check tools, intake gate, rule 3 framing). All PASS; one item (46a) is informational/already-known (Morillo grade-count split not mechanically representable, previously surfaced by V9-QA10); one item (51) shows the new room.py typed-time warning catching and the session self-correcting its own error within the same window, which is the mechanism working, not a failure. No ROOM `flag:` lines posted.
