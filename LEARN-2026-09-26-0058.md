# Cross-account learning pass 4, 26 Sept 2026, 00:58 UTC (parent worker LEARN-4, for parent 7e)

Window: 2026-09-25T21:34:00Z onward (this account's own named sessions from that window were all before
21:34 and already folded into CLAUDE.md -- EN-FOLDS, key livecheck -- so nothing new there). Read
`git log origin/main --since="2026-09-25T21:34:00Z" --stat` (69 commits after fetch), skipping this
account's own (parent 7d/7e handover, LANE V7/GOLD3/GOLD-K4/GOLD-CONS4, retrospective m). Read
LEARN-2026-09-25-2133.md and LEARN-2026-09-26-0022.md first (pass 3 covers the same window through 00:22;
this pass adds 00:22-00:58 and re-checks pass 3's two open items). Not applied; the parent applies.

## 1. Practices/lessons not yet in a shared file

| # | Practice / lesson | Evidence | Portable? | Diff |
|---|---|---|---|---|
| 1 | **A hard-EM cipher-to-plaintext alignment algorithm has now been hand-copied into a second target folder** despite a shared, more general version already sitting in `tools/`. AX-5799 (00:34 UTC) wrote `ciphers/lodewijk-van-nassau-1573-74/align/em_align_5799.py` (212 lines), whose own docstring says "Same algorithm as `ciphers/jan-van-nassau-1572-75/align/em_align.py`" (76 lines) -- both do dynamic-programming/hard-EM chunking of a cipher group to 0-3 letters of a printed span, iterated to convergence, emitting a value->meaning key with counts. `tools/interlinear_align.py` (301 lines) already does the same job (its own docstring: "aligns each pair by dynamic programming ... iterating so that a group's chunk agrees with what the same group reads elsewhere ... writes ... a value -> meaning key TSV with counts") for Thurloe's printed interlinear pairs. This is exactly CLAUDE.md Usage item 8 ("a target that needs something they lack gets an option added to the tool, not a private copy") not being followed a third time (the tool itself, then two per-target copies). | `ciphers/lodewijk-van-nassau-1573-74/align/em_align_5799.py:1-11` (its own "same algorithm as" line); `ciphers/jan-van-nassau-1572-75/align/em_align.py:1-7`; `tools/interlinear_align.py:1-17`. | Yes -- both target scripts take the same shape of input (a list of (group, printed-span) pairs) that `interlinear_align.py`'s `pairs` mode already produces; only the *source* of the pairs differs (Thurloe's printed interlinear line vs. a target's own manually-anchored spans TSV). | Add an `--align-from-tsv PATH` mode to `tools/interlinear_align.py`'s `align` subcommand that reads (group, span) pairs directly from a TSV instead of extracting them from djvu OCR, so a target with its own hand-built pairs file (like 5799's `RUN_PRINT_SPANS` list or jan-van-nassau's `passes/align_<n>.tsv`) calls the shared tool instead of re-deriving the DP/hard-EM loop. Add a `tools/tests/test_interlinear_align_tsv.py` case. Note for the next worker on this pool (a fourth Nassau letter is likely, per STATUS.md:405's "13 un-nominated" letters): point it at the shared tool, not at copying `em_align_5799.py` again. |
| 2 | **The "Cost and time" COMMON-file paragraph duplication (flagged twice already, LEARN-2118 and LEARN-0022) is not a README problem and is wider than one lane**: `.claude/briefs/README.md` has zero occurrences of the phrase, so there is nothing to fix there. But `grep -c "Cost and time"` across `.claude/briefs/runs/*-COMMON.md` finds the paragraph duplicated (2x) in **five of seven** lane COMMON files on this account (`-lane-cx-COMMON.md`, `-cx2-`, `-yx-`, `-zx-`, `-zx2-`) as well as the owner account's `-2026-09-26-lane-ax-COMMON.md`; only `-kx-` and `-vx-` have it once. So the bug is copy-forward within lane briefs on *both* accounts, not something either side can fix by editing a shared file once. | `grep -c "Cost and time" .claude/briefs/runs/*.md` at 00:57 UTC: cx/cx2/yx/zx/zx2/ax = 2 each, kx/vx = 1. | Yes, per CLAUDE.md Usage 8a (a rule broken twice gets a mechanical check): this is now confirmed broken in six files across two accounts, past the threshold LEARN-0022 already named. | Answers LEARN-0022's own open question directly: no README diff exists to write (the source text lives only in copied COMMON files, not the shared tail), so the fix is the mechanical check LEARN-0022 already specified (a duplicate-paragraph detector over `.claude/briefs/runs/*-COMMON.md`, run when a new dated COMMON file is committed) -- not a `.claude/briefs/README.md` edit. Recommend the parent apply LEARN-0022's tool diff rather than wait for a README fix that would not catch this. |
| 3 | **`tools/room.py --push`'s flag-validation gap (LEARN-0022) is still open** -- `grep -n 'startswith("-")' tools/room.py` shows the only such check is on `a[0]` (the leading CLI option), never on the path list passed to `--push`; the fix LEARN-0022 already wrote out (reject `paths` entries starting with `-`) is not on disk. RETRO-2026-09-26a.md's five proposals (being applied now by RETRO-APPLY-M) do not include it -- checked all five section headers, none mention `room.py --push` or path validation. | `tools/room.py:255` (only `a[0].startswith("-")`); `RETRO-2026-09-26a.md` section headers (1-5), no match for "room.py --push". | Yes, small and already fully specified. | No new diff needed; just flagging that this is an orphaned fix (not folded into RETRO-APPLY-M's queue) so it doesn't fall through twice more. Recommend a short follow-up worker apply LEARN-0022's exact diff after RETRO-APPLY-M finishes, since it touches the same file. |
| 4 | **A lane brief can carve out a partial scope when `intake_gate_check.py` blocks a target, rather than stopping the whole worker.** AX-5799's job brief let it proceed with pure known-plaintext alignment (grade C, no decode of unprinted ciphertext) while flagging the gate failure in ROOM.md and leaving the actual NOTES.md header fix to a sibling worker (AX-5797, which added the missing check-solved citation line within 6 lines of the status word). Both finished in the same 20-minute window with no idle time. | ROOM.md 2026-09-26 00:22 ("flag: intake_gate_check.py exits 1 on lodewijk-van-nassau-1573-74 header format (AX-5797 owns the fix); continuing only with steps 1-2 ... per job brief carve-out"); `ciphers/lodewijk-van-nassau-1573-74/NOTES.md` diff in commit 872b419. | Marginal -- useful pattern for a lane running several workers on sibling targets in the same folder at once, not a universal rule. | Not proposing a CLAUDE.md diff; noting it as a technique the parent can reuse when opening a multi-worker lane on one target folder (split "needs the gate fix" from "doesn't need it" up front rather than blocking the whole lane on one header edit). |

## 2. What we have that they lack

Checked LANE AX's four job briefs and its COMMON file against this account's `tools/family_run.py`,
`tools/near_check.py`, `tools/intake_gate_check.py` and NEAR.md discipline: no gap found this window --
AX's targets (5797, 5799, BRO3, STEV) are all key-recovery/attestation/verification jobs below the
family-run or NEAR.md thresholds (no cryptanalytic family run, no gate crossed). Same "no new gap" result
as pass 3. for the owner-account parent: nothing to flag this window.

## 3. Conflicts

None found. No rule disagreement surfaced between this window's owner-account commits and this account's
briefs.

## 4. Overlaps on targets

Confirmed no overlap, and already recorded independently by parent 7e itself: STATUS.md:507 ("The other
account's LANE AX ... holds the WVO Nassau pool, Brochado and pro3055; none of ours touch them"). This
pass's own git-log read agrees -- LANE AX's four commits this window (AX-5797, AX-5799, AX-BRO3, AX-STEV)
touch only `ciphers/lodewijk-van-nassau-1573-74/`, `ciphers/antt-msliv0638-brochado-1712/` and
`ciphers/pro3055-clinton-1779/`; none of this account's live lanes (V7, GOLD3, B5) claim those folders in
ROOM.md.

count: 1 clear tool-reuse diff (item 1, own numeric evidence: 76+212 duplicated lines vs. a 301-line shared
tool already doing the same DP/hard-EM job), 1 answered open question from LEARN-0022 (item 2: no README
fix exists, the mechanical-check diff is the right one), 1 orphaned-but-already-specified fix flagged
(item 3), 1 marginal technique noted (item 4), 0 reverse-direction gaps, 0 conflicts, 0 target overlaps.
