# RESCAN-2026-09-25: ASKS.md and LOCAL-QUEUE.tsv against the key probe

Written by parent worker KEYPROBE-TOOL (session_0186bWXk44p5CLJUgKi4kZay), owner-account parent,
per `.claude/briefs/runs/2026-09-25-parent-keyprobe-tool.md` item 4. Method: read every row of
ASKS.md (60 numbered rows) and every row of LOCAL-QUEUE.tsv (L1-L16), classify each against
KEYS-STATUS.md (`python3 tools/key_livecheck.py --force`, run 2026-09-25 23:02 UTC) and the Access
playbook. "Doable now" means the probe shows the specific credential the row is waiting on is
present and working, and the call needed is one this account's cloud container can already reach
(the Access playbook's own host table, not just the credential). Rows blocked by something other
than a credential (a paywall, a Cloudflare challenge, a payment, an archive's own permission gate)
are listed as needing a person regardless of key status.

Note on the filename: this brief's `tools/key_probe.py` collided on rebase with a same-named tool
parent 7d (the other account's parent) pushed minutes earlier for a different job -- name-presence
and cross-account sync (`--sync`, KEYS.md), never a network call. Rather than overwrite a tool
already wired into `tools/room.py --start`, `parent.md` duty 0a, ASKS.md and UPDATES.md, this
brief's live-call tool is committed as `tools/key_livecheck.py`; both are kept and both are wired
into `tools/room.py --start` (see UPDATES.md, this session's rows).

## 1. Doable now with an existing key (new, from this probe)

| Row | Target | What's owed | Key (probe result) | Call |
|---|---|---|---|---|
| ASKS #40 | huntington-blathwayt-madrid-1728 | Outreach gate 2(b): OpenAlex query `Pareti Marchmont 1728` and `Ripperda Segovia escape 1728`, previously logged as "OpenAlex answered 429" and pushed to the owner's machine | OPENALEX_KEY: present, working (HTTP 200, `Authorization: Bearer`, confirmed live 23:02 UTC -- the 429s in this row predate the key being wired everywhere) | `https://api.openalex.org/works?search=<query>` with the bearer header, exactly as `tools/print_check.py`/`tools/key_livecheck.py` already do |
| ASKS #41 | eckert-1864 (E4/E5) | Outreach gate 2(b): OpenAlex + Semantic Scholar queries `Fox Butler 1864 camels Tecumseh` and `Meigs Butler 1864 cavalry depot`, logged as "answered 429 every time" and pushed to the owner's machine | OPENALEX_KEY and S2_KEY: both present, both working (HTTP 200 live 23:02 UTC -- S2 needed a couple of retries after transient 429s from concurrent cloud sessions sharing its 1 req/s keyed pool) | same OpenAlex call as above, plus `https://api.semanticscholar.org/graph/v1/paper/search?query=<query>` with the `x-api-key` header (1 req/s) |

Neither row's JSTOR sub-item moves: JSTOR is Cloudflare-blocked from the cloud entirely regardless
of credentials (Access playbook host table), so JSTOR-QUEUE.tsv rows for both targets still need
the owner's machine (`tools/jstor_runner_brief.md`).

No other open ASKS.md row or queued LOCAL-QUEUE.tsv row is gated on a credential this probe shows
newly present or newly working: Google Books, Europeana, DPLA and CORE were already in active use
by print_check.py runs logged elsewhere the same day (row 47's own text), and IA's login was
already exercised live today by the IA-BORROW worker (see section 3) rather than sitting unused.

## 2. Needs a person (credential presence/works does not change this)

Grouped by why, not restated row by row where the reason is identical across a batch.

**JSTOR (Cloudflare blocks the cloud entirely, JSTOR_USER/JSTOR_PASS present but presence-only by
design -- probe confirms nothing changed here):** ASKS #17, #26 (part), #32, #33, #36, #37, #39,
#40 (part), #41 (part), #44; LOCAL-QUEUE L2; all of JSTOR-QUEUE.tsv's `queued` rows.

**Physical archive copy orders or payments (BL, TNA, NARA-adjacent, BnF, AGR, Nationaal Archief,
Marburg, Clements, Kew, NLS-style enquiries -- no credential exists for these, they are
person-and-money, not access):** ASKS #4, #6, #7, #14, #15, #16, #18 (part -- see below), #26,
#31, #35, #38, #43 (part b), #46, #48, #49, #52, #55 (part a), #56, #57, #58, #59 (part, if the
Brussels lead needs a reproduction order), #60.

**Sites Cloudflare/bot-blocks from the cloud regardless of any key (a different failure mode than
a missing credential -- these need a home browser, not a login):** LOCAL-QUEUE L3, L4, L5, L8, L9
(browser-check leg only; the API leg is reassigned, section 3), L10 (bportugal.pt confirmed 403
even via headless Chromium as of 25 Sept), L11, L12, L13 (British Newspaper Archive/newspapers.com
-- no BNA credential is named anywhere in the Access playbook), L14, L15, L16 (browser-check leg
only).

**Account-level permission gates, not a presence/works problem (the login itself works; a
different, higher permission is what's missing):** ASKS #42 and #43 (part) -- DECODE_USER/
DECODE_PASS are present and the browser login succeeds, but full-size images and non-image
documents are blocked account-wide even when logged in (confirmed from the owner's own browser
session too); the fix already in flight is the role-upgrade email (`outreach/decode-image-access.md`,
sent 24 Sept), not another login attempt. A repeat login would only reconfirm the same block and is
exactly what the probe's own presence-only-by-default rule for DECODE exists to prevent.

**Reading a held IA loan (login works, borrowing works, but the page image itself is served
obfuscated for scripts -- `tools/ia_borrow.py` already detects and refuses this, it is not a
credential gap):** ASKS #45 (academia.edu paywall, separately -- no credential exists for
academia.edu either), #55 (part b, Farago -- reassigned today, see section 3).

**Owner decisions, not access (a choice between routes, a waived gate, an environment-settings
click unrelated to a credential):** ASKS #7, #8, #9, #12 (part -- Ingram 1970 check still to run,
not a key issue), #13 (part), #28, #47 (DDB_API_KEY still not visible to this container as of the
22:48 UTC probe -- re-add/re-check the variable name; APE already dropped, no self-service key
exists), #50 (part -- reading a held loan, reassigned today), #53/#59/#60 (parts already answered
by today's IA-BORROW pass, remainder needs a person -- see section 3).

**Already closed, lapsed, dropped or waiting on an external reply (unaffected by this probe):**
ASKS #1 (DECODE login itself, distinct from #42's image block), #2, #3, #5, #10, #11, #19, #20,
#21, #22, #23, #24, #25, #29, #34, #42 (sent, reply pending); LOCAL-QUEUE L1, L6 (done/dropped
already).

## 3. Already reassigned today (before this probe ran -- listed so this rescan doesn't duplicate them)

- **IA-BORROW** (parent worker, session_01DzmCQYEVbNRHz3etXbHcrV, claimed 22:34 UTC): five IA
  lending-only page checks via `tools/ia_borrow.py` -- `correspondancede0006jose` (ASKS #59,
  Mercy 1648), `sim_cryptologia_1981-04_5_2` (ASKS #53, Koehler 1944 Kahn groups),
  `supplementaryrep0000grea` (ASKS #18 / LOCAL-QUEUE L7, Hamilton 1650), `edwardelgarmemor0000mrsr`
  (ASKS #50, Dorabella covering letter), and Farago's *Game of the Foxes* (ASKS #55 part b, Koehler
  1944). This exercised IA's live login as a side effect of the borrow itself -- consistent with
  this probe's own IA-login default (presence-only unless a worker already needs to borrow), so no
  separate `--ia-login` run was needed today.
- **GBOOKS** (parent worker, session_01XPZjPXaAssNyaoEDgSnLoN, claimed 22:40 UTC): LOCAL-QUEUE L9
  (fr16092-maisse-1582) and L16 (pro3055-clinton-1779) Google Books API re-checks with
  `country=US`, per the same cloud fix this probe's `test_google_books` call exercises.

## Summary

- Probe run 2026-09-25 23:02 UTC (`KEYS-STATUS.md`): 9 of 12 credential families present, 6 of 9
  testable ones live-confirmed working (Google Books, OpenAlex, Semantic Scholar, Europeana, DPLA,
  CORE); DECODE/JSTOR/IA presence-only by design; DDB and APE absent/not applicable.
- 2 ASKS.md rows (#40, #41) have a sub-item now doable from the cloud with a key the probe
  confirms working, where the row's own text still says "from your machine" from before the key
  was wired everywhere.
- 5 targets already had their IA/Google-Books work reassigned today by other parent workers before
  this probe ran; not duplicated here.
- Everything else open in ASKS.md and LOCAL-QUEUE.tsv is blocked by something a credential probe
  cannot fix: a Cloudflare/bot challenge with no credential route, a paywall with no repo
  credential, a physical archive copy order, an account-level permission gate distinct from login
  success, or an owner decision.
