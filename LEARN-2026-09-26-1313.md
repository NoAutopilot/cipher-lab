# Cross-account learning pass 8, 26 Sept 2026, 13:13 UTC (parent worker LEARN-8, for parent 7h)

Window: 2026-09-26T09:06:00Z onward, focused on 12:52-13:11Z (LEARN4, session_017LaWfuA5LcF7UaVNnpvxkq, already
covered 09:06-12:52 in depth; read first, not repeated here). Shallow clone deepened (`--deepen=400`, now at
ff156a33). Skipped this account's own sessions (parent 7g/7h, LANE V9, B9/B10/B11, ARM/ARM2, RETRO-APPLY-R/S,
retrospectives r/s, SALV-DIAG, LEARN-7, their workers). Read: owner-account LANE NX close (7704d66a), SCOUT-OWN-3
(41a67043/2e9b40d6), EM3-CHECK (5088f894), D1-CHECK (bf17b87c), and every owner-account ROOM.md line 08:43-12:45.

## 1. Practices/tools not yet in a shared file

| # | Practice | Evidence | Portable? | Diff |
|---|---|---|---|---|
| 1 | **EMLO manifestation-level Solr note as a check-solved source, stronger than the collection-title inference EM5 used.** EM3-CHECK queried `emlo.bodleian.ox.ac.uk/solr/all/select` directly for the per-letter `bibo_Note` field (not the finding-aid's volume title) and got a verbatim "deciphered interline, and the key to the cipher used" note for two specific letters, versus a third item in the *same* volume whose own note reads only "implicit cryptographic features" -- a per-item signal the volume-title method (EM5) cannot distinguish. Not in `.claude/briefs/check-solved.md` or CLAUDE.md's Access playbook table (grepped, no `emlo`/`solr` hit either file); the playbook has rows for many hosts but none for EMLO. | QUEUE.md:3723-3770 ("EM3 check-solved, 26 Sept 2026"); ROOM.md 12:56/12:58. | Yes -- reusable for every Bodleian/EMLO row (EM1/EM2/EM4/EM5 and any future EMLO harvest), and cheap (a plain GET, no login). | Add an EMLO row to CLAUDE.md's Access playbook host table: `emlo.bodleian.ox.ac.uk/solr/all/select?q=...` -- per-manifestation catalogue notes (`bibo_Note`), no auth, ~1.5s apart; and one sentence to `.claude/briefs/check-solved.md`'s source list: "for an EMLO-sourced item, query the manifestation's own Solr note before falling back to the collection-level finding-aid title -- the two can disagree within one volume (EM3 vs the Cardigan item, 26 Sept 2026)." |
| 2 | **`ciphers/_triage/<slug>.md` for a settle-only question that should never become a target folder** is already shared practice (both accounts use it: `2025-09-25-lane-yx-triage.md` is ours, `doria-donjuan-1568.md` is theirs) -- confirmed working as intended, not a gap. No diff needed. | ciphers/_triage/ (2 files, one per account). | N/A | none |

## 2. What we have that they lack (for the owner-account parent)

`tools/key_livecheck.py` (25 Sept 2026, "the first command of every parent check-in" per the Access playbook)
still does not appear in any owner-account ROOM.md line this whole window (checked every owner-account check-in:
08:43, 09:42, 10:43/10:44, 11:45, 11:54, 12:45 -- none mentions it, though "orphan check clean"/"Rate allowed
everywhere" appear every time, so `tools/orphan_check.py` is adopted). LEARN-7 (09:06 pass) already flagged this
exact gap five hours ago; it is unfixed as of the 12:45 check-in. For the owner-account parent: run
`python3 tools/key_livecheck.py` at check-ins per the Access playbook's own first line -- this is now a
twice-flagged, still-open gap, not a new one.

Separately confirmed as *already* adopted, so not a gap: the LIVE/ARCHIVED title rule (every owner-account ROOM
line reads "LIVE parent (owner account, session_...)"), and running `print_check.py` on decoded phrases before a
reading is called ready (V9-MOR's own N0 for rah-morillo-1817 item 3 came from `print_check.py`'s IA-global pass
on 3 decoded phrases, not a bare claim -- ROOM.md 11:37).

## 3. Conflicts

None found. NX's own close correctly absorbed our V9-MOR correction (Morillo item 3 key grade H -> C, "a key read
off a leaf's own decipherment is grade C, not H" ledgered as NX's own lesson, STATUS.md LANE NX handoff) -- this
originated on our side (rule 4) and was applied without dispute, not a live disagreement.

## 4. Overlaps on targets

No solver-role collision confirmed. NX's five targets (rah-morillo-1817, fr3625-lauriere-1593, esp318-sicilia-1503,
wvo-hessen-1564, fr3151-noailles-1558) and SCOUT-OWN-3/EM3-CHECK/D1-CHECK's leads (williams-1690 c.382,
doria-donjuan-1568 triage) are untouched by any of our live lanes (V9 solver-side, B9/B10/B11: malsburg-hessen-1636
+ fr2933-salviati-1525, ARM/ARM2: armstrong-madison-1808) -- our only touch on an NX target is V9's verifier pass
on Morillo item 3, which is cross-account-by-design (LANE V9 verifies for both accounts). SCOUT-OWN-3's editorial-
note phrase sweep (D1/D2/D3, EM3) and LEARN4's proposed diff to `scout.md`/`print_check.py` for it are still
unapplied on `main` as of this pass (no commit to either file since 12:54) -- the parent's job, not re-proposed
here to avoid a duplicate diff on top of LEARN4's.

count: 1 portable practice item (EMLO Solr note, check-solved.md + Access playbook diff), 1 already-shared
practice confirmed (no diff), 1 persisting tool-adoption gap re-flagged for the owner account (key_livecheck.py,
now twice-flagged), 0 conflicts, 0 target overlaps.
